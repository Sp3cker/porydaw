# Editcheck native-engine reachability (Task 15)

The three shared check files `src/checks/editcheck/tst_songdocument.h`, `tst_songdocument_support.cpp`, and `tst_songdocument_support.h` were removed. Before removal, a source-only search found only `tst_songdocument_support.cpp:1` including the support header; no surviving code included the shared check header or support header. After removal, the harness search for `tst_songdocument\.h|tst_songdocument_support` over `src/**/*.cpp`, `src/**/*.h`, `src/**/*.hpp`, `src/**/*.swift`, `src/**/*.qml`, and `src/**/CMakeLists.txt` returned **no matches**. Historical excerpts in proof certificates are not code includers. LSP references to `EditCheckTest`/`SyntheticDocument` still reported previously deleted originals (for example `tst_songdocument_metadata.cpp` and `tst_songdocument_runner.cpp`); these are stale index entries, not on-disk consumers. LSP diagnostics on the three removed file paths returned OK, but cannot establish a post-deletion parse of absent files.

The certificate tally below records the pre-cleanup snapshot. The five
wholly `MATCHED` song-document ledgers (songmoves, songnotes, songranges,
songraw, songtime) were subsequently retired under the approved policy;
their originals remain at Git revision
`4c52acd8c1193d27167c301002c67f18165938b8`. Mixed and active-native
inventories were not part of that cleanup.

## Certificate and build gates

Fresh `deno task proof list --area editcheck` parsed 12/12 proofs. All eleven song-document proofs (the seven originals retired by this plan plus the four pre-existing retired originals) are terminal: document 210 MATCHED + 40 RETIRED-REPRESENTATION; logic 28 + 32; metadata 108 + 17; runner 3 + 1; songmoves 138 MATCHED; songnotes 111; songranges 79; songraw 43; songtime 56; songtracks 87 + 2; timerange 192 + 1. `proof.tst_scale.txt` remains 24 GAP, not terminal; `src/checks/CMakeLists.txt:55-60` still lists `tst_scale.cpp`, `tst_scale.h`, and `porydaw_scale.cpp/.h`, and `src/checks/checkcatalog.cpp:113-115` still registers `scalecheck`. No active native or QML check was changed.

The fresh harness search for the **exact native candidate filenames** (`songdocument*.cpp/.h/.hpp` and `songhistory.cpp/.h`) in all five on-disk project `CMakeLists.txt` files — root, `src/checks`, and `src/swift/{core,app,playback}` — returned no matches. The similarly named `SongDocument.swift` and `SongHistory.swift` are separate active Swift sources (`src/swift/core/CMakeLists.txt:7-8`) and do not count as native candidate links. The following table's CMake column is thus PASS for every candidate, not evidence sufficient for deletion.

Proof abbreviations below mean `src/checks/editcheck/proof.tst_songdocument_<name>.txt`, whose `Covered native implementation:` line names the candidate (a wildcard `songdocument_timeeditor*.cpp` covers its three translation units). **T** means that proof is terminal per the tally above. `—` means no proof names that header; the universal proof gate is vacuously satisfied, not a claim that a proof covers the header.

| Exact `src/core/` candidate | CMake | Naming proof certificates | Remaining unretired source dependency / includer gate | Decision |
|---|---|---|---|---|
| `songdocument.cpp` | PASS (0) | document, metadata, runner, songmoves, songnotes, songranges, songraw, songtime, songtracks, timerange (all T) | BLOCKED: its own `songdocument.h:1` include and retained declarations in `songdocument.h:118-661` are consumed by dormant `src/project/projectworkspace.h:14` and `src/ui/songtab.h:11,132`; no translation unit directly includes the `.cpp`, but removing its method definitions strands the retained family. | KEEP |
| `songdocument.h` | PASS (0) | — | BLOCKED: `src/project/projectworkspace.h:14`, `src/ui/songview.h:22`, `src/ui/songtab.h:11`, and unretired `src/checks/midi/tst_midismf.cpp:16` directly include it; many further UI/check includers remain. | KEEP |
| `songdocument_range.cpp` | PASS (0) | songranges, songtime (T) | BLOCKED: includes `songdocument.h:1`; its `SongDocument::applyRangeEdit` / `moveRange` implementations (`:10,205`) are called from dormant `src/ui/songview/rangeedit.cpp:499,605,749`. No direct `.cpp` include. | KEEP |
| `songdocument_tempo.cpp` | PASS (0) | songtime (T) | BLOCKED: includes `songdocument.h:1`; dormant `src/ui/eventtablemodeledit.cpp:55` and `src/ui/editordrawer/nodelane/tempoadapter.cpp:58` call its `applyTempoEdit` (`:235`). No direct `.cpp` include. | KEEP |
| `songdocument_timeeditor.cpp` | PASS (0) | metadata, songtime, timerange (T, wildcard) | BLOCKED: includes `songdocument_timeeditor.hpp:1`, which includes retained `songdocument.h:7`; its time-edit entry points (`:548-559`) are called from dormant `src/ui/songview/rangeedit.cpp:291,626,665,683`. No direct `.cpp` include. | KEEP |
| `songdocument_timeeditor.hpp` | PASS (0) | — | BLOCKED: directly included by unretired `songdocument_timeeditor.cpp:1`, `songdocument_timeeditor_insert.cpp:1`, and `songdocument_timeeditor_xcmd.cpp:1`; includes `songdocument.h:7`. | KEEP |
| `songdocument_timeeditor_insert.cpp` | PASS (0) | metadata, songtime, timerange (T, wildcard) | BLOCKED: includes `songdocument_timeeditor.hpp:1`; provides `TimeEditor::insertBlank` and `duplicate` (`:14,110`) invoked by retained `songdocument_timeeditor.cpp:554,559` and hence dormant UI. No direct `.cpp` include. | KEEP |
| `songdocument_timeeditor_xcmd.cpp` | PASS (0) | metadata, songtime, timerange (T, wildcard) | BLOCKED: includes `songdocument_timeeditor.hpp:1`; supplies its XCMD reconciliation methods (e.g. `:35,113`) for the retained time-editor family. No direct `.cpp` include. | KEEP |
| `songdocument_xcmd.cpp` | PASS (0) | metadata, songranges, songtime (T) | BLOCKED: includes `songdocument.h:16`; defines `moveLanePoints` (`:230`), still called by dormant `src/ui/editordrawer/voicechangearea/voicechangearea.cpp:539` and `src/ui/songview/trackvoiceops.cpp:287`. No direct `.cpp` include. | KEEP |
| `songhistory.cpp` | PASS (0) | document (T) | BLOCKED: includes `songhistory.h:1`; implements `SongHistory::requestUndo` / `requestRedo` (`:203,214`), called by dormant `src/ui/workspaceui_voicegroup.cpp:73`, and `pushDocument` (`:280`), called by retained `songdocument.cpp:1371`. No direct `.cpp` include. | KEEP |
| `songhistory.h` | PASS (0) | — | BLOCKED: directly included by retained `songdocument.h:15`, dormant `src/ui/songtab.h:12`, and unretired `src/checks/project/identity.cpp:9` / `src/checks/voicegroup/tst_voicegroupviewcache.cpp:10`. | KEEP |

**Blocker chain and ruling.** `src/project/projectworkspace.h:13-14` retains `core/smf.h` and `core/songdocument.h`. That dormant, unretired production seam is itself included by `src/project/projectworkspace.cpp:1` and `src/project/projectio.h:12`; `src/ui/songtab.h:11-12,132` also owns a concrete native `SongDocument` with `SongHistory`, and the old widget UI calls the split `.cpp` implementation methods as shown above. The CMake absence does not retire those on-disk interfaces. No candidate translation unit is directly text-included as a `.cpp`; interpreting that fact in isolation as gate 3 passing and deleting implementations would strand their still-unretired headers/callers. Gate 3 therefore blocks the *connected native engine family*, including these translation units, under spec §8's explicit dormant-production rule. This is the expected blocker, not a workaround or an unexpected gate failure. The future decision is whether to retire/migrate the dormant `ProjectWorkspace` / widget UI and unretired native check originals of other areas, with their own proof and consumer audit; only then rerun all three gates per candidate and delete the newly unreferenced engine files. No production blocker, `smf.*`, `miditimeline.*`, `xcmd.*`, or other noncandidate was removed. Full build/verification/format/lint are deferred to the controller.
