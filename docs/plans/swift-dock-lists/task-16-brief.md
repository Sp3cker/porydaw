# Context

Reconcile the frozen 70-site `src/checks/voicegroup/proof.tst_voicegroupviewcache.txt` assertion inventory with the runnable SwiftCore viewcache/history parity delivered by task 15. The old no-host `VoicegroupViewCacheTest` is a historical source oracle, not a runnable Swift check; some pre-existing Swift assertions carry its `cppID` but do not cover all 70 sites. Task 17 consumes this completed correspondence and the task-15 runtime evidence before retiring `src/checks/voicegroup/tst_voicegroupviewcache.cpp`. Tasks 13/14 independently own the other proof packages.

# Exact write set

- `src/checks/voicegroup/proof.tst_voicegroupviewcache.txt` only.

No Swift/C++ source, check registration, fixture, other ledger, plan, or spec edit; do not delete the original source in this task. Task 17 alone owns source retirement. Keep this ledger and frozen fixtures after that deletion.

# Prerequisites

Task 15 must have delivered its complete 70-site runtime parity contract and a successful `deno task verify --filter swiftcore --verbose` run showing the viewcache/history predicates actually executed. Task 15 consumes task 12; neither a pre-existing `cppID` nor a registered-but-unexecuted method satisfies this prerequisite. Tasks 13/14 are not prerequisites for this disjoint ledger edit, but task 17 waits for all three proof gates.

# Interface contract

- Keep `Reference revision: a7fcaa3e9ea51a057c85bc1959e541c69fe4a3ea`, `Original: src/checks/voicegroup/tst_voicegroupviewcache.cpp`, and `Original SHA-256: d15a353f65745a70977fdbd756cd37e87e25d71940b5b6d5ac1267702e2dd693` verbatim. Keep A001–A070 exactly once, in original order, with original `VoicegroupViewCacheTest::<method>` identity, original file/line, expression, intervening source context, closing source sequence, and fixture/sequence/row constraints. Do not recompute the original hash from a changed proof file or replace source context with a count summary.
- Replace the stale `Swift counterpart: none identified` / `Swift SHA-256: n/a` metadata with the actual task-15 Swift check source path and its SHA-256. Record each additional mapping/check source as `Additional Swift counterpart: <path>` followed by its own `Swift SHA-256: <hash>` (the existing `proof.xcmd.txt` multi-source format). Include all task-15 assertion owners in `src/checks/workspace/bank_edits.swift`, `bank_history_probes.swift`, and `bank_sharing.swift` that actually support mapped sites, and `src/checks/workspace/SessionChecks.swift` if its dispatch is material to execution. Hash the landed files, not their earlier partial-`cppID` versions. A source with no site predicate cannot substitute for one.
- Each site's `Disposition` and `Mapping/reason` must describe its *own* observable assertion: the exact executing SwiftCore predicate/check identity from task 15, source file and assertion anchor, scenario/branch/row, and the observation equivalent to the original expression at that point in its sequence. The expected check identity is `voicegroupviewcachecheck/VoicegroupViewCacheTest::<method>` for the matching historical method; verify it in the actual check registration and runtime output. Method ranges below are navigation aids, never permission to mark a family `MATCHED` wholesale. `cppID` is an annotation, not an assertion, registration, or proof that the annotated predicate covers the site. A single Swift assertion may justify multiple sites only when its setup and observed result actually prove *each* specified condition; identify the condition separately in every mapping.
- Task 15's runnable predicate contract is: A001–A032 → `viewcacheHistoryLifecycleParity` in `bank_history_probes.swift` with `voicegroupviewcachecheck/VoicegroupViewCacheTest::historyLifecycleAndStaleTransitions`; A033–A039 → `viewcacheMergeRulesParity` in `bank_edits.swift` with `voicegroupviewcachecheck/VoicegroupViewCacheTest::mergeRules`; A040–A070 → `viewcacheCoordinatorRoutesParity` in `bank_sharing.swift` with `voicegroupviewcachecheck/VoicegroupViewCacheTest::coordinatorRoutesTransitionsAndGates`. `SessionChecks.swift` dispatches them through SwiftCore. These ranges are finding aids; verify the actual assertion and run per A-site before mapping.
- Rewrite stale status, verification, execution/ownership, and trailing `Swift assertion predicates` / `Shared helpers` statements that deny a Swift counterpart or claim an old `vgbankcheck` runtime lane. Name the task-15 SwiftCore invocation path and its actually executed predicates, original no-host FakeBank/CountCommand provenance, and the observed green task-15 run without claiming a live native C++ run. Preserve the historical source-context sections unchanged; distinguish original fixture/setup from the Swift fixture and do not attribute real-worker behavior to the fake or vice versa. If adding Swift `S###` assertion anchors, use only real source predicates and maintain a consistent cross-reference at each mapped A-site; do not invent anchors or infer coverage from counts.

# Implementation steps

1. Read task 15's landed registration/check assertions and green SwiftCore output first. Identify the actual runnable predicate(s), assertion locations, fixtures, setup and branch/row exercised for each of the three original methods. Compare the original expression and immediate source context of each A-site, not its method label or any existing `cppID`. Preserve the original shared source setup, four separate merge scopes, and identity/slot/sequence dependencies as historical context.
2. Reconcile A001–A032, `historyLifecycleAndStaleTransitions`, independently in source order. Check the initial valid identity and empty undo/redo state; document-command undo/redo values and current/saved document identities; scalar application, bank-entry depth/index and fresh inverse/redo drafts with their slot/value/expected fields; blank-slot materialization token, revert, rematerialization with a *new* token/header; stale undo and stale redo with bank rejection, exact entry removal/count, canUndo/canRedo, and preserved current/saved document identities. A check of the final voice alone cannot cover draft shape, token freshness, history depth, or unchanged document identity. The original script is sequential: do not conflate different slots or reuse a pre-transition draft as proof of a later step.
3. Reconcile A033–A039, `mergeRules`, against four distinct histories: two consecutive scalar edits coalesce to one entry whose inverse spans first→third; an A→B→A edit annihilates the entry; the five-command blank/different slot/different field/structural set stays unmerged; marking saved still permits the specified merge at A038, whereas `sealBankMerge()` prevents the subsequent merge at A039 and leaves two entries. Preserve the isolated-stack boundaries and the exact count/inverse predicates; a pan value, one undo, or a generic self-cancellation assertion does not by itself prove all seven sites. Task 15 addresses the Swift `SongHistory.markSaved` versus bank-merge-sealing discrepancy; only map A038/A039 after the landed runtime assertions demonstrate the distinct saved/explicit-seal outcomes, not from `cppID` or a pre-fix save assertion.
4. Reconcile A040–A070, `coordinatorRoutesTransitionsAndGates`, against the original two identities/two tabs and ordered Initial→Undo→Redo→conflict→hard-error→clear source script. Preserve its foreign-identity callback expressions and context as historical assertions, but map to the task-15 observable Swift analog: while the owning transition is held, a separate session/identity completes, conflicts, or fails and the owner remains pending, unchanged and gated; only the owner's result changes its history/view and releases its gate. Never state that a foreign callback was executed on the owning Swift session if that API does not exist. Check the empty cache/default action and close gates; dirty→clean view replacement; first begin admitted and overlapping begin rejected; pending origin and per-tab close/action gates; matching applied transition recorded and cleared with exact history count/index across undo/redo; initial conflict retaining index; stale undo and stale redo conflicts removing the appropriate entry and capability; owning hard error releasing the gate without consuming the retryable command; and closing/clearing with empty/default state. A one-session serialization assertion or coincidentally unchanged end state cannot cover identity isolation, tab-specific closing, error paths, or stack transitions. If a native site requires a stronger observation than this analog actually asserts, leave that site `GAP` and block task 17.
5. For each of the 70 sites, set `MATCHED` only when the task-15 executed Swift predicate establishes that exact observation. If genuinely missing or only related, leave `GAP` or `PARTIAL` with the precise unproved condition, site ID, and task-15 owner; do not label a product-behavior site `NATIVE` to bypass parity. Reconcile status/ownership/header/footer, any disposition tally, source SHA pins, all mapped Swift SHA pins, and the task-15 runtime command/result. Keep the original no-project-root/no-staged-fixture description as historical C++ provenance while stating the actual Swift setup separately. Any open GAP/PARTIAL blocks task 17, even when the structural proof command passes.

# Acceptance predicate

- The sole proof file retains all 70 original sites and original source provenance: 32 lifecycle/stale, 7 merge, 31 coordinator. All original expressions, file/line identities, contextual setup/loops/guards, source sequence, fixture/row constraints, historical revision and SHA remain intact. Each of the 70 sites, including identity/fixture setup sites, must have an individually explained executed Swift assertion equivalent for task-17 retirement. A genuinely unresolved site stays `GAP` or `PARTIAL` and blocks that retirement even if the structural command passes; no behavior-level `NATIVE` exception or count-only equivalence claim.
- The ledger names every actually supporting task-15 Swift source and its captured SHA-256, the registered SwiftCore check/predicate identity at each mapped site, and the task-15 observed successful runtime result. The green `deno task verify --filter swiftcore --verbose` from task 15 is a prerequisite, **not** a command to rerun or repair in this proof-only task. If that result is missing, or a required predicate is absent, stop and report the exact missing prerequisite/site to the task-15 owner instead of manufacturing parity.
- Run `deno task proof check` for this ledger change; it must pass with no grammar, provenance, unique-site, or tally error. This checks structure only: it does not execute Swift assertions, validate source freshness, or prove semantic parity. Do not run `vgbankcheck` as parity evidence.

# Task-specific constraints

- Do not alter task 15 checks to accommodate a proof mapping or rerun/fix their behavior here. Historical source is read-only and remains recoverable by reference revision/hash after task 17 deletes the C++ file; the proof ledger and frozen fixtures remain in the tree.
- Do not apply a single `MATCHED` disposition to a whole method on the strength of `cppID`, check registration, or related assertions. Every A-site must name a runnable predicate and the exact matching observed condition; missing behavior stays visible and blocks retirement.
