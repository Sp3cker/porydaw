
Shell interface (producer ShellLogic, consumer ShellSurface): @MainActor @QtBridgeable public final class ShellPresenter with @QtTracked public var session: ApplicationSession, initialized once; init(); @QtTracked public var actionIds: [String] (existing RewriteWindow menu actions plus grid actions, no fabricated unavailable feature); actionLabel(id: String)->String; actionSequences(id: String)->[String] (portable Qt sequences); actionEnabled(id: String)->Bool; activate(id: String); routeEditorKey(key: Int, modifiers: Int, autoRepeat: Bool)->Bool (executes once, returns consumed); beginClose()->Bool (true permits final window close); allTabsClosed(); closeCancelled(); sceneDestroyed(); public tracked closeReady Bool and sceneActive Bool; openStartup(); chooseProject(fileURL: String); chooseSong(label: String); public signals chooseProjectRequested(), chooseSongRequested(), quitRequested(). QML forwards session signals to shell and uses shell.session state for binding refresh. Shell logic owns action mapping and policy, QML delivery only. ShellSurface may coordinate precise additional lifecycle/error accessor needs with ShellLogic via hub before edits, Main must receive changes. No optional legacy fallback in production. Existing native fixtures may omit shell context only if their existing native routing is explicitly retained as oracle, not production fallback.

Coordinated additions: `windowActionIds` and `contextActionIds` expose Swift-owned delivery scope/menu subsets. ShellPresenter is QML-instantiated after QApp creates QGuiApplication; it is not constructed as a pre-application initial property. `songLabels: [String]`, `informationRequested`, `criticalRequested`, `openFailed(message:)`, `operationFailed(message:)`, and `saveStateChanged()` keep native dialog policy in Swift; QML forwards signals and renders dialogs. Empty-song text and error titles follow RewriteWindow exactly.

Appearance: ShellAppearance owns native preset palette and settings canonicalization. QML supplies actual QtCore.Settings values and applies Swift results. Explicit positive resolved font flows through reused QML; global QApplication font initialization is not assumed. Velocity-zero fill must consume the active palette role, as required by native note-rendering checks.

Application identity is executable startup policy: the small PorydawApplication.qml bootstrap sets porydaw/sp3cker/empty organization domain before loading ShellWindow. Swift cannot import noncopyable QCoreApplication, including its static metadata API; Qt.application is the existing public Qt access path. Reusable ShellWindow never changes the host identity. The shell check uses a fresh native application name under sp3cker, preserving genuine QtCore.Settings and the original private-fixture boundary. QML supplies the active application name to restoreAppearance; CFPreferences legacy-key removal follows it, so production remains com.sp3cker.porydaw.

Menu hints: actionShortcut(id: String) -> String returns the primary binding's Qt NativeText, separate from portable delivery sequences. Native menu-bar text carries the tab/NativeText suffix used by QCocoaMenuItem; the existing Cocoa ShortcutOverride/keyDown arbitration is preserved, without adding QQuickAction shortcut-map entries. The non-native context popup paints the same hint in a separate column. Only the former QSS-painted song dialog and context menu select theme-capable Controls locally; native folder/message dialogs and the application-wide style stay unchanged.

FolderDialog supplies selectedFolder.toString(); Foundation URL decodes its file URL to the native local path before opening the project. QML JavaScript has no browser URL constructor.

QtBridge publication constraint: QML-facing object references and action-ID arrays use stored @QtTracked vars, assigned during initialization. The installed macro does not publish object `let` fields or computed arrays. This is Qt-access glue, not mutable domain policy. `sceneDestroyed()` acknowledges Loader detachment and QML-context invalidation (including Component.onDestruction), not synchronous physical QObject deletion.
