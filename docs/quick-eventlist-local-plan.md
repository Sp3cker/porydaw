# Event List local implementation plan

Corrected quick-eventlist local plan (replaces agent://eventlist-local-plan findings bodies that still passed item maps to the menu; blueprint forbids nested QVariantMap command descriptions).

1) SCOPE / TARGETS (disjoint slices, no other files)
- Slice A controller+model: src/ui/eventtablemodel.h (+ role additions implemented in src/ui/eventtablemodel.cpp / src/ui/eventtablemodeledit.cpp, signatures of existing edit helpers unchanged), NEW src/ui/songview/quick/eventlistcontroller.h + eventlistcontroller.cpp (cohesive feature dir per repo rule; no new top-level src files, no moved model files).
- Slice B shared typed menu renderer: NEW src/ui/songview/quick/quickmenumodel.h + quickmenumodel.cpp, NEW src/ui/songview/quick/QuickMenuPanel.qml.
- Slice C page renderer: NEW src/ui/songview/quick/EventListPage.qml (editors inline in the page; no separate delegate widget file).
- Slice D integration: src/ui/songview.h/.cpp, src/ui/songview/editkeyrouting.cpp, src/ui/songview/quick/timelinequickview_keyrouting.cpp (+ existing TimelineInputItem), DELETE src/ui/eventlistview.h and the widget body of src/ui/eventlistview.cpp / src/ui/eventlistviewactions.cpp (CheckMenu, EventItemDelegate, QTableView/QComboBox/QMenu wiring). Keep eventtablemodel.* and eventtabletypes.* in place.
- Slice E checks: src/checks/eventviews/ (eventview_fixture.h/.cpp, chrome.cpp, edits.cpp, remap.cpp, playhead.cpp, tst_eventviews.h), selectionkey event-list local tier (src/checks/selectionkey/localinput.cpp), rollcheck-static gate. Exact verify filters: eventviews-chrome, eventviews-edits, eventviews-remap, eventviews-playhead, selectionkey-local-input, rollcheck-static; orchestrator runs `deno task verify --filter <each> --verbose`. Agents edit only: no builds/formatters/gates.

2) MODEL CONTRACT (src/ui/eventtablemodel.h) — header-shaped delta
- Real current state (verified 2026-09-06): NO roleNames() override, NO Q_INVOKABLE, NO Q_PROPERTY. Plain statics `static bool usesNumericFont(int)` and `static QList<std::pair<QString,int>> typeChoices(bool)`; const lookups return std::optional (`rawEventIndexForRow`, `tempoPointForRow`, `exactTickForRow`) or int (`rowForRawEventIndex`, `tempoRowForExactTick`, `playheadRowAtOrBeforeTick`); `uint8_t fallbackChannel() const`; tick commits in eventtablemodeledit.cpp via `value.toULongLong()` (handleRawTick/handleTempoTick/handleEndTick) — exact when the QVariant holds QString digits or qulonglong, lossy only if QML passes a JS Number.
- Rule: keep EVERY existing C++ signature untouched (checks + controller keep calling them). Add QML-edge surface only:
  `Q_OBJECT` stays; add `QHash<int,QByteArray> roleNames() const override;`
  `enum EventRoles { TickStringRole = Qt::UserRole+1, TickValueRole, TypeKindRole, TypeNameRole, ChannelRole, Data1Role, Data2Role, BlobRole, BlobDisplayRole, SummaryRole, RowKindRole };`
  data(): keep all existing Display/Edit/Font/Alignment/Background behavior; serve new roles read-only from the same RowKey switch (TickStringRole = QString::number(tick) decimal; TickValueRole = qulonglong; RowKindRole = 0 raw / 1 tempo / 2 EOT sentinel).
  Add QML-safe wrappers (Q_INVOKABLE, no std::optional, no QModelIndex, no pair-list):
  `Q_INVOKABLE int qmlRawIndexForRow(int row) const;` (-1 none)
  `Q_INVOKABLE bool qmlHasTempo(int row) const;`
  `Q_INVOKABLE QString qmlTickString(int row) const;` ("" none)
  `Q_INVOKABLE int qmlRowForRawIndex(qulonglong eventIndex) const;`
  `Q_INVOKABLE int qmlTempoRowForTick(const QString &tickDigits) const;`
  `Q_INVOKABLE static bool qmlUsesNumericFont(int column);`
  `setData(index, value, Qt::EditRole)` unchanged: QML tick editors MUST pass `TextInput.text` (QString) straight through; NEVER Number()/parseInt. Type choices for QML come from Slice B choice model, not typeChoices() (which stays C++-only).

3) CONTROLLER CONTRACT (NEW eventlistcontroller.h) — owns everything the widget owned
- `class EventListController : public QObject { Q_OBJECT`
  Properties (type + NOTIFY): `int chunk READ chunk NOTIFY chunkChanged; QStringList chunkLabels READ chunkLabels NOTIFY chunkLabelsChanged; int filterMask READ filterMask NOTIFY filterMaskChanged; QString filterSummary READ filterSummary NOTIFY filterSummaryChanged; QString countText READ countText NOTIFY countTextChanged; int currentRow READ currentRow NOTIFY currentRowChanged; int playRow READ playRow NOTIFY playRowChanged; bool followPlayhead READ followPlayhead WRITE setFollowPlayhead NOTIFY followPlayheadChanged; QList<double> columnWidths READ columnWidths NOTIFY columnWidthsChanged; QList<int> selectedRows READ selectedRows NOTIFY selectedRowsChanged;`
  Push API from SongView (plain methods, not QML): `setDocument(SongDocument*)`, `refresh()`, `syncTrackSelection()`, `onTracksRemapped(const TrackRemap&)`, `setPlayheadTick(double tick, bool playing)`.
  QML invokables: `Q_INVOKABLE void chunkPicked(int comboIndex); Q_INVOKABLE void filterToggled(int bit); Q_INVOKABLE void addEvent(); Q_INVOKABLE void insertCopyOfRow(int row); Q_INVOKABLE void deleteSelected(); Q_INVOKABLE void reorderRows(int fromRow, int toRow); Q_INVOKABLE void moveCurrentRow(int delta); Q_INVOKABLE qlonglong moveDestForRow(int row, int delta) const; Q_INVOKABLE void jumpCursorToRow(int row); Q_INVOKABLE bool commitCellEdit(int row, int column, const QString &text); Q_INVOKABLE void resizeColumn(int column, double width); Q_INVOKABLE QString rowTickString(int row) const;`
  Signals: `void chunkChanged(); void chunkLabelsChanged(); void filterMaskChanged(); void filterSummaryChanged(); void countTextChanged(); void currentRowChanged(); void playRowChanged(int row); void followPlayheadChanged(); void columnWidthsChanged(); void selectedRowsChanged(); void scrollToRow(int row); void announce(const QString &text); void revealVoice(int program);`
- Moved verbatim from eventlistview.cpp/actions: moveDestForRow same-tick + rawEventMoveBounds pinning + translatable why-strings via announce(); reorder clamp via SongDocument::rawEventMoveBounds; add/insert-copy tempo-vs-raw branches (chunk 0 current-row TempoPoint copy at edit cursor); deleteSelected raw-indices + TempoEdit batch via removeRawEventsAndEditTempo/deleteRawEvents/applyTempoEdit; refresh selection-restore + m_chunkRemapped/m_documentRevision remap path; updatePlayRow tint + follow incl. focused-sibling (current-row tick within 0.5) preference, no-scroll while mouse held or while page reports editing; fallbackChannel default; filter summary All events / No events / single / First +N; count "%n event(s)" vs "%1 of %2 events"; chunk labels "Chunk %1 — Track %2" / "Chunk %1 (tempo/meta)"; extended selection + currentRow owned in C++, never duplicated in QML. `commitCellEdit` converts per column then calls model setData (tick: pass QString through; channel/data: validated ints; type: kind int; blob: hex-or-quoted text) so the 64-bit path never sees a double.

4) TYPED MENU CONTRACT (Slice B) — no maps anywhere
- `struct QuickMenuItem { Q_GADGET; Q_PROPERTY(QString id MEMBER m_id); Q_PROPERTY(QString text MEMBER m_text); Q_PROPERTY(QString shortcutText MEMBER m_shortcutText); Q_PROPERTY(bool checkable MEMBER m_checkable); Q_PROPERTY(bool checked MEMBER m_checked); Q_PROPERTY(bool enabled MEMBER m_enabled); Q_PROPERTY(bool separator MEMBER m_separator); ... };` value type, explicit members. Submenus are NOT nested maps: a menu is a `QuickMenuModel : QAbstractListModel` with explicit roles `{ IdRole=User+1, TextRole, ShortcutRole, CheckableRole, CheckedRole, EnabledRole, SeparatorRole, HasSubmenuRole }` plus `Q_INVOKABLE QuickMenuModel *submenuForRow(int row) const;` Owners (controller) build three models: chunk selector (id "chunk:<n>", text = chunk label, radio-check current), filter model (7 category bits Notes/CC/Program/Bend/Aftertouch/SysEx/Meta, checked from filterMask; toggling emits toggled and does NOT close the panel), row menu (ids insert / show-voice / move-up / move-down / delete; show-voice only when current event typeKindOf == TypeProgram with program number; move enablement from moveDestForRow(row,±1); delete text plural "Delete %n event(s)" with deletable count, disabled at 0).
- `class QuickMenuHost : public QObject { Q_OBJECT; Q_PROPERTY(bool isOpen READ isOpen NOTIFY isOpenChanged); Q_INVOKABLE void open(QuickMenuModel *model, double sceneX, double sceneY); Q_INVOKABLE void close(); signals: void chosen(const QString &id); void toggled(const QString &id, bool checked); void cancelled(); void closed(); ... }` Session rule: clear session BEFORE emitting chosen/toggled (controller executes insert/move/delete/voice/chunk/filter after close); Escape/outside-press/focus-loss emits cancelled()+closed() and never chosen/toggled. Renderer owns layout/navigation/dismissal/shortcut display only; enablement and effects stay with the controller. Theme via existing appearance roles; no QtQuick.Controls styling fork.
- QML: QuickMenuPanel.qml (hosted popup: clamped+flipped position, hover, arrow/Enter/Escape traversal, checkmarks, submenu flyout, underlay outside-press). NO `open(QVariantList)` / `open(QVariantMap)` / `items:` list-of-maps property anywhere.

5) PAGE CONTRACT (Slice C: EventListPage.qml)
- QtQuick TableView (QtQuick import, not Controls) with model = EventTableModel, columnWidthProvider reading controller.columnWidths (initials font-derived 70/120/36/56/56/140, stretch-last preserved, interactive resize calls resizeColumn); delegates per column (mono table font via typography roles, right-align Tick/Ch/Data1/Data2, alternating backgrounds, playhead tint when row == playRow via RowKindRole/TickStringRole); EOT sentinel row rendered italic "End of track" with editable Tick; editors: Tick TextInput digits-only committing QString via commitCellEdit, Channel/Data numeric inputs, Type via QuickMenuHost choice model, Data hex-or-quoted validation; extended selection + currentRow two-way with controller; keyboard nav + local text clipboard/undo; timeline commands when not editing; pointer drag with drop indicator calling reorderRows (same legal-reorder authority as keyboard); vertical TimelineScrollbar reuse; toolbar objectNames eventListChunk/eventListFilter/eventListAdd/eventListRemove/eventListTable preserved as QML objectNames for the migrated checks; page occupies the roll-band rect with Roll suppression while visible.

6) INTEGRATION CONTRACT (Slice D)
- SongView keeps `eventListVisible()/setEventListVisible()/eventListVisibilityChanged(bool)`, document push (`setDocument/refresh/syncTrackSelection/setPlayheadTick/setFollowPlayhead`), `selectedTrackChanged + documentChanged/tracksRemapped` wiring, and `selectTrack/revealVoice/commitEditCursor/ensureTickVisible/announce` continuations; `m_events` becomes `EventListController*` (+ context property) with the Quick window owned per shell contract (no extra Quick window for the event list; no QWidget fallback).
- Key chain: keep `EditKeyOrigin::EventList` in handleEditKey (src/ui/songview/editkeyrouting.cpp); add TimelineInputItem `timelineEventListInput` + EventListInteraction joining the timelinequickview_keyrouting.cpp policy chain: ShortcutOverride Space-ignore, Delete/Backspace → deleteSelected, eventlist.move_up/down, selectionkey arbitration; timeline commands active only when the page is not editing.

7) 64-BIT TICK PROOF (Slice E: edits.cpp) — 3000000000 passes through a double exactly, so it proves nothing about JS >2^53. Allowed by the document (SongDocument::insertRawEvent/modifyRawEvent/setTrackEndTick take full uint64_t, no upper clamp; model commits via QString.toULongLong()): use tick 9007199254740993 (2^53+1, QStringLiteral("9007199254740993")): (a) model-level `setData(index, QString, EditRole)` asserts `track.events.back().tick == 9007199254740993ULL` + sorted + undo restore; (b) QML-level proof committing the same digits through the rendered Tick TextInput (not setData directly) asserting TickStringRole round-trips the exact digits (a Number round-trip would yield ...992). Keep the existing 3000000000 case; add actual cell-edit, column-resize, and drag-reorder interaction proof (not model-only).

8) CHECKS CONTRACT (Slice E detail)
- eventview_fixture.h EventWidgets resolves `{ EventListController *controller; EventTableModel *model; QQuickWindow *quickWindow; }` (no QTableView/QComboBox/QMenu); keep helper shapes `rowForTickAndType/selectChunk/trackIsSorted/chunkForTrack` repointed at controller+model. chrome: visibility/track-selection/rowMirror+EOT/monoTypography/viewState vs controller state. edits: tickEditQueued/tick64BitExact(high-bit)+type/channel/data/meta/tempo conversions via commitCellEdit/setData with undo. remap: notifyOrder/anchorFollowsMove via chunk anchor. playhead: tint/focus/sibling + follow-scroll via scrollToRow. selectionkey event-list tier drives keys through quickWindow into timelineEventListInput with identical outcomes. rollcheck-static gates on controller presence. Update host/scrollbar/workspace/mainwindow-routing eventListVisible touchpoints.

9) OWNERSHIP (no overlap): A controller+model, B menu renderer, C page renderer, D integration+deletions, E checks. B's QuickMenuModel/Host is built FOR these three first real consumers (chunk/filter/row) with only the enabled/checked/separator/shortcut/submenu behavior they need.

## Binding corrections by orchestrator

This plan consumes fork-main aad9fd4 (tooltip landed). Preserve all behavior, not a subset. No tests/lint/build/format by subagents. Use absolute worktree paths for every tool.

- Qt model columns are not separate rows. Expose display/edit/font/alignment/row-kind roles only where consumed; do not invent redundant qml forwarding wrappers. Keep ticks strings across QML.
- QuickMenuItem is a typed C++ value with int id, QString text/shortcutText, bool enabled/checkable/checked/separator/stayOpen and vector<QuickMenuItem> children. QuickMenuModel owns typed rows, explicit roles and child models; setItems(vector<QuickMenuItem>), signal activated(int id). QuickMenuHost opens a QuickMenuModel at scene QPointF, publishes open/root/current state and cancels on invalid owner. Clear before ordinary activation, NOT persistent filter toggles: stayOpen toggles keep the session and emit model activated(id). Model rebuild while toggling must preserve valid session/navigation. QML never receives nested command maps.
- EventListController constructor SongView*, optional QObject parent; model() accessor; selectRow(int row,int modifiers), selectAll(), selectedRows(), currentRow(), chunkPicked(int), filterToggled(int), commitCellEdit(int row,int column,const QString&), resizeColumn(int,double), reorderRows(int,int), setEditing(bool), setPointerDown(bool), setVisible(bool), handleKey(QKeyEvent*) and openChunkMenu/openFilterMenu/openRowMenu/openTypeMenu with QPointF scene coordinates. Properties model, appearance, chunk, chunkLabels, filterMask/filterSummary, countText,currentRow,playRow,selectedRows,columnWidths,visible,editing. Use QVector/QList variants consistently with existing Qt patterns; controller.h is the published authority.
- Integration owns TimelineCanvas.qml, timelinequickview.h/.cpp and keyrouting, SongView callers/deletions and CMake registrations. Menu agent does not edit those files. Page agent owns only EventListPage.qml. Checks agent owns checks only.
- Before deleting old eventlistview files, integration confirms controller agent finished reading them; main-checkout originals remain available read-only.
- Real input proof must exercise cell edits, selection keys, column resize and pointer reorder. No source-shape tests or replacement plumbing assertions.
