Assertion correspondence: voicegroupsave/switching.cpp

Reference revision: a7fcaa3e9ea51a057c85bc1959e541c69fe4a3ea
Original: src/checks/voicegroupsave/switching.cpp
Original SHA-256: f4a4c944efbb52d6f293323025ad3aba5a6c11bf586b10ebcb0ed3eb1290aa8b
Swift counterpart: src/checks/workspace/bank_switching.swift


Command: deno task verify --filter swiftcore --verbose.
Result: PASS for the Swift session predicates; the retained native harness
is not Swift parity evidence.

A001 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:26
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
#include "checks/voicegroupsave/tst_voicegroupsave.h"

#include <QComboBox>
#include <QtTest>

#include "checks/support/songfixture.h"
#include "checks/support/voicegroupbrowserdriver.h"
#include "core/songdocument.h"
#include "mainwindow.h"
#include "ui/songtab.h"
#include "ui/workspaceui.h"

namespace checks {
namespace {

int adjacentRelease(int value)
{
    return value < 255 ? value + 1 : value - 1;
}

} // namespace

void VoicegroupSaveTest::switchCarriesUnsavedBankEdit()
{
    const QString other = otherVoicegroupArg();

Original expression:
QVERIFY2(m_window->m_workspace->projectState().catalog.groupArgs.size() >= 2 &&
                 !other.isEmpty(),
             "required staged voicegroups missing: >=2 groupArgs via sound/voice_groups.inc and "
             "sound/voicegroups/fixture_alt.inc")

A002 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:34
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QString homeArg = m_document->cfg().voicegroupArg;
    VgVoice edited = m_originalVoice;
    edited.release = adjacentRelease(edited.release);
    m_browser->submitPickerEdit(m_dsSlot, edited);

Original expression:
QVERIFY2(waitForBankRelease(m_dsSlot, edited.release, true),
             "worker did not apply setup voice edit")

A003 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:40
Disposition: MATCHED
Mapping: S001

A004 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:47
Disposition: MATCHED
Mapping: S002

A005 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:48
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(settle([this, &edited] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 const LoadedVoiceGroup *const engine = m_window->m_audio.voicegroup();
                 return bank && bank->loadName == m_loadName && bank->dirty &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == edited.release && engine &&
                        engine->voices[m_dsSlot].release == edited.release;
             }),
             "undo restored cfg but did not replay the canonical unsaved bank edit")

A006 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:58
Disposition: MATCHED
Mapping: S005

A007 | VoicegroupSaveTest::switchCarriesUnsavedBankEdit | src/checks/voicegroupsave/switching.cpp:63
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QCOMPARE(readFileBytes(m_voicegroupPath), m_voicegroupBytes)

A008 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:69
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
}

void VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit()
{
    const QString other = otherVoicegroupArg();

Original expression:
QVERIFY2(m_window->m_workspace->projectState().catalog.groupArgs.size() >= 2 &&
                 !other.isEmpty(),
             "required staged voicegroups missing: >=2 groupArgs via sound/voice_groups.inc and "
             "sound/voicegroups/fixture_alt.inc")

A009 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:75
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QString homeArg = m_document->cfg().voicegroupArg;
    QComboBox *const selector = m_browser->voicegroupSelector();

Original expression:
QVERIFY(selector)

A010 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:77
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QString shownHome = SongRegistry::voicegroupDisplayName(homeArg);

Original expression:
QCOMPARE(selector->currentText(), shownHome)

A011 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:78
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY(selector->findText(SongRegistry::voicegroupDisplayName(other)) >= 0)

A012 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:81
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

    selector->setCurrentText(SongRegistry::voicegroupDisplayName(other));

Original expression:
QVERIFY(QMetaObject::invokeMethod(selector, "activated", Qt::DirectConnection, Q_ARG(int, 0)))

A013 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:82
Disposition: MATCHED
Mapping: S001

A014 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:88
Disposition: MATCHED
Mapping: S002

A015 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:89
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY(!m_document->isDirty())

A016 | VoicegroupSaveTest::selectorSwitchUsesUndoableCfgEdit | src/checks/voicegroupsave/switching.cpp:90
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QTRY_COMPARE(selector->currentText(), shownHome)

A017 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:96
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
}

void VoicegroupSaveTest::valueCommandSurvivesSourceReplacement()
{
    const QString other = otherVoicegroupArg();

Original expression:
QVERIFY2(m_window->m_workspace->projectState().catalog.groupArgs.size() >= 2 &&
                 !other.isEmpty(),
             "required staged voicegroups missing: >=2 groupArgs via sound/voice_groups.inc and "
             "sound/voicegroups/fixture_alt.inc")

A018 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:123
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QString homeArg = m_document->cfg().voicegroupArg;
    const auto reopenCleanHome = [this, &other, &homeArg] {
        const QByteArray disk = readFileBytes(m_voicegroupPath);
        SongCfg cfg = m_document->cfg();
        cfg.voicegroupArg = other;
        m_document->setCfg(cfg);
        if (!settle([this, &other] {
                return m_document->cfg().voicegroupArg == other && m_tab->voicegroupId() &&
                       *m_tab->voicegroupId() != *m_homeId;
            }))
            return false;
        requestUndo();
        return settle([this, &homeArg, &disk] {
            const LoadedBankView *const bank = m_browser->selectedBankView();
            return m_document->cfg().voicegroupArg == homeArg && m_tab->voicegroupId() &&
                   *m_tab->voicegroupId() == *m_homeId && bank && bank->loadName == m_loadName &&
                   !bank->dirty && readFileBytes(m_voicegroupPath) == disk;
        });
    };

    VgVoice edited = m_originalVoice;
    edited.release = adjacentRelease(edited.release);
    m_browser->submitPickerEdit(m_dsSlot, edited);

Original expression:
QVERIFY2(waitForBankRelease(m_dsSlot, edited.release, true), "value command did not apply")

A019 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:125
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const int firstSave = m_savedReceipts;

Original expression:
QVERIFY(saveSelectedSong())

A020 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:126
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(waitForCleanSave(firstSave), "value command save did not settle")

A021 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:127
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(reopenCleanHome(), "clean -G round trip did not replace the home source")

A022 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:131
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QByteArray refreshed = readFileBytes(m_voicegroupPath);

    requestUndo();

Original expression:
QVERIFY2(settle([this, &refreshed] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && bank->dirty && !m_document->isDirty() &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == m_originalVoice.release &&
                        readFileBytes(m_voicegroupPath) == refreshed;
             }),
             "executed value undo after rebind did not resolve current bytes")

A023 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:140
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    requestRedo();

Original expression:
QVERIFY2(settle([this, &edited, &refreshed] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && !bank->dirty && !m_document->isDirty() &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == edited.release &&
                        readFileBytes(m_voicegroupPath) == refreshed;
             }),
             "executed value redo after rebind did not resolve current bytes")

A024 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:150
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

    requestUndo();

Original expression:
QVERIFY2(settle([this] {
                 return m_browser->selectedBankView() && m_browser->selectedBankView()->dirty;
             }),
             "undo before redo-tail refresh did not apply")

A025 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:155
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const int restoreSave = m_savedReceipts;

Original expression:
QVERIFY(saveSelectedSong())

A026 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:156
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(waitForCleanSave(restoreSave), "baseline restoration save did not settle")

A027 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:157
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QCOMPARE(readFileBytes(m_voicegroupPath), m_voicegroupBytes)

A028 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:159
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    requestRedo();

Original expression:
QVERIFY2(settle([this, &edited] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && bank->dirty && !m_document->isDirty() &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == edited.release &&
                        readFileBytes(m_voicegroupPath) == m_voicegroupBytes;
             }),
             "redo-tail command did not reapply to refreshed canonical bytes")

A029 | VoicegroupSaveTest::valueCommandSurvivesSourceReplacement | src/checks/voicegroupsave/switching.cpp:168
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    requestUndo();

Original expression:
QVERIFY2(settle([this] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && !bank->dirty && !m_document->isDirty() &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == m_originalVoice.release;
             }),
             "redo-tail undo did not restore clean baseline")

A030 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:180
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
}

void VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement()
{
    const QString other = otherVoicegroupArg();

Original expression:
QVERIFY2(m_window->m_workspace->projectState().catalog.groupArgs.size() >= 2 &&
                 !other.isEmpty(),
             "required staged voicegroups missing: >=2 groupArgs via sound/voice_groups.inc and "
             "sound/voicegroups/fixture_alt.inc")

A031 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:185
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const int blank = firstBlankSlot();

Original expression:
QVERIFY2(blank >= 0, "fixture must provide a blank slot for structural-token rebasing")

A032 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:204
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QString homeArg = m_document->cfg().voicegroupArg;
    const auto reopenCleanHome = [this, &other, &homeArg] {
        SongCfg cfg = m_document->cfg();
        cfg.voicegroupArg = other;
        m_document->setCfg(cfg);
        if (!settle([this, &other] {
                return m_document->cfg().voicegroupArg == other && m_tab->voicegroupId() &&
                       *m_tab->voicegroupId() != *m_homeId;
            }))
            return false;
        requestUndo();
        return waitForVoicegroup(homeArg, *m_homeId) && settle([this] {
                   return m_browser->selectedBankView() && !m_browser->selectedBankView()->dirty;
               });
    };

    const VgVoice materialized = m_originalVoice;
    m_browser->submitPickerEdit(blank, materialized);

Original expression:
QVERIFY2(settle([this, blank, &materialized] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && bank->dirty && bank->slotViews.at(blank).voice &&
                        *bank->slotViews.at(blank).voice == materialized;
             }),
             "blank slot did not materialize under a reversible token")

A033 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:211
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const int saveBefore = m_savedReceipts;

Original expression:
QVERIFY(saveSelectedSong())

A034 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:212
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(waitForCleanSave(saveBefore), "materialized blank did not save")

A035 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:218
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

    VoicegroupSource sibling;
    QString error;
    VgVoice unrelated = m_originalVoice;
    unrelated.release = adjacentRelease(unrelated.release);

Original expression:
QVERIFY2(sibling.open(m_project->root(), homeArg, &error), qPrintable(error))

A036 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:219
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(sibling.setVoice(m_dsSlot, unrelated), "could not update sibling source slot")

A037 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:220
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(sibling.save(&error), qPrintable(error))

A038 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:221
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(reopenCleanHome(), "external source refresh did not rebind cleanly")

A039 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:223
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const QByteArray refreshed = readFileBytes(m_voicegroupPath);

Original expression:
QVERIFY2(settle([this, blank, &unrelated] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && bank->slotViews.at(blank).voice &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == unrelated.release;
             }),
             "reopened source did not contain structural and unrelated edits")

A040 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:232
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

    requestUndo();

Original expression:
QVERIFY2(settle([this, blank, &unrelated, &refreshed] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && !bank->slotViews.at(blank).voice &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == unrelated.release &&
                        bank->dirty && readFileBytes(m_voicegroupPath) == refreshed;
             }),
             "blank undo restored stale file bytes instead of rebasing its token")

A041 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:241
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    requestRedo();

Original expression:
QVERIFY2(settle([this, blank, &materialized, &unrelated, &refreshed] {
                 const LoadedBankView *const bank = m_browser->selectedBankView();
                 return bank && bank->slotViews.at(blank).voice &&
                        *bank->slotViews.at(blank).voice == materialized &&
                        bank->slotViews.at(m_dsSlot).voice &&
                        bank->slotViews.at(m_dsSlot).voice->release == unrelated.release &&
                        !bank->dirty && readFileBytes(m_voicegroupPath) == refreshed;
             }),
             "blank redo did not preserve unrelated refreshed bytes")

A042 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:252
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

    requestUndo();

Original expression:
QVERIFY2(settle([this, blank] {
                 return m_browser->selectedBankView() &&
                        !m_browser->selectedBankView()->slotViews.at(blank).voice;
             }),
             "cleanup blank undo did not apply")

A043 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:258
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    m_browser->submitPickerEdit(m_dsSlot, m_originalVoice);

Original expression:
QVERIFY2(waitForBankRelease(m_dsSlot, m_originalVoice.release, true),
             "restoring unrelated slot did not settle")

A044 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:261
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;
    const int finalSave = m_savedReceipts;

Original expression:
QVERIFY(saveSelectedSong())

A045 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:262
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QVERIFY2(waitForCleanSave(finalSave), "cleanup source save did not settle")

A046 | VoicegroupSaveTest::blankTokenRebasesAcrossSourceReplacement | src/checks/voicegroupsave/switching.cpp:263
Disposition: GAP
Mapping/reason: No Swift assertion establishes this site's original condition and setup; the predicate remains open.
Source context since previous assertion:
;

Original expression:
QCOMPARE(readFileBytes(m_voicegroupPath), m_voicegroupBytes)

Remaining original source (closing sequence and helper bodies):
;
}

} // namespace checks


Swift assertion predicates

S001 | bankSwitchingParity | src/checks/workspace/bank_switching.swift
Anchor: message "selector binds the alternate source and makes -G undoable"

S002 | bankSwitchingParity | src/checks/workspace/bank_switching.swift
Anchor: message "undo of -G rebinds the unsaved home voicegroup"

S003 | bankSwitchingParity | src/checks/workspace/bank_switching.swift
Anchor: message "redo of -G rebinds the alternate bank"

S004 | bankSwitchingParity | src/checks/workspace/bank_switching.swift
Anchor: message "failed source load does not change the lease or history"

S005 | bankSwitchingParity | src/checks/workspace/bank_switching.swift
Anchor: message "save of song and edited home bank refreshes the clean lease"
