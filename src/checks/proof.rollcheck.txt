Assertion correspondence: rollcheck.cpp

Reference revision: a1244957bb05a59d62b8912e053d49b2a6f3d771
Original: src/checks/rollcheck.cpp
Original SHA-256: 4bbd205e3a1f4762746d34d9fe0fc9feb2d898b12fb80c9c1e7534a0c79c9d6b
Swift counterpart: none identified
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

PianoRollTest::init builds the shared native suite fixture (project copy, song load, voicegroup bank lease, PianoRollFixture) and every site below is one of its setup guards; runRollCheck dispatches the slots through QTest::qExec. No counterpart executes suite-entry setup, so all seven sites are retained native setup obligations.

Command: deno task verify --filter swiftrollgated --verbose
Result: pending integrated run.

PianoRollTest::init/cleanup run around every PianoRollTest slot; runRollCheck (bottom of this file) executes the suite through QTest::qExec. Seeds and the fixture shell live in src/checks/rollcheck/harness.cpp, fixtures.h and fixtures.cpp, with slot declarations in tst_pianoroll.h. No counterpart file carries predicates for suite-entry setup.

A001 | PianoRollTest::init | src/checks/rollcheck.cpp:27
Disposition: RETIRED-REPRESENTATION

A002 | PianoRollTest::init | src/checks/rollcheck.cpp:31
Disposition: RETIRED-REPRESENTATION

A003 | PianoRollTest::init | src/checks/rollcheck.cpp:34
Disposition: RETIRED-REPRESENTATION

A004 | PianoRollTest::init | src/checks/rollcheck.cpp:45
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck.cpp) no longer builds or runs. Previous native note: Original native fixture/loader setup obligation. A presenter-level Swift check cannot establish this precondition.
Source context since previous assertion:
      m_bank = std::make_unique<LoadedVoiceGroup>();
      m_bank->voices[0].type = VOICE_DIRECTSOUND;
      m_bank->voices[1].type = VOICE_SQUARE_1;
      m_bank->voices[2].type = VOICE_PROGRAMMABLE_WAVE;
      m_bank->voices[3].type = VOICE_NOISE;

      m_tab = std::make_unique<SongTab>(std::move(*name));
      m_tab->setSampleRate(48000.0);
      m_tab->applyMidiStage(loaded->songInfo(), loaded->document().smf(),
                            track_limits::kHardwareCapacity);
Original expression:
  QVERIFY2(m_tab->presentationError().isEmpty(), qPrintable(m_tab->presentationError()));

A005 | PianoRollTest::init | src/checks/rollcheck.cpp:49
Disposition: RETIRED-REPRESENTATION

A006 | PianoRollTest::init | src/checks/rollcheck.cpp:53
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck.cpp) no longer builds or runs. Previous native note: Original native fixture/loader setup obligation. A presenter-level Swift check cannot establish this precondition.
Source context since previous assertion:
      m_tab->applyBankView(
          LoadedBankView{*bankId, borrowVoicegroupLease(m_bank.get()), QString(), false, {}});
      m_tab->applyVoicegroupBound(*bankId);
Original expression:
  QTRY_VERIFY(m_tab->isReady());

A007 | PianoRollTest::init | src/checks/rollcheck.cpp:57
Disposition: RETIRED-REPRESENTATION

Remaining original source (closing sequence and helper bodies):
  }

  void PianoRollTest::cleanup()
  {
      m_fixture.reset();
      m_tab.reset();
      m_bank.reset();
      m_project.reset();
  }

  // Aggregate rollback disposition: removed.  Each transactional row below owns
  // its post-seed undo baseline and proves byte-for-byte SMF restoration locally.
  // Identity/remap/header rows use isolated documents; interlock and selection
  // raster are explicitly undo-neutral; scale rows retain their own lifecycle
  // probes and now also restore their local byte baseline.  No slot depends on
  // another slot's mutations.
  // B3 disposition: retain the legacy device-pixel samples and their documented
  // tolerances in the raster rows.  They already use the captured image DPR;
  // no new theme or DPR permutation is added because it would be new coverage,
  // not a migration of a legacy contract.

  int runRollCheck(const QString &projectRoot, const QString &songLabel,
                   const QStringList &qtArguments)
  {
      PianoRollTest test(projectRoot, songLabel);
      QStringList arguments{QStringLiteral("rollcheck")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

Counterpart executable predicate index
No counterpart predicates identified; no S entries.
