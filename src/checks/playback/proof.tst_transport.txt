Assertion correspondence: playback/tst_transport.cpp

Reference revision: 97dc7fea819cb7c4a97a0dac819244ee570e993f
Original: src/checks/playback/tst_transport.cpp
Original SHA-256: 9ae0f5a1037fd587686246459ae09086ee4c235e27ba5b9a60a391376296570f
Swift counterpart: src/checks/playback/tails.swift (the ringingTail control guards now MATCH S004/S005; no same-name Swift file exists; the init device preconditions have no Swift ingress and are retired)
Covered native implementation: src/audio/audioengine.h :: AudioEngine::init, usingNullBackend, nullBackendForced, previewNoteTimed, activePcmChannels; the removed slot bodies it served lived in src/checks/playback/tails.cpp and src/checks/playback/transportfixture.cpp, both deleted in this lane and recoverable at their own reference revisions.
Native engine status: the legacy C++ AudioEngine remains on disk and still builds for the retained audiocheck-backend harness; this lane removed the transportcheck registration (src/checks/checkcatalog.cpp), the runTransportCheck declaration (src/checks/fwd.hpp) and the check sources (src/checks/CMakeLists.txt), so no native transport check runs and no active native bridge is claimed. Active transport coverage is the Swift swiftcore suite 3.
Original digest verification: git show 97dc7fea819cb7c4a97a0dac819244ee570e993f:src/checks/playback/tst_transport.cpp | shasum -a 256 reproduced Original SHA-256 9ae0f5a1037fd587686246459ae09086ee4c235e27ba5b9a60a391376296570f before this certificate was written.

Retirement: 4 original assertion sites, all terminal (MATCHED 2, RETIRED-REPRESENTATION 2, PARTIAL 0, GAP 0, NATIVE 0); the C++ check was deleted in this lane and is recoverable from Reference revision and Original SHA-256. No live native run or differential comparison is claimed.

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site in tst_transport.cpp appears below, including the
init preconditions and the ringingTail control guards. loadedSong carries no
assertion site: it returns an empty pointer on encoding/write/load/publication
failure and each per-case proof inventories the caller's QVERIFY2 of that
return value. Constructors, cleanup, engine() and runTransportCheck carry no
sites. S identifiers identify exact Swift predicates, listed after the
inventory. MATCHED identifies an equivalent Swift predicate asserting the same
observation under the original cppID (S004/S005 for the ringingTail control
guards); execution is evidenced by the lane run recorded below, not by this
inventory. RETIRED-REPRESENTATION identifies a C++ observation whose mechanism
has no ingress in the Swift path, with the no-ingress justification stated at
the site: such a row is retired, never executed, and carries no predicate. No
PARTIAL, GAP, NATIVE or NATIVE-SETUP row remains, because the native path that
carried the unconverted obligations is deleted.

Verification
Command: deno task verify --verbose (post-deletion lane run, provided by the controller)
Result: PASS — verify: 6/6 ok (11.75s); run_checks: PASS, all harnesses in 11.75s,
covering production-startup, audiocheck-backend, vgbankcheck, exportcheck-loop,
exportcheck-tail and swiftcore.
Command: deno task verify --filter swiftcore --verbose
Result: PASS — swiftcore, including the new scalecheck predicates in suite 2.
Command: deno task verify:shell --filter shellwindow --verbose
Result: PASS — verify: 1/11 ok (shellwindow).
This proof edit ran no build, verify or check command and contributes no run of
its own: the results above are the controller's handoff, and no live native
transportcheck run or differential parity is claimed.

Execution ownership
runAudioControllerChecks (src/checks/audio/AudioControllerChecks.swift,10)
calls checkAuditionTailTransitions (src/checks/playback/tails.swift), reached
from pdc_suite_run case 3 (src/checks/support/corecheck/CoreCheckSupport.swift)
under the swiftcore harness, which is what now executes the MATCHED guards.
AudioControllerCheckFixture (src/checks/audio/controllerfixture.swift)
constructs AudioRenderEngine directly and reports thrown construction errors as
failures, so the retired device-init rows A001/A002 have no Swift execution path.
transportfixture.swift builds the Swift timelines and carries no report.expect
calls (verified by search), so no shared-helper predicate section applies to it.

Fixture, sequence and row constraints
Every original slot bound PORYDAW_AUDIO_BACKEND=null and failed (never skipped)
when the forced-null init or backend report broke. ringingTail strikes
previewNoteTimed(track, 60, 127, 0.15 * sampleRate), requires audibility
within a 2 s qWaitFor bound, waits a further 400 ms wall-clock, then requires
the slow release still to ring. No data rows expand these sites: one guard
execution per helper call (tails.cpp called it from
playFromStoppedCutsAuditionTail and spacePathSeekAndPlayCutsTail, both track 0).
The Swift counterpart runs the same two scenarios in checkAuditionTailTransitions
with the 0.15 s timed note and renders exactly 400 ms of samples in place of the
wall-clock wait; countdown scenarios keep the 60 s preview this helper never used.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory

A001 | TransportTest::init | src/checks/playback/tst_transport.cpp:29
Disposition: RETIRED-REPRESENTATION
Mapping/reason: C++ observed a forced-null device-init preflight that the
removed native transportcheck slots could not run without. The Swift
counterpart (src/checks/playback/tails.swift) constructs AudioRenderEngine
directly inside AudioControllerCheckFixture
(src/checks/audio/controllerfixture.swift, renderer = try
AudioRenderEngine(sampleRate: 48_000, periodFrames: 512)) and
runAudioControllerChecks reports any thrown construction error as a failure,
so the forced-null device init itself has no ingress in the Swift path: the
mechanism is retired, not executed, and no Swift predicate is claimed. The
forced-null device init remains covered by the retained native
audiocheck-backend harness.
Source context since previous assertion:
  #include "checks/playback/tst_transport.h"

  #include <QDebug>
  #include <QFile>
  #include <QTemporaryFile>
  #include <QTest>
  #include <array>

  #include "audio/audioengine.h"
  #include "checks/playback/transportfixture.h"
  #include "project/voicegroupsource.h"

  namespace checks {

  TransportTest::TransportTest() = default;

  TransportTest::~TransportTest() = default;

  void TransportTest::init()
  {
      // The transport contracts are sample- and timing-exact, so they run on
      // the forced null backend: init failure fails the slot instead of
      // silently skipping (a missing real device is not a valid skip — the
      // null device runs the same callbacks without hardware).
      qputenv("PORYDAW_AUDIO_BACKEND", "null");
      m_bank = std::make_unique<AuditionVoicegroup>();
      m_engine = std::make_unique<AudioEngine>();
      QString error;
Original expression:
  QVERIFY2(m_engine->init(&error),
           qPrintable(QStringLiteral("forced null audio backend init failed: %1").arg(error)));

A002 | TransportTest::init | src/checks/playback/tst_transport.cpp:31
Disposition: RETIRED-REPRESENTATION
Mapping/reason: C++ observed the forced-null backend selection report
(usingNullBackend() && nullBackendForced()) after that device init. The Swift
counterpart (src/checks/playback/tails.swift) uses the device-independent
AudioRenderEngine, which exposes no backend-selection step or selection query
(src/swift/app/audio/AudioRenderEngine.swift), so the forced-null
selection has no ingress in the Swift path: the mechanism is retired, not
executed, and no Swift predicate is claimed. Backend selection remains
covered by the retained native audiocheck-backend harness.
Source context since previous assertion:
  QVERIFY2(m_engine->init(&error),
           qPrintable(QStringLiteral("forced null audio backend init failed: %1").arg(error)));
Original expression:
  QVERIFY2(m_engine->usingNullBackend() && m_engine->nullBackendForced(),
           "AudioEngine did not land on the forced null backend");

A003 | TransportTest::ringingTail | src/checks/playback/tst_transport.cpp:84
Disposition: MATCHED
Mapping/reason: S004 (src/checks/playback/tails.swift) executes the same
observation under the original cppID for both ringingTail callers
(transportcheck/TransportTest::playFromStoppedCutsAuditionTail and
transportcheck/TransportTest::spacePathSeekAndPlayCutsTail): after
audio.audition.previewNoteTimed(track: 0, key: 60, velocity: 127,
durationSamples: rig.rate * 15 / 100) and a 512-frame render it reports
audio.activePcmChannels >= 1, the positive form of the original
QTest::qWaitFor(engine.activePcmChannels() >= 1, 2000) guard. The square-wave
rig sets release 254 for all 128 programs and rendering is synchronous, so the
2 s wall-clock bound becomes exact rendered sample counts; no device
scheduling is claimed.
Source context since previous assertion:
  QVERIFY2(m_engine->usingNullBackend() && m_engine->nullBackendForced(),
           "AudioEngine did not land on the forced null backend");
  }

  void TransportTest::cleanup()
  {
      // Audio dies before the bank it borrowed (contract: destroy audio
      // before fixture data).
      m_engine.reset();
      m_bank.reset();
  }

  AudioEngine &TransportTest::engine() noexcept
  {
      return *m_engine;
  }

  std::shared_ptr<const PdPlaybackData> TransportTest::loadedSong(const TransportMidiFile &midi,
                                                                  const char *what)
  {
      const QByteArray bytes = midi.encode();
      if (bytes.isEmpty()) {
          qWarning("%s: fixture MIDI encoding failed", what);
          return {};
      }

      QTemporaryFile file;
      if (!file.open() || file.write(bytes) != bytes.size() || !file.flush()) {
          qWarning("%s: could not write fixture MIDI bytes", what);
          return {};
      }

      PdPlaybackData *publication = nullptr;
      std::array<char, 1024> diagnostic{};
      const QByteArray path = QFile::encodeName(file.fileName());
      if (!pd_playback_data_load_file(path.constData(), engine().sampleRate(), &publication,
                                      diagnostic.data(), diagnostic.size())) {
          if (publication)
              pd_playback_data_release(publication);
          qWarning("%s: Swift playback loader failed: %s", what, diagnostic.data());
          return {};
      }
      if (!publication) {
          qWarning("%s: Swift playback loader returned no publication", what);
          return {};
      }
      return std::shared_ptr<const PdPlaybackData>{publication, pd_playback_data_release};
  }

  bool TransportTest::ringingTail(AudioEngine &engine, uint8_t track)
  {
      engine.previewNoteTimed(track, 60, 127, uint32_t(0.15 * engine.sampleRate()));
      if (!QTest::qWaitFor([&] { return engine.activePcmChannels() >= 1; }, 2000)) {
Original expression:
  QTest::qFail("timed preview never sounded", __FILE__, __LINE__);

A004 | TransportTest::ringingTail | src/checks/playback/tst_transport.cpp:89
Disposition: MATCHED
Mapping/reason: S005 (src/checks/playback/tails.swift) executes under the
same two original cppIDs as A003 and, after the same 0.15 s timed preview plus
exactly 400 ms of rendered samples (rig.render(rig.rate * 4 / 10)) on the
release-254 rig, reports audio.polySnapshot().pcm.contains { $0.on &&
$0.releasing }. That live on-and-releasing channel is the survival the
original QTest::qWait(400) followed by the activePcmChannels() < 1 failure
guard asserted, tightened from a channel count to a releasing voice, and only
the tail scenarios (this helper's callers) run it. The 400 ms sample render
replaces the original 400 ms wall-clock wait; no device scheduling is
claimed.
Source context since previous assertion:
  QTest::qFail("timed preview never sounded", __FILE__, __LINE__);
          return false;
      }
      QTest::qWait(400); // note-off sent; the slow release rings on
      if (engine.activePcmChannels() < 1) {
Original expression:
  QTest::qFail("slow-release tail died early (control expectation changed?)", __FILE__,
                       __LINE__);

Remaining original source (closing sequence and helper bodies):
          return false;
      }
      return true;
  }

  } // namespace checks

  int runTransportCheck(const QStringList &qtArguments)
  {
      checks::TransportTest test;
      QStringList arguments{QStringLiteral("transportcheck")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

Swift assertion predicates

S001 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "both audition tracks initialized"
  report.expectEqual(2, timeline.usedTrackCount, cppID: id, what: "both audition tracks initialized")

S002 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "silent song loaded"
  report.expect(audio.songLoaded, cppID: id, message: "silent song loaded")

S003 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "playback started"
  report.expect(audio.playheadSamples > 0, cppID: id, message: "playback started")

S004 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "timed preview sounds before transition"
  report.expect(audio.activePcmChannels >= 1, cppID: id,
                        message: "timed preview sounds before transition")

S005 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "expired preview has a live release tail"
  report.expect(audio.polySnapshot().pcm.contains { $0.on && $0.releasing },
                  cppID: id, message: "expired preview has a live release tail")

S006 | checkAuditionTailTransitions | src/checks/playback/tails.swift
Anchor: message "transition cuts preview and release tail within two seconds"
  report.expectEqual(0, audio.activePcmChannels, cppID: id,
                            what: "transition cuts preview and release tail within two seconds")

S007 | checkUnloadPlayingSong | src/checks/playback/tails.swift
Anchor: message "song voice sounds before unload"
  report.expect(audio.activePcmChannels >= 1,
          cppID: "transportcheck/TransportTest::unloadWhilePlayingCutsSongVoices",
          message: "song voice sounds before unload")

S008 | checkUnloadPlayingSong | src/checks/playback/tails.swift
Anchor: message "unload retires voices before bank release"
  report.expect(audio.activePcmChannels == 0 && audio.activeCgbChannels == 0 && !audio.songLoaded,
          cppID: "transportcheck/TransportTest::unloadWhilePlayingCutsSongVoices", message: "unload retires voices before bank release")

S009 | checkUnloadedVoicegroupLifetime | src/checks/playback/tails.swift
Anchor: message "heap-bank note song loaded"
  report.expect(audio.songLoaded, cppID: id, message: "heap-bank note song loaded")

S010 | checkUnloadedVoicegroupLifetime | src/checks/playback/tails.swift
Anchor: message "one song track"
  report.expectEqual(1, audio.timeline!.usedTrackCount, cppID: id, what: "one song track")

S011 | checkUnloadedVoicegroupLifetime | src/checks/playback/tails.swift
Anchor: message "heap-bank note sounds"
  report.expect(audio.activePcmChannels >= 1, cppID: id, message: "heap-bank note sounds")

S012 | checkUnloadedVoicegroupLifetime | src/checks/playback/tails.swift
Anchor: message "unload immediately retires song voices"
  report.expectEqual(0, audio.activePcmChannels, cppID: id, what: "unload immediately retires song voices")

S013 | checkUnloadedVoicegroupLifetime | src/checks/playback/tails.swift
Anchor: message "callbacks remain voiceless after heap-bank destruction"
  report.expectEqual(0, audio.activePcmChannels, cppID: id,
                        what: "callbacks remain voiceless after heap-bank destruction")

Shared Swift sources used by this pairing (SHA-256)
src/checks/audio/controllerfixture.swift: 60f6f6e5d489f2781c18bfea29e40cab4b0f56d73885e2e6fbb3dab02769ff4d
src/checks/playback/transportfixture.swift: 9954fe9db6763cb78623fe4c8c9410ea5ff30b4b6d0e417b066ede2339edf4ab
src/checks/playback/publish.swift: 9cf64e5a4b6e1e7dd6f9b6244162aaaedd01cbb85674fa35a006e85e8d79f2a4
src/checks/audio/AudioControllerChecks.swift: 019c0ce1191fe474b2655a24b414f1f402da0c18148c8e03643a6e0be573d77c
src/checks/playback/PlaybackChecks.swift: 7e1cc988b6542fc2fdcdcb377a7b73b2369df0c4901dfe460b0986fefb7f1bfe
src/checks/support/corecheck/CoreCheckSupport.swift: 99be72eb74c04654e9d7a742ff35c9f055be6acd8a8e69c950c1d5fa9a30b9ae
