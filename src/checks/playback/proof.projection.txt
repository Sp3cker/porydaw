Assertion correspondence: playback/projection.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/playback/projection.cpp
Original SHA-256: dc0fefef3ae76a0525a7ec0972bcb2b1472d5e444c9974aaffc7f11a5600c56d
Swift counterpart: src/checks/playback/projection.swift
Swift SHA-256: f0cb8f444881053b2402a05cebdfeb87a13c7dfd8058ae01b48fac19734e6ed0

Purpose and status
Each entry is one original assertion/failure site. MATCHED records an exact
source-level predicate, fixture and action sequence; execution is tracked below.
No assertion is accepted by cppID name alone. Short-circuit ordering is retained.
No original has data-driven QTest rows or assertion-bearing shared helper calls.
All functions use their original literal event index/tick/stream/controller/value/
channel inputs, write/remove/relocation inputs, and action ordering. Missing
original scenario predicates were added before advancing to the next file.
Additional pre-existing Swift fixture variants remain executable and are not
substitutes for the original cases enumerated here.

Execution and ownership
runXcmdEditsSuite executes the paired scenarios. The same-stem file contains
the actual Swift assertions; playback/tst_xcmd.swift constructs event values
with the original ev helper argument order, including default channel zero.
The production Xcmd projection/rewrite/reconciliation/export APIs are exercised.

Verification evidence
Command: deno task verify --filter swiftcore --filter clickcheck --verbose
Result: PASS 2/2 selected checks (24 total, 22 skipped); swiftcore 1.83s.
This records the completed checkpoint before the subsequent shared corpus-loader
extraction/range additions; updated combined verification is pending.
The current build does not register the old xcmdcheck native harness; no native
execution or live differential parity is claimed.

Original assertion inventory

A001 | XcmdTest::sharedSelectorServesTwoCompletions | src/checks/playback/projection.cpp:16
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:26 :: xcmdPairSharedSelectorServesTwoCompletions; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.points.size() == 2 && result.points[0].lane == xcmd::kEchoVolumeLane &&
                   result.points[0].value == 34 && result.points[0].tick == 2 &&
                   result.points[0].index == 1 && result.points[1].value == 35 &&
                   result.points[1].tick == 3 && result.points[1].index == 2 &&
                   result.consumed == std::vector<uint64_t>({0, 1, 2}),
               "shared-selector projection did not expose both points and consumed indices")

A002 | XcmdTest::unknownSelectorEpochStaysOpaque | src/checks/playback/projection.cpp:32
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:40 :: xcmdPairUnknownSelectorEpochStaysOpaque; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.points.empty() && result.consumed == std::vector<uint64_t>({0, 1, 2}),
               "unknown-selector epoch did not stay opaque with consumed bytes")

A003 | XcmdTest::danglingKnownSelectorProjectsOpaque | src/checks/playback/projection.cpp:42
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:50 :: xcmdPairDanglingKnownSelectorProjectsOpaque; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.points.empty() && result.consumed == std::vector<uint64_t>({0}),
               "payload-less selector epoch did not project as opaque")

A004 | XcmdTest::leadingStrayPayloadsStayOpaque | src/checks/playback/projection.cpp:54
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:58 :: xcmdPairLeadingStrayPayloadsStayOpaque; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.points.size() == 1 && result.points[0].value == 17 &&
                   result.consumed == std::vector<uint64_t>({0, 1, 2}),
               "leading stray payload run was not kept opaque")

A005 | XcmdTest::streamsProjectIndependently | src/checks/playback/projection.cpp:67
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:69 :: xcmdPairStreamsProjectIndependently; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.points.size() == 2 && result.points[0].stream == 0 &&
                   result.points[0].lane == xcmd::kEchoVolumeLane && result.points[1].stream == 1 &&
                   result.points[1].lane == xcmd::kEchoLengthLane && result.points[1].value == 17,
               "streams did not project independently")

A006 | XcmdTest::consumedIsSortedDedupIndexSet | src/checks/playback/projection.cpp:81
Disposition: MATCHED
Swift: src/checks/playback/projection.swift:81 :: xcmdPairConsumedIsSortedDedupIndexSet; report.expect assertion 1
Mapping/reason: Original predicate is reproduced conjunct by conjunct with the same fixture inputs and preceding operations. C++ vector size/empty map to Swift count/isEmpty; nullable patch checks map to nil checks. Raw SIZE_MAX source indices map to nil, the Swift fresh-emission representation.
Original expression:
  QVERIFY2(result.consumed == std::vector<uint64_t>({2, 7, 9}),
               "protocol consumption was not a sorted raw-index set")
