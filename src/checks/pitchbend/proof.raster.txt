Assertion correspondence: pitchbend/raster.cpp

Reference revision: 91cab247f7600b41c92b14114ad0352e3a002df0
Original: src/checks/pitchbend/raster.cpp
Original SHA-256: 91c692e0cfb7a0ecc7c797cc397958c954685b30ce3adee78fe8473ae922df88
Swift counterpart: none identified
Swift SHA-256: n/a

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of any converted Swift window.
GAP identifies an original predicate without a replacement assertion.
No Swift counterpart covers this suite: src/checks/pitchbend/ holds no .swift
file; src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not
Swift; swiftcore/*.swift and automation Swift touch only unrelated parameter
metadata (e.g. AutomationParameter.pitchBend titles), never the pitch-bend
editor popup, its graphs, or its controllers. Unsure between PARTIAL and GAP
resolves to GAP: bookkeeping must not overclaim.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: pending integrated run; source hashes pin the tree inspected here.

Execution ownership
No Swift entry point executes these checks. The C++ suite entry points are
runPitchBendRasterCheck (PitchBendRasterTest::init/cleanup drive the fixture);
PitchBendEditingTest::popup/pitchGraph/modGraph/drawPitchCurve/drawModCurve and
the PitchBendFixture helpers live in src/checks/pitchbend/fixture.cpp with
declarations in src/checks/pitchbend/tst_pitchbendediting.h
(SHA-256 7580a8bae6dab536e3f3a4ceeb170a23d047ee5cf3459bb5e1584e4b2e64b895).

Fixture, sequence and row constraints
No QTest data rows in this file; each site executes once per test run.
Loops over lane points, SMF events, surface pixels, and probe fractions expand
one site into many runtime checks; undo-index and SMF comparisons pin the
document state around each gesture. init/setup guards gate every test method.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | PitchBendRasterTest::init | src/checks/pitchbend/raster.cpp:43
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
  #include "checks/pitchbend/tst_pitchbendediting.h"

  #include <QCoreApplication>
  #include <QtTest>

  #include "ui/theme/themeruntime.h"

  namespace {
  QPoint canvasPoint(const songview::PitchBendGraph &graph, qreal xFraction, qreal yFraction)
  {
      const QRect canvas = graph.canvasRect();
      return QPoint(qRound(canvas.left() + canvas.width() * xFraction),
                    qRound(canvas.top() + canvas.height() * yFraction));
  }

  QColor scenePixel(const QImage &image, QPoint scenePoint)
  {
      const qreal dpr = image.devicePixelRatio();
      const QPoint pixel(qRound(scenePoint.x() * dpr), qRound(scenePoint.y() * dpr));
      return image.pixelColor(pixel);
  }

  int coloredHits(const QImage &image, const songview::PitchBendGraph &graph, QPoint start,
                  QPoint finish)
  {
      int hits = 0;
      const QColor background =
          scenePixel(image, graph.mapToScene(graph.canvasRect().topLeft()).toPoint());
      for (int i = 0; i < 7; ++i) {
          const qreal fraction = qreal(i + 1) / 8.0;
          const QPoint graphPoint = start + (finish - start) * fraction;
          const QColor pixel = scenePixel(image, graph.mapToScene(graphPoint).toPoint());
          if (pixel != background && pixel.alpha() == 255)
              ++hits;
      }
      return hits;
  }

  } // namespace

  void PitchBendRasterTest::init()
  {
Original expression:
      QVERIFY(m_fixture.setUp());

A002 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:54
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
  }

  void PitchBendRasterTest::cleanup()
  {
      m_fixture.tearDown();
  }

  void PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground()
  {
      songview::PitchBendEditor *editor = m_fixture.openPopup();
Original expression:
      QVERIFY(editor);

A003 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:56
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      QQuickItem *content = m_fixture.formContent();
Original expression:
      QVERIFY(content);

A004 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:58
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
      const QImage image = m_fixture.timelineWindow().grabWindow();
Original expression:
      QVERIFY(!image.isNull());

A005 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:65
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
      // Crop the shared canvas grab to the popup form before asserting opacity.
      const QPointF contentScene = content->mapToScene(QPointF(0, 0));
      const qreal dpr = image.devicePixelRatio();
      const QRect surfaceRect((contentScene * dpr).toPoint(),
                              QSize(qRound(content->width() * dpr), qRound(content->height() * dpr)));
      const QImage surface = image.copy(surfaceRect.intersected(image.rect()));
Original expression:
      QVERIFY(!surface.isNull());

A006 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:68
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
      for (int y = 0; y < surface.height(); ++y) {
          for (int x = 0; x < surface.width(); ++x)
Original expression:
              QCOMPARE(surface.pixelColor(x, y).alpha(), 255);

A007 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:71
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
      }
      const QColor background = themes::color(themes::Role::window_background);
Original expression:
      QCOMPARE(surface.pixelColor(qRound(4 * dpr), qRound(24 * dpr)), background);

A008 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:72
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
  (none: assertion opens the file segment)
Original expression:
      QCOMPARE(surface.pixelColor(qRound(4 * dpr), qRound(80 * dpr)), background);

A009 | PitchBendRasterTest::popupSurfaceIsOpaqueAndUsesWindowBackground | src/checks/pitchbend/raster.cpp:73
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
  (none: assertion opens the file segment)
Original expression:
      QCOMPARE(surface.pixelColor(qRound(4 * dpr), surface.height() - qRound(4 * dpr)), background);

A010 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:79
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
  }

  void PitchBendRasterTest::shiftCurvePaintsDiagonal()
  {
      QPointer<songview::PitchBendEditor> editor = m_fixture.openPopup();
Original expression:
      QVERIFY(editor);

A011 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:81
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      QPointer<songview::PitchBendGraph> graph = m_fixture.graph(QStringLiteral("pitchBendGraph"));
Original expression:
      QVERIFY(graph);

A012 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:84
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      const QPoint start = canvasPoint(*graph, 0.15, 0.80);
      const QPoint finish = canvasPoint(*graph, 0.85, 0.20);
Original expression:
      QVERIFY(m_fixture.stroke(*graph, start, finish, Qt::ShiftModifier));

A013 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:87
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      QCoreApplication::processEvents();
      if (!editor || !editor->isOpen()) {
Original expression:
          QFAIL("pitch-bend popup was dismissed while rendering its Shift line");

A014 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:91
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
          return;
      }
      const QImage image = m_fixture.timelineWindow().grabWindow();
Original expression:
      QVERIFY(!image.isNull());

A015 | PitchBendRasterTest::shiftCurvePaintsDiagonal | src/checks/pitchbend/raster.cpp:92
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
  (none: assertion opens the file segment)
Original expression:
      QVERIFY(coloredHits(image, *graph, start, finish) >= 4);

A016 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:98
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
  }

  void PitchBendRasterTest::altRampPaintsDiagonalAfterReopen()
  {
      songview::PitchBendEditor *editor = m_fixture.openPopup();
Original expression:
      QVERIFY(editor);

A017 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:100
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      songview::PitchBendGraph *graph = m_fixture.graph(QStringLiteral("pitchBendGraph"));
Original expression:
      QVERIFY(graph);

A018 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:103
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      const QPoint start = canvasPoint(*graph, 0.10, 0.85);
      const QPoint finish = canvasPoint(*graph, 0.90, 0.15);
Original expression:
      QVERIFY(m_fixture.stroke(*graph, start, finish, Qt::AltModifier));

A019 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:106
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      m_fixture.closePopupViaEscape();
      editor = m_fixture.openPopup();
Original expression:
      QVERIFY(editor);

A020 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:108
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing Swift checks has been established; no Swift counterpart covers this suite (src/checks/selectionkey/localinputtier_pitchbend.cpp is a C++ suite, not Swift; swiftcore/ and automation Swift cover only unrelated parameter metadata). Original predicate and setup below remain an open obligation.
Source context since previous assertion:
      graph = m_fixture.graph(QStringLiteral("pitchBendGraph"));
Original expression:
      QVERIFY(graph);

A021 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:110
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
      const QImage image = m_fixture.timelineWindow().grabWindow();
Original expression:
      QVERIFY(!image.isNull());

A022 | PitchBendRasterTest::altRampPaintsDiagonalAfterReopen | src/checks/pitchbend/raster.cpp:111
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering observation. A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:
  (none: assertion opens the file segment)
Original expression:
      QVERIFY(coloredHits(image, *graph, start, finish) >= 4);

Remaining original source (closing sequence and helper bodies):
  }

  int runPitchBendRasterCheck(const QStringList &qtArguments)
  {
      PitchBendRasterTest test;
      QStringList arguments{QStringLiteral("pitch-bend-raster")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

Swift assertion predicates
None: no Swift counterpart file carries report.expect*/expectEqual predicates
for this suite.

Tally: 22 sites: MATCHED 0, PARTIAL 0, GAP 12, NATIVE 10.
