Assertion correspondence: nativegraphics/tst_playhead_math.cpp

Reference revision: 0b4c9ea649baa7174e53be880163d45b4d951d8d
Deleted in: 91cab247f7600b41c92b14114ad0352e3a002df0 Refine check suite boundaries and native fixture isolation
Original: src/checks/nativegraphics/tst_playhead_math.cpp
Original SHA-256: 0a97529912e1e79c51d9ba9960d295fae79331687d0f0fc04a82b6ea223fdba5
Swift counterpart: none identified
Swift SHA-256: n/a

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below. Source context preserves setup between
assertions verbatim.
One devicePixelRect slot maps logical rects across devicePixelRatio 1.0, 2.0, and
1.5 (including rounding). All four sites stay open: no Swift predicate in
tst_playhead_quick.swift, SharedPlayheadChecks.swift, or the camera checks
observes devicePixelRect mapping, so the counterpart is none identified.
Disposition tally: 4 sites: 4 GAP.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related counterpart predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of the converted Swift surface.
GAP identifies an original predicate without a replacement assertion.
Unsure between PARTIAL and GAP resolves to GAP: bookkeeping must not overclaim.

Verification
Command: deno task verify --filter rendering-playhead --verbose
Result: pending integrated run; source hashes pin the tree inspected here.
The legacy rendering-playhead C++ slots were retired during the boundary sweep;
their sources are reference inventories only, never current execution evidence.

Execution and ownership
RenderingPlayheadTest::devicePixelRect exercises checks::support::devicePixelRect
directly on QImages with devicePixelRatio 1.0, 2.0, and 1.5; no fixture file
carries predicates beyond quickframebuffer.h helpers.

Fixture, sequence and row constraints
The slot needs no song, view, or window; three QImages and four expected rects
are the whole setup. No _data rows expand sites in this file.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | devicePixelRect | src/checks/nativegraphics/tst_playhead_math.cpp:22
Disposition: GAP
Mapping/reason: No Swift predicate observes devicePixelRect mapping; DPR rect math stays open.
Source context since previous assertion:
  #include "checks/nativegraphics/tst_renderingplayhead.h"

  #include <QtTest>

  #include <QImage>
  #include <QRect>

  #include <utility>

  #include "checks/support/quickframebuffer.h"

  RenderingPlayheadTest::RenderingPlayheadTest(QString projectRoot, QString songLabel)
      : m_projectRoot(std::move(projectRoot))
      , m_songLabel(std::move(songLabel))
  {}

  void RenderingPlayheadTest::devicePixelRect()
  {
      const QRect logicalRect{3, 2, 5, 4};
      QImage dprOne{QSize{16, 10}, QImage::Format_ARGB32_Premultiplied};
      dprOne.setDevicePixelRatio(1.0);
Original expression:
      QCOMPARE(checks::support::devicePixelRect(dprOne, logicalRect), logicalRect);

A002 | devicePixelRect | src/checks/nativegraphics/tst_playhead_math.cpp:27
Disposition: GAP
Mapping/reason: No Swift predicate observes devicePixelRect mapping; DPR rect math stays open.
Source context since previous assertion:

      QImage dprTwo{QSize{32, 20}, QImage::Format_ARGB32_Premultiplied};
      dprTwo.setDevicePixelRatio(2.0);
      const QRect doubleRect{6, 4, 10, 8};
Original expression:
      QCOMPARE(checks::support::devicePixelRect(dprTwo, logicalRect), doubleRect);

A003 | devicePixelRect | src/checks/nativegraphics/tst_playhead_math.cpp:33
Disposition: GAP
Mapping/reason: No Swift predicate observes devicePixelRect mapping; DPR rect math stays open.
Source context since previous assertion:

      QImage dprOnePointFive{QSize{12, 14}, QImage::Format_ARGB32_Premultiplied};
      dprOnePointFive.setDevicePixelRatio(1.5);
      const QRect fractionalRect{4, 3, 8, 6};
      const QRect roundingRect{1, 1, 4, 4};
Original expression:
      QCOMPARE(checks::support::devicePixelRect(dprOnePointFive, logicalRect), fractionalRect);

A004 | devicePixelRect | src/checks/nativegraphics/tst_playhead_math.cpp:34
Disposition: GAP
Mapping/reason: No Swift predicate observes devicePixelRect mapping; DPR rect math stays open.
Source context since previous assertion:
Original expression:
      QCOMPARE(checks::support::devicePixelRect(dprOnePointFive, QRect{1, 1, 2, 2}), roundingRect);

Remaining original source (closing sequence and helper bodies):
  }

Counterpart executable predicate index: none identified.
No Swift predicate observes the devicePixelRect mapping asserted here; there is no counterpart index to preserve. The converted playhead Swift predicates observe publication and reprojection (tick and contentX), not DPR rect math.
