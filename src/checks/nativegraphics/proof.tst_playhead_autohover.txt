Assertion correspondence: nativegraphics/tst_playhead_autohover.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/nativegraphics/tst_playhead_autohover.cpp
Original SHA-256: 4d838f6b058c18312c6f126177dd9b65f04cda390a8b35fd0fa044fc5408ae4b
Swift counterpart: src/checks/nativegraphics/PlayheadFeatureChecks.swift (no autohover predicate)
Swift SHA-256: 22ebafab5676e0d2457b87e91f69288f39894ab1e5baa22918fbab01e356fc7d
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files

Status and evidence
The deleted C++ file above is audited from its parent revision (pinned by
Reference revision and SHA-256), so no assertion is dropped during migration.
tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor ports the case
onto the real Swift session's Quick surface: it activates the CC1 Modulation
selector tab, arms the page model's isPencilMode, delivers the same
enter/leave/hover move sequence, and compares the baseline/hovered/cleared
framebuffer triple over the automation body. The capture is the whole-window
framebuffer read over the plot input's scene-mapped rect - Qt Quick Test's
grabImage crops the window by an item's local rect, so the whole-window grab
is the faithful equivalent of the original's scene.capture(input). The
cleared-vs-baseline compare tolerates a one-channel glyph antialiasing repaint
wobble; a real decor remnant differs by far more.
Disposition tally: 25 sites: 25 MATCHED.

Verification
Command: deno task verify:qml-roll
Result: green — tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor
passes in the rollqml lane.
The deleted C++ autohover harness is not registered; its hover/event/window and
framebuffer assertions are observed by the ported QML case.

Execution ownership
The rollqml lane (src/checks/rollqml/) executes
tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor through its own
manifest entry; the lane bootstrap boots the production Swift session and
composition.

Fixture, sequence and row constraints
A single automationHoverDecor case: one hover point inside the lane body and one leave point outside it expand the capture sites into a baseline/hovered/cleared triple, with primeMouseMove delivery guarded by qWaitFor between captures.
Original setup, branch guards and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:71
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the lane bootstrap start + session.songOpen wait surfaces open failures through verify().
Source context since previous assertion:
  #include "checks/nativegraphics/tst_renderingplayhead.h"

  #include <QtTest>

  #include <QEnterEvent>
  #include <QImage>
  #include <QQuickItem>
  #include <QQuickWindow>
  #include <QScopeGuard>

  #include <algorithm>
  #include <cstdint>
  #include <memory>
  #include <optional>

  #include "checks/nativegraphics/nativegraphics_fixture.h"
  #include "checks/support/eventsynth.h"
  #include "checks/support/quickframebuffer.h"
  #include "checks/support/songfixture.h"
  #include "checks/support/timelinequickcheck.h"
  #include "core/timedefaults.h"
  #include "ui/editordrawer/automationcanvas.h"
  #include "ui/editordrawer/automationpage.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/layout.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"

  namespace {

  // The catalog position comes from the production mapping only; clicking the
  // real selector label is the same activation path a user takes.
  bool activateAutomationParameter(SongView &view, AutomationCanvas &canvas,
                                   const EditorAutomationRowId &row)
  {
      const int index = checks::support::automationParameterIndex(canvas, row);
      if (index < 0)
          return false;
      songview::TimelineQuickView *const quick = view.quickView();
      QQuickItem *const root = quick ? quick->rootObject() : nullptr;
      if (!root)
          return false;
      QQuickItem *label = nullptr;
      if (!QTest::qWaitFor([&root, &label, index] {
              label = checks::support::visualDescendant(
                  root, QStringLiteral("automationParameterTab%1").arg(index));
              return label && label->isVisible() && label->isEnabled() && label->width() > 0.0 &&
                     label->height() > 0.0 && label->window();
          }))
          return false;
      QQuickWindow *const window = label->window();
      QQuickItem *const content = window ? window->contentItem() : nullptr;
      if (!content)
          return false;
      const QPointF point = content->mapFromScene(
          label->mapToScene(QPointF(label->width() / 2.0, label->height() / 2.0)));
      if (!content->boundingRect().contains(point))
          return false;
      QTest::mouseClick(window, Qt::LeftButton, Qt::NoModifier, point.toPoint());
      return QTest::qWaitFor([&canvas, index] { return canvas.activeParameter() == index; });
  }

  } // namespace

  void RenderingPlayheadTest::automationHoverDecor()
  {
      QString error;
      std::unique_ptr<checks::nativegraphics::Rig> rig =
          checks::nativegraphics::makeRig(m_projectRoot, m_songLabel, QSize{1280, 800}, error);

Original expression:
  QVERIFY2(rig, qPrintable(error))

A002 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:76
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — scene.item("automationPage") resolves the mounted automation page.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      auto *page = view.editorDrawer() ? view.editorDrawer()->automationPage() : nullptr;
      auto *canvas = page ? page->canvas() : nullptr;
      auto *quick = view.quickView();

Original expression:
  QVERIFY(page)

A003 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:77
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — scene.model("automationPage") resolves the Swift page model, the canvas counterpart.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(canvas)

A004 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:78
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — scene.root && scene.view prove the production root item and its real QQuickView.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(quick && quick->rootObject() && quick->quickWindow())

A005 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:82
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — model->property("trackAvailable") publishes the selected-track precondition; the Swift page exposes availability rather than a raw index.
Source context since previous assertion:
  ;
      auto *window = quick->quickWindow();

      const int track = view.selectionModel().primaryTrack();

Original expression:
  QVERIFY(track >= 0 && track < 16)

A006 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:87
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — scene.item("automationPlotInput") resolves the plot's MouseArea input.
Source context since previous assertion:
  ;
      const bool originalPencil = canvas->pencilMode();
      songview::TimelineInputItem *input =
          quick->rootObject()->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineAutomationInput"));

Original expression:
  QVERIFY(input)

A007 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:102
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the real Modulation selector tab is clicked and QTRY_VERIFY(tab->property("checked")) proves activation through the same user path.
Source context since previous assertion:
  ;
      QPoint nativeLeavePoint;
      const auto restore = qScopeGuard([&] {
          QTest::mouseEvent(QTest::MouseMove, window, Qt::NoButton, Qt::NoModifier, nativeLeavePoint);
          if (canvas->pencilMode() != originalPencil)
              canvas->setPencilMode(originalPencil);
          checks::support::pumpQuick();
      });

      // Real activation on the shared plot: the CC1 Modulation selector label
      // replaces the old add-empty-lane setup, and the active slot's full-height
      // body replaces the pinned Tempo header. The plot has no vertical
      // automation scroll, so content and input coordinates already agree.
      const EditorAutomationRowId lane{EditorAutomationRowKind::ControlChange,
                                       static_cast<uint8_t>(track), CoreTimeDefaults::kCcModulation};

Original expression:
  QVERIFY2(activateAutomationParameter(view, *canvas, lane),
               "the modulation parameter label did not activate")

A008 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:112
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the Swift page has no LaneHandle; the checked Modulation tab plus the published section body prove the lane is active.
Source context since previous assertion:
  ;
      checks::support::pumpQuick();

      LaneHandle handle;
      const auto &rows = canvas->rows();
      for (int index = 0; index < int(rows.size()); ++index) {
          if (rows[std::size_t(index)].id == lane)
              handle = LaneHandle{index};
      }

Original expression:
  QVERIFY(handle.valid())

A009 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:113
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — input->property("hoverEnabled") is the Swift MouseArea's hover-delivery counterpart of the native interaction object.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(input->interaction())

A010 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:114
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(input->window() == scene.view).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(input->window())

A011 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:117
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — drawerBody_automation's scene rect is valid and non-empty, the laneBody counterpart.
Source context since previous assertion:
  ;

      const QRect body = canvas->laneBody(handle);

Original expression:
  QVERIFY(!body.isEmpty())

A012 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:120
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the drawer presenter's automationSection publishes visible plus bodyHeight > 0, the band-geometry counterpart.
Source context since previous assertion:
  ;
      const std::optional<songview::TimelineBandGeometry> &band =
          view.timelineBandLayout().geometry(songview::TimelineBand::Automation);

Original expression:
  QVERIFY(band && band->rect.isValid())

A013 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:122
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(inputBounds.width() > 0 && inputBounds.height() > 0).
Source context since previous assertion:
  ;
      const QSize inputSize{qFloor(input->width()), qFloor(input->height())};

Original expression:
  QVERIFY(inputSize.width() > 0 && inputSize.height() > 0)

A014 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:126
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the same singlePixel-inset body∩input intersection is computed and asserted non-empty.
Source context since previous assertion:
  ;
      const QRect inputBounds{QPoint{}, inputSize};
      const QRect interior = body.adjusted(0, layout::singlePixel() + 1, 0, -layout::singlePixel())
                                 .intersected(inputBounds);

Original expression:
  QVERIFY(!interior.isEmpty())

A015 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:129
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(input->boundingRect().contains(point)) at the same two-thirds-width interior point.
Source context since previous assertion:
  ;
      const int x = (std::clamp)(inputSize.width() * 2 / 3, interior.left(), interior.right());
      const QPoint point{x, interior.center().y()};

Original expression:
  QVERIFY(inputBounds.contains(point))

A016 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:130
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(body->boundingRect().contains(body->mapFromItem(input, point))).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(body.contains(point))

A017 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:134
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(windowBounds.contains(hoverPoint)) with the same scene-mapped point.
Source context since previous assertion:
  ;
      const QPoint nativeHoverPoint = input->mapToScene(QPointF(point)).toPoint();
      nativeLeavePoint = input->mapToScene(QPointF{-1.0, -1.0}).toPoint();
      const QRect windowBounds{QPoint{}, window->size()};

Original expression:
  QVERIFY(windowBounds.contains(nativeHoverPoint))

A018 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:135
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(windowBounds.contains(leavePoint)) with the same scene-mapped (-1,-1) leave point.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(windowBounds.contains(nativeLeavePoint))

A019 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:136
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — QVERIFY(!input->contains(input->mapFromScene(QPointF(leavePoint)))).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!input->contains(input->mapFromScene(QPointF(nativeLeavePoint))))

A020 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:149
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the same QEnterEvent plus leave move precedes QVERIFY(!baseline.isNull()) on the body capture.
Source context since previous assertion:
  ;

      canvas->cancelInteraction();
      canvas->setPencilMode(true);
      // QTest's window mouse delivery does not synthesize the platform Enter
      // event that enables Quick's hover dispatch.
      QEnterEvent enter(QPointF(nativeLeavePoint), QPointF(nativeLeavePoint),
                        window->mapToGlobal(QPointF(nativeLeavePoint)));
      QCoreApplication::sendEvent(window, &enter);
      QTest::mouseEvent(QTest::MouseMove, window, Qt::NoButton, Qt::NoModifier, nativeLeavePoint);
      checks::support::pumpQuick();
      QString captureError;
      const QImage baseline = checks::support::captureQuickBand(view, band->rect, &captureError);

Original expression:
  QVERIFY2(!baseline.isNull(), qPrintable(captureError))

A021 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:153
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the same adjacent-point staging move runs inline (eventsynth is not compiled into this target) guarded by QVERIFY2(primed, ...).
Source context since previous assertion:
  ;

      // The first move into a fresh surface may deliver only HoverEnter.
      // Move within the surface before the target so this exercises HoverMove.

Original expression:
  QVERIFY2(checks::events::primeMouseMove(*window, *input, nativeHoverPoint),
               "the automation surface has no interior mouse-move staging point")

A022 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:158
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — tryVerify(regionsDiffer(grabImage(testCase), baseline, region)) covers the non-null capture; the window grab is always non-null.
Source context since previous assertion:
  ;
      QTest::mouseEvent(QTest::MouseMove, window, Qt::NoButton, Qt::NoModifier, nativeHoverPoint);
      checks::support::pumpQuick();
      const QImage hovered = checks::support::captureQuickBand(view, band->rect, &captureError);

Original expression:
  QVERIFY2(!hovered.isNull(), qPrintable(captureError))

A023 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:159
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — tryVerify(regionsDiffer(hovered, baseline, region)) proves the hover decor paints inside the input's scene-mapped rect.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(hovered != baseline)

A024 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:164
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — the cleared capture runs inside tryVerify(regionsEquivalent(...)); the window grab is always non-null.
Source context since previous assertion:
  ;
      QTest::mouseEvent(QTest::MouseMove, window, Qt::NoButton, Qt::NoModifier, nativeLeavePoint);
      checks::support::pumpQuick();
      const QImage cleared = checks::support::captureQuickBand(view, band->rect, &captureError);

Original expression:
  QVERIFY2(!cleared.isNull(), qPrintable(captureError))

A025 | RenderingPlayheadTest::automationHoverDecor | src/checks/nativegraphics/tst_playhead_autohover.cpp:165
Disposition: MATCHED
Mapping/reason: tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor — tryVerify(regionsEquivalent(grabImage(testCase), baseline, region)) proves the decor clears; one-channel glyph antialiasing repaint wobble is tolerated.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(cleared, baseline)

Remaining original source (closing sequence and helper bodies):
  ;
  }


Swift assertion predicates
PlayheadFeatureChecks.swift has no autohover predicate; the ported observation
lives in the rollqml lane:
automationPencilHoverDecor -> rollqml/tst_SwiftRollAutomation.qml::test_automationPencilHoverDecor
