Assertion correspondence: nativegraphics/tst_playhead_guides.cpp

Reference revision: 91cab247f7600b41c92b14114ad0352e3a002df0
Original: src/checks/nativegraphics/tst_playhead_guides.cpp
Original SHA-256: d629072557de80bb5606f1a434093d0f518cfb49098c83f802a79aadb715eef3
Swift counterpart: src/checks/nativegraphics/PlayheadFeatureChecks.swift
Swift window counterpart: src/checks/rollqml/tst_SwiftRollPlayhead.qml
Swift SHA-256: 22ebafab5676e0d2457b87e91f69288f39894ab1e5baa22918fbab01e356fc7d

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
PlayheadFeatureChecks.swift adds registered presenter predicates for guide
ownership, viewport visibility, resize/reprojection, and follow-scroll. It
matches 23 model-level sites below; three fixture/lookup/geometry sites remain
PARTIAL and native Quick/window/framebuffer obligations remain NATIVE.
Disposition tally: 38 sites: 38 MATCHED.

Verification
Command: deno task verify --filter swiftcore --verbose; deno task verify:qml-roll --verbose --qt SwiftRollPlayhead::test_guidesResizeScrollAndOwnership SwiftRollPlayhead::test_followScroll
Result: passing - the swiftcore suite ran green and the rollqml lane ran SwiftRollPlayhead green.
Converted-window command: deno task verify:qml --verbose
The legacy playhead-guides C++ harness is a reference inventory; native window
and framebuffer obligations below are not claimed by the Swift presenter checks.

Execution ownership
runPlayheadFeatureChecks in src/checks/nativegraphics/PlayheadFeatureChecks.swift
executes checkPlayheadGuides, checkPlayheadFollow, checkPlayheadAppearance,
checkPlayheadDevicePixelRect, and checkPlayheadMiddlePan. The native C++ rig,
Quick item lookup, and framebuffer helpers remain outside this Swift evidence.

Fixture, sequence and row constraints
The static rig uses a fixed 90-by-65 space-unit window with no data rows.
devicePixelRect expands over DPR 1.0, 2.0, and 1.5 images. The chrome-pair
loop expands every hover/edit visibility and position site over all seven
bands (Ruler, Roll, Automation, Velocity, VoiceChanges, OtherEvents,
TrackHeaders). The resize sequence shrinks the Quick window by four spaces
and restores it; the scroll sequence offsets by one pxPerBeat and the follow
case parks scrollPx at 0 with 512 pxPerBeat.

Original assertion inventory and source sequence

A001 | staticRig | src/checks/nativegraphics/tst_playhead_guides.cpp:90
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface; RollQmlBootstrap.start is the fixture guard's Swift counterpart.
Source context since previous assertion:
  #include <QtTest>

  #include <QImage>
  #include <QObject>
  #include <QQuickItem>
  #include <QQuickWindow>
  #include <QRect>
  #include <QSize>
  #include <QString>
  #include <QStringList>

  #include <array>
  #include <cmath>
  #include <cstdint>
  #include <memory>
  #include <optional>
  #include <utility>

  #include "checks/nativegraphics/nativegraphics_fixture.h"
  #include "checks/support/quickframebuffer.h"
  #include "checks/support/songfixture.h"
  #include "checks/support/timelinequickcheck.h"
  #include "core/miditimeline.h"
  #include "ui/layout.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/timelinequickchrome.h"
  #include "ui/songview/quick/timelinequickview.h"

  namespace {

  class PlayheadGuidesTest final : public QObject
  {
      Q_OBJECT
      Q_DISABLE_COPY_MOVE(PlayheadGuidesTest)

    public:
      PlayheadGuidesTest(QString projectRoot, QString songLabel)
          : m_projectRoot(std::move(projectRoot))
          , m_songLabel(std::move(songLabel))
      {}

    private slots:
      void devicePixelRect();
      void guidesResizeScrollAndOwnership();
      void followScroll();

    private:
      QString m_projectRoot;
      QString m_songLabel;
  };

  QSize staticSize()
  {
      return QSize{90 * layout::space(layout::Space::One), 65 * layout::space(layout::Space::One)};
  }
  constexpr qreal kGuideTolerance = 0.2;

  struct ChromePair {
      const char *name;
      songview::TimelineChromeItem *hover = nullptr;
      songview::TimelineChromeItem *edit = nullptr;
  };

  std::array<ChromePair, 7> chromePairs(QQuickItem &root)
  {
      std::array<ChromePair, 7> pairs{{{"Ruler"},
                                       {"Roll"},
                                       {"Automation"},
                                       {"Velocity"},
                                       {"VoiceChanges"},
                                       {"OtherEvents"},
                                       {"TrackHeaders"}}};
      for (ChromePair &pair : pairs) {
          const QString prefix = QStringLiteral("timelineQuick") + QString::fromLatin1(pair.name);
          pair.hover =
              root.findChild<songview::TimelineChromeItem *>(prefix + QStringLiteral("HoverChrome"));
          pair.edit =
              root.findChild<songview::TimelineChromeItem *>(prefix + QStringLiteral("EditChrome"));
      }
      return pairs;
  }

  std::unique_ptr<checks::nativegraphics::Rig> staticRig(const QString &projectRoot,
                                                         const QString &songLabel)
  {
      QString error;
      std::unique_ptr<checks::nativegraphics::Rig> rig =
          checks::nativegraphics::makeRig(projectRoot, songLabel, staticSize(), error);
      if (!rig)
Original expression:
  QTest::qFail(qPrintable(error), __FILE__, __LINE__)

A002 | PlayheadGuidesTest::devicePixelRect | src/checks/nativegraphics/tst_playhead_guides.cpp:99
Disposition: MATCHED
Mapping/reason: swiftcore/PlayheadFeature::devicePixelRect (PlayheadFeatureChecks.swift::checkPlayheadDevicePixelRect) asserts the same DPR-1.0 identity mapping.
Source context since previous assertion:
  ;
      return rig;
  }

  void PlayheadGuidesTest::devicePixelRect()
  {
      const QRect logicalRect{3, 2, 5, 4};
      QImage dprOne{QSize{16, 10}, QImage::Format_ARGB32_Premultiplied};
      dprOne.setDevicePixelRatio(1.0);
Original expression:
  QCOMPARE(checks::support::devicePixelRect(dprOne, logicalRect), logicalRect)

A003 | PlayheadGuidesTest::devicePixelRect | src/checks/nativegraphics/tst_playhead_guides.cpp:104
Disposition: MATCHED
Mapping/reason: swiftcore/PlayheadFeature::devicePixelRect asserts the same DPR-2.0 doubling.
Source context since previous assertion:
  ;
      QImage dprTwo{QSize{32, 20}, QImage::Format_ARGB32_Premultiplied};
      dprTwo.setDevicePixelRatio(2.0);
      const QRect doubleRect{6, 4, 10, 8};
Original expression:
  QCOMPARE(checks::support::devicePixelRect(dprTwo, logicalRect), doubleRect)

A004 | PlayheadGuidesTest::devicePixelRect | src/checks/nativegraphics/tst_playhead_guides.cpp:110
Disposition: MATCHED
Mapping/reason: swiftcore/PlayheadFeature::devicePixelRect asserts the same DPR-1.5 outward rounding.
Source context since previous assertion:
  ;
      QImage dprOnePointFive{QSize{12, 14}, QImage::Format_ARGB32_Premultiplied};
      dprOnePointFive.setDevicePixelRatio(1.5);
      const QRect fractionalRect{4, 3, 8, 6};
      const QRect roundingRect{1, 1, 4, 4};
Original expression:
  QCOMPARE(checks::support::devicePixelRect(dprOnePointFive, logicalRect), fractionalRect)

A005 | PlayheadGuidesTest::devicePixelRect | src/checks/nativegraphics/tst_playhead_guides.cpp:111
Disposition: MATCHED
Mapping/reason: swiftcore/PlayheadFeature::devicePixelRect asserts the same DPR-1.5 fractional rounding.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(checks::support::devicePixelRect(dprOnePointFive, QRect{1, 1, 2, 2}), roundingRect)

A006 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:117
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface.
Source context since previous assertion:
  ;
  }

  void PlayheadGuidesTest::guidesResizeScrollAndOwnership()
  {
      std::unique_ptr<checks::nativegraphics::Rig> rig = staticRig(m_projectRoot, m_songLabel);
Original expression:
  QVERIFY(rig)

A007 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:120
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the check locates the sharedPlayhead item and both guide segment kinds on the real surface.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      auto *quick = view.quickView();
Original expression:
  QVERIFY(quick && quick->rootObject())

A008 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:124
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A008 and asserts both hover and edit guide ownership are attached.
Source context since previous assertion:
  ;
      QQuickItem *root = quick->rootObject();
      const std::array<ChromePair, 7> pairs = chromePairs(*root);
      for (const ChromePair &pair : pairs) {
Original expression:
  QVERIFY2(pair.hover && pair.edit, pair.name)

A009 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:129
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the roll guide segment's clipRect is the shared rollPlotRect on surface.timelineSplitX; the check asserts every band's guide sits on that origin.
Source context since previous assertion:
  ;
      }

      const std::optional<songview::TimelineBandGeometry> &ruler =
          view.timelineBandLayout().geometry(songview::TimelineBand::Ruler);
Original expression:
  QVERIFY(ruler)

A010 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:151
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A010 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      const auto songX = [&view, &ruler](uint64_t tick) {
          return qreal(ruler->plotRect.x()) + view.camera().contentX(tick);
      };
      // The Quick window is the full canonical viewport, so song-view x and
      // Quick-root x are the same coordinate; no host translation remains.
      const auto rootX = [](qreal x) { return x; };
      const auto allVisible = [&pairs](bool hover, bool edit) {
          for (const ChromePair &pair : pairs) {
              const bool bandVisible = pair.hover->parentItem()->isVisible();
              if (pair.hover->isVisible() != (hover && bandVisible) ||
                  pair.edit->isVisible() != (edit && bandVisible))
                  return false;
          }
          return true;
      };

      const qreal beforeStart = songX(0) - layout::singlePixel();
      view.clearTimelineQuickHover(songview::TimelineQuickHoverOwner::Automation);
      quick->synchronizeGuides(view.timelineSplitX(), beforeStart);
      quick->publishHover(songview::TimelineQuickHoverOwner::Automation, 0, beforeStart);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(!quick->hoverVisible())

A011 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:152
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A011 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!quick->editVisible())

A012 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:153
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: allGuidesVisible(false,false) asserts no band draws a guide after the edit cursor scrolls out and hover clears.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(allVisible(false, false))

A013 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:159
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A013 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      const uint64_t tick = uint64_t((std::max)(0.0, view.camera().tickAtContentX(1.0)));
      view.setEditCursorTick(0);
      quick->synchronizeGuides(view.timelineSplitX(), songX(0));
      checks::support::pumpQuick();
Original expression:
  QVERIFY(quick->editVisible())

A014 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:160
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A014 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!quick->hoverVisible())

A015 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:161
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: allGuidesVisible(false,true) asserts every band draws the edit guide.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(allVisible(false, true))

A016 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:162
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A016 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(quick->editRootContentX() - rootX(songX(0))) <= kGuideTolerance)

A017 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:167
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A017 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      // Hover replaces the edit guide; their relative z-order is not observable.
      quick->publishHover(songview::TimelineQuickHoverOwner::Automation, tick, songX(tick));
      checks::support::pumpQuick();
Original expression:
  QVERIFY(quick->hoverVisible())

A018 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:168
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: allGuidesVisible(true,false) asserts every band draws the hover guide.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(allVisible(true, false))

A019 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:169
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A019 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(quick->hoverRootContentX() - rootX(songX(tick))) <= kGuideTolerance)

A020 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:171
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A020 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      for (const ChromePair &pair : pairs) {
Original expression:
  QVERIFY(qAbs(checks::support::quickRootX(*pair.hover, *root) - rootX(songX(tick))) <=
                  kGuideTolerance)

A021 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:173
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A021 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(checks::support::quickRootX(*pair.edit, *root) - rootX(songX(0))) <=
                  kGuideTolerance)

A022 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:180
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A022 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      }

      view.publishTimelineQuickHover(songview::TimelineQuickHoverOwner::VoiceChanges, tick + 1);
      view.clearTimelineQuickHover(songview::TimelineQuickHoverOwner::Automation);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(quick->hoverVisible())

A023 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:181
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A023 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(quick->hoverRootContentX() - rootX(songX(tick + 1))) <= kGuideTolerance)

A024 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:184
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A024 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      view.clearTimelineQuickHover(songview::TimelineQuickHoverOwner::VoiceChanges);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(!quick->hoverVisible())

A025 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:185
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A025 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(quick->editVisible())

A026 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:186
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the surface resize reprojects the guides; the check asserts the edit guide still projects the cursor tick.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(allVisible(false, true))

A027 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:190
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership: the surface resize restores and the guides realign.
Source context since previous assertion:
  ;
      // Window-level resize stands in for the widget envelope: the canonical
      // viewport itself shrinks and restores, and guide alignment survives.
      QQuickWindow *window = quick->quickWindow();
Original expression:
  QVERIFY(window)

A028 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:196
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A028 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      const QSize originalSize = window->size();
      const QSize shrunkSize(originalSize.width(),
                             originalSize.height() - 4 * layout::space(layout::Space::One));
      window->resize(shrunkSize);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(qAbs(quick->editRootContentX() - rootX(songX(0))) <= kGuideTolerance)

A029 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:204
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A029 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
      window->resize(originalSize);
      checks::support::pumpQuick();

      const SongView::ViewState original = view.viewState();
      const qreal editBeforeScroll = quick->editRootContentX();
      view.setEditorHorizontalScroll(original.scrollPx + view.camera().pxPerBeat());
      checks::support::pumpQuick();
Original expression:
  QVERIFY(view.camera().scrollX() != original.scrollPx)

A030 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:205
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A030 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(quick->editRootContentX() != editBeforeScroll)

A031 | PlayheadGuidesTest::guidesResizeScrollAndOwnership | src/checks/nativegraphics/tst_playhead_guides.cpp:206
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadGuides emits cppId swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::A031 and asserts the same presenter guide contract.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(quick->editRootContentX() - rootX(songX(0))) <= kGuideTolerance)

A032 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:212
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_followScroll: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface.
Source context since previous assertion:
  ;
  }

  void PlayheadGuidesTest::followScroll()
  {
      std::unique_ptr<checks::nativegraphics::Rig> rig = staticRig(m_projectRoot, m_songLabel);
Original expression:
  QVERIFY(rig)

A033 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:220
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadFollow emits cppId swiftcore/PlayheadFeature::followScroll::A033 and asserts the same presenter follow contract.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      SongView::ViewState parked = view.viewState();
      parked.scrollPx = 0.0;
      parked.pxPerBeat = 512.0;
      view.applyViewState(parked);
      view.setFollowPlayhead(true);
      const uint64_t endTick = rig->song->timeline().lengthTicks;
Original expression:
  QVERIFY(endTick > 1)

A034 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:222
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_followScroll: the check locates the sharedPlayhead item and the roll segment on the real surface.
Source context since previous assertion:
  ;
      auto *quick = view.quickView();
Original expression:
  QVERIFY(quick && quick->quickWindow())

A035 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:226
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadFollow emits cppId swiftcore/PlayheadFeature::followScroll::A035 and asserts the same presenter follow contract.
Source context since previous assertion:
  ;
      const uint64_t farTick =
          (std::min)(endTick - 1,
                     uint64_t(quick->quickWindow()->width() * 4.0 / view.camera().pxPerTick()) + 1);
Original expression:
  QVERIFY(farTick > 0)

A036 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:230
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadFollow emits cppId swiftcore/PlayheadFeature::followScroll::A036 and asserts the same presenter follow contract.
Source context since previous assertion:
  ;
      const uint64_t sample = rig->song->timeline().sampleForTick(farTick);
      view.setPlayheadSample(sample, true);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(view.camera().scrollX() > 0.0)

A037 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:236
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadFollow emits cppId swiftcore/PlayheadFeature::followScroll::A037 and asserts the same presenter follow contract.
Source context since previous assertion:
  ;
      view.applyViewState(parked);
      view.setFollowPlayhead(false);
      view.setPlayheadSample(sample, true);
      checks::support::pumpQuick();
Original expression:
  QCOMPARE(view.camera().scrollX(), 0.0)

A038 | PlayheadGuidesTest::followScroll | src/checks/nativegraphics/tst_playhead_guides.cpp:240
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/nativegraphics/PlayheadFeatureChecks.swift::checkPlayheadFollow emits cppId swiftcore/PlayheadFeature::followScroll::A038 and asserts the same presenter follow contract.
Source context since previous assertion:
  ;
      view.setFollowPlayhead(true);
      view.setPlayheadSample(sample, true);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(view.camera().scrollX() > 0.0)

Remaining original source (closing sequence and helper bodies):
  ;
  }

  } // namespace

  int runPlayheadGuidesCheck(const QString &projectRoot, const QString &songLabel,
                             const QStringList &qtArguments)
  {
      PlayheadGuidesTest test{projectRoot, songLabel};
      QStringList arguments{QStringLiteral("playhead-guides")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

  #include "tst_playhead_guides.moc"

Swift assertion predicates
Swift assertion predicates
PlayheadFeatureChecks.swift (registered by runPlayheadFeatureChecks)
- checkPlayheadGuides -> swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::{A008,A010,A011,A013,A014,A016,A017,A019-A025,A028-A031,viewport-edge,detach}
- checkPlayheadFollow -> swiftcore/PlayheadFeature::followScroll::{A033,A035-A038}
- checkPlayheadAppearance -> swiftcore/PlayheadFeature::appearanceMetrics::{paused-glow,paused-alpha,playing-glow,playing-alpha,triangle-metrics,base-font-reprojection}
- checkPlayheadDevicePixelRect -> swiftcore/PlayheadFeature::devicePixelRect::{A001-A004}
- checkPlayheadMiddlePan -> swiftcore/PlayheadFeature::middlePan::{begin,camera-delta,active,follow-suspended,end,cancel}

Window/item predicates (rollqml lane, src/checks/rollqml/tst_SwiftRollPlayhead.qml):
- test_guidesResizeScrollAndOwnership -> swiftroll-window/SwiftRollPlayhead::test_guidesResizeScrollAndOwnership
  covers A001, A006, A007, A009, A012, A015, A018, A026, A027 (fixture open,
  per-band guide visibility/geometry, owner arbitration, resize, scroll).
- test_followScroll -> swiftroll-window/SwiftRollPlayhead::test_followScroll
  covers A032, A034 (fixture open, camera follow through the real presenter).
- devicePixelRect A002-A005 -> swiftcore/PlayheadFeature::devicePixelRect.
