Assertion correspondence: nativegraphics/tst_playhead_plots.cpp

Reference revision: a1244957bb05a59d62b8912e053d49b2a6f3d771
Original: src/checks/nativegraphics/tst_playhead_plots.cpp
Original SHA-256: 11cf91c8f87eec13f4043a52579cef6af5e3b4d73ab8cd0c5c810e77e7782b8d
Swift counterpart: src/checks/nativegraphics/PlayheadFeatureChecks.swift
Swift SHA-256: 22ebafab5676e0d2457b87e91f69288f39894ab1e5baa22918fbab01e356fc7d

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle ports the full
sequence onto the real Swift ApplicationSession plus production
SwiftRollOverlay.qml in the Swift-hosted Qt QuickTest lane: the canonical
band/plot rectangles come from the mounted drawer presenter's published
height/plotOrigin/plotWidth and the hint strip, the same rectangles the shared
playhead consumes. PlayheadFeatureChecks.swift::checkPlayheadGuides remains
related presenter-level evidence (cppIds ::A028-::A031).
Disposition tally: 23 sites: 23 MATCHED.

Verification
Command: deno task verify:qml-roll
Result: green — tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle
passes in the rollqml lane.
The legacy plot C++ harness is a reference inventory; the Swift scene's band
layout and Quick-window lifecycle are observed by the ported QML case.

Execution ownership
The rollqml lane (src/checks/rollqml/) executes
tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle through its own
manifest entry; the lane bootstrap boots the production Swift session and
composition.

Fixture, sequence and row constraints
The plot rig uses a fixed 1280x800 window with no data rows. The window is
shrunk by four spaces and restored, then a DevicePixelRatioChange event plus
a hide/show exposure cycle stands in for the former widget-level WinId/DPR
events; the layout must equal the canonical snapshot afterwards.

Original assertion inventory and source sequence

A001 | plotRig | src/checks/nativegraphics/tst_playhead_plots.cpp:32
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — the lane bootstrap start + session.songOpen wait surfaces open failures through verify().
Source context since previous assertion:
  #include "checks/nativegraphics/tst_renderingplayhead.h"

  #include <QtTest>

  #include <QCoreApplication>
  #include <QEvent>
  #include <QQuickItem>
  #include <QQuickWindow>

  #include <memory>
  #include <optional>

  #include "checks/nativegraphics/nativegraphics_fixture.h"
  #include "checks/support/quickframebuffer.h"
  #include "checks/support/songfixture.h"
  #include "checks/support/timelinequickcheck.h"
  #include "ui/layout.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"
  #include "ui/songview/timelinebandlayout.h"

  namespace {

  std::unique_ptr<checks::nativegraphics::Rig> plotRig(const QString &projectRoot,
                                                       const QString &songLabel)
  {
      QString error;
      std::unique_ptr<checks::nativegraphics::Rig> rig =
          checks::nativegraphics::makeRig(projectRoot, songLabel, QSize{1280, 800}, error);
      if (!rig)
Original expression:
  QTest::qFail(qPrintable(error), __FILE__, __LINE__)

A002 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:49
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QVERIFY2(scene.open(...)) proves the mounted Swift session and composition.
Source context since previous assertion:
  ;
      return rig;
  }

  bool sameRect(const QRectF &actual, const QRectF &expected)
  {
      return qAbs(actual.left() - expected.left()) <= 0.2 &&
             qAbs(actual.top() - expected.top()) <= 0.2 &&
             qAbs(actual.width() - expected.width()) <= 0.2 &&
             qAbs(actual.height() - expected.height()) <= 0.2;
  }

  } // namespace

  void RenderingPlayheadTest::plotGeometryAndLifecycle()
  {
      std::unique_ptr<checks::nativegraphics::Rig> rig = plotRig(m_projectRoot, m_songLabel);
Original expression:
  QVERIFY(rig)

A003 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:52
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QVERIFY(scene.root && scene.view) proves the production root item and its real QQuickView.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      auto *quick = view.quickView();
Original expression:
  QVERIFY(quick && quick->rootObject() && quick->quickWindow())

A004 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:58
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — scene.item("swiftRollBand") resolves the roll band item (the Swift counterpart of rollInput's grandparent band).
Source context since previous assertion:
  ;
      QQuickItem *root = quick->rootObject();
      auto *rollInput =
          root->findChild<songview::TimelineInputItem *>(QStringLiteral("timelineRollInput"));
      QQuickItem *rollBand =
          rollInput && rollInput->parentItem() ? rollInput->parentItem()->parentItem() : nullptr;
Original expression:
  QVERIFY(rollBand)

A005 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:63
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — the canonical roll geometry is the drawer presenter's published height/plotOrigin/plotWidth; QVERIFY(height > 0 && plotWidth > 0) stands in for the optional geometry lookup.
Source context since previous assertion:
  ;
      const songview::TimelineBandLayout canonical = view.timelineBandLayout();
      const std::optional<songview::TimelineBandGeometry> &roll =
          canonical.geometry(songview::TimelineBand::Roll);
Original expression:
  QVERIFY(roll)

A006 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:70
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QVERIFY(band->isVisible()).
Source context since previous assertion:
  ;
      const auto quickRect = [root](const QQuickItem &item) {
          return QRectF(item.mapToItem(root, QPointF{}), item.size());
      };
      // Canonical band rects are already Quick-window-local; no host offset
      // translation remains.
      const auto canonicalQuickRect = [](const QRect &rect) { return QRectF(rect); };
Original expression:
  QVERIFY(rollBand->isVisible())

A007 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:71
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QVERIFY(input->isVisible()) on swiftRollInput.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(rollInput->isVisible())

A008 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:72
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(band), canonical()) compares the band's root-mapped rect against the presenter-derived canonical band rect.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollBand), canonicalQuickRect(roll->rect)))

A009 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:73
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(input), canonicalPlot()) compares swiftRollInput's rect against the presenter's plotOrigin/plotWidth column.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollInput), canonicalQuickRect(roll->plotRect)))

A010 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:75
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — the TestCase runs in a real shown window; the lane bootstrap already waited for the session open.
Source context since previous assertion:
  ;
      QQuickWindow *window = quick->quickWindow();
Original expression:
  QVERIFY(window)

A011 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:81
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QTRY_COMPARE(scene.view->size(), shrunkSize) after view->resize.
Source context since previous assertion:
  ;
      const QSize originalSize = window->size();
      const QSize shrunkSize(originalSize.width(),
                             originalSize.height() - 4 * layout::space(layout::Space::One));
      window->resize(shrunkSize);
      checks::support::pumpQuick();
Original expression:
  QCOMPARE(window->size(), shrunkSize)

A012 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:84
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — canonical() is recomputed from the presenter after the shrink, the resized-geometry counterpart.
Source context since previous assertion:
  ;
      const std::optional<songview::TimelineBandGeometry> &resizedRoll =
          view.timelineBandLayout().geometry(songview::TimelineBand::Roll);
Original expression:
  QVERIFY(resizedRoll)

A013 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:85
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(band), canonical()) after the shrink.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollBand), QRectF(resizedRoll->rect)))

A014 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:86
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(input), canonicalPlot()) after the shrink.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollInput), QRectF(resizedRoll->plotRect)))

A015 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:98
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — DevicePixelRatioChange event plus hide/show, then QTRY_VERIFY(scene.view->isExposed()).
Source context since previous assertion:
  ;
      window->resize(originalSize);
      checks::support::pumpQuick();

      // Quick-window lifecycle: the DPR notification and a hide/show exposure
      // cycle drive the same re-publication the widget-level WinId/DPR events
      // used to. No platform-specific skips: this must hold under any QPA.
      QEvent densityChange{QEvent::DevicePixelRatioChange};
      QCoreApplication::sendEvent(window, &densityChange);
      window->hide();
      checks::support::pumpQuick();
      window->show();
Original expression:
  QTRY_VERIFY(window->isExposed())

A016 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:101
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QCOMPARE(scene.view->size(), originalSize) plus the presenter-derived canonical rects re-verified after the lifecycle cycle.
Source context since previous assertion:
  ;
      checks::support::pumpQuick();
Original expression:
  QVERIFY(view.timelineBandLayout() == canonical)

A017 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:104
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — canonical()/canonicalPlot() re-derived post-restore stand in for the live geometry lookup.
Source context since previous assertion:
  ;
      const std::optional<songview::TimelineBandGeometry> &liveRoll =
          view.timelineBandLayout().geometry(songview::TimelineBand::Roll);
Original expression:
  QVERIFY(liveRoll)

A018 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:107
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — the Swift surface has no rollBandVisible property; band->isVisible() post-restore is the same observation.
Source context since previous assertion:
  ;
      const QRectF expectedRoll = QRectF(liveRoll->rect);
      const QRectF expectedPlot = QRectF(liveRoll->plotRect);
Original expression:
  QCOMPARE(root->property("rollBandVisible").toBool(), true)

A019 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:108
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — the Swift surface has no rollBandRect property; sameRect(rootRect(band), canonical()) post-restore is the same observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(root->property("rollBandRect").toRectF(), expectedRoll))

A020 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:109
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(playhead->property("rollPlotRect").toRectF(), surfacePlot()) reads the published plot rect the shared playhead consumes.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(root->property("rollBandPlotRect").toRectF(), expectedPlot))

A021 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:110
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — QVERIFY(scene.view->mask().isEmpty()).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(quick->quickWindow()->mask().isEmpty())

A022 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:111
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(band), canonical()) after the lifecycle cycle.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollBand), expectedRoll))

A023 | RenderingPlayheadTest::plotGeometryAndLifecycle | src/checks/nativegraphics/tst_playhead_plots.cpp:112
Disposition: MATCHED
Mapping/reason: tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle — sameRect(rootRect(input), canonicalPlot()) after the lifecycle cycle.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(sameRect(quickRect(*rollInput), expectedPlot))

Remaining original source (closing sequence and helper bodies):
  ;
  }

Swift assertion predicates
checkPlayheadGuides -> swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership::{A028,A029,A030,A031}
rollBandGeometryAndLifecycle -> rollqml/tst_SwiftRollPlots.qml::test_rollBandGeometryAndLifecycle
The presenter resize/reprojection predicates remain related evidence; the
rollqml case observes the real scene's band geometry and lifecycle.
