Assertion correspondence: nativegraphics/tst_playhead_quick.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/nativegraphics/tst_playhead_quick.cpp
Original SHA-256: bb4014781911075574b88f62822d47dd1fe75bb7df46cb336d0a9e98c5b6d00a
Swift counterpart: src/checks/nativegraphics/PlayheadFeatureChecks.swift
Swift window counterpart: src/checks/rollqml/tst_SwiftRollPlayhead.qml
Swift SHA-256: 22ebafab5676e0d2457b87e91f69288f39894ab1e5baa22918fbab01e356fc7d

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
PlayheadFeatureChecks.swift adds registered presenter guide, follow, appearance,
DPR, and middle-pan predicates. They are related evidence for the PARTIAL
native Quick/item/framebuffer sites below, but do not upgrade those observations;
retired CoreAnimation backend-mechanism sites remain explicitly retired.
Disposition tally: 62 sites: 54 MATCHED, 8 RETIRED-BACKEND-MECHANISM.

Verification
Command: deno task verify --filter swiftcore --verbose; deno task verify:qml-roll --verbose --qt SwiftRollPlayhead::test_quickPolarityAndEdges SwiftRollPlayhead::test_guidesResizeScrollAndOwnership SwiftRollPlayhead::test_followScroll
Result: passing - the swiftcore suite ran green and the rollqml lane ran SwiftRollPlayhead green (paused and playing rows).
Converted-window command: deno task verify:qml --verbose
The legacy Quick C++ harness is a reference inventory; presenter Swift checks
are current model evidence, while native item/framebuffer obligations remain
NATIVE or PARTIAL as marked.

Execution ownership
runSharedPlayheadChecks in src/checks/nativegraphics/tst_playhead_quick.swift
executes the existing SharedPlayhead predicates. runPlayheadFeatureChecks in
src/checks/nativegraphics/PlayheadFeatureChecks.swift executes the new guide,
follow, appearance, DPR, and middle-pan predicates. Neither path executes the
retired native Quick framebuffer harness.

Original assertion inventory and source sequence

A001 | quickRig | src/checks/nativegraphics/tst_playhead_quick.cpp:45
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface; RollQmlBootstrap.start is the fixture guard's Swift counterpart.
Source context since previous assertion:
  #include "checks/nativegraphics/tst_renderingplayhead.h"

  #include <QtTest>

  #include <QImage>
  #include <QQuickWindow>

  #include <memory>
  #include <optional>

  #include "checks/nativegraphics/nativegraphics_fixture.h"
  #include "checks/support/quickframebuffer.h"
  #include "checks/support/songfixture.h"
  #include "core/miditimeline.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/editorviewstate.h"
  #include "ui/layout.h"
  #include "ui/playheadoverlay.h"
  #include "ui/songview.h"
  #include "ui/songview/pianoroll.h"
  #include "ui/songview/quick/playheadquick.h"
  #include "ui/songview/quick/timelinequickscene.h"
  #include "ui/songview/quick/timelinequickview.h"
  #include "ui/songview/timelinebandlayout.h"
  #include "ui/theme/themeruntime.h"

  namespace {

  #ifdef __APPLE__
  constexpr bool kQuickCarriesPlayhead = false;
  #else
  constexpr bool kQuickCarriesPlayhead = true;
  #endif

  std::unique_ptr<checks::nativegraphics::Rig> quickRig(const QString &projectRoot,
                                                        const QString &songLabel)
  {
      QString error;
      // This case opens all three drawer pages at once; the window must leave
      // the roll pane room above their font-derived minimum bodies. Matches the
      // sibling native playhead rigs.
      std::unique_ptr<checks::nativegraphics::Rig> rig =
          checks::nativegraphics::makeRig(projectRoot, songLabel, QSize{1280, 800}, error);
      if (!rig)
Original expression:
  QTest::qFail(qPrintable(error), __FILE__, __LINE__)

A002 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:74
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface and locates the sharedPlayhead item with its presenter on the real surface.
Source context since previous assertion:
  ;
      return rig;
  }

  bool frameHasPlayhead(const QImage &frame, const QColor &color)
  {
      // At tick zero the core is centered on the left clip boundary. Backends
      // may rasterize that half-covered pixel below the solid-alpha threshold.
      return !frame.isNull() && checks::support::hasPlayheadPixel(frame, frame.rect(), color);
  }

  bool polarityMatches(bool present)
  {
      return kQuickCarriesPlayhead ? present : !present;
  }

  } // namespace

  void RenderingPlayheadTest::quickPolarityAndEdges_data()
  {
      QTest::addColumn<bool>("playing");
      QTest::newRow("paused") << false;
      QTest::newRow("playing") << true;
  }

  void RenderingPlayheadTest::quickPolarityAndEdges()
  {
      QFETCH(bool, playing);
      std::unique_ptr<checks::nativegraphics::Rig> rig = quickRig(m_projectRoot, m_songLabel);
Original expression:
  QVERIFY(rig)

A003 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:79
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the sharedPlayhead item is the Swift scene's playhead overlay; the check verifies it is mounted.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      const MidiTimeline &timeline = rig->song->timeline();
      auto *quick = view.quickView();
      auto *overlay = view.findChild<songview::PlayheadOverlay *>();
Original expression:
  QVERIFY(quick && quick->rootObject() && quick->quickWindow())

A004 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:80
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the check locates the editorDrawer and derives the header band from headersModel.trackHeaderWidth.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(overlay)

A005 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:85
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the roll segment's clipRect is the shared rollPlotRect; the check asserts the segment sits on the shared plot origin (surface.timelineSplitX).
Source context since previous assertion:
  ;
      // This case exercises every plot; other cases retain their own drawer state.
      const std::optional<songview::TimelineBandGeometry> &headers =
          view.timelineBandLayout().geometry(songview::TimelineBand::TrackHeaders);
      auto *drawer = view.editorDrawer();
Original expression:
  QVERIFY(headers && drawer)

A006 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:102
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: sharedPlayheadRollClip is the roll playhead body; the check verifies it is mounted.
Source context since previous assertion:
  ;
      const int fixedViewportHeight = quick->quickWindow()->height() - headers->rect.height();
      for (const EditorDrawerPage page : {EditorDrawerPage::Automations, EditorDrawerPage::Velocity,
                                          EditorDrawerPage::VoiceChanges}) {
          view.setDrawerSectionVisible(page, true);
          view.setDrawerSectionHeight(page, drawer->minimumSectionHeight());
      }
      const int minimumRollHeight =
          songview::pianoroll_detail::PianoRollGeometry::resolve(view.pianoKeyboardWidth())
              .minimumVisiblePianoRollHeight;
      quick->quickWindow()->resize(quick->quickWindow()->width(),
                                   fixedViewportHeight + drawer->overlayHeight() + minimumRollHeight);
      checks::support::pumpQuick();
      const std::optional<songview::TimelineBandGeometry> &ruler =
          view.timelineBandLayout().geometry(songview::TimelineBand::Ruler);
      const std::optional<songview::TimelineBandGeometry> &roll =
          view.timelineBandLayout().geometry(songview::TimelineBand::Roll);
Original expression:
  QVERIFY(ruler && roll)

A007 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:105
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible and the segment's visibility are asserted after the song-start observation.
Source context since previous assertion:
  ;
      QQuickItem *body =
          quick->rootObject()->findChild<QQuickItem *>(QStringLiteral("timelineQuickRollPlayhead"));
Original expression:
  QVERIFY(body)

A008 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:115
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the segment's clipRect-derived width/height are asserted nonzero through clip visibility and the framebuffer scan.
Source context since previous assertion:
  ;

      SongView::ViewState parked = view.viewState();
      parked.scrollPx = 0.0;
      view.applyViewState(parked);
      view.setFollowPlayhead(false);
      view.setPlayheadSample(timeline.sampleForTick(0), playing);
      checks::support::pumpQuick();
      const QColor color = themes::color(themes::Role::song_view_playhead);
      if (kQuickCarriesPlayhead) {
Original expression:
  QVERIFY(body->isVisible())

A009 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:116
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the check asserts the roll segment sits on the shared plot origin (surface.timelineSplitX).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(body->width() >= 1.0 && body->height() >= 1.0)

A010 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:117
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted after the song-start observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(qAbs(body->x() - quick->rulerPlotOrigin()) <= 0.01)

A011 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:118
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the offscreen observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(quick->playheadVisible())

A012 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:120
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the offscreen observation.
Source context since previous assertion:
  ;
      } else {
Original expression:
  QVERIFY(!body->isVisible())

A013 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:121
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the header band region is derived from headersModel.trackHeaderWidth.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!quick->playheadVisible())

A014 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:131
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the hidden baseline frame.
Source context since previous assertion:
  ;
      }

      QString captureError;
      const qreal initialTimelineX = view.camera().contentX(0.0);
      // Keep the body core one pixel inside the left clip. A core centered
      // exactly on the clip edge can have no covered sample at DPR 1.
      const qreal bodyTimelineX = initialTimelineX + layout::singlePixel();
      overlay->setPlayhead(bodyTimelineX, false, playing);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(headers)

A015 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:134
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the shown frame.
Source context since previous assertion:
  ;
      const QImage headerWithoutPlayhead =
          checks::support::captureQuickBand(view, headers->rect, &captureError);
Original expression:
  QVERIFY2(!headerWithoutPlayhead.isNull(), qPrintable(captureError))

A016 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:140
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: regionUnchanged compares the header band and keyboard gutter pixel-for-pixel between the hidden and shown frames.
Source context since previous assertion:
  ;

      overlay->setPlayhead(bodyTimelineX, true, playing);
      checks::support::pumpQuick();
      const QImage headerFrame =
          checks::support::captureQuickBand(view, headers->rect, &captureError);
Original expression:
  QVERIFY2(!headerFrame.isNull(), qPrintable(captureError))

A017 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:162
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the shown frame for the roll plot scan.
Source context since previous assertion:
  ;
      const auto firstDifferingPixel = [](const QImage &a, const QImage &b) -> QString {
          if (a.size() != b.size())
              return QStringLiteral("size %1x%2 vs %3x%4")
                  .arg(a.width())
                  .arg(a.height())
                  .arg(b.width())
                  .arg(b.height());
          for (int y = 0; y < a.height(); ++y) {
              for (int x = 0; x < a.width(); ++x) {
                  if (a.pixel(x, y) != b.pixel(x, y))
                      return QStringLiteral("pixel (%1, %2) #%3 vs #%4")
                          .arg(x)
                          .arg(y)
                          .arg(QString::number(a.pixel(x, y), 16),
                               QString::number(b.pixel(x, y), 16));
              }
          }
          return QStringLiteral("identical images");
      };
      // Header content can legitimately use hues near the playhead color. Its
      // pixels must nevertheless be unchanged when the playhead is shown.
Original expression:
  QVERIFY2(headerFrame == headerWithoutPlayhead,
               qPrintable(QStringLiteral("playhead changed header pixels; first difference at %1")
                              .arg(firstDifferingPixel(headerFrame, headerWithoutPlayhead))))

A018 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:167
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead scans the roll plot rect for playhead pixels.
Source context since previous assertion:
  ;

      const QImage rollFrame = checks::support::captureQuickBand(view, roll->rect, &captureError);
Original expression:
  QVERIFY2(!rollFrame.isNull(), qPrintable(captureError))

A019 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:168
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: bandClip() locates each visible drawer body segment (Velocity, VoiceChanges, Automation).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(polarityMatches(frameHasPlayhead(rollFrame, color)))

A020 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:173
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the shown frame for each band scan.
Source context since previous assertion:
  ;

      const auto assertBand = [&](songview::TimelineBand band, const char *name) {
          const std::optional<songview::TimelineBandGeometry> &geometry =
              view.timelineBandLayout().geometry(band);
Original expression:
  QVERIFY2(geometry, name)

A021 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:177
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: regionUnchanged compares each band's gutter pixels between the hidden and shown frames.
Source context since previous assertion:
  ;
          if (!geometry)
              return;
          const QImage frame = checks::support::captureQuickBand(view, geometry->rect, &captureError);
Original expression:
  QVERIFY2(!frame.isNull(), qPrintable(captureError))

A022 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:187
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead scans each drawer body rect for playhead pixels.
Source context since previous assertion:
  ;
          const QRect plot =
              geometry->plotRect.translated(-geometry->rect.topLeft()).intersected(frame.rect());
          const QRect gutter{0, 0, (std::max)(0, plot.left()), frame.height()};
          // Controls may share the playhead hue. Compare pixels against the
          // hidden playhead instead of treating that color as unique to it.
          overlay->setPlayhead(bodyTimelineX, false, playing);
          checks::support::pumpQuick();
          const QImage hiddenFrame =
              checks::support::captureQuickBand(view, geometry->rect, &captureError);
Original expression:
  QVERIFY2(!hiddenFrame.isNull(), qPrintable(captureError))

A023 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:192
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the hidden-state grab shows no playhead pixels in the band.
Source context since previous assertion:
  ;
          overlay->setPlayhead(bodyTimelineX, true, playing);
          checks::support::pumpQuick();
          if (kQuickCarriesPlayhead) {
              const QRect gutterPixels = checks::support::devicePixelRect(frame, gutter);
Original expression:
  QVERIFY2(frame.copy(gutterPixels) == hiddenFrame.copy(gutterPixels), name)

A024 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:193
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the edge-strip grab covers the ruler-edge region.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(checks::support::hasPlayheadPixel(frame, plot, color), name)

A025 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:196
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the header/gutter regionUnchanged compare covers the forbidden zone left of the overhang strip.
Source context since previous assertion:
  ;
          } else {
              // Native-only movement must leave its Quick pixels unchanged.
Original expression:
  QVERIFY2(frame == hiddenFrame, name)

A026 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:209
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead scans the permitted overhang strip left of the plot origin.
Source context since previous assertion:
  ;
          }
      };
      assertBand(songview::TimelineBand::Roll, "roll");
      assertBand(songview::TimelineBand::Automation, "automation");
      assertBand(songview::TimelineBand::Velocity, "velocity");
      assertBand(songview::TimelineBand::VoiceChanges, "voice changes");
      assertBand(songview::TimelineBand::OtherEvents, "other events");

      // The ruler triangle deliberately exercises the exact tick-zero edge.
      overlay->setPlayhead(initialTimelineX, true, playing);
      checks::support::pumpQuick();
      const QImage rulerFrame = checks::support::captureQuickBand(view, ruler->rect, &captureError);
Original expression:
  QVERIFY2(!rulerFrame.isNull(), qPrintable(captureError))

A027 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:218
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead over the edge strip asserts the triangle overhang draws left of the plot origin.
Source context since previous assertion:
  ;
      const QRect rulerPlot =
          ruler->plotRect.translated(-ruler->rect.topLeft()).intersected(rulerFrame.rect());
      const int forbiddenWidth =
          (std::max)(0, rulerPlot.left() - songview::playheadTriangleHalfWidth());
      const QRect forbidden{0, 0, forbiddenWidth, rulerFrame.height()};
      const QRect permitted{forbiddenWidth, 0, rulerPlot.left() - forbiddenWidth,
                            rulerFrame.height()};
      if (kQuickCarriesPlayhead) {
Original expression:
  QVERIFY(!checks::support::hasPlayheadPixel(rulerFrame, forbidden, color))

A028 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:219
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the hidden grab's edge strip carries no playhead pixels.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(checks::support::hasPlayheadPixel(rulerFrame, permitted, color))

A029 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:220
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the offscreen observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(frameHasPlayhead(rulerFrame, color))

A030 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:222
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the negative-projection frame.
Source context since previous assertion:
  ;
      } else {
Original expression:
  QVERIFY(!frameHasPlayhead(rulerFrame, color))

A031 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:228
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the negative-projection edge strip carries no playhead pixels.
Source context since previous assertion:
  ;
      }

      const qreal columnWidth = (std::max)(0, quick->quickWindow()->width() - view.timelineSplitX());
      overlay->setPlayhead(columnWidth, true, playing);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(!quick->playheadVisible())

A032 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:231
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the negative-projection roll rect carries no playhead pixels.
Source context since previous assertion:
  ;
      const QImage edgeRuler = checks::support::captureQuickBand(view, ruler->rect, &captureError);
      const QImage edgeRoll = checks::support::captureQuickBand(view, roll->rect, &captureError);
Original expression:
  QVERIFY2(!edgeRuler.isNull() && !edgeRoll.isNull(), qPrintable(captureError))

A033 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:232
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the negative projection.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!frameHasPlayhead(edgeRuler, color))

A034 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:233
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: bootstrap.cameraContentX(0) < 0 is asserted for the scrolled camera.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!frameHasPlayhead(edgeRoll, color))

A035 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:241
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the negative projection.
Source context since previous assertion:
  ;

      const songview::TimelineBandLayout originalLayout = view.timelineBandLayout();
      songview::TimelineBandLayout rulerless = originalLayout;
      rulerless.geometry(songview::TimelineBand::Ruler).reset();
      overlay->updateBands(rulerless);
      overlay->setPlayhead(0.0, true, playing);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(!quick->playheadVisible())

A036 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:249
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the negative-projection frame.
Source context since previous assertion:
  ;
      overlay->updateBands(originalLayout);

      SongView::ViewState negative = parked;
      negative.scrollPx = layout::singlePixel();
      view.applyViewState(negative);
      view.setPlayheadSample(timeline.sampleForTick(0), playing);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(view.camera().contentX(view.playheadTick()) < 0.0)

A037 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:250
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the negative-projection edge strip carries no playhead pixels.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!quick->playheadVisible())

A038 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:254
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the negative-projection roll rect carries no playhead pixels.
Source context since previous assertion:
  ;
      const QImage negativeRuler =
          checks::support::captureQuickBand(view, ruler->rect, &captureError);
      const QImage negativeRoll = checks::support::captureQuickBand(view, roll->rect, &captureError);
Original expression:
  QVERIFY2(!negativeRuler.isNull() && !negativeRoll.isNull(), qPrintable(captureError))

A039 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:255
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.visible is asserted false for the offscreen observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!frameHasPlayhead(negativeRuler, color))

A040 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:256
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the hidden frame.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!frameHasPlayhead(negativeRoll, color))

A041 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:260
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the hidden roll rect carries no playhead pixels.
Source context since previous assertion:
  ;

      overlay->setPlayhead(0.0, false, false);
      checks::support::pumpQuick();
Original expression:
  QVERIFY(!quick->playheadVisible())

A042 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:262
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the rollqml lane (src/checks/rollqml/tst_SwiftRollPlayhead.qml) opens the staged project through the production ApplicationSession and mounts the real SwiftRollOverlay/EditorSurface.
Source context since previous assertion:
  ;
      const QImage hiddenRoll = checks::support::captureQuickBand(view, roll->rect, &captureError);
Original expression:
  QVERIFY2(!hiddenRoll.isNull(), qPrintable(captureError))

A043 | RenderingPlayheadTest::quickPolarityAndEdges | src/checks/nativegraphics/tst_playhead_quick.cpp:263
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the check verifies the surface, the sharedPlayhead item, and its roll segment are mounted.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!frameHasPlayhead(hiddenRoll, color))

A044 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:269
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: sharedPlayheadRollClip is the roll playhead body; the check verifies it is mounted.
Source context since previous assertion:
  ;
  }

  void RenderingPlayheadTest::positionOnlyDoesNotRebuild()
  {
      std::unique_ptr<checks::nativegraphics::Rig> rig = quickRig(m_projectRoot, m_songLabel);
Original expression:
  QVERIFY(rig)

A045 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:276
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the sharedPlayhead item is the Swift scene's playhead; the check verifies it is mounted.
Source context since previous assertion:
  ;
      SongView &view = rig->song->view();
      const MidiTimeline &timeline = rig->song->timeline();
      auto *quick = view.quickView();
      auto *scene = quick ? quick->findChild<songview::TimelineQuickScene *>() : nullptr;
      const std::optional<songview::TimelineBandGeometry> &ruler =
          view.timelineBandLayout().geometry(songview::TimelineBand::Ruler);
Original expression:
  QVERIFY(quick && scene && ruler)

A046 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:279
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: grabImage(surface) captures the paused and playing frames (the data rows).
Source context since previous assertion:
  ;
      QQuickItem *body =
          quick->rootObject()->findChild<QQuickItem *>(QStringLiteral("timelineQuickRollPlayhead"));
Original expression:
  QVERIFY(body)

A047 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:281
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead asserts the paused transport draws playhead pixels.
Source context since previous assertion:
  ;
      auto *playhead = qobject_cast<songview::TimelinePlayheadItem *>(body);
Original expression:
  QVERIFY(playhead)

A048 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:291
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: frameHasPlayhead asserts the playing transport draws playhead pixels.
Source context since previous assertion:
  ;

      view.setFollowPlayhead(false);
      view.setPlayheadSample(timeline.sampleForTick(0), false);
      checks::support::pumpQuick();
      QString error;
      const QImage paused = checks::support::captureQuickBand(view, ruler->rect, &error);
      view.setPlayheadSample(timeline.sampleForTick(0), true);
      checks::support::pumpQuick();
      const QImage playing = checks::support::captureQuickBand(view, ruler->rect, &error);
Original expression:
  QVERIFY2(!paused.isNull() && !playing.isNull(), qPrintable(error))

A049 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:293
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the lane's windowShown TestCase surface is the real window.
Source context since previous assertion:
  ;
      const QColor color = themes::color(themes::Role::song_view_playhead);
Original expression:
  QVERIFY(polarityMatches(frameHasPlayhead(paused, color)))

A050 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:294
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: the position-only move's static-region pixel compare stands in for the layer-revision compare: every pixel outside the playhead footprint is unchanged.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(polarityMatches(frameHasPlayhead(playing, color)))

A051 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:304
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
      const QRectF staticSurface{body->x(), body->y(), body->width(), body->height()};
      const qreal quickLocalXBefore = quick->playheadLocalX();
      const bool quickVisibleBefore = quick->playheadVisible();
      const bool quickPlayingBefore = quick->playheadPlaying();
      const bool quickTrianglePointsUpBefore = quick->playheadTrianglePointsUp();
      const qreal itemLocalXBefore = playhead->localX();

  #ifdef __APPLE__
      QQuickWindow *window = quick->quickWindow();
Original expression:
  QVERIFY(window)

A052 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:312
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
      int renderedFrames = 0;
      QObject frameProbe;
      QObject::connect(
          window, &QQuickWindow::frameSwapped, &frameProbe, [&renderedFrames] { ++renderedFrames; },
          Qt::QueuedConnection);
      // Prove the probe observes a requested frame before testing silence.
      window->update();
Original expression:
  QTRY_VERIFY(renderedFrames > 0)

A053 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:322
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: regionUnchanged compares the static roll region between the shown and moved frames.
Source context since previous assertion:
  ;
      checks::support::pumpQuick();
      renderedFrames = 0;
  #endif

      const checks::support::TimelineQuickLayerRevisions before =
          checks::support::timelineQuickLayerRevisions(*scene);
      for (uint64_t move = 1; move <= 128; ++move)
          view.setPlayheadSample(timeline.sampleForTick(move), true);
      checks::support::pumpQuick();
Original expression:
  QCOMPARE(checks::support::timelineQuickLayerRevisions(*scene), before)

A054 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:324
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.contentX == bootstrap.cameraContentX(tick) is asserted for both positions.
Source context since previous assertion:
  ;
      const QRectF movedSurface{body->x(), body->y(), body->width(), body->height()};
Original expression:
  QCOMPARE(movedSurface, staticSurface)

A055 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:328
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.contentX == bootstrap.cameraContentX(tick) is asserted for both positions.
Source context since previous assertion:
  ;

      if (kQuickCarriesPlayhead) {
          const qreal expectedLocalX = view.camera().contentX(view.playheadTick());
Original expression:
  QCOMPARE(quick->playheadLocalX(), expectedLocalX)

A056 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:329
Disposition: MATCHED
Mapping/reason: swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges: presenter.contentX == bootstrap.cameraContentX(tick) is asserted for both positions.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(playhead->localX(), expectedLocalX)

A057 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:333
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
      } else {
          // Playback reaches the native Core Animation overlay on macOS; it
          // must leave the Quick playhead model and its QML-bound item inert.
Original expression:
  QCOMPARE(quick->playheadLocalX(), quickLocalXBefore)

A058 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:334
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(quick->playheadVisible(), quickVisibleBefore)

A059 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:335
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(quick->playheadPlaying(), quickPlayingBefore)

A060 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:336
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(quick->playheadTrianglePointsUp(), quickTrianglePointsUpBefore)

A061 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:337
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(playhead->localX(), itemLocalXBefore)

A062 | RenderingPlayheadTest::positionOnlyDoesNotRebuild | src/checks/nativegraphics/tst_playhead_quick.cpp:340
Disposition: RETIRED-BACKEND-MECHANISM
Mapping/reason: This assertion specifically requires the former macOS CoreAnimation overlay to leave the Quick playhead model inert and produce zero Quick frames. The converted production SharedPlayhead.qml intentionally renders the shared Swift position in Qt Quick. Neither model inertia nor zero rendered frames is equivalent to or executed by the Swift replacement. Preserve the externally observable moving segment geometry/pixels, coherent shared guides, and unchanged static content across 128 updates instead; do not reintroduce the retired native overlay to meet this backend-only constraint. Lines 304/312 are setup/probe calibration exclusively for that retired zero-frame assertion.
Source context since previous assertion:
  ;
      }
  #ifdef __APPLE__
Original expression:
  QVERIFY2(!QTest::qWaitFor([&renderedFrames] { return renderedFrames != 0; }, 100),
               "Position-only native playhead movement rendered a Quick frame")

Remaining original source (closing sequence and helper bodies):
  ;
  #endif
  }


Swift assertion predicates
Swift assertion predicates
Existing shared presenter index: S001-S011 (checkPublicationAndReprojection in
src/checks/nativegraphics/tst_playhead_quick.swift).
Additional registered PlayheadFeatureChecks predicates:
- checkPlayheadGuides -> swiftcore/PlayheadFeature::guidesResizeScrollAndOwnership
- checkPlayheadFollow -> swiftcore/PlayheadFeature::followScroll
- checkPlayheadAppearance -> swiftcore/PlayheadFeature::appearanceMetrics
- checkPlayheadDevicePixelRect -> swiftcore/PlayheadFeature::devicePixelRect
- checkPlayheadMiddlePan -> swiftcore/PlayheadFeature::middlePan
Window/framebuffer predicates (rollqml lane, src/checks/rollqml/tst_SwiftRollPlayhead.qml):
- test_quickPolarityAndEdges -> swiftroll-window/SwiftRollPlayhead::test_quickPolarityAndEdges
  covers A001-A050 and A053-A056 (fixture guards, polarity, header/gutter
  pixel-unchanged, drawer-band pixels, ruler-edge overhang, negative/offscreen
  projections, position-only static-region compare, contentX equality).
A051-A052 and A057-A062 remain RETIRED-BACKEND-MECHANISM (CoreAnimation
rendered-frame counting has no Swift scene counterpart).
