Assertion correspondence: automation/automationstroke.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files
Original: src/checks/automation/automationstroke.cpp
Original SHA-256: 8b8caef7912b56d633bdf21e766e8e8beb414e2d64e5b7a8cb19d911b26a46f6
Swift counterpart: src/checks/automation/domain/gestures.swift
Swift SHA-256: 63adc07fcdc5c8e068bd224b684c7ac90f674ef1d857a741bccc31d562d1bc14

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
Of the 75 sites in this file, 68 are MATCHED and 7 are PARTIAL. The nine original stroke behaviors (pencilSubCellHorizontalJitterDoesNotAlterStroke, pencilZigzagStrokePreservesDirectionalExtrema, pencilVerticalMotionInSingleCellRetainsFinalValue, pencilDiagonalStrokeEventDensityInvariance, pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit, pencilShiftModifierLocksValueDimension, pencilControlModifierDrawsUnsnappedClockQuantizedPoints, pencilMixedModifierComposesFreehandAndSnappedSegments, pencilAltModifierIsIgnoredDuringStroke) are executed by drawerAutomationPencilStrokeFilters and drawerAutomationPencilStrokeModifiers in src/checks/automation/domain/gestures.swift, under the original cppIDs. Each PARTIAL line names the exact unproved condition; no site is relabeled without an executing predicate.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of the converted Swift window.
GAP identifies an original predicate without a replacement assertion.
Unsure between PARTIAL and GAP resolves to GAP: bookkeeping must not overclaim.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: PASS — deno task verify --filter swiftcore --verbose on this worktree at 470231af (1/14 harnesses selected; swiftcore green, including drawerAutomationPencilStrokeFilters and drawerAutomationPencilStrokeModifiers). Evidence supplied by the lane controller for this settled source snapshot; this ledger task did not re-run the suite.
Converted-window command: deno task verify:qml --verbose
The legacy automation C++ check harnesses were retired during the T2 boundary sweep.
Their sources are reference inventories only, never current execution evidence.

Execution ownership
The legacy AutomationEditingTest stroke suite tested stroke simplification, directional extrema preservation, modifier locks, and sub-cell jitter filtering. In the converted Swift suite, drawerAutomationPencilStrokeFilters executes the sub-cell jitter, zigzag extrema, single-cell final value, density invariance and backtracking behaviors, and drawerAutomationPencilStrokeModifiers executes the Shift value lock, Control freehand quantization, mixed-segment composition and Alt-ignored behaviors, all in src/checks/automation/domain/gestures.swift. Registered run path: src/checks/automation/AutomationPageChecks.swift:371-372 :: runAutomationPageChecks → projectSession (swiftcore suite 10); the counterpart is compiled in src/checks/CMakeLists.txt :: swift_core_check.

Fixture, sequence and row constraints
Data rows expand pencilDiagonalStrokeEventDensityInvariance across 4 sample density rates. Loops verify stroke points across cells, checking that zigzag extrema are preserved, sub-cell horizontal jitter is discarded, and modifiers (Shift for axis lock, Ctrl for quantization, Alt ignored) behave deterministically.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:144
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated (drawerAutomationAutomationFixture + lane facts) and the transaction guard reports failure when the stroke cannot start, so the original lane activation is the same executing step. [S152]
Source context since previous assertion:
  #include "checks/automation/tst_automationediting.h"

  #include <QtTest>
  #include <cstddef>

  #include <algorithm>
  #include <array>
  #include <cmath>
  #include <cstdint>
  #include <limits>
  #include <utility>
  #include <vector>

  #include <QCoreApplication>
  #include <QMouseEvent>
  #include <QQuickWindow>

  #include "ui/editordrawer/automationcanvas.h"
  #include "ui/editordrawer/automationpage.h"
  #include "ui/editordrawer/cclanes.h"

  namespace {

  constexpr int kTrack = 0;
  constexpr uint8_t kPanController = 10;

  using LanePoints = std::vector<std::pair<Tick, int>>;

  struct StrokePoint {
      QPointF content;
      QPoint window;
      AutomationProjection::PointerMapping mapping;
  };

  struct ShiftCapture {
      LanePoints points;
      StrokePoint middle;
      StrokePoint end;
  };

  struct ControlCapture {
      LanePoints points;
      StrokePoint turn;
      StrokePoint finish;
  };

  EditorAutomationRowId panRow()
  {
      return {EditorAutomationRowKind::ControlChange, kTrack, kPanController};
  }

  void resetPencilView(SongView &view, double zoom = 96.0)
  {
      view.setEditorTimeZoom(zoom);
      view.setEditorHorizontalScroll(0.0);
      QCoreApplication::processEvents();
  }

  void stageEmptyPanLane(SongDocument &document)
  {
      document.writeLanePoints(kTrack, kPanController, 0, CoreTimeDefaults::kNoTick, {});
      QCoreApplication::processEvents();
  }

  StrokePoint strokePoint(SongView &view, SongDocument &document, AutomationPage &page,
                          songview::TimelineInputItem &input, LaneHandle handle, double tick,
                          int value)
  {
      CCLaneAdapter lane(document, kTrack, kPanController);
      const QRect body = page.canvas()->laneBody(handle);
      const AutomationGeometry geometry = AutomationGeometry::resolve();
      const QPointF intended(view.camera().displayX(tick, 0.0, input.devicePixelRatio()),
                             AutomationProjection::valueY(body, geometry, lane.minimumValue(),
                                                          lane.maximumValue(), value));
      const QPoint window = automation_test::windowFromContent(page, input, intended);
      const QPointF content = automation_test::contentFromWindow(page, input, window);
      return {
          content, window,
          AutomationProjection(geometry, &page).pointerMapping(lane, body, content.x(), content.y())};
  }

  StrokePoint strokePointInCell(SongView &view, SongDocument &document, AutomationPage &page,
                                songview::TimelineInputItem &input, LaneHandle handle, double tick,
                                int value)
  {
      const StrokePoint probe = strokePoint(view, document, page, input, handle, tick, value);
      const AutomationGridCell &cell = probe.mapping.cell;
      const double midpoint = double(cell.tickBegin) + double(cell.tickEnd - cell.tickBegin) / 2.0;
      return strokePoint(view, document, page, input, handle, midpoint, value);
  }

  StrokePoint nextCellPoint(SongView &view, SongDocument &document, AutomationPage &page,
                            songview::TimelineInputItem &input, LaneHandle handle,
                            const StrokePoint &point, int value)
  {
      return strokePointInCell(view, document, page, input, handle, point.mapping.cell.tickEnd,
                               value);
  }

  LanePoints lanePoints(const SongDocument &document)
  {
      LanePoints points;
      for (const DocLanePoint &point : document.lanePoints(kTrack, kPanController))
          points.emplace_back(point.tick, point.value);
      return points;
  }

  bool pointValue(const LanePoints &points, Tick tick, int *value)
  {
      const auto found = std::find_if(points.cbegin(), points.cend(),
                                      [tick](const auto &point) { return point.first == tick; });
      if (found == points.cend())
          return false;
      *value = found->second;
      return true;
  }

  bool heldValue(const LanePoints &points, Tick tick, int *value)
  {
      const auto next =
          std::upper_bound(points.cbegin(), points.cend(), tick,
                           [](Tick candidate, const auto &point) { return candidate < point.first; });
      if (next == points.cbegin())
          return false;
      *value = std::prev(next)->second;
      return true;
  }

  bool containsInteriorPoint(const LanePoints &points, Tick first, Tick last)
  {
      return std::any_of(points.cbegin(), points.cend(), [first, last](const auto &point) {
          return point.first > first && point.first < last;
      });
  }

  } // namespace

  void AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke()
  {
      SongDocument &document = tab().document();
      AutomationCanvas &canvas = *page().canvas();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A002 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:145
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated (drawerAutomationAutomationFixture + lane facts) and the transaction guard reports failure when the stroke cannot start, so the original lane activation is the same executing step. [S152]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A003 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:146
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated (drawerAutomationAutomationFixture + lane facts) and the transaction guard reports failure when the stroke cannot start, so the original lane activation is the same executing step. [S152]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A004 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:159
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeFilters samples with the page's production nodeDragActivationDistance as the vertical slop; that predicate is the related evidence and no Swift predicate asserts the distance is positive. Unproved: the positivity precondition itself. [S152]
Source context since previous assertion:

      const LaneHandle handle = findRow(panRow());
      setPencilMode(true);
      const auto point = [this, &document, handle](double tick, int value) {
          return strokePointInCell(tab().view(), document, page(), automationInput(), handle, tick,
                                   value);
      };
      const StrokePoint start = point(36, 48);
      const StrokePoint second =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, start, 48);
      const StrokePoint third =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, second, 48);
      const int slop = AutomationGeometry::resolve().nodeDragActivationDistance - 1;

Original expression:
  QVERIFY(slop > 0);

A005 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:160
Disposition: PARTIAL
Mapping/reason: The transaction-level predicates drive the production AutomationPencilTransaction without asserting the page's pencil-mode flag (only drawerAutomationPencilStrokeModifiers' Alt case sets page.isPencilMode). Unproved: canvas pencil mode for this method. [S152]
Source context since previous assertion:


Original expression:
  QVERIFY(canvas.pencilMode());

A006 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:171
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts one point per crossed cell holding the jittered value; the original's per-cell held value is that same point stream. [S155]
Source context since previous assertion:


      mousePress(Qt::LeftButton, start.window);
      mouseMove(start.window + QPoint(slop, 0));
      mouseMove(third.window);
      mouseRelease(Qt::LeftButton, third.window);

      const LanePoints points = lanePoints(document);
      int firstValue = -1;
      int secondValue = -1;
      int thirdValue = -1;

Original expression:
  QVERIFY(heldValue(points, start.mapping.cell.tickBegin, &firstValue));

A007 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:172
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts one point per crossed cell holding the jittered value; the original's per-cell held value is that same point stream. [S155]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(points, second.mapping.cell.tickBegin, &secondValue));

A008 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:173
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts one point per crossed cell holding the jittered value; the original's per-cell held value is that same point stream. [S155]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(points, third.mapping.cell.tickBegin, &thirdValue));

A009 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:174
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts pure horizontal sub-cell jitter keeps the stroke's continuous value (48), the original's start.mapping.point.value. [S153,S155]
Source context since previous assertion:


Original expression:
  QCOMPARE(firstValue, start.mapping.point.value);

A010 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:175
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts pure horizontal sub-cell jitter keeps the stroke's continuous value (48), the original's start.mapping.point.value. [S153,S155]
Source context since previous assertion:


Original expression:
  QCOMPARE(secondValue, start.mapping.point.value);

A011 | AutomationEditingTest::pencilSubCellHorizontalJitterDoesNotAlterStroke | src/checks/automation/automationstroke.cpp:176
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts pure horizontal sub-cell jitter keeps the stroke's continuous value (48), the original's start.mapping.point.value. [S153,S155]
Source context since previous assertion:


Original expression:
  QCOMPARE(thirdValue, start.mapping.point.value);

A012 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:184
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the zigzag transaction guard reports failure when the stroke cannot start; the original lane activation is that same step. [S156]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A013 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:185
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the zigzag transaction guard reports failure when the stroke cannot start; the original lane activation is that same step. [S156]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A014 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:186
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the zigzag transaction guard reports failure when the stroke cannot start; the original lane activation is that same step. [S156]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A015 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:208
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the zigzag's ordered ticks and per-cell values, covering every loop iteration's held value. [S157,S158]
Source context since previous assertion:

      const LaneHandle handle = findRow(panRow());
      setPencilMode(true);
      const auto point = [this, &document, handle](double tick, int value) {
          return strokePointInCell(tab().view(), document, page(), automationInput(), handle, tick,
                                   value);
      };
      const std::array<int, 6> values{32, 100, 28, 92, 44, 84};
      std::array<StrokePoint, 6> stroke{};
      stroke.front() = point(36, values.front());
      for (std::size_t index = 1; index < stroke.size(); ++index)
          stroke[index] = nextCellPoint(tab().view(), document, page(), automationInput(), handle,
                                        stroke[index - 1], values[index]);

      mousePress(Qt::LeftButton, stroke.front().window);
      for (std::size_t index = 1; index < stroke.size(); ++index)
          mouseMove(stroke[index].window);
      mouseRelease(Qt::LeftButton, stroke.back().window);

      const LanePoints points = lanePoints(document);
      std::array<int, 6> actual{};
      for (std::size_t index = 0; index < stroke.size(); ++index) {

Original expression:
  QVERIFY(heldValue(points, stroke[index].mapping.cell.tickBegin, &actual[index]));

A016 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:209
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the zigzag's ordered ticks and per-cell values, covering every loop iteration's held value. [S157,S158]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(actual[index] - stroke[index].mapping.point.value) <= 1);

A017 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:211
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts every directional extremum of the zigzag value sequence. [S159]
Source context since previous assertion:

      }

Original expression:
  QVERIFY(actual[0] < actual[1]);

A018 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:212
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts every directional extremum of the zigzag value sequence. [S159]
Source context since previous assertion:


Original expression:
  QVERIFY(actual[1] > actual[2]);

A019 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:213
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts every directional extremum of the zigzag value sequence. [S159]
Source context since previous assertion:


Original expression:
  QVERIFY(actual[2] < actual[3]);

A020 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:214
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts every directional extremum of the zigzag value sequence. [S159]
Source context since previous assertion:


Original expression:
  QVERIFY(actual[3] > actual[4]);

A021 | AutomationEditingTest::pencilZigzagStrokePreservesDirectionalExtrema | src/checks/automation/automationstroke.cpp:215
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts every directional extremum of the zigzag value sequence. [S159]
Source context since previous assertion:


Original expression:
  QVERIFY(actual[4] < actual[5]);

A022 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:223
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the vertical-stroke guard reports failure when the stroke cannot start. [S160]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A023 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:224
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the vertical-stroke guard reports failure when the stroke cannot start. [S160]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A024 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:225
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the vertical-stroke guard reports failure when the stroke cannot start. [S160]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A025 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:234
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts vertical motion inside one cell keeps only the final value at that cell's tick, the original single-cell end value. [S161]
Source context since previous assertion:

      const LaneHandle handle = findRow(panRow());
      setPencilMode(true);
      const auto point = [this, &document, handle](double tick, int value) {
          return strokePointInCell(tab().view(), document, page(), automationInput(), handle, tick,
                                   value);
      };
      const StrokePoint start = point(48, 32);
      const StrokePoint end = point(start.mapping.cell.tickBegin, 96);

Original expression:
  QCOMPARE(start.mapping.cell.tickBegin, end.mapping.cell.tickBegin);

A026 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:241
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts vertical motion inside one cell keeps only the final value at that cell's tick, the original single-cell end value. [S161]
Source context since previous assertion:


      mousePress(Qt::LeftButton, start.window);
      mouseMove(end.window);
      mouseRelease(Qt::LeftButton, end.window);

      int value = -1;

Original expression:
  QVERIFY(pointValue(lanePoints(document), start.mapping.cell.tickBegin, &value));

A027 | AutomationEditingTest::pencilVerticalMotionInSingleCellRetainsFinalValue | src/checks/automation/automationstroke.cpp:242
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts vertical motion inside one cell keeps only the final value at that cell's tick, the original single-cell end value. [S161]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(value - end.mapping.point.value) <= 1);

A028 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:262
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the density-stroke guard reports failure when the strokes cannot start. [S162]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance_data()
  {
      QTest::addColumn<double>("zoom");
      QTest::addColumn<bool>("canonical");
      QTest::newRow("96 pixels-per-beat") << 96.0 << false;
      QTest::newRow("256 pixels-per-beat") << 256.0 << false;
      QTest::newRow("512 pixels-per-beat") << 512.0 << false;
      QTest::newRow("canonical 96 staircase") << 96.0 << true;
  }

  void AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance()
  {
      QFETCH(double, zoom);
      QFETCH(bool, canonical);
      SongDocument &document = tab().document();
      resetPencilView(tab().view(), zoom);
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A029 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:263
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the density-stroke guard reports failure when the strokes cannot start. [S162]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A030 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:264
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the density-stroke guard reports failure when the strokes cannot start. [S162]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A031 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:326
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the sparse diagonal stroke writes points. [S163]
Source context since previous assertion:

      const auto capture = [this, &document, canonical, zoom](bool dense) {
          stageEmptyPanLane(document);
          setPencilMode(true);
          const LaneHandle handle = findRow(panRow());
          const auto point = [this, &document, handle](double tick, int value) {
              return strokePointInCell(tab().view(), document, page(), automationInput(), handle,
                                       tick, value);
          };
          const auto deliveredPoint = [this, &document, handle](QPointF content) {
              const QPoint window =
                  automation_test::windowFromContent(page(), automationInput(), content);
              content = automation_test::contentFromWindow(page(), automationInput(), window);
              CCLaneAdapter lane(document, kTrack, kPanController);
              const QRect body = page().canvas()->laneBody(handle);
              return StrokePoint{content, window,
                                 AutomationProjection(AutomationGeometry::resolve(), &page())
                                     .pointerMapping(lane, body, content.x(), content.y())};
          };
          std::array<StrokePoint, 6> input{};
          constexpr std::array<int, 6> canonicalValues{32, 44, 56, 68, 80, 92};
          const double firstTick = zoom >= 512.0 ? 12.0 : 36.0;
          input.front() = point(firstTick, canonical ? canonicalValues.front() : 8);
          for (std::size_t index = 1; index < input.size(); ++index)
              input[index] = point(double(input[index - 1].mapping.cell.tickEnd) + 0.5,
                                   canonical ? canonicalValues[index] : 64);
          if (!canonical) {
              input.back() = point(input.back().mapping.cell.tickBegin, 120);
              const StrokePoint start = input.front();
              const StrokePoint end = input.back();
              for (std::size_t index = 1; index + 1 < input.size(); ++index) {
                  const qreal fraction = (input[index].content.x() - start.content.x()) /
                                         std::max<qreal>(1.0, end.content.x() - start.content.x());
                  input[index] = deliveredPoint(
                      QPointF(input[index].content.x(),
                              start.content.y() + (end.content.y() - start.content.y()) * fraction));
              }
          }
          mousePress(Qt::LeftButton, input.front().window);
          for (std::size_t index = 1; index < input.size(); ++index) {
              if (dense && (canonical || index > 1)) {
                  for (int sample = 1; sample < 4; ++sample) {
                      const QPointF interpolated =
                          input[index - 1].content +
                          (input[index].content - input[index - 1].content) * (qreal(sample) / 4.0);
                      // Keep extra samples on the same delivered segment: integer
                      // rounding would turn event-density changes into path changes.
                      const QPointF window = automationInput().mapToScene(interpolated);
                      QMouseEvent move(QEvent::MouseMove, window, m_quickWindow->mapToGlobal(window),
                                       Qt::NoButton, Qt::LeftButton, Qt::NoModifier);
                      QCoreApplication::sendEvent(m_quickWindow, &move);
                  }
              }
              mouseMove(input[index].window);
          }
          mouseRelease(Qt::LeftButton, input.back().window);
          return std::pair{lanePoints(document), input};
      };

      const auto [sparse, expected] = capture(false);
      const auto [dense, ignored] = capture(true);
      Q_UNUSED(ignored);

Original expression:
  QVERIFY(!sparse.empty());

A032 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:327
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the sparse diagonal stroke writes points. [S163]
Source context since previous assertion:


Original expression:
  QVERIFY(!dense.empty());

A033 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:331
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the 10-sample and 50-sample diagonal strokes produce identical completion points; exact equality is stronger than the original ±1 tolerance. [S164]
Source context since previous assertion:

      for (const StrokePoint &sample : expected) {
          int sparseValue = -1;
          int denseValue = -1;

Original expression:
  QVERIFY(heldValue(sparse, sample.mapping.cell.tickBegin, &sparseValue));

A034 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:332
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the 10-sample and 50-sample diagonal strokes produce identical completion points; exact equality is stronger than the original ±1 tolerance. [S164]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(dense, sample.mapping.cell.tickBegin, &denseValue));

A035 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:333
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the 10-sample and 50-sample diagonal strokes produce identical completion points; exact equality is stronger than the original ±1 tolerance. [S164]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(sparseValue - denseValue) <= 1);

A036 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:334
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeFilters asserts sparse-equals-dense completion points; it does not compare the held value against the requested sample value. Unproved: the per-sample ±1 tolerance. [S164]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(sparseValue - sample.mapping.point.value) <= 1);

A037 | AutomationEditingTest::pencilDiagonalStrokeEventDensityInvariance | src/checks/automation/automationstroke.cpp:335
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeFilters asserts sparse-equals-dense completion points; it does not compare the held value against the requested sample value. Unproved: the per-sample ±1 tolerance. [S164]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(denseValue - sample.mapping.point.value) <= 1);

A038 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:344
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the backtracking-stroke guard reports failure when the stroke cannot start. [S165]
Source context since previous assertion:

      }
  }

  void AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A039 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:345
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the backtracking-stroke guard reports failure when the stroke cannot start. [S165]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A040 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:346
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters builds the production page with the Pan lane activated and the backtracking-stroke guard reports failure when the stroke cannot start. [S165]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A041 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:368
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the revisited cells' leftward values and the standing far extremum, covering the original start/far held-value comparisons. [S166]
Source context since previous assertion:

      const LaneHandle handle = findRow(panRow());
      setPencilMode(true);
      const auto point = [this, &document, handle](double tick, int value) {
          return strokePointInCell(tab().view(), document, page(), automationInput(), handle, tick,
                                   value);
      };
      const StrokePoint start = point(36, 28);
      const StrokePoint middle =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, start, 64);
      const StrokePoint far =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, middle, 100);
      const StrokePoint finish = point(36, 68);

      mousePress(Qt::LeftButton, start.window);
      mouseMove(far.window);
      mouseMove(finish.window);
      mouseRelease(Qt::LeftButton, finish.window);

      const LanePoints points = lanePoints(document);
      int startValue = -1;
      int farValue = -1;

Original expression:
  QVERIFY(heldValue(points, start.mapping.cell.tickBegin, &startValue));

A042 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:369
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the revisited cells' leftward values and the standing far extremum, covering the original start/far held-value comparisons. [S166]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(points, far.mapping.cell.tickBegin, &farValue));

A043 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:370
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the revisited cells' leftward values and the standing far extremum, covering the original start/far held-value comparisons. [S166]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(startValue - finish.mapping.point.value) <= 1);

A044 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:371
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the revisited cells' leftward values and the standing far extremum, covering the original start/far held-value comparisons. [S166]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(farValue - far.mapping.point.value) <= 1);

A045 | AutomationEditingTest::pencilBacktrackingStrokeRetainsExtremaAndLatestRevisit | src/checks/automation/automationstroke.cpp:372
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeFilters asserts the backtracking stroke stays sorted by tick. [S167]
Source context since previous assertion:


Original expression:
  QVERIFY(std::is_sorted(points.cbegin(), points.cend()));

A046 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:380
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page with the Pan lane activated and the locked/unlocked stroke guards report failure when a stroke cannot start. [S168]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilShiftModifierLocksValueDimension()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A047 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:381
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page with the Pan lane activated and the locked/unlocked stroke guards report failure when a stroke cannot start. [S168]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A048 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:382
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page with the Pan lane activated and the locked/unlocked stroke guards report failure when a stroke cannot start. [S168]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A049 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:413
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the same travel unlocked moves the value, the original plainEnd != plainMiddle observation. [S173]
Source context since previous assertion:

      const auto capture = [this, &document](bool held) {
          stageEmptyPanLane(document);
          const LaneHandle handle = findRow(panRow());
          setPencilMode(true);
          const auto point = [this, &document, handle](double tick, int value) {
              return strokePointInCell(tab().view(), document, page(), automationInput(), handle,
                                       tick, value);
          };
          const StrokePoint start = point(36, 64);
          const StrokePoint middle =
              nextCellPoint(tab().view(), document, page(), automationInput(), handle, start, 48);
          const StrokePoint end =
              nextCellPoint(tab().view(), document, page(), automationInput(), handle, middle, 96);
          mousePress(Qt::LeftButton, start.window);
          mouseMove(middle.window);
          if (held)
              keyPress(Qt::Key_Shift);
          mouseMove(end.window, held ? Qt::ShiftModifier : Qt::NoModifier);
          mouseRelease(Qt::LeftButton, end.window, held ? Qt::ShiftModifier : Qt::NoModifier);
          if (held)
              keyRelease(Qt::Key_Shift);
          return ShiftCapture{lanePoints(document), middle, end};
      };

      const auto plain = capture(false);
      const auto locked = capture(true);
      int plainMiddle = -1;
      int plainEnd = -1;
      int lockedMiddle = -1;
      int lockedEnd = -1;

Original expression:
  QVERIFY(heldValue(plain.points, plain.middle.mapping.cell.tickBegin, &plainMiddle));

A050 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:414
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the same travel unlocked moves the value, the original plainEnd != plainMiddle observation. [S173]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(plain.points, plain.end.mapping.cell.tickBegin, &plainEnd));

A051 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:415
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts a locked run keeps the initial value through later vertical travel. [S169]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(locked.points, locked.middle.mapping.cell.tickBegin, &lockedMiddle));

A052 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:416
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts a locked run ignores later vertical travel, the original lockedEnd == lockedMiddle value. [S170]
Source context since previous assertion:


Original expression:
  QVERIFY(heldValue(locked.points, locked.end.mapping.cell.tickBegin, &lockedEnd));

A053 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:417
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the same travel unlocked moves the value, the original plainEnd != plainMiddle observation. [S173]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(plainMiddle - plain.middle.mapping.point.value) <= 1);

A054 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:418
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the same travel unlocked moves the value, the original plainEnd != plainMiddle observation. [S173]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(plainEnd - plain.end.mapping.point.value) <= 1);

A055 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:419
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the same travel unlocked moves the value, the original plainEnd != plainMiddle observation. [S173]
Source context since previous assertion:


Original expression:
  QVERIFY(plainEnd != plainMiddle);

A056 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:420
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the locked stroke writes its constant initial value at every crossed cell. [S171]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(lockedMiddle - locked.middle.mapping.point.value) <= 1);

A057 | AutomationEditingTest::pencilShiftModifierLocksValueDimension | src/checks/automation/automationstroke.cpp:421
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the locked stroke writes its constant initial value at every crossed cell. [S171]
Source context since previous assertion:


Original expression:
  QCOMPARE(lockedEnd, lockedMiddle);

A058 | AutomationEditingTest::pencilControlModifierDrawsUnsnappedClockQuantizedPoints | src/checks/automation/automationstroke.cpp:429
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the freehand-stroke guard reports failure when the stroke cannot start. [S174]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilControlModifierDrawsUnsnappedClockQuantizedPoints()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A059 | AutomationEditingTest::pencilControlModifierDrawsUnsnappedClockQuantizedPoints | src/checks/automation/automationstroke.cpp:430
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the freehand-stroke guard reports failure when the stroke cannot start. [S174]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A060 | AutomationEditingTest::pencilControlModifierDrawsUnsnappedClockQuantizedPoints | src/checks/automation/automationstroke.cpp:431
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the freehand-stroke guard reports failure when the stroke cannot start. [S174]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A061 | std::abs | src/checks/automation/automationstroke.cpp:495
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the freehand run's clock-quantized lattice ticks and its escape from the snapped cell lattice; it does not compare sparse and dense freehand strokes. Unproved: freehand density invariance. [S175,S176]
Source context since previous assertion:

      const auto capture = [this, &document](bool reverse, bool dense) {
          stageEmptyPanLane(document);
          const LaneHandle handle = findRow(panRow());
          setPencilMode(true);
          const auto point = [this, &document, handle](double tick, int value) {
              return strokePoint(tab().view(), document, page(), automationInput(), handle, tick,
                                 value);
          };
          const StrokePoint start = point(31.25, 30);
          const StrokePoint turn = point(103.375, reverse ? 60 : 90);
          const StrokePoint far = point(151.625, 30);
          const StrokePoint end = point(55.75, 90);
          const auto moveSegment = [this, dense](const StrokePoint &from, const StrokePoint &to) {
              if (dense) {
                  for (int sample = 1; sample < 4; ++sample) {
                      const QPointF content =
                          from.content + (to.content - from.content) * (qreal(sample) / 4.0);
                      mouseMove(
                          automation_test::windowFromContent(page(), automationInput(), content),
                          Qt::ControlModifier);
                  }
              }
              mouseMove(to.window, Qt::ControlModifier);
          };

          keyPress(Qt::Key_Control);
          mousePress(Qt::LeftButton, start.window, Qt::ControlModifier);
          if (reverse) {
              moveSegment(start, far);
              moveSegment(far, turn);
              moveSegment(turn, end);
          } else {
              moveSegment(start, turn);
              moveSegment(turn, far);
          }
          const StrokePoint finish = reverse ? end : far;
          mouseRelease(Qt::LeftButton, finish.window, Qt::ControlModifier);
          keyRelease(Qt::Key_Control);
          return ControlCapture{lanePoints(document), turn, finish};
      };
      const auto clockTick = [&document](const StrokePoint &sample) {
          const Tick raw = CoreTimeDefaults::tickFromDouble(std::floor(sample.mapping.rawTick));
          return (raw / document.ticksPerClock()) * document.ticksPerClock();
      };
      const auto matches = [&clockTick](const ControlCapture &sparse, const ControlCapture &dense) {
          int sparseTurn = -1;
          int denseTurn = -1;
          int sparseFinish = -1;
          int denseFinish = -1;
          return heldValue(sparse.points, clockTick(sparse.turn), &sparseTurn) &&
                 heldValue(dense.points, clockTick(dense.turn), &denseTurn) &&
                 heldValue(sparse.points, clockTick(sparse.finish), &sparseFinish) &&
                 heldValue(dense.points, clockTick(dense.finish), &denseFinish) &&
                 std::abs(sparseTurn - sparse.turn.mapping.point.value) <= 1 &&
                 std::abs(denseTurn - sparse.turn.mapping.point.value) <= 1 &&
                 std::abs(sparseFinish - sparse.finish.mapping.point.value) <= 1 &&
                 std::abs(denseFinish - sparse.finish.mapping.point.value) <= 1;
      };

      const ControlCapture forwardSparse = capture(false, false);
      const ControlCapture forwardDense = capture(false, true);
      const ControlCapture reverseSparse = capture(true, false);
      const ControlCapture reverseDense = capture(true, true);

Original expression:
  QVERIFY(matches(forwardSparse, forwardDense));

A062 | std::abs | src/checks/automation/automationstroke.cpp:496
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the freehand run's clock-quantized lattice ticks and its escape from the snapped cell lattice; it does not compare sparse and dense freehand strokes. Unproved: freehand density invariance. [S175,S176]
Source context since previous assertion:


Original expression:
  QVERIFY(matches(reverseSparse, reverseDense));

A063 | std::abs | src/checks/automation/automationstroke.cpp:497
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the freehand run lands on clock ticks off the snapped grid, the original clockTick != cell tickBegin observation. [S177]
Source context since previous assertion:


Original expression:
  QVERIFY(clockTick(forwardSparse.turn) != forwardSparse.turn.mapping.cell.tickBegin);

A064 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:505
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the mixed-stroke guard reports failure when the stroke cannot start. [S178]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A065 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:506
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the mixed-stroke guard reports failure when the stroke cannot start. [S178]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A066 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:507
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers builds the production page and the mixed-stroke guard reports failure when the stroke cannot start. [S178]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A067 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:531
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the mixed stroke's ordered tick set, which includes the freehand interior point between the snapped head's cell and the next cell. [S179]
Source context since previous assertion:

      const LaneHandle handle = findRow(panRow());
      setPencilMode(true);
      const auto point = [this, &document, handle](double tick, int value) {
          return strokePointInCell(tab().view(), document, page(), automationInput(), handle, tick,
                                   value);
      };
      const StrokePoint start = point(36, 36);
      const StrokePoint middle =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, start, 76);
      const StrokePoint end =
          nextCellPoint(tab().view(), document, page(), automationInput(), handle, middle, 104);
      const QPointF interior = start.content + (middle.content - start.content) * 0.4;

      keyPress(Qt::Key_Control);
      mousePress(Qt::LeftButton, start.window, Qt::ControlModifier);
      mouseMove(automation_test::windowFromContent(page(), automationInput(), interior),
                Qt::ControlModifier);
      mouseMove(middle.window, Qt::ControlModifier);
      keyRelease(Qt::Key_Control);
      mouseMove(end.window);
      mouseRelease(Qt::LeftButton, end.window);

      const LanePoints points = lanePoints(document);

Original expression:
  QVERIFY(
          containsInteriorPoint(points, start.mapping.cell.tickBegin, middle.mapping.cell.tickBegin));

A068 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:534
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the mixed stroke's ordered tick set, including the point at the freehand end cell and no extra interior point filling the end cell. [S179]
Source context since previous assertion:

      int endValue = -1;

Original expression:
  QVERIFY(pointValue(points, end.mapping.cell.tickBegin, &endValue));

A069 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:535
Disposition: PARTIAL
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the mixed stroke's first (snapped head) value; it does not compare the freehand end value against the requested sample. Unproved: the end-value tolerance. [S180]
Source context since previous assertion:


Original expression:
  QVERIFY(std::abs(endValue - end.mapping.point.value) <= 1);

A070 | AutomationEditingTest::pencilMixedModifierComposesFreehandAndSnappedSegments | src/checks/automation/automationstroke.cpp:536
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the exact ordered tick set, so the end cell gains no interior point. [S179]
Source context since previous assertion:


Original expression:
  QVERIFY(!containsInteriorPoint(points, end.mapping.cell.tickBegin, end.mapping.cell.tickEnd));

A071 | AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke | src/checks/automation/automationstroke.cpp:544
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers drives the production page pointer route with page.isPencilMode set and reports failure when either stroke cannot start or commit; the original lane activation is that same pointer sequence. [S182]
Source context since previous assertion:

  }

  void AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke()
  {
      SongDocument &document = tab().document();
      resetPencilView(tab().view());
      stageEmptyPanLane(document);

Original expression:
  QVERIFY(activateParameter(panRow()));

A072 | AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke | src/checks/automation/automationstroke.cpp:545
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers drives the production page pointer route with page.isPencilMode set and reports failure when either stroke cannot start or commit; the original lane activation is that same pointer sequence. [S182]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(findRow(panRow()).valid());

A073 | AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke | src/checks/automation/automationstroke.cpp:546
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers drives the production page pointer route with page.isPencilMode set and reports failure when either stroke cannot start or commit; the original lane activation is that same pointer sequence. [S182]
Source context since previous assertion:


Original expression:
  QTRY_VERIFY(!laneBody(findRow(panRow())).isEmpty());

A074 | AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke | src/checks/automation/automationstroke.cpp:565
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the plain stroke writes the empty lane and the fine (Alt) modifier leaves the committed stroke unchanged. [S186,S187]
Source context since previous assertion:

      const auto capture = [this, &document](Qt::KeyboardModifiers modifiers) {
          stageEmptyPanLane(document);
          setPencilMode(true);
          const LaneHandle handle = findRow(panRow());
          const StrokePoint start =
              strokePointInCell(tab().view(), document, page(), automationInput(), handle, 36, 36);
          const StrokePoint end =
              nextCellPoint(tab().view(), document, page(), automationInput(), handle, start, 96);
          mousePress(Qt::LeftButton, start.window, modifiers);
          mouseMove(end.window, modifiers);
          mouseRelease(Qt::LeftButton, end.window, modifiers);
          return lanePoints(document);
      };

      const LanePoints plain = capture(Qt::NoModifier);
      keyPress(Qt::Key_Alt);
      const LanePoints alternate = capture(Qt::AltModifier);
      keyRelease(Qt::Key_Alt);

Original expression:
  QVERIFY(!plain.empty());

A075 | AutomationEditingTest::pencilAltModifierIsIgnoredDuringStroke | src/checks/automation/automationstroke.cpp:566
Disposition: MATCHED
Mapping/reason: drawerAutomationPencilStrokeModifiers asserts the plain stroke writes the empty lane and the fine (Alt) modifier leaves the committed stroke unchanged. [S186,S187]
Source context since previous assertion:


Original expression:
  QVERIFY(plain == alternate);

Remaining original source (closing sequence and helper bodies):

  }


Swift assertion predicates
The S entries are exact predicates called by the paired Swift suite. Their presence alone
does not close any A entry. Preserve their surrounding setup and order in the paired source.

S001 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:24
  report.expectEqual(Tick(1), projection.snapPolicy.clockTicks, cppID: drawerAutomationSweepStepsID,
                         what: "a 24-tick document's clock lattice steps one tick")

S002 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:30
  report.expect(!sweep.slopExceeded, cppID: drawerAutomationSweepStepsID,
                    message: "a drag sweep waits for the activation distance")

S003 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:33
  report.expect(sweep.slopExceeded, cppID: drawerAutomationSweepStepsID,
                    message: "a move past the activation distance arms the sweep")

S004 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:37
  report.expectEqual((0...10).map { "\($0):\($0 * 10)" },
                         fixture.laneValues(sweep.points), cppID: drawerAutomationSweepStepsID,
                         what: "each stepped tick interpolates its value across the stroke")

S005 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:40
  report.expectEqual(11, sweep.points.count, cppID: drawerAutomationSweepStepsID,
                         what: "the sweep steps every lattice tick of the stroke")

S006 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:48
  report.expectEqual("5:50", "\(update.current.tick):\(update.current.value)", cppID: drawerAutomationSweepStepsID,
                         what: "the stroke's current point is its mapped one")

S007 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:50
  report.expectEqual((0...5).map { "\($0):\($0 * 10)" },
                         fixture.laneValues(update.points), cppID: drawerAutomationSweepStepsID,
                         what: "the second stroke interpolates across its own span")

S008 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:53
  report.expectEqual(6, update.points.count, cppID: drawerAutomationSweepStepsID,
                         what: "the second stroke has all six lattice points")

S009 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:61
  report.expectEqual(11, rampPoints.count, cppID: drawerAutomationSweepStepsID,
                         what: "a ramp sweep fills every lattice tick between its endpoints")

S010 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:63
  report.expectEqual(["0:0", "10:100"],
                         fixture.laneValues([rampPoints[0], rampPoints[10]]), cppID: drawerAutomationSweepStepsID,
                         what: "the ramp runs from its anchor value to its release value")

S011 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:66
  report.expectEqual(50, rampPoints[5].value, cppID: drawerAutomationSweepStepsID,
                         what: "the ramp's midpoint interpolates linearly")

S012 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:69
  report.expect(edit != nil && !edit!.unchanged, cppID: drawerAutomationSweepStepsID,
                    message: "the completed ramp is a changed span replacement")

S013 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:71
  report.expectEqual(Tick(0), edit?.tickBegin ?? 99, cppID: drawerAutomationSweepStepsID,
                         what: "the ramp replacement starts at its anchor")

S014 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:73
  report.expectEqual(Tick(11), edit?.tickEnd ?? 0, cppID: drawerAutomationSweepStepsID,
                         what: "the ramp replacement closes one lattice step past its release")

S015 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:75
  report.expectEqual("0:0", "\(edit?.points.first?.tick ?? 0):\(edit?.points.first?.value ?? 0)",
                         cppID: drawerAutomationSweepStepsID, what: "the replacement keeps the ramp's first point")

S016 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:81
  report.expect(emptyFacts.displayPoints.isEmpty && emptyFacts.snapshot.leadInValue == nil,
                    cppID: drawerAutomationSweepStepsID, message: "the completion fixture is an empty lane")

S017 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:88
  report.fail(drawerAutomationSweepStepsID, "the empty-lane ramp produced no completion")

S018 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:91
  report.expect(!emptyCompletion.unchanged, cppID: drawerAutomationSweepStepsID,
                    message: "the empty-lane completion changes the lane")

S019 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:93
  report.expectEqual(11, emptyCompletion.points.count, cppID: drawerAutomationSweepStepsID,
                         what: "the completion itself contains eleven points")

S020 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:95
  report.expectEqual(AutomationLanePoint(tick: 0, value: 0), emptyCompletion.points.first,
                         cppID: drawerAutomationSweepStepsID, what: "the completion starts at the anchor")

S021 | drawerAutomationSweepSteppingAndRampFinish | src/checks/automation/domain/gestures.swift:97
  report.expectEqual(AutomationLanePoint(tick: 10, value: 100), emptyCompletion.points.last,
                         cppID: drawerAutomationSweepStepsID, what: "the empty lane adds no trailing held point")

S022 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:118
  report.expectEqual(Tick(24), projection.snapPolicy.clockTicks, cppID: drawerAutomationSweepTailID,
                         what: "the lattice step of this document is 24 ticks")

S023 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:121
  report.fail(drawerAutomationSweepTailID,
                      "the fixture song ends at \(fixture.songEndTick), shorter than the seam")

S024 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:132
  report.expect(dragEdit != nil && !dragEdit!.unchanged, cppID: drawerAutomationSweepTailID,
                    message: "the drag over a flat lane is a changed span")

S025 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:134
  report.expect(dragEdit?.points.contains { $0.tick == 144 && $0.value == 25 } == true,
                    cppID: drawerAutomationSweepTailID, message: "the stroke's release point is written")

S026 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:136
  report.expectEqual("168:85",
                         "\(dragEdit?.points.last?.tick ?? 0):\(dragEdit?.points.last?.value ?? 0)",
                         cppID: drawerAutomationSweepTailID,
                         what: "the tail re-anchors the original held value one step past the release")

S027 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:140
  report.expectEqual(Tick(168), dragEdit?.tickEnd ?? 0, cppID: drawerAutomationSweepTailID,
                         what: "the replacement's span covers the restored tail")

S028 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:148
  report.expect(rampEdit != nil && !rampEdit!.unchanged, cppID: drawerAutomationSweepTailID,
                    message: "the ramp over a flat lane is a changed span")

S029 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:150
  report.expect(rampEdit?.points.contains { $0.tick == 144 && $0.value == 25 } == true,
                    cppID: drawerAutomationSweepTailID, message: "the ramp's release point is written")

S030 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:152
  report.expectEqual("168:85",
                         "\(rampEdit?.points.last?.tick ?? 0):\(rampEdit?.points.last?.value ?? 0)",
                         cppID: drawerAutomationSweepTailID,
                         what: "the ramp's tail re-anchors the original held value too")

S031 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:174
  report.expectEqual(endTick, endSweep.points.last?.tick ?? 0, cppID: drawerAutomationSweepTailID,
                         what: "a stroke that ends at the song's end steps to the song's end")

S032 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:177
  report.expect(endEdit?.points.allSatisfy { $0.tick <= endTick } == true, cppID: drawerAutomationSweepTailID,
                    message: "a stroke that ends at the song's end restores no tail")

S033 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:199
  report.expectEqual(["48:85", "72:70", "96:55", "120:40", "144:25"],
                         committed.laneValues(live.preview), cppID: drawerAutomationSweepTailID,
                         what: "the sweep preview is the stepped draft alone")

S034 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:202
  report.expectEqual(beforeRelease, committed.snapshot, cppID: drawerAutomationSweepTailID,
                         what: "the draft writes nothing before the release")

S035 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:205
  report.fail(drawerAutomationSweepTailID, "the released sweep produced no edit")

S036 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:208
  report.expect(AutomationCommit.apply(committedEdit, in: committed.document), cppID: drawerAutomationSweepTailID,
                    message: "the released sweep commits once")

S037 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:210
  report.expectEqual(["0:85", "72:70", "96:55", "120:40", "144:25", "168:85"],
                         committed.values(committed.modulationLane), cppID: drawerAutomationSweepTailID,
                         what: "the committed lane holds the stroke and its restored tail")

S038 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:213
  report.expectEqual(beforeRelease.revision + 1, committed.document.revision, cppID: drawerAutomationSweepTailID,
                         what: "one released sweep is one revision")

S039 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:215
  report.expect(committed.undo(), cppID: drawerAutomationSweepTailID, message: "the sweep is undoable")

S040 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:216
  report.expectEqual(["0:85"], committed.values(committed.modulationLane), cppID: drawerAutomationSweepTailID,
                         what: "one undo restores the flat lane")

S041 | drawerAutomationSweepFinishRestoresTrailingHeldValue | src/checks/automation/domain/gestures.swift:218
  report.expect(!committed.document.history.canUndo, cppID: drawerAutomationSweepTailID,
                    message: "the released sweep recorded exactly one history entry")

S042 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:227
  report.expectEqual(64, metadata.neutral, cppID: drawerAutomationNeutralSnapID,
                         what: "Pan's neutral is 64, the centered domain's midpoint")

S043 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:232
  report.expect(threshold > 0, cppID: drawerAutomationNeutralSnapID,
                    message: "the neutral snap radius covers more than one value step")

S044 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:248
  report.fail(drawerAutomationNeutralSnapID, "the plot contains no pixel inside the neutral snap radius")

S045 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:255
  report.expectEqual(Tick(100), plain.tick, cppID: drawerAutomationNeutralSnapID,
                         what: "the unsnapped value mapping preserves tick 100")

S046 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:257
  report.expectEqual(valueNear, plain.value, cppID: drawerAutomationNeutralSnapID,
                         what: "the unsnapped value mapping keeps the near-neutral pixel value")

S047 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:262
  report.expectEqual(Tick(100), snapped.tick, cppID: drawerAutomationNeutralSnapID,
                         what: "neutral snapping preserves tick 100")

S048 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:264
  report.expectEqual(64, snapped.value, cppID: drawerAutomationNeutralSnapID,
                         what: "the near-neutral pixel snaps to 64")

S049 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:270
  report.expectEqual(AutomationLanePoint(tick: 200, value: 64), exact,
                         cppID: drawerAutomationNeutralSnapID,
                         what: "the exact neutral maps to the original tick-200 point")

S050 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:273
  report.expectEqual(near, metadata.snappedValue(near, snapValue: false, plotHeight: height,
                                                     neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID,
                         what: "an unsnapped drag keeps the value under the pointer")

S051 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:277
  report.expectEqual(64, metadata.snappedValue(near, snapValue: true, plotHeight: height,
                                                   neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID, what: "a snapped drag lands exactly on the neutral")

S052 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:280
  report.expectEqual(64, metadata.snappedValue(64, snapValue: true, plotHeight: height,
                                                   neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID, what: "a pointer on the neutral stays neutral")

S053 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:284
  report.expectEqual(outside, metadata.snappedValue(outside, snapValue: true, plotHeight: height,
                                                        neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID,
                         what: "a value outside the radius keeps its own value")

S054 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:288
  report.expectEqual(19, metadata.snappedValue(19, snapValue: true, plotHeight: height,
                                                   neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID,
                         what: "a snap only ever moves a value onto the neutral")

S055 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:293
  report.expectEqual(60, volume.snappedValue(60, snapValue: true, plotHeight: height,
                                                 neutralSnapRadius: radius),
                         cppID: drawerAutomationNeutralSnapID,
                         what: "a parameter with no neutral keeps the dragged value")

S056 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:304
  report.expect(fixture.page.pointerPress(x: nodeX, y: nodeY, surface: 1, button: 1, modifiers: drawerAutomationQtModifiers(modifiers)),
                    cppID: drawerAutomationNeutralSnapID, message: "a snapped press grabs the node under the pointer")

S057 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:307
  report.expectEqual(Tick(24), fixture.page.previewPoints.first?.tick ?? 0, cppID: drawerAutomationNeutralSnapID,
                         what: "the armed preview keeps the node's own tick")

S058 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:310
  report.expectEqual(64, fixture.page.previewPoints.first?.value ?? 0, cppID: drawerAutomationNeutralSnapID,
                         what: "the preview publishes the snapped neutral")

S059 | drawerAutomationPanNeutralSnap | src/checks/automation/domain/gestures.swift:313
  report.expectEqual(["24:64"], fixture.values(fixture.panLane), cppID: drawerAutomationNeutralSnapID,
                         what: "the snapped release writes the neutral at the node's own tick")

S060 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:324
  report.expectEqual(3, sources.count, cppID: drawerAutomationNodeDragID,
                         what: "the fixture offers three draggable nodes")

S061 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:331
  report.expectEqual(AutomationPointRelease.noOp, empty.finish().release, cppID: drawerAutomationNodeDragID,
                         what: "a gesture with no target finishes as a no-op")

S062 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:333
  report.expect(!empty.finish().changed, cppID: drawerAutomationNodeDragID,
                    message: "an empty gesture reports no change")

S063 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:340
  report.expectEqual(AutomationPointRelease.noOp, shiftPress.finish().release, cppID: drawerAutomationNodeDragID,
                         what: "a Shift-held stationary release is a no-op")

S064 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:342
  report.expect(!shiftPress.finish().changed, cppID: drawerAutomationNodeDragID,
                    message: "a Shift-held stationary release changes nothing")

S065 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:347
  report.expectEqual(AutomationPointRelease.stationaryDelete, plainPress.finish().release,
                         cppID: drawerAutomationNodeDragID, what: "a stationary release without Shift deletes")

S066 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:349
  report.expect(!plainPress.finish().changed, cppID: drawerAutomationNodeDragID,
                    message: "the stationary delete reports no move")

S067 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:354
  report.expectEqual(AutomationPointRelease.move, plainPress.finish().release, cppID: drawerAutomationNodeDragID,
                         what: "a released stroke past the slop is a move")

S068 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:356
  report.expect(!plainPress.finish().changed, cppID: drawerAutomationNodeDragID,
                    message: "a stroke that never moved the node changes nothing")

S069 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:364
  report.expectEqual(AutomationPointRelease.move, moved.release, cppID: drawerAutomationNodeDragID,
                         what: "the moved gesture is still a move")

S070 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:366
  report.expect(moved.changed, cppID: drawerAutomationNodeDragID, message: "the moved node reports a change")

S071 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:367
  report.expectEqual(6, moved.dTick, cppID: drawerAutomationNodeDragID,
                         what: "the reported delta is the grabbed node's own tick delta")

S072 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:369
  report.expectEqual(AutomationLanePoint(tick: 30, value: 80), plainPress.targets[0].current,
                         cppID: drawerAutomationNodeDragID, what: "the target's preview is the mapped destination")

S073 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:385
  report.expectEqual(AutomationPointRelease.move, groupFinish.release,
                         cppID: drawerAutomationNodeDragID, what: "the selection releases as a move")

S074 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:387
  report.expect(groupFinish.changed, cppID: drawerAutomationNodeDragID,
                    message: "the moved selection reports a change")

S075 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:389
  report.expectEqual(6, groupFinish.dTick, cppID: drawerAutomationNodeDragID,
                         what: "the shared delta is the grabbed node's delta")

S076 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:391
  report.expect(groupFinish.selectionDrag, cppID: drawerAutomationNodeDragID,
                    message: "the finish reports that the whole selection moved")

S077 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:393
  report.expectEqual(["30:70", "54:90"], group.targets.map { "\($0.current.tick):\($0.current.value)" },
                         cppID: drawerAutomationNodeDragID, what: "every selected node moves by the same delta")

S078 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:408
  report.expectEqual(["0:60", "24:80"], edge.targets.map { "\($0.current.tick):\($0.current.value)" },
                         cppID: drawerAutomationNodeDragID,
                         what: "a leftward drag clamps at tick zero without collapsing the group")

S079 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:411
  report.expectEqual(-24, edge.finish().dTick, cppID: drawerAutomationNodeDragID,
                         what: "the clamped delta is what the finish reports")

S080 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:413
  report.expectEqual(AutomationPointRelease.move, edge.finish().release,
                         cppID: drawerAutomationNodeDragID, what: "the clamped group releases as a move")

S081 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:415
  report.expect(edge.finish().changed, cppID: drawerAutomationNodeDragID,
                    message: "the clamped group reports a change")

S082 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:425
  report.expectEqual(80, locked.targets[0].current.value, cppID: drawerAutomationNodeDragID,
                         what: "a time lock keeps the original value")

S083 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:427
  report.expectEqual(Tick(60), locked.targets[0].current.tick, cppID: drawerAutomationNodeDragID,
                         what: "a time lock keeps the mapped tick")

S084 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:433
  report.expectEqual(AutomationAxisLock.value,
                         phantom.update(AutomationPointDrag.Update(phase: .reset, effectiveX: 100,
                                                                   effectiveY: 100, axisLock: .none),
                                        mappedValue: 127),
                         cppID: drawerAutomationNodeDragID, what: "a phantom drag only ever locks the value axis")

S085 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:438
  report.expectEqual(AutomationLanePoint(tick: 24, value: 60), phantom.target.current,
                         cppID: drawerAutomationNodeDragID, what: "a reset restores the phantom's source value")

S086 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:440
  report.expect(phantom.finish() == nil, cppID: drawerAutomationNodeDragID,
                    message: "a reset phantom finishes as no edit")

S087 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:447
  report.fail(drawerAutomationNodeDragID, "the dragged phantom publishes no target")

S088 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:450
  report.expectEqual(phantomTarget.original.tick, phantomTarget.current.tick, cppID: drawerAutomationNodeDragID,
                         what: "a phantom drag never moves in time")

S089 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:452
  report.expectEqual(127, phantomTarget.current.value, cppID: drawerAutomationNodeDragID,
                         what: "a phantom drag clamps into the lane's domain")

S090 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:454
  report.expectEqual(Tick(24), phantom.move?.sourceTick ?? 0, cppID: drawerAutomationNodeDragID,
                         what: "the phantom's write names its own source tick")

S091 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:470
  report.fail(drawerAutomationNodeDragID, "the committed drag resolved no plan")

S092 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:473
  report.expect(AutomationCommit.apply(plan, in: fixture.document), cppID: drawerAutomationNodeDragID,
                    message: "the committed drag writes once")

S093 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:475
  report.expectEqual(["48:90", "120:40"], fixture.values(fixture.panLane), cppID: drawerAutomationNodeDragID,
                         what: "the node lands on its destination and evicts the occupant there")

S094 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:477
  report.expectEqual(beforeCommit.revision + 1, fixture.document.revision, cppID: drawerAutomationNodeDragID,
                         what: "one drag is one revision")

S095 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:479
  report.expect(fixture.undo(), cppID: drawerAutomationNodeDragID, message: "the drag is undoable")

S096 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:480
  report.expectEqual(["24:60", "48:80", "120:40"], fixture.values(fixture.panLane),
                         cppID: drawerAutomationNodeDragID,
                         what: "one undo restores the moved node and the evicted occupant")

S097 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:487
  report.expectEqual(["0:64"], promotion.laneValues(promotionFacts.displayPoints), cppID: drawerAutomationNodeDragID,
                         what: "the empty lane displays its projected engine node")

S098 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:494
  report.fail(drawerAutomationNodeDragID, "the promotion resolved no plan")

S099 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:497
  report.expect(!promotionPlan.isEmpty, cppID: drawerAutomationNodeDragID,
                    message: "a write on the projected node resolves to a promotion")

S100 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:499
  report.expect(AutomationCommit.apply(promotionPlan, in: promotion.document), cppID: drawerAutomationNodeDragID,
                    message: "the promotion commits")

S101 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:501
  report.expectEqual(["0:20"], promotion.values(promotion.panLane), cppID: drawerAutomationNodeDragID,
                         what: "the projected node becomes a written event at its own tick")

S102 | drawerAutomationNodeDragAndPhantomOutcomes | src/checks/automation/domain/gestures.swift:503
  report.expect(promotion.undo() && promotion.values(promotion.panLane).isEmpty, cppID: drawerAutomationNodeDragID,
                    message: "one undo returns the lane to its projected node alone")

S103 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:518
  report.expectEqual(7, freeze.revision, cppID: drawerAutomationPointRangeID,
                         what: "the frozen lane carries the revision it was read at")

S104 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:524
  report.expect(AutomationLaneReplacement.pointRange(
          originalPointRange, begin: 24, end: 48,
          points: [AutomationLanePoint(tick: 24, value: 64),
                   AutomationLanePoint(tick: 48, value: 64)]).unchanged,
          cppID: drawerAutomationPointRangeID, message: "the original two-point range is unchanged")

S105 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:529
  report.expect(!AutomationLaneReplacement.pointRange(
          originalPointRange, begin: 24, end: 48,
          points: [AutomationLanePoint(tick: 24, value: 64)]).unchanged,
          cppID: drawerAutomationPointRangeID, message: "dropping the original range endpoint changes it")

S106 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:542
  report.expect(!leadInCompletion.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "the original implicit lead-in replacement changes the lane")

S107 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:544
  report.expectEqual([AutomationLanePoint(tick: 24, value: 80),
                          AutomationLanePoint(tick: 48, value: 20)], leadInCompletion.points,
                         cppID: drawerAutomationPointRangeID,
                         what: "the original implicit lead-in is restored at the two-point span end")

S108 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:552
  report.expect(identical.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "an identical point range is unchanged")

S109 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:558
  report.expect(!shortened.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "a range the lane does not hold is a change")

S110 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:564
  report.expect(!restored.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "a held-span replacement that differs is a change")

S111 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:566
  report.expectEqual(["24:80", "48:60"], restored.points.map { "\($0.tick):\($0.value)" },
                         cppID: drawerAutomationPointRangeID,
                         what: "the held span closes on the original endpoint's value")

S112 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:569
  report.expectEqual(Tick(48), restored.tickEnd, cppID: drawerAutomationPointRangeID,
                         what: "the replacement names the last tick it covers")

S113 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:575
  report.expect(flat.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "a replacement the lane already holds is unchanged")

S114 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:577
  report.expectEqual(0, flat.points.count, cppID: drawerAutomationPointRangeID,
                         what: "an unchanged replacement carries no point")

S115 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:580
  report.expect(!deletion.unchanged, cppID: drawerAutomationPointRangeID,
                    message: "an empty replacement over a covered span deletes it")

S116 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:582
  report.expectEqual(0, deletion.points.count, cppID: drawerAutomationPointRangeID,
                         what: "the deleting replacement carries no point")

S117 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:586
  report.expectEqual(["8:20", "24:40"],
                         AutomationLaneReplacement.canonical(
                             [AutomationLanePoint(tick: 24, value: 30),
                              AutomationLanePoint(tick: 8, value: 20),
                              AutomationLanePoint(tick: 32, value: 30),
                              AutomationLanePoint(tick: 24, value: 40)],
                             begin: 0, end: 30, minimum: 0, maximum: 127, priorValue: nil)
                             .map { "\($0.tick):\($0.value)" },
                         cppID: drawerAutomationPointRangeID,
                         what: "canonicalization sorts, drops the far tick and keeps the last occupant")

S118 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:596
  report.expectEqual(["0:0", "24:9"],
                         AutomationLaneReplacement.canonical(
                             [AutomationLanePoint(tick: 0, value: -4),
                              AutomationLanePoint(tick: 9, value: -4),
                              AutomationLanePoint(tick: 24, value: 9)],
                             begin: 0, end: 96, minimum: 0, maximum: 9, priorValue: nil)
                             .map { "\($0.tick):\($0.value)" },
                         cppID: drawerAutomationPointRangeID,
                         what: "canonicalization clamps into the domain and drops repeated values")

S119 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:606
  report.expect(AutomationLaneReplacement.held(held, at: 8, inclusive: false) == nil,
                    cppID: drawerAutomationPointRangeID,
                    message: "the exclusive held lookup skips a point on the tick")

S120 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:609
  report.expectEqual(20, AutomationLaneReplacement.held(held, at: 8, inclusive: true) ?? -1,
                         cppID: drawerAutomationPointRangeID,
                         what: "the inclusive held lookup takes a point on the tick")

S121 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:612
  report.expectEqual(30, AutomationLaneReplacement.held(held, at: 40, inclusive: true) ?? -1,
                         cppID: drawerAutomationPointRangeID,
                         what: "the held lookup walks to the last point before the tick")

S122 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:615
  report.expect(AutomationLaneReplacement.held(held, at: 4, inclusive: true) == nil,
                    cppID: drawerAutomationPointRangeID, message: "the held lookup reports nothing before the first")

S123 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:621
  report.expectEqual(0, emptyFacts.snapshot.leadInValue ?? -1, cppID: drawerAutomationPointRangeID,
                         what: "an unwritten Modulation lane leads in on its engine default")

S124 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:629
  report.fail(drawerAutomationPointRangeID, "the pencil stroke did not start on an empty lane")

S125 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:632
  report.expectEqual(["24:80", "48:0"], emptyLane.laneValues(stroke.completion().points),
                         cppID: drawerAutomationPointRangeID,
                         what: "the stroke writes its point and restores the lane's held value")

S126 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:635
  report.expectEqual(["24:80", "48:0"], emptyLane.laneValues(stroke.preview.points),
                         cppID: drawerAutomationPointRangeID, what: "the stroke's preview matches its completion")

S127 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:637
  report.expect(!stroke.completion().unchanged, cppID: drawerAutomationPointRangeID,
                    message: "an empty lane's first stroke is a change")

S128 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:649
  report.fail(drawerAutomationPointRangeID, "the pencil stroke did not start past the last point")

S129 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:652
  report.expectEqual(["24:80", "48:20"], pastLast.laneValues(pastLastStroke.completion().points),
                         cppID: drawerAutomationPointRangeID,
                         what: "a stroke past the last point re-anchors that point's value")

S130 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:655
  report.expect(!pastLastStroke.completion().unchanged, cppID: drawerAutomationPointRangeID,
                    message: "a stroke past the last point reports a change")

S131 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:666
  report.expect(committed.page.consumeMenuAction(actionId: AutomationMenuAction.range127.rawValue),
                    cppID: drawerAutomationPointRangeID, message: "the stroke fixture selects its intended value range")

S132 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:668
  report.expectEqual(committedBefore, committed.snapshot, cppID: drawerAutomationPointRangeID,
                         what: "preparing the display range leaves document and history unchanged")

S133 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:672
  report.expect(committed.page.pointerPress(x: committed.x(24), y: strokeY, surface: 1, button: 1), cppID: drawerAutomationPointRangeID,
                    message: "the pencil press starts a stroke")

S134 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:674
  report.expect(committed.page.isPainting, cppID: drawerAutomationPointRangeID,
                    message: "the page publishes the painting gesture")

S135 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:676
  report.expect(committed.page.pointerRelease(x: committed.x(24), y: strokeY, button: 1), cppID: drawerAutomationPointRangeID,
                    message: "the pencil release commits")

S136 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:678
  report.expectEqual(committedBefore.revision + 1, committed.document.revision, cppID: drawerAutomationPointRangeID,
                         what: "one pencil press and release is one revision")

S137 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:680
  report.expect(committed.values(committed.modulationLane).contains { $0.hasSuffix(":60") },
                    cppID: drawerAutomationPointRangeID,
                    message: "the released stroke writes the value under the pointer")

S138 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:683
  report.expect(committed.undo(), cppID: drawerAutomationPointRangeID, message: "the stroke is undoable")

S139 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:684
  report.expectEqual(["0:20"], committed.values(committed.modulationLane), cppID: drawerAutomationPointRangeID,
                         what: "one undo restores the pre-stroke lane")

S140 | drawerAutomationPointRangeAndPencilReplacements | src/checks/automation/domain/gestures.swift:686
  report.expect(!committed.document.history.canUndo, cppID: drawerAutomationPointRangeID,
                    message: "the released stroke recorded exactly one history entry")

S141 | coreEventAutomationGestureCoreSeams | src/checks/automation/domain/gestures.swift:703
  report.expectEqual(
          ["0:20", "24:80", "48:60", "72:90"],
          replacement.lanePoints(track: 0, lane: .controller(7))
              .map { "\($0.tick):\($0.value)" },
          cppID: "automation-domain/AutomationDomainTest::pointRangeAndPencilReplacements",
          what: "core held-span replacement including the trailing held-value seam")

S142 | coreEventAutomationGestureCoreSeams | src/checks/automation/domain/gestures.swift:718
  report.expectEqual(
          ["24:80", "48:20"],
          emptyLane.lanePoints(track: 0, lane: .controller(7))
              .map { "\($0.tick):\($0.value)" },
          cppID: "automation-domain/AutomationDomainTest::pointRangeAndPencilReplacements",
          what: "core empty-lane replacement stores the pencil value and restored tail")

S143 | coreEventAutomationGestureCoreSeams | src/checks/automation/domain/gestures.swift:734
  report.expectEqual(
          ["0:85", "144:25", "168:85"],
          trailingHold.lanePoints(track: 0, lane: .controller(7))
              .map { "\($0.tick):\($0.value)" },
          cppID: "automation-domain/AutomationDomainTest::sweepFinishRestoresTrailingHeldValue",
          what: "core lane write retains the explicit post-sweep held-value seam")

S144 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:758
  report.fail(id, "Pan node move resolves")

S145 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:759
  report.expect(AutomationCommit.apply(plan, in: fx.document), cppID: id,
                    message: "node value move records its own transaction")

S146 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:762
  report.expect(moved != beforeMove, cppID: id, message: "the move actually changes document state")

S147 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:764
  report.expect(undone, cppID: id, message: "awaited undo completes")

S148 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:765
  report.expectEqual(beforeMove, fx.document.state, cppID: id,
                         what: "one undo restores all four written Pan occurrences")

S149 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:768
  report.expect(redone, cppID: id, message: "awaited redo completes")

S150 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:769
  report.expectEqual(moved, fx.document.state, cppID: id, what: "redo restores the node value move")

S151 | coreAutomationPanUndoRegression | src/checks/automation/domain/gestures.swift:772
  report.expectEqual(firstWrite, fx.document.state, cppID: id,
                         what: "only a second undo removes the preceding Pan sweep")

S152 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:993
Swift expression:
  report.fail(jitterID, "the jitter stroke did not start")

S153 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:999
Swift expression:
  report.expectEqual(48.0, jittered, cppID: jitterID, what: "pure horizontal jitter keeps the stroke's continuous value")

S154 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1002
Swift expression:
  report.expectEqual(1, jitter.strokePoints.count, cppID: jitterID, what: "a same-cell revisit keeps one point for the cell")

S155 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1006
Swift expression:
  report.expectEqual(["24:48", "48:48", "72:48"], jitterLane.laneValues(jitter.strokePoints), cppID: jitterID, what: "one point per crossed cell holds the jittered value")

S156 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1016
Swift expression:
  report.fail(zigzagID, "the zigzag stroke did not start")

S157 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1024
Swift expression:
  report.expectEqual((0..<6).map { Tick(24 + $0 * 24) }, zigzag.strokePoints.map(\.tick), cppID: zigzagID, what: "the zigzag keeps one point per crossed cell")

S158 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1026
Swift expression:
  report.expectEqual(zigzagValues, zigzag.strokePoints.map(\.value), cppID: zigzagID, what: "each zigzag cell holds its own extremum")

S159 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1029
Swift expression:
  report.expect(extrema[0] < extrema[1] && extrema[1] > extrema[2] && extrema[2] < extrema[3] && extrema[3] > extrema[4] && extrema[4] < extrema[5], cppID: zigzagID, message: "the zigzag preserves every directional extremum")

S160 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1039
Swift expression:
  report.fail(verticalID, "the vertical stroke did not start")

S161 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1043
Swift expression:
  report.expectEqual([AutomationLanePoint(tick: 24, value: 96)], vertical.strokePoints, cppID: verticalID, what: "vertical motion in one cell keeps only the final value")

S162 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1064
Swift expression:
  report.fail(densityID, "the density strokes did not start")

S163 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1067-1068
Swift expression:
  report.expect(!sparse.isEmpty, cppID: densityID,
                message: "the sparse diagonal stroke writes points")

S164 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1069
Swift expression:
  report.expectEqual(sparse, dense, cppID: densityID, what: "10 vs 50 intermediate samples give identical completion points")

S165 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1078
Swift expression:
  report.fail(backtrackID, "the backtracking stroke did not start")

S166 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1084-1086
Swift expression:
  backtrackLane.laneValues(backtrack.strokePoints),
  cppID: backtrackID,
  what: "revisited cells hold the leftward values while the far extremum stands")

S167 | drawerAutomationPencilStrokeFilters | src/checks/automation/domain/gestures.swift:1087
Swift expression:
  report.expectEqual([Tick(24), Tick(48), Tick(72)], backtrack.strokePoints.map(\.tick), cppID: backtrackID, what: "the backtracking stroke stays sorted by tick")

S168 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1110
Swift expression:
  report.fail(shiftID, "the locked stroke did not start")

S169 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1119
Swift expression:
  report.expectEqual(64.0, firstLocked, cppID: shiftID, what: "a locked run keeps the initial value")

S170 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1121
Swift expression:
  report.expectEqual(64.0, secondLocked, cppID: shiftID, what: "a locked run ignores later vertical travel")

S171 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1125
Swift expression:
  report.expectEqual(["24:64", "48:64", "72:64"], lockedLane.laneValues(locked.strokePoints), cppID: shiftID, what: "the locked stroke writes its constant initial value")

S172 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1131
Swift expression:
  report.fail(shiftID, "the unlocked stroke did not start")

S173 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1137
Swift expression:
  report.expect(unlockedValue != 64, cppID: shiftID, message: "the same travel unlocked moves the value")

S174 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1144
Swift expression:
  report.fail(controlID, "the freehand stroke did not start")

S175 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1148
Swift expression:
  report.expectEqual([Tick(24), Tick(30)], freehand.strokePoints.map(\.tick), cppID: controlID, what: "a freehand run lands on clock ticks off the snapped grid")

S176 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1150
Swift expression:
  report.expect(freehand.strokePoints.allSatisfy { $0.tick % 6 == 0 }, cppID: controlID, message: "every freehand point is clock-quantized")

S177 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1152
Swift expression:
  report.expect(freehand.strokePoints.contains { $0.tick % 24 != 0 }, cppID: controlID, message: "the freehand run escapes the snapped cell lattice")

S178 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1159
Swift expression:
  report.fail(mixedID, "the mixed stroke did not start")

S179 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1164
Swift expression:
  report.expectEqual([Tick(24), Tick(48), Tick(60), Tick(66)], mixed.strokePoints.map(\.tick), cppID: mixedID, what: "snapped then freehand segments compose contiguously")

S180 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1167
Swift expression:
  report.expectEqual(36, mixed.strokePoints.first?.value ?? -1, cppID: mixedID, what: "the snapped head keeps its cell value")

S181 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1169
Swift expression:
  report.expectEqual(Tick(24), mixed.tickBegin, cppID: mixedID, what: "the mixed stroke spans from its first cell")

S182 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1178
Swift expression:
  report.fail(altID, "the plain stroke did not start")

S183 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1185
Swift expression:
  report.fail(altID, "the plain stroke did not commit")

S184 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1194
Swift expression:
  report.fail(altID, "the fine stroke did not start")

S185 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1201
Swift expression:
  report.fail(altID, "the fine stroke did not commit")

S186 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1205
Swift expression:
  report.expect(!plain.isEmpty, cppID: altID, message: "the plain stroke writes the empty lane")

S187 | drawerAutomationPencilStrokeModifiers | src/checks/automation/domain/gestures.swift:1206
Swift expression:
  report.expectEqual(plain, altLane.values(altLane.panLane), cppID: altID, what: "the fine modifier leaves the stroke unchanged")

Shared helper: src/checks/automation/AutomationPageChecks.swift
SHA-256: 4155aec5230e18a72801ddadf35e29a74c99e60712d0a6df8377685f4e5d51e9
src/checks/automation/AutomationPageChecks.swift:402
  report.fail("swiftcore/Automation::supplementalAwaitedPanUndo", "history regression failed: \(error)")
