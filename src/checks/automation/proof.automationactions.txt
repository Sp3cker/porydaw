Assertion correspondence: automation/automationactions.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/automation/automationactions.cpp
Original SHA-256: 32f5e5de2ff62c9d9bf4b0c9b8b73f462a356dd2f83a5f22bfba1e65fad98768
Swift counterpart: src/checks/automation/automationactions.swift
Swift SHA-256: d00e05534bcb9e621426c20fbe36aca9d386c8fb906c735901b97509f866fea1
Additional Swift counterpart: src/checks/automation/automationcanvaslayout.swift
Swift SHA-256: d70079064b3497280fc052f594e9d53e374cd8b65e2e902a92abb83f82727e44
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Swift SHA-256: a9c8ef79e003a9396492f2d27cadc6c88514a664ad5b45872ca79a1a593bb7b7
Additional Swift counterpart: src/checks/keyboard/KeybindingRegistryChecks.swift
Swift SHA-256: 1815b25fa9324fe0c694879db826643105c7fc5563408e0335dbe1d21f91159e

Purpose and status
Each A entry is one original assertion or failure site, including helper and setup sites.
Repeated rows and loops execute a site more than once. The source SHA pins the entire
original sequence; the context inventory below records its rows, loops and fixtures.
MATCHED requires identified equivalent predicates and execution evidence; PARTIAL means
a specific related scenario differs; GAP means exact assertion correspondence is not
established; NATIVE-SETUP identifies a retained C++ loader/window/input precondition.
A method name or related Swift case alone never establishes an assertion match.
This inventory does not authorize removing native checks. Unresolved entries remain work.

Execution and ownership
The original swiftcore suite entrypoints invoke these paired functions in their original
order. Shared setup remains in the owner-prefixed AutomationPageChecks.swift,
VelocityPageChecks.swift, VoiceChangesPageChecks.swift and EditorDrawerChecks.swift.
Core domain scenarios retain their original EventChecks/TimeChecks entrypoint positions.
The shortcut-delivery predicates S028-S029 execute from runKeybindingRegistryChecks
(src/checks/keyboard/KeybindingRegistryChecks.swift) through runProjectSessionSuite
(SessionChecks.swift:11, swiftcore lane) and through the shellwindow lane bootstrap
(ShellQmlTests.swift:169, reached from test_aKeymapNativeSettingsSeeds at
tst_ShellWindow.qml:181). S030-S034 execute in the shellwindow lane as
test_fPencilLatchTextAndSpaceOwnership over the same tst_ShellWindow.qml.

Verification evidence
Command: deno task verify --filter swiftcore --verbose. Result: PASS — build ok (0.15s);
ok swiftcore (10.38s); verify 1/6 ok (5 skipped); run_checks PASS. Controller-run on the
frozen tree pinned by the hashes above; the ledger agent did not run it.
Command: deno task verify:shell --filter shellwindow --verbose. Result: PASS — build ok
(0.16s); ok shellwindow (3.74s); verify 1/11 ok (10 skipped); run_checks PASS.
Controller-run on the same frozen tree; the ledger agent did not run it. The shellwindow
lane executes the tst_ShellWindow.qml rows cited by S030-S034 and re-runs
runKeybindingRegistryChecks (S028/S029, S035) through ShellQmlTests.registryFailures. The legacy automation C++ harness is retired, so this file is an
assertion-obligation inventory, never current execution evidence for the C++ original.
Converted-window command: deno task verify:qml --verbose.
drawerAutomationProjectionInsertionAndPencilClick (automationactions.swift:98) is called at
the end of drawerAutomationProjectionValueBounds, registered by runAutomationPageChecks at
AutomationPageChecks.swift:380, and executes under cppID
automation/AutomationEditingTest::projectionInsertionTiming and cppID
automation/AutomationEditingTest::pencilClickHalfOpenQuantization. Its indexed statements
are S020-S027 below.
Disposition tally: MATCHED 34, PARTIAL 11, GAP 1, NATIVE 5, RETIRED-REPRESENTATION 2
(53 sites).

Fixture, sequence, data-row and loop inventory
Each line below is an exact original source anchor. Multiline declarations, helper
implementations, native event delivery and the complete order remain in the pinned source.
src/checks/automation/automationactions.cpp:64 | QTest::keyClick(&editor, shortcut.key(), shortcut.keyboardModifiers());
src/checks/automation/automationactions.cpp:104 | QTest::qWait(510);
src/checks/automation/automationactions.cpp:143 | for (const LaneHandle lane : {pan, lfo}) {

Original assertion inventory

A001 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:35
Disposition: MATCHED
Mapping/reason: S028 asserts the shipped fixed catalogue registers the automation.pencil_mode command with its editor-routed delivery scope (registry.scope precondition-fails on an unknown id, so the equality proves the binding exists); binding availability stands in for the site non-null QAction guard, and no QAction object or pointer identity is claimed. [S028]
Original expression:
  QVERIFY(action)

A002 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:37
Disposition: MATCHED
Mapping/reason: S029 asserts registry.sequences(automation.pencil_mode) resolves to exactly one unmodified B stroke ([QtKeyCode.b.rawValue]), the shipped binding decoding to a real key rather than Qt::Key_unknown; the C++ read-back QKeyCombination is not reproduced. [S029]
Original expression:
  QVERIFY(shortcut.key() != Qt::Key_unknown)

A003 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:41
Disposition: MATCHED
Mapping/reason: S030 asserts a B press delivered to the focused roll turns BOTH grid.pencilMode and automation.isPencilMode true from the verified-false start state, the same production sync that mirrors the grid flag onto the automation page (ApplicationSession.swift:867-871); this site reads that latch back from the action. [S030]
Original expression:
  QVERIFY(action->isChecked())

A004 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:43
Disposition: MATCHED
Mapping/reason: S031 asserts the B release keeps BOTH grid.pencilMode and automation.isPencilMode true, the checked latch surviving the release that this site reads back. [S031]
Original expression:
  QVERIFY(action->isChecked())

A005 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:46
Disposition: MATCHED
Mapping/reason: S032 asserts the second B press turns BOTH grid.pencilMode and automation.isPencilMode false again, the unlatch this site reads back. [S032]
Original expression:
  QVERIFY(!action->isChecked())

A006 | AutomationEditingTest::actionShortcutLatching | src/checks/automation/automationactions.cpp:48
Disposition: MATCHED
Mapping/reason: S032 asserts the second B release keeps BOTH flags false, the unlatched state surviving the second release that this site reads back. [S032]
Original expression:
  QVERIFY(!action->isChecked())

A007 | AutomationEditingTest::actionTextInputImmunity | src/checks/automation/automationactions.cpp:54
Disposition: MATCHED
Mapping/reason: S028 asserts the same shipped automation.pencil_mode catalogue binding exists with its editor-routed scope; the text-immunity scenario reads the same binding, and no QAction object or pointer identity is claimed. [S028]
Original expression:
  QVERIFY(action)

A008 | AutomationEditingTest::actionTextInputImmunity | src/checks/automation/automationactions.cpp:56
Disposition: MATCHED
Mapping/reason: S029 asserts the same single unmodified B stroke for automation.pencil_mode, the key this text-immunity scenario decodes. [S029]
Original expression:
  QVERIFY(shortcut.key() != Qt::Key_unknown)

A009 | AutomationEditingTest::actionTextInputImmunity | src/checks/automation/automationactions.cpp:62
Disposition: MATCHED
Mapping/reason: S033 asserts the real text probe created under the production window reaches activeFocus (:588) before B is delivered, the focus establishment this QTRY waits for; both layers use a test-owned text input. [S033]
Original expression:
  QTRY_VERIFY(editor.hasFocus())

A010 | AutomationEditingTest::actionTextInputImmunity | src/checks/automation/automationactions.cpp:66
Disposition: MATCHED
Mapping/reason: S033 asserts the delivered B reaches the focused text probe and is kept as its typed b, the same delivered-character observation. [S033]
Original expression:
  QCOMPARE(editor.text(), QStringLiteral("b"))

A011 | AutomationEditingTest::actionTextInputImmunity | src/checks/automation/automationactions.cpp:67
Disposition: MATCHED
Mapping/reason: S033 asserts that while the text probe owns focus the same B press leaves BOTH grid.pencilMode and automation.isPencilMode false, the shortcut suppression this site reads back; the numeric prompt rows S034 assert the same for the velocity input. [S033][S034]
Original expression:
  QVERIFY(!action->isChecked())

A012 | AutomationEditingTest::actionRepeatImmunity | src/checks/automation/automationactions.cpp:73
Disposition: MATCHED
Mapping/reason: S028 asserts the same shipped automation.pencil_mode catalogue binding exists with its editor-routed scope; the auto-repeat scenario reads the same binding, and no QAction object or pointer identity is claimed. [S028]
Original expression:
  QVERIFY(action)

A013 | AutomationEditingTest::actionRepeatImmunity | src/checks/automation/automationactions.cpp:75
Disposition: MATCHED
Mapping/reason: S029 asserts the same single unmodified B stroke for automation.pencil_mode, the key this auto-repeat scenario decodes. [S029]
Original expression:
  QVERIFY(shortcut.key() != Qt::Key_unknown)

A014 | AutomationEditingTest::actionRepeatImmunity | src/checks/automation/automationactions.cpp:79
Disposition: MATCHED
Mapping/reason: S030 asserts the B press latches BOTH grid.pencilMode and automation.isPencilMode true, the press latch this held-key scenario reads back before its 510 ms wait. [S030]
Original expression:
  QVERIFY(action->isChecked())

A015 | AutomationEditingTest::actionRepeatImmunity | src/checks/automation/automationactions.cpp:83
Disposition: PARTIAL
Mapping/reason: Related predicate S031 holds B for 520 ms without release and asserts both grid.pencilMode and automation.isPencilMode stay true; unproved: the synthetic auto-repeat pair itself, because QTest cannot set QKeyEvent.isAutoRepeat, so neither lane synthesizes the autorep press/release this site delivers and only the un-synthesized hold is observed. [S031]
Original expression:
  QVERIFY(action->isChecked())

A016 | AutomationEditingTest::actionRepeatImmunity | src/checks/automation/automationactions.cpp:86
Disposition: MATCHED
Mapping/reason: S031 asserts the real B release after the 520 ms hold leaves BOTH grid.pencilMode and automation.isPencilMode true, the release behaviour this site reads back. [S031]
Original expression:
  QVERIFY(action->isChecked())

A017 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:97
Disposition: MATCHED
Mapping/reason: S028 asserts the same shipped automation.pencil_mode catalogue binding exists with its editor-routed scope; the held-key scenario reads the same binding, and no QAction object or pointer identity is claimed. [S028]
Original expression:
  QVERIFY(action)

A018 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:99
Disposition: MATCHED
Mapping/reason: S029 asserts the same single unmodified B stroke for automation.pencil_mode, the key this held-key scenario decodes. [S029]
Original expression:
  QVERIFY(shortcut.key() != Qt::Key_unknown)

A019 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:103
Disposition: MATCHED
Mapping/reason: S030 asserts a B press from the verified-false start state latches BOTH grid.pencilMode and automation.isPencilMode true, the latch this scenario establishes before its 510 ms wait. [S030]
Original expression:
  QVERIFY(action->isChecked())

A020 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:106
Disposition: MATCHED
Mapping/reason: S031 asserts the latch survives the 520 ms held key (both flags still true after wait(520)) and that the following release keeps them true, the 510 ms hold behaviour this site reads back. [S031]
Original expression:
  QVERIFY(action->isChecked())

A021 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:110
Disposition: MATCHED
Mapping/reason: S030 asserts a fresh B press from the verified-false start state re-latches BOTH grid.pencilMode and automation.isPencilMode true, which is this forced-off re-press observation; the intervening toggle cycle of the same test is asserted by S032 (second B press and release leave both flags false). [S030][S032]
Original expression:
  QVERIFY(action->isChecked())

A022 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:112
Disposition: PARTIAL
Mapping/reason: The presenter route this guard precedes is exercised through the production page (S009 and S011 drive a real pan drag on the same fixture), but no Swift predicate asserts a rendered row handle's validity: the canvas row handle is not part of the converted page's published surface.
Original expression:
  QVERIFY(pan.valid())

A023 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:113
Disposition: PARTIAL
Mapping/reason: The converted page activates parameters through the production catalog, the same route S009 exercises, but the original's activateParameter return through a rendered tab has no equivalent predicate.
Original expression:
  QVERIFY(activateParameter({EditorAutomationRowKind::ControlChange, 0, kPanController}))

A024 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:115
Disposition: NATIVE
Mapping/reason: Original native QQuickWindow/QQuickItem/scene-graph obligation (the computed press point must lie inside the automation input item's bounds). No presenter-level Swift check can establish this rendered observation; the converted-window lane (deno task verify:qml) is the only place it could be observed.
Original expression:
  QVERIFY(automationInput().bounds().contains(start))

A025 | AutomationEditingTest::actionHeldKeyGestures | src/checks/automation/automationactions.cpp:119
Disposition: GAP
Mapping/reason: The keyboard half of this conjunction is now asserted (S030/S031 hold B and keep BOTH grid.pencilMode and automation.isPencilMode latched), but no predicate delivers a pointer press/release under the held B, so the pointer half is unexercised and the conjunction stays unproved.
Original expression:
  QVERIFY(action->isChecked())

A026 | AutomationEditingTest::projectionPartialCell | src/checks/automation/automationactions.cpp:126
Disposition: RETIRED-REPRESENTATION
Mapping/reason: PlaybackTimeline.build is non-optional in Swift, so the C++ nullable timeline pointer has no failure branch and no Swift predicate can observe it. The cell the original then reads is asserted by S016 (half-open and inside the song) and S015 (its end is the song end).
Original expression:
  QVERIFY(timeline)

A027 | AutomationEditingTest::projectionPartialCell | src/checks/automation/automationactions.cpp:129
Disposition: MATCHED
Mapping/reason: S016 asserts the projection's cell is half-open and inside the song (tickBegin < tickEnd) under the same production projection the original snaps at the song length; S015 pins that end cell's tickEnd. [S016]
Original expression:
  QVERIFY(finalCell.tickBegin < finalCell.tickEnd)

A028 | AutomationEditingTest::projectionPartialCell | src/checks/automation/automationactions.cpp:130
Disposition: MATCHED
Mapping/reason: S015 asserts the cell at the song end ends exactly there (endCell.tickEnd == songEndTick), the same observation as finalCell.tickEnd == timeline->lengthTicks. [S015]
Original expression:
  QCOMPARE(finalCell.tickEnd, timeline->lengthTicks)

A029 | AutomationEditingTest::projectionValueBounds | src/checks/automation/automationactions.cpp:138
Disposition: PARTIAL
Mapping/reason: S013 and S014 execute the value-axis round-trip against the fixture's pan parameter, but no Swift predicate asserts the original's rendered row validity for that parameter.
Original expression:
  QTRY_VERIFY(findRow(panRow).valid())

A030 | AutomationEditingTest::projectionValueBounds | src/checks/automation/automationactions.cpp:139
Disposition: PARTIAL
Mapping/reason: S013 and S014 exercise the same bounds against the production parameter model, but no Swift predicate asserts the original's rendered row validity for the LFO row.
Original expression:
  QTRY_VERIFY(findRow(lfoRow).valid())

A031 | AutomationEditingTest::projectionValueBounds | src/checks/automation/automationactions.cpp:145
Disposition: NATIVE
Mapping/reason: Original native QQuickWindow/QQuickItem/scene-graph obligation (the QQuickItem lane body rect is non-empty). No presenter-level Swift check can establish this rendered observation; the converted-window lane (deno task verify:qml) is the only place it could be observed.
Original expression:
  QVERIFY(!body.isEmpty())

A032 | AutomationEditingTest::projectionValueBounds | src/checks/automation/automationactions.cpp:148
Disposition: MATCHED
Mapping/reason: S013 asserts the value axis minimum maps back to zero (projection.value(atY: yMin) == 0), the same round-trip the original computes at the lane body's minimum y. [S013]
Original expression:
  QCOMPARE(qRound(AutomationProjection::valueAtY(body, geometry, 0, 127, minimum.y())), 0)

A033 | AutomationEditingTest::projectionValueBounds | src/checks/automation/automationactions.cpp:149
Disposition: MATCHED
Mapping/reason: S014 asserts the value axis maximum maps back to full scale (projection.value(atY: yMax) == 127), the same round-trip the original computes at the maximum y. [S014]
Original expression:
  QCOMPARE(qRound(AutomationProjection::valueAtY(body, geometry, 0, 127, maximum.y())), 127)

A034 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:156
Disposition: PARTIAL
Mapping/reason: The converted row stack publishes AutomationCatalog.count rows (S018) with Tempo first (S019), but the original observes the rendered canvas row list and no Swift predicate asserts that list's size.
Original expression:
  QVERIFY(rows.size() > 1)

A035 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:159
Disposition: NATIVE
Mapping/reason: Original native QQuickWindow/QQuickItem/scene-graph obligation (the first row's QQuickItem lane body rect is non-empty). No presenter-level Swift check can establish this rendered observation; the converted-window lane (deno task verify:qml) is the only place it could be observed.
Original expression:
  QVERIFY(!body.isEmpty())

A036 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:160
Disposition: RETIRED-REPRESENTATION
Mapping/reason: The original's per-row QQuickItem body origin (body.top() == 0) has no ingress in the converted page: lanes are placed from AutomationPlotBounds rather than a per-row item origin and no Swift predicate publishes a per-row item rect. The row order it corroborates is asserted by S019.
Original expression:
  QCOMPARE(body.top(), 0)

A037 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:161
Disposition: PARTIAL
Mapping/reason: The converted row stack's first row is pinned to Tempo (S019), but no Swift predicate asserts that the second row's kind is a ControlChange as this site observes.
Original expression:
  QVERIFY(rows[1].id.kind == EditorAutomationRowKind::ControlChange)

A038 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:164
Disposition: NATIVE
Mapping/reason: Original native QQuickWindow/QQuickItem/scene-graph obligation (the mapped y must lie inside the native lane body's top edge). No presenter-level Swift check can establish this rendered observation; the converted-window lane (deno task verify:qml) is the only place it could be observed.
Original expression:
  QVERIFY(input.y() >= qreal(body.top()))

A039 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:165
Disposition: NATIVE
Mapping/reason: Original native QQuickWindow/QQuickItem/scene-graph obligation (the mapped y must lie inside the native lane body's bottom edge). No presenter-level Swift check can establish this rendered observation; the converted-window lane (deno task verify:qml) is the only place it could be observed.
Original expression:
  QVERIFY(input.y() < qreal(body.bottom()))

A040 | AutomationEditingTest::projectionCanvasOrigin | src/checks/automation/automationactions.cpp:166
Disposition: PARTIAL
Mapping/reason: S015 pins the neutral value's midpoint placement (projection.y(64) is within a pixel of the lane midpoint) and S013/S014 the endpoint round-trip, but no Swift predicate asserts the original's exact round-trip value(atY: y(64)) == 64.
Original expression:
  QCOMPARE(qRound(AutomationProjection::valueAtY(body, AutomationGeometry::resolve(), 0, 127,
                                                     input.y())),
               64)

A041 | AutomationEditingTest::projectionInsertionTiming | src/checks/automation/automationactions.cpp:175
Disposition: PARTIAL
Mapping/reason: The timing predicate executes on the activated production pan lane (S020), but no Swift predicate asserts the rendered row-handle validity this guard observes: the converted page publishes no row handle. [S020]
Original expression:
  QVERIFY(pan.valid())

A042 | AutomationEditingTest::projectionInsertionTiming | src/checks/automation/automationactions.cpp:189
Disposition: MATCHED
Mapping/reason: S020 asserts the intermediate raw tick differs from the mapped cell begin under the same projection and fixture. [S020]
Original expression:
  QVERIFY(rawTick != double(mappedCell.tickBegin))

A043 | AutomationEditingTest::projectionInsertionTiming | src/checks/automation/automationactions.cpp:190
Disposition: MATCHED
Mapping/reason: S020 asserts the intermediate raw tick differs from the fine-grid caret tick on the same fixture. [S020]
Original expression:
  QVERIFY(rawTick != caretTick)

A044 | AutomationEditingTest::projectionInsertionTiming | src/checks/automation/automationactions.cpp:191
Disposition: MATCHED
Mapping/reason: S021 asserts insertion with pencil mode floors to the half-open cell begin, the same insertionTick(projection, true) comparison. [S021]
Original expression:
  QCOMPARE(indicator.insertionTick(projection, true), double(mappedCell.tickBegin))

A045 | AutomationEditingTest::projectionInsertionTiming | src/checks/automation/automationactions.cpp:192
Disposition: MATCHED
Mapping/reason: S022 asserts insertion without pencil mode uses the fine-grid caret, the same insertionTick(projection, false) comparison. [S022]
Original expression:
  QCOMPARE(indicator.insertionTick(projection, false), caretTick)

A046 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:199
Disposition: PARTIAL
Mapping/reason: The pencil-click predicate executes on the activated production pan lane (S023-S026: the pointer route writes at the half-open cell begin and then the following cell), but no Swift predicate asserts the rendered row-handle validity this guard observes. [S023]
Original expression:
  QVERIFY(pan.valid())

A047 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:200
Disposition: PARTIAL
Mapping/reason: The pencil-click predicate activates the pan parameter through the production catalog (fixture.activate to page.activateParameter) and its click path then executes (S023), but the original activateParameter return through a rendered tab has no equivalent predicate. [S023]
Original expression:
  QVERIFY(activateParameter(panRow))

A048 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:213
Disposition: MATCHED
Mapping/reason: S023 asserts the first half-open cell holds the clicked value 72, the same findLanePoint at the mapped cell begin and its value comparison; S027 strengthens it with a click three quarters through the cell. [S023][S027]
Original expression:
  QTRY_VERIFY(tab().document().findLanePoint(0, kPanController, mappedCell.tickBegin, &first))

A049 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:214
Disposition: MATCHED
Mapping/reason: S023 asserts the point at the half-open cell begin holds value 72, the same first.value the original reads. [S023]
Original expression:
  QCOMPARE(first.value, 72)

A050 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:217
Disposition: MATCHED
Mapping/reason: S024 asserts the following cell is a nonempty half-open interval, the same following tick-begin/tick-end guard. [S024]
Original expression:
  QVERIFY(following.tickBegin < following.tickEnd)

A051 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:223
Disposition: MATCHED
Mapping/reason: S026 asserts a point exists at the following cell begin holding the second clicked value, the same findLanePoint at following.tickBegin. [S026]
Original expression:
  QTRY_VERIFY(
          tab().document().findLanePoint(0, kPanController, following.tickBegin, &followingPoint))

A052 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:225
Disposition: MATCHED
Mapping/reason: S025 asserts the first cell still holds 72 after the following click, the same first.value re-check. [S025]
Original expression:
  QCOMPARE(first.value, 72)

A053 | AutomationEditingTest::pencilClickHalfOpenQuantization | src/checks/automation/automationactions.cpp:226
Disposition: MATCHED
Mapping/reason: S026 asserts the following cell holds value 96, the same followingPoint.value comparison. [S026]
Original expression:
  QCOMPARE(followingPoint.value, 96)

Swift executable assertion index
The S entries are exact predicates already called by the suite. Their presence alone
does not close any A entry. Preserve their surrounding setup and order in the paired source.

S001 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:12
Swift expression:
  report.expectEqual(AutomationModifiers(), AutomationQtModifier.automation(0), cppID: drawerAutomationModifierMappingID,
                         what: "no Qt bit arms no policy")

S002 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:14
Swift expression:
  report.expectEqual(AutomationModifiers(shift: true),
                         AutomationQtModifier.automation(AutomationQtModifier.shift), cppID: drawerAutomationModifierMappingID,
                         what: "Qt's shift bit arms the ramp and axis-lock policy")

S003 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:17
Swift expression:
  report.expectEqual(AutomationModifiers(snapValue: true),
                         AutomationQtModifier.automation(AutomationQtModifier.control),
                         cppID: drawerAutomationModifierMappingID,
                         what: "Qt's control bit arms the value snap")

S004 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:21
Swift expression:
  report.expectEqual(AutomationModifiers(fine: true),
                         AutomationQtModifier.automation(AutomationQtModifier.alt), cppID: drawerAutomationModifierMappingID,
                         what: "Qt's alt bit arms the fine lattice")

S005 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:24
Swift expression:
  report.expectEqual(AutomationModifiers(),
                         AutomationQtModifier.automation(AutomationQtModifier.meta), cppID: drawerAutomationModifierMappingID,
                         what: "Qt's meta bit arms nothing")

S006 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:27
Swift expression:
  report.expectEqual(AutomationModifiers(fine: true, snapValue: true, shift: true),
                         AutomationQtModifier.automation(AutomationQtModifier.shift
                                                         | AutomationQtModifier.control
                                                         | AutomationQtModifier.alt),
                         cppID: drawerAutomationModifierMappingID,
                         what: "the three policy bits compose through the same mapping")

S007 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:39
Swift expression:
  report.expectEqual(policy, AutomationQtModifier.automation(drawerAutomationQtModifiers(policy)),
                             cppID: drawerAutomationModifierMappingID,
                             what: "the Qt bits that policy composes to map back to it")

S008 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:52
Swift expression:
  report.expect(threshold > 1, cppID: drawerAutomationModifierMappingID,
                    message: "the neutral radius covers more than one value step")

S009 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:56
Swift expression:
  report.expect(plain.drag(plain.panLane, from: (24, 80), to: near, modifiers: 0),
                    cppID: drawerAutomationModifierMappingID,
                    message: "the pointer route took the drag without a Qt modifier bit")

S010 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:59
Swift expression:
  report.expectEqual(["24:\(near)"], plain.values(plain.panLane), cppID: drawerAutomationModifierMappingID,
                         what: "a drag without the Qt control bit keeps the dragged value")

S011 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:64
Swift expression:
  report.expect(snapped.drag(snapped.panLane, from: (24, 80), to: near,
                                 modifiers: AutomationQtModifier.control),
                    cppID: drawerAutomationModifierMappingID,
                    message: "the pointer route took the drag carrying the Qt control bit")

S012 | drawerAutomationQtModifierMapping | src/checks/automation/automationactions.swift:68
Swift expression:
  report.expectEqual(["24:64"], snapped.values(snapped.panLane), cppID: drawerAutomationModifierMappingID,
                         what: "the Qt control bit a QML event carries lands the drag on the neutral")

S013 | drawerAutomationProjectionValueBounds | src/checks/automation/automationactions.swift:88
Swift expression:
  report.expectEqual(0, projection.value(atY: yMin, metadata: metadata), cppID: id,
                     what: "the value axis minimum maps back to zero")

S014 | drawerAutomationProjectionValueBounds | src/checks/automation/automationactions.swift:90
Swift expression:
  report.expectEqual(127, projection.value(atY: yMax, metadata: metadata), cppID: id,
                     what: "the value axis maximum maps back to full scale")

S015 | drawerAutomationProjectionValueBounds | src/checks/automation/automationactions.swift:92
Swift expression:
  report.expect(abs(projection.y(64, metadata: metadata) - 60) < 1.0, cppID: id,
                message: "the neutral value maps within a pixel of the lane midpoint")

S016 | drawerAutomationHitGeometry | src/checks/automation/automationcanvaslayout.swift:61
Swift expression:
  report.expect(cell.tickBegin < cell.tickEnd && cell.tickEnd <= fixture.songEndTick,
                    cppID: drawerAutomationLayoutID, message: "a cell is half-open and inside the song")

S017 | drawerAutomationHitGeometry | src/checks/automation/automationcanvaslayout.swift:68
Swift expression:
  report.expectEqual(fixture.songEndTick, endCell.tickEnd, cppID: drawerAutomationLayoutID,
                         what: "the song's end belongs to the cell that ends there")

S018 | drawerAutomationScaleLabelsAndLaneCounts | src/checks/automation/presentation/painting.swift:43
Swift expression:
  report.expectEqual(AutomationCatalog.count, stack.rows.count, cppID: drawerAutomationLabelsID,
                         what: "the row stack carries one row per catalog parameter")

S019 | drawerAutomationScaleLabelsAndLaneCounts | src/checks/automation/presentation/painting.swift:45
Swift expression:
  report.expectEqual(AutomationParameter.tempo, stack.rows[0].parameter, cppID: drawerAutomationLabelsID,
                         what: "the Tempo row leads the stack")

S020 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:118
Swift expression:
  report.expect(rawTick != Double(mappedCell.tickBegin) && rawTick != Double(caretTick),
                cppID: timingID, message: "the intermediate position is neither the cell begin nor caret")

S021 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:120
Swift expression:
  report.expectEqual(mappedCell.tickBegin, projection.insertionTick(atX: x, pencil: true),
                     cppID: timingID, what: "pencil insertion floors to the half-open cell begin")

S022 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:122
Swift expression:
  report.expectEqual(caretTick, projection.insertionTick(atX: x, pencil: false),
                     cppID: timingID, what: "non-pencil insertion uses the fine-grid caret")

S023 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:132
Swift expression:
  report.expectEqual([72], fixture.lanePoints(fixture.panLane)
      .filter { $0.tick == mappedCell.tickBegin }.map(\.value),
      cppID: clickID, what: "the first half-open cell holds the clicked value")

S024 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:137
Swift expression:
  report.expect(following.tickBegin < following.tickEnd, cppID: clickID,
                message: "the following cell is a nonempty half-open interval")

S025 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:147
Swift expression:
  report.expectEqual([72], fixture.lanePoints(fixture.panLane)
      .filter { $0.tick == mappedCell.tickBegin }.map(\.value),
      cppID: clickID, what: "the first cell survives the following click")

S026 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:150
Swift expression:
  report.expectEqual([96], fixture.lanePoints(fixture.panLane)
      .filter { $0.tick == following.tickBegin }.map(\.value),
      cppID: clickID, what: "the following half-open cell holds the second clicked value")

S027 | drawerAutomationProjectionInsertionAndPencilClick | src/checks/automation/automationactions.swift:167
Swift expression:
  report.expectEqual([72], biased.lanePoints(biased.panLane)
      .filter { $0.tick == cell.tickBegin }.map(\.value),
      cppID: clickID, what: "a late-cell click still writes at the half-open cell begin")

S028 | runKeybindingRegistryChecks (deliveryScopes) | src/checks/keyboard/KeybindingRegistryChecks.swift:92
Swift expression:
  for row in scopeRows {
      onAssertion(registry.scope(row.id) == row.expected, deliveryScopes, row.id)
  }
      with the row ("automation.pencil_mode", .editorRouted) at :87; registry.scope
      precondition-fails on an unknown id, so this equality asserts both the shipped
      binding's presence and its editor-routed delivery scope.
      cppID: KeymapCheckTest::deliveryScopes.

S029 | runKeybindingRegistryChecks (deliveryScopes) | src/checks/keyboard/KeybindingRegistryChecks.swift:105
Swift expression:
  onAssertion(
      registry.sequences("automation.pencil_mode").map(\.strokes) ==
          [[QtKeyCode.b.rawValue]],
      deliveryScopes,
      "Pencil Mode keeps unmodified B")
      with the defaultMatching row ("automation-pencil", "automation.pencil_mode",
      QtKeyCode.b.rawValue, QtKeyboardModifier.none.rawValue, true) at :122 asserting
      registry.matches(0x42, 0, "automation.pencil_mode"). cppID: KeymapCheckTest::deliveryScopes.

S030 | test_fPencilLatchTextAndSpaceOwnership | src/checks/editorqml/tst_ShellWindow.qml:558
Swift expression:
  keyPress(Qt.Key_B) with tryCompare(grid, "pencilMode", true, 3000) at :559 and
  tryCompare(automation, "isPencilMode", true, 3000) at :560, after
  compare(grid.pencilMode, false) and compare(automation.isPencilMode, false) at :554-555
  establish the off state; automation is session.automationPage().

S031 | test_fPencilLatchTextAndSpaceOwnership | src/checks/editorqml/tst_ShellWindow.qml:561
Swift expression:
  wait(520); compare(grid.pencilMode, true, "the pencil latch survives the B hold without release") at
  :562; compare(automation.isPencilMode, true, "holding B preserves automation pencil mode")
  at :563; keyRelease(Qt.Key_B) at :564; compare(grid.pencilMode, true, "releasing B keeps
  the pencil latched") at :565 and compare(automation.isPencilMode, true, "releasing B keeps
  automation pencil latched") at :566.

S032 | test_fPencilLatchTextAndSpaceOwnership | src/checks/editorqml/tst_ShellWindow.qml:575
Swift expression:
  keyPress(Qt.Key_B) at :575 with tryCompare(grid, "pencilMode", false, 3000) at :576 and
  tryCompare(automation, "isPencilMode", false, 3000) at :577; keyRelease(Qt.Key_B) at :578;
  compare(grid.pencilMode, false, "releasing the second B preserves the off state") at :579
  and compare(automation.isPencilMode, false, "releasing the second B preserves the
  automation off state") at :580-581.

S033 | test_fPencilLatchTextAndSpaceOwnership | src/checks/editorqml/tst_ShellWindow.qml:587
Swift expression:
  textProbe.forceActiveFocus(Qt.OtherFocusReason) at :587 with
  tryCompare(textProbe, "activeFocus", true, 3000) at :588; keyClick(Qt.Key_B) at :589;
  compare(textProbe.text.toLowerCase(), "b", "text focus keeps its typed B") at :590;
  compare(grid.pencilMode, false, "text-field B never enables the pencil") at :591 and
  compare(automation.isPencilMode, false, "text-field B never changes automation pencil
  mode") at :592-593.

S034 | test_fPencilLatchTextAndSpaceOwnership | src/checks/editorqml/tst_ShellWindow.qml:601
Swift expression:
  var field = findChild(surface, "noteVelocityInput") at :599 with
  tryCompare(field, "activeFocus", true, 3000) at :601; keyClick(Qt.Key_B) at :603;
  compare(field.text, numericText, "numeric text rejects the nonnumeric B") at :604;
  compare(grid.pencilMode, false, "numeric B never reaches the pencil shortcut") at :605 and
  compare(automation.isPencilMode, false, "numeric B never changes automation pencil mode")
  at :606-607.

S035 | test_aKeymapNativeSettingsSeeds (shellwindow lane) | src/checks/editorqml/tst_ShellWindow.qml:181
Swift expression:
  var failures = bootstrap.registryFailures();
  compare(failures.length, 0, "the complete native keymap assertions: " + failures.join("; "))
      ShellQmlBootstrap.registryFailures (ShellQmlTests.swift:169) runs
      runKeybindingRegistryChecks and collects every failed assertion, so this row is the
      shellwindow lane's execution binding for S028/S029; the swiftcore lane runs the same
      predicates through runProjectSessionSuite (SessionChecks.swift:11).
