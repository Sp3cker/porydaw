Assertion correspondence: swiftrollbench/tst_swiftrollbench.cpp

Reference revision: 65a72a959c853ec155871d9b33065d897d704aa8
Original: src/checks/swiftrollbench/tst_swiftrollbench.cpp
Original SHA-256: 4ef9cc29122dd7d160fe3cd916d6d49f538ba4f81a7d437635e0d24487b78178
Swift counterpart: none identified
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftrollbench --verbose
Result: pending integrated run.

A001 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:130
Disposition: RETIRED-REPRESENTATION

A002 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:133
Disposition: RETIRED-REPRESENTATION

A003 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:137
Disposition: RETIRED-REPRESENTATION

A004 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:139
Disposition: RETIRED-REPRESENTATION

A005 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:143
Disposition: RETIRED-REPRESENTATION

A006 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:144
Disposition: RETIRED-REPRESENTATION

A007 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:154
Disposition: RETIRED-REPRESENTATION

A008 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:158
Disposition: RETIRED-REPRESENTATION

A009 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:160
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;
      const QRectF rollBand = root->property("rollBandRect").toRectF();

Original expression:
  QVERIFY2(rollBand.isValid() && !rollBand.isEmpty(),
               "the roll band rect must be published before the cadence")

A010 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:183
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;
      const QPointF rollCenter = root->mapToScene(rollBand.center());

      FrameStamps stamps;
      stamps.clock.start();
      QObject frameProbe;
      QObject::connect(
          quickWin, &QQuickWindow::frameSwapped, &frameProbe,
          [&stamps] {
              if (stamps.recording)
                  stamps.stamps.push_back(stamps.clock.nsecsElapsed());
          },
          Qt::QueuedConnection);

      // Warm-up: one swapped frame proves the render loop is live before the
      // measured phases; the stamps collected here are discarded.
      stamps.recording = true;
      quickWin->update();
      QElapsedTimer warmup;
      warmup.start();
      while (stamps.stamps.empty() && warmup.elapsed() < kWarmupDeadlineMs)
          QTest::qWait(10);

Original expression:
  QVERIFY2(!stamps.stamps.empty(), "the Quick window must swap a frame before the cadence")

A011 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:231
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;
      stamps.stamps.clear();

      // One phase of paced wheel notches. The notch direction alternates: a
      // single-direction cadence saturates the roll's scroll/zoom clamp within a
      // handful of notches (the Swift viewport reaches its content bound, the
      // C++ camera its zoom limit), after which the roll publishes no scene
      // update at all and the phase would time an idle window instead of frame
      // cost. Alternating keeps every iteration a real update in both flavors.
      const auto runPhase = [&](int iterations, Qt::KeyboardModifiers modifiers) {
          PhaseResult result;
          MovementToken previous = movementToken(view, swiftViewport);
          stamps.stamps.clear();
          stamps.recording = true;
          for (int i = 0; i < iterations; ++i) {
              sendWheel(*quickWin, rollCenter, i % 2 == 0 ? -kWheelNotch : kWheelNotch, modifiers);
              QTest::qWait(kPaceMs);
              const MovementToken current = movementToken(view, swiftViewport);
              result.moved = result.moved || current != previous;
              previous = current;
          }
          stamps.recording = false;
          // Settle after the cadence: the tail frames are not part of the
          // measured cadence, and the last notch's effect must land before the
          // next phase starts.
          QTest::qWait(kSettleMs);
          result.stats = summarize(stamps.stamps);
          return result;
      };

      const PhaseResult scroll = runPhase(kScrollIterations, Qt::NoModifier);
      const PhaseResult zoom = runPhase(kZoomIterations, Qt::ControlModifier);

      const auto report = [&roll](const char *phase, const PhaseResult &result) {
          qInfo().noquote() << QStringLiteral(
                                   "SWIFTROLLBENCH roll=%1 phase=%2 frames=%3 median_us=%4 "
                                   "p95_us=%5 max_us=%6 viewport_moved=%7")
                                   .arg(roll)
                                   .arg(QString::fromLatin1(phase))
                                   .arg(result.stats.frames)
                                   .arg(result.stats.medianUs)
                                   .arg(result.stats.p95Us)
                                   .arg(result.stats.maxUs)
                                   .arg(result.moved ? 1 : 0);
      };
      report("scroll", scroll);
      report("zoom", zoom);


Original expression:
  QVERIFY2(scroll.stats.frames >= kMinimumFramesPerPhase,
               "the scroll cadence must swap at least 24 frames")

A012 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:233
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(zoom.stats.frames >= kMinimumFramesPerPhase,
               "the zoom cadence must swap at least 24 frames")

A013 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:235
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(scroll.moved, "the scroll cadence must move the active roll's viewport")

A014 | SwiftRollBenchTest::testScrollZoomFrameCadence | src/checks/swiftrollbench/tst_swiftrollbench.cpp:236
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/swiftrollbench/tst_swiftrollbench.cpp) no longer builds or runs. Previous native note: Native window/frame-cadence observation on the mounted roll (exposure, active state, swapped-frame stamps, viewport/camera movement tokens, printed SWIFTROLLBENCH timing). No Swift counterpart exists. A presenter-level Swift check cannot establish frame delivery or timing.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(zoom.moved, "the zoom cadence must move the active roll's viewport")

Remaining original source (closing sequence and helper bodies):
  ;
  }

  int runSwiftRollBenchCheck(const QString &projectRoot, const QString &song,
                             const QStringList &qtArguments)
  {
      SwiftRollBenchTest test(projectRoot, song);
      QStringList arguments = {QStringLiteral("swiftrollbench")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }


Swift assertion predicates
No Swift counterpart exists in this checkout, so no S entries exist.

Shared helper:
No shared Swift fixture file exists for this lane.
