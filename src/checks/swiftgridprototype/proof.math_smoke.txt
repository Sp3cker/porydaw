Assertion correspondence: swiftgridprototype/math_smoke.cpp

Reference revision: 18fee9357273c3351d1eaadf4fe6f8d52edb9a98
Original: src/checks/swiftgridprototype/math_smoke.cpp
Original SHA-256: 71563857606606545677f277b7fbef96e053b3bfdce42cc6dcd81452f7e732ac
Swift counterpart: src/checks/rollcheck/static/camera.swift
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Deleted in: fe1ff4df9f4834cbd4e4e59d2e710078c10a8ced Wave 4 re-scoped grid milestone: writable Swift roll through production seams
Note: swiftrollgated/*.swift matches no file in this checkout; the receiving lane is C++-only (tst_swiftrollgated.cpp). The deletion commit records the load-bearing prototype rows as moved to swiftrollgated (testBandLeadingGripDecline, testBandTrailingGripResizeCommitOnce).
Retired in: feature/w2-retired-prototypes — this ledger is the deletion certificate for
src/checks/swiftgridprototype/math_smoke.cpp. The C++ original is uncompiled in every CMake target and
unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt,
src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from
disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired shim. The
controller deletes the dead sources after this ledger passes; nothing is deleted by this edit.

Reference revision: 18fee9357273c3351d1eaadf4fe6f8d52edb9a98
Original: src/checks/swiftgridprototype/math_smoke.cpp
Original SHA-256: 71563857606606545677f277b7fbef96e053b3bfdce42cc6dcd81452f7e732ac
Verification: lane run recorded by the controller, not re-run for this ledger:
deno task verify --filter swiftcore --verbose -> verify: 1/6 ok (11.40s, 5 skipped); run_checks: PASS
(all harnesses in 11.40s).
Result: swiftcore PASS. No live native math_smoke run exists or is claimed; the deleted shim neither builds
nor executes.

Deletion certificate
Retirement: zero assertion sites; there is nothing to dispose and no disposition to change. This file
records the sgm_* shim role (pure forwarding over CoreTimeDefaults::shiftTickClamped/tickFromDouble,
songview::TimeAxis and songview::PitchProjection) and its production owners: presenter-level time/pitch
math in src/checks/rollcheck/static/camera.swift::runEditorCameraChecks:9, executed in the swiftcore lane.
Parity-sweep execution lived in the likewise-deleted prototype MathSelftest.swift. No whole-case
equivalence, native execution or parity claim is made.

Status and evidence
The deleted math_smoke.cpp contains zero assertion sites: it is a pure parity-oracle forwarding shim (sgm_* over CoreTimeDefaults, songview::TimeAxis, and songview::PitchProjection) with no QVERIFY/QCOMPARE/QFAIL/require construct. There is nothing to dispose; the forwarding functions are listed below so the shim is not mistaken for dropped coverage. Parity-sweep execution lived in the likewise-deleted prototype MathSelftest.swift; presenter-level time/pitch math is covered by S001-S041 and S064-S076. Reference inventories only, never current execution evidence.
Execution boundary: SwiftGridPrototype is retired: its C++ sources were deleted in fe1ff4df9f4834cbd4e4e59d2e710078c10a8ced and no target/registration for this prototype remains.

Execution and ownership
No execution is paired: the shim forwarded to production C++ oracles and the parity sweep ran in the deleted prototype MathSelftest.swift. Presenter-level time/pitch coverage that overlaps the oracle domain lives in runEditorCameraChecks/checkGridCameraWheel (S001-S041) and checkProjection (S064-S076), which do not call these sgm_ symbols.

Fixture, sequence and row constraints
No rows, loops, or guards carry assertions; the SGTimeMapFixture fill and the capacity/needed two-pass grid-line protocol are pure data plumbing around the forwarding calls.
Original setup, branch guards, and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory
No assertion sites: this file contains no QVERIFY, QVERIFY2, QCOMPARE, QFAIL, QTRY_*, QEXPECT_FAIL, QSKIP, or explicit failure/report call. Forwarded symbols:

  sgm_shift_tick_clamped (no predicate; forwarding shim)
  sgm_tick_from_double (no predicate; forwarding shim)
  fillTimelineFromFixture (no predicate; forwarding shim)
  sgm_timeaxis_answer (no predicate; forwarding shim)
  sgm_timeaxis_grid_lines (no predicate; forwarding shim)
  makePitchProjection (no predicate; forwarding shim)
  sgm_pitch_row_for_pitch (no predicate; forwarding shim)
  sgm_pitch_nearest_visible (no predicate; forwarding shim)
  sgm_pitch_row_top (no predicate; forwarding shim)
  sgm_pitch_y_to_pitch (no predicate; forwarding shim)
Pure forwarding over CoreTimeDefaults::shiftTickClamped/tickFromDouble, songview::TimeAxis (segmentAt/signatureAt/ticksPerBeat/lengthTicks/loop ticks/forEachGridLine), and songview::PitchProjection (buildChromatic/buildFromPitches/rowForPitch/nearestVisiblePitch/rowTop/yToPitch). Related Swift domain coverage, not calls: S001-S041 (camera/time math) and S064-S076 (checkProjection pitch math).

Remaining original source (full file, verbatim; no assertion tail to split):
// Parity-seam oracles for the Swift Wave-1 tick helpers (spec §4, Task 1):
// thin forwarding over the production CoreTimeDefaults — no logic of its own.
// Tasks 2–3 append the time-axis and pitch groups to this file.

#include "math_smoke.h"

#include "core/miditimeline.h"
#include "core/timedefaults.h"
#include "ui/pitchprojection.h"
#include "ui/songview/timeaxis.h"

uint32_t sgm_shift_tick_clamped(uint32_t tick, int64_t delta)
{
    return CoreTimeDefaults::shiftTickClamped(static_cast<Tick>(tick), delta);
}

uint32_t sgm_tick_from_double(double tick)
{
    return CoreTimeDefaults::tickFromDouble(tick);
}

// Parity-seam oracles for the Swift Wave-1 TimeAxis port (spec §4, Task 2):
// thin forwarding over the production songview::TimeAxis bound to a
// stack-constructed MidiTimeline whose viewer fields are filled from the
// SGTimeMapFixture — no logic of its own. Swift never sees MidiTimeline.

namespace {

void fillTimelineFromFixture(const SGTimeMapFixture *map, MidiTimeline &timeline)
{
    timeline.ticksPerBeat = map->ticksPerBeat;
    timeline.lengthTicks = static_cast<Tick>(map->lengthTicks);
    timeline.loopStartTick = static_cast<Tick>(map->loopStartTick);
    timeline.loopEndTick = static_cast<Tick>(map->loopEndTick);
    timeline.timeSigs.clear();
    for (size_t i = 0; i < map->timeSigCount; ++i) {
        TimeSigPoint point;
        point.tick = static_cast<Tick>(map->timeSigs[i].tick);
        point.numerator = map->timeSigs[i].numerator;
        point.denomPow2 = map->timeSigs[i].denomPow2;
        timeline.timeSigs.push_back(point);
    }
}

} // namespace

void sgm_timeaxis_answer(const SGTimeMapFixture *map, uint32_t tick, SGTimeAxisAnswer *out)
{
    MidiTimeline timeline;
    fillTimelineFromFixture(map, timeline);
    songview::TimeAxis axis;
    axis.bind(&timeline);
    const songview::TimeAxis::GridSegment seg = axis.segmentAt(static_cast<Tick>(tick));
    const songview::TimeAxis::ResolvedTimeSignature sig = axis.signatureAt(static_cast<Tick>(tick));
    out->ticksPerBeat = axis.ticksPerBeat();
    out->lengthTicks = static_cast<uint32_t>(axis.lengthTicks());
    out->loopStartTick = static_cast<uint32_t>(axis.loopStartTick());
    out->loopEndTick = static_cast<uint32_t>(axis.loopEndTick());
    out->segStart = static_cast<uint32_t>(seg.start);
    out->segNext = static_cast<uint32_t>(seg.next);
    out->segBeatTicks = seg.beatTicks;
    out->segBeatsPerBar = seg.beatsPerBar;
    out->sigNumerator = static_cast<int32_t>(sig.numerator);
    out->sigDenomPow2 = static_cast<int32_t>(sig.denomPow2);
    out->sigImplicit = sig.implicit ? 1 : 0;
}

size_t sgm_timeaxis_grid_lines(const SGTimeMapFixture *map, uint32_t begin, uint32_t end,
                               SGGridLine *out, size_t capacity)
{
    MidiTimeline timeline;
    fillTimelineFromFixture(map, timeline);
    songview::TimeAxis axis;
    axis.bind(&timeline);
    size_t needed = 0;
    axis.forEachGridLine(static_cast<Tick>(begin), static_cast<Tick>(end),
                         [&](Tick tick, bool isBar, int barNumber, int beatNumber) {
                             if (needed < capacity && out != nullptr) {
                                 out[needed].tick = static_cast<uint32_t>(tick);
                                 out[needed].isBar = static_cast<uint8_t>(isBar ? 1 : 0);
                                 out[needed].bar = static_cast<int32_t>(barNumber);
                                 out[needed].beat = static_cast<int32_t>(beatNumber);
                             }
                             ++needed;
                         });
    return needed;
}

// Parity-seam oracles for the Swift Wave-1 PitchProjection port (spec §4,
// Task 3): thin forwarding over the production songview::PitchProjection —
// no logic of its own. pitches == NULL (count 0) selects the chromatic
// build; otherwise buildFromPitches over the ascending list.

namespace {

songview::PitchProjection makePitchProjection(const uint8_t *pitches, size_t count)
{
    songview::PitchProjection projection;
    if (pitches == nullptr || count == 0) {
        projection.buildChromatic();
    } else {
        projection.buildFromPitches(std::span<const uint8_t>(pitches, count));
    }
    return projection;
}

} // namespace

int32_t sgm_pitch_row_for_pitch(const uint8_t *pitches, size_t count, int32_t pitch)
{
    return static_cast<int32_t>(makePitchProjection(pitches, count).rowForPitch(pitch));
}

int32_t sgm_pitch_nearest_visible(const uint8_t *pitches, size_t count, int32_t pitch)
{
    return static_cast<int32_t>(makePitchProjection(pitches, count).nearestVisiblePitch(pitch));
}

double sgm_pitch_row_top(const uint8_t *pitches, size_t count, int32_t row, double keyHeight,
                         double scrollY, double dpr)
{
    return static_cast<double>(
        makePitchProjection(pitches, count).rowTop(row, keyHeight, scrollY, dpr));
}

int32_t sgm_pitch_y_to_pitch(const uint8_t *pitches, size_t count, double y, double keyHeight,
                             double scrollY, double dpr)
{
    return static_cast<int32_t>(
        makePitchProjection(pitches, count).yToPitch(y, keyHeight, scrollY, dpr));
}

Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "caller-resolved minimum viewport produces observable camera bounds"
  report.expect(gridCameraNear(compact.snapshot.viewportWidth, 50) &&
                gridCameraNear(compact.snapshot.minHScroll, -48) &&
                gridCameraNear(compact.snapshot.maxHScroll, 0),
                cppID: cameraTransformID,
                message: "caller-resolved minimum viewport produces observable camera bounds")

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick/content transforms round-trip with a fractional offset"
  report.expect(gridCameraNear(camera.tickAtContentX(content), 96), cppID: cameraTransformID,
                message: "tick/content transforms round-trip with a fractional offset")

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal restoration preserves fractional offsets"
  report.expect(gridCameraNear(camera.snapshot.scrollX, 10.25), cppID: cameraTransformID,
                message: "horizontal restoration preserves fractional offsets")

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "display projection snaps to a physical pixel"
  report.expect(gridCameraNear(camera.displayX(tick: 24, origin: 0.2, dpr: 2), 22),
                cppID: cameraTransformID, message: "display projection snaps to a physical pixel")

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal scrolling clamps to production pre-roll"
  report.expect(gridCameraNear(camera.snapshot.scrollX, -48) && gridCameraNear(camera.snapshot.minHScroll, -48),
                cppID: cameraTransformID, message: "horizontal scrolling clamps to production pre-roll")

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound timeline end reaches the plot origin"
  report.expect(gridCameraNear(camera.snapshot.scrollX, 640), cppID: cameraTransformID,
                message: "bound timeline end reaches the plot origin")

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport shrink reclamps the changed pre-roll floor"
  report.expect(gridCameraNear(viewportCamera.snapshot.scrollX, -48),
                cppID: cameraTransformID,
                message: "bound viewport shrink reclamps the changed pre-roll floor")

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport updates preserve a legal fractional offset"
  report.expect(gridCameraNear(viewportCamera.snapshot.scrollX, 10.25),
                cppID: cameraTransformID,
                message: "bound viewport updates preserve a legal fractional offset")

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "interactive time zoom preserves its tick anchor"
report.expect(zoom.zoomChanged && zoom.scrollChanged &&
                  near(camera.tickAtContentX(anchorX), anchoredTick), cppID: cameraZoomID,
                  message: "interactive time zoom preserves its tick anchor")

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "time zoom reaches its bound when the pointer anchor is unreachable"
  report.expect(gridCameraNear(clampedTimeZoom.snapshot.scrollX, 64) &&
                !gridCameraNear(clampedTimeZoom.tickAtContentX(boundedAnchor), tickBeforeBoundedZoom),
                cppID: cameraZoomID,
                message: "time zoom reaches its bound when the pointer anchor is unreachable")

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unanchored restoration keeps valid fractional camera state"
  report.expect(gridCameraNear(camera.snapshot.pixelsPerBeat, 48) &&
                gridCameraNear(camera.snapshot.keyHeight, 16) && gridCameraNear(camera.snapshot.scrollX, 10.5) &&
                gridCameraNear(camera.snapshot.scrollY, 20.25), cppID: cameraZoomID,
                message: "unanchored restoration keeps valid fractional camera state")

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "restoration sanitizes non-finite transient view-state values independently"
  report.expect(gridCameraNear(sanitized.snapshot.pixelsPerBeat, 32) &&
                gridCameraNear(sanitized.snapshot.keyHeight, 12) &&
                gridCameraNear(sanitized.snapshot.scrollX, 0) && gridCameraNear(sanitized.snapshot.scrollY, 0),
                cppID: cameraZoomID,
                message: "restoration sanitizes non-finite transient view-state values independently")

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "content shrink reclamps the horizontal offset"
  report.expect(gridCameraNear(camera.snapshot.scrollX, 20), cppID: cameraTransformID,
                message: "content shrink reclamps the horizontal offset")

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "viewport growth reclamps vertical content"
  report.expect(gridCameraNear(camera.snapshot.viewportWidth, 800) && gridCameraNear(camera.snapshot.scrollY, 0),
                cppID: cameraTransformID,
                message: "viewport growth reclamps vertical content")

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "an unbound timeline homes to its lead pad"
  report.expect(gridCameraNear(camera.snapshot.scrollX, -80), cppID: cameraTransformID,
                message: "an unbound timeline homes to its lead pad")

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unbound viewport updates follow the resolved home"
  report.expect(gridCameraNear(camera.snapshot.scrollX, -100), cppID: cameraTransformID,
                message: "unbound viewport updates follow the resolved home")

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "chromatic pitch mapping remains bijective across the full MIDI range"
report.expect(chromatic.visibleRowCount == 128 && (0..<128).allSatisfy { pitch in
        chromatic.row(forPitch: pitch) == 127 - pitch &&
            chromatic.visiblePitch(at: 127 - pitch) == pitch
    }, cppID: cameraPitchID,
    message: "chromatic pitch mapping remains bijective across the full MIDI range")

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch rows and nearest lookup preserve production ordering"
report.expect(folded.visiblePitch(at: 0) == 67 && folded.row(forPitch: 60) == 2 &&
                  folded.row(forPitch: 61) == PitchProjection.hiddenRow &&
                  folded.nearestVisiblePitch(to: 62) == 60 &&
                  folded.nearestVisiblePitch(to: 100) == 67 &&
                  folded.nearestVisiblePitch(to: -5) == 60 &&
                  emptyProjection.nearestVisiblePitch(to: 60) == nil,
                  cppID: cameraPitchID,
                  message: "pitch rows and nearest lookup preserve production ordering")

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row and pitch inverses agree"
report.expect(folded.row(atY: 10, keyHeight: 10, scrollY: 0, dpr: 2) == 1 &&
                  folded.pitch(atY: 29.75, keyHeight: 10, scrollY: 0, dpr: 2) == 60,
                  cppID: cameraPitchID, message: "DPR-snapped row and pitch inverses agree")

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional DPR snapping defines the inverse row boundary"
  report.expect(gridCameraNear(folded.rowBottom(0, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) ?? -1,
                     10) &&
                folded.row(atY: 9.99, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 0 &&
                folded.row(atY: 10, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 1,
                cppID: cameraPitchID,
                message: "fractional DPR snapping defines the inverse row boundary")

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch projection shrink reclamps the vertical offset"
  report.expect(gridCameraNear(projectedContent.snapshot.scrollY, 15),
                cppID: cameraPitchID,
                message: "pitch projection shrink reclamps the vertical offset")

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "vertical bounds derive from projected row height"
  report.expect(gridCameraNear(pitchCamera.snapshot.maxVScroll, 15), cppID: cameraPitchID,
                message: "vertical bounds derive from projected row height")

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom preserves its row anchor when bounds permit"
  report.expect(gridCameraNear(anchoredRow, resultingRow), cppID: cameraZoomID,
                message: "pitch zoom preserves its row anchor when bounds permit")

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom reaches its bound when the row anchor is unreachable"
  report.expect(gridCameraNear(clampedPitchZoom.snapshot.scrollY, 0) &&
                gridCameraNear(clampedPitchZoom.snapshot.maxVScroll, 0) &&
                !gridCameraNear(rowAfterBoundedZoom, rowBeforeBoundedZoom),
                cppID: cameraZoomID,
                message: "pitch zoom reaches its bound when the row anchor is unreachable")

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "key reveal scrolls only enough to show its row"
report.expect(pitchCamera.ensureKeyVisible(60) && near(pitchCamera.snapshot.scrollY, 15),
                  cppID: cameraPitchID, message: "key reveal scrolls only enough to show its row")

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "hidden pitches do not move the camera"
report.expect(!pitchCamera.ensureKeyVisible(61), cppID: cameraPitchID,
                  message: "hidden pitches do not move the camera")

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick reveal uses the production one-third anchor"
report.expect(reveal.ensureTickVisible(480, dpr: 2) &&
                  near(reveal.snapshot.scrollX, 573.3333333333),
                  cppID: cameraTransformID,
                  message: "tick reveal uses the production one-third anchor")

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "a fitting range on the physical right boundary stays visible"
report.expect(!reveal.ensureRangeVisible(startTick: 0, endTick: 149,
                                             preferEnd: false, dpr: 1) &&
                  near(reveal.snapshot.scrollX, 0), cppID: cameraTransformID,
                  message: "a fitting range on the physical right boundary stays visible")

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal prefers its requested leading edge"
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: false, dpr: 1) &&
                  near(reveal.contentX(tick: 240), 0), cppID: cameraTransformID,
                  message: "wide range reveal prefers its requested leading edge")

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal honors the requested trailing edge"
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: true, dpr: 1) &&
                  near(reveal.contentX(tick: 480), 199), cppID: cameraTransformID,
                  message: "wide range reveal honors the requested trailing edge")

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "phase \(phase.rawValue) performs one anchored time zoom publication"
report.expect(
            after.pixelsPerBeat > before.pixelsPerBeat
                && gridCameraNear(session.camera.tickAtContentX(anchorX), anchorTick, tolerance: 1e-7)
                && counters.camera == publications + 1,
            cppID: gridCameraWheelID, message: "phase \(phase.rawValue) performs one anchored time zoom publication")

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses time zoom without publishing"
report.expect(
        session.camera.snapshot == beforeMomentum && counters.camera == momentumPublications,
        cppID: gridCameraWheelID, message: "momentum suppresses time zoom without publishing")

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "pixel deltas receive five-times angle weighting"
report.expect(
        gridCameraNear(angleZoom, 35 * pow(1.0015, 120), tolerance: 1e-7)
            && gridCameraNear(session.camera.snapshot.pixelsPerBeat,
                    angleZoom * pow(1.0015, 120), tolerance: 1e-7),
        cppID: gridCameraWheelID, message: "pixel deltas receive five-times angle weighting")

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "effective angle and pixel zooms each publish once"
report.expect(
        counters.camera == 2,
        cppID: gridCameraWheelID, message: "effective angle and pixel zooms each publish once")

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift wheel pans by negative selected delta exactly once"
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 90) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Shift wheel pans by negative selected delta exactly once")

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at minimum and repeated clamped pan is silent"
report.expect(
        gridCameraNear(minimumPan.scrollX, minimumPan.minHScroll)
            && minimumPublications == 1 && counters.camera == minimumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at minimum and repeated clamped pan is silent")

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at maximum and repeated clamped pan is silent"
report.expect(
        gridCameraNear(maximumPan.scrollX, maximumPan.maxHScroll)
            && maximumPublications == 1 && counters.camera == maximumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at maximum and repeated clamped pan is silent")

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal-only momentum still pans exactly once"
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 112) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "horizontal-only momentum still pans exactly once")

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Ctrl wheel performs one row-anchored pitch zoom publication"
report.expect(
        pitchAfter.keyHeight > pitchBefore.keyHeight
            && gridCameraNear((pitchAfter.scrollY + 100) / pitchAfter.keyHeight, pitchRow, tolerance: 1e-7)
            && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Ctrl wheel performs one row-anchored pitch zoom publication")

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses pitch zoom without publishing"
report.expect(
        session.camera.snapshot == pitchMomentum && counters.camera == 0,
        cppID: gridCameraWheelID, message: "momentum suppresses pitch zoom without publishing")

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter wheel scrolls by negative half-delta exactly once"
report.expect(
        gridCameraNear(session.camera.snapshot.scrollY, gutterBefore + 6) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "gutter wheel scrolls by negative half-delta exactly once")

S042 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "Ctrl-add of earlier A(v40) retains later B(v90) as first selection"
report.expect(session.selectedNoteOrder == [b, a]
        && session.document.note(session.selectedNoteOrder[0])?.velocity == 90,
        cppID: id, message: "Ctrl-add of earlier A(v40) retains later B(v90) as first selection")

S043 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "deselecting and re-adding appends the note"
report.expectEqual([a, b], session.selectedNoteOrder, cppID: id,
                       what: "deselecting and re-adding appends the note")

S044 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "replacement preserves first occurrence and removes duplicates"
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "replacement preserves first occurrence and removes duplicates")

S045 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "band keeps press order before newly covered notes"
report.expect(Array(session.selectedNoteOrder.prefix(2)) == [b, a]
        && session.selectedNotes.contains(c),
        cppID: id, message: "band keeps press order before newly covered notes")

S046 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "band cancellation restores selection order"
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "band cancellation restores selection order")

S047 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "order-only replacement publishes the selection domain"
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement publishes the selection domain")

S048 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "order-only replacement and additive selection coalesce"
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement and additive selection coalesce")

S049 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "unchanged normalized selection publishes nothing"
report.expect(publications.isEmpty, cppID: id,
                  message: "unchanged normalized selection publishes nothing")

S050 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "selection gestures never mutate document or history"
report.expect(session.document.revision == revision
        && session.document.history.currentIdentity == history
        && session.document.isDirty == dirty,
        cppID: id, message: "selection gestures never mutate document or history")

S051 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "deletion prunes membership and preserves survivor order"
report.expect(session.selectedNoteOrder == [c, a] && session.selectedNotes == Set([c, a]),
                  cppID: id, message: "deletion prunes membership and preserves survivor order")

S052 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "viewport dimensions are pushed into the session camera"
report.expect(
        gridCameraNear(first.viewportWidth, 640) && gridCameraNear(first.rollHeight, 320),
        cppID: viewportID, message: "viewport dimensions are pushed into the session camera")

S053 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "camera bounds derive from viewport, document extent, and projection"
report.expect(
        gridCameraNear(first.minHScroll, expectedMin)
            && gridCameraNear(first.maxHScroll,
                    Double(session.timeline.lengthTicks) * first.pixelsPerTick)
            && gridCameraNear(first.maxVScroll, expectedMaxV),
        cppID: viewportID, message: "camera bounds derive from viewport, document extent, and projection")

S054 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "first viewport push homes vertically within camera bounds"
report.expect(
        first.scrollY >= 0 && first.scrollY <= first.maxVScroll,
        cppID: viewportID, message: "first viewport push homes vertically within camera bounds")

S055 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "published scales equal the session camera snapshot"
report.expect(
        gridCameraNear(grid.beatWidth, first.pixelsPerBeat) && gridCameraNear(grid.rowHeight, first.keyHeight),
        cppID: viewportID, message: "published scales equal the session camera snapshot")

S056 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "identical viewport push preserves fractional offsets and publishes nothing"
report.expect(
        session.camera.snapshot == fractional && counters.camera == 0,
        cppID: viewportID, message: "identical viewport push preserves fractional offsets and publishes nothing")

S057 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "roll-height increase reclamps vertical scroll"
report.expect(
        tallViewport.maxVScroll < shortHeight.maxVScroll
            && gridCameraNear(tallViewport.scrollY, tallViewport.maxVScroll),
        cppID: viewportID, message: "roll-height increase reclamps vertical scroll")

S058 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "double-font push applies the rounded font-relative defaults"
report.expect(
        gridCameraNear(doubled.pixelsPerBeat, 69) && gridCameraNear(doubled.keyHeight, 26),
        cppID: viewportID, message: "double-font push applies the rounded font-relative defaults")

S059 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "font push back restores the exact earlier scale"
report.expect(
        gridCameraNear(restored.pixelsPerBeat, 35) && gridCameraNear(restored.keyHeight, 13),
        cppID: viewportID, message: "font push back restores the exact earlier scale")

S060 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "each effective font push publishes exactly once"
report.expect(
        counters.camera == 2,
        cppID: viewportID, message: "each effective font push publishes exactly once")

S061 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "pushed font defines time and key-height minimums"
report.expect(
        gridCameraNear(fontMinimum.pixelsPerBeat, 9) && gridCameraNear(fontMinimum.keyHeight, 9),
        cppID: viewportID, message: "pushed font defines time and key-height minimums")

S062 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "pushed font defines time and key-height maximums"
report.expect(
        gridCameraNear(fontMaximum.pixelsPerBeat, 1_387) && gridCameraNear(fontMaximum.keyHeight, 69),
        cppID: viewportID, message: "pushed font defines time and key-height maximums")

S063 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "document shrink privately reconciles extent and reclamps horizontal scroll"
report.expect(
        farID != nil && expandedLength > originalLength
            && session.timeline.lengthTicks < expandedLength
            && gridCameraNear(shrunk.scrollX, shrunk.maxHScroll),
        cppID: viewportID, message: "document shrink privately reconciles extent and reclamps horizontal scroll")

S064 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "generated time marks stay inside the one-viewport culling window"
report.expect(
        !markXs.isEmpty && markXs.allSatisfy { $0 >= -cullingWidth - 2 && $0 <= 2 * cullingWidth + 2 },
        cppID: projectionID, message: "generated time marks stay inside the one-viewport culling window")

S065 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "generated time marks cover the visible plot"
report.expect(
        (markXs.min() ?? 1) <= 0 && (markXs.max() ?? -1) >= cullingWidth,
        cppID: projectionID, message: "generated time marks cover the visible plot")

S066 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "camera movement does not alter noteSummary document state"
report.expect(
        grid.noteSummary == summaryBeforeCameraMove,
        cppID: projectionID, message: "camera movement does not alter noteSummary document state")

S067 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "known note rectangle equals the camera projection"
report.expect(
        projected,
        cppID: projectionID, message: "known note rectangle equals the camera projection")

S068 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "pre-roll mask presence follows the projected tick-zero position"
report.expect(
        maskCount == (zeroX > 0 ? 1 : 0),
        cppID: projectionID, message: "pre-roll mask presence follows the projected tick-zero position")

S069 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "beginPointer selects the note under the projected rectangle"
report.expect(
        session.selectedNotes.contains(originalNote.id),
        cppID: projectionID, message: "beginPointer selects the note under the projected rectangle")

S070 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "one-cell drag publishes the snapped tick and pitch preview"
report.expect(
        previewRect.map {
            gridCameraNear($0.x, session.camera.displayX(
                tick: Double(expectedTick), origin: 0, dpr: grid.devicePixelRatio))
                && gridCameraNear($0.y, expectedPreviewY + 1 / grid.devicePixelRatio)
        } ?? false,
        cppID: projectionID, message: "one-cell drag publishes the snapped tick and pitch preview")

S071 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "endPointer commits one revision at the snapped tick and pitch"
report.expect(
        session.document.revision == originalRevision + 1
            && moved.map { Int($0.tick) == expectedTick && Int($0.pitch) == expectedPitch } == true,
        cppID: projectionID, message: "endPointer commits one revision at the snapped tick and pitch")

S072 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "draw drag on an empty row adds one snapped note"
report.expect(
        session.document.revision == drawRevision + 1
            && created.map { Int($0.tick) == drawTick && Int($0.pitch) == drawPitch } == true,
        cppID: projectionID, message: "draw drag on an empty row adds one snapped note")

S073 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "right-drag band selects exactly the visible intersecting notes"
report.expect(
        session.selectedNotes == visibleIDs,
        cppID: projectionID, message: "right-drag band selects exactly the visible intersecting notes")

S074 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "inputCancelled discards an active gesture without document mutation"
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "inputCancelled discards an active gesture without document mutation")

S075 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "Escape discards an active gesture without document mutation"
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "Escape discards an active gesture without document mutation")

S076 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "press outside projected pitch rows is rejected"
report.expect(
        session.document.revision == cancelRevision,
        cppID: projectionID, message: "press outside projected pitch rows is rejected")

S077 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "ensureTickVisible uses the reveal fraction in one publication"
report.expect(
        counters.camera == before + 1
            && gridCameraNear(tickReveal.scrollX, expectedTickReveal),
        cppID: isolationID, message: "ensureTickVisible uses the reveal fraction in one publication")

S078 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "preferEnd true aligns an oversized range to the right edge"
report.expect(
        counters.camera == before + 1
            && gridCameraNear(
                endReveal.scrollX,
                endReveal.maxHScroll - endReveal.viewportWidth
                    + 1 / grid.devicePixelRatio),
        cppID: isolationID, message: "preferEnd true aligns an oversized range to the right edge")

S079 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "preferEnd false aligns an oversized range to its start"
report.expect(
        counters.camera == before + 1 && gridCameraNear(startReveal.scrollX, 0),
        cppID: isolationID, message: "preferEnd false aligns an oversized range to its start")

S080 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "ensureKeyVisible aligns the bottom pitch at the viewport edge"
report.expect(
        counters.camera == before + 1
            && gridCameraNear(keyReveal.scrollY, keyReveal.maxVScroll),
        cppID: isolationID, message: "ensureKeyVisible aligns the bottom pitch at the viewport edge")

S081 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "presenter scroll values track the revealed session camera"
report.expect(
        gridCameraNear(grid.cameraScrollX, session.camera.snapshot.scrollX)
            && gridCameraNear(grid.cameraScrollY, session.camera.snapshot.scrollY),
        cppID: isolationID, message: "presenter scroll values track the revealed session camera")

S082 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "redundant, non-finite, hidden, and already-visible navigation is silent"
report.expect(
        counters.camera == before,
        cppID: isolationID, message: "redundant, non-finite, hidden, and already-visible navigation is silent")

S083 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "direct edit-cursor change neither moves camera nor republishes presenter cursor"
report.expect(
        session.camera.snapshot == published && grid.editCursorTick == Int(cursor),
        cppID: isolationID, message: "direct edit-cursor change neither moves camera nor republishes presenter cursor")

S084 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "camera navigation is isolated from document, playback, dirty state, and history"
report.expect(
        counters.document == 0 && counters.playback == 0 && counters.cursor == 1
            && session.document.revision == revision && session.document.isDirty == dirty
            && session.document.history.canUndo == canUndo
            && session.document.history.canRedo == canRedo,
        cppID: isolationID, message: "camera navigation is isolated from document, playback, dirty state, and history")

S085 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "committed document edit performs no standalone camera publication"
report.expect(
        inserted?.count == 1 && counters.camera == 0,
        cppID: isolationID, message: "committed document edit performs no standalone camera publication")

S086 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift
Anchor: message "one edit publishes one coherent playback and document refresh after reconciliation"
report.expect(
        counters.playback == 1 && counters.document == 1 && counters.coherentDocumentCallback,
        cppID: isolationID, message: "one edit publishes one coherent playback and document refresh after reconciliation")

Shared helper:
No shared Swift fixture file carrying report.expect*/expectEqual calls was identified for this pairing: camera.swift is self-contained (Foundation+PorydawApp) and EditorGridCameraChecks.swift builds its DocumentSession inline (fixture-guard report.fail calls at EditorGridCameraChecks.swift:78,88,322,393,533 are guard aborts, not predicates). The expectEqual helper itself lives in src/checks/support/corecheck/CoreCheckSupport.swift (SHA-256: d4419a4b2e15d1291d435c2e410d3b2f8357a875f9cb27751e01ef1d01beaf65) as a definition, not a call site:
    func expectEqual<T: Equatable>(_ expected: T, _ actual: T, cppID: String,
                                    what: String) {
        if expected == actual {
            pass(cppID, row: what)
        } else {
            fail(cppID, "\(what): expected=\(expected) actual=\(actual)")
        }
    }
