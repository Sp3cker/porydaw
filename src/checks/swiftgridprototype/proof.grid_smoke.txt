Assertion correspondence: swiftgridprototype/grid_smoke.cpp

Reference revision: 18fee9357273c3351d1eaadf4fe6f8d52edb9a98
Original: src/checks/swiftgridprototype/grid_smoke.cpp
Original SHA-256: a2d440b3eafa3cd25957d0e5725e0144728f1dec9e10180c3305b754259a14a1
Swift counterpart: src/checks/rollcheck/static/camera.swift
Swift SHA-256: 0321944175f23cab5587282618a52d043e1c44e650859d3599e42ce8c7db1f76
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift SHA-256: ad6b435c13a3df031787816508ea95f14b5ec8b96a8539df4fb620a3107f08a3
Deleted in: fe1ff4df9f4834cbd4e4e59d2e710078c10a8ced Wave 4 re-scoped grid milestone: writable Swift roll through production seams
Note: swiftrollgated/*.swift matches no file in this checkout; the receiving lane is C++-only (tst_swiftrollgated.cpp). The deletion commit records the load-bearing prototype rows as moved to swiftrollgated (testBandLeadingGripDecline, testBandTrailingGripResizeCommitOnce).

Status and evidence
This is an assertion-site inventory of the deleted prototype grid smoke, not a count-based equivalence claim. Every require/awaitState/fail site appears below, including Scene-harness guards and the shared wait/abort primitives. S identifiers identify exact Swift predicates in the named production counterparts, listed after the inventory. MATCHED identifies an equivalent model predicate; PARTIAL a related predicate with an explicit unproved condition; NATIVE a prototype native-window/rendering/harness observation a presenter-level Swift check cannot establish; GAP an original predicate without a replacement assertion.

Verification
Command: deno task verify --filter swiftrollgated --verbose
Result: pending integrated run; source hashes pin the tree inspected here.

Execution and ownership
RunEditorCameraChecks/checkGridCameraWheel own the camera-geometry pairs (PARTIAL-CAM rows); checkOrderedSelection owns the selection/revision pairs (PARTIAL-SEL rows); checkViewport/checkProjection own the viewport/pitch-projection pairs. They execute through the production Swift grid lane, not the deleted prototype QML scene: Scene::reveal/press/move/release/click/doubleClick/cancel, expectBox/expectNamedBox/expectPublished/expectPreview, and the pixel-probe helpers (pixelAt/windowPixel/hasSelectionRing) have no Swift owner. The deleted prototype QML scene (gridNote_* primitives, drawPreview, gutters, borders layer) is the fixture; no shared Swift fixture file carries expects for these rows.

Fixture, sequence and row constraints
No QTest data rows: loops expand one site into many executions without new sites. verifyFixture publishes exactly 30 notes (15 lead pitches + mirrored series, ghost flags, track/velocity defaults); note loops (notes/note/musicalState/addedNoteId) fold the 30-note JSON array per site. Pixel loops (colorCount/pixelAt/windowPixel/hasSelectionRing/forEachGridLine oracles) fold framebuffer regions per site. awaitState polls each predicate up to 3s (Scene::expectNamedBox uses qWaitFor + fail with an actual-vs-expected dump). Guards: Scene::notes/note require the model JSON; reveal pins viewport/contentX/contentY and the native-window containment check before every pointer helper; resetDemo/30-note restore precedes each exercise phase.
Original setup, branch guards, and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory

A001 | require | src/checks/swiftgridprototype/grid_smoke.cpp:49
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:
#include "grid_smoke.h"
#include "math_smoke.h"
#include "ui/songview/quick/swiftgrid/document_feed.h"

#include <QColor>
#include <QCoreApplication>
#include <QDeadlineTimer>
#include <QFont>
#include <QGuiApplication>
#include <QImage>
#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QPointF>
#include <QPointer>
#include <QQmlContext>
#include <QQuickItem>
#include <QQuickWindow>
#include <QScopeGuard>
#include <QString>
#include <QTimer>
#include <QVariant>
#include <QWheelEvent>
#include <QtQml/qqml.h>
#include <QtTest/QTest>
#include <algorithm>
#include <array>
#include <chrono>
#include <cmath>
#include <cstdio>
#include <cstdlib>
#include <limits>
#include <utility>
#include <vector>

namespace {
using namespace std::chrono_literals;

[[noreturn]] void fail(const char *reason)
{
    std::fprintf(stderr, "SWIFT_GRID_SMOKE FAIL: %s\n", reason);
    std::fflush(stderr);
    std::exit(EXIT_FAILURE);
}

void require(bool condition, const char *reason)
{
    if (!condition)
Original expression:
        fail(reason);

A002 | awaitState | src/checks/swiftgridprototype/grid_smoke.cpp:64
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

}

void pass(const char *scenario)
{
    std::printf("SWIFT_GRID_SMOKE %s PASS\n", scenario);
    std::fflush(stdout);
}

template <typename Predicate>
void awaitState(Predicate predicate, const char *reason)
{
    QDeadlineTimer deadline(3s);
    while (!predicate()) {
        if (deadline.hasExpired())
Original expression:
            fail(reason);

A003 | Scene::notes | src/checks/swiftgridprototype/grid_smoke.cpp:113
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        QTest::qWait(10ms);
    }
}

QQuickItem *namedItem(QQuickItem *parent, const QString &name)
{
    if (parent->objectName() == name)
        return parent;
    for (QQuickItem *child : parent->childItems()) {
        if (auto *found = namedItem(child, name))
            return found;
    }
    return nullptr;
}

int noteId(const QJsonObject &note)
{
    return note.value("id").toInt(-1);
}

QJsonArray musicalState(const QJsonArray &notes)
{
    QJsonArray result;
    for (const QJsonValue &value : notes) {
        const QJsonObject note = value.toObject();
        QJsonObject musical;
        for (const char *key : {"id", "tick", "duration", "pitch", "track", "velocity", "ghost"})
            musical.insert(QLatin1String(key), note.value(QLatin1String(key)));
        result.append(musical);
    }
    return result;
}

struct Scene {
    QPointer<QQuickWindow> window;
    QPointer<QQuickItem> viewport;
    QPointer<QQuickItem> surface;
    QPointer<QObject> model;
    double baseFont = 0;
    double dpr = 1;
    double beatWidth = 0;
    double rowHeight = 0;
    double leadPad = 0;
    int ticksPerBeat = 0;
    int snap = 0;

    QJsonArray notes() const
    {
Original expression:
        require(model, "grid model destroyed during smoke");

A004 | Scene::notes | src/checks/swiftgridprototype/grid_smoke.cpp:116
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        const QJsonDocument document =
            QJsonDocument::fromJson(model->property("noteSummary").toString().toUtf8());
Original expression:
        require(document.isArray(), "noteSummary is not a JSON array");

A005 | Scene::reveal | src/checks/swiftgridprototype/grid_smoke.cpp:154
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        return document.array();
    }

    QJsonObject note(int id) const
    {
        for (const QJsonValue &value : notes()) {
            if (noteId(value.toObject()) == id)
                return value.toObject();
        }
        return {};
    }

    QPointF point(double tick, int pitch) const
    {
        return {leadPad + tick * beatWidth / ticksPerBeat, (127.0 - pitch + 0.5) * rowHeight};
    }

    double pixelSnap(double value) const { return std::round(value * dpr) / dpr; }

    QRectF expectedBox(int tick, int duration, int pitch) const
    {
        const double pixel = 1.0 / dpr;
        const double x0 = pixelSnap(leadPad + tick * beatWidth / ticksPerBeat);
        const double x1 = pixelSnap(leadPad + (tick + duration) * beatWidth / ticksPerBeat);
        const double top = pixelSnap((127 - pitch) * rowHeight);
        const double bottom = pixelSnap((128 - pitch) * rowHeight);
        return {x0, top + pixel, qMax(std::round(baseFont / 6.0), x1 - x0),
                bottom - top - 2 * pixel};
    }

    QRectF itemRect(QQuickItem *item) const
    {
        return {item->mapToItem(surface, QPointF{}), item->size()};
    }

    void reveal(QPointF content) const
    {
Original expression:
        require(viewport && surface && window, "grid scene destroyed during smoke");

A006 | Scene::reveal | src/checks/swiftgridprototype/grid_smoke.cpp:159
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        const double maxX =
            qMax(0.0, viewport->property("contentWidth").toDouble() - viewport->width());
        const double maxY =
            qMax(0.0, viewport->property("contentHeight").toDouble() - viewport->height());
Original expression:
        require(viewport->setProperty("contentX",
                                      qBound(0.0, content.x() - viewport->width() / 2, maxX)),
                "cannot scroll horizontal viewport");

A007 | Scene::reveal | src/checks/swiftgridprototype/grid_smoke.cpp:162
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

Original expression:
        require(viewport->setProperty("contentY",
                                      qBound(0.0, content.y() - viewport->height() / 2, maxY)),
                "cannot scroll vertical viewport");

A008 | Scene::reveal | src/checks/swiftgridprototype/grid_smoke.cpp:167
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        QTest::qWait(20ms);
        const QPointF scenePoint = surface->mapToScene(content);
Original expression:
        require(scenePoint.x() >= 0 && scenePoint.x() < window->width() && scenePoint.y() >= 0 &&
                    scenePoint.y() < window->height(),
                "gesture target is outside the native window");

A009 | Scene::release | src/checks/swiftgridprototype/grid_smoke.cpp:190
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    }

    QPoint windowPoint(QPointF content) const { return surface->mapToScene(content).toPoint(); }

    void press(QPointF content) const
    {
        reveal(content);
        QTest::mousePress(window, Qt::LeftButton, Qt::NoModifier, windowPoint(content));
        QTest::qWait(30ms);
    }

    void move(QPointF content) const
    {
        QTest::mouseMove(window, windowPoint(content));
        QTest::qWait(30ms);
    }

    void release(QPointF content) const
    {
        QTest::mouseRelease(window, Qt::LeftButton, Qt::NoModifier, windowPoint(content));
Original expression:
        awaitState([this] { return model->property("activeNoteId").toInt() == -1; },
                   "pointer release did not finish the gesture");

A010 | Scene::cancel | src/checks/swiftgridprototype/grid_smoke.cpp:210
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }

    void click(QPointF content) const
    {
        press(content);
        release(content);
    }

    void doubleClick(QPointF content) const
    {
        reveal(content);
        QTest::mouseDClick(window, Qt::LeftButton, Qt::NoModifier, windowPoint(content));
        QTest::qWait(50ms);
    }

    void cancel(QPointF content) const
    {
        QQuickItem *grabber = window->mouseGrabberItem();
Original expression:
        require(grabber, "pressed QML input item has no native mouse grab");

A011 | Scene::cancel | src/checks/swiftgridprototype/grid_smoke.cpp:212
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        grabber->ungrabMouse();
Original expression:
        awaitState([this] { return model->property("activeNoteId").toInt() == -1; },
                   "native mouse ungrab did not cancel the gesture");

A012 | Scene::expectBox | src/checks/swiftgridprototype/grid_smoke.cpp:233
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        QTest::mouseRelease(window, Qt::LeftButton, Qt::NoModifier, windowPoint(content));
        QTest::qWait(30ms);
    }

    bool boxMatches(QQuickItem *item, const QRectF &expected) const
    {
        if (!item || !item->isVisible())
            return false;
        const QRectF actual = itemRect(item);
        const double tolerance = 0.26 / dpr + 0.02;
        return std::abs(actual.x() - expected.x()) <= tolerance &&
               std::abs(actual.y() - expected.y()) <= tolerance &&
               std::abs(actual.width() - expected.width()) <= tolerance &&
               std::abs(actual.height() - expected.height()) <= tolerance;
    }

    void expectBox(QQuickItem *item, const QRectF &expected, const char *reason) const
    {
        QPointer<QQuickItem> guarded = item;
Original expression:
        awaitState([&] { return boxMatches(guarded.data(), expected); }, reason);

A013 | Scene::expectNamedBox | src/checks/swiftgridprototype/grid_smoke.cpp:246
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    }

    void expectNamedBox(const QString &name, const QRectF &expected, const char *reason) const
    {
        if (QTest::qWaitFor([&] { return boxMatches(namedItem(surface, name), expected); }, 3s))
            return;
        auto *item = namedItem(surface, name);
        const QRectF actual = item ? itemRect(item) : QRectF{};
        std::fprintf(stderr, "%s actual=(%g,%g %gx%g) expected=(%g,%g %gx%g) visible=%d\n",
                     qPrintable(name), actual.x(), actual.y(), actual.width(), actual.height(),
                     expected.x(), expected.y(), expected.width(), expected.height(),
                     item && item->isVisible());
Original expression:
        fail(reason);

A014 | Scene::expectPublished | src/checks/swiftgridprototype/grid_smoke.cpp:253
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    }

    void expectPublished(int id, int tick, int duration, int pitch, const char *reason) const
    {
        const QRectF expected = expectedBox(tick, duration, pitch);
        reveal(expected.center());
Original expression:
        awaitState(
            [&] {
                const QJsonObject value = note(id);
                return value.value("tick").toInt(-1) == tick &&
                       value.value("duration").toInt(-1) == duration &&
                       value.value("pitch").toInt(-1) == pitch;
            },
            reason);

A015 | verifyFixture | src/checks/swiftgridprototype/grid_smoke.cpp:311
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        expectNamedBox(QStringLiteral("gridNote_%1").arg(id), expected,
                       "scene note primitive does not follow published geometry");
    }

    void expectPreview(const QRectF &expected) const
    {
        expectNamedBox(QStringLiteral("drawPreview"), expected,
                       "draw preview geometry is not source projected");
    }
};

bool hasSelectionRing(QQuickItem *root, QQuickItem *surface, const QRectF &noteBox)
{
    for (QQuickItem *child : root->childItems()) {
        const QColor color = child->property("color").value<QColor>();
        const QRectF rect(child->mapToItem(surface, QPointF{}), child->size());
        if (color == QColor(QStringLiteral("#B9E8EE")) &&
            rect.intersects(noteBox.adjusted(-8, -8, 8, 8)))
            return true;
        if (hasSelectionRing(child, surface, noteBox))
            return true;
    }
    return false;
}

int addedNoteId(const QJsonArray &before, const QJsonArray &after)
{
    for (const QJsonValue &candidate : after) {
        const int id = noteId(candidate.toObject());
        bool existed = false;
        for (const QJsonValue &old : before)
            existed = existed || noteId(old.toObject()) == id;
        if (!existed)
            return id;
    }
    return -1;
}

void verifyFixture(Scene &scene)
{
    static constexpr std::array<int, 15> leadPitches = {
        60, 64, 67, 71, 74, 71, 67, 64, 60, 64, 67, 71, 74, 71, 67,
    };
    static constexpr std::array<int, 15> ghostPitches = {
        48, 55, 52, 57, 50, 57, 52, 55, 48, 55, 52, 57, 50, 57, 52,
    };
    static constexpr std::array<int, 3> leadVelocities = {98, 104, 110};
    static constexpr std::array<int, 3> ghostVelocities = {86, 92, 98};

    const QJsonArray initial = scene.notes();
Original expression:
    require(initial.size() == 30, "fixture must publish exactly 30 notes");

A016 | verifyFixture | src/checks/swiftgridprototype/grid_smoke.cpp:320
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    for (int i = 0; i < 30; ++i) {
        const bool ghost = i >= 15;
        const int local = ghost ? i - 15 : i;
        const int id = i + 1;
        const int tick = 24 * local + (ghost ? 12 : 0);
        const int pitch = ghost ? ghostPitches[local] : leadPitches[local];
        const int velocity = ghost ? ghostVelocities[local % 3] : leadVelocities[local % 3];
        const QJsonObject note = scene.note(id);
Original expression:
        require(note.value("track").toInt(-1) == (ghost ? 1 : 0) &&
                    note.value("velocity").toInt(-1) == velocity &&
                    note.value("ghost").toBool(!ghost) == ghost,
                "fixture track, velocity, or ghost role differs from source fixture");

A017 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:334
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        scene.expectPublished(id, tick, 18, pitch, "fixture note fields differ from source MIDI");
    }
    pass("source-fixture-and-projected-boxes");
}

void verifyRaster(Scene &scene)
{
    const auto capture = [&] {
        QTest::qWait(30ms);
        QImage image = scene.window->grabWindow();
Original expression:
        require(!image.isNull(), "native grid framebuffer is empty");

A018 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:340
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        return image;
    };
    const auto pixelAt = [&](const QImage &image, QPointF content) {
        const QPointF position = scene.surface->mapToScene(content) * image.devicePixelRatio();
        const QPoint pixel(qFloor(position.x()), qFloor(position.y()));
Original expression:
        require(image.rect().contains(pixel), "raster probe is outside the framebuffer");

A019 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:349
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        return image.pixelColor(pixel);
    };
    const auto isBlack = [](QColor color) {
        return color.red() <= 16 && color.green() <= 16 && color.blue() <= 16;
    };

    scene.reveal(scene.point(9, 60));
    QImage image = capture();
Original expression:
    require(pixelAt(image, scene.point(9, 62)) == QColor("#C9C1BB") &&
                pixelAt(image, scene.point(9, 61)) == QColor("#B4ACA6"),
            "natural and accidental row pixels differ from production colors");

A020 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:353
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QRectF note = scene.expectedBox(0, 18, 60);
Original expression:
    require(pixelAt(image, note.center()) == QColor("#C0625E"),
            "velocity-98 note face differs from production OKLab fill");

A021 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:358
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const double pixel = 1.0 / scene.dpr;
    const int borderPixels = qRound(scene.dpr);
    for (int i = 0; i < borderPixels; ++i) {
Original expression:
        require(isBlack(pixelAt(image, {note.center().x(), note.top() + i * pixel})) &&
                    isBlack(pixelAt(image, {note.center().x(), note.bottom() - (i + 1) * pixel})),
                "unselected note lacks its display-scaled black border");

A022 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:369
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }
    pass("production-row-colors-note-fill-and-border-raster");

    scene.click(note.center());
    image = capture();
    const int ringPixels = qMax(1, qRound(scene.baseFont / 8.0 * scene.dpr));
    const QColor ring("#B9E8EE");
    for (int i = 0; i < ringPixels; ++i) {
Original expression:
        require(pixelAt(image, {note.center().x(), note.top() + i * pixel}) == ring &&
                    pixelAt(image, {note.center().x(), note.bottom() - (i + 1) * pixel}) == ring,
                "selected note ring is not contiguous at its production weight");

A023 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:375
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }
    for (int i = 0; i < borderPixels; ++i) {
        const double inset = (ringPixels + i) * pixel;
Original expression:
        require(isBlack(pixelAt(image, {note.center().x(), note.top() + inset})) &&
                    isBlack(pixelAt(image, {note.center().x(), note.bottom() - inset - pixel})) &&
                    isBlack(pixelAt(image, {note.left() + inset, note.center().y()})) &&
                    isBlack(pixelAt(image, {note.right() - inset - pixel, note.center().y()})),
                "selected note lacks its inset black frame");

A024 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:381
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }
Original expression:
    require(pixelAt(image, note.center()) == QColor("#C0625E"),
            "selection frame swallowed the note face");

A025 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:389
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    pass("production-selected-note-frame-raster");

    const QRectF ghost = scene.expectedBox(12, 18, 48);
    scene.reveal(ghost.center());
    image = capture();
    const QColor ghostFace = pixelAt(image, ghost.center());
Original expression:
    require(!isBlack(ghostFace) && pixelAt(image, {ghost.center().x(), ghost.top()}) == ghostFace &&
                pixelAt(image, {ghost.center().x(), ghost.bottom() - pixel}) == ghostFace,
            "ghost note acquired an opaque border instead of an uninterrupted face");

A026 | verifyRaster | src/checks/swiftgridprototype/grid_smoke.cpp:393
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    pass("production-ghost-note-edge-raster");
Original expression:
    require(QMetaObject::invokeMethod(scene.window, "resetDemo"),
            "cannot reset the demo after raster probes");

A027 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:403
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    QTest::qWait(30ms);
}

void verifyChromeRaster(Scene &scene)
{
    const auto capture = [&] {
        QTest::qWait(30ms);
        QImage image = scene.window->grabWindow();
Original expression:
        require(!image.isNull(), "native grid framebuffer is empty");

A028 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:409
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        return image;
    };
    const auto windowPixel = [](const QImage &image, QPointF scenePoint) {
        const QPoint pixel(qRound(scenePoint.x() * image.devicePixelRatio()),
                           qRound(scenePoint.y() * image.devicePixelRatio()));
Original expression:
        require(image.rect().contains(pixel), "chrome raster probe is outside the framebuffer");

A029 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:420
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        return image.pixelColor(pixel);
    };
    const auto gutterPoint = [&](QQuickItem *gutter, double x, double y) {
        return gutter->mapToScene(QPointF(x, y));
    };

    auto *rulerGutter =
        namedItem(scene.window->contentItem(), QStringLiteral("timelineQuickRulerGutter"));
    auto *rollGutter =
        namedItem(scene.window->contentItem(), QStringLiteral("timelineQuickRollGutter"));
Original expression:
    require(rulerGutter && rollGutter, "ruler or keyboard gutter is missing");

A030 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:435
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:


    // Read-only visual probe: preserve the flickable scroll position so later
    // gesture scenarios observe the same camera state verifyRaster left.
    const double priorContentX = scene.viewport->property("contentX").toDouble();
    const double priorContentY = scene.viewport->property("contentY").toDouble();
    const auto restoreScroll = qScopeGuard([&] {
        scene.viewport->setProperty("contentX", priorContentX);
        scene.viewport->setProperty("contentY", priorContentY);
    });

    scene.reveal(scene.point(9, 60));
    QImage image = capture();
    const double keyboardWidth = scene.model->property("keyboardWidth").toDouble();
    const double rulerHeight = scene.model->property("rulerHeight").toDouble();
Original expression:
    require(windowPixel(image, gutterPoint(rulerGutter, keyboardWidth / 2, rulerHeight / 2)) ==
                QColor("#BDB5AF"),
            "ruler gutter chrome diverged from the production chrome background");

A031 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:450
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    // The ruler separator is a 1-logical-pixel rect centered on rulerHeight
    // (GridScene.swift:351), clipped by the gutter item's own height. The
    // scene-graph rect has no antialiasing: a device row is filled when its
    // pixel center falls inside the mapped clipped span. Probe the first row
    // whose center is inside for the exact production color.
    {
        const double dpr = image.devicePixelRatio();
        const QPointF gutterOrigin = rulerGutter->mapToScene(QPointF{});
        const double clipBottom = std::min(rulerHeight + 0.5, rulerGutter->height());
        const double devTop = (gutterOrigin.y() + rulerHeight - 0.5) * dpr;
        const double devBottom = (gutterOrigin.y() + clipBottom) * dpr;
        const int row = int(std::ceil(devTop - 0.5));
Original expression:
        require(row + 0.5 < devBottom,
                "ruler gutter separator has no device row inside its clipped span");

A032 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:460
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        const QColor actual =
            windowPixel(image, QPointF(gutterOrigin.x() + keyboardWidth / 2, double(row) / dpr));
        if (actual != QColor("#5B5652"))
            std::fprintf(stderr,
                         "ruler separator actual=%s row=%d devSpan=[%g,%g] dpr=%g "
                         "gutterY=%g gutterH=%g\n",
                         qPrintable(actual.name()), row, devTop, devBottom, dpr, gutterOrigin.y(),
                         rulerGutter->height());
Original expression:
        require(actual == QColor("#5B5652"),
                "ruler gutter separator diverged from the production separator color");

A033 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:469
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }
    // A scene-graph rect without antialiasing fills exactly the device rows
    // whose pixel centers fall inside its mapped span; returns that row.
    const auto rowInsideSpan = [](double originY, double localTop, double localBottom, double dpr) {
        const double devTop = (originY + localTop) * dpr;
        const double devBottom = (originY + localBottom) * dpr;
        const int row = int(std::ceil(devTop - 0.5));
Original expression:
        require(row + 0.5 < devBottom, "separator strip has no device row inside its mapped span");

A034 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:475
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        return row;
    };
    // rollBandContent already carries -contentY, so gutter-local y is the
    // content-space row coordinate directly.
    const auto keyboardY = [&](int pitch) { return (127.0 - pitch + 0.5) * scene.rowHeight; };
Original expression:
    require(windowPixel(image, gutterPoint(rollGutter, keyboardWidth / 2, keyboardY(60))) ==
                    QColor("#F4F4F4") &&
                windowPixel(image, gutterPoint(rollGutter, keyboardWidth / 2, keyboardY(61))) ==
                    QColor("#202224"),
            "keyboard natural/black key fills diverged from production colors");

A035 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:493
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    // The C-boundary separator is one physical pixel (m.pixel = 1/dpr) ending
    // at the snapped row edge; probe the device row whose center falls inside
    // its mapped span for the exact production color.
    const double cEdge = (127.0 - 60 + 1.0) * scene.rowHeight;
    const double pixel = 1.0 / scene.dpr;
    const QPointF rollOrigin = rollGutter->mapToScene(QPointF{});
    const int sepRow =
        rowInsideSpan(rollOrigin.y(), cEdge - pixel, cEdge, image.devicePixelRatio());
    const auto keyboardSeparatorAt = [&](int row) {
        return windowPixel(image, QPointF(rollOrigin.x() + keyboardWidth / 2,
                                          double(row) / image.devicePixelRatio())) ==
               QColor("#BCB4AF");
    };
Original expression:
    require(keyboardSeparatorAt(sepRow - 1) || keyboardSeparatorAt(sepRow) ||
                keyboardSeparatorAt(sepRow + 1),
            "keyboard C-boundary separator diverged from its production color");

A036 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:500
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:


    const auto pixelAt = [&](const QImage &frame, QPointF content) {
        const QPointF position = scene.surface->mapToScene(content) * frame.devicePixelRatio();
        const QPoint point(qRound(position.x()), qRound(position.y()));
Original expression:
        require(frame.rect().contains(point), "grid raster probe is outside the framebuffer");

A037 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:509
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        return frame.pixelColor(point);
    };
    const double probeY = scene.point(9, 62).y();
    // The first bar line sits at leadPad + 4 beats (tick 96); the leadPad edge
    // itself is a red start marker, not a bar line.
    const double barX = scene.leadPad + 4 * scene.beatWidth;
    const QColor rowBackground = pixelAt(image, {barX + 24, probeY});
    const QColor barLine = pixelAt(image, {barX, probeY});
Original expression:
    require(barLine != rowBackground && barLine.red() < rowBackground.red() &&
                barLine.green() < rowBackground.green() && barLine.blue() < rowBackground.blue(),
            "bar grid line is not a darkened overlay over the row background");

A038 | verifyChromeRaster | src/checks/swiftgridprototype/grid_smoke.cpp:519
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QPointF surfaceOrigin = scene.surface->mapToScene(QPointF{});
    const int gridSepRow =
        rowInsideSpan(surfaceOrigin.y(), cEdge - pixel, cEdge, image.devicePixelRatio());
    const auto gridSeparatorAt = [&](int row) {
        return pixelAt(image, {barX + 24, double(row) / image.devicePixelRatio() -
                                              surfaceOrigin.y()}) == QColor("#BCB4AF");
    };
Original expression:
    require(gridSeparatorAt(gridSepRow - 1) || gridSeparatorAt(gridSepRow) ||
                gridSeparatorAt(gridSepRow + 1),
            "grid C-row separator diverged from its production color");

A039 | verifyDocumentNote | src/checks/swiftgridprototype/grid_smoke.cpp:536
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    pass("production-grid-line-raster");
}

// Document rows deliberately reuse the shown prototype, its pointer host and
// production receiver. The standalone process has no production feed observer;
// this sole synthetic endpoint reserves the top token, away from host IDs.
void verifyDocumentNote(Scene &scene, int id, const SgdNote &note)
{
    const QRectF box = scene.expectedBox(int(note.onTick), int(note.durationTicks), note.key);
    scene.reveal(box.center());
    scene.expectNamedBox(QStringLiteral("gridNote_%1").arg(id), box,
                         "document note was re-quantized or projected with the demo timebase");
    QTest::qWait(30ms);
    const QImage image = scene.window->grabWindow();
Original expression:
    require(!image.isNull(), "document grid framebuffer is empty");

A040 | verifyDocumentNote | src/checks/swiftgridprototype/grid_smoke.cpp:538
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    auto *item = namedItem(scene.surface, QStringLiteral("gridNote_%1").arg(id));
Original expression:
    require(item, "document note vanished before its framebuffer probe");

A041 | verifyDocumentNote | src/checks/swiftgridprototype/grid_smoke.cpp:545
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QColor face = item->property("color").value<QColor>();
    const QRectF interior = box.adjusted(1, 1, -1, -1);
    const QRect deviceBox =
        QRectF(scene.surface->mapToScene(interior.topLeft()) * image.devicePixelRatio(),
               interior.size() * image.devicePixelRatio())
            .toAlignedRect();
Original expression:
    require(image.rect().contains(deviceBox), "document note raster probe is clipped");

A042 | verifyDocumentNote | src/checks/swiftgridprototype/grid_smoke.cpp:550
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    int facePixels = 0;
    for (int y = deviceBox.top(); y <= deviceBox.bottom(); ++y)
        for (int x = deviceBox.left(); x <= deviceBox.right(); ++x)
            facePixels += image.pixelColor(x, y) == face;
Original expression:
    require(facePixels >= deviceBox.width() * deviceBox.height() / 4,
            "document note face does not cover its native framebuffer bounds");

A043 | documentNoteScene | src/checks/swiftgridprototype/grid_smoke.cpp:557
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

}

QVariantList documentNoteScene(const Scene &scene)
{
    auto *fills = namedItem(scene.surface, QStringLiteral("timelineQuickPianoNoteFills"));
Original expression:
    require(fills, "document note fill layer is missing");

A044 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:577
Disposition: PARTIAL
Mapping/reason: Related predicates S001-S041 (runEditorCameraChecks/checkGridCameraWheel, rollcheck/static/camera.swift) and S052-S076 (checkViewport/checkProjection, swiftcore/EditorGridCameraChecks.swift) assert the same camera/scroll/zoom/time-axis/pitch-projection math at presenter level. Unproved: the prototype observed it through the QML viewport or the sgm_ parity-oracle substrate; no live C++/Swift differential sweep establishes these numbers here.
Source context since previous assertion:

    QVariantList result;
    for (auto *item : fills->childItems()) {
        if (item->objectName().startsWith(QStringLiteral("gridNote_")))
            result.append(QVariant::fromValue(
                QVariantList{item->objectName(), scene.itemRect(item), item->property("color")}));
    }
    return result;
}

void verifyDocumentMarks(Scene &scene, const std::vector<SgdTimeSignature> &signatures,
                         uint32_t begin, uint32_t end, bool checkLabels)
{
    scene.reveal(scene.point((double(begin) + end) / 2, 62));
    std::vector<SGSigPoint> oracleSignatures;
    for (const auto &signature : signatures)
        oracleSignatures.push_back({signature.startTick, signature.numerator, signature.denomPow2});
    const SGTimeMapFixture map{uint32_t(scene.ticksPerBeat), end, 0, end, oracleSignatures.data(),
                               oracleSignatures.size()};
    std::vector<SGGridLine> lines(sgm_timeaxis_grid_lines(&map, begin, end, nullptr, 0));
Original expression:
    require(sgm_timeaxis_grid_lines(&map, begin, end, lines.data(), lines.size()) == lines.size(),
            "production TimeAxis oracle changed within one synchronous query");

A045 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:579
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

Original expression:
    require(!lines.empty(), "document signature probe has no oracle grid lines");

A046 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:583
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    auto *time = namedItem(scene.surface, QStringLiteral("timelineQuickPianoGridTime"));
    auto *marks = namedItem(scene.window->contentItem(), QStringLiteral("timelineQuickRulerMarks"));
    auto *palette = scene.model->property("palette").value<QObject *>();
Original expression:
    require(time && marks && palette, "document time/ruler scene or palette is missing");

A047 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:601
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QColor barColor(palette->property("gridLineBar").toString());
    const QColor beatColor(palette->property("gridLineBeat").toString());
    const QColor fineBeatColor(palette->property("gridLineBeatFine").toString());
    const auto hasLine = [&](QQuickItem *layer, double x, const QColor &color) {
        for (auto *item : layer->childItems()) {
            const QRectF rect = scene.itemRect(item);
            if (item->isVisible() && rect.height() > rect.width() &&
                std::abs(rect.center().x() - x) < 0.26 / scene.dpr + 0.02 &&
                item->property("color").value<QColor>() == color)
                return true;
        }
        return false;
    };
    for (const auto &line : lines) {
        const double x = scene.pixelSnap(scene.point(line.tick, 62).x());
        // TimeAxis determines musical role; density emphasis is local to each
        // governing segment, not the tick-zero visibleGridTicks output.
Original expression:
        awaitState(
            [&] {
                return line.isBar ? hasLine(time, x, barColor)
                                  : hasLine(time, x, beatColor) || hasLine(time, x, fineBeatColor);
            },
            "rendered grid mark disagrees with production TimeAxis signature semantics");

A048 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:610
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

        if (checkLabels && line.isBar) {
            auto *label = namedItem(scene.window->contentItem(),
                                    QStringLiteral("rulerLabel_%1").arg(line.bar));
Original expression:
            require(label && label->isVisible() &&
                        std::abs(scene.itemRect(label).left() -
                                 (x + std::round(scene.baseFont * 0.125))) < 0.3,
                    "document ruler bar number/position disagrees with production TimeAxis");

A049 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:614
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

Original expression:
            require(hasLine(marks, x, QColor(palette->property("gridLine").toString())),
                    "document ruler lacks the production TimeAxis bar mark");

A050 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:629
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        }
    }
    // Reject extra bar marks too: coincident-event first-wins would otherwise
    // be able to draw the required bars plus incorrect ones.
    size_t renderedBars = 0;
    for (auto *item : time->childItems()) {
        if (item->property("color").value<QColor>() != barColor)
            continue;
        const double x = scene.itemRect(item).center().x();
        if (x < scene.pixelSnap(scene.point(begin, 62).x()) - 0.01 ||
            x >= scene.pixelSnap(scene.point(end, 62).x()) - 0.01)
            continue;
        ++renderedBars;
Original expression:
        require(std::any_of(lines.begin(), lines.end(),
                            [&](const SGGridLine &line) {
                                return line.isBar &&
                                       std::abs(x - scene.pixelSnap(
                                                        scene.point(line.tick, 62).x())) < 0.3;
                            }),
                "document scene contains a bar rejected by production TimeAxis");

A051 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:637
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    }
Original expression:
    require(renderedBars ==
                size_t(std::count_if(lines.begin(), lines.end(),
                                     [](const SGGridLine &line) { return line.isBar; })),
            "document scene lost or duplicated production TimeAxis bar marks");

A052 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:643
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const auto rasterLine = std::find_if(lines.begin(), lines.end(),
                                         [](const SGGridLine &line) { return line.tick != 0; });
Original expression:
    require(rasterLine != lines.end(), "signature raster probe overlaps the start playhead");

A053 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:647
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    scene.reveal(scene.point(rasterLine->tick, 62));
    QTest::qWait(30ms);
    const QImage image = scene.window->grabWindow();
Original expression:
    require(!image.isNull(), "signature framebuffer is empty");

A054 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:651
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QPoint probe =
        (scene.surface->mapToScene(scene.point(rasterLine->tick, 62)) * image.devicePixelRatio())
            .toPoint();
Original expression:
    require(image.rect().contains(probe), "document time raster probe is clipped");

A055 | verifyDocumentMarks | src/checks/swiftgridprototype/grid_smoke.cpp:653
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    const QColor pixel = image.pixelColor(probe);
Original expression:
    require(pixel.red() < 0xC9 && pixel.green() < 0xC1 && pixel.blue() < 0xBB,
            "document time mark does not darken the actual framebuffer row");

A056 | verifyReadOnlyInput | src/checks/swiftgridprototype/grid_smoke.cpp:669
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

}

void verifyReadOnlyInput(Scene &scene, const SgdNote &note)
{
    const QVariantList before = documentNoteScene(scene);
    const QVariant revision = scene.model->property("revision");
    const QVariant applied = scene.model->property("appliedRevisionText");
    const QVariant canUndo = scene.model->property("canUndo");
    const QVariant canRedo = scene.model->property("canRedo");
    const QVariant cursor = scene.model->property("editCursorTick");
    const QRectF box = scene.expectedBox(int(note.onTick), int(note.durationTicks), note.key);
    const auto inert = [&] {
        auto *borders = namedItem(scene.surface, "timelineQuickPianoNoteBordersAndSelection");
        auto *menu = namedItem(scene.window->contentItem(), "noteContextMenu");
Original expression:
        require(documentNoteScene(scene) == before && borders && menu &&
                    !hasSelectionRing(borders, scene.surface, box) &&
                    !namedItem(scene.surface, "drawPreview") &&
                    !menu->property("opened").toBool() &&
                    !scene.window->property("pitchBridge").value<QObject *>() &&
                    scene.model->property("activeNoteId").toInt() == -1 &&
                    scene.model->property("revision") == revision &&
                    scene.model->property("appliedRevisionText") == applied &&
                    scene.model->property("canUndo") == canUndo &&
                    scene.model->property("canRedo") == canRedo &&
                    scene.model->property("editCursorTick") == cursor,
                "read-only input changed notes, selection, edit state, undo or revision");

A057 | verifyReadOnlyInput | src/checks/swiftgridprototype/grid_smoke.cpp:712
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    };
    // Real pointer presses, movement and release: draw, move, both resize
    // grips, double-click add/delete, right-band selection and note menu.
    for (const QPointF start :
         {scene.point(1200, 70), box.center(), QPointF(box.left(), box.center().y()),
          QPointF(box.right(), box.center().y())}) {
        const QPointF end = start + QPointF(scene.beatWidth, -scene.rowHeight);
        scene.press(start);
        scene.move(end);
        inert();
        scene.release(end);
        inert();
    }
    scene.doubleClick(scene.point(1200, 70));
    inert();
    scene.doubleClick(box.center());
    inert();
    scene.reveal(box.center());
    const QPoint rightStart = scene.windowPoint(box.topLeft() - QPointF(4, 4));
    const QPoint rightEnd = scene.windowPoint(box.bottomRight() + QPointF(4, 4));
    QTest::mousePress(scene.window, Qt::RightButton, Qt::NoModifier, rightStart);
    QTest::mouseMove(scene.window, rightEnd);
    QTest::qWait(30ms);
    inert();
    QTest::mouseRelease(scene.window, Qt::RightButton, Qt::NoModifier, rightEnd);
    QTest::mouseClick(scene.window, Qt::RightButton, Qt::NoModifier,
                      scene.windowPoint(box.center()));
    QTest::qWait(30ms);
    inert();
    for (const char *method : {"deleteSelection", "undo", "redo", "resetDemo", "previewPitchCurves",
                               "commitPitchCurves", "cancelPitchCurves", "closePitchEditor"}) {
Original expression:
        require(QMetaObject::invokeMethod(scene.model, method),
                "cannot invoke read-only semantic mutation entry");

A058 | verifyReadOnlyInput | src/checks/swiftgridprototype/grid_smoke.cpp:719
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        inert();
    }
    // A typed null must overwrite the sentinel; an invalid QVariant must not
    // pass merely because the caller already initialized its result to null.
    QObject *editor = scene.model.data();
Original expression:
    require(QMetaObject::invokeMethod(scene.model, "makePitchEditor", qReturnArg(editor), 20.0,
                                      16.0, QStringLiteral("Helvetica"), QStringLiteral("Menlo")) &&
                !editor,
            "read-only semantic pitch-editor entry opened an edit session");

A059 | verifyReadOnlyInput | src/checks/swiftgridprototype/grid_smoke.cpp:724
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    int escapeAction = -1;
Original expression:
    require(
        QMetaObject::invokeMethod(scene.model, "escapePressed", qReturnArg(escapeAction), false) &&
            escapeAction == 0,
        "read-only Escape found an edit session to resolve");

A060 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:742
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    inert();
    pass("document-read-only-pointer-and-semantic-mutation-inertness");
}

void verifyDocumentFeed(Scene scene)
{
    constexpr uint64_t token = std::numeric_limits<uint64_t>::max();
    constexpr uint64_t firstRevision = (uint64_t(1) << 63) + 17;
    SgdDelivery delivery{};
    sgd_register_feed(token, &delivery);
    const auto unregister = qScopeGuard([&] {
        sgd_clear_delivery(token);
        sgd_unregister_feed(token);
    });
Original expression:
    require(scene.model->setProperty("readOnly", true) &&
                scene.model->setProperty("documentTrack", 0) &&
                QMetaObject::invokeMethod(scene.model, "bindDocument", QString::number(token)),
            "cannot bind the existing shown grid to a read-only document");

A061 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:746
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

Original expression:
    require(delivery.fn && delivery.context, "production receiver did not bind the endpoint");

A062 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:764
Disposition: PARTIAL
Mapping/reason: Related predicates S042-S051 (checkOrderedSelection, swiftcore/EditorGridCameraChecks.swift) assert selection order, selection publication domains, and document revision at DocumentSession level. Unproved: the prototype observed QUndoStack revision/canUndo state and QML noteSummary selection through the prototype model substrate, not a production DocumentSession; no differential sweep maps the two undo substrates.
Source context since previous assertion:

    scene.ticksPerBeat = 480;
    std::vector<SgdNote> notes{{1, 0, 60, 480, 240, 98},
                               {2, 1, 64, 960, 480, 127},
                               {3, 0, 67, 1440, 360, 110},
                               {4, 0, 72, 48000, 480, 100},
                               {5, 1, 72, 48000, 480, 100}};
    std::vector<SgdTimeSignature> signatures{{480, 3, 2}, {1920, 7, 3}, {1920, 5, 3}};
    const auto push = [&](uint64_t revision, int tracks = 2) {
        const SgdDocumentHeader header{
            token, revision, 480, tracks, int32_t(notes.size()), int32_t(signatures.size())};
        // Vectors and header outlive this synchronous callback only. The real
        // Swift receiver must own accepted values after it returns.
        delivery.fn(&header, notes.empty() ? nullptr : notes.data(),
                    signatures.empty() ? nullptr : signatures.data(), delivery.context);
        QTest::qWait(30ms);
    };
    const auto expectCount = [&](int count, uint64_t revision) {
Original expression:
        awaitState(
            [&] {
                return scene.model->property("renderedNoteCount").toInt() == count &&
                       documentNoteScene(scene).size() == count &&
                       scene.model->property("appliedRevisionText").toString() ==
                           QString::number(revision);
            },
            "document track/revision outputs disagree with the rendered scene");

A063 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:775
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    };
    push(firstRevision);
    expectCount(3, firstRevision);
Original expression:
    require(scene.model->property("ticksPerBeat").toInt() == 480,
            "document division did not replace the demo timebase");

A064 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:783
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    verifyDocumentNote(scene, 1, notes[0]);
    verifyDocumentNote(scene, 3, notes[2]);
    verifyDocumentMarks(scene, signatures, 0, 3600, true);
    for (const auto &signature : {std::pair{"3/4", 480}, std::pair{"5/8", 1920}}) {
        auto *label =
            namedItem(scene.window->contentItem(), QStringLiteral("rulerLabel_") + signature.first);
Original expression:
        require(label && label->isVisible() &&
                    std::abs(scene.itemRect(label).left() -
                             (scene.pixelSnap(scene.point(signature.second, 62).x()) +
                              std::round(scene.baseFont * 0.125))) < 0.3,
                "document signature label did not follow its real tick");

A065 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:789
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    }
Original expression:
    require(!namedItem(scene.window->contentItem(), "rulerLabel_7/8"),
            "overwritten coincident signature remains in the ruler");

A066 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:802
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    verifyReadOnlyInput(scene, notes[0]);

    // Exercise the real wheel host, then its writable FontInfo scale authority.
    // Direct model configuration would be overwritten by host layout updates.
    scene.reveal(scene.point(4800, 60));
    const double oldX = scene.viewport->property("contentX").toDouble();
    const double oldY = scene.viewport->property("contentY").toDouble();
    const QPoint wheelPoint = scene.windowPoint(scene.point(4800, 60));
    QWheelEvent wheel(wheelPoint, scene.window->mapToGlobal(wheelPoint), {}, {-120, -120},
                      Qt::NoButton, Qt::NoModifier, Qt::NoScrollPhase, false);
    QCoreApplication::sendEvent(scene.window, &wheel);
Original expression:
    awaitState(
        [&] {
            return scene.viewport->property("contentY").toDouble() > oldY &&
                   scene.viewport->property("contentX").toDouble() > oldX;
        },
        "read-only horizontal/vertical wheel pan is inert");

A067 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:812
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    const double previousBeatWidth = scene.beatWidth;
    auto *context = qmlContext(scene.window);
    const QPointer<QObject> fontInfo =
        context ? context->objectForName(QStringLiteral("appFontInfo")) : nullptr;
Original expression:
    require(fontInfo, "the existing appFontInfo QML id is inaccessible from the window context");

A068 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:821
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    const QFont originalFont = fontInfo->property("font").value<QFont>();
    const auto restoreFont = qScopeGuard([fontInfo, originalFont] {
        if (fontInfo)
            fontInfo->setProperty("font", originalFont);
    });
    QFont scaledFont = originalFont;
    const int scaledPixelSize = qRound(scene.baseFont * 1.5);
    scaledFont.setPixelSize(scaledPixelSize);
Original expression:
    require(fontInfo->setProperty("font", scaledFont),
            "cannot configure the existing host FontInfo font property");

A069 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:823
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

Original expression:
    awaitState([&] { return scene.window->property("baseFontPx").toDouble() == scaledPixelSize; },
               "host FontInfo scale did not update the authoritative root metric");

A070 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:826
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    QTest::qWait(30ms);
Original expression:
    require(QMetaObject::invokeMethod(scene.window, "configureViewport"),
            "cannot apply the existing host viewport configuration");

A071 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:828
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

Original expression:
    awaitState([&] { return scene.model->property("baseFontPx").toDouble() == scaledPixelSize; },
               "host viewport configuration did not publish the requested metric scale");

A072 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:835
Disposition: PARTIAL
Mapping/reason: Related predicates S001-S041 (runEditorCameraChecks/checkGridCameraWheel, rollcheck/static/camera.swift) and S052-S076 (checkViewport/checkProjection, swiftcore/EditorGridCameraChecks.swift) assert the same camera/scroll/zoom/time-axis/pitch-projection math at presenter level. Unproved: the prototype observed it through the QML viewport or the sgm_ parity-oracle substrate; no live C++/Swift differential sweep establishes these numbers here.
Source context since previous assertion:

    QTest::qWait(30ms);
    scene.baseFont = scene.model->property("baseFontPx").toDouble();
    scene.beatWidth = scene.model->property("beatWidth").toDouble();
    scene.rowHeight = scene.model->property("rowHeight").toDouble();
    scene.leadPad = scene.model->property("leadPadWidth").toDouble();
Original expression:
    require(scene.beatWidth > previousBeatWidth, "host metric scale did not change projection");

A073 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:840
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    verifyDocumentNote(scene, 1, notes[0]);
    const QPoint hoverTarget(qRound(scene.model->property("keyboardWidth").toDouble() / 2),
                             scene.windowPoint(scene.point(600, 60)).y());
    QTest::mouseMove(scene.window, hoverTarget);
Original expression:
    awaitState(
        [&] {
            auto *chip = namedItem(scene.window->contentItem(), "timelineQuickPianoHoverChip");
            return chip && chip->isVisible() &&
                   std::abs(chip->mapToScene({0, chip->height() / 2}).y() - hoverTarget.y()) <= 1;
        },
        "read-only keyboard hover preview is not live after host pan/metric-scale changes");

A074 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:854
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    expectCount(3, firstRevision);
    pass("document-read-only-live-pan-metric-scale-and-hover");

    scene.reveal(scene.point(24000, 60));
    const double savedX = scene.viewport->property("contentX").toDouble();
    const double savedY = scene.viewport->property("contentY").toDouble();
    const auto viewUnchanged = [&] {
Original expression:
        require(scene.viewport->property("contentX").toDouble() == savedX &&
                    scene.viewport->property("contentY").toDouble() == savedY &&
                    scene.model->property("beatWidth").toDouble() == scene.beatWidth &&
                    scene.model->property("rowHeight").toDouble() == scene.rowHeight,
                "document refresh or track selection reset the viewport");

A075 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:860
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    };
Original expression:
    require(scene.model->setProperty("documentTrack", 1), "cannot select document track");

A076 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:864
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    expectCount(2, firstRevision);
    viewUnchanged();
    verifyDocumentNote(scene, 2, notes[1]);
Original expression:
    require(!namedItem(scene.surface, "gridNote_1") && !namedItem(scene.surface, "gridNote_3"),
            "selected document track leaked another track's notes");

A077 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:874
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    notes[1].onTick = 1200;
    notes[1].durationTicks = 240;
    notes[1].key = 65;
    scene.reveal(scene.point(24000, 60));
    const double refreshX = scene.viewport->property("contentX").toDouble();
    const double refreshY = scene.viewport->property("contentY").toDouble();
    push(firstRevision + 1);
    expectCount(2, firstRevision + 1);
Original expression:
    require(scene.viewport->property("contentX").toDouble() == refreshX &&
                scene.viewport->property("contentY").toDouble() == refreshY,
            "higher revision changed the current scroll");

A078 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:882
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    verifyDocumentNote(scene, 2, notes[1]);
    const QVariantList accepted = documentNoteScene(scene);
    notes[1].key = 80;
    push(firstRevision);
    expectCount(2, firstRevision + 1);
Original expression:
    require(documentNoteScene(scene) == accepted,
            "track switch reset the revision guard and accepted a stale snapshot");

A079 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:886
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.reveal(scene.point(24000, 60));
    for (int track : {-1, 2}) {
Original expression:
        require(scene.model->setProperty("documentTrack", track), "cannot select invalid track");

A080 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:890
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

        expectCount(0, firstRevision + 1);
        viewUnchanged();
    }
Original expression:
    require(scene.model->setProperty("documentTrack", 1), "cannot restore document track");

A081 | verifyDocumentFeed | src/checks/swiftgridprototype/grid_smoke.cpp:892
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    expectCount(2, firstRevision + 1);
Original expression:
    require(documentNoteScene(scene) == accepted,
            "invalid track lost the retained accepted snapshot");

A082 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:932
Disposition: PARTIAL
Mapping/reason: Related predicates S001-S041 (runEditorCameraChecks/checkGridCameraWheel, rollcheck/static/camera.swift) and S052-S076 (checkViewport/checkProjection, swiftcore/EditorGridCameraChecks.swift) assert the same camera/scroll/zoom/time-axis/pitch-projection math at presenter level. Unproved: the prototype observed it through the QML viewport or the sgm_ parity-oracle substrate; no live C++/Swift differential sweep establishes these numbers here.
Source context since previous assertion:

    pass("document-track-filter-refresh-stale-guard-and-invalid-track");

    // Bound the pathological segment to ten ticks. Its raw numerator zero
    // and exponent 255 go through production TimeAxis, not copied shift math.
    signatures = {{480, 3, 2}, {1920, 7, 3}, {1920, 5, 3}, {3840, 0, 255}, {3850, 4, 2}};
    notes.clear();
    push(firstRevision + 2);
    expectCount(0, firstRevision + 2);
    verifyDocumentMarks(scene, signatures, 3820, 4000, false);
    signatures.clear();
    push(firstRevision + 3, 0);
    expectCount(0, firstRevision + 3);
    verifyDocumentMarks(scene, signatures, 0, 1920, true);
    pass("document-raw-signatures-production-oracle-and-empty-snapshot");
}

void exercise(Scene scene)
{
    verifyGridAudio();
    scene.baseFont = scene.model->property("baseFontPx").toDouble();
    scene.dpr = scene.model->property("devicePixelRatio").toDouble();
    scene.beatWidth = scene.model->property("beatWidth").toDouble();
    scene.rowHeight = scene.model->property("rowHeight").toDouble();
    scene.leadPad = scene.model->property("leadPadWidth").toDouble();
    scene.ticksPerBeat = scene.model->property("ticksPerBeat").toInt();
    scene.snap = scene.model->property("snapTicks").toInt();
    const int visibleGridTicks = scene.model->property("visibleGridTicks").toInt();
    const bool metricsValid =
        scene.baseFont > 0 && scene.dpr > 0 && scene.beatWidth > 0 && scene.rowHeight > 0 &&
        scene.leadPad >= 48 && scene.ticksPerBeat == 24 && scene.snap == 6 &&
        visibleGridTicks == 12 && scene.model->property("highestPitch").toInt() == 127 &&
        scene.model->property("lowestPitch").toInt() == 0;
    if (!metricsValid)
        std::fprintf(
            stderr,
            "grid metrics: font=%g dpr=%g beat=%g row=%g lead=%g tpb=%d snap=%d visible=%d\n",
            scene.baseFont, scene.dpr, scene.beatWidth, scene.rowHeight, scene.leadPad,
            scene.ticksPerBeat, scene.snap, visibleGridTicks);
Original expression:
    require(metricsValid, "grid metrics do not match the production 24 TPQN projection");

A083 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:941
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    verifyFixture(scene);
    verifyRaster(scene);
    verifyChromeRaster(scene);

    const QPointF pending = scene.point(98, 100);
    const QJsonArray beforePending = scene.notes();
    const int oldCursor = scene.model->property("editCursorTick").toInt();
    scene.press(pending);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforePending) &&
                !namedItem(scene.surface, QStringLiteral("drawPreview")) &&
                scene.model->property("editCursorTick").toInt() == oldCursor,
            "empty press committed a note, preview, or edit cursor before release");

A084 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:946
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.release(pending);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforePending) &&
                scene.model->property("editCursorTick").toInt() == 96,
            "empty click did not remain pending and snap its edit cursor");

A085 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:957
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    pass("pending-empty-click");

    const QJsonArray beforeShortDrag = scene.notes();
    const QPointF shortDragStart = scene.point(120, 108);
    const QPointF shortDragEnd = shortDragStart + QPointF(2, 0);
    scene.press(shortDragStart);
    scene.move(shortDragEnd);
    scene.release(shortDragEnd);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeShortDrag) &&
                !namedItem(scene.surface, QStringLiteral("drawPreview")),
            "subthreshold horizontal drag drew a note");

A086 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:966
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("production-minimum-draw-distance");
    const QJsonArray beforeDraw = scene.notes();
    const QPointF drawAnchor = scene.point(168, 100);

    const QPointF drawLeft = scene.point(146, 102);
    scene.press(drawAnchor);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeDraw),
            "draw press changed committed musical state");

A087 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:969
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.move(drawLeft);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeDraw),
            "draw preview leaked into committed musical state");

A088 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:973
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPreview(scene.expectedBox(144, 30, 102));
    scene.release(drawLeft);
Original expression:
    awaitState([&] { return scene.notes().size() == beforeDraw.size() + 1; },
               "leftward draw did not commit exactly one note");

A089 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:976
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    const int drawnId = addedNoteId(beforeDraw, scene.notes());
Original expression:
    require(drawnId >= 31, "draw did not publish a new note identity");

A090 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:980
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPublished(drawnId, 144, 30, 102,
                          "leftward draw span or pointer-following pitch is wrong");
    const QJsonObject drawn = scene.note(drawnId);
Original expression:
    require(drawn.value("track").toInt(-1) == 0 && drawn.value("velocity").toInt(-1) == 100 &&
                !drawn.value("ghost").toBool(true) && drawn.value("selected").toBool(),
            "drawn note did not publish active-track defaults or persistent selection");

A091 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:983
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

Original expression:
    require(!namedItem(scene.surface, QStringLiteral("drawPreview")),
            "draw preview remained after commit");

A092 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:987
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    auto *borderLayer =
        namedItem(scene.surface, QStringLiteral("timelineQuickPianoNoteBordersAndSelection"));
Original expression:
    require(borderLayer &&
                hasSelectionRing(borderLayer, scene.surface, scene.expectedBox(144, 30, 102)),
            "selected ring is not published after gesture release");

A093 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:997
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("leftward-preview-commit-and-selection");

    const QJsonArray beforeRightDraw = scene.notes();
    scene.press(scene.point(192, 96));
    scene.move(scene.point(200, 96));
    const QPointF drawRight = scene.point(213, 95);
    scene.move(drawRight);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeRightDraw),
            "rightward draw preview changed committed musical state");

A094 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1001
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPreview(scene.expectedBox(192, 24, 95));
    scene.release(drawRight);
Original expression:
    awaitState([&] { return scene.notes().size() == beforeRightDraw.size() + 1; },
               "rightward draw did not commit exactly one note");

A095 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1012
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPublished(addedNoteId(beforeRightDraw, scene.notes()), 192, 24, 95,
                          "rightward draw span or pointer-following pitch is wrong");
    pass("rightward-preview-and-commit");

    const QJsonArray beforeMove = scene.notes();
    const QRectF firstBox = scene.expectedBox(0, 18, 60);
    scene.press(firstBox.center());
    const QJsonArray pressedMove = scene.notes();
    scene.move(firstBox.center() + QPointF(scene.beatWidth * 6 / 24, -scene.rowHeight));
Original expression:
    require(musicalState(scene.notes()) == musicalState(pressedMove),
            "existing-note move changed committed JSON during preview");

A096 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1018
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectBox(namedItem(scene.surface, QStringLiteral("gridNote_1")),
                    scene.expectedBox(6, 18, 61), "note body did not preview a move");
    scene.release(firstBox.center() + QPointF(scene.beatWidth * 6 / 24, -scene.rowHeight));
    scene.expectPublished(1, 6, 18, 61, "note body did not commit source-rounded movement");
Original expression:
    require(beforeMove.size() == scene.notes().size(), "move changed note count");

A097 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1028
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("body-move-preview-and-commit");

    const double reach = scene.baseFont * 0.25;
    const QRectF resizeBox = scene.expectedBox(24, 18, 64);
    const QPointF outsideRight(resizeBox.right() + reach / 2, resizeBox.center().y());
    scene.press(outsideRight);
    const QJsonArray beforeResizePreview = scene.notes();
    const QPointF longer = outsideRight + QPointF(scene.beatWidth * 6 / 24, 0);
    scene.move(longer);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeResizePreview),
            "right-resize preview changed committed JSON");

A098 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1049
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectBox(namedItem(scene.surface, QStringLiteral("gridNote_2")),
                    scene.expectedBox(24, 24, 64), "outside right grip did not resize");
    scene.release(longer);
    scene.expectPublished(2, 24, 24, 64, "right resize did not preserve the start");

    const QRectF leftBox = scene.expectedBox(72, 18, 71);
    const QPointF outsideLeft(leftBox.left() - reach / 2, leftBox.center().y());
    scene.press(outsideLeft);
    const QPointF earlier = outsideLeft - QPointF(scene.beatWidth * 6 / 24, 0);
    scene.move(earlier);
    scene.release(earlier);
    scene.expectPublished(4, 66, 24, 71, "outside left grip did not preserve the end");
    pass("outside-edge-resize-both-sides");

    const QRectF offsetBox = scene.expectedBox(48, 18, 67);
    const QPointF offsetGrip(offsetBox.right() + reach / 2, offsetBox.center().y());
    const QJsonArray beforeDeadband = scene.notes();
    scene.press(offsetGrip);
    scene.move(offsetGrip + QPointF(1, 0));
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeDeadband),
            "offset resize deadband changed committed JSON");

A099 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1063
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectBox(namedItem(scene.surface, QStringLiteral("gridNote_3")), offsetBox,
                    "offset grip jumped before crossing resize deadband");
    scene.cancel(offsetGrip + QPointF(1, 0));
    scene.expectPublished(3, 48, 18, 67, "cancel after deadband changed fixture note");
    pass("offset-resize-deadband");

    const QRectF cancelBox = scene.expectedBox(96, 18, 74);
    const QJsonArray beforeCancel = scene.notes();
    scene.press(cancelBox.center());
    const QPointF cancelMoved =
        cancelBox.center() + QPointF(scene.beatWidth * 6 / 24, scene.rowHeight);
    scene.move(cancelMoved);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeCancel),
            "existing-note cancel setup changed committed JSON");

A100 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1068
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectBox(namedItem(scene.surface, QStringLiteral("gridNote_5")),
                    scene.expectedBox(102, 18, 73), "existing note did not preview before cancel");
    scene.cancel(cancelMoved);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeCancel),
            "cancellation did not restore existing musical state");

A101 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1076
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPublished(5, 96, 18, 74, "cancelled existing delegate did not restore");

    const QPointF cancelDrawStart = scene.point(216, 105);
    const QPointF cancelDrawEnd = scene.point(226, 104);
    scene.press(cancelDrawStart);
    scene.move(cancelDrawEnd);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeCancel),
            "cancelled draw preview entered committed JSON");

A102 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1080
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    scene.expectPreview(scene.expectedBox(216, 12, 104));
    scene.cancel(cancelDrawEnd);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeCancel) &&
                !namedItem(scene.surface, QStringLiteral("drawPreview")),
            "cancelled draw did not retract its preview");

A103 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1088
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("pointer-cancellation");

    const QJsonArray beforeGhost = scene.notes();
    const QPointF ghostPoint = scene.point(13, 48);
    scene.click(ghostPoint);
Original expression:
    require(musicalState(scene.notes()) == musicalState(beforeGhost) &&
                !scene.note(16).value("selected").toBool() &&
                scene.model->property("editCursorTick").toInt() == 12,
            "ghost note was editable instead of acting as empty active-track space");

A104 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1097
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("ghost-is-not-editable");

    const QJsonArray beforeDouble = scene.notes();
    const QPointF beyondOldCeiling = scene.point(420, 110);
    scene.doubleClick(beyondOldCeiling);
Original expression:
    awaitState([&] { return scene.notes().size() == beforeDouble.size() + 1; },
               "double-click beyond 16 beats did not add one note");

A105 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1104
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    const int doubleId = addedNoteId(beforeDouble, scene.notes());
    scene.expectPublished(doubleId, 420, 6, 110,
                          "double-click did not add one snap cell beyond old ceiling");
    const QRectF narrow = scene.expectedBox(420, 6, 110);
    const QJsonObject doubled = scene.note(doubleId);
Original expression:
    require(doubled.value("track").toInt(-1) == 0 && !doubled.value("ghost").toBool(true),
            "double-click note was not created on the active track");

A106 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1111
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.press(narrow.center());
    const QJsonArray narrowPressed = scene.notes();
    const QPointF narrowMoved =
        narrow.center() + QPointF(scene.beatWidth * 6 / 24, -scene.rowHeight);
    scene.move(narrowMoved);
Original expression:
    require(musicalState(scene.notes()) == musicalState(narrowPressed),
            "narrow-note move changed committed JSON during preview");

A107 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1117
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.release(narrowMoved);
    scene.expectPublished(doubleId, 426, 6, 111,
                          "protected narrow-note center resized instead of moving");
    scene.doubleClick(scene.expectedBox(426, 6, 111).center());
Original expression:
    awaitState([&] { return scene.notes().size() == beforeDouble.size(); },
               "double-click on an existing note did not delete it");

A108 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1119
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

Original expression:
    require(scene.note(doubleId).isEmpty(), "deleted double-click note remains published");

A109 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1130
Disposition: NATIVE
Mapping/reason: Prototype native-window/rendering/harness observation (QML item geometry or visibility, framebuffer raster or pixel color, native mouse grab, viewport handle, QML chrome handle, or smoke-harness precondition). A presenter-level Swift check cannot establish this observation.
Source context since previous assertion:

    pass("double-click-add-move-delete-without-16-beat-clamp");

    const double oldWidth = scene.viewport->property("contentWidth").toDouble();
    const int extentTick =
        (int((oldWidth - scene.leadPad) / scene.beatWidth * scene.ticksPerBeat) / scene.snap - 2) *
        scene.snap;
    const QString addedBar =
        QString::number(int((oldWidth - scene.leadPad) / scene.beatWidth / 4) + 2);
    const QJsonArray beforeGrowth = scene.notes();
    scene.doubleClick(scene.point(extentTick + 1, 106));
Original expression:
    awaitState(
        [&] {
            auto *label = namedItem(scene.window->contentItem(), "rulerLabel_" + addedBar);
            return scene.viewport->property("contentWidth").toDouble() > oldWidth && label &&
                   label->width() >= label->property("contentWidth").toDouble();
        },
        "extending the song did not publish complete ruler metrics");

A110 | exercise | src/checks/swiftgridprototype/grid_smoke.cpp:1164
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    scene.expectPublished(addedNoteId(beforeGrowth, scene.notes()), extentTick, scene.snap, 106,
                          "note creation at the old content edge failed");
    pass("content-growth-and-ruler-metrics");

    const QPointF hoverContent = scene.point(24, 60);
    scene.reveal(hoverContent);
    const QPoint hoverTarget(qRound(scene.model->property("keyboardWidth").toDouble() / 2),
                             scene.windowPoint(hoverContent).y());
    QTest::mouseMove(scene.window, hoverTarget);
    const QPointer<QQuickItem> chip =
        namedItem(scene.window->contentItem(), QStringLiteral("timelineQuickPianoHoverChip"));
    const bool hoverAligned = QTest::qWaitFor(
        [&] {
            return chip && chip->isVisible() &&
                   std::abs(chip->mapToScene(QPointF(0, chip->height() / 2)).y() -
                            hoverTarget.y()) <= 1.0;
        },
        3s);
    if (!hoverAligned) {
        auto *grabber = scene.window->mouseGrabberItem();
        std::fprintf(
            stderr, "hover target=%d chipCenter=%g visible=%d key=%d scroll=%g grabber=%s\n",
            hoverTarget.y(), chip ? chip->mapToScene(QPointF(0, chip->height() / 2)).y() : -1,
            chip && chip->isVisible(), scene.model->property("hoverKey").toInt(),
            scene.viewport->property("contentY").toDouble(),
            grabber ? grabber->metaObject()->className() : "none");
    }
Original expression:
    require(hoverAligned, "scrolled keyboard hover chip is not beside the hovered row");

A111 | startSmoke | src/checks/swiftgridprototype/grid_smoke.cpp:1180
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    pass("scrolled-keyboard-hover-chip");
    verifyGridInteractions(scene.window, scene.model);
    verifyGridUndo(scene.window, scene.model);
    verifyGridCancel(scene.window, scene.model);
    verifyGridEscape(scene.window, scene.model);
    verifyDocumentFeed(scene);

    std::puts("SWIFT_GRID_SMOKE PASS");
    std::fflush(stdout);
    QCoreApplication::exit(EXIT_SUCCESS);
}

void startSmoke()
{
    auto *app = QCoreApplication::instance();
Original expression:
    require(app, "pre-routine ran without application");

A112 | startSmoke | src/checks/swiftgridprototype/grid_smoke.cpp:1204
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. No equivalent Swift predicate in the existing paired S001-S086 inventory has been established; the prototype gesture/model/document-feed observation below remains an open obligation. Production behavior moved to the swiftrollgated C++ lane and the sgc_/sgs_ seams, which carry no Swift predicates in this pairing.
Source context since previous assertion:

    auto *poll = new QTimer(app);
    const auto checkReady = [poll] {
        for (QWindow *candidate : QGuiApplication::allWindows()) {
            auto *window = qobject_cast<QQuickWindow *>(candidate);
            if (!window || !window->isExposed())
                continue;
            auto *surface = namedItem(window->contentItem(), QStringLiteral("pianoGridSurface"));
            auto *viewport = namedItem(window->contentItem(), QStringLiteral("pianoGridViewport"));
            auto *model = window->property("gridModel").value<QObject *>();
            if (!surface || !viewport || !model || viewport->width() <= 0 ||
                model->property("noteSummary").toString().isEmpty() ||
                !namedItem(surface, QStringLiteral("gridNote_1")) ||
                !namedItem(window->contentItem(), QStringLiteral("rulerLabel_1")))
                continue;
            poll->stop();
            poll->deleteLater();
            exercise({window, viewport, surface, model});
            return;
        }
    };
    QObject::connect(poll, &QTimer::timeout, app, checkReady);
    poll->start(25ms);
    QTimer::singleShot(30s, app,
                       [] {
Original expression:
                       [] { fail("30-second wall timeout waiting for scene or gesture result");

Remaining original source (closing sequence and helper bodies):
 });
}

void prepareSmoke()
{
    QFont font = QGuiApplication::font();
    font.setPixelSize(13);
    QGuiApplication::setFont(font);
    QTimer::singleShot(0ms, QCoreApplication::instance(), startSmoke);
}
} // namespace

extern "C" void installGridSmoke(void)
{
    if (qEnvironmentVariable("PORYDAW_SWIFT_GRID_SMOKE") != QStringLiteral("1"))
        return;
    qAddPreRoutine(prepareSmoke);
}

Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:16
report.expect(near(compact.snapshot.viewportWidth, 50) &&
                  near(compact.snapshot.minHScroll, -48) &&
                  near(compact.snapshot.maxHScroll, 0),
                  cppID: cameraTransformID,
                  message: "caller-resolved minimum viewport produces observable camera bounds")

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:26
report.expect(near(camera.tickAtContentX(content), 96), cppID: cameraTransformID,
                  message: "tick/content transforms round-trip with a fractional offset")

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:28
report.expect(near(camera.snapshot.scrollX, 10.25), cppID: cameraTransformID,
                  message: "horizontal restoration preserves fractional offsets")

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:30
report.expect(near(camera.displayX(tick: 24, origin: 0.2, dpr: 2), 22),
                  cppID: cameraTransformID, message: "display projection snaps to a physical pixel")

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:33
report.expect(near(camera.snapshot.scrollX, -48) && near(camera.snapshot.minHScroll, -48),
                  cppID: cameraTransformID, message: "horizontal scrolling clamps to production pre-roll")

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:36
report.expect(near(camera.snapshot.scrollX, 640), cppID: cameraTransformID,
                  message: "bound timeline end reaches the plot origin")

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:43
report.expect(near(viewportCamera.snapshot.scrollX, -48),
                  cppID: cameraTransformID,
                  message: "bound viewport shrink reclamps the changed pre-roll floor")

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:48
report.expect(near(viewportCamera.snapshot.scrollX, 10.25),
                  cppID: cameraTransformID,
                  message: "bound viewport updates preserve a legal fractional offset")

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:56
report.expect(zoom.zoomChanged && zoom.scrollChanged &&
                  near(camera.tickAtContentX(anchorX), anchoredTick), cppID: cameraZoomID,
                  message: "interactive time zoom preserves its tick anchor")

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:65
report.expect(near(clampedTimeZoom.snapshot.scrollX, 64) &&
                  !near(clampedTimeZoom.tickAtContentX(boundedAnchor), tickBeforeBoundedZoom),
                  cppID: cameraZoomID,
                  message: "time zoom reaches its bound when the pointer anchor is unreachable")

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:70
report.expect(near(camera.snapshot.pixelsPerBeat, 48) &&
                  near(camera.snapshot.keyHeight, 16) && near(camera.snapshot.scrollX, 10.5) &&
                  near(camera.snapshot.scrollY, 20.25), cppID: cameraZoomID,
                  message: "unanchored restoration keeps valid fractional camera state")

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:78
report.expect(near(sanitized.snapshot.pixelsPerBeat, 32) &&
                  near(sanitized.snapshot.keyHeight, 12) &&
                  near(sanitized.snapshot.scrollX, 0) && near(sanitized.snapshot.scrollY, 0),
                  cppID: cameraZoomID,
                  message: "restoration sanitizes non-finite transient view-state values independently")

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:86
report.expect(near(camera.snapshot.scrollX, 20), cppID: cameraTransformID,
                  message: "content shrink reclamps the horizontal offset")

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:89
report.expect(near(camera.snapshot.viewportWidth, 800) && near(camera.snapshot.scrollY, 0),
                  cppID: cameraTransformID,
                  message: "viewport growth reclamps vertical content")

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:93
report.expect(near(camera.snapshot.scrollX, -80), cppID: cameraTransformID,
                  message: "an unbound timeline homes to its lead pad")

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:96
report.expect(near(camera.snapshot.scrollX, -100), cppID: cameraTransformID,
                  message: "unbound viewport updates follow the resolved home")

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:100
report.expect(chromatic.visibleRowCount == 128 && (0..<128).allSatisfy { pitch in
        chromatic.row(forPitch: pitch) == 127 - pitch &&
            chromatic.visiblePitch(at: 127 - pitch) == pitch
    }, cppID: cameraPitchID,
    message: "chromatic pitch mapping remains bijective across the full MIDI range")

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:108
report.expect(folded.visiblePitch(at: 0) == 67 && folded.row(forPitch: 60) == 2 &&
                  folded.row(forPitch: 61) == PitchProjection.hiddenRow &&
                  folded.nearestVisiblePitch(to: 62) == 60 &&
                  folded.nearestVisiblePitch(to: 100) == 67 &&
                  folded.nearestVisiblePitch(to: -5) == 60 &&
                  emptyProjection.nearestVisiblePitch(to: 60) == nil,
                  cppID: cameraPitchID,
                  message: "pitch rows and nearest lookup preserve production ordering")

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:116
report.expect(folded.row(atY: 10, keyHeight: 10, scrollY: 0, dpr: 2) == 1 &&
                  folded.pitch(atY: 29.75, keyHeight: 10, scrollY: 0, dpr: 2) == 60,
                  cppID: cameraPitchID, message: "DPR-snapped row and pitch inverses agree")

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:119
report.expect(near(folded.rowBottom(0, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) ?? -1,
                       10) &&
                  folded.row(atY: 9.99, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 0 &&
                  folded.row(atY: 10, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 1,
                  cppID: cameraPitchID,
                  message: "fractional DPR snapping defines the inverse row boundary")

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:131
report.expect(near(projectedContent.snapshot.scrollY, 15),
                  cppID: cameraPitchID,
                  message: "pitch projection shrink reclamps the vertical offset")

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:139
report.expect(near(pitchCamera.snapshot.maxVScroll, 15), cppID: cameraPitchID,
                  message: "vertical bounds derive from projected row height")

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:146
report.expect(near(anchoredRow, resultingRow), cppID: cameraZoomID,
                  message: "pitch zoom preserves its row anchor when bounds permit")

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:159
report.expect(near(clampedPitchZoom.snapshot.scrollY, 0) &&
                  near(clampedPitchZoom.snapshot.maxVScroll, 0) &&
                  !near(rowAfterBoundedZoom, rowBeforeBoundedZoom),
                  cppID: cameraZoomID,
                  message: "pitch zoom reaches its bound when the row anchor is unreachable")

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:166
report.expect(pitchCamera.ensureKeyVisible(60) && near(pitchCamera.snapshot.scrollY, 15),
                  cppID: cameraPitchID, message: "key reveal scrolls only enough to show its row")

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:168
report.expect(!pitchCamera.ensureKeyVisible(61), cppID: cameraPitchID,
                  message: "hidden pitches do not move the camera")

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:173
report.expect(reveal.ensureTickVisible(480, dpr: 2) &&
                  near(reveal.snapshot.scrollX, 573.3333333333),
                  cppID: cameraTransformID,
                  message: "tick reveal uses the production one-third anchor")

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:178
report.expect(!reveal.ensureRangeVisible(startTick: 0, endTick: 149,
                                             preferEnd: false, dpr: 1) &&
                  near(reveal.snapshot.scrollX, 0), cppID: cameraTransformID,
                  message: "a fitting range on the physical right boundary stays visible")

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:182
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: false, dpr: 1) &&
                  near(reveal.contentX(tick: 240), 0), cppID: cameraTransformID,
                  message: "wide range reveal prefers its requested leading edge")

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:187
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: true, dpr: 1) &&
                  near(reveal.contentX(tick: 480), 199), cppID: cameraTransformID,
                  message: "wide range reveal honors the requested trailing edge")

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:220
report.expect(
            after.pixelsPerBeat > before.pixelsPerBeat
                && gridCameraNear(session.camera.tickAtContentX(anchorX), anchorTick, tolerance: 1e-7)
                && counters.camera == publications + 1,
            cppID: gridCameraWheelID, message: "phase \(phase.rawValue) performs one anchored time zoom publication")

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:233
report.expect(
        session.camera.snapshot == beforeMomentum && counters.camera == momentumPublications,
        cppID: gridCameraWheelID, message: "momentum suppresses time zoom without publishing")

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:248
report.expect(
        gridCameraNear(angleZoom, 35 * pow(1.0015, 120), tolerance: 1e-7)
            && gridCameraNear(session.camera.snapshot.pixelsPerBeat,
                    angleZoom * pow(1.0015, 120), tolerance: 1e-7),
        cppID: gridCameraWheelID, message: "pixel deltas receive five-times angle weighting")

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:253
report.expect(
        counters.camera == 2,
        cppID: gridCameraWheelID, message: "effective angle and pixel zooms each publish once")

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:263
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 90) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Shift wheel pans by negative selected delta exactly once")

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:278
report.expect(
        gridCameraNear(minimumPan.scrollX, minimumPan.minHScroll)
            && minimumPublications == 1 && counters.camera == minimumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at minimum and repeated clamped pan is silent")

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:295
report.expect(
        gridCameraNear(maximumPan.scrollX, maximumPan.maxHScroll)
            && maximumPublications == 1 && counters.camera == maximumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at maximum and repeated clamped pan is silent")

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:306
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 112) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "horizontal-only momentum still pans exactly once")

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:318
report.expect(
        pitchAfter.keyHeight > pitchBefore.keyHeight
            && gridCameraNear((pitchAfter.scrollY + 100) / pitchAfter.keyHeight, pitchRow, tolerance: 1e-7)
            && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Ctrl wheel performs one row-anchored pitch zoom publication")

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:329
report.expect(
        session.camera.snapshot == pitchMomentum && counters.camera == 0,
        cppID: gridCameraWheelID, message: "momentum suppresses pitch zoom without publishing")

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:339
report.expect(
        gridCameraNear(session.camera.snapshot.scrollY, gutterBefore + 6) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "gutter wheel scrolls by negative half-delta exactly once")

S042 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:106
report.expect(session.selectedNoteOrder == [b, a]
        && session.document.note(session.selectedNoteOrder[0])?.velocity == 90,
        cppID: id, message: "Ctrl-add of earlier A(v40) retains later B(v90) as first selection")

S043 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:111
report.expectEqual([a, b], session.selectedNoteOrder, cppID: id,
                       what: "deselecting and re-adding appends the note")

S044 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:114
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "replacement preserves first occurrence and removes duplicates")

S045 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:118
report.expect(Array(session.selectedNoteOrder.prefix(2)) == [b, a]
        && session.selectedNotes.contains(c),
        cppID: id, message: "band keeps press order before newly covered notes")

S046 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:122
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "band cancellation restores selection order")

S047 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:126
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement publishes the selection domain")

S048 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:133
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement and additive selection coalesce")

S049 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:138
report.expect(publications.isEmpty, cppID: id,
                  message: "unchanged normalized selection publishes nothing")

S050 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:140
report.expect(session.document.revision == revision
        && session.document.history.currentIdentity == history
        && session.document.isDirty == dirty,
        cppID: id, message: "selection gestures never mutate document or history")

S051 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:146
report.expect(session.selectedNoteOrder == [c, a] && session.selectedNotes == Set([c, a]),
                  cppID: id, message: "deletion prunes membership and preserves survivor order")

S052 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:160
report.expect(
        gridCameraNear(first.viewportWidth, 640) && gridCameraNear(first.rollHeight, 320),
        cppID: viewportID, message: "viewport dimensions are pushed into the session camera")

S053 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:163
report.expect(
        gridCameraNear(first.minHScroll, expectedMin)
            && gridCameraNear(first.maxHScroll,
                    Double(session.timeline.lengthTicks) * first.pixelsPerTick)
            && gridCameraNear(first.maxVScroll, expectedMaxV),
        cppID: viewportID, message: "camera bounds derive from viewport, document extent, and projection")

S054 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:169
report.expect(
        first.scrollY >= 0 && first.scrollY <= first.maxVScroll,
        cppID: viewportID, message: "first viewport push homes vertically within camera bounds")

S055 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:172
report.expect(
        gridCameraNear(grid.beatWidth, first.pixelsPerBeat) && gridCameraNear(grid.rowHeight, first.keyHeight),
        cppID: viewportID, message: "published scales equal the session camera snapshot")

S056 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:183
report.expect(
        session.camera.snapshot == fractional && counters.camera == 0,
        cppID: viewportID, message: "identical viewport push preserves fractional offsets and publishes nothing")

S057 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:191
report.expect(
        tallViewport.maxVScroll < shortHeight.maxVScroll
            && gridCameraNear(tallViewport.scrollY, tallViewport.maxVScroll),
        cppID: viewportID, message: "roll-height increase reclamps vertical scroll")

S058 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:206
report.expect(
        gridCameraNear(doubled.pixelsPerBeat, 69) && gridCameraNear(doubled.keyHeight, 26),
        cppID: viewportID, message: "double-font push applies the rounded font-relative defaults")

S059 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:209
report.expect(
        gridCameraNear(restored.pixelsPerBeat, 35) && gridCameraNear(restored.keyHeight, 13),
        cppID: viewportID, message: "font push back restores the exact earlier scale")

S060 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:212
report.expect(
        counters.camera == 2,
        cppID: viewportID, message: "each effective font push publishes exactly once")

S061 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:227
report.expect(
        gridCameraNear(fontMinimum.pixelsPerBeat, 9) && gridCameraNear(fontMinimum.keyHeight, 9),
        cppID: viewportID, message: "pushed font defines time and key-height minimums")

S062 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:230
report.expect(
        gridCameraNear(fontMaximum.pixelsPerBeat, 1_387) && gridCameraNear(fontMaximum.keyHeight, 69),
        cppID: viewportID, message: "pushed font defines time and key-height maximums")

S063 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:250
report.expect(
        farID != nil && expandedLength > originalLength
            && session.timeline.lengthTicks < expandedLength
            && gridCameraNear(shrunk.scrollX, shrunk.maxHScroll),
        cppID: viewportID, message: "document shrink privately reconciles extent and reclamps horizontal scroll")

S064 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:272
report.expect(
        !markXs.isEmpty && markXs.allSatisfy { $0 >= -cullingWidth - 2 && $0 <= 2 * cullingWidth + 2 },
        cppID: projectionID, message: "generated time marks stay inside the one-viewport culling window")

S065 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:275
report.expect(
        (markXs.min() ?? 1) <= 0 && (markXs.max() ?? -1) >= cullingWidth,
        cppID: projectionID, message: "generated time marks cover the visible plot")

S066 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:281
report.expect(
        grid.noteSummary == summaryBeforeCameraMove,
        cppID: projectionID, message: "camera movement does not alter noteSummary document state")

S067 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:309
report.expect(
        projected,
        cppID: projectionID, message: "known note rectangle equals the camera projection")

S068 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:317
report.expect(
        maskCount == (zeroX > 0 ? 1 : 0),
        cppID: projectionID, message: "pre-roll mask presence follows the projected tick-zero position")

S069 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:329
report.expect(
        session.selectedNotes.contains(originalNote.id),
        cppID: projectionID, message: "beginPointer selects the note under the projected rectangle")

S070 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:347
report.expect(
        previewRect.map {
            gridCameraNear($0.x, session.camera.displayX(
                tick: Double(expectedTick), origin: 0, dpr: grid.devicePixelRatio))
                && gridCameraNear($0.y, expectedPreviewY + 1 / grid.devicePixelRatio)
        } ?? false,
        cppID: projectionID, message: "one-cell drag publishes the snapped tick and pitch preview")

S071 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:358
report.expect(
        session.document.revision == originalRevision + 1
            && moved.map { Int($0.tick) == expectedTick && Int($0.pitch) == expectedPitch } == true,
        cppID: projectionID, message: "endPointer commits one revision at the snapped tick and pitch")

S072 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:402
report.expect(
        session.document.revision == drawRevision + 1
            && created.map { Int($0.tick) == drawTick && Int($0.pitch) == drawPitch } == true,
        cppID: projectionID, message: "draw drag on an empty row adds one snapped note")

S073 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:416
report.expect(
        session.selectedNotes == visibleIDs,
        cppID: projectionID, message: "right-drag band selects exactly the visible intersecting notes")

S074 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:425
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "inputCancelled discards an active gesture without document mutation")

S075 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:432
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "Escape discards an active gesture without document mutation")

S076 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:440
report.expect(
        session.document.revision == cancelRevision,
        cppID: projectionID, message: "press outside projected pitch rows is rejected")

S077 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:475
report.expect(
        counters.camera == before + 1
            && gridCameraNear(tickReveal.scrollX, expectedTickReveal),
        cppID: isolationID, message: "ensureTickVisible uses the reveal fraction in one publication")

S078 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:488
report.expect(
        counters.camera == before + 1
            && gridCameraNear(
                endReveal.scrollX,
                endReveal.maxHScroll - endReveal.viewportWidth
                    + 1 / grid.devicePixelRatio),
        cppID: isolationID, message: "preferEnd true aligns an oversized range to the right edge")

S079 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:504
report.expect(
        counters.camera == before + 1 && gridCameraNear(startReveal.scrollX, 0),
        cppID: isolationID, message: "preferEnd false aligns an oversized range to its start")

S080 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:512
report.expect(
        counters.camera == before + 1
            && gridCameraNear(keyReveal.scrollY, keyReveal.maxVScroll),
        cppID: isolationID, message: "ensureKeyVisible aligns the bottom pitch at the viewport edge")

S081 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:516
report.expect(
        gridCameraNear(grid.cameraScrollX, session.camera.snapshot.scrollX)
            && gridCameraNear(grid.cameraScrollY, session.camera.snapshot.scrollY),
        cppID: isolationID, message: "presenter scroll values track the revealed session camera")

S082 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:536
report.expect(
        counters.camera == before,
        cppID: isolationID, message: "redundant, non-finite, hidden, and already-visible navigation is silent")

S083 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:542
report.expect(
        session.camera.snapshot == published && grid.editCursorTick == Int(cursor),
        cppID: isolationID, message: "direct edit-cursor change neither moves camera nor republishes presenter cursor")

S084 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:545
report.expect(
        counters.document == 0 && counters.playback == 0 && counters.cursor == 1
            && session.document.revision == revision && session.document.isDirty == dirty
            && session.document.history.canUndo == canUndo
            && session.document.history.canRedo == canRedo,
        cppID: isolationID, message: "camera navigation is isolated from document, playback, dirty state, and history")

S085 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:563
report.expect(
        inserted?.count == 1 && counters.camera == 0,
        cppID: isolationID, message: "committed document edit performs no standalone camera publication")

S086 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:566
report.expect(
        counters.playback == 1 && counters.document == 1 && counters.coherentDocumentCallback,
        cppID: isolationID, message: "one edit publishes one coherent playback and document refresh after reconciliation")

Shared helper:
No shared Swift fixture file carrying report.expect*/expectEqual calls was identified for this pairing: camera.swift is self-contained (Foundation+PorydawApp) and EditorGridCameraChecks.swift builds its DocumentSession inline (fixture-guard report.fail calls at EditorGridCameraChecks.swift:78,88,322,393,533 are guard aborts, not predicates). The expectEqual helper itself lives in src/checks/support/corecheck/CoreCheckSupport.swift (SHA-256: d4419a4b2e15d1291d435c2e410d3b2f8357a875f9cb27751e01ef1d01beaf65) as a definition, not a call site:
    func expectEqual<T: Equatable>(_ expected: T, _ actual: T, cppID: String,
                                    what: String) {
        if expected == actual {
            pass(cppID, row: what)
        } else {
            fail(cppID, "\(what): expected=\(expected) actual=\(actual)")
        }
    }
