Assertion correspondence: swiftgridprototype/interaction_smoke.cpp

Reference revision: 18fee9357273c3351d1eaadf4fe6f8d52edb9a98
Original: src/checks/swiftgridprototype/interaction_smoke.cpp
Original SHA-256: 6e26d7182fa795f65ff85310df925b71838f146cbd684d0dd33ed760f4a25a9f
Swift counterpart: src/checks/rollcheck/static/camera.swift
Swift SHA-256: 95fb2e80f95bf57e41250632cb8b3e7378fa32be143d02f5bd092313f6029808
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift SHA-256: 3865b7618bb681a7720785153c04ce764b67d2780e09dfcf6e833fc6a20c62fd
Deleted in: fe1ff4df9f4834cbd4e4e59d2e710078c10a8ced Wave 4 re-scoped grid milestone: writable Swift roll through production seams
Note: swiftrollgated/*.swift matches no file in this checkout; the receiving lane is C++-only (tst_swiftrollgated.cpp). The deletion commit records the load-bearing prototype rows as moved to swiftrollgated (testBandLeadingGripDecline, testBandTrailingGripResizeCommitOnce).
Retired in: feature/w2-retired-prototypes — this ledger is the deletion certificate for
src/checks/swiftgridprototype/interaction_smoke.cpp. The C++ original is uncompiled in every CMake target
and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt,
src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from
disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype
interaction scene. The controller deletes the dead sources after this ledger passes; nothing is deleted by
this edit.

Reference revision: 18fee9357273c3351d1eaadf4fe6f8d52edb9a98
Original: src/checks/swiftgridprototype/interaction_smoke.cpp
Original SHA-256: 6e26d7182fa795f65ff85310df925b71838f146cbd684d0dd33ed760f4a25a9f
Verification: lane run recorded by the controller, not re-run for this ledger:
deno task verify --filter swiftcore --verbose -> verify: 1/6 ok (11.40s, 5 skipped); run_checks: PASS
(all harnesses in 11.40s).
Result: swiftcore PASS. No live native interaction_smoke run exists or is claimed; the deleted harness
neither builds nor executes.

Deletion certificate
Retirement: 48 original assertion sites, all terminal (12 RETIRED-REPRESENTATION, 36 honest GAPs, zero
PARTIAL, zero NATIVE, zero MATCHED). No executing predicate carries an original interaction_smoke cppID, so
nothing is MATCHED. Band-selection sites (A011,A014,A015,A017) and reticle/grabber-raster plus demo-reset
sites are RETIRED-REPRESENTATION with executing rollcheck citations plus an explicit note that the exact
prototype scene end-to-end observation is not reproduced. The 36 GAPs are honest feature gaps: A003-A008
and A032-A048 (pitch-bend editor; PitchBendPopup.qml unreferenced by production), A019-A020 (note context
menu keyboard navigation; PianoGrid.contextMenuRequested at PianoGrid.swift:676 is emitted but no
shell/editorqml test navigates the menu), and A021-A028 (prototype audio-session transport/playhead rows
with no executing predicate in the cited production lanes). No whole-case equivalence, native execution or
parity claim is made.

Status and evidence
This is an assertion-site inventory of the deleted prototype interaction smoke, not a count-based equivalence claim. Every require/awaitState site appears below, including the sgc_ curve-seam probes and prototype chrome guards. S identifiers identify exact Swift predicates in the named production counterparts, listed after the inventory. MATCHED means a corresponding Swift predicate carrying the original cppID was identified and executed; RETIRED-REPRESENTATION identifies an unreachable old implementation input with an explicit replacement-contract justification, not an omitted user behavior; PARTIAL means a related check exists but differs in a stated way; GAP means an equivalent Swift assertion is not established.
Execution boundary: SwiftGridPrototype is retired: its C++ sources were deleted in fe1ff4df9f4834cbd4e4e59d2e710078c10a8ced and no target/registration for this prototype remains. The band-selection predicates below execute through the production Swift grid lane (PianoGrid/DocumentSession), not the deleted prototype scene.

Execution and ownership
Registered run paths (swiftcore lane): rollcheck -> checkProjection
(EditorGridCameraChecks.swift:266: :423 band selection) and checkOrderedSelection (:65: :125 band order,
:148 never-mutate, :152 deletion-prunes).

Fixture, sequence and row constraints
No QTest data rows: the reset() closure restores the 30-note musical fixture before each phase; notes()/selectedCount() fold the noteSummary JSON array per site; colorCount() folds framebuffer regions (cyan dash qRgb(0,202,219), pitch-curve qRgb(205,84,84)) per site. awaitState polls each predicate up to 3s. The requireCanonical closure re-runs the two canonical-export assertions after every curve mutation (export, vertex-drag cancel, stroke cancel). The focusTarget loop (pitchBendGraph/pitchBendPopup) expands the Escape-dismissal sites across two focus targets. Guards: center() requires the native interaction target; item() lookups require named QML primitives.
Original setup, branch guards, and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory

A001 | awaitState | src/checks/swiftgridprototype/interaction_smoke.cpp:35
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:
#include "curve_session.h"
#include "grid_smoke.h"

#include <QCoreApplication>
#include <QImage>
#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QPointer>
#include <QQuickItem>
#include <QQuickWindow>
#include <QTest>

#include <algorithm>
#include <array>
#include <chrono>
#include <cstdio>
#include <cstdlib>

namespace {
using namespace std::chrono_literals;

void require(bool condition, const char *reason)
{
    if (!condition) {
        std::fprintf(stderr, "SWIFT_GRID_SMOKE FAIL: %s\n", reason);
        std::fflush(stderr);
        std::exit(EXIT_FAILURE);
    }
}

template <typename Predicate>
void awaitState(Predicate predicate, const char *reason)
{
Original expression:
    require(QTest::qWaitFor(predicate, 3s), reason);

A002 | center | src/checks/swiftgridprototype/interaction_smoke.cpp:73
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

}

void pass(const char *scenario)
{
    std::printf("SWIFT_GRID_SMOKE %s PASS\n", scenario);
    std::fflush(stdout);
}

QQuickItem *namedItem(QQuickItem *parent, const QString &name)
{
    if (parent->objectName() == name)
        return parent;
    for (QQuickItem *child : parent->childItems()) {
        if (auto *found = namedItem(child, name))
            return found;
    }
    return nullptr;
}

bool hasTruncatedText(QQuickItem *parent)
{
    if (parent->property("truncated").toBool())
        return true;
    for (QQuickItem *child : parent->childItems()) {
        if (hasTruncatedText(child))
            return true;
    }
    return false;
}

QRectF sceneRect(QQuickItem *item)
{
    return item->mapRectToScene(QRectF(0, 0, item->width(), item->height()));
}

QPoint center(QQuickItem *item)
{
Original expression:
    require(item, "missing native interaction target");

A003 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:117
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    return sceneRect(item).center().toPoint();
}

QJsonArray notes(QObject *model)
{
    return QJsonDocument::fromJson(model->property("noteSummary").toString().toUtf8()).array();
}

int selectedCount(QObject *model)
{
    int count = 0;
    for (const QJsonValue value : notes(model)) {
        if (value.toObject().value("selected").toBool())
            ++count;
    }
    return count;
}

int colorCount(const QImage &image, QRectF logical, QRgb color)
{
    const double dpr = image.devicePixelRatio();
    const QRect region = QRectF(logical.topLeft() * dpr, logical.size() * dpr)
                             .toAlignedRect()
                             .intersected(image.rect());
    int count = 0;
    for (int y = region.top(); y <= region.bottom(); ++y) {
        for (int x = region.left(); x <= region.right(); ++x) {
            if (image.pixelColor(x, y).rgb() == color)
                ++count;
        }
    }
    return count;
}

void verifyCurveExport()
{
    constexpr std::array<SGCurveValue, 6> input{
        {{0, 0}, {6, 0}, {13, 0}, {14, 4096}, {18, 4096}, {24, 0}}};
    constexpr std::array<SGCurveValue, 4> expected{{{0, 0}, {13, 0}, {14, 4096}, {24, 0}}};
    auto *session = sgc_create(0, 0, 24, input.data(), input.size(), 0, {0, 0, 481, 129, 1, 2});
    std::array<SGCurveValue, input.size()> output;
    auto requireCanonical = [&] {
        const size_t count = sgc_copy_curve_points(session, output.data());
Original expression:
        require(count == expected.size(),
                "canonical curve export kept redundant plateaus or removed fine samples");

A004 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:120
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

        for (size_t i = 0; i < count; ++i)
Original expression:
            require(output[i].tick == expected[i].tick && output[i].value == expected[i].value,
                    "canonical curve export changed an endpoint or fine interpolation anchor");

A005 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:128
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    };
    requireCanonical();
    pass("canonical-curve-export-preserves-fine-samples");
    sgc_press(session, sgc_x_at_tick(session, 13), sgc_y_at_value(session, 0), 0);
    sgc_move(session, sgc_x_at_tick(session, 16), sgc_y_at_value(session, 8191), 1);
    const size_t movedCount = sgc_copy_curve_points(session, output.data());
Original expression:
    require(std::any_of(
                output.begin(), output.begin() + movedCount,
                [](const SGCurveValue &point) { return point.tick == 16 && point.value == 8191; }),
            "vertex drag did not preview the moved controller event");

A006 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:137
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    sgc_cancel(session);
    requireCanonical();
    pass("cancelled-vertex-drag-restores-controller-curve");
    const SGCurveState beforeStroke = sgc_state(session);
    sgc_press(session, sgc_x_at_tick(session, 8), sgc_y_at_value(session, 0), 0);
Original expression:
    require(sgc_state(session).has_gesture && sgc_state(session).selected_tick == -1,
            "stroke press did not open a selection-free curve gesture");

A007 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:140
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    sgc_move(session, sgc_x_at_tick(session, 10), sgc_y_at_value(session, 8191), 0);
Original expression:
    require(sgc_state(session).live_value == 8191,
            "stroke move did not preview the live controller value");

A008 | verifyCurveExport | src/checks/swiftgridprototype/interaction_smoke.cpp:143
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    sgc_cancel(session);
Original expression:
    require(sgc_state(session).keyboard_tick == beforeStroke.keyboard_tick &&
                sgc_state(session).live_value == beforeStroke.live_value,
            "cancelled stroke did not restore the keyboard cursor and live value");

A009 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:159
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

    requireCanonical();
    sgc_destroy(session);
    pass("cancelled-stroke-restores-cursor-and-curve");
}
} // namespace

void verifyGridInteractions(QQuickWindow *window, QObject *model)
{
    verifyCurveExport();
    auto item = [window](const char *name) {
        return namedItem(window->contentItem(), QString::fromLatin1(name));
    };
    auto reset = [&] {
Original expression:
        require(QMetaObject::invokeMethod(window, "resetDemo"), "cannot reset the prototype");

A010 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:160
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

Original expression:
        awaitState([&] { return notes(model).size() == 30 && item("gridNote_1"); },
                   "reset did not restore the musical fixture");

A011 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:177
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Consumer-visible right-drag band selection (live counts, shrink deselection, back-drag, commit) is asserted by executing src/checks/rollcheck/EditorGridCameraChecks.swift::checkProjection:423 (right-drag band selects exactly the visible intersecting notes) and ::checkOrderedSelection:125 (band keeps press order). No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed. The exact retired prototype scene end-to-end observation is not reproduced.
Source context since previous assertion:

        QTest::qWait(50ms);
    };
    auto click = [&](const char *name) {
        QTest::mouseClick(window, Qt::LeftButton, Qt::NoModifier, center(item(name)));
        QTest::qWait(30ms);
    };
    reset();

    const QRectF band =
        sceneRect(item("gridNote_1")).united(sceneRect(item("gridNote_2"))).adjusted(-6, -6, 6, 6);
    const QPoint start = band.topLeft().toPoint();
    const QPoint end = band.bottomRight().toPoint();
    auto beginBand = [&] {
        QTest::mousePress(window, Qt::RightButton, Qt::NoModifier, start);
        QTest::mouseMove(window, end);
Original expression:
        awaitState([&] { return selectedCount(model) == 2; },
                   "right-drag did not select intersecting editable notes live");

A012 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:182
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

    };
    beginBand();
    QTest::qWait(30ms);
Original expression:
    require(colorCount(window->grabWindow(), band.adjusted(-2, -2, 2, 2), qRgb(0, 202, 219)) > 8,
            "right-drag reticle does not render the production cyan dash frame");

A013 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:185
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

    auto *grabber = window->mouseGrabberItem();
Original expression:
    require(grabber, "right-drag did not acquire the native mouse grab");

A014 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:188
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Consumer-visible right-drag band selection (live counts, shrink deselection, back-drag, commit) is asserted by executing src/checks/rollcheck/EditorGridCameraChecks.swift::checkProjection:423 (right-drag band selects exactly the visible intersecting notes) and ::checkOrderedSelection:125 (band keeps press order). No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed. The exact retired prototype scene end-to-end observation is not reproduced.
Source context since previous assertion:

    grabber->ungrabMouse();
    QTest::mouseRelease(window, Qt::RightButton, Qt::NoModifier, end);
Original expression:
    awaitState([&] { return selectedCount(model) == 0; },
               "cancelled reticle did not restore the previous selection");

A015 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:192
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Consumer-visible right-drag band selection (live counts, shrink deselection, back-drag, commit) is asserted by executing src/checks/rollcheck/EditorGridCameraChecks.swift::checkProjection:423 (right-drag band selects exactly the visible intersecting notes) and ::checkOrderedSelection:125 (band keeps press order). No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed. The exact retired prototype scene end-to-end observation is not reproduced.
Source context since previous assertion:

    beginBand();
    QTest::mouseRelease(window, Qt::RightButton, Qt::NoModifier, end);
Original expression:
    require(selectedCount(model) == 2, "released reticle discarded its selection");

A016 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:196
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

    pass("right-reticle-raster-live-selection-and-cancel");

    QTest::mouseClick(window, Qt::RightButton, Qt::NoModifier, center(item("gridNote_1")));
Original expression:
    awaitState([&] { return item("noteContextMenu")->property("opened").toBool(); },
               "right click did not open the note menu");

A017 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:198
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Consumer-visible multi-selection preservation across note-menu destruction is asserted by executing src/checks/rollcheck/EditorGridCameraChecks.swift::checkOrderedSelection:148 (selection gestures never mutate document or history) and :152 (deletion prunes membership and preserves survivor order). No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed. The popup-menu keyboard half of the observation is not reproduced.
Source context since previous assertion:

Original expression:
    require(selectedCount(model) == 2, "note menu destroyed an existing multi-selection");

A018 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:200
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION. Retired-harness mechanism: awaitState/center helper, prototype demo-reset, or reticle frame-raster / native mouse-grabber observation of the deleted scene. No-ingress: interaction_smoke.cpp is uncompiled in every CMake target and unregistered in src/checks/checkcatalog.cpp (verified zero references in src/checks/CMakeLists.txt, src/checks/checkcatalog.cpp and src/checks/fwd.hpp); src/ui/songview/quick/swiftgrid/ is deleted from disk; SwiftGrid*/Sgc* are absent from src/swift/; the Swift suite never touches the retired prototype interaction scene. No live native run exists or is claimed.
Source context since previous assertion:

    QTest::qWait(30ms);
Original expression:
    require(!hasTruncatedText(item("noteContextMenu")),
            "note menu clips an action or shortcut label");

A019 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:205
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (note context menu keyboard navigation). No executing shell/editorqml predicate drives popup Up/Down/Enter/Escape traversal; PianoGrid.contextMenuRequested exists at src/swift/app/roll/PianoGrid.swift:676 (emitted at :581) but no test navigates the menu.
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_Down);
    QTest::keyClick(window, Qt::Key_Down);
    QTest::keyClick(window, Qt::Key_Return);
Original expression:
    awaitState([&] { return notes(model).size() == 28; }, "menu Delete did not delete selection");

A020 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:208
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (note context menu keyboard navigation). No executing shell/editorqml predicate drives popup Up/Down/Enter/Escape traversal; PianoGrid.contextMenuRequested exists at src/swift/app/roll/PianoGrid.swift:676 (emitted at :581) but no test navigates the menu.
Source context since previous assertion:

    for (const QJsonValue value : notes(model)) {
        const int id = value.toObject().value("id").toInt();
Original expression:
        require(id != 1 && id != 2, "menu Delete removed the wrong notes");

A021 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:215
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    }
    pass("note-menu-keyboard-delete-preserves-target-selection");

    reset();
    click("transportPlayPause");
    auto *audio = window->property("audio").value<QObject *>();
Original expression:
    require(audio, "missing audio session");

A022 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:216
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

Original expression:
    awaitState(
        [&] {
            return audio->property("playing").toBool() &&
                   audio->property("playheadTick").toDouble() > 0;
        },
        "Play did not advance the real poryaaaa transport");

A023 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:222
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

Original expression:
    require(item("gridPlayhead")->isVisible() && item("rulerPlayheadTriangle")->isVisible(),
            "playing transport has no grid and ruler playheads");

A024 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:225
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    click("transportPlayPause");
Original expression:
    require(!audio->property("playing").toBool(), "Pause did not pause playback");

A025 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:228
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    const double paused = audio->property("playheadTick").toDouble();
    QTest::qWait(100ms);
Original expression:
    require(audio->property("playheadTick").toDouble() == paused, "paused playhead kept moving");

A026 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:230
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    click("transportStop");
Original expression:
    require(audio->property("playheadTick").toDouble() == 0, "Stop did not return to song start");

A027 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:233
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    pass("real-audio-play-pause-stop-and-playhead");
    QTest::keyClick(window, Qt::Key_Space);
Original expression:
    awaitState([&] { return audio->property("playing").toBool(); },
               "focused Stop button stole Space from the window transport command");

A028 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:236
Disposition: GAP
Mapping/reason: GAP. Honest gap: the retired prototype audio-session transport/playhead rows (playing flag, playheadTick pause/stop, playhead visibility) have no executing predicate in the cited production lanes (camera/selection/viewport Swift checks and shell grid-input tests observe no transport state).
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_Return);
Original expression:
    require(!audio->property("playing").toBool() && audio->property("playheadTick").toDouble() == 0,
            "Enter did not activate the focused Stop button");

A029 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:242
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    pass("transport-space-priority-and-local-enter");

    click("gridNote_1");
    QTest::keyClick(window, Qt::Key_G);
Original expression:
    awaitState([&] { return item("pitchBendGraph") && item("pitchBendGraph")->isVisible(); },
               "G did not open a functional pitch popup");

A030 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:245
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    auto *popup = item("pitchBendPopup");
Original expression:
    require(popup && window->contentItem()->boundingRect().contains(sceneRect(popup)),
            "pitch popup is not clamped inside the application window");

A031 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:249
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    auto *graph = item("pitchBendGraph");
    const QRectF plot = graph->property("canvasRect").toRectF();
Original expression:
    require(plot.width() > 0 && plot.height() > 0, "pitch graph has no native canvas geometry");

A032 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:263
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    const QPoint low = graph
                           ->mapToScene(QPointF(plot.x() + (plot.width() - 1) * 2 / 18,
                                                plot.y() + (plot.height() - 1) * 0.8))
                           .toPoint();
    const QPoint high = graph
                            ->mapToScene(QPointF(plot.x() + (plot.width() - 1) * 16 / 18,
                                                 plot.y() + (plot.height() - 1) * 0.2))
                            .toPoint();
    QTest::mousePress(window, Qt::LeftButton, Qt::ShiftModifier, low);
    QTest::mouseMove(window, high);
    QTest::mouseRelease(window, Qt::LeftButton, Qt::ShiftModifier, high);
    QTest::qWait(40ms);
    const QRectF curveRegion = graph->mapRectToScene(plot).adjusted(6, 6, -6, -6);
Original expression:
    require(colorCount(window->grabWindow(), curveRegion, qRgb(205, 84, 84)) > 30,
            "pitch gesture did not render the production track-colored curve");

A033 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:267
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    const QImage drawn = window->grabWindow();
    QTest::keyClick(window, Qt::Key_Escape);
Original expression:
    awaitState([&] { return !window->property("pitchBridge").value<QObject *>(); },
               "Escape did not dismiss the pitch popup");

A034 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:270
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_G);
Original expression:
    awaitState([&] { return item("pitchBendGraph") && item("pitchBendGraph")->isVisible(); },
               "pitch popup did not reopen");

A035 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:277
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::qWait(40ms);
    const QImage reopened = window->grabWindow();
    const QRectF upperCurve(curveRegion.x(), curveRegion.y(), curveRegion.width(),
                            curveRegion.height() / 3);
    const int upperPixels = colorCount(drawn, upperCurve, qRgb(205, 84, 84));
Original expression:
    require(upperPixels > 5 && colorCount(reopened, upperCurve, qRgb(205, 84, 84)) > 5,
            "edited pitch curve did not survive popup close and reopen");

A036 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:285
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    pass("native-pitch-curve-raster-and-committed-reopen");

    QTest::mouseDClick(window, Qt::LeftButton, Qt::NoModifier, center(item("bendRangeInput")));
    QTest::qWait(30ms);
    QTest::keyClick(window, Qt::Key_7);
    QTest::keyClick(window, Qt::Key_Return);
Original expression:
    awaitState([&] { return item("bendRangeInput")->property("text").toString() == "7"; },
               "pitch bend range numeric editor did not accept text input");

A037 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:289
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    click("pitchBendReset");
    QTest::qWait(40ms);
Original expression:
    require(colorCount(window->grabWindow(), upperCurve, qRgb(205, 84, 84)) == 0,
            "Reset did not restore the zero pitch curve");

A038 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:292
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_Escape);
Original expression:
    awaitState([&] { return !window->property("pitchBridge").value<QObject *>(); },
               "pitch popup did not close after numeric editing");

A039 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:295
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_G);
Original expression:
    awaitState(
        [&] {
            return item("bendRangeInput") &&
                   item("bendRangeInput")->property("text").toString() == "7";
        },
        "pitch bend range did not survive popup reopen");

A040 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:307
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    pass("pitch-popup-numeric-range-reset-and-persistence");

    auto previewCurve = [&] {
        QTest::mousePress(window, Qt::LeftButton, Qt::ShiftModifier, low);
        QTest::mouseMove(window, high);
        QTest::qWait(40ms);
Original expression:
        require(colorCount(window->grabWindow(), upperCurve, qRgb(205, 84, 84)) > 5,
                "unreleased pitch gesture did not produce a live preview");

A041 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:312
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    };
    previewCurve();
    auto *pitchGrabber = window->mouseGrabberItem();
Original expression:
    require(pitchGrabber, "pitch gesture did not acquire the native mouse grab");

A042 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:316
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    pitchGrabber->ungrabMouse();
    QTest::mouseRelease(window, Qt::LeftButton, Qt::ShiftModifier, high);
    QTest::qWait(40ms);
Original expression:
    require(colorCount(window->grabWindow(), upperCurve, qRgb(205, 84, 84)) == 0,
            "aborted pitch gesture did not restore the committed curve");

A043 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:322
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    for (const char *focusTarget : std::array{"pitchBendGraph", "pitchBendPopup"}) {
        previewCurve();
        item(focusTarget)->forceActiveFocus();
        QTest::keyClick(window, Qt::Key_Escape);
Original expression:
        awaitState([&] { return !window->property("pitchBridge").value<QObject *>(); },
                   "Escape did not dismiss an active pitch gesture");

A044 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:326
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

        QTest::mouseRelease(window, Qt::LeftButton, Qt::ShiftModifier, high);
        QTest::keyClick(window, Qt::Key_G);
Original expression:
        awaitState([&] { return item("pitchBendGraph") && item("pitchBendGraph")->isVisible(); },
                   "pitch popup did not reopen after cancelling a live gesture");

A045 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:329
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

        QTest::qWait(40ms);
Original expression:
        require(colorCount(window->grabWindow(), upperCurve, qRgb(205, 84, 84)) == 0 &&
                    item("bendRangeInput")->property("text").toString() == "7",
                "Escape persisted a live preview or discarded a previously committed numeric edit");

A046 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:354
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    }
    pass("pitch-abort-and-escape-discard-live-preview");
    graph = item("modWheelGraph");
    const QRectF modPlot = graph->property("canvasRect").toRectF();
    const QPoint modLow = graph
                              ->mapToScene(QPointF(modPlot.x() + (modPlot.width() - 1) * 2 / 18,
                                                   modPlot.y() + (modPlot.height() - 1) * 0.8))
                              .toPoint();
    const QPoint modHigh = graph
                               ->mapToScene(QPointF(modPlot.x() + (modPlot.width() - 1) * 16 / 18,
                                                    modPlot.y() + (modPlot.height() - 1) * 0.2))
                               .toPoint();
    QTest::mousePress(window, Qt::LeftButton, Qt::AltModifier, modLow);
    QTest::mouseMove(window, modHigh);
    QTest::mouseRelease(window, Qt::LeftButton, Qt::AltModifier, modHigh);
    QTest::mouseDClick(window, Qt::LeftButton, Qt::NoModifier, center(item("lfoSpeedInput")));
    QTest::qWait(30ms);
    QTest::keyClick(window, Qt::Key_5);
    QTest::keyClick(window, Qt::Key_Return);
    const QRectF modUpper =
        graph->mapRectToScene(modPlot).adjusted(6, 6, -6, -modPlot.height() * 2 / 3);
    QTest::mouseClick(window, Qt::LeftButton, Qt::NoModifier, QPoint(window->width() - 10, 100));
Original expression:
    awaitState([&] { return !window->property("pitchBridge").value<QObject *>(); },
               "outside click did not dismiss the pitch popup");

A047 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:357
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::keyClick(window, Qt::Key_G);
Original expression:
    awaitState(
        [&] {
            return item("lfoSpeedInput") &&
                   item("lfoSpeedInput")->property("text").toString() == "5";
        },
        "LFO speed did not survive popup reopen");

A048 | verifyGridInteractions | src/checks/swiftgridprototype/interaction_smoke.cpp:365
Disposition: GAP
Mapping/reason: GAP. Honest gap: feature not yet ported (pitch-bend editor; PitchBendPopup.qml unreferenced by production). The retired sgc_ curve seam, pitch/modulation popup graph editing, raster persistence, and Escape-dismissal rows have no executing Swift presenter predicate (C++ coverage lives under src/checks/pitchbend/, outside this Swift pairing).
Source context since previous assertion:

    QTest::qWait(40ms);
    const QImage modReopened = window->grabWindow();
Original expression:
    require(colorCount(modReopened, modUpper, qRgb(205, 84, 84)) > 5 &&
                colorCount(modReopened, upperCurve, qRgb(205, 84, 84)) == 0,
            "modulation editing did not persist independently of pitch editing");

Remaining original source (closing sequence and helper bodies):

    pass("modulation-lfo-and-outside-dismiss-persistence");
    QTest::keyClick(window, Qt::Key_Escape);
}

Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:16
report.expect(near(compact.snapshot.viewportWidth, 50) &&
                  near(compact.snapshot.minHScroll, -48) &&
                  near(compact.snapshot.maxHScroll, 0),
                  cppID: cameraTransformID,
                  message: "caller-resolved minimum viewport produces observable camera bounds")

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:26
report.expect(near(camera.tickAtContentX(content), 96), cppID: cameraTransformID,
                  message: "tick/content transforms round-trip with a fractional offset")

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:28
report.expect(near(camera.snapshot.scrollX, 10.25), cppID: cameraTransformID,
                  message: "horizontal restoration preserves fractional offsets")

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:30
report.expect(near(camera.displayX(tick: 24, origin: 0.2, dpr: 2), 22),
                  cppID: cameraTransformID, message: "display projection snaps to a physical pixel")

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:33
report.expect(near(camera.snapshot.scrollX, -48) && near(camera.snapshot.minHScroll, -48),
                  cppID: cameraTransformID, message: "horizontal scrolling clamps to production pre-roll")

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:36
report.expect(near(camera.snapshot.scrollX, 640), cppID: cameraTransformID,
                  message: "bound timeline end reaches the plot origin")

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:43
report.expect(near(viewportCamera.snapshot.scrollX, -48),
                  cppID: cameraTransformID,
                  message: "bound viewport shrink reclamps the changed pre-roll floor")

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:48
report.expect(near(viewportCamera.snapshot.scrollX, 10.25),
                  cppID: cameraTransformID,
                  message: "bound viewport updates preserve a legal fractional offset")

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:56
report.expect(zoom.zoomChanged && zoom.scrollChanged &&
                  near(camera.tickAtContentX(anchorX), anchoredTick), cppID: cameraZoomID,
                  message: "interactive time zoom preserves its tick anchor")

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:65
report.expect(near(clampedTimeZoom.snapshot.scrollX, 64) &&
                  !near(clampedTimeZoom.tickAtContentX(boundedAnchor), tickBeforeBoundedZoom),
                  cppID: cameraZoomID,
                  message: "time zoom reaches its bound when the pointer anchor is unreachable")

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:70
report.expect(near(camera.snapshot.pixelsPerBeat, 48) &&
                  near(camera.snapshot.keyHeight, 16) && near(camera.snapshot.scrollX, 10.5) &&
                  near(camera.snapshot.scrollY, 20.25), cppID: cameraZoomID,
                  message: "unanchored restoration keeps valid fractional camera state")

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:78
report.expect(near(sanitized.snapshot.pixelsPerBeat, 32) &&
                  near(sanitized.snapshot.keyHeight, 12) &&
                  near(sanitized.snapshot.scrollX, 0) && near(sanitized.snapshot.scrollY, 0),
                  cppID: cameraZoomID,
                  message: "restoration sanitizes non-finite transient view-state values independently")

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:86
report.expect(near(camera.snapshot.scrollX, 20), cppID: cameraTransformID,
                  message: "content shrink reclamps the horizontal offset")

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:89
report.expect(near(camera.snapshot.viewportWidth, 800) && near(camera.snapshot.scrollY, 0),
                  cppID: cameraTransformID,
                  message: "viewport growth reclamps vertical content")

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:93
report.expect(near(camera.snapshot.scrollX, -80), cppID: cameraTransformID,
                  message: "an unbound timeline homes to its lead pad")

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:96
report.expect(near(camera.snapshot.scrollX, -100), cppID: cameraTransformID,
                  message: "unbound viewport updates follow the resolved home")

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:100
report.expect(chromatic.visibleRowCount == 128 && (0..<128).allSatisfy { pitch in
        chromatic.row(forPitch: pitch) == 127 - pitch &&
            chromatic.visiblePitch(at: 127 - pitch) == pitch
    }, cppID: cameraPitchID,
    message: "chromatic pitch mapping remains bijective across the full MIDI range")

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:108
report.expect(folded.visiblePitch(at: 0) == 67 && folded.row(forPitch: 60) == 2 &&
                  folded.row(forPitch: 61) == PitchProjection.hiddenRow &&
                  folded.nearestVisiblePitch(to: 62) == 60 &&
                  folded.nearestVisiblePitch(to: 100) == 67 &&
                  folded.nearestVisiblePitch(to: -5) == 60 &&
                  emptyProjection.nearestVisiblePitch(to: 60) == nil,
                  cppID: cameraPitchID,
                  message: "pitch rows and nearest lookup preserve production ordering")

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:116
report.expect(folded.row(atY: 10, keyHeight: 10, scrollY: 0, dpr: 2) == 1 &&
                  folded.pitch(atY: 29.75, keyHeight: 10, scrollY: 0, dpr: 2) == 60,
                  cppID: cameraPitchID, message: "DPR-snapped row and pitch inverses agree")

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:119
report.expect(near(folded.rowBottom(0, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) ?? -1,
                       10) &&
                  folded.row(atY: 9.99, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 0 &&
                  folded.row(atY: 10, keyHeight: 10.2, scrollY: 0.1, dpr: 1.5) == 1,
                  cppID: cameraPitchID,
                  message: "fractional DPR snapping defines the inverse row boundary")

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:131
report.expect(near(projectedContent.snapshot.scrollY, 15),
                  cppID: cameraPitchID,
                  message: "pitch projection shrink reclamps the vertical offset")

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:139
report.expect(near(pitchCamera.snapshot.maxVScroll, 15), cppID: cameraPitchID,
                  message: "vertical bounds derive from projected row height")

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:146
report.expect(near(anchoredRow, resultingRow), cppID: cameraZoomID,
                  message: "pitch zoom preserves its row anchor when bounds permit")

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:159
report.expect(near(clampedPitchZoom.snapshot.scrollY, 0) &&
                  near(clampedPitchZoom.snapshot.maxVScroll, 0) &&
                  !near(rowAfterBoundedZoom, rowBeforeBoundedZoom),
                  cppID: cameraZoomID,
                  message: "pitch zoom reaches its bound when the row anchor is unreachable")

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:166
report.expect(pitchCamera.ensureKeyVisible(60) && near(pitchCamera.snapshot.scrollY, 15),
                  cppID: cameraPitchID, message: "key reveal scrolls only enough to show its row")

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:168
report.expect(!pitchCamera.ensureKeyVisible(61), cppID: cameraPitchID,
                  message: "hidden pitches do not move the camera")

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:173
report.expect(reveal.ensureTickVisible(480, dpr: 2) &&
                  near(reveal.snapshot.scrollX, 573.3333333333),
                  cppID: cameraTransformID,
                  message: "tick reveal uses the production one-third anchor")

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:178
report.expect(!reveal.ensureRangeVisible(startTick: 0, endTick: 149,
                                             preferEnd: false, dpr: 1) &&
                  near(reveal.snapshot.scrollX, 0), cppID: cameraTransformID,
                  message: "a fitting range on the physical right boundary stays visible")

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:182
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: false, dpr: 1) &&
                  near(reveal.contentX(tick: 240), 0), cppID: cameraTransformID,
                  message: "wide range reveal prefers its requested leading edge")

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift:187
report.expect(reveal.ensureRangeVisible(startTick: 240, endTick: 480,
                                            preferEnd: true, dpr: 1) &&
                  near(reveal.contentX(tick: 480), 199), cppID: cameraTransformID,
                  message: "wide range reveal honors the requested trailing edge")

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:220
report.expect(
            after.pixelsPerBeat > before.pixelsPerBeat
                && gridCameraNear(session.camera.tickAtContentX(anchorX), anchorTick, tolerance: 1e-7)
                && counters.camera == publications + 1,
            cppID: gridCameraWheelID, message: "phase \(phase.rawValue) performs one anchored time zoom publication")

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:233
report.expect(
        session.camera.snapshot == beforeMomentum && counters.camera == momentumPublications,
        cppID: gridCameraWheelID, message: "momentum suppresses time zoom without publishing")

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:248
report.expect(
        gridCameraNear(angleZoom, 35 * pow(1.0015, 120), tolerance: 1e-7)
            && gridCameraNear(session.camera.snapshot.pixelsPerBeat,
                    angleZoom * pow(1.0015, 120), tolerance: 1e-7),
        cppID: gridCameraWheelID, message: "pixel deltas receive five-times angle weighting")

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:253
report.expect(
        counters.camera == 2,
        cppID: gridCameraWheelID, message: "effective angle and pixel zooms each publish once")

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:263
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 90) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Shift wheel pans by negative selected delta exactly once")

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:278
report.expect(
        gridCameraNear(minimumPan.scrollX, minimumPan.minHScroll)
            && minimumPublications == 1 && counters.camera == minimumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at minimum and repeated clamped pan is silent")

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:295
report.expect(
        gridCameraNear(maximumPan.scrollX, maximumPan.maxHScroll)
            && maximumPublications == 1 && counters.camera == maximumPublications,
        cppID: gridCameraWheelID, message: "Shift pan clamps at maximum and repeated clamped pan is silent")

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:306
report.expect(
        gridCameraNear(session.camera.snapshot.scrollX, 112) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "horizontal-only momentum still pans exactly once")

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:318
report.expect(
        pitchAfter.keyHeight > pitchBefore.keyHeight
            && gridCameraNear((pitchAfter.scrollY + 100) / pitchAfter.keyHeight, pitchRow, tolerance: 1e-7)
            && counters.camera == 1,
        cppID: gridCameraWheelID, message: "Ctrl wheel performs one row-anchored pitch zoom publication")

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:329
report.expect(
        session.camera.snapshot == pitchMomentum && counters.camera == 0,
        cppID: gridCameraWheelID, message: "momentum suppresses pitch zoom without publishing")

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift:339
report.expect(
        gridCameraNear(session.camera.snapshot.scrollY, gutterBefore + 6) && counters.camera == 1,
        cppID: gridCameraWheelID, message: "gutter wheel scrolls by negative half-delta exactly once")

S042 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:111
report.expect(session.selectedNoteOrder == [b, a]
        && session.document.note(session.selectedNoteOrder[0])?.velocity == 90,
        cppID: id, message: "Ctrl-add of earlier A(v40) retains later B(v90) as first selection")

S043 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:116
report.expectEqual([a, b], session.selectedNoteOrder, cppID: id,
                       what: "deselecting and re-adding appends the note")

S044 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:119
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "replacement preserves first occurrence and removes duplicates")

S045 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:123
report.expect(Array(session.selectedNoteOrder.prefix(2)) == [b, a]
        && session.selectedNotes.contains(c),
        cppID: id, message: "band keeps press order before newly covered notes")

S046 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:127
report.expectEqual([b, a], session.selectedNoteOrder, cppID: id,
                       what: "band cancellation restores selection order")

S047 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:131
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement publishes the selection domain")

S048 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:138
report.expectEqual([SessionChangeDomains.selection], publications, cppID: id,
                       what: "order-only replacement and additive selection coalesce")

S049 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:143
report.expect(publications.isEmpty, cppID: id,
                  message: "unchanged normalized selection publishes nothing")

S050 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:145
report.expect(session.document.revision == revision
        && session.document.history.currentIdentity == history
        && session.document.isDirty == dirty,
        cppID: id, message: "selection gestures never mutate document or history")

S051 | checkOrderedSelection | src/checks/rollcheck/EditorGridCameraChecks.swift:151
report.expect(session.selectedNoteOrder == [c, a] && session.selectedNotes == Set([c, a]),
                  cppID: id, message: "deletion prunes membership and preserves survivor order")

S052 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:165
report.expect(
        gridCameraNear(first.viewportWidth, 640) && gridCameraNear(first.rollHeight, 320),
        cppID: viewportID, message: "viewport dimensions are pushed into the session camera")

S053 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:168
report.expect(
        gridCameraNear(first.minHScroll, expectedMin)
            && gridCameraNear(first.maxHScroll,
                    Double(session.timeline.lengthTicks) * first.pixelsPerTick)
            && gridCameraNear(first.maxVScroll, expectedMaxV),
        cppID: viewportID, message: "camera bounds derive from viewport, document extent, and projection")

S054 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:174
report.expect(
        first.scrollY >= 0 && first.scrollY <= first.maxVScroll,
        cppID: viewportID, message: "first viewport push homes vertically within camera bounds")

S055 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:177
report.expect(
        gridCameraNear(grid.beatWidth, first.pixelsPerBeat) && gridCameraNear(grid.rowHeight, first.keyHeight),
        cppID: viewportID, message: "published scales equal the session camera snapshot")

S056 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:188
report.expect(
        session.camera.snapshot == fractional && counters.camera == 0,
        cppID: viewportID, message: "identical viewport push preserves fractional offsets and publishes nothing")

S057 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:196
report.expect(
        tallViewport.maxVScroll < shortHeight.maxVScroll
            && gridCameraNear(tallViewport.scrollY, tallViewport.maxVScroll),
        cppID: viewportID, message: "roll-height increase reclamps vertical scroll")

S058 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:211
report.expect(
        gridCameraNear(doubled.pixelsPerBeat, 69) && gridCameraNear(doubled.keyHeight, 26),
        cppID: viewportID, message: "double-font push applies the rounded font-relative defaults")

S059 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:214
report.expect(
        gridCameraNear(restored.pixelsPerBeat, 35) && gridCameraNear(restored.keyHeight, 13),
        cppID: viewportID, message: "font push back restores the exact earlier scale")

S060 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:217
report.expect(
        counters.camera == 2,
        cppID: viewportID, message: "each effective font push publishes exactly once")

S061 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:232
report.expect(
        gridCameraNear(fontMinimum.pixelsPerBeat, 9) && gridCameraNear(fontMinimum.keyHeight, 9),
        cppID: viewportID, message: "pushed font defines time and key-height minimums")

S062 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:235
report.expect(
        gridCameraNear(fontMaximum.pixelsPerBeat, 1_387) && gridCameraNear(fontMaximum.keyHeight, 69),
        cppID: viewportID, message: "pushed font defines time and key-height maximums")

S063 | checkViewport | src/checks/rollcheck/EditorGridCameraChecks.swift:255
report.expect(
        farID != nil && expandedLength > originalLength
            && session.timeline.lengthTicks < expandedLength
            && gridCameraNear(shrunk.scrollX, shrunk.maxHScroll),
        cppID: viewportID, message: "document shrink privately reconciles extent and reclamps horizontal scroll")

S064 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:277
report.expect(
        !markXs.isEmpty && markXs.allSatisfy { $0 >= -cullingWidth - 2 && $0 <= 2 * cullingWidth + 2 },
        cppID: projectionID, message: "generated time marks stay inside the one-viewport culling window")

S065 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:280
report.expect(
        (markXs.min() ?? 1) <= 0 && (markXs.max() ?? -1) >= cullingWidth,
        cppID: projectionID, message: "generated time marks cover the visible plot")

S066 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:286
report.expect(
        grid.noteSummary == summaryBeforeCameraMove,
        cppID: projectionID, message: "camera movement does not alter noteSummary document state")

S067 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:314
report.expect(
        projected,
        cppID: projectionID, message: "known note rectangle equals the camera projection")

S068 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:322
report.expect(
        maskCount == (zeroX > 0 ? 1 : 0),
        cppID: projectionID, message: "pre-roll mask presence follows the projected tick-zero position")

S069 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:334
report.expect(
        session.selectedNotes.contains(originalNote.id),
        cppID: projectionID, message: "beginPointer selects the note under the projected rectangle")

S070 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:352
report.expect(
        previewRect.map {
            gridCameraNear($0.x, session.camera.displayX(
                tick: Double(expectedTick), origin: 0, dpr: grid.devicePixelRatio))
                && gridCameraNear($0.y, expectedPreviewY + 1 / grid.devicePixelRatio)
        } ?? false,
        cppID: projectionID, message: "one-cell drag publishes the snapped tick and pitch preview")

S071 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:363
report.expect(
        session.document.revision == originalRevision + 1
            && moved.map { Int($0.tick) == expectedTick && Int($0.pitch) == expectedPitch } == true,
        cppID: projectionID, message: "endPointer commits one revision at the snapped tick and pitch")

S072 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:407
report.expect(
        session.document.revision == drawRevision + 1
            && created.map { Int($0.tick) == drawTick && Int($0.pitch) == drawPitch } == true,
        cppID: projectionID, message: "draw drag on an empty row adds one snapped note")

S073 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:421
report.expect(
        session.selectedNotes == visibleIDs,
        cppID: projectionID, message: "right-drag band selects exactly the visible intersecting notes")

S074 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:430
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "inputCancelled discards an active gesture without document mutation")

S075 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:437
report.expect(
        session.document.revision == cancelRevision
            && session.document.notes(in: grid.trackIndex).count == cancelCount,
        cppID: projectionID, message: "Escape discards an active gesture without document mutation")

S076 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:445
report.expect(
        session.document.revision == cancelRevision,
        cppID: projectionID, message: "press outside projected pitch rows is rejected")

S077 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:480
report.expect(
        counters.camera == before + 1
            && gridCameraNear(tickReveal.scrollX, expectedTickReveal),
        cppID: isolationID, message: "ensureTickVisible uses the reveal fraction in one publication")

S078 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:493
report.expect(
        counters.camera == before + 1
            && gridCameraNear(
                endReveal.scrollX,
                endReveal.maxHScroll - endReveal.viewportWidth
                    + 1 / grid.devicePixelRatio),
        cppID: isolationID, message: "preferEnd true aligns an oversized range to the right edge")

S079 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:509
report.expect(
        counters.camera == before + 1 && gridCameraNear(startReveal.scrollX, 0),
        cppID: isolationID, message: "preferEnd false aligns an oversized range to its start")

S080 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:517
report.expect(
        counters.camera == before + 1
            && gridCameraNear(keyReveal.scrollY, keyReveal.maxVScroll),
        cppID: isolationID, message: "ensureKeyVisible aligns the bottom pitch at the viewport edge")

S081 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:521
report.expect(
        gridCameraNear(grid.cameraScrollX, session.camera.snapshot.scrollX)
            && gridCameraNear(grid.cameraScrollY, session.camera.snapshot.scrollY),
        cppID: isolationID, message: "presenter scroll values track the revealed session camera")

S082 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:541
report.expect(
        counters.camera == before,
        cppID: isolationID, message: "redundant, non-finite, hidden, and already-visible navigation is silent")

S083 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:547
report.expect(
        session.camera.snapshot == published && grid.editCursorTick == Int(cursor),
        cppID: isolationID, message: "direct edit-cursor change neither moves camera nor republishes presenter cursor")

S084 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:550
report.expect(
        counters.document == 0 && counters.playback == 0 && counters.cursor == 1
            && session.document.revision == revision && session.document.isDirty == dirty
            && session.document.history.canUndo == canUndo
            && session.document.history.canRedo == canRedo,
        cppID: isolationID, message: "camera navigation is isolated from document, playback, dirty state, and history")

S085 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:568
report.expect(
        inserted?.count == 1 && counters.camera == 0,
        cppID: isolationID, message: "committed document edit performs no standalone camera publication")

S086 | checkIsolation | src/checks/rollcheck/EditorGridCameraChecks.swift:571
report.expect(
        counters.playback == 1 && counters.document == 1 && counters.coherentDocumentCallback,
        cppID: isolationID, message: "one edit publishes one coherent playback and document refresh after reconciliation")

Shared helper:
No shared Swift fixture file carrying report.expect*/expectEqual calls was identified for this pairing: camera.swift is self-contained (Foundation+PorydawApp) and EditorGridCameraChecks.swift builds its DocumentSession inline (fixture-guard report.fail calls at EditorGridCameraChecks.swift:78,88,322,393,533 are guard aborts, not predicates). The expectEqual helper itself lives in src/checks/support/corecheck/CoreCheckSupport.swift (SHA-256: d4419a4b2e15d1291d435c2e410d3b2f8357a875f9cb27751e01ef1d01beaf65) as a definition, not a call site:
    func expectEqual<T: Equatable>(_ expected: T, _ actual: T, cppID: String,
                                    what: String) {
        if expected == actual {
            pass(cppID, row: what)
        } else {
            fail(cppID, "\(what): expected=\(expected) actual=\(actual)")
        }
    }
