Assertion correspondence: timelinepan/tst_timelinepannative.cpp

Reference revision: 91cab247f7600b41c92b14114ad0352e3a002df0
Original: src/checks/timelinepan/tst_timelinepannative.cpp
Original SHA-256: c5bcff6546892498a16575dbcbd4afbc38f1d944e7aad2d9d5815eda9608b61f
Swift counterpart: none identified
Swift SHA-256: n/a

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
No Swift counterpart file was identified in this checkout (no .swift under
src/checks/timelinepan/ or covering this suite elsewhere), so there are no
Swift predicates to cite. This file is a native performance/diagnostic harness:
every site guards native fixture loading, window readiness, frame capture, or
pan-measurement execution, so every site below is NATIVE.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of any converted window.
GAP identifies an original predicate without a replacement assertion.

Verification
Command: deno task verify --filter timelinepan --verbose
Result: pending integrated run; source hashes pin the tree inspected here.
No converted-window evidence is available for this suite in this checkout.

Execution ownership
No Swift entry point executes these checks. Native ownership:
runTimelinePanNativeCheck drives TimelinePanNativeTest with slots init(),
cleanup(), and selectionExtentPanPerformance(), on top of the
checks::timelinepan::TimelinePanFixture declared in
src/checks/timelinepan/timelinepanfixture.h and implemented in
src/checks/timelinepan/timelinepanfixture.cpp (no assertion sites): load(),
show(), isReady(), capture(), render(), measureSelectedPan(), cleanup().
The shared C++ fixture files carry no assertion sites, so no separate fixture
proof is written for them.

Fixture, sequence and row constraints
No data rows or loops expand these sites. initializeFixture() runs before each
test function via init(); selectionExtentPanPerformance() tears the fixture
down with cleanup() and re-initializes between the short (4) and long (65536)
pan extents so the compared extents share no selection or renderer state.
QTRY_VERIFY(isReady) polls until the native window is ready; it is still one
site. The short/long millisecond samples are opt-in diagnostics reported via
qInfo, not correctness gates: no assertion compares the two timings. Original
setup, branch guards and helper implementations at the reference revision
remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | TimelinePanNativeTest::initializeFixture | src/checks/timelinepan/tst_timelinepannative.cpp:39
Disposition: NATIVE
Mapping/reason: Original native harness obligation: guards native fixture project loading, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
  #include "checks/timelinepan/timelinepanfixture.h"

  #include <QImage>
  #include <QStringList>
  #include <QtTest>

  namespace {

  class TimelinePanNativeTest final : public QObject
  {
      Q_OBJECT
      Q_DISABLE_COPY_MOVE(TimelinePanNativeTest)

    public:
      TimelinePanNativeTest(const QString &projectRoot, const QString &songLabel)
          : m_fixture(projectRoot, songLabel)
      {}

      ~TimelinePanNativeTest() override = default;

    private slots:
      void init();
      void cleanup();

      void selectionExtentPanPerformance();

    private:
      void initializeFixture();

      checks::timelinepan::TimelinePanFixture m_fixture;
      bool m_initialized = false;
  };

  void TimelinePanNativeTest::initializeFixture()
  {
      m_initialized = false;

      QString error;
Original expression:
  QVERIFY2(m_fixture.load(error), qPrintable(error));

A002 | TimelinePanNativeTest::initializeFixture | src/checks/timelinepan/tst_timelinepannative.cpp:41
Disposition: NATIVE
Mapping/reason: Original native fixture/window obligation: polls native window readiness, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
      m_fixture.show();
Original expression:
  QTRY_VERIFY(m_fixture.isReady(true));

A003 | TimelinePanNativeTest::initializeFixture | src/checks/timelinepan/tst_timelinepannative.cpp:44
Disposition: NATIVE
Mapping/reason: Original native rendering obligation: guards native frame capture, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
      const QImage captured = m_fixture.capture(&error);
Original expression:
  QVERIFY2(!captured.isNull(), qPrintable(error));

A004 | TimelinePanNativeTest::initializeFixture | src/checks/timelinepan/tst_timelinepannative.cpp:45
Disposition: NATIVE
Mapping/reason: Original native rendering obligation: guards the native render output, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!m_fixture.render().isNull());

A005 | TimelinePanNativeTest::initializeFixture | src/checks/timelinepan/tst_timelinepannative.cpp:46
Disposition: NATIVE
Mapping/reason: Original native rendering obligation: guards the native render output, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!m_fixture.render().isNull());

A006 | TimelinePanNativeTest::selectionExtentPanPerformance | src/checks/timelinepan/tst_timelinepannative.cpp:64
Disposition: NATIVE
Mapping/reason: Original native harness obligation: guards that fixture initialization completed before measuring, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
      m_initialized = true;
  }

  void TimelinePanNativeTest::init()
  {
      initializeFixture();
  }

  void TimelinePanNativeTest::cleanup()
  {
      m_initialized = false;
      m_fixture.cleanup();
  }

  void TimelinePanNativeTest::selectionExtentPanPerformance()
  {
      qint64 shortMilliseconds = 0;
Original expression:
  QVERIFY(m_initialized);

A007 | TimelinePanNativeTest::selectionExtentPanPerformance | src/checks/timelinepan/tst_timelinepannative.cpp:67
Disposition: NATIVE
Mapping/reason: Original native harness obligation: guards native short-extent pan measurement execution, which a presenter-level Swift check cannot establish. The sampled milliseconds are diagnostic only.
Source context since previous assertion:
      QString error;
Original expression:
  QVERIFY2(m_fixture.measureSelectedPan(4, &shortMilliseconds, &error), qPrintable(error));

A008 | TimelinePanNativeTest::selectionExtentPanPerformance | src/checks/timelinepan/tst_timelinepannative.cpp:72
Disposition: NATIVE
Mapping/reason: Original native harness obligation: guards that the torn-down and re-initialized fixture is ready before the long-extent measurement, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
      // The compared extents must not share selection or renderer state.
      cleanup();
      initializeFixture();
Original expression:
  QVERIFY(m_initialized);

A009 | TimelinePanNativeTest::selectionExtentPanPerformance | src/checks/timelinepan/tst_timelinepannative.cpp:76
Disposition: NATIVE
Mapping/reason: Original native harness obligation: guards native long-extent pan measurement execution, which a presenter-level Swift check cannot establish. The sampled milliseconds are diagnostic only.
Source context since previous assertion:
      qint64 longMilliseconds = 0;
      error.clear();
Original expression:
  QVERIFY2(m_fixture.measureSelectedPan(65536, &longMilliseconds, &error), qPrintable(error));

Remaining original source (closing sequence and helper bodies):
      // Opt-in diagnostic: capture/readback and host load affect these samples.
      // Report both extents without treating one timing pair as a correctness gate.
      qInfo().nospace() << "timelinepan-native: short=" << shortMilliseconds
                        << "ms long=" << longMilliseconds << "ms";
  }

  } // namespace

  int runTimelinePanNativeCheck(const QString &projectRoot, const QString &songLabel,
                                const QStringList &qtArguments)
  {
      TimelinePanNativeTest test(projectRoot, songLabel);
      QStringList arguments{QStringLiteral("timelinepan-native")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

  #include "tst_timelinepannative.moc"

Swift assertion predicates
No Swift counterpart was identified, so no S entries exist.

Shared helper: none (no Swift fixture files carrying expects were identified).
