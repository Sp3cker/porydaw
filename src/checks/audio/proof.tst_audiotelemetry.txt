Assertion correspondence: audio/tst_audiotelemetry.cpp
Deleted in: feature/gaps-audio-scale — staged deletion (uncommitted in this worktree) of src/checks/audio/tst_audiotelemetry.cpp and src/checks/audio/tst_audiotelemetry.h as part of the PorydawCore telemetry port.

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/audio/tst_audiotelemetry.cpp
Original SHA-256: be59b9fc427b087715c7ff78ff2cfccf7c60639dafb2c883a912f38d3461f735
Swift counterpart: src/checks/audio/tst_audiotelemetry.swift
Verification: lane integration run recorded by the controller, not re-run for this ledger: deno task verify --verbose — verify: 6/6 ok (11.75s); run_checks PASS (all harnesses in 11.75s), covering production-startup, audiocheck-backend, vgbankcheck, exportcheck-loop, exportcheck-tail and swiftcore.
Result: swiftcore PASS; SwiftCoreTest::playback runs PDC_SUITE_PLAYBACK, which calls runAudioControllerChecks and executes every predicate below green. No live native audiocheck run and no differential comparison exist or are claimed; the deleted harness neither builds nor executes.

Deletion certificate
The C++ original src/checks/audio/tst_audiotelemetry.cpp (110 lines at the reference revision) and its header src/checks/audio/tst_audiotelemetry.h were deleted in this lane. The audiocheck registration was removed from src/checks/checkcatalog.cpp, the runAudioCheck declaration from src/checks/fwd.hpp, and both source entries from src/checks/CMakeLists.txt; the Swift replacement src/checks/audio/tst_audiotelemetry.swift is compiled into the swiftcore check target. The counterpart file is unmodified in this lane and its hash above matches the ledger's pre-retirement record, so every site below is reconciled against the frozen source as it stood before this rewrite. The original stays recoverable from the Reference revision and Original SHA-256 pair, re-verified before this rewrite:
  git show f3069ef693542bdb63564b80a29773e2f5b2a360:src/checks/audio/tst_audiotelemetry.cpp | shasum -a 256
  = be59b9fc427b087715c7ff78ff2cfccf7c60639dafb2c883a912f38d3461f735
Retirement: 15 original assertion sites, all terminal (13 MATCHED, 2 RETIRED-REPRESENTATION). The 13 MATCHED sites name executing Swift predicates that carry the original audiocheck/AudioTelemetryTest cppID; the 2 retired sites are the raw-word ingress of A004/A005, whose 0xDEADBEEF input is not representable through the Swift typed-channel contract. The reference-revision sources remain the authority for the original setup, guards and helpers, and no whole-case equivalence, native execution or parity claim is made.

Purpose and status
Each numbered entry records one original assertion/failure site, not one test
method or one runtime execution. Loops/data rows reuse a site; their instances
are described below. QFAIL/qFail guards and helper calls are part of the contract.
MATCHED means a corresponding Swift predicate was identified and executed;
RETIRED-REPRESENTATION identifies an unreachable old implementation input with
an explicit replacement-contract justification, not an omitted user behavior;
PARTIAL means a related check exists but differs in a stated way;
GAP means an equivalent Swift assertion is not established;
NATIVE-SETUP identifies a C++ harness/loader/device precondition still exercised
only by the retained native path. None of these labels authorizes deletion or
claims whole-case equivalence. Native passes do not prove the Swift replacement.

Execution and ownership
Registered run path: swiftcore check -> src/checks/support/corecheck/tst_swiftcore.cpp:36 SwiftCoreTest::playback -> pdc_suite_run(PDC_SUITE_PLAYBACK) at tst_swiftcore.cpp:38 -> src/checks/support/corecheck/CoreCheckSupport.swift case 3 -> runAudioControllerChecks (src/checks/audio/AudioControllerChecks.swift) -> checkControllerControls (:24) -> checkAudioVolumeClamps (src/checks/audio/tst_audiotelemetry.swift) -> checkAudioActivityTelemetry (src/checks/audio/tst_audiotelemetry.swift). Every Swift predicate mapped below carries the original audiocheck/AudioTelemetryTest cppID and reports through tst_swiftcore.cpp reportSwiftCheck, which fails the QtTest run on any failed predicate.
checkAudioVolumeClamps receives the same renderer from checkControllerControls; it leaves volume at 100 before subsequent rendering, preserving existing order.

Fixture, sequence and row constraints
packedActivity rows: silent=(0,0,0x0000), maximum=(255,255,0xffff), asymmetric=(0x5A,0xA5,0xa55a). pcmPan rows: center=(255,127,127 ->255,255), hard-left=(255,127,0 ->255,0), hard-right=(255,0,127 ->0,255), asymmetric-balance=(128,32,64 ->64,128), silent=(255,0,0 ->0,0). freshConsume loops over every track and both stereo components. The Swift rows publish the same values through production AudioTelemetry.publish on a real M4AEngine and consume the result, so the byte-order and envelope predicates observe the shipped telemetry path instead of copied constants.
Original setup, branch guards and helper implementations are read at the reference revision (recoverable through the revision and hash above); this inventory does not replace those deleted sources.

Verification evidence
Only the lane integration run quoted above is cited; it was recorded by the controller and was not re-run for this ledger. The pre-retirement "deno task verify --filter swiftcore --filter clickcheck" line is dropped because the clickcheck and audiocheck harnesses no longer exist, so no native run of this pair can be cited. Raw-word-only implementation assertions are retired at A004/A005, not claimed as executed Swift assertions. Semantic parity is not inferred from a passing suite.

Shared Swift sources used by this refactor (SHA-256)
src/checks/audio/controllerfixture.swift: 60f6f6e5d489f2781c18bfea29e40cab4b0f56d73885e2e6fbb3dab02769ff4d
src/checks/audio/resonancefixture.swift: 65fd4fc77f54ed1e6f225551f1e54e5fee7dc93d4078d4dda15261b213790654
src/checks/playback/transportfixture.swift: 9954fe9db6763cb78623fe4c8c9410ea5ff30b4b6d0e417b066ede2339edf4ab
src/checks/audio/AudioControllerChecks.swift: 019c0ce1191fe474b2655a24b414f1f402da0c18148c8e03643a6e0be573d77c
src/checks/playback/PlaybackChecks.swift: 7e1cc988b6542fc2fdcdcb377a7b73b2369df0c4901dfe460b0986fefb7f1bfe
src/checks/playback/ResonanceSuppressionChecks.swift: 2be2bee46bd865a0c085647d0109afb4b01984e9e54f80747d0b4ea2a5b98f32

Swift production sources exercised (SHA-256)
src/swift/app/audio/AudioTelemetry.swift: c4dd3ee85d0c1a73c31e8c1f1a4beb165ae9c687f079012a3a1072f82e536f02
src/swift/app/audio/AudioRenderEngine.swift: ecd5e5c114cec3013f9589ec4ca18dfd1a03429476ce9291ae80a60e3cf10a78

Original assertion inventory

A001 | AudioTelemetryTest::packedActivityPreservesByteOrder | src/checks/audio/tst_audiotelemetry.cpp:28
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes the original silent/maximum/asymmetric rows through AudioTelemetry.publish and consumes exact left/right bytes; the same expected packed word is reconstructed for the byte-order predicate. Private storage packing is no longer a public ABI.
Original expression:
  QCOMPARE(packedActivity(level), packed)

A002 | AudioTelemetryTest::packedActivityPreservesByteOrder | src/checks/audio/tst_audiotelemetry.cpp:30
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes the original silent/maximum/asymmetric rows through AudioTelemetry.publish and consumes exact left/right bytes; the same expected packed word is reconstructed for the byte-order predicate. Private storage packing is no longer a public ABI.
Original expression:
  QCOMPARE(unpacked.left, left)

A003 | AudioTelemetryTest::packedActivityPreservesByteOrder | src/checks/audio/tst_audiotelemetry.cpp:31
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes the original silent/maximum/asymmetric rows through AudioTelemetry.publish and consumes exact left/right bytes; the same expected packed word is reconstructed for the byte-order predicate. Private storage packing is no longer a public ABI.
Original expression:
  QCOMPARE(unpacked.right, right)

A004 | AudioTelemetryTest::unpackConsumesOnlyActivityBytes | src/checks/audio/tst_audiotelemetry.cpp:37
Disposition: RETIRED-REPRESENTATION
Mapping/reason: V-1 representation retirement: the old standalone unpackedActivity(UInt32) accepted arbitrary upper bytes. Swift accepts typed UInt8 channel levels; its private peak words are constructed only from those two bytes, with no raw-word ingress. The 0xDEADBEEF input is not representable through the replacement contract. Exact valid-byte consumption is exercised by packedActivityPreservesByteOrder; no production test-only raw-word setter was added.
Original expression:
  QCOMPARE(unpacked.left, uint8_t{0xEF})

A005 | AudioTelemetryTest::unpackConsumesOnlyActivityBytes | src/checks/audio/tst_audiotelemetry.cpp:38
Disposition: RETIRED-REPRESENTATION
Mapping/reason: V-1 representation retirement: the old standalone unpackedActivity(UInt32) accepted arbitrary upper bytes. Swift accepts typed UInt8 channel levels; its private peak words are constructed only from those two bytes, with no raw-word ingress. The 0xDEADBEEF input is not representable through the replacement contract. Exact valid-byte consumption is exercised by packedActivityPreservesByteOrder; no production test-only raw-word setter was added.
Original expression:
  QCOMPARE(unpacked.right, uint8_t{0xBE})

A006 | AudioTelemetryTest::maxLevelIsComponentWise | src/checks/audio/tst_audiotelemetry.cpp:44
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes two simultaneous PCM channels with (24,220) and (220,24), then asserts both consumed components equal220 through the real production collector.
Original expression:
  QCOMPARE(combined.left, uint8_t{220})

A007 | AudioTelemetryTest::maxLevelIsComponentWise | src/checks/audio/tst_audiotelemetry.cpp:45
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes two simultaneous PCM channels with (24,220) and (220,24), then asserts both consumed components equal220 through the real production collector.
Original expression:
  QCOMPARE(combined.right, uint8_t{220})

A008 | AudioTelemetryTest::pcmPanRetainsEnvelope | src/checks/audio/tst_audiotelemetry.cpp:76
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes all five original envelope/pan rows through AudioTelemetry.publish and compares both output components individually against the original expected values.
Original expression:
  QCOMPARE(level.left, expectedLeft)

A009 | AudioTelemetryTest::pcmPanRetainsEnvelope | src/checks/audio/tst_audiotelemetry.cpp:77
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry publishes all five original envelope/pan rows through AudioTelemetry.publish and compares both output components individually against the original expected values.
Original expression:
  QCOMPARE(level.right, expectedRight)

A010 | AudioTelemetryTest::outputVolumeClamps | src/checks/audio/tst_audiotelemetry.cpp:83
Disposition: MATCHED
Mapping/reason: checkAudioVolumeClamps asserts initial100, in-range42, lower-1=>0, upper101=>100 using AudioRenderEngine.setOutputVolume and outputVolume.
Original expression:
  QCOMPARE(engine.outputVolume(), 100)

A011 | AudioTelemetryTest::outputVolumeClamps | src/checks/audio/tst_audiotelemetry.cpp:85
Disposition: MATCHED
Mapping/reason: checkAudioVolumeClamps asserts initial100, in-range42, lower-1=>0, upper101=>100 using AudioRenderEngine.setOutputVolume and outputVolume.
Original expression:
  QCOMPARE(engine.outputVolume(), 42)

A012 | AudioTelemetryTest::outputVolumeClamps | src/checks/audio/tst_audiotelemetry.cpp:87
Disposition: MATCHED
Mapping/reason: checkAudioVolumeClamps asserts initial100, in-range42, lower-1=>0, upper101=>100 using AudioRenderEngine.setOutputVolume and outputVolume.
Original expression:
  QCOMPARE(engine.outputVolume(), 0)

A013 | AudioTelemetryTest::outputVolumeClamps | src/checks/audio/tst_audiotelemetry.cpp:89
Disposition: MATCHED
Mapping/reason: checkAudioVolumeClamps asserts initial100, in-range42, lower-1=>0, upper101=>100 using AudioRenderEngine.setOutputVolume and outputVolume.
Original expression:
  QCOMPARE(engine.outputVolume(), 100)

A014 | AudioTelemetryTest::freshConsumeIsAllDark | src/checks/audio/tst_audiotelemetry.cpp:97
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry consumes a fresh AudioTelemetry, asserts MAX_TRACKS entries, then checks both components equal0 for every track.
Original expression:
  QCOMPARE(consumed[track].left, uint8_t{0})

A015 | AudioTelemetryTest::freshConsumeIsAllDark | src/checks/audio/tst_audiotelemetry.cpp:98
Disposition: MATCHED
Mapping/reason: checkAudioActivityTelemetry consumes a fresh AudioTelemetry, asserts MAX_TRACKS entries, then checks both components equal0 for every track.
Original expression:
  QCOMPARE(consumed[track].right, uint8_t{0})
