Assertion correspondence: audio/tst_resonancelaw.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/audio/tst_resonancelaw.cpp
Original SHA-256: 8cd681576b00626d3b93c4606feb8ff03da0f4aff9d1df5903d7833468a6f4f5
Swift counterpart: src/checks/audio/tst_resonancelaw.swift
Swift SHA-256: b05654d1714191620beeda4612926d78a723669cb4fa55a5b5f901665dc7c8d3

Purpose and status
Each numbered entry records one original assertion/failure site, not one test
method or one runtime execution. Loops/data rows reuse a site; their instances
are described below. QFAIL/qFail guards and helper calls are part of the contract.
MATCHED means a corresponding Swift predicate was identified and executed;
PARTIAL means a related check exists but differs in a stated way;
GAP means an equivalent Swift assertion is not established;
NATIVE-SETUP identifies a C++ harness/loader/device precondition still exercised
only by the retained native path. None of these labels authorizes deletion or
claims whole-case equivalence. Native passes do not prove the Swift replacement.
File retirement: BLOCKED. These Swift predicates exercise the separate Swift
ResonanceSuppression implementation; the native ResonanceSuppressor still runs
in the C++ audio engine and wave export. Identical input/output assertions
do not replace coverage of that live native DSP implementation.

Execution and ownership
resonanceLawChecks is called by runResonanceSuppressionChecks. Shared signal generation, measurement and rendering live in audio/resonancefixture.swift. Additional Swift-only reset/chunking checks remain in resonanceTransitionChecks in playback/ResonanceSuppressionChecks.swift.

Fixture, sequence and row constraints
Default 48 kHz stereo, 512-frame processing chunks and 2047-frame latency. Keep each original case's signal seed, frequency, level, onset, duration, analysis window and curve settings; the paired Swift do/loop blocks carry these inputs. Embedded-tone loop expands loud=-15 dB and modest=-25 dB into distinct case names. Stereo assertion expands over every output frame. Guard-bin assertion has two original predicates combined in Swift, plus an additional Nyquist predicate.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Verification evidence
Latest Swift run: deno task verify --filter swiftcore --verbose — PASS 1/24
(23 skipped), swiftcore 9.64s. Native run: deno task verify --filter
trackactivitycheck --filter resonancecheck --filter resonancecheck-timing
--verbose — PASS 3/24 (21 skipped). No cross-implementation differential
or retirement certification is claimed.

Shared Swift sources used by this refactor (SHA-256)
src/checks/audio/controllerfixture.swift: ed301a278f5c66b9d77f7cd251315a9ecaa47889dc7b838ddac761ce01950290
src/checks/audio/resonancefixture.swift: 65fd4fc77f54ed1e6f225551f1e54e5fee7dc93d4078d4dda15261b213790654
src/checks/playback/transportfixture.swift: 9954fe9db6763cb78623fe4c8c9410ea5ff30b4b6d0e417b066ede2339edf4ab
src/checks/audio/AudioControllerChecks.swift: 019c0ce1191fe474b2655a24b414f1f402da0c18148c8e03643a6e0be573d77c
src/checks/playback/PlaybackChecks.swift: 7e1cc988b6542fc2fdcdcb377a7b73b2369df0c4901dfe460b0986fefb7f1bfe
src/checks/playback/ResonanceSuppressionChecks.swift: 2be2bee46bd865a0c085647d0109afb4b01984e9e54f80747d0b4ea2a5b98f32

Original assertion inventory

A001 | ResonanceLawTest::disabledPathIsBitExact | src/checks/audio/tst_resonancelaw.cpp:16
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(exactSamples(input, output), "disabled path must be bit-exact for noise")

A002 | ResonanceLawTest::unitMaskReconstructionFloor | src/checks/audio/tst_resonancelaw.cpp:26
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(errorDb <= -120.0,
               qUtf8Printable(QStringLiteral("forced unit mask reconstruction must be at most "
                                             "-120 dB RMS (got %1 dB)")
                                  .arg(errorDb, 0, 'f', 1)))

A003 | ResonanceLawTest::belowThresholdTonePasses | src/checks/audio/tst_resonancelaw.cpp:38
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(outputDb - inputDb) <= 0.1,
               qUtf8Printable(QStringLiteral("below-threshold 1 kHz tone must remain within 0.1 dB "
                                             "(in %1 dB, out %2 dB)")
                                  .arg(inputDb, 0, 'f', 3)
                                  .arg(outputDb, 0, 'f', 3)))

A004 | ResonanceLawTest::shippingDefaultLimitsSaturatedResonance | src/checks/audio/tst_resonancelaw.cpp:54
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(reduction - 5.9375) <= 1.0,
               qUtf8Printable(QStringLiteral("shipping default must limit a saturated resonance to "
                                             "about -5.9 dB (got %1 dB)")
                                  .arg(reduction, 0, 'f', 3)))

A005 | ResonanceLawTest::testCurvePlateauAboveGuard | src/checks/audio/tst_resonancelaw.cpp:67
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(
          std::abs(reduction - 9.375) <= 1.0,
          qUtf8Printable(QStringLiteral("above-guard 1 kHz tone plateau must be -9.4 dB +/- 1 dB "
                                        "(got %1 dB)")
                             .arg(reduction, 0, 'f', 3)))

A006 | ResonanceLawTest::lowBandTonePassesThrough | src/checks/audio/tst_resonancelaw.cpp:82
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(-15.0 - outputDb) <= 0.1,
               qUtf8Printable(QStringLiteral("low-band 300 Hz tone must stay within 0.1 dB (knots "
                                             "0-4 inactive; out %1 dB)")
                                  .arg(outputDb, 0, 'f', 3)))

A007 | ResonanceLawTest::saturationPlateausWithoutOvershoot | src/checks/audio/tst_resonancelaw.cpp:102
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(finalReduction - 9.375) <= 1.0 && deepestReduction <= 10.5,
               qUtf8Printable(QStringLiteral("saturation must plateau near -9.4 dB and never exceed "
                                             "-10.5 dB (final %1 dB, deepest %2 dB)")
                                  .arg(finalReduction, 0, 'f', 3)
                                  .arg(deepestReduction, 0, 'f', 3)))

A008 | ResonanceLawTest::levelStaircaseHoldsPlateau | src/checks/audio/tst_resonancelaw.cpp:136
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(allNearPlateau && maximumReduction - minimumReduction < 0.5,
               qUtf8Printable(QStringLiteral("level staircase must hold a -25 dB plateau with less "
                                             "than 0.5 dB drift (range %1..%2 dB)")
                                  .arg(minimumReduction, 0, 'f', 3)
                                  .arg(maximumReduction, 0, 'f', 3)))

A009 | ResonanceLawTest::dualTonesEngageSeparately | src/checks/audio/tst_resonancelaw.cpp:152
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(firstReduction - 25.0) <= 1.0 && std::abs(secondReduction - 25.0) <= 1.0,
               qUtf8Printable(QStringLiteral("two spectrally separated tones must both engage near "
                                             "the -25 dB plateau (1 kHz %1 dB, 12 kHz %2 dB)")
                                  .arg(firstReduction, 0, 'f', 3)
                                  .arg(secondReduction, 0, 'f', 3)))

A010 | ResonanceLawTest::broadbandProgramPasses | src/checks/audio/tst_resonancelaw.cpp:170
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(std::abs(inDb - outDb) <= 0.5,
               qUtf8Printable(QStringLiteral("broadband program must pass with at most 0.5 dB of "
                                             "suppression (shift %1 dB)")
                                  .arg(inDb - outDb, 0, 'f', 3)))

A011 | ResonanceLawTest::embeddedLoudToneEngagesProgramSurvives | src/checks/audio/tst_resonancelaw.cpp:189
Disposition: MATCHED
Mapping/reason: resonanceLawChecks, loud=true: 'embedded reduction', reduction >=10 dB.
Original expression:
  QVERIFY2(
          reduction >= 10.0,
          qUtf8Printable(QStringLiteral("embedded 3 kHz tone must engage at least 10 dB (got %1)")
                             .arg(reduction, 0, 'f', 3)))

A012 | ResonanceLawTest::embeddedLoudToneEngagesProgramSurvives | src/checks/audio/tst_resonancelaw.cpp:197
Disposition: MATCHED
Mapping/reason: resonanceLawChecks, loud=true: 'program RMS change', rmsDelta <=3.5 dB.
Original expression:
  QVERIFY2(std::abs(inDb - outDb) <= 3.5,
               qUtf8Printable(QStringLiteral("broadband program must survive an embedded tone's "
                                             "suppression (shift %1 dB)")
                                  .arg(inDb - outDb, 0, 'f', 3)))

A013 | ResonanceLawTest::embeddedModestToneEngagesGently | src/checks/audio/tst_resonancelaw.cpp:217
Disposition: MATCHED
Mapping/reason: resonanceLawChecks, loud=false: 'embedded reduction', reduction in [1.5,8.5] dB.
Original expression:
  QVERIFY2(reduction >= 1.5 && reduction <= 8.5,
               qUtf8Printable(QStringLiteral("modest resonance in program must engage gently, far "
                                             "below the full plateau (got %1 dB)")
                                  .arg(reduction, 0, 'f', 3)))

A014 | ResonanceLawTest::embeddedModestToneEngagesGently | src/checks/audio/tst_resonancelaw.cpp:221
Disposition: MATCHED
Mapping/reason: resonanceLawChecks, loud=false: 'program RMS change', rmsDelta <=1.5 dB.
Original expression:
  QVERIFY2(std::abs(inDb - outDb) <= 1.5,
               qUtf8Printable(QStringLiteral("broadband program must survive a modest resonance's "
                                             "suppression (shift %1 dB)")
                                  .arg(inDb - outDb, 0, 'f', 3)))

A015 | ResonanceLawTest::stereoChannelsStayBitIdentical | src/checks/audio/tst_resonancelaw.cpp:236
Disposition: MATCHED
Mapping/reason: resonanceLawChecks: 'identical channels remain bit-identical', allSatisfy over every output frame compares left/right Float.bitPattern. Original QFAIL is guarded by memcmp(left,right,sizeof(float)) !=0.
Original expression:
  QFAIL(qUtf8Printable(
                  QStringLiteral("identical stereo channels diverged at frame %1").arg(frame)))

A016 | ResonanceLawTest::midStreamDisableRestoresBypass | src/checks/audio/tst_resonancelaw.cpp:247
Disposition: MATCHED
Mapping/reason: resonanceLawChecks emits the original method name through its expect helper; the original predicate below is represented by the corresponding Swift numeric/bit-exact assertion. This is source correspondence plus execution, not a live C++/Swift differential sweep.
Original expression:
  QVERIFY2(renderDisableMidStream(input, disableFrame, output),
               "disabling after full-scale sine must restore bit-exact bypass")

A017 | ResonanceLawTest::guardBinsNeverMasked | src/checks/audio/tst_resonancelaw.cpp:276
Disposition: MATCHED
Mapping/reason: resonanceLawChecks: 'DC/Nyquist untouched while neighboring tone engages', binGainDb(0)==0 conjunct.
Original expression:
  QCOMPARE(suppressor.binGainDb(0), 0.0)

A018 | ResonanceLawTest::guardBinsNeverMasked | src/checks/audio/tst_resonancelaw.cpp:277
Disposition: MATCHED
Mapping/reason: Same combined guard assertion, binGainDb(1003)<-1 conjunct.
Original expression:
  QVERIFY2(suppressor.binGainDb(1003) < -1.0,
               "a real bin near the Nyquist guard must still engage")
