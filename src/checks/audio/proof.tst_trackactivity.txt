Assertion correspondence: audio/tst_trackactivity.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/audio/tst_trackactivity.cpp
Original SHA-256: edbde7297cc2f3e7d0dbdaa813141f1ec2a0624e9266476ac1ab0828e80cfa51
Swift counterpart: src/checks/audio/tst_trackactivity.swift
Swift SHA-256: 5d6e3d7a57a12628ad024258d5bfdd6077af440d6db656dee5780f096c85246d

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
S identifiers identify exact Swift predicates, listed after the inventory.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of the converted Swift window.
GAP identifies an original predicate without a replacement assertion.
File retirement: BLOCKED. Swift TrackActivity exercises the shipped model,
but the native C++ implementation remains referenced by the native activity
check and unretired legacy track-header C++ sources. Remove those consumers
and the C++ implementation together before deleting the native check.

Verification
Swift: deno task verify --filter swiftcore --verbose — PASS 1/24
(23 skipped), swiftcore 9.64s. Native: deno task verify --filter
trackactivitycheck --filter resonancecheck --filter resonancecheck-timing
--verbose — PASS 3/24 (21 skipped). No retirement certification is claimed.

Execution ownership
runProjectSessionSuite (workspace/SessionChecks.swift:422, 1057) invokes
runTrackHeadersChecks (trackheaders/TrackHeadersChecks.swift:6-8), which runs
runTrackActivityChecks before the track-header model cases. All ten original
policy scenarios are ported with the same inputs, loop bounds, and tolerances.

Original assertion inventory and source sequence

A001 | TrackActivityTest::freshActivityIsDark | src/checks/audio/tst_trackactivity.cpp:41
Disposition: MATCHED
Mapping/reason: S001: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  #include "checks/audio/tst_trackactivity.h"
  #include "checks/fwd.hpp"

  #include <QtTest>

  #include <cmath>

  #include "ui/activity/trackactivity.h"

  namespace checks {
  namespace {

  constexpr float kTolerance = 0.0001f;
  constexpr int kIsolatedTrack = 3;

  bool nearlyEqual(float actual, float expected)
  {
      return std::abs(actual - expected) <= kTolerance;
  }

  bool dark(TrackActivityIntensity intensity)
  {
      return intensity.left == 0.0f && intensity.right == 0.0f;
  }

  bool everyTrackDark(const TrackActivity &activity)
  {
      for (int track = 0; track < int(kMaxTracks); ++track) {
          if (!dark(activity.intensity(track)))
              return false;
      }
      return true;
  }

  } // namespace

  void TrackActivityTest::freshActivityIsDark()
  {
      TrackActivity activity;
      for (int track = 0; track < int(kMaxTracks); ++track)
Original expression:
  QVERIFY2(dark(activity.intensity(track)), "new activity must be dark")

A002 | TrackActivityTest::freshActivityIsDark | src/checks/audio/tst_trackactivity.cpp:42
Disposition: MATCHED
Mapping/reason: S002: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(dark(activity.intensity(-1)), "invalid negative track must be dark")

A003 | TrackActivityTest::freshActivityIsDark | src/checks/audio/tst_trackactivity.cpp:43
Disposition: MATCHED
Mapping/reason: S003: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(dark(activity.intensity(int(kMaxTracks))), "invalid high track must be dark")

A004 | TrackActivityTest::attackRetainsStereoTargets | src/checks/audio/tst_trackactivity.cpp:64
Disposition: MATCHED
Mapping/reason: S004: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::attackRetainsStereoTargets_data()
  {
      QTest::addColumn<uint8_t>("peakLeft");
      QTest::addColumn<uint8_t>("peakRight");

      QTest::newRow("asymmetric") << uint8_t{128} << uint8_t{32};
      QTest::newRow("left-only") << uint8_t{255} << uint8_t{0};
      QTest::newRow("right-only") << uint8_t{0} << uint8_t{255};
  }

  void TrackActivityTest::attackRetainsStereoTargets()
  {
      QFETCH(uint8_t, peakLeft);
      QFETCH(uint8_t, peakRight);

      TrackActivity activity;
      TrackActivityLevels levels{};
      levels[kIsolatedTrack] = {peakLeft, peakRight};
Original expression:
  QVERIFY2(activity.advance(levels, 0.017f, true), "playing activity must require another tick")

A005 | TrackActivityTest::attackRetainsStereoTargets | src/checks/audio/tst_trackactivity.cpp:72
Disposition: MATCHED
Mapping/reason: S005: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;

      const auto first = activity.intensity(kIsolatedTrack);
      // A side with a peak attacks toward it without snapping; a silent side
      // stays exactly at zero (hard-panned peaks never cross-bleed).
      const auto approaches = [](float value, uint8_t peak) {
          return peak > 0 ? (value > 0.0f && value < peak / 255.0f) : value == 0.0f;
      };
Original expression:
  QVERIFY2(approaches(first.left, peakLeft),
               "left attack must approach the peak without snapping")

A006 | TrackActivityTest::attackRetainsStereoTargets | src/checks/audio/tst_trackactivity.cpp:74
Disposition: MATCHED
Mapping/reason: S006: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(approaches(first.right, peakRight),
               "right attack must approach the peak without snapping")

A007 | TrackActivityTest::attackRetainsStereoTargets | src/checks/audio/tst_trackactivity.cpp:76
Disposition: MATCHED
Mapping/reason: S007: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(peakLeft > peakRight ? first.left > first.right : first.right > first.left,
               "stereo sides must retain independent targets with no channel bleed")

A008 | TrackActivityTest::attackRetainsStereoTargets | src/checks/audio/tst_trackactivity.cpp:80
Disposition: MATCHED
Mapping/reason: S008: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      for (int track = 0; track < int(kMaxTracks); ++track) {
          if (track != kIsolatedTrack)
Original expression:
  QVERIFY2(dark(activity.intensity(track)), "a peak must not light another track")

A009 | TrackActivityTest::releaseIsGradualAndMonotonic | src/checks/audio/tst_trackactivity.cpp:95
Disposition: MATCHED
Mapping/reason: S009: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      }
  }

  void TrackActivityTest::releaseIsGradualAndMonotonic()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};
      levels[kIsolatedTrack] = {128, 32};
      activity.advance(levels, 0.017f, true);

      levels.fill({});
      auto previous = activity.intensity(kIsolatedTrack);
      activity.advance(levels, 0.125f, true);
      auto current = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(current.left > previous.left * 0.5f && current.right > previous.right * 0.5f,
               "ordinary release must remain visibly gradual")

A010 | TrackActivityTest::releaseIsGradualAndMonotonic | src/checks/audio/tst_trackactivity.cpp:101
Disposition: MATCHED
Mapping/reason: S010: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      previous = current;
      for (auto step = 0; step < 23; ++step) {
          activity.advance(levels, 0.125f, true);
          current = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(current.left <= previous.left && current.right <= previous.right,
                   "ordinary release must decay monotonically on both sides")

A011 | TrackActivityTest::releaseIsGradualAndMonotonic | src/checks/audio/tst_trackactivity.cpp:105
Disposition: MATCHED
Mapping/reason: S011: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
          previous = current;
      }
Original expression:
  QVERIFY2(dark(activity.intensity(kIsolatedTrack)), "released activity must settle at zero")

A012 | TrackActivityTest::imperceptibleTailSnapsToZero | src/checks/audio/tst_trackactivity.cpp:116
Disposition: MATCHED
Mapping/reason: S012: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::imperceptibleTailSnapsToZero()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};
      levels[kIsolatedTrack] = {255, 255};
      activity.advance(levels, 0.015f, true);
      levels.fill({});
      activity.advance(levels, 2.0f, true);
Original expression:
  QVERIFY2(dark(activity.intensity(kIsolatedTrack)),
               "the visible floor must remove an imperceptible release tail")

A013 | TrackActivityTest::retriggerRetainsInertia | src/checks/audio/tst_trackactivity.cpp:135
Disposition: MATCHED
Mapping/reason: S013: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::retriggerRetainsInertia()
  {
      constexpr int kRetriggerTrack = 7;
      TrackActivity activity;
      TrackActivityLevels levels{};

      levels[kRetriggerTrack] = {48, 0};
      activity.advance(levels, 0.017f, true);
      levels.fill({});
      activity.advance(levels, 1.0f, true);
      const auto decayed = activity.intensity(kRetriggerTrack);

      levels[kRetriggerTrack] = {224, 64};
      activity.advance(levels, 0.017f, true);
      const auto retriggered = activity.intensity(kRetriggerTrack);
Original expression:
  QVERIFY2(retriggered.left > decayed.left && retriggered.right > decayed.right,
               "a new peak must retrigger each side independently")

A014 | TrackActivityTest::retriggerRetainsInertia | src/checks/audio/tst_trackactivity.cpp:137
Disposition: MATCHED
Mapping/reason: S014: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(retriggered.left < 224.0f / 255.0f && retriggered.right < 64.0f / 255.0f,
               "a retrigger must retain thermal attack inertia")

A015 | TrackActivityTest::resetClearsEveryLight | src/checks/audio/tst_trackactivity.cpp:148
Disposition: MATCHED
Mapping/reason: S015: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::resetClearsEveryLight()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};
      levels[kIsolatedTrack] = {200, 120};
      activity.advance(levels, 0.017f, true);
      activity.reset();
Original expression:
  QVERIFY2(everyTrackDark(activity), "reset must clear every light")

A016 | TrackActivityTest::pausedFillSettlesEveryTrack | src/checks/audio/tst_trackactivity.cpp:160
Disposition: MATCHED
Mapping/reason: S016: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::pausedFillSettlesEveryTrack()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};

      levels[kIsolatedTrack] = {255, 64};
      activity.advance(levels, 0.015f, true);
      const auto beforePause = activity.intensity(kIsolatedTrack);
      levels.fill({});
Original expression:
  QVERIFY2(activity.advance(levels, 0.015f, false), "paused fill must require another tick")

A017 | TrackActivityTest::pausedFillSettlesEveryTrack | src/checks/audio/tst_trackactivity.cpp:162
Disposition: MATCHED
Mapping/reason: S017: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      const auto filling = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(filling.left > beforePause.left && filling.right > beforePause.right,
               "paused fill must gently approach full brightness")

A018 | TrackActivityTest::pausedFillSettlesEveryTrack | src/checks/audio/tst_trackactivity.cpp:164
Disposition: MATCHED
Mapping/reason: S018: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(!activity.advance(levels, 10.0f, false), "settled paused fill must stop ticks")

A019 | TrackActivityTest::pausedFillSettlesEveryTrack | src/checks/audio/tst_trackactivity.cpp:167
Disposition: MATCHED
Mapping/reason: S019: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      for (int track = 0; track < int(kMaxTracks); ++track) {
          const auto settled = activity.intensity(track);
Original expression:
  QVERIFY2(settled.left == 1.0f && settled.right == 1.0f,
                   "paused fill must settle every side at full brightness")

A020 | TrackActivityTest::pausedFillSettlesEveryTrack | src/checks/audio/tst_trackactivity.cpp:170
Disposition: MATCHED
Mapping/reason: S020: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      }
Original expression:
  QVERIFY2(!activity.advance(levels, 0.0f, false), "settled pause must remain settled")

A021 | TrackActivityTest::resumeUsesFastWindowThenOrdinaryRelease | src/checks/audio/tst_trackactivity.cpp:179
Disposition: MATCHED
Mapping/reason: S021: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::resumeUsesFastWindowThenOrdinaryRelease()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};

      activity.resetPaused();
Original expression:
  QVERIFY2(activity.advance(levels, 0.075f, true), "resuming activity must require another tick")

A022 | TrackActivityTest::resumeUsesFastWindowThenOrdinaryRelease | src/checks/audio/tst_trackactivity.cpp:181
Disposition: MATCHED
Mapping/reason: S022: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      const auto resumed = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(nearlyEqual(resumed.left, std::exp(-0.075f / 0.015f)),
               "resuming descent must use the 75 ms fast window")

A023 | TrackActivityTest::resumeUsesFastWindowThenOrdinaryRelease | src/checks/audio/tst_trackactivity.cpp:185
Disposition: MATCHED
Mapping/reason: S023: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      activity.advance(levels, 0.015f, true);
      const auto ordinaryRelease = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(nearlyEqual(ordinaryRelease.left, resumed.left * std::exp(-0.015f / 0.250f)),
               "release must return to 250 ms after the resume window")

A024 | TrackActivityTest::resumeUsesFastWindowThenOrdinaryRelease | src/checks/audio/tst_trackactivity.cpp:187
Disposition: MATCHED
Mapping/reason: S024: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(ordinaryRelease.left > resumed.left * std::exp(-0.015f / 0.015f),
               "ordinary release must not retain the fast resume descent")

A025 | TrackActivityTest::rapidPauseRearmsFastDescent | src/checks/audio/tst_trackactivity.cpp:202
Disposition: MATCHED
Mapping/reason: S025: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::rapidPauseRearmsFastDescent()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};

      activity.resetPaused();
      activity.advance(levels, 0.050f, true);
      activity.advance(levels, 0.001f, false);
      const auto beforeRapidResume = activity.intensity(kIsolatedTrack);
      activity.advance(levels, 0.015f, true);
      const auto rapidResume = activity.intensity(kIsolatedTrack);
Original expression:
  QVERIFY2(rapidResume.left < beforeRapidResume.left * std::exp(-0.015f / 0.250f),
               "a rapid pause/resume must restart fast descent")

A026 | TrackActivityTest::clampedElapsedNeedsTicksWithoutMovement | src/checks/audio/tst_trackactivity.cpp:211
Disposition: MATCHED
Mapping/reason: S026: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
  }

  void TrackActivityTest::clampedElapsedNeedsTicksWithoutMovement()
  {
      TrackActivity activity;
      TrackActivityLevels levels{};
      levels[kIsolatedTrack] = {255, 255};
Original expression:
  QVERIFY2(activity.advance(levels, -1.0f, true),
               "playing must require ticks with negative elapsed")

A027 | TrackActivityTest::clampedElapsedNeedsTicksWithoutMovement | src/checks/audio/tst_trackactivity.cpp:213
Disposition: MATCHED
Mapping/reason: S027: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(dark(activity.intensity(kIsolatedTrack)), "negative elapsed must not move activity")

A028 | TrackActivityTest::clampedElapsedNeedsTicksWithoutMovement | src/checks/audio/tst_trackactivity.cpp:215
Disposition: MATCHED
Mapping/reason: S028: identical original policy scenario, stereo input, elapsed time, loop range and tolerance; see exact Swift predicate below.
Source context since previous assertion:
  ;
      activity.advance(levels, 0.0f, true);
Original expression:
  QVERIFY2(dark(activity.intensity(kIsolatedTrack)), "zero elapsed must not move activity")

Remaining original source (closing sequence and helper bodies):
  ;
  }

  } // namespace checks

  int runTrackActivityCheck(const QStringList &qtArguments)
  {
      checks::TrackActivityTest test;
      QStringList arguments{QStringLiteral("trackactivitycheck")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }


Swift assertion predicates

S001 | dark | src/checks/audio/tst_trackactivity.swift:10
  report.expect(dark(activity.intensity(track: track)), cppID: fresh, message: "new track is dark")

S002 | dark | src/checks/audio/tst_trackactivity.swift:12
  report.expect(dark(activity.intensity(track: -1)), cppID: fresh, message: "negative track is dark")

S003 | dark | src/checks/audio/tst_trackactivity.swift:13
  report.expect(dark(activity.intensity(track: 16)), cppID: fresh, message: "high track is dark")

S004 | dark | src/checks/audio/tst_trackactivity.swift:20
  report.expect(activity.advance(levels, elapsedSeconds: 0.017, playing: true),
                        cppID: id, message: "playing requires another tick")

S005 | approaches | src/checks/audio/tst_trackactivity.swift:26
  report.expect(approaches(first.left, left), cppID: id, message: "left attacks without snapping")

S006 | approaches | src/checks/audio/tst_trackactivity.swift:27
  report.expect(approaches(first.right, right), cppID: id, message: "right attacks without snapping")

S007 | approaches | src/checks/audio/tst_trackactivity.swift:28
  report.expect(left > right ? first.left > first.right : first.right > first.left,
                        cppID: id, message: "stereo sides retain independent targets")

S008 | approaches | src/checks/audio/tst_trackactivity.swift:31
  report.expect(dark(activity.intensity(track: track)), cppID: id, message: "other tracks remain dark")

S009 | approaches | src/checks/audio/tst_trackactivity.swift:42
  report.expect(current.left > previous.left * 0.5 && current.right > previous.right * 0.5,
                    cppID: release, message: "release remains visibly gradual")

S010 | approaches | src/checks/audio/tst_trackactivity.swift:48
  report.expect(current.left <= previous.left && current.right <= previous.right,
                        cppID: release, message: "both sides decay monotonically")

S011 | approaches | src/checks/audio/tst_trackactivity.swift:52
  report.expect(dark(activity.intensity(track: 3)), cppID: release, message: "release settles at zero")

S012 | approaches | src/checks/audio/tst_trackactivity.swift:59
  report.expect(dark(activity.intensity(track: 3)), cppID: "TrackActivityTest::imperceptibleTailSnapsToZero",
                    message: "visible floor removes imperceptible tail")

S013 | approaches | src/checks/audio/tst_trackactivity.swift:72
  report.expect(retriggered.left > decayed.left && retriggered.right > decayed.right,
                    cppID: retrigger, message: "new peak retriggers independently")

S014 | approaches | src/checks/audio/tst_trackactivity.swift:74
  report.expect(retriggered.left < 224 / Float(255) && retriggered.right < 64 / Float(255),
                    cppID: retrigger, message: "retrigger retains attack inertia")

S015 | approaches | src/checks/audio/tst_trackactivity.swift:82
  report.expect((0..<16).allSatisfy { dark(activity.intensity(track: $0)) },
                    cppID: "TrackActivityTest::resetClearsEveryLight", message: "reset clears every light")

S016 | approaches | src/checks/audio/tst_trackactivity.swift:90
  report.expect(activity.advance(zero, elapsedSeconds: 0.015, playing: false), cppID: paused,
                    message: "paused fill requires another tick")

S017 | approaches | src/checks/audio/tst_trackactivity.swift:93
  report.expect(filling.left > beforePause.left && filling.right > beforePause.right,
                    cppID: paused, message: "paused fill gently approaches full brightness")

S018 | approaches | src/checks/audio/tst_trackactivity.swift:95
  report.expect(!activity.advance(zero, elapsedSeconds: 10, playing: false), cppID: paused,
                    message: "settled paused fill stops ticks")

S019 | approaches | src/checks/audio/tst_trackactivity.swift:99
  report.expect(settled.left == 1 && settled.right == 1, cppID: paused,
                        message: "all sides settle at full brightness")

S020 | approaches | src/checks/audio/tst_trackactivity.swift:102
  report.expect(!activity.advance(zero, elapsedSeconds: 0, playing: false), cppID: paused,
                    message: "settled pause remains settled")

S021 | approaches | src/checks/audio/tst_trackactivity.swift:108
  report.expect(activity.advance(zero, elapsedSeconds: 0.075, playing: true), cppID: resume,
                    message: "resume requires ticks")

S022 | approaches | src/checks/audio/tst_trackactivity.swift:111
  report.expect(abs(resumed.left - exp(-0.075 / Float(0.015))) <= 0.0001, cppID: resume,
                    message: "resume descends through 75ms fast window")

S023 | approaches | src/checks/audio/tst_trackactivity.swift:115
  report.expect(abs(ordinary.left - resumed.left * exp(-0.015 / Float(0.250))) <= 0.0001,
                    cppID: resume, message: "release returns to 250ms")

S024 | approaches | src/checks/audio/tst_trackactivity.swift:117
  report.expect(ordinary.left > resumed.left * exp(-0.015 / Float(0.015)), cppID: resume,
                    message: "ordinary release does not retain fast descent")

S025 | approaches | src/checks/audio/tst_trackactivity.swift:126
  report.expect(activity.intensity(track: 3).left < beforeRapidResume.left * exp(-0.015 / Float(0.250)),
                    cppID: "TrackActivityTest::rapidPauseRearmsFastDescent", message: "rapid pause restarts descent")

S026 | approaches | src/checks/audio/tst_trackactivity.swift:133
  report.expect(activity.advance(levels, elapsedSeconds: -1, playing: true), cppID: elapsed,
                    message: "negative elapsed still requires ticks")

S027 | approaches | src/checks/audio/tst_trackactivity.swift:135
  report.expect(dark(activity.intensity(track: 3)), cppID: elapsed, message: "negative elapsed does not move")

S028 | approaches | src/checks/audio/tst_trackactivity.swift:137
  report.expect(dark(activity.intensity(track: 3)), cppID: elapsed, message: "zero elapsed does not move")
