Assertion correspondence: drawerpresentation/valueprompt.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/drawerpresentation/valueprompt.cpp
Original SHA-256: fe22f6c7cc53ff6c156e216811a8e2d45196b6518017a165a687212a213ea8f0
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Swift SHA-256: 3579cf4cbc5c7a00b79a00411404a30e1e835ba5a242d42260f3d9aca3f77abd
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files

Status and evidence
The C++ file above was deleted; this inventory audits its parent-revision content (pinned by Reference revision and SHA-256) so no assertion is dropped during the Swift check migration. This is an assertion-site inventory, not a count-based equivalence claim. Every original macro/failure site appears below, including fixture guards. Source context preserves setup, rows, loops, and guards between assertions verbatim. S identifiers identify exact Swift predicates in the paired numeric-prompt transaction, listed after the inventory. MATCHED identifies equivalent model predicates; execution must still be verified. PARTIAL identifies related Swift predicates with an explicit unproved condition. NATIVE identifies retained native-window/rendering or native setup obligations; it does not establish coverage of any converted window. GAP identifies an original predicate without a replacement assertion. Same-directory valueprompt.swift does not exist; the identified prompt-behavior replacement is the numeric-prompt transaction in localinputtier_text.swift, which opens, drafts, and cancels but never commits. Commit writes, clamp outcomes, chrome strings, and focus observations therefore stay GAP or NATIVE. Unsure between PARTIAL and GAP was resolved to GAP. Native passes do not prove the Swift replacement.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: pending integrated run; source hashes pin the tree inspected here.
Converted-window command: deno task verify:qml --verbose
The deleted C++ drawer harness is not registered in this checkout; the drawer QML lane covers the converted drawer window, not the deleted cases inventoried here.

Execution ownership
drawerOriginalNumericPromptTransaction in src/checks/selectionkey/localinputtier_text.swift executes the paired prompt checks; it is called by the swiftcore session suite (SessionChecks.swift, alongside runAutomationPageChecks). The drawerAutomationAutomationFixture struct (swiftcore/AutomationPageChecks.swift) builds the document/session/page used by that transaction and carries no report predicates of its own, so no Shared helper section applies. The transaction also carries a report.fail path for fixture-note creation failure, which short-circuits before any predicate; no deleted C++ site corresponds to it.

Fixture, sequence and row constraints
Insertion constants kNodeTick (node tick) and kEmptyTick (empty plot tick) anchor every case. The tempo case runs open, commit, and two clamp rows (kNodeTick+8 ceiling, kNodeTick+16 floor). The CC case runs insertion commit, node-entry display round-trip on both offset ends (displayed -64 and 63 typed back to stored 0 and 127). Escape, focus-loss, document-change, deactivation, and page-hide rows each expand the same open site into a distinct cancel trigger; the late-accept row reuses the closed-prompt site. QTRY_VERIFY/qWaitFor guards poll chrome visibility; revision, undo-index, and snapshot guards expand one site into per-stage document comparisons.
Original setup, branch guards and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:117
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  // Inline Quick value prompt coverage for the automation drawer (drawer plan
  // Cleanup phase 4): the Tempo and CC prompt families behind NodeLane publish
  // through DrawerChrome, both canvas entry points open without writing, Enter
  // commits the displayed value through the stored offset, and Escape, focus
  // loss, document change, window deactivation, page hide, and a late accept all
  // resolve the pending edit without touching the song.

  #include "checks/drawerpresentation/tst_drawerpresentation.h"

  #include <QtTest>

  #include <algorithm>
  #include <cmath>
  #include <cstdint>

  #include <QCoreApplication>
  #include <QEvent>
  #include <QKeySequence>
  #include <QQuickItem>
  #include <QQuickWindow>

  #include "checks/automation/automationvalueprompt.h"
  #include "checks/drawerpresentation/fixtures.h"
  #include "checks/selectionkey/automationprobe.h"
  #include "checks/support/timelinequickcheck.h"
  #include "core/timedefaults.h"
  #include "ui/editordrawer/automationcanvas.h"
  #include "ui/editordrawer/automationpage.h"
  #include "ui/editordrawer/drawerchrome.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/editordrawer/nodelane/nodelane.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"

  using namespace checks::drawerpresentation;

  namespace {

  constexpr uint8_t kController = 10;
  constexpr Tick kNodeTick = 24;
  constexpr Tick kEmptyTick = 96;

  Snapshot snapshot(SongDocument &document)
  {
      return {document.smf().write(), document.revision(), document.undoStack()->index()};
  }

  AutomationCanvas *canvasOf(const DrawerFixture &fixture)
  {
      return fixture.view->editorDrawer()->automationPage()->canvas();
  }

  songview::TimelineInputItem *automationInput(const DrawerFixture &fixture)
  {
      return fixture.quickRoot->findChild<songview::TimelineInputItem *>(
          QStringLiteral("timelineAutomationInput"));
  }

  LaneHandle ccLaneHandle(const AutomationCanvas &canvas)
  {
      const auto &rows = canvas.rows();
      for (int index = 0; index < int(rows.size()); ++index) {
          if (rows[std::size_t(index)].id ==
              EditorAutomationRowId{EditorAutomationRowKind::ControlChange, 0, kController})
              return LaneHandle{index};
      }
      return {};
  }

  // Parameter selection is a real click on the live visual label; node probes
  // only project the already-active lane's geometry.
  bool activateParameter(DrawerFixture &fixture, AutomationCanvas &canvas,
                         const EditorAutomationRowId &row)
  {
      const int index = checks::support::automationParameterIndex(canvas, row);
      if (index < 0 ||
          !checks::support::clickVisibleTab(fixture.quickRoot,
                                            QStringLiteral("automationParameterTab%1").arg(index))) {
          return false;
      }
      return QTest::qWaitFor([&canvas, index] { return canvas.activeParameter() == index; });
  }

  const TempoPoint *tempoPointAt(const SongDocument &document, Tick tick)
  {
      const auto &points = document.tempoPoints();
      const auto found = std::find_if(points.cbegin(), points.cend(),
                                      [tick](const TempoPoint &point) { return point.tick == tick; });
      return found == points.cend() ? nullptr : &*found;
  }

  int tempoBpmAt(const SongDocument &document, Tick tick)
  {
      const TempoPoint *const point = tempoPointAt(document, tick);
      Q_ASSERT(point);
      return int(std::lround(CoreTimeDefaults::tempoBpm(point->microsecondsPerQuarterNote)));
  }

  // Opens the Set-Value prompt on an existing CC node and waits until the chrome
  // publishes it.
  bool openCcNodePrompt(DrawerFixture &fixture, LaneHandle cc, Tick tick, int storedValue)
  {
      AutomationCanvas *const canvas = canvasOf(fixture);
      if (!canvas || !canvas->openValuePromptForNode(cc, NodePoint{tick, storedValue}))
          return false;
      DrawerChrome &chrome = fixture.chrome();
      return QTest::qWaitFor([&chrome] { return automation_valueprompt::promptVisible(chrome); });
  }

  } // namespace

  void DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp()
  {
      DrawerFixture fixture;
      QString error;

Original expression:
  QVERIFY2(fixture.create(error), qUtf8Printable(error))

A002 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:120
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      DrawerChrome &chrome = fixture.chrome();
      AutomationCanvas *const canvas = canvasOf(fixture);

Original expression:
  QVERIFY(canvas)

A003 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:122
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      QQuickWindow *const window = fixture.quick->quickWindow();

Original expression:
  QVERIFY(window)

A004 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:124
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      auto *const input = automationInput(fixture);

Original expression:
  QVERIFY(input)

A005 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:131
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;
      SongDocument &document = fixture.tab->document();

      // The insertion entry point publishes the BPM domain with the displayed
      // insertion value and writes nothing by opening.
      const uint64_t revision = document.revision();
      const int undoIndex = document.undoStack()->index();

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(LaneHandle{0}, kNodeTick, 140))

A006 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:132
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML chrome visibility flag versus the Swift presenter promptOpen flag.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

A007 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:133
Disposition: PARTIAL
Mapping/reason: Related predicates S004, S008 in src/checks/selectionkey/localinputtier_text.swift (draft editing preserves snapshot and full song state); unproved: different document/surface and QML chrome path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision)

A008 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:134
Disposition: PARTIAL
Mapping/reason: Related predicates S004, S008 in src/checks/selectionkey/localinputtier_text.swift (draft editing preserves snapshot and full song state); unproved: different document/surface and QML chrome path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoIndex)

A009 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:135
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptTitle(), QStringLiteral("Set tempo"))

A010 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:136
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptLabel(), QStringLiteral("BPM:"))

A011 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:137
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptMinimum(), CoreTimeDefaults::kMinTempoBpm)

A012 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:138
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptMaximum(), CoreTimeDefaults::kMaxTempoBpm)

A013 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:139
Disposition: PARTIAL
Mapping/reason: Related predicates S002, S003 in src/checks/selectionkey/localinputtier_text.swift (prompt opens with value 64; draft retained); unproved: displayed-unit offset mapping and differing tick/value.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptInitialValue(), 140)

A014 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:144
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      // Enter commits the typed displayed value as the stored BPM and hands
      // focus back to the automation input.
      QQuickItem *const prompt = automation_valueprompt::focusedTextInput(*window);

Original expression:
  QVERIFY2(prompt, "the tempo prompt did not take active focus")

A015 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:145
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(prompt->property("selectedText").toString(), QStringLiteral("140"))

A016 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:148
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      QTest::keySequence(window, QKeySequence(Qt::Key_9, Qt::Key_0));
      QTest::keyClick(window, Qt::Key_Return);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A017 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:149
Disposition: MATCHED
Mapping/reason: Equivalent predicate S011 in src/checks/selectionkey/localinputtier_text.swift:249 — one tempo acceptance advances the revision once through the production AutomationPage prompt.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision + 1)

A018 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:150
Disposition: MATCHED
Mapping/reason: Equivalent predicate S022 in src/checks/selectionkey/localinputtier_text.swift:276 — one tempo acceptance records one history entry via coreEditHistoryCountAtTip public undo/redo traversal.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoIndex + 1)

A019 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:151
Disposition: MATCHED
Mapping/reason: Equivalent predicate S012 in src/checks/selectionkey/localinputtier_text.swift:251 — the committed tempo point reads 90 BPM at tick 24.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(tempoBpmAt(document, kNodeTick), 90)

A020 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:152
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::inputOwnsFocus(*window, *input))

A021 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:155
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      // Acceptance clamps out-of-domain displayed values into the lane limits.

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(LaneHandle{0}, kNodeTick + 8, 120))

A022 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:156
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML chrome visibility flag versus the Swift presenter promptOpen flag.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

A023 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:158
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      canvas->acceptNodeValuePrompt(CoreTimeDefaults::kMaxTempoBpm + 1000);

Original expression:
  QVERIFY(!automation_valueprompt::promptVisible(chrome))

A024 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:159
Disposition: MATCHED
Mapping/reason: Equivalent predicate S015 in src/checks/selectionkey/localinputtier_text.swift:257 — the over-maximum acceptance clamps to the maximum BPM at tick 32.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(tempoBpmAt(document, kNodeTick + 8), CoreTimeDefaults::kMaxTempoBpm)

A025 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:160
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(LaneHandle{0}, kNodeTick + 16, 120))

A026 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:161
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML chrome visibility flag versus the Swift presenter promptOpen flag.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

A027 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:163
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      canvas->acceptNodeValuePrompt(CoreTimeDefaults::kMinTempoBpm - 1000);

Original expression:
  QVERIFY(!automation_valueprompt::promptVisible(chrome))

A028 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:164
Disposition: MATCHED
Mapping/reason: Equivalent predicate S018 in src/checks/selectionkey/localinputtier_text.swift:263 — the under-minimum acceptance clamps to the minimum BPM at tick 40.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(tempoBpmAt(document, kNodeTick + 16), CoreTimeDefaults::kMinTempoBpm)

A029 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:165
Disposition: MATCHED
Mapping/reason: Equivalent predicate S019 in src/checks/selectionkey/localinputtier_text.swift:265 — three tempo acceptances advance the revision three times.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision + 3)

A030 | DrawerPresentationTest::valuePromptTempoLimitsAcceptAndClamp | src/checks/drawerpresentation/valueprompt.cpp:166
Disposition: MATCHED
Mapping/reason: Equivalent predicate S027 in src/checks/selectionkey/localinputtier_text.swift:291 — three tempo acceptances record three history entries via coreEditHistoryCountAtTip traversal.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoIndex + 3)

A031 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:173
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
  }

  void DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit()
  {
      DrawerFixture fixture;
      QString error;

Original expression:
  QVERIFY2(fixture.create(error), qUtf8Printable(error))

A032 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:176
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      DrawerChrome &chrome = fixture.chrome();
      AutomationCanvas *const canvas = canvasOf(fixture);

Original expression:
  QVERIFY(canvas)

A033 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:178
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      QQuickWindow *const window = fixture.quick->quickWindow();

Original expression:
  QVERIFY(window)

A034 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:182
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      SongDocument &document = fixture.tab->document();
      document.addLanePoint(0, kController, kNodeTick, 64);
      pump();

Original expression:
  QVERIFY2(activateParameter(fixture, *canvas,
                                 {EditorAutomationRowKind::ControlChange, 0, kController}),
               "the Pan parameter label did not activate")

A035 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:186
Disposition: MATCHED
Mapping/reason: Equivalent predicate S028 in src/checks/selectionkey/localinputtier_text.swift:298 — the CC10 lane is present in the production page row stack.
Source context since previous assertion:
  ;
      const LaneHandle cc = ccLaneHandle(*canvas);

Original expression:
  QVERIFY(cc.valid())

A036 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:192
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      // Entry point B: the insertion entry point opens at an empty CC-10 plot
      // tick in displayed (stored-64) units and writes nothing.
      const uint64_t revision = document.revision();
      const int undoIndex = document.undoStack()->index();

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(cc, kEmptyTick, 64))

A037 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:193
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML chrome visibility flag versus the Swift presenter promptOpen flag.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

A038 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:194
Disposition: PARTIAL
Mapping/reason: Related predicates S004, S008 in src/checks/selectionkey/localinputtier_text.swift (draft editing preserves snapshot and full song state); unproved: different document/surface and QML chrome path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision)

A039 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:195
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptMinimum(), -64)

A040 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:196
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptMaximum(), 63)

A041 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:201
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;

      // Displayed 0 commits as the stored center 64 on the new insertion tick.
      QTest::keySequence(window, QKeySequence(Qt::Key_0));
      QTest::keyClick(window, Qt::Key_Return);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A042 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:202
Disposition: MATCHED
Mapping/reason: Equivalent predicate S031 in src/checks/selectionkey/localinputtier_text.swift:305 — one CC insertion advances the revision once.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision + 1)

A043 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:203
Disposition: MATCHED
Mapping/reason: Equivalent predicate S045 in src/checks/selectionkey/localinputtier_text.swift:338 — one CC insertion records one history entry via coreEditHistoryCountAtTip traversal.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoIndex + 1)

A044 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:205
Disposition: MATCHED
Mapping/reason: Equivalent predicate S032 in src/checks/selectionkey/localinputtier_text.swift:307 — the insertion leaves two lane points.
Source context since previous assertion:
  ;
      const auto points = document.lanePoints(0, kController);

Original expression:
  QCOMPARE(points.size(), std::size_t{2})

A045 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:207
Disposition: MATCHED
Mapping/reason: Equivalent predicate S033 in src/checks/selectionkey/localinputtier_text.swift:309 — displayed 0 commits as stored 64 through the pan center offset.
Source context since previous assertion:
  ;
      const DocLanePoint &inserted = points[0].tick == kNodeTick ? points[1] : points[0];

Original expression:
  QCOMPARE(inserted.value, 64)

A046 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:211
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      // The Set-Value entry point shows the same node in displayed units, and
      // the typed bounds round-trip through the stored offset on both ends.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, inserted.tick, 64))

A047 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:212
Disposition: PARTIAL
Mapping/reason: Related predicates S002, S003 in src/checks/selectionkey/localinputtier_text.swift (prompt opens with value 64; draft retained); unproved: displayed-unit offset mapping and differing tick/value.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptInitialValue(), 0)

A048 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:215
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      QTest::keySequence(window, QKeySequence(Qt::Key_Minus, Qt::Key_6, Qt::Key_4));
      QTest::keyClick(window, Qt::Key_Return);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A049 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:217
Disposition: MATCHED
Mapping/reason: Equivalent predicate S036 in src/checks/selectionkey/localinputtier_text.swift:315 — the lowered node is found at tick 96 after displayed -64 commits.
Source context since previous assertion:
  ;
      DocLanePoint point;

Original expression:
  QVERIFY(document.findLanePoint(0, kController, inserted.tick, &point))

A050 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:218
Disposition: MATCHED
Mapping/reason: Equivalent predicate S037 in src/checks/selectionkey/localinputtier_text.swift:317 — displayed -64 stores 0 through the pan offset.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 0)

A051 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:219
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, inserted.tick, 0))

A052 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:220
Disposition: PARTIAL
Mapping/reason: Related predicates S002, S003 in src/checks/selectionkey/localinputtier_text.swift (prompt opens with value 64; draft retained); unproved: displayed-unit offset mapping and differing tick/value.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.valuePromptInitialValue(), -64)

A053 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:223
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      QTest::keySequence(window, QKeySequence(Qt::Key_6, Qt::Key_3));
      QTest::keyClick(window, Qt::Key_Return);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A054 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:224
Disposition: MATCHED
Mapping/reason: Equivalent predicate S040 in src/checks/selectionkey/localinputtier_text.swift:323 — the raised node is found at tick 96 after displayed 63 commits.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(document.findLanePoint(0, kController, inserted.tick, &point))

A055 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:225
Disposition: MATCHED
Mapping/reason: Equivalent predicate S041 in src/checks/selectionkey/localinputtier_text.swift:325 — displayed 63 stores 127 through the pan offset.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 127)

A056 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:226
Disposition: MATCHED
Mapping/reason: Equivalent predicate S042 in src/checks/selectionkey/localinputtier_text.swift:327 — three CC acceptances advance the revision three times.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), revision + 3)

A057 | DrawerPresentationTest::valuePromptCcCenterOffsetInsertionCommit | src/checks/drawerpresentation/valueprompt.cpp:227
Disposition: MATCHED
Mapping/reason: Equivalent predicate S050 in src/checks/selectionkey/localinputtier_text.swift:353 — three CC acceptances record three history entries via coreEditHistoryCountAtTip traversal.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoIndex + 3)

A058 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:234
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
  }

  void DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus()
  {
      DrawerFixture fixture;
      QString error;

Original expression:
  QVERIFY2(fixture.create(error), qUtf8Printable(error))

A059 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:237
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      DrawerChrome &chrome = fixture.chrome();
      QQuickWindow *const window = fixture.quick->quickWindow();

Original expression:
  QVERIFY(window)

A060 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:239
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      auto *const input = automationInput(fixture);

Original expression:
  QVERIFY(input)

A061 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:245
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      SongDocument &document = fixture.tab->document();
      document.addLanePoint(0, kController, kNodeTick, 64);
      pump();

      auto *const canvas = canvasOf(fixture);

Original expression:
  QVERIFY(canvas)

A062 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:246
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(activateParameter(fixture, *canvas,
                                 {EditorAutomationRowKind::ControlChange, 0, kController}),
               "the Pan parameter label did not activate")

A063 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:250
Disposition: MATCHED
Mapping/reason: Equivalent predicate S051 in src/checks/selectionkey/localinputtier_text.swift:360 — the CC10 lane is present in the escape-fixture production page row stack.
Source context since previous assertion:
  ;
      const LaneHandle escapeCc = ccLaneHandle(*canvas);

Original expression:
  QVERIFY(escapeCc.valid())

A064 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:253
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      const Snapshot before = snapshot(document);

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(escapeCc, kEmptyTick, 64))

A065 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:254
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML chrome visibility flag versus the Swift presenter promptOpen flag.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

A066 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:256
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      QQuickItem *const prompt = automation_valueprompt::focusedTextInput(*window);

Original expression:
  QVERIFY2(prompt, "the insertion prompt did not take active focus")

A067 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:259
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      QTest::keyClick(window, Qt::Key_Escape);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A068 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:260
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), before)

A069 | DrawerPresentationTest::valuePromptEscapeCancelsAndReturnsFocus | src/checks/drawerpresentation/valueprompt.cpp:261
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::inputOwnsFocus(*window, *input))

A070 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:268
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
  }

  void DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel()
  {
      DrawerFixture fixture;
      QString error;

Original expression:
  QVERIFY2(fixture.create(error), qUtf8Printable(error))

A071 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:271
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      DrawerChrome &chrome = fixture.chrome();
      QQuickWindow *const window = fixture.quick->quickWindow();

Original expression:
  QVERIFY(window)

A072 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:273
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      auto *const input = automationInput(fixture);

Original expression:
  QVERIFY(input)

A073 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:278
Disposition: MATCHED
Mapping/reason: Equivalent predicate S052 in src/checks/selectionkey/localinputtier_text.swift:364 — the CC10 lane is present in the focus-fixture production page row stack.
Source context since previous assertion:
  ;
      SongDocument &document = fixture.tab->document();
      document.addLanePoint(0, kController, kNodeTick, 64);
      pump();
      const LaneHandle cc = ccLaneHandle(*canvasOf(fixture));

Original expression:
  QVERIFY(cc.valid())

A074 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:283
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;
      const Snapshot before = snapshot(document);

      // Focus loss cancels: forcing the roll band to take active focus closes
      // the pending prompt without a write.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, kNodeTick, 64))

A075 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:286
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      auto *const roll = fixture.quickRoot->findChild<songview::TimelineInputItem *>(
          QStringLiteral("timelineRollInput"));

Original expression:
  QVERIFY(roll)

A076 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:288
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      roll->forceActiveFocus(Qt::OtherFocusReason);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A077 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:289
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), before)

A078 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:293
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      // A document change during the prompt cancels it: the only document delta
      // is the added point, and the pending node edit never commits.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, kNodeTick, 64))

A079 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:296
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      document.addLanePoint(0, kController, kNodeTick + 24, 32);
      pump();

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A080 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:298
Disposition: MATCHED
Mapping/reason: Equivalent predicate S055 in src/checks/selectionkey/localinputtier_text.swift:374 — the original node is found at tick 24 after the document change cancels the pending prompt.
Source context since previous assertion:
  ;
      DocLanePoint point;

Original expression:
  QVERIFY(document.findLanePoint(0, kController, kNodeTick, &point))

A081 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:299
Disposition: MATCHED
Mapping/reason: Equivalent predicate S056 in src/checks/selectionkey/localinputtier_text.swift:376 — the cancelled prompt keeps the original value 64.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 64)

A082 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:300
Disposition: MATCHED
Mapping/reason: Equivalent predicate S057 in src/checks/selectionkey/localinputtier_text.swift:378 — the added point is found at tick 48.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(document.findLanePoint(0, kController, kNodeTick + 24, &point))

A083 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:301
Disposition: MATCHED
Mapping/reason: Equivalent predicate S058 in src/checks/selectionkey/localinputtier_text.swift:380 — the added point stores 32.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 32)

A084 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:302
Disposition: MATCHED
Mapping/reason: Equivalent predicate S059 in src/checks/selectionkey/localinputtier_text.swift:382 — the lane holds exactly the original plus the added point.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.lanePoints(0, kController).size(), std::size_t{2})

A085 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:303
Disposition: MATCHED
Mapping/reason: Equivalent predicate S060 in src/checks/selectionkey/localinputtier_text.swift:384 — the document change alone advances the revision once; the pending edit never commits.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.revision(), before.revision + 1)

A086 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:304
Disposition: MATCHED
Mapping/reason: Equivalent predicate S061 in src/checks/selectionkey/localinputtier_text.swift:388 — the document change records one history entry via coreEditHistoryCountAtTip traversal.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), before.undoIndex + 1)

A087 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:308
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;
      const Snapshot afterLaneGrew = snapshot(document);

      // Quick-window deactivation cancels without a write.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, kNodeTick, 64))

A088 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:311
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      QEvent deactivation(QEvent::WindowDeactivate);
      QCoreApplication::sendEvent(window, &deactivation);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A089 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:312
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), afterLaneGrew)

A090 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:315
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;

      // Hiding the automations page cancels without a write.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, kNodeTick, 64))

A091 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:317
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      fixture.view->setDrawerSectionVisible(EditorDrawerPage::Automations, false);

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A092 | DrawerPresentationTest::valuePromptFocusLossDocumentChangeAndPageHideCancel | src/checks/drawerpresentation/valueprompt.cpp:318
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), afterLaneGrew)

A093 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:325
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
  }

  void DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing()
  {
      DrawerFixture fixture;
      QString error;

Original expression:
  QVERIFY2(fixture.create(error), qUtf8Printable(error))

A094 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:328
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      DrawerChrome &chrome = fixture.chrome();
      QQuickWindow *const window = fixture.quick->quickWindow();

Original expression:
  QVERIFY(window)

A095 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:330
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      auto *const input = automationInput(fixture);

Original expression:
  QVERIFY(input)

A096 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:335
Disposition: MATCHED
Mapping/reason: Equivalent predicate S062 in src/checks/selectionkey/localinputtier_text.swift:395 — the CC10 lane is present in the cancel-fixture production page row stack.
Source context since previous assertion:
  ;
      SongDocument &document = fixture.tab->document();
      document.addLanePoint(0, kController, kNodeTick, 64);
      pump();
      const LaneHandle cc = ccLaneHandle(*canvasOf(fixture));

Original expression:
  QVERIFY(cc.valid())

A097 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:340
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift (the numeric prompt opens); unproved: AutomationCanvas QML insertion/node entry, lane/tick/value mapping, and native window delivery versus the Swift page prompt at tick 144 value 64.
Source context since previous assertion:
  ;
      const Snapshot before = snapshot(document);

      // The chrome cancel invokable - the Escape path's entry point - clears the
      // pending edit, writes nothing, and returns focus to the automation input.

Original expression:
  QVERIFY(openCcNodePrompt(fixture, cc, kNodeTick, 64))

A098 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:342
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      QMetaObject::invokeMethod(&chrome, "cancelNodeValuePrompt");

Original expression:
  QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

A099 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:343
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), before)

A100 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:344
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/drawerpresentation/valueprompt.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(automation_valueprompt::inputOwnsFocus(*window, *input))

A101 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:349
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/drawerpresentation/valueprompt.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;

      // A late accept with no pending edit is a no-op: no write and the prompt
      // stays closed.
      AutomationCanvas *const canvas = canvasOf(fixture);

Original expression:
  QVERIFY(canvas)

A102 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:351
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: native close trigger (Return commit, clamp accept, Escape, focus loss, deactivation, page hide, chrome invokable) versus page.cancelPrompt; any commit write itself is unproved.
Source context since previous assertion:
  ;
      canvas->acceptNodeValuePrompt(50);

Original expression:
  QVERIFY(!automation_valueprompt::promptVisible(chrome))

A103 | DrawerPresentationTest::valuePromptCancelAndLateAcceptWriteNothing | src/checks/drawerpresentation/valueprompt.cpp:352
Disposition: PARTIAL
Mapping/reason: Related predicates S007, S008 in src/checks/selectionkey/localinputtier_text.swift (cancel leaves snapshot and song state unchanged); unproved: different document/surface and native cancel path.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(snapshot(document), before)

Remaining original source (closing sequence and helper bodies):
  ;
  }


Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:23
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:34
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:37
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:39
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:41
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:44
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:45
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:47
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")

S009 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:245
  report.expect(tempoFixture.page.openPrompt(tick: 24, value: 140), cppID: tempoID,
                  message: "tempo insertion prompt opens at tick 24")

S010 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:247
  report.expect(tempoFixture.page.acceptPrompt(displayedValue: 90), cppID: tempoID,
                  message: "tempo prompt commits 90 BPM")

S011 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:249
  report.expectEqual(tempoBaseRevision + 1, tempoFixture.document.revision, cppID: tempoID,
                       what: "A017 one tempo acceptance advances the revision once")

S012 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:251
  report.expect(tempoFixture.tempoValues.contains("24:90"), cppID: tempoID,
                  message: "A019 committed tempo reads 90 BPM at tick 24")

S013 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:253
  report.expect(tempoFixture.page.openPrompt(tick: 32, value: 120), cppID: tempoID,
                  message: "tempo ceiling prompt opens at tick 32")

S014 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:255
  report.expect(tempoFixture.page.acceptPrompt(displayedValue: TimeDefaults.maximumTempoBPM + 1000),
                  cppID: tempoID, message: "over-maximum tempo acceptance commits")

S015 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:257
  report.expect(tempoFixture.tempoValues.contains("32:\(TimeDefaults.maximumTempoBPM)"), cppID: tempoID,
                  message: "A024 ceiling acceptance clamps to the maximum BPM")

S016 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:259
  report.expect(tempoFixture.page.openPrompt(tick: 40, value: 120), cppID: tempoID,
                  message: "tempo floor prompt opens at tick 40")

S017 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:261
  report.expect(tempoFixture.page.acceptPrompt(displayedValue: TimeDefaults.minimumTempoBPM - 1000),
                  cppID: tempoID, message: "under-minimum tempo acceptance commits")

S018 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:263
  report.expect(tempoFixture.tempoValues.contains("40:\(TimeDefaults.minimumTempoBPM)"), cppID: tempoID,
                  message: "A028 floor acceptance clamps to the minimum BPM")

S019 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:265
  report.expectEqual(tempoBaseRevision + 3, tempoFixture.document.revision, cppID: tempoID,
                       what: "A029 three tempo acceptances advance the revision three times")

S020 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:270
  report.expect(tempoDepthFixture.page.openPrompt(tick: 24, value: 140), cppID: tempoID,
                  message: "depth tempo prompt opens at tick 24")

S021 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:272
  report.expect(tempoDepthFixture.page.acceptPrompt(displayedValue: 90), cppID: tempoID,
                  message: "depth tempo prompt commits 90 BPM")

S022 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:276
  report.expectEqual(base + 1, afterFirst, cppID: tempoID,
                           what: "A018 one tempo acceptance records one history entry")

S023 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:281
  report.expect(tempoDepthFixture.page.openPrompt(tick: 32, value: 120), cppID: tempoID,
                  message: "depth tempo ceiling prompt opens at tick 32")

S024 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:283
  report.expect(tempoDepthFixture.page.acceptPrompt(displayedValue: TimeDefaults.maximumTempoBPM + 1000),
                  cppID: tempoID, message: "depth over-maximum tempo acceptance commits")

S025 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:285
  report.expect(tempoDepthFixture.page.openPrompt(tick: 40, value: 120), cppID: tempoID,
                  message: "depth tempo floor prompt opens at tick 40")

S026 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:287
  report.expect(tempoDepthFixture.page.acceptPrompt(displayedValue: TimeDefaults.minimumTempoBPM - 1000),
                  cppID: tempoID, message: "depth under-minimum tempo acceptance commits")

S027 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:291
  report.expectEqual(base + 3, afterAll, cppID: tempoID,
                           what: "A030 three tempo acceptances record three history entries")

S028 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:298
  report.expect(ccFixture.page.rows.contains { $0.parameter == ccFixture.panLane }, cppID: ccID,
                  message: "A035 CC10 lane present in the insertion fixture row stack")

S029 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:301
  report.expect(ccFixture.page.openPrompt(tick: 96, value: 64), cppID: ccID,
                  message: "CC insertion prompt opens at empty tick 96")

S030 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:303
  report.expect(ccFixture.page.acceptPrompt(displayedValue: 0), cppID: ccID,
                  message: "displayed 0 commits through the center offset")

S031 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:305
  report.expectEqual(ccBaseRevision + 1, ccFixture.document.revision, cppID: ccID,
                       what: "A042 one CC insertion advances the revision once")

S032 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:307
  report.expectEqual(2, ccFixture.lanePoints(ccFixture.panLane).count, cppID: ccID,
                       what: "A044 insertion leaves two lane points")

S033 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:309
  report.expectEqual(64, ccFixture.lanePoints(ccFixture.panLane).first { $0.tick == 96 }?.value ?? -1,
                       cppID: ccID, what: "A045 inserted point stores 64")

S034 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:311
  report.expect(ccFixture.page.openPrompt(tick: 96, value: 64), cppID: ccID,
                  message: "node prompt opens on the inserted point")

S035 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:313
  report.expect(ccFixture.page.acceptPrompt(displayedValue: -64), cppID: ccID,
                  message: "displayed -64 commits through the stored offset")

S036 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:315
  report.expect(ccFixture.lanePoints(ccFixture.panLane).contains { $0.tick == 96 }, cppID: ccID,
                  message: "A049 lowered node found at tick 96")

S037 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:317
  report.expectEqual(0, ccFixture.lanePoints(ccFixture.panLane).first { $0.tick == 96 }?.value ?? -1,
                       cppID: ccID, what: "A050 displayed -64 stores 0")

S038 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:319
  report.expect(ccFixture.page.openPrompt(tick: 96, value: 0), cppID: ccID,
                  message: "node prompt reopens on the lowered point")

S039 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:321
  report.expect(ccFixture.page.acceptPrompt(displayedValue: 63), cppID: ccID,
                  message: "displayed 63 commits through the stored offset")

S040 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:323
  report.expect(ccFixture.lanePoints(ccFixture.panLane).contains { $0.tick == 96 }, cppID: ccID,
                  message: "A054 raised node found at tick 96")

S041 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:325
  report.expectEqual(127, ccFixture.lanePoints(ccFixture.panLane).first { $0.tick == 96 }?.value ?? -1,
                       cppID: ccID, what: "A055 displayed 63 stores 127")

S042 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:327
  report.expectEqual(ccBaseRevision + 3, ccFixture.document.revision, cppID: ccID,
                       what: "A056 three CC acceptances advance the revision three times")

S043 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:332
  report.expect(ccDepthFixture.page.openPrompt(tick: 96, value: 64), cppID: ccID,
                  message: "depth CC insertion prompt opens at tick 96")

S044 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:334
  report.expect(ccDepthFixture.page.acceptPrompt(displayedValue: 0), cppID: ccID,
                  message: "depth displayed 0 commits through the center offset")

S045 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:338
  report.expectEqual(base + 1, afterFirst, cppID: ccID,
                           what: "A043 one CC insertion records one history entry")

S046 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:343
  report.expect(ccDepthFixture.page.openPrompt(tick: 96, value: 64), cppID: ccID,
                  message: "depth node prompt opens on the inserted point")

S047 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:345
  report.expect(ccDepthFixture.page.acceptPrompt(displayedValue: -64), cppID: ccID,
                  message: "depth displayed -64 commits")

S048 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:347
  report.expect(ccDepthFixture.page.openPrompt(tick: 96, value: 0), cppID: ccID,
                  message: "depth node prompt reopens on the lowered point")

S049 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:349
  report.expect(ccDepthFixture.page.acceptPrompt(displayedValue: 63), cppID: ccID,
                  message: "depth displayed 63 commits")

S050 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:353
  report.expectEqual(base + 3, afterAll, cppID: ccID,
                           what: "A057 three CC acceptances record three history entries")

S051 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:360
  report.expect(escapeFixture.page.rows.contains { $0.parameter == escapeFixture.panLane }, cppID: escapeID,
                  message: "A063 CC10 lane present in the escape fixture row stack")

S052 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:364
  report.expect(focusFixture.page.rows.contains { $0.parameter == focusFixture.panLane }, cppID: focusID,
                  message: "A073 CC10 lane present in the focus fixture row stack")

S053 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:368
  report.expect(focusFixture.page.openPrompt(tick: 24, value: 64), cppID: focusID,
                  message: "node prompt opens before the document change")

S054 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:372
  report.expect(!focusFixture.page.promptOpen, cppID: focusID,
                  message: "document change cancels the pending prompt")

S055 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:374
  report.expect(focusFixture.lanePoints(focusFixture.panLane).contains { $0.tick == 24 }, cppID: focusID,
                  message: "A080 original node found after the cancel")

S056 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:376
  report.expectEqual(64, focusFixture.lanePoints(focusFixture.panLane).first { $0.tick == 24 }?.value ?? -1,
                       cppID: focusID, what: "A081 cancelled prompt keeps the original value 64")

S057 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:378
  report.expect(focusFixture.lanePoints(focusFixture.panLane).contains { $0.tick == 48 }, cppID: focusID,
                  message: "A082 added point found at tick 48")

S058 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:380
  report.expectEqual(32, focusFixture.lanePoints(focusFixture.panLane).first { $0.tick == 48 }?.value ?? -1,
                       cppID: focusID, what: "A083 added point stores 32")

S059 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:382
  report.expectEqual(["24:64", "48:32"], focusFixture.values(focusFixture.panLane), cppID: focusID,
                       what: "A084 lane holds the original plus the added point")

S060 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:384
  report.expectEqual(focusBaseRevision + 1, focusFixture.document.revision, cppID: focusID,
                       what: "A085 document change alone advances the revision once")

S061 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:388
  report.expectEqual(base + 1, afterChange, cppID: focusID,
                           what: "A086 document change records one history entry")

S062 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:395
  report.expect(lateFixture.page.rows.contains { $0.parameter == lateFixture.panLane }, cppID: lateID,
                  message: "A096 CC10 lane present in the cancel fixture row stack")
