Assertion correspondence: editorqml/tst_Theme.qml

Reference revision: 03a190a0bf1bba3a007f31bf74eb01ed362b92ce
Original: src/checks/editorqml/tst_Theme.qml
Original SHA-256: 786dd987bae68ca30c9376f6dc47b0e7cae6ea04129021fb089e79a1aec7181e
Swift counterpart: src/checks/themecolor/ThemeColorChecks.swift
Additional Swift counterpart: src/checks/editorqml/ShellQmlTests.swift

Task 30 note (shell chrome typography and geometry): the Original SHA-256 above was re-pinned to the final src/checks/editorqml/tst_Theme.qml after task 30 added the mounted context-menu typography and padding assertions to test_menuChromeFollowsTheme (menu font family and size equal to the published body role, context-item weight inherited, context-item padding equal to layoutSpaces.one); the Reference revision above records the deleted C++ crosswalk revision and does not contain this QML counterpart, so this hash pins the counterpart file itself, as elsewhere in this corpus. The new predicates add observations to the chrome crosswalk bullet below and are indexed as S001-S004; this ledger still has no assertion sites. This bookkeeping pass re-ran only deno task proof check; no lane was re-run and no parser result is a semantic-parity claim.

Task 36 note (Swift-owned settings): task 36 re-pointed this entry's seeding and
read-back from a private native QtCore.Settings domain to the shared
PreferencesStore staged at an absolute plist path inside the entry's scratch
directory, and re-pointed the production restore it observes to
ShellPresenter.restoreAppearance over that same store; the dotted on-disk keys
theme.mode and theme.grid-line-contrast, the defaults and the canonical
write-back are unchanged, bootstrap.settingsApplicationName and its teardown are
deleted, and no check writes to ~/Library/Preferences. The Original SHA-256
above was therefore re-pinned to the final src/checks/editorqml/tst_Theme.qml
(the Reference revision above records the deleted C++ crosswalk revision and
does not contain this QML counterpart, as the task 30 note records). The four
test functions, their order and every message literal cited below are unchanged.

No assertion sites.

Status and evidence
This is a crosswalk for the shell-theme lane entry, not an original-site
inventory: it records which themelayout assertion sites the production
ShellWindow observes through the shared PreferencesStore and applied GridPalette
values. The inventoried sites and their dispositions live in
proof.tst_themelayout_color.txt, proof.tst_themelayout_chrome.txt and
proof.tst_themelayout_settings.txt; the Swift value predicates live in
src/checks/themecolor/ThemeColorChecks.swift (swiftcore suite 12).
Production surface: src/ui/shell/ShellWindow.qml (shell.configureSettings then
restoreAppearance restore and canonical writeback; Basic context-menu delegate
bindings, ShellWindow.qml) driven by ShellPresenter.restoreAppearance
and ShellAppearance.apply. The TestCase is src/checks/editorqml/tst_Theme.qml;
the lane entry is shell-theme in src/checks/editorqml/ShellQmlTests.swift
(inputFileName tst_Theme.qml, fixture mus_route101 staged but never opened).
Its standalone manifest stages an existing decomp-project scratch directory
through tools/run_checks.ts. No test-only production visual controls.

Verification
Command: deno task verify:shell --filter shell-theme --verbose
Result: PASS shell-theme (1/2 ok, 1 skipped); Finished testing of Theme.
Command: deno task verify:shell --filter shellwindow --verbose
Result: PASS shellwindow (1/2 ok, 1 skipped); Finished testing of ShellWindow.
Task 30 execution evidence: build/proof-evidence/shell-theme.json and build/proof-evidence/shellwindow.json record test_menuChromeFollowsTheme and test_chromeTypographyAndWindowGeometry executing on the frozen tree; the PASS results above are the earlier recorded runs and were not re-observed by this bookkeeping pass.
Task 36 execution evidence: build/proof-evidence/shell-theme.json (task-36 run) records test_themePersistence (three rows), test_themeRepair, test_gridContrastDirection and test_menuChromeFollowsTheme executing, and build/proof-evidence/shellwindow.json records test_bThemeRepairThroughProductionShell; the task-36 implementer reported the full shell lane green (26/26 entries). This bookkeeping pass re-ran only deno task proof check and did not re-observe those lanes.

Execution and ownership
The entry runs offscreen with the shared PreferencesStore staged at an absolute
plist path inside the entry's scratch directory, so no check writes to
~/Library/Preferences. Four test functions execute in order:
test_themePersistence (three mode rows), test_themeRepair, test_gridContrast-
Direction and test_menuChromeFollowsTheme. No songs open, so teardown destroys
the shell directly with no close-all walk.

Source-to-new-observation mapping (source predicate remains authoritative):

* tst_themelayout_color.cpp:282-303, ThemeLayoutTest::themePersistence.
  test_themePersistence seeds each mode (vanilla/50, dark-neutral-high/80,
  immaterial/100) through the shared PreferencesStore staged in the entry's
  scratch directory, mounts the production shell, and observes themeMode,
  gridLineContrast, the windowBackground
  literal, applied legibility floors (menu-bar and pressed pairs 4.5,
  disabled pair 1.3) and canonical settings writeback. Color proof
  A048-A050; settings-dialog commit direction A011-A012 (dark/80 row).
* tst_themelayout_color.cpp:249-267, ThemeLayoutTest::settingsRepair.
  Deliberately not duplicated: custom/#000000/#FFFFFF/80 stays covered by
  tst_ShellWindow.qml test_b (see proof.shellwindow.txt:50-56). This lane
  adds the complementary repair fixture only: test_themeRepair seeds empty
  mode and unparseable contrast and observes vanilla/50 with canonical
  writeback.
* tst_themelayout_color.cpp:160-207 gridContrast and
  tst_themelayout_settings.cpp:309-311 grid strength rules.
  test_gridContrastDirection drives the production restoreAppearance path
  (vanilla 0/100/50) and observes grid alpha and contrast movement plus
  pinned-value restoration. Color proof A032-A036; settings A039-A040.
* tst_themelayout_chrome.cpp:78-133 combo raster and :135-193 palette pins,
  user-observable halves. test_menuChromeFollowsTheme reads the production
  context-menu delegate's bound background/foreground, drives highlighted,
  and switches the theme, observing the item surface, the item-hover
  surface, the disabled ink and cross-theme propagation. Chrome proof
  A007 (hover fill), A012-A014 (value pins via swiftcore); color menu-alias
  sites A006-A010. Task 30 adds the mounted menu typography and padding
  observations to that same reading: menu.font family and pixelSize equal the
  published body role, the context item keeps the body weight, and the
  context-item padding equals layoutSpaces.one (S001-S004, tst_Theme.qml:206-210).
* tst_themelayout_settings.cpp:159-193 dialog commit direction.
  The dark/80 persistence row of test_themePersistence plus
  themeCommitPreviewRevertChecks in swiftcore observe the committed values;
  the dialog click mechanics have no Swift counterpart (GAP in the settings
  proof).

Explicit gaps (not silently counted as covered): theme dialog, wizard and
polyphony-panel surfaces (GAP in the settings proof, needs owner decision);
sample-editor legibility rows (PARTIAL in the color proof); the eleven-widget
grab sweep (PARTIAL in the chrome proof); native macOS MenuBar rendering,
which is platform-owned and unthemed by the Swift shell; audio, songs and
drawer behavior, which this song-less lane never stages.

Swift assertion predicates

S001 | test_menuChromeFollowsTheme | src/checks/editorqml/tst_Theme.qml
Anchor: message "grid context menu resolves body family"

S002 | test_menuChromeFollowsTheme | src/checks/editorqml/tst_Theme.qml
Anchor: message "grid context menu resolves body size"

S003 | test_menuChromeFollowsTheme | src/checks/editorqml/tst_Theme.qml
Anchor: message "context items inherit body weight"

S004 | test_menuChromeFollowsTheme | src/checks/editorqml/tst_Theme.qml
Anchor: message "context-item padding follows the One token"
