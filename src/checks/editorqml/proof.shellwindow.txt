ShellWindow Qt Quick Test provenance and verification gate

Production surface: src/ui/shell/ShellWindow.qml, with original
src/ui/songview/quick/swiftroll/SongTabs.qml, SongTab.qml, EditorSurface.qml,
TrackHeaderBand.qml and Velocity page. The TestCase is
src/checks/editorqml/tst_ShellWindow.qml; the Swift QTest bootstrap is
src/checks/editorqml/ShellQmlTests.swift. Its standalone shellwindow manifest
stages an existing decomp-project scratch directory through tools/run_checks.ts
with sound/song_table.inc, midi.cfg, mus_route101.mid, mus_littleroot_test.mid
and the original fixture_rich voices. No test-only production visual controls.
Current verified counterpart SHA-256:
  ShellQmlTests.swift:
    3daa910fabecf047d5190a7d928b360bcd981f1650315a5b02a1614197ab8d11
  tst_ShellWindow.qml:
    079c4223c573450e02552e0e16aff49466d5975fe9192e6419936d583c0c580a
  production ShellWindow.qml:
    7bc1beb5d972f2332428664c48a25c9e956fee7a97ce68c8239e093c53008f07

Original C++ source SHA-256, at the reference revisions recorded in each
named existing per-source assertion inventory (not hashes of this QML test):
  keyboard/keymapregistry.cpp:
    2f58e54dca9f1fa9ae9edd47eff3fea35bf2faddff5656412ec77469b828e749
  themelayout/tst_themelayout_color.cpp:
    a594fe67298004f6cbb8bc76f8dce1a2173b91b408a0b5b9b3414c78ec72fbaf
  mainwindowrouting/tst_mainwindowrouting_native.cpp:
    5cff3396fa2e9a6835a0b390cc988163cc2131ae382b898cbd1f14b2fc762d13
  mainwindowrouting/tst_mainwindowrouting_state.cpp:
    dafc9e19db3b921e7c33dd8e908f35028b4b3a6d9b23c22cbf4aa673ae2ecf32
  mainwindowrouting/tst_mainwindowrouting_lifecycle.cpp:
    8cbb6d5233d79b129a9bdf3aefa2030e44f879d81a99c91841e10504c1c0541f
  selectionkey/windowtier_keyboard.cpp (historical, since deleted):
    f00861f60ece3fc2b9de78b2c6b886d6139a71f79c7c07aeb5d1c8a69a5730d0
  selectionkey/localinputtier_text.cpp (historical, since deleted):
    80adaaa8ce5a7eddfa1a779243b266c24be61bd830b111f4fd65f4f6f0d8b3d6
The existing proof.<source>.txt inventories retain each reference revision,
original assertion-site ID, expanded rows, and unproved predicates; this
focused new lane is a crosswalk, not a replacement for their full inventories.

Source-to-new-observation mapping (source predicate remains authoritative):

* src/checks/keyboard/keymapregistry.cpp:11-37,
  KeymapCheckTest::keymapSettingsSeedsAreIgnored. test_aKeymapNativeSettingsSeeds
  writes all four original keymap/ seed keys through genuine QtCore.Settings,
  syncs, reads them back, and calls the complete public
  runKeybindingRegistryChecks(onAssertion:) inventory. Original keymap proof
  A001-A008 at keymapregistry.cpp:24-36 are matched by that callable inventory.
  The test host uses a fresh native application identity under the original
  sp3cker organization, then removes only that private domain at teardown.
  No production preference is seeded, snapshotted or restored.
* src/checks/themelayout/tst_themelayout_color.cpp:249-267,
  ThemeLayoutTest::settingsRepair. test_bThemeRepairThroughProductionShell
  writes custom/#000000/#FFFFFF/80 through real QtCore.Settings, then mounts
  production ShellWindow. It observes repaired ShellPresenter themeMode,
  gridLineContrast, canonical persisted mode, absent obsolete colour keys and
  persisted contrast. As in the original temporary-INI fixture, the store is
  private to this run.
  QtCore.Settings uses the genuine native backend, not an in-memory replacement.
* src/checks/mainwindowrouting/mainwindowroutingfixture.h:93-130,
  MainWindowRoutingFixture::openSession. openTwoSongShell opens mus_route101,
  then mus_littleroot_test, observes two tabs and selected second page, from
  the same staged two-song fixture. It copies the original velocity visible/
  173-height, automation hidden, voice-changes hidden and active velocity page
  preferences before constructing the shell. The original empty lane-state
  JSON is supplied as an ArrayBuffer/QByteArray; nullopt height keys are absent
  in the fresh native domain. No caller's drawer preferences supply the fixture.
* src/checks/mainwindowrouting/tst_mainwindowrouting_native.cpp:26-125,
  MainWindowRoutingNativeTest::nativeMenuAndWindowShortcutRouting. test_c
  opens the real Edit menu, selects one note in the second original song and
  delivers native StandardKey.Copy to the focused production roll. SignalSpy
  on the existing window Shortcut observes one activation. The test-only
  Swift clipboard reader uses production pd_clipboard_read and
  ClipboardCodec.decode to compare one track, one note, and that note's key
  against the selected pitch. It then clears the musical selection, creates
  a child TextField with the original text, focuses it, clears the previous
  clipboard clip and delivers Copy; pasting into the field observes the
  copied native text. A child non-text Item receives S twice, the original
  roll receives S twice, and the text field receives S once. The existing
  Solo Shortcut's activated SignalSpy observes counts 1, 2, 3, 4, 4 with
  corresponding true/false track Solo states. These are QML Shortcut signal
  counts, not claims about QWidget QAction pointer identity or Cocoa menus.
* src/checks/mainwindowrouting/tst_mainwindowrouting_state.cpp:210-261,
  MainWindowRoutingStateTest::editActionProjectionAndIdentity. test_c clicks
  each original tab button, checks the controller retargets the selected id,
  then checks the actual Edit-menu Copy/Solo items enabled against the second
  page's one-note selection. It does not claim native QAction pointer identity
  or the Time/Tracks grouping assertions.
* src/checks/selectionkey/proof.localinputtier_text.txt, original lines
  481-573: test_c opens the production Set Velocity prompt, types digits into
  its focused noteVelocityInput, sends native Copy/Paste and S to that field,
  observes local text ownership, no Solo, and native Space twice toggling the
  shared playback state while the prompt remains focused and text unchanged.
  Escape closes the original prompt. The production window Shortcut, not a
  synthetic test Shortcut, owns Space.
  Originals 555/566 observe playPause trigger counts; this lane observes the
  presenter's playing-state transitions instead, not native signal identity.
  It also verifies the production transport.play_pause sequence is Space.

Exact crosswalk limits: theme proof A041-A046
  (tst_themelayout_color.cpp:262-267) have analogous observable repaired
  values in test_b; its A040 scratch QTemporaryDir guard is not transplanted.
  A fresh native application domain provides equivalent fixture isolation while
  the new check deliberately exercises real QtCore.Settings.
  Native routing proof A002 (tst_mainwindowrouting_native.cpp:31) and state
  A067 (tst_mainwindowrouting_state.cpp:213) have analogous two-song opening
  evidence; native routing A004-A008 (:44-63) map to QML Edit-menu, actions,
  visibility and selected-note guards, not to QAction identity. Native routing
  A011 (:73), A021/A023/A026/A028/A030 (:109-125) are checked with the
  production Qt Quick Shortcut's activated SignalSpy, replacing QAction
  test machinery without asserting QWidget pointer identity. A019/A024
  (:106/:115) observe Qt Quick Item focus, not QApplication::focusWidget().
  A012-A015 (:75-78) decode the same native clipboard MIME through the
  production codec and check presence, one track, one note, and note key.
  No pasted-note surrogate counts as clipboard evidence. Native Cocoa
  active-window delivery A001/A003 and QWidget identity remain unproved.
  State proof A068-A085 (tst_mainwindowrouting_state.cpp:224-248) require
  QAction pointer identity, Time/Tracks ownership and explicit unbinding:
  this lane observes actual QML Edit action visibility/enabled state and tab
  retargeting only. Lifecycle proof A091-A109
  (tst_mainwindowrouting_lifecycle.cpp:350-389) concerns clean
  project-switch/reopen snapshots and remains GAP; it does not authorize
  a permanent dirty-window-close test.

Explicit gaps (not silently counted as covered): Cocoa menu-bar integration,
foreign-window focus, Shift/auto-repeat edge cases, keyboard focus traversal,
Time/Tracks submenu identity, clipboard MIME byte equality, widget-native
QAction triggered counts, tab reorder/load failures, audio output/device
quality, rollback/saving on the dirty dialog, alternate themes, and every
other original native assertion not named above. These are not converted by
this narrowly scoped offscreen shell lane; existing Swift headless and native
checks own their separate surfaces where still applicable. An offscreen Qt
Quick input result is not evidence of Cocoa's physical menu bar.

Temporary independent integration smoke (removed after observed pass):
In the controller's focused verify:shell run, QML StandardKey.Find was passed
to native QKeySequence.keyBindings; its nonempty combined strokes matched
production registry songs.find, whose source constant is Standard.find = 22
(src/swift/app/commands/KeybindingRegistry.swift:80). This confirms Qt's enum
without permanently adding a second keymap assertion inventory.

Temporary dirty-window-close integration smoke (removed after observed pass):
An earlier shell_qml_tests source exercised real edit, dirty Cancel/reclose,
Discard, grid detach and window hide. The controller's complete focused run
passed in 1.59 s build plus 0.94 s shellwindow, 1/1 harness and six Qt Quick
Test slots including init/cleanup. Its immutable, passed-source snapshot is
local://swift-keybindings-shell-smoke-passed.diff. The original C++ lifecycle
test instead switches a clean project and checks snapshots/SMF bytes, so the
dirty-close scenario was removed from permanent test_c/test_d assertions.

Temporary Loader FocusOut integration smoke (removed after A/B proof):
Original RewriteWindow.cpp:320-349 cancels roll input on scene FocusOut,
and rollcheck/pencil_velocity.cpp:322-361 observes rollback for a different
velocity-drag fixture. A temporary production QML note-move probe staged a
visible selected-note preview, retained it while focus moved between Loader
children, then moved focus to a real outside-Loader window child and released
the pointer. With the approved Loader.onActiveFocusChanged reason-0 hook,
the controller observed no note/document-revision/document-dirty mutation and
the original drawn note restored (three focused Qt Test slots PASS, 2.83 s
build, 0.67 s test, 0.68 s runner). The pre-fix root negative control
committed the note from tick 0 to 48 instead (artifact://180). The exact
passing source and approved root hook are frozen at
local://swift-keybindings-focus-smoke-passed.diff. This different move
sequence is smoke evidence, not a permanent native assertion match.

Temporary dark-menu/dialog integration smoke (removed after observed pass):
The complete shell lane passed after observing restored dark window color,
primary NativeText hints, empty hints for unbound actions, context-menu
background/hover roles, and the real song dialog's colors, two buttons,
untruncated original song label and Escape dismissal. Screenshots of both
surfaces were inspected. Build 5.66 s; shellwindow 0.86 s, runner 0.87 s.
The passed-source snapshot is local://swift-keybindings-menu-smoke-passed.diff.
QtBridge scalar notifications are already queued and automatically tracked;
the probe awaits delivery. Explicit hover enablement restores pointer
highlighting for the non-native controls under macOS style defaults.
This smoke is not counted as another original C++ assertion match.

Controller verification of the current permanent sources:
  deno task verify:shell --verbose --qt -v1
    PASS: all five Qt Test slots, zero failures; build 6.08 s,
    shellwindow 0.64 s, runner 0.65 s.
  deno task build:app
    PASS: the real Swift application entry and bundled QML bootstrap compile.
  deno task verify --filter swiftcore --verbose
    PASS: swiftcore 10.32 s, 1/24 harness OK, 23 intentionally filtered out;
    all 28 native-derived registry predicates use the canonical required sink.
  deno task verify:qml --verbose
    PASS: all four retained DPR/font reference profiles, 32.86 s,
    without rebaselining the editor visuals.
Missing shell_qml_tests fails resolution on supported macOS rather than
skipping. Physical Cocoa menu delivery is not claimed by these offscreen runs.
