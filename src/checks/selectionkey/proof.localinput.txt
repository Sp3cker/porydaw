Assertion correspondence: selectionkey/localinput.cpp

Reference revision: 996c6446f2cbfaa5b88ed721df4fa831129bd7bc
Original: src/checks/selectionkey/localinput.cpp
Original SHA-256: 0eabe04716106ce2fb03f89a6693752467dbb4e3194aeb4075954bda14dd5e2e
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Swift SHA-256: 6d6d5461a57ced89902dae8964f17b8f992c5932c0117428ee96eec7e9a5a853

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below. Source context preserves setup,
helpers, and lifecycle code between assertions verbatim. S identifiers identify
exact Swift predicates in the counterpart, listed after the inventory.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of any converted Swift window.
GAP identifies an original predicate without a replacement assertion.
This file holds only the SelectionLocalInputTierTest fixture: production-shell
lifecycle, text-binding/focus seams, clipboard and settings isolation, and the
clean-session close contract. Every site below observes native shell, window,
or harness state. The declared Swift counterpart exercises only the
numeric-prompt transaction (drawerOriginalNumericPromptTransaction); it has no
predicate for this native lifecycle, so no site here is MATCHED or PARTIAL.
The deleted localinputtier_text.cpp is covered separately by
proof.localinputtier_text.txt, which is out of scope for this file.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: pending integrated run; source hashes pin the tree inspected here.

Execution ownership
runSelectionKeyLocalInputCheck assembles SelectionLocalInputTierTest and
dispatches exactly one QTest::qExec; init() opens a fresh MainWindow session
and scratch song tab per case, and cleanup() ends the session through the
guarded production close path. No Swift entry point executes this native
lifecycle. drawerOriginalNumericPromptTransaction is called by
runProjectSessionSuite in src/checks/workspace/SessionChecks.swift; it builds a
drawerAutomationAutomationFixture (CC10 pan rows 48=32, 96=64) and drives the
production Swift AutomationPage prompt. That fixture struct, defined in
src/checks/automation/AutomationPageChecks.swift, carries no report predicates
of its own.

Fixture, sequence and row constraints
No data rows expand the sites below; init()/cleanup() run once per case across
every SelectionLocalInputTierTest slot (rename, search, numeric, velocity,
pitch-bend, event list). Teardown outcomes are computed first and asserted
last, so a failed assertion can never skip the remaining teardown.

Original assertion inventory and source sequence

A001 | SelectionLocalInputTierTest::activateShellForCommands | src/checks/selectionkey/localinput.cpp:57
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the shell-window existence precondition requires the native
production shell and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  // Qt Test fixture for the selectionkey protected-local-input tier: per-case
  // production-shell lifecycle (fresh MainWindow, scratch project, first song
  // tab), the shared text-binding/focus seams, clipboard and settings isolation,
  // and the clean-session close contract every case ends with. Scenario bodies
  // live in localinputtier_text.cpp, localinputtier_pitchbend.cpp, and
  // localinputtier_eventlist.cpp.

  #include "checks/fwd.hpp"
  #include "checks/selectionkey/tst_localinputtier.h"

  #include <QApplication>
  #include <QCloseEvent>
  #include <QGuiApplication>
  #include <QWidget>

  #include <utility>
  #include <vector>

  #include "checks/support/asyncwait.h"
  #include "ui/songview.h"

  SelectionLocalInputTierTest::SelectionLocalInputTierTest(QString projectRoot, QString songLabel)
      : m_projectRoot(std::move(projectRoot))
      , m_songLabel(std::move(songLabel))
  {}

  SongView &SelectionLocalInputTierTest::view() const
  {
      return m_tab->view();
  }

  SongDocument &SelectionLocalInputTierTest::document() const
  {
      return m_tab->document();
  }

  QQuickWindow *SelectionLocalInputTierTest::quickWindow() const
  {
      return m_quickWindow.data();
  }

  std::optional<QString> SelectionLocalInputTierTest::singleKeyText(const QKeyCombination &binding)
  {
      if (binding.keyboardModifiers() != Qt::NoModifier)
          return std::nullopt;
      const int key = int(binding.key());
      if (key >= int(Qt::Key_A) && key <= int(Qt::Key_Z))
          return QString(QChar::fromLatin1(char('a' + (key - int(Qt::Key_A)))));
      if (key >= int(Qt::Key_0) && key <= int(Qt::Key_9))
          return QString(QChar::fromLatin1(char('0' + (key - int(Qt::Key_0)))));
      return std::nullopt;
  }

  void SelectionLocalInputTierTest::activateShellForCommands()
  {
      QWidget *const shell = m_session.window.get();
Original expression:
  QVERIFY2(shell, "the view has no shell window for resumed commands")

A002 | SelectionLocalInputTierTest::activateShellForCommands | src/checks/selectionkey/localinput.cpp:64
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; Cocoa active-window arbitration requires the native shell and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      // Cocoa activation alone selects a key window without foregrounding the app.
      // A fresh shell must also reclaim the foreground after the prior case closes.
      shell->raise();
      shell->activateWindow();
      checks::async_wait::waitUntil([] { return true; }, [shell] { return shell->isActiveWindow(); },
                                    5000, 10);
Original expression:
  QVERIFY2(shell->isActiveWindow(),
               "the production shell did not become the active window for resumed commands")

A003 | SelectionLocalInputTierTest::init | src/checks/selectionkey/localinput.cpp:97
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the window-session open precondition is a native loader
precondition a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
  }

  QString SelectionLocalInputTierTest::applicationFocusState() const
  {
      return QStringLiteral("QWidget=%1 QGui-object=%2 QGui-window=%3")
          .arg(selectionkey::focusObjectIdentity(QApplication::focusWidget()),
               selectionkey::focusObjectIdentity(QGuiApplication::focusObject()),
               selectionkey::focusObjectIdentity(QGuiApplication::focusWindow()));
  }

  std::optional<SelectionLocalInputTierTest::NoteRef>
  SelectionLocalInputTierTest::addNote(int track, uint64_t tick, uint8_t key, uint32_t duration)
  {
      QString error;
      const std::vector<NoteId> inserted = selectionkey::insertIsolatedNotes(
          document(), track, {{Tick(tick), key, duration, 100}}, error);
      if (inserted.size() != 1)
          return std::nullopt;
      const std::optional<DocNote> note = selectionkey::noteById(document(), inserted.front());
      if (!note)
          return std::nullopt;
      return NoteRef{inserted.front(), *note};
  }

  void SelectionLocalInputTierTest::init()
  {
      m_counts = {};
      m_settings = std::make_unique<selectionkey::SessionSettingsGuard>();
      m_clipboard = std::make_unique<clipcheck_support::ClipboardStateGuard>();

      QString error;
Original expression:
  QVERIFY2(selectionkey::openWindowSession(m_session, m_projectRoot, error),
               qUtf8Printable(error))

A004 | SelectionLocalInputTierTest::init | src/checks/selectionkey/localinput.cpp:101
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the song-tab open precondition is a native loader precondition a
presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
      m_workspace = m_session.workspace;
      SongTab *const tab = selectionkey::openSongTab(m_session, m_songLabel, false, error);
Original expression:
  QVERIFY2(tab, qUtf8Printable(error))

A005 | SelectionLocalInputTierTest::init | src/checks/selectionkey/localinput.cpp:103
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; observing production window actions requires the native shell and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      m_tab = tab;
Original expression:
  QVERIFY2(selectionkey::observeWindowActions(*m_session.window, m_counts),
               "the production shell is missing the window actions")

A006 | SelectionLocalInputTierTest::init | src/checks/selectionkey/localinput.cpp:107
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the tab Quick-surface precondition requires the native Quick
surface and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;

      songview::TimelineQuickView *const quick = selectionkey::quickCanvas(view());
Original expression:
  QVERIFY2(quick, "the tab Quick surface is missing")

A007 | SelectionLocalInputTierTest::init | src/checks/selectionkey/localinput.cpp:109
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the tab Quick-window/root precondition requires the native Quick
surface and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      m_quickWindow = quick->quickWindow();
Original expression:
  QVERIFY2(m_quickWindow && quick->rootObject(),
               "the tab Quick window or root object is missing")

A008 | SelectionLocalInputTierTest::cleanup | src/checks/selectionkey/localinput.cpp:149
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the residual Quick mouse-grab observation requires the native
window and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
  }

  void SelectionLocalInputTierTest::cleanup()
  {
      // Unconditional teardown, independent of how the case ended: drop any
      // residual grab, then end the session through the guarded production close
      // path. Outcomes are computed first and asserted last, so a failed
      // assertion can never skip the remaining teardown.
      bool released = true;
      if (m_quickWindow) {
          if (QQuickItem *grabber = m_quickWindow->mouseGrabberItem())
              grabber->ungrabMouse();
          released = QTest::qWaitFor(
              [this] { return m_quickWindow.isNull() || !m_quickWindow->mouseGrabberItem(); });
      }
      m_quickWindow.clear();
      m_tab.clear();

      // Every scenario rolls its edits back through the real undo stack; a
      // dirty tab here means a rollback or cleanup path regressed.
      QString dirtyProblem;
      if (m_session.window && m_session.workspace) {
          if (m_session.workspace->selectedSongDirty())
              dirtyProblem = QStringLiteral("the case left the selected tab dirty before the "
                                            "explicit close");
          {
              selectionkey::DeclineModalsWithin guard(QStringLiteral("closing the window session"));
              m_session.window->close();
              selectionkey::settle();
          }
      }
      // Reset the shell even when a partial open left it without a WorkspaceUi,
      // so no init-failure path can leak the window into the next case.
      m_session.window.reset();
      m_session.workspace = nullptr;
      m_workspace = nullptr;
      m_clipboard.reset();
      m_settings.reset();
Original expression:
  QVERIFY2(released, "the Quick mouse grab survived the case teardown")

A009 | SelectionLocalInputTierTest::cleanup | src/checks/selectionkey/localinput.cpp:150
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the clean-close contract (no dirty tab at explicit close)
observes native session state a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(dirtyProblem.isEmpty(), qPrintable(dirtyProblem))

Remaining original source (closing sequence and helper bodies):
      QVERIFY2(dirtyProblem.isEmpty(), qPrintable(dirtyProblem));
  }

  // Dispatched once per process by the selectionkey-local-input catalog row; the
  // catalog already passed only the Qt payload (everything after --qt) and this
  // forwards it verbatim into exactly one qExec.
  int runSelectionKeyLocalInputCheck(const QString &projectRoot, const QString &songLabel,
                                     const QStringList &qtArguments)
  {
      SelectionLocalInputTierTest test(projectRoot, songLabel);
      QStringList arguments{QStringLiteral("selectionkey-local-input")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }
Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:19
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:29
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:32
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:34
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:36
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:39
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:40
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:42
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")
