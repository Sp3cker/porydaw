Assertion correspondence: selectionkey/coreediting.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/selectionkey/coreediting.cpp
Original SHA-256: b7d9cbae89ad503c6650e245838dc43a02d476bd5784746e639c27dcd3df748c
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Additional Swift counterpart: src/checks/automation/automationselection.swift
Additional Swift counterpart: src/checks/editcheck/SelectionChecks.swift
Additional Swift counterpart: src/checks/rollcheck/keyboard.swift
Additional Swift counterpart: src/checks/keyboard/KeybindingRegistryChecks.swift
Additional Swift counterpart: src/checks/rollcheck/selection.swift
Additional Swift counterpart: src/checks/rollcheck/keyboard_parity.swift
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files


Command: deno task verify --filter swiftcore --verbose (controller-observed)
Result: PASS; build ok (0.15 s), ok swiftcore (10.38 s); verify 1/6 ok (5 skipped); run_checks PASS. That run executes the paired prompt-transaction predicates S001-S008 only.
Command: deno task verify:shell --filter shellwindow --verbose (controller-observed)
Result: PASS; build ok (0.16 s), ok shellwindow (3.74 s); verify 1/11 ok (10 skipped); run_checks PASS. That run executes the shellwindow predicates S009-S016 through the shellwindow entry of ShellQmlTests.swift (src/checks/editorqml/ShellQmlTests.swift).

Command: deno task verify --filter swiftcore --verbose and deno task verify:shell --filter shellwindow --verbose
Result: PASS - task-58 implementer-recorded runs on the frozen sources: swiftcore 12/36
harnesses passed and the mounted shellwindow lane 1/26 selected harnesses passed. Evidence
build/proof-evidence/shellwindow.json (mtime 16:31:03) postdates the frozen check edit
tst_ShellWindow.qml (16:30:46) and records test_nLabelTimeSelectionCommands, so S017-S032 and
S040 classify executed. Every non-QML literal cited by S033-S041 was verified present in the
swiftcore evidence pass rows of build/proof-evidence/swiftcore-projectsession.json (observed
at 16:39 and again at 16:44); concurrent sibling lane runs rewrite that artifact (16:43 and
16:44 writes), so the executed classification belongs to the controller's settled re-run.
This task-58 ledger pass flipped the row dispositions and indexed the added anchors; it ran no
lane, rebuilt nothing and re-ran no check.

Correction pass (controller review): the roll.delete, roll.paste and roll.select_all
binding rows map to the production KeybindingRegistry predicates (src/checks/keyboard/
KeybindingRegistryChecks.swift) instead of retiring as native keymap queries, and the audition
PARTIAL reasons name the existing PianoGrid.onAudition observable instead of claiming none
exists.

Counts: MATCHED 9, PARTIAL 9, GAP 0, RETIRED-REPRESENTATION 35

A001 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:63
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Automations) plus the m_fixture check) for the drawer-audition fixture; the mounted lane stages its own Route 101 fixture.

A002 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:67
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick-window plumbing: focusTimelineInput stages native input focus on the automation drawer input; the mounted lanes take focus through real gestures.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      SongView &songView = m_fixture->view();

Original expression:
  QVERIFY2(focusTimelineInput(quick, m_fixture->input("timelineAutomationInput")),
               "could not focus the automation drawer for transpose audition")

A003 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:76
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKeyEvent posts the transpose key-down straight into the QQuickWindow; the mounted lane delivers real keyClick input.
Source context since previous assertion:
  ;
      songView.selectionModel().setNoteSelection({m_fixture->notes()[0]});

      std::vector<std::pair<int, int>> auditions;
      const QMetaObject::Connection connection = QObject::connect(
          &songView, &SongView::auditionNote, &songView,
          [&auditions](int, int key, int velocity) { auditions.emplace_back(key, velocity); });


Original expression:
  QVERIFY2(deliverKeyEvent(quick, QEvent::KeyPress, Qt::Key_Up, false),
               "drawer transpose key-down did not reach the Quick window")

A004 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:79
Disposition: PARTIAL
Mapping/reason: PARTIAL - the fork clause is the transpose key-down's live audition (one emission with velocity > 0). The Swift audition observable exists (PianoGrid.onAudition, wired to the audio engine) and its emissions - including the velocity-0 release - are asserted for pointer flavors (S046, S047 in rollcheck/selection.swift), but no predicate stages a drawer transpose key-down: unproved as stated (the Swift app has no keyboard-transpose audition path).
Source context since previous assertion:
  ;
      const std::size_t started = auditions.size();

Original expression:
  QVERIFY2(started == 1 && auditions.back().second > 0,
               "drawer transpose key-down did not start a live audition")

A005 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:82
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKeyEvent posts the autorepeat key-up (autorepeat=true) into the QQuickWindow; the mounted lane delivers real key input.
Source context since previous assertion:
  ;


Original expression:
  QVERIFY2(deliverKeyEvent(quick, QEvent::KeyRelease, Qt::Key_Up, true),
               "drawer autorepeat key-up did not reach the Quick window")

A006 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:84
Disposition: PARTIAL
Mapping/reason: PARTIAL - the fork clause is that an autorepeat key-up must not end the live audition. No predicate stages an audition term at all (the Swift audition observable is exercised only by pointer flavors, S046, S047), and the autorepeat predicates that do exist assert key routing rather than an audition (S048, the repeated pencil key): unproved as stated.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(auditions.size() == started,
               "drawer autorepeat key-up prematurely ended the live audition")

A007 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:87
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKeyEvent posts the physical key-up into the QQuickWindow; the mounted lane delivers real key input.
Source context since previous assertion:
  ;


Original expression:
  QVERIFY2(deliverKeyEvent(quick, QEvent::KeyRelease, Qt::Key_Up, false),
               "drawer physical key-up did not reach the Quick window")

A008 | SelectionKeyCoreTest::drawerTransposeAuditionReleasesOnPhysicalKeyUp | src/checks/selectionkey/coreediting.cpp:93
Disposition: PARTIAL
Mapping/reason: PARTIAL - the fork clause is the physical key-up ending the audition with the velocity-0 release. The velocity-0 release observable is asserted for pointer flavors (S047 asserts zero-velocity releases at auditions[1]/auditions[3]) but no predicate stages a transpose key-up: unproved as stated.
Source context since previous assertion:
  ;
      // The shared release path ends the audition with the velocity-0
      // release of the engine's single preview slot; that emission repeats
      // the slot, not the sounding key (AudioEngine::previewNote replaces
      // whatever preview sounded).

Original expression:
  QVERIFY2(auditions.size() == started + 1 && auditions.back().second == 0,
               "drawer physical key-up did not end the transposed note audition")

A009 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:101
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Automations) plus the m_fixture check); the mounted lane stages its own Route 101 fixture.

A010 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:107
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native surface guard: the Quick window, the automation input item and the projected lane window points must exist; the mounted lane resolves its own items by object name.

A011 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:109
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Automation); the mounted lane routes through the QML label and plot focus instead.
Source context since previous assertion:
  ;


Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Automation),
               "could not stage automation focus for range delivery")

A012 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:113
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick-input pointer delivery: moveMouseToTimelineInput projects the lane point into the native input item; the mounted lane stages the band with real mouse press/move/release.
Source context since previous assertion:
  ;
      m_fixture->view().selectionModel().setNoteSelection({m_fixture->notes()[0]});
      selectionkey::settle();

Original expression:
  QVERIFY2(moveMouseToTimelineInput(quick, automation, points->at(0)),
               "automation range start did not reach the Quick input item")

A013 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:128
Disposition: PARTIAL
Mapping/reason: PARTIAL - the right band stages an active lane-scoped Volume range without writing a lane (S017/S018) and the presenter clears the competing note selection when a lane-scoped range commits (S039); unproved as stated: the staged range's exact boundary ticks (kFirstPointTick/kOutsidePointTick) and the replacement of a pre-existing note selection are not asserted (S019 pins the empty-selection precondition form only).
Source context since previous assertion:
  ;
      stageMousePress(Qt::RightButton, points->at(0));
      stageMouseMove(points->at(1));
      stageMouseRelease(Qt::RightButton, points->at(1));
      selectionkey::settle();

      const auto &selection = m_fixture->view().selectionModel().timeSelection();
      // Hoisted: the lane pair's commas would split the QVERIFY2 macro args.
      const bool rangeReady =
          selection.active() && selection.startTick == kFirstPointTick &&
          selection.endTick == kOutsidePointTick &&
          selection.scope == songview::EditorSelectionModel::TimeSelection::Lanes &&
          selection.lanes == std::vector<std::pair<int, uint8_t>>{{kTrack, kController}} &&
          m_fixture->view().selectionModel().noteSelection().empty();

Original expression:
  QVERIFY2(rangeReady, "actual automation range did not replace note selection with lane scope")

A014 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:130
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the shipped Delete into the QQuickWindow; the mounted lane sends a real Delete keyClick.
Source context since previous assertion:
  ;


Original expression:
  QVERIFY2(deliverKey(quick, Qt::Key_Delete),
               "shipped Delete did not reach the Quick automation surface")

A015 | SelectionKeyCoreTest::automationRangeAndReboundDelete | src/checks/selectionkey/coreediting.cpp:132
Disposition: MATCHED
Mapping: S024, S023, S025 (the lane-scoped Delete drops the in-range Volume points and keeps the out-of-range point, the notes stay byte-identical and the range remains selected; the named inside ticks are asserted through the node count rather than per tick)
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(
          !m_fixture->document().findLanePoint(kTrack, kController, kFirstPointTick, nullptr) &&
              !m_fixture->document().findLanePoint(kTrack, kController, kInsidePointTick, nullptr) &&
              !m_fixture->document().findLanePoint(kTrack, kController, kSecondPointTick, nullptr) &&
              m_fixture->document().findLanePoint(kTrack, kController, kOutsidePointTick, nullptr),
          "automation range Delete did not affect only its selected points")

A016 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:159
Disposition: MATCHED
Mapping: S042, S043 (the production KeybindingRegistry matches Delete and Backspace with no modifiers for roll.delete - the shipped single-key defaults - and the mounted lane delivers the real Delete key)
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::pencilHoverDeletePrecedence_data()
  {
      QTest::addColumn<int>("scenario");
      QTest::newRow("note selection wins over hover") << int(PencilScenario::NotePrecedence);
      QTest::newRow("track time selection wins over hover") << int(PencilScenario::TimePrecedence);
      QTest::newRow("eligible hover delete") << int(PencilScenario::HoverDelete);
      QTest::newRow("hover miss keeps document") << int(PencilScenario::HoverMiss);
  }

  void SelectionKeyCoreTest::pencilHoverDeletePrecedence()
  {
      QFETCH(int, scenario);
      const auto kind = PencilScenario(scenario);
      const bool miss = kind == PencilScenario::HoverMiss;

      // The shipped roll.delete defaults ("Delete;Backspace") drive the
      // precedence matrix; the automation-range scenario separately delivers
      // unmodified Delete.
      const auto deleteKey = firstBinding(QStringLiteral("roll.delete"));

Original expression:
  QVERIFY2(deleteKey.has_value(),
               "roll.delete has no single-key default binding for the pencil-hover surface")

A017 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:162
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Automations) plus the m_fixture check) for the pencil-hover fixture; the mounted lane stages its own Route 101 fixture.

A018 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:167
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Automation) for the pencil-hover delete; the mounted lane stages the plot and label focus instead.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      songview::TimelineInputItem *const automation = m_fixture->input("timelineAutomationInput");
      m_fixture->view().editorDrawer()->automationPage()->canvas()->setPencilMode(true);

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Automation),
               "could not stage automation focus for pencil-hover delete")

A019 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:187
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native hover fixture guard: the Quick window, the automation input item and the projected hover point must exist before the hover delivery.

A020 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:188
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick-input pointer delivery: moveMouseToTimelineInput sends the hover move into the native input item; the mounted lane uses real mouseMove input.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(moveMouseToTimelineInput(quick, automation, *point),
               "automation hover did not reach the Quick input item")

A021 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:191
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the default Delete binding into the QQuickWindow; the mounted lane sends a real Delete keyClick.
Source context since previous assertion:
  ;
      const QByteArray before = m_fixture->document().smf().write();

Original expression:
  QVERIFY2(deliverKey(quick, deleteKey->key(), deleteKey->keyboardModifiers()),
               "default Delete did not reach the pencil-hover surface")

A022 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:198
Disposition: MATCHED
Mapping: S031 (a hovered point survives a selected-note Delete: the selected notes are removed while the hovered point's tick list stays unchanged)
Source context since previous assertion:
  ;

      const bool pointAlive =
          m_fixture->document().findLanePoint(kTrack, kController, kSecondPointTick, nullptr);
      switch (kind) {
      case PencilScenario::NotePrecedence:

Original expression:
  QVERIFY2(!noteExists(m_fixture->document(), m_fixture->notes()[0]) && pointAlive,
                   "selected notes did not win over pencil hover Delete")

A023 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:202
Disposition: MATCHED
Mapping: S033 (time selection retains semantic delete priority: the presenter declines the hover delete while a lane-scoped range is active)
Source context since previous assertion:
  ;
          break;
      case PencilScenario::TimePrecedence:

Original expression:
  QVERIFY2(!noteExists(m_fixture->document(), m_fixture->notes()[0]) && pointAlive,
                   "track time selection did not win over pencil hover Delete")

A024 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:206
Disposition: MATCHED
Mapping: S034, S035 (the pencil hover delete consumes the written point and removes the actual written tick)
Source context since previous assertion:
  ;
          break;
      case PencilScenario::HoverDelete:

Original expression:
  QVERIFY2(!pointAlive, "eligible pencil hover Delete did not remove the hovered point")

A025 | SelectionKeyCoreTest::pencilHoverDeletePrecedence | src/checks/selectionkey/coreediting.cpp:209
Disposition: MATCHED
Mapping: S036, S032 (a blank hover and a hover miss both leave the document bytes and revision unchanged)
Source context since previous assertion:
  ;
          break;
      case PencilScenario::HoverMiss:

Original expression:
  QVERIFY2(m_fixture->document().smf().write() == before,
                   "pencil hover miss Delete changed the document")

A026 | SelectionKeyCoreTest::laneScopedVerticalArrowLeavesDocumentUntouched | src/checks/selectionkey/coreediting.cpp:218
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Automations) plus the m_fixture check) for the lane-nudge fixture; the mounted lane stages its own fixture.
Source context since previous assertion:
  ;
          break;
      }
  }

  void SelectionKeyCoreTest::laneScopedVerticalArrowLeavesDocumentUntouched()
  {
      m_fixture = createFixture(EditorDrawerPage::Automations);

Original expression:
  QVERIFY2(
          m_fixture,
          qPrintable(
              QStringLiteral("could not create lane-nudge fixture: %1").arg(m_lastFixtureError)))

A027 | SelectionKeyCoreTest::laneScopedVerticalArrowLeavesDocumentUntouched | src/checks/selectionkey/coreediting.cpp:222
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Automation); the mounted lane routes through the QML label focus.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Automation),
               "could not focus the automation Quick band")

A028 | SelectionKeyCoreTest::laneScopedVerticalArrowLeavesDocumentUntouched | src/checks/selectionkey/coreediting.cpp:227
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the lane-scoped Up into the QQuickWindow; the mounted lane sends a real Up keyClick.
Source context since previous assertion:
  ;
      m_fixture->view().selectionModel().setTimeSelection(
          coreLaneRange(kSecondPointTick, kOutsidePointTick));
      const QByteArray beforeVertical = m_fixture->document().smf().write();

Original expression:
  QVERIFY2(deliverKey(m_fixture->window(), Qt::Key_Up),
               "Quick window did not accept the lane-scoped vertical arrow")

A029 | SelectionKeyCoreTest::laneScopedVerticalArrowLeavesDocumentUntouched | src/checks/selectionkey/coreediting.cpp:229
Disposition: MATCHED
Mapping: S020, S021 (lane-focus Up leaves the document untouched and preserves every Volume point byte-identically)
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_fixture->document().smf().write() == beforeVertical,
               "lane-scoped vertical arrow mutated automation or hidden notes")

A030 | SelectionKeyCoreTest::laneScopedHorizontalArrowNudgesPointsAndInterval | src/checks/selectionkey/coreediting.cpp:236
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Automations) plus the m_fixture check) for the horizontal lane-nudge fixture; the mounted lane stages its own fixture.
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::laneScopedHorizontalArrowNudgesPointsAndInterval()
  {
      m_fixture = createFixture(EditorDrawerPage::Automations);

Original expression:
  QVERIFY2(
          m_fixture,
          qPrintable(
              QStringLiteral("could not create lane-nudge fixture: %1").arg(m_lastFixtureError)))

A031 | SelectionKeyCoreTest::laneScopedHorizontalArrowNudgesPointsAndInterval | src/checks/selectionkey/coreediting.cpp:240
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Automation); the mounted lane routes through the QML label focus.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Automation),
               "could not focus the automation Quick band")

A032 | SelectionKeyCoreTest::laneScopedHorizontalArrowNudgesPointsAndInterval | src/checks/selectionkey/coreediting.cpp:247
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the lane-scoped Right into the QQuickWindow; the mounted lane sends a real Right keyClick.
Source context since previous assertion:
  ;
      m_fixture->view().selectionModel().setTimeSelection(
          coreLaneRange(kSecondPointTick, kOutsidePointTick));

      const uint64_t destination =
          m_fixture->view().grid().snapTickUp(double(kSecondPointTick) + 1.0);

Original expression:
  QVERIFY2(deliverKey(m_fixture->window(), Qt::Key_Right),
               "Quick window did not accept the lane-scoped horizontal arrow")

A033 | SelectionKeyCoreTest::laneScopedHorizontalArrowNudgesPointsAndInterval | src/checks/selectionkey/coreediting.cpp:250
Disposition: PARTIAL
Mapping/reason: PARTIAL - the mounted lane Right moves the point set and keeps the range active (S022), the presenter's empty lanes-scoped range advances on the camera grid without changing its span and writes nothing (S037/S038), and the same-delta interval translation executes for a track-scoped range (S041); unproved as stated: no predicate asserts a populated lanes-scoped range whose point lands on the snapped destination and whose interval translates by the same delta.
Source context since previous assertion:
  ;
      const auto &moved = m_fixture->view().selectionModel().timeSelection();

Original expression:
  QVERIFY2(
          m_fixture->document().findLanePoint(kTrack, kController, destination, nullptr) &&
              !m_fixture->document().findLanePoint(kTrack, kController, kSecondPointTick, nullptr) &&
              moved.startTick == destination &&
              moved.endTick == kOutsidePointTick + destination - kSecondPointTick,
          "lane-scoped horizontal arrow did not nudge points and the selected interval")

A034 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:275
Disposition: PARTIAL
Mapping/reason: PARTIAL - the production registry pins roll.paste's delivery scope as editor-routed (S044) and the mounted lane delivers the platform standard Paste and asserts its landing (S013, S016, S029); unproved as stated: no predicate asserts the paste single-key stroke itself (the registry checks assert strokes only for transpose-up, play-pause, insert-time, delete-time, pencil-mode and the delete/backspace/copy chords).
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::keyboardClipboardParity_data()
  {
      QTest::addColumn<int>("destinationBand");
      QTest::newRow("roll") << int(songview::TimelineBand::Roll);
      QTest::newRow("automation drawer") << int(songview::TimelineBand::Automation);
  }

  void SelectionKeyCoreTest::keyboardClipboardParity()
  {
      QFETCH(int, destinationBand);
      const auto band = songview::TimelineBand(destinationBand);

      // roll.copy writes the process-global QClipboard; the guard restores it
      // even when the row aborts, so clipboard state cannot leak across rows.
      const clipcheck_support::ClipboardStateGuard clipboardGuard;

      const auto paste = firstBinding(QStringLiteral("roll.paste"));

Original expression:
  QVERIFY2(paste.has_value(), "keyboard Paste has no single-key binding")

A035 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:278
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(drawerPageForBand(band)) plus the m_fixture check) for the per-band clipboard fixture; the mounted lane stages its own two-song fixture.
Source context since previous assertion:
  ;

      m_fixture = createFixture(drawerPageForBand(band));

Original expression:
  QVERIFY2(
          m_fixture,
          qPrintable(
              QStringLiteral("could not create the clipboard fixture: %1").arg(m_lastFixtureError)))

A036 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:285
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Roll) for the keyboard Copy; the mounted lane focuses the roll through QML activeFocus.
Source context since previous assertion:
  ;
      SongView &songView = m_fixture->view();
      QQuickWindow *const quick = m_fixture->window();
      songView.selectionModel().setNoteSelection({m_fixture->notes()[0]});

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Roll),
               "could not focus the roll for keyboard Copy")

A037 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:290
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native harness plumbing: the unhosted rig's SongView::editActions() handle (the row only guards that the rig holds a production action set); production command enablement is asserted through the presenter and grid APIs instead (actionEnabled in the mounted lanes).
Source context since previous assertion:
  ;
      // Copy is Window-class: the unhosted rig registers no shortcut owner, so
      // the stimulus is the bound production action, not a raw key.
      const songview::EditActions *const actions = songView.editActions();

Original expression:
  QVERIFY2(actions, "the unhosted rig has no production action set")

A038 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:292
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native harness plumbing: the rig's production Copy QAction existence and enablement (the native trigger path); the mounted lanes exercise the equivalent journey through the platform standard Copy/Paste and assert its landing (S013, S016, S029).
Source context since previous assertion:
  ;
      QAction *const copy = actions->action(SongView::EditCommand::Copy);

Original expression:
  QVERIFY2(copy && copy->isEnabled(), "the rig has no enabled production Copy action")

A039 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:296
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(band) for the paste destination band.
Source context since previous assertion:
  ;
      copy->trigger();
      songView.selectionModel().clearNoteSelection();
      songView.commitEditCursor(kPasteTick);

Original expression:
  QVERIFY2(m_fixture->focusBand(band), "could not focus paste destination band")

A040 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:297
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the Paste binding into the QQuickWindow; the mounted lane sends a real standard Paste keySequence.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(deliverKey(quick, paste->key(), paste->keyboardModifiers()),
               "Quick window did not accept the keyboard Paste binding")

A041 | SelectionKeyCoreTest::keyboardClipboardParity | src/checks/selectionkey/coreediting.cpp:299
Disposition: PARTIAL
Mapping/reason: PARTIAL - related: S013 (Paste selects at least one inserted note), S016 (Paste advances the destination cursor past the inserted note) and S029 (Paste over label focus writes the copied Volume lane at the edit cursor, the node count grown past tick 7680); unproved as stated: the exact committed-cursor landing tick of the pasted clip and the pasted note key (the kPasteTick/60 identity).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(noteExists(m_fixture->document(), kTrack, kPasteTick, 60),
               "keyboard Paste did not land the roll-copied note clip at the committed cursor")

A042 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:306
Disposition: PARTIAL
Mapping/reason: PARTIAL - the production registry pins roll.select_all's delivery scope as editor-routed (S045) and the mounted Select All deliveries assert the resulting note selection and the cleared range (S019, S027, S028); unproved as stated: no predicate asserts the select-all single-key stroke itself.
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::selectAllFromEmptySelection()
  {
      const auto shortcut = firstBinding(QStringLiteral("roll.select_all"));

Original expression:
  QVERIFY2(shortcut.has_value(), "Select All Notes has no single-key binding")

A043 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:309
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Velocity) plus the m_fixture check) for the Select All fixture; the mounted lane stages its own fixture.
Source context since previous assertion:
  ;

      m_fixture = createFixture(EditorDrawerPage::Velocity);

Original expression:
  QVERIFY2(
          m_fixture,
          qPrintable(
              QStringLiteral("could not create Select All fixture: %1").arg(m_lastFixtureError)))

A044 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:317
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick-window plumbing: focusTimelineInput stages native input focus on the velocity input; the mounted lane uses QML focus.
Source context since previous assertion:
  ;
      SongView &songView = m_fixture->view();
      QQuickWindow *const quick = m_fixture->window();
      songView.selectionModel().clearBothSelections();
      selectionkey::settle();

Original expression:
  QVERIFY2(focusTimelineInput(quick, m_fixture->input("timelineVelocityInput")),
               "could not focus the velocity band before Select All")

A045 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:319
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Velocity) before Select All.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Velocity),
               "could not stage velocity focus before Select All")

A046 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:321
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the Select All binding into the QQuickWindow; the mounted lane sends a real standard SelectAll keySequence.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(deliverKey(quick, shortcut->key(), shortcut->keyboardModifiers()),
               "Select All key did not reach the velocity band")

A047 | SelectionKeyCoreTest::selectAllFromEmptySelection | src/checks/selectionkey/coreediting.cpp:324
Disposition: MATCHED
Mapping: S019, S028, S027 (Select All from an empty note selection takes the active track's notes - asserted per note, standing in for the C++ >= count over the staged fixture list - and leaves no time range; the mounted stimulus is the Volume label focus rather than the velocity band)
Source context since previous assertion:
  ;
      // >= is deliberate: the project fixture carries pre-existing primary-track notes.

Original expression:
  QVERIFY2(songView.selectionModel().noteSelection().size() >= m_fixture->notes().size() &&
                   !songView.selectionModel().timeSelection().active(),
               "Select All Notes from empty selection did not select primary-track notes")

A048 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:332
Disposition: PARTIAL
Mapping/reason: PARTIAL - same clause as A042 in the ruler-click row: the registry pins roll.select_all's delivery scope as editor-routed (S045) and the mounted Select All deliveries assert the selection results (S019, S027, S028); unproved as stated: no predicate asserts the select-all single-key stroke itself.
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick()
  {
      const auto shortcut = firstBinding(QStringLiteral("roll.select_all"));

Original expression:
  QVERIFY2(shortcut.has_value(), "Select All Notes has no single-key binding")

A049 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:335
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(EditorDrawerPage::Velocity) plus the m_fixture check) for the ruler-click fixture; the mounted lane stages its own fixture.
Source context since previous assertion:
  ;

      m_fixture = createFixture(EditorDrawerPage::Velocity);

Original expression:
  QVERIFY2(
          m_fixture,
          qPrintable(
              QStringLiteral("could not create Select All fixture: %1").arg(m_lastFixtureError)))

A050 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:347
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native ruler probe and click delivery (plainRulerPoint plus clickTimelineInput into the native ruler input item).
Source context since previous assertion:
  ;
      SongView &songView = m_fixture->view();
      QQuickWindow *const quick = m_fixture->window();

      songView.selectionModel().setTimeSelection(coreTrackRange(kFirstNoteTick, kFirstNoteTick + 48));
      selectionkey::settle();
      m_fixture->configureDrawerSurface(std::nullopt);
      songview::TimelineInputItem *const ruler = m_fixture->input("timelineRulerInput");
      const auto rulerPoint = ruler ? m_fixture->plainRulerPoint(ruler) : std::optional<QPoint>{};

Original expression:
  QVERIFY2(ruler && rulerPoint && clickTimelineInput(quick, ruler, *rulerPoint),
               "could not deliver the ruler click before Select All")

A051 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:349
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging: m_fixture->focusBand(TimelineBand::Ruler) before Select All.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::Ruler),
               "could not stage ruler focus before Select All")

A052 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:351
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery: deliverKey posts the Select All binding into the QQuickWindow; the mounted lane sends a real standard SelectAll.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(deliverKey(quick, shortcut->key(), shortcut->keyboardModifiers()),
               "Select All key did not reach the ruler band")

A053 | SelectionKeyCoreTest::selectAllReplacesTimeSelectionAfterRulerClick | src/checks/selectionkey/coreediting.cpp:354
Disposition: MATCHED
Mapping: S027, S028, S019 (Select All after an active lane range replaces the time selection with the active track's notes and clears the range; the mounted stimulus is the Volume label focus rather than the ruler click)
Source context since previous assertion:
  ;
      // >= is deliberate: the project fixture carries pre-existing primary-track notes.

Original expression:
  QVERIFY2(songView.selectionModel().noteSelection().size() >= m_fixture->notes().size() &&
                   !songView.selectionModel().timeSelection().active(),
               "Select All Notes did not replace a time selection from incidental chrome")

Remaining original source (closing sequence and helper bodies):
  ;
  }


Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original selected fixture note exists"

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original empty-tick numeric prompt opens"

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the production prompt retains the supplied numeric draft"

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing does not mutate the song"

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing preserves note selection"

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation closes the prompt"

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation leaves the original document unchanged"

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft and cancellation preserve the full song contents"

S009 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Ctrl+D duplicates and selects the original note"

S010 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Select All includes both same-track notes"

S011 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Join selects a subdividable merged note"

S012 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Split keeps the resulting note fragments selected"

S013 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Paste selects at least one inserted note"

S014 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "text-focused Delete never deletes selected notes"

S015 | test_iEditorCommandDeliveryAndTextLocalKeys | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Delete removes only the selected track's notes"

S016 | test_dCopyFromOneSongTabPastesIntoAnother | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Paste changes only the destination by one note"


S017 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the right-band selects Volume time"

S018 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the right-band alone never writes a lane"

S019 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the lane-only range begins with an empty note selection"

S020 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "lane-focus Up leaves the document untouched"

S021 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "lane-focus Up preserves every Volume point"

S022 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "lane-focus Right moves the point and translates the interval"

S023 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Delete over label focus removes the selected Volume points"

S024 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Delete removes only the lane points inside the staged range"

S025 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Volume range Delete preserves the notes"

S026 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Delete keeps the selected time range"

S027 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Select All over label focus clears the time range"

S028 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Select All over label focus selects the active track's notes"

S029 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Paste over label focus writes the copied Volume lane at the edit cursor"

S030 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "a written Volume node is visible for hover deletion"

S031 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "a hovered point survives a selected-note Delete"

S032 | test_nLabelTimeSelectionCommands | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "a hover miss changes no document bytes"

S033 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "time selection retains semantic delete priority"

S034 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "pencil hover deletion consumes the written point"

S035 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "hover deletion removes the actual written point"

S036 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "blank hover deletion changes nothing"

S037 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "an empty range owns its nudge command"

S038 | drawerAutomationRestoredInteractionContracts | src/checks/automation/automationselection.swift
Anchor: message "an empty range advances on the camera grid without changing its span"

S039 | clipboardUnifiedTimeSelectionChecks | src/checks/editcheck/SelectionChecks.swift
Anchor: message "A008 committing an active range clears the competing note selection"

S040 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Up transposes one semitone"

S041 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "Right over an active time selection nudges the covered notes and advances the band start"


S042 | runKeybindingRegistryChecks | src/checks/keyboard/KeybindingRegistryChecks.swift
Anchor: message "delete"

S043 | runKeybindingRegistryChecks | src/checks/keyboard/KeybindingRegistryChecks.swift
Anchor: message "backspace"

S044 | runKeybindingRegistryChecks | src/checks/keyboard/KeybindingRegistryChecks.swift
Anchor: message "roll.paste"

S045 | runKeybindingRegistryChecks | src/checks/keyboard/KeybindingRegistryChecks.swift
Anchor: message "roll.select_all"

S046 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "Ctrl note press auditions its own velocity and latches the drawing velocity"

S047 | checkThresholdDrawCell | src/checks/rollcheck/selection.swift
Anchor: message "pending-draw row changes glissando before horizontal draw slop"

S048 | checkBandKeyAutoRepeat | src/checks/rollcheck/keyboard_parity.swift
Anchor: message "repeated eligible pencil key is consumed"

Shared helpers
selectDrawnVelocityNote (src/checks/editorqml/tst_ShellWindow.qml) and
openTwoSongShell (:88) stage the fixture and select the drawn velocity note; the
helper itself asserts selectedCount 1 (:166) and carries no other predicate.
