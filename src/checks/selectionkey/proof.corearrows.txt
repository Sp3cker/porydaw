Assertion correspondence: selectionkey/corearrows.cpp

Reference revision: acc555488c87b59885e5c58814626318039878ab
Original: src/checks/selectionkey/corearrows.cpp
Original SHA-256: 69dfaa2c94085416c2507de2b7167f5df3db2a327698490d34dda13bbe77d3e8
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Swift SHA-256: 0b1d5de7907758291583092f0d482a14eec5af37dd8d4f610689ea2e64d8c1eb
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Additional Swift SHA-256: a9c8ef79e003a9396492f2d27cadc6c88514a664ad5b45872ca79a1a593bb7b7

Status and evidence
Tally: MATCHED 0, PARTIAL 3, GAP 3, NATIVE 14.
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below. Source context preserves setup,
rows, loops, and guards between assertions verbatim. S identifiers identify
exact Swift predicates in the counterparts, listed after the inventory.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of any converted Swift window.
GAP identifies an original predicate without a replacement assertion.
This file holds the core arrow-routing half of plan scenario 1 plus the
mergeable-move undo proof. The paired localinputtier_text.swift transaction
(S001-S008) exercises only the numeric-prompt transaction
(drawerOriginalNumericPromptTransaction) and asserts nothing about band clicks,
arrow moves, or undo history. The frozen shellwindow lane
(src/checks/editorqml/tst_ShellWindow.qml :: test_gEditorRoutedNoteKeysAndChromeArrows,
S009-S014) delivers real arrow keys to the production roll and observes the
selected note's transpose/nudge/length deltas, so A013, A014 and A018 are
PARTIAL, each naming its unproved condition. The incidental band-click
selection site (A007) and the fixture-presence guards (A011, A017) still have no
Swift predicate and stay GAP; every observation requiring the native fixture,
window, delivery, or focus is NATIVE.

Verification
Command: deno task verify --filter swiftcore --verbose (controller-observed)
Result: PASS; build ok (0.15 s), ok swiftcore (10.38 s); verify 1/6 ok
(5 skipped); run_checks PASS. That run executes the paired prompt-transaction
predicates S001-S008 only.
Command: deno task verify:shell --filter shellwindow --verbose
(controller-observed)
Result: PASS; build ok (0.16 s), ok shellwindow (3.74 s); verify 1/11 ok
(10 skipped); run_checks PASS. That run executes the shellwindow predicates
S009-S014 through the shellwindow entry of ShellQmlTests.swift
(src/checks/editorqml/ShellQmlTests.swift:46).
The native C++ harness is not registered in this checkout (the swift_core_check
source list carries only selectionkey/localinputtier_text.swift); its cases are
reference inventories only, never current execution evidence.

Execution ownership
SelectionKeyCoreTest slots run on freshly assembled CoreFixture instances via
createFixture in core.cpp; entry point and lifecycle live in core.cpp. No Swift
entry point executes this native routing. drawerOriginalNumericPrompt-
Transaction is called by runProjectSessionSuite in
src/checks/workspace/SessionChecks.swift; it builds a
drawerAutomationAutomationFixture (CC10 pan rows 48=32, 96=64) and drives the
production Swift AutomationPage prompt. That fixture struct, defined in
src/checks/automation/AutomationPageChecks.swift, carries no report predicates
of its own. test_gEditorRoutedNoteKeysAndChromeArrows reaches the production
editor through the shellwindow entry of ShellQmlTests.swift
(src/checks/editorqml/ShellQmlTests.swift:46), which stages the
mus_route101/mus_littleroot_test fixture and drives real QTest arrow delivery
into the focused roll (S009-S014).

Fixture, sequence and row constraints
incidentalBandClickPreservesSelection_data registers 4 band rows (velocity,
voice changes, ruler, other events); arrowsMoveSelectedNotesAcrossBands_data
registers the 4-band x 4-direction matrix (16 rows: Up/Down/Right/Left per
band). Every site below executes once per row of its case. The per-note loop
in arrowsMoveSelectedNotesAcrossBands expands one site across every fixture
note; expectShift in the undo proof expands one site across the Up/Undo/Redo/
Down stages times every fixture note. Horizontal arrows snap one grid step
from the first selected note's tick; only the two selected notes move.

Original assertion inventory and source sequence

A001 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:85
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the cross-band fixture assembly is a native harness precondition
a presenter-level Swift check cannot establish.
Source context since previous assertion:
  #include "checks/selectionkey/tst_selectionkeycore.h"

  #include "checks/support/eventsynth.h"

  #include <QGuiApplication>
  #include <QQuickWindow>
  #include <QStyleHints>
  #include <QtTest>

  #include <array>
  #include <optional>
  #include <vector>

  using selectionkey::ClickTarget;
  using selectionkey::describeNoteIds;
  using selectionkey::describePoint;
  using selectionkey::describeRect;
  using selectionkey::drawerPageForBand;

  namespace {

  enum class CoreClick {
      SelectedVelocityStemDrag,
      PlainRuler,
      BandCenter,
  };

  struct BandProbe {
      songview::TimelineBand band;
      const char *item;
      const char *label;
      bool clickTakesFocus;
      CoreClick click;
  };

  constexpr std::array<BandProbe, 4> kCoreBands{{
      {songview::TimelineBand::Velocity, "timelineVelocityInput", "velocity", true,
       CoreClick::SelectedVelocityStemDrag},
      {songview::TimelineBand::VoiceChanges, "timelineVoiceChangesInput", "voice changes", true,
       CoreClick::BandCenter},
      {songview::TimelineBand::Ruler, "timelineRulerInput", "ruler", false, CoreClick::PlainRuler},
      {songview::TimelineBand::OtherEvents, "timelineOtherEventsInput", "other events", false,
       CoreClick::BandCenter},
  }};

  struct ArrowProbe {
      Qt::Key key;
      const char *label;
  };

  constexpr std::array<ArrowProbe, 4> kCoreArrows{{
      {Qt::Key_Up, "Up"},
      {Qt::Key_Down, "Down"},
      {Qt::Key_Right, "Right"},
      {Qt::Key_Left, "Left"},
  }};

  } // namespace

  void SelectionKeyCoreTest::incidentalBandClickPreservesSelection_data()
  {
      QTest::addColumn<int>("bandIndex");
      QTest::addColumn<int>("click");
      for (std::size_t index = 0; index < kCoreBands.size(); ++index) {
          QTest::newRow(
              qPrintable(QStringLiteral("%1 band").arg(QLatin1String(kCoreBands[index].label))))
              << int(index) << int(kCoreBands[index].click);
      }
  }

  void SelectionKeyCoreTest::incidentalBandClickPreservesSelection()
  {
      QFETCH(int, bandIndex);
      QFETCH(int, click);
      const BandProbe &probe = kCoreBands[std::size_t(bandIndex)];
      const auto kind = CoreClick(click);

      // Automation uses an empty visible lane click. Velocity exercises the
      // intended selected-stem contract with a real threshold-crossing drag:
      // a press/release without relative movement intentionally becomes a
      // singleton selection. The remaining bands stage their plain production
      // clicks; every row proves the incidental click preserves the eligible
      // selection the keyboard contract will act on.
      m_fixture = createFixture(std::nullopt);
Original expression:
  QVERIFY2(m_fixture,
               qPrintable(QStringLiteral("could not create the cross-band note fixture: %1")
                              .arg(m_lastFixtureError)))

A002 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:94
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; band-input availability observes the native Quick surface and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      SongView &songView = m_fixture->view();
      const std::vector<NoteId> selection{m_fixture->notes()[0], m_fixture->notes()[1]};

      m_fixture->configureDrawerSurface(drawerPageForBand(probe.band));
      songview::TimelineInputItem *const band = m_fixture->input(probe.item);
Original expression:
  QVERIFY2(band && QTest::qWaitFor([band, quick] {
                   return !band->bounds().isEmpty() && band->window() == quick;
               }),
               "Quick band input is unavailable")

A003 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:124
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the click-point probe depends on native production geometry and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;

      // The click begins from the same eligible note selection every row uses.
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();

      std::optional<QPoint> point;
      QString clickDiagnostics;
      switch (kind) {
      case CoreClick::SelectedVelocityStemDrag: {
          const ClickTarget target = m_fixture->selectedVelocityStemPoint();
          point = target.point;
          clickDiagnostics = target.diagnostics;
          break;
      }
      case CoreClick::PlainRuler:
          point = m_fixture->plainRulerPoint(band);
          break;
      case CoreClick::BandCenter:
          point = band->mapToScene(band->bounds().center()).toPoint();
          break;
      }
      if (clickDiagnostics.isEmpty()) {
          clickDiagnostics = QStringLiteral("input-bounds=%1; chosen-window=%2")
                                 .arg(describeRect(band->bounds()))
                                 .arg(point ? describePoint(*point) : QStringLiteral("unavailable"));
      }
Original expression:
  QVERIFY2(point.has_value(), qPrintable(QStringLiteral("%1 band click point is unavailable: %2")
                                                 .arg(QLatin1String(probe.label), clickDiagnostics)))

A004 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:145
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the threshold-crossing stem-drag delivery requires native mouse
input and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;

      if (kind == CoreClick::SelectedVelocityStemDrag) {
          const QPointF pressPoint(*point);
          const QPointF inputPoint = band->mapFromScene(pressPoint);
          const qreal dragY =
              qMin(band->bounds().bottom() - 2.0,
                   inputPoint.y() + QGuiApplication::styleHints()->startDragDistance() + 4.0);
          const QPoint dragPoint = band->mapToScene(QPointF(inputPoint.x(), dragY)).toPoint();
          bool delivered = checks::events::primeMouseMove(*quick, *band, *point);
          if (delivered) {
              stageMousePress(Qt::LeftButton, *point);
              stageMouseMove(dragPoint);
              delivered = QTest::qWaitFor([&] {
                  return songView.userGestureActive() &&
                         songView.selectionModel().noteSelection() == selection;
              });
              stageMouseRelease(Qt::LeftButton, dragPoint);
              selectionkey::settle();
          }
Original expression:
  QVERIFY2(delivered,
                   qPrintable(QStringLiteral("%1 band selected velocity-stem drag did not activate "
                                             "and retain the group: %2")
                                  .arg(QLatin1String(probe.label), clickDiagnostics)))

A005 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:150
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; click delivery into the production band requires native input and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      } else {
Original expression:
  QVERIFY2(selectionkey::clickTimelineInput(quick, band, *point),
                   qPrintable(QStringLiteral("%1 band click did not reach its target band: %2")
                                  .arg(QLatin1String(probe.label), clickDiagnostics)))

A006 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:157
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; Quick active-focus observation requires the native window and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      }

      // Ruler and other-events clicks intentionally do not focus.
      if (probe.clickTakesFocus)
Original expression:
  QVERIFY2(band->hasActiveFocus(), "Quick band input did not focus its band")

A007 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:159
Disposition: GAP
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the incidental band-click selection equality is pure selection
state a presenter-level Swift check could in principle assert, but none does.
The frozen shellwindow rows (S009) click the drawn velocity node and create a
one-note selection; no predicate asserts that an incidental band click preserves
an already eligible note selection. Original predicate and setup below remain an
open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(songView.selectionModel().noteSelection() == selection,
               qPrintable(QStringLiteral("%1 band input disturbed the eligible selection: "
                                         "actual=%2 intended=%3 %4")
                              .arg(QLatin1String(probe.label),
                                   describeNoteIds(songView.selectionModel().noteSelection()),
                                   describeNoteIds(selection), clickDiagnostics)))

A008 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:190
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the cross-band fixture assembly is a native harness precondition
a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands_data()
  {
      QTest::addColumn<int>("bandIndex");
      QTest::addColumn<int>("arrow");
      QTest::addColumn<QString>("direction");
      for (std::size_t index = 0; index < kCoreBands.size(); ++index) {
          for (const ArrowProbe &arrow : kCoreArrows) {
              QTest::newRow(qPrintable(QStringLiteral("%1: %2").arg(
                  QLatin1String(kCoreBands[index].label), QLatin1String(arrow.label))))
                  << int(index) << int(arrow.key) << QString::fromLatin1(arrow.label);
          }
      }
  }

  void SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands()
  {
      QFETCH(int, bandIndex);
      QFETCH(int, arrow);
      QFETCH(QString, direction);
      const BandProbe &probe = kCoreBands[std::size_t(bandIndex)];
      const auto key = Qt::Key(arrow);

      m_fixture = createFixture(std::nullopt);
Original expression:
  QVERIFY2(m_fixture,
               qPrintable(QStringLiteral("could not create the cross-band note fixture: %1")
                              .arg(m_lastFixtureError)))

A009 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:199
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; band-input availability observes the native Quick surface and
cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      SongView &songView = m_fixture->view();
      const std::vector<NoteId> selection{m_fixture->notes()[0], m_fixture->notes()[1]};

      m_fixture->configureDrawerSurface(drawerPageForBand(probe.band));
      songview::TimelineInputItem *const band = m_fixture->input(probe.item);
Original expression:
  QVERIFY2(band && QTest::qWaitFor([band, quick] {
                   return !band->bounds().isEmpty() && band->window() == quick;
               }),
               "Quick band input is unavailable")

A010 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:206
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; explicit band-focus staging requires the native window and cannot
be established by a presenter-level Swift check.
Source context since previous assertion:
  ;

      // Focus is explicit for every band's keyboard contract rather than
      // accidentally inherited from an incidental click.
Original expression:
  QVERIFY2(m_fixture->focusBand(probe.band),
               qPrintable(QStringLiteral("%1 band could not stage focus for arrows")
                              .arg(QLatin1String(probe.label))))

A011 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:213
Disposition: GAP
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the cross-band fixture note presence is a native harness
precondition with no Swift counterpart (the shellwindow lane stages its own
single selected velocity note instead, S009). Original predicate and setup below
remain an open obligation.
Source context since previous assertion:
  ;
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();

      const auto before = m_fixture->snapshotNotes();
Original expression:
  QVERIFY2(
          before.has_value(),
          qPrintable(QStringLiteral("%1 band: a cross-band fixture note vanished before an arrow")
                         .arg(QLatin1String(probe.label))))

A012 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:227
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; Quick-window key acceptance requires native delivery and cannot
be established by a presenter-level Swift check.
Source context since previous assertion:
  ;

      // Expected per-note deltas: only the two selected notes move. Horizontal
      // arrows snap one grid step from the first selected note's tick.
      const int64_t tick = int64_t(before->at(0).tick);
      const int64_t tickDelta =
          key == Qt::Key_Right  ? int64_t(songView.grid().snapTickUp(double(tick) + 1.0)) - tick
          : key == Qt::Key_Left ? int64_t(songView.grid().snapTickDown(double(tick) - 1.0)) - tick
                                : 0;
      const int keyDelta = key == Qt::Key_Up ? 1 : key == Qt::Key_Down ? -1 : 0;

Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, key),
               qPrintable(QStringLiteral("%1 band %2 arrow: Quick window did not accept the key")
                              .arg(QLatin1String(probe.label), direction)))

A013 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:238
Disposition: PARTIAL
Mapping/reason: Related predicates S010-S013 in
src/checks/editorqml/tst_ShellWindow.qml
(test_gEditorRoutedNoteKeysAndChromeArrows) deliver real arrow keys to the
focused production roll and observe the selected note transposition (Up +1,
Down back, Shift+Up +12, Shift+Down back), its nudge (Right advances the tick,
Left returns it) and its length (Shift+Right grows, Shift+Left restores).
Unproved as stated: invariance of the unselected fixture notes, the exact
snap-step tick delta (the shell row asserts only tick > original), and the
four-band row expansion (velocity, voice changes, ruler, other events).
Source context since previous assertion:
  ;

      for (std::size_t index = 0; index < before->size(); ++index) {
          const DocNote &noteBefore = before->at(index);
          const bool isSelected = index < 2;
          const int expectedKey = int(noteBefore.key) + (isSelected ? keyDelta : 0);
          const uint64_t expectedTick =
              uint64_t(int64_t(noteBefore.tick) + (isSelected ? tickDelta : 0));
          const auto actual = m_fixture->note(index);
Original expression:
  QVERIFY2(
              actual && int(actual->key) == expectedKey && actual->tick == expectedTick,
              qPrintable(
                  QStringLiteral("%1 band %2 arrow note[%3] (%4): "
                                 "actual=(present=%5,key=%6,tick=%7) "
                                 "expected=(key=%8,tick=%9)")
                      .arg(QLatin1String(probe.label), direction, QString::number(index),
                           isSelected ? QStringLiteral("selected") : QStringLiteral("unselected"),
                           actual.has_value() ? QStringLiteral("1") : QStringLiteral("0"),
                           actual ? QString::number(int(actual->key)) : QStringLiteral("-1"),
                           actual ? QString::number(actual->tick) : QStringLiteral("0"),
                           QString::number(expectedKey), QString::number(expectedTick))))

A014 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:251
Disposition: PARTIAL
Mapping/reason: Related predicate S014 in
src/checks/editorqml/tst_ShellWindow.qml
(test_gEditorRoutedNoteKeysAndChromeArrows): the edited source note stays
selected and resolvable by its original id after every routed arrow
(compare(selection.length, 1, "the edited source note stays selected")).
Unproved as stated: full noteSelection() vector equality over the whole
selection (the shell row tracks the single source note, not the pair) and the
four-band row expansion.
Source context since previous assertion:
  ;
      }
Original expression:
  QVERIFY2(songView.selectionModel().noteSelection() == selection,
               qPrintable(QStringLiteral("%1 band %2 arrow disturbed the selection: actual=%3 "
                                         "intended=%4")
                              .arg(QLatin1String(probe.label), direction,
                                   describeNoteIds(songView.selectionModel().noteSelection()),
                                   describeNoteIds(selection))))

A015 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:267
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the cross-band fixture assembly is a native harness precondition
a presenter-level Swift check cannot establish.
Source context since previous assertion:
  ;
  }

  void SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip()
  {
      // The arrows route through the production mergeable-move history. With
      // per-case isolation the history is pristine, so the undo proof runs
      // unconditionally; the old band loop gated it on every band's arrows
      // having been clean because a dirty history could have reverted an
      // unrelated leftover command.
      m_fixture = createFixture(std::nullopt);
Original expression:
  QVERIFY2(m_fixture,
               qPrintable(QStringLiteral("could not create the cross-band note fixture: %1")
                              .arg(m_lastFixtureError)))

A016 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:274
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; staging band focus for the undo proof requires the native window
and cannot be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      SongView &songView = m_fixture->view();
      const std::vector<NoteId> selection{m_fixture->notes()[0], m_fixture->notes()[1]};

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::OtherEvents),
               "could not stage band focus for the undo proof")

A017 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:279
Disposition: GAP
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; the fixture note presence before the undo proof is a native harness
precondition with no Swift counterpart. The shellwindow rows observe the selected
note before and after each arrow (S014) but assert no separate pre-undo snapshot.
Original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();
      const auto beforeUndo = m_fixture->snapshotNotes();
Original expression:
  QVERIFY2(beforeUndo.has_value(), "the fixture notes vanished before the undo proof")

A018 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:291
Disposition: PARTIAL
Mapping/reason: Related predicates S010-S013 in
src/checks/editorqml/tst_ShellWindow.qml
(test_gEditorRoutedNoteKeysAndChromeArrows): the shell delivers Up and then Down
through the production roll and observes the selected note key return to its
original value, proving the transposing press and its reversal by the opposite
arrow. Unproved as stated: the production mergeable-move history undo/redo
stages (the shell rows press the opposite arrow instead of traversing history)
and invariance of the unselected notes across stages.
Source context since previous assertion:
  ;

      // One transposing press proves increment/undo/redo preservation end to
      // end; the closing Down press cancels it through the same history.
      const auto expectShift = [this, &beforeUndo](const char *stage, int keyShift,
                                                   int64_t tickShift) {
          for (std::size_t index = 0; index < beforeUndo->size(); ++index) {
              const DocNote &noteBefore = beforeUndo->at(index);
              const int expectedKey = int(noteBefore.key) + (index < 2 ? keyShift : 0);
              const uint64_t expectedTick =
                  uint64_t(int64_t(noteBefore.tick) + (index < 2 ? tickShift : 0));
              const auto actual = m_fixture->note(index);
Original expression:
  QVERIFY2(
                  actual && int(actual->key) == expectedKey && actual->tick == expectedTick,
                  qPrintable(
                      QStringLiteral("undo proof %1 note[%2] (%3): "
                                     "actual=(present=%4,key=%5,tick=%6) "
                                     "expected=(key=%7,tick=%8)")
                          .arg(QLatin1String(stage), QString::number(index),
                               index < 2 ? QStringLiteral("selected") : QStringLiteral("unselected"),
                               actual.has_value() ? QStringLiteral("1") : QStringLiteral("0"),
                               actual ? QString::number(int(actual->key)) : QStringLiteral("-1"),
                               actual ? QString::number(actual->tick) : QStringLiteral("0"),
                               QString::number(expectedKey), QString::number(expectedTick))))

A019 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:306
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; Quick-window key acceptance requires native delivery and cannot
be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
          }
      };

Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, Qt::Key_Up),
               "Quick window did not accept the undo-proof key")

A020 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:313
Disposition: NATIVE
Mapping/reason: No equivalent predicate in the paired Swift transaction has been
established; Quick-window key acceptance requires native delivery and cannot
be established by a presenter-level Swift check.
Source context since previous assertion:
  ;
      expectShift("Up", +1, 0);
      m_fixture->document().undoStack()->undo();
      expectShift("Undo", 0, 0);
      m_fixture->document().undoStack()->redo();
      expectShift("Redo", +1, 0);
Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, Qt::Key_Down),
               "Quick window did not accept the undo-proof key")

Remaining original source (closing sequence and helper bodies):
               "Quick window did not accept the undo-proof key");
      expectShift("Down", 0, 0);
  }
Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:23
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:34
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:37
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:39
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:41
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:44
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:45
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:47
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")

S009 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:612
  function test_gEditorRoutedNoteKeysAndChromeArrows() {
      openTwoSongShell()
      var roll = findChild(selectedSurface(), "swiftRollInput")
      verify(roll && roll.visible, "the focused roll receives editor keys")
      selectDrawnVelocityNote(selectedSurface())
      compare(chosen.length, 1, "the pointer selected one source note")
  // Reaches the production editor through the shellwindow entry of
  // ShellQmlLane (src/checks/editorqml/ShellQmlTests.swift:46), which stages
  // mus_route101 and mus_littleroot_test; selectDrawnVelocityNote clicks the
  // drawn velocity node fill (:153-167).

S010 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:631
  keyClick(Qt.Key_Up)
  compare(selectedNote().pitch, original.pitch + 1, "Up transposes one semitone")
  keyClick(Qt.Key_Down)
  compare(selectedNote().pitch, original.pitch, "Down reverses Up")

S011 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:635
  keyClick(Qt.Key_Up, Qt.ShiftModifier)
  compare(selectedNote().pitch, original.pitch + 12, "Shift+Up transposes an octave")
  keyClick(Qt.Key_Down, Qt.ShiftModifier)
  compare(selectedNote().pitch, original.pitch, "Shift+Down reverses the octave")

S012 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:639
  keyClick(Qt.Key_Right)
  verify(selectedNote().tick > original.tick, "Right nudges the selected note forward")
  keyClick(Qt.Key_Left)
  compare(selectedNote().tick, original.tick, "Left returns the selected note")

S013 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:643
  keyClick(Qt.Key_Right, Qt.ShiftModifier)
  verify(selectedNote().duration > original.duration,
         "Shift+Right lengthens the selected note")
  keyClick(Qt.Key_Left, Qt.ShiftModifier)
  compare(selectedNote().duration, original.duration,
          "Shift+Left restores the selected note duration")

S014 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:622
  function selectedNote() {
      var selection = JSON.parse(grid.noteSummary).filter(function(note) {
          return note.selected && note.id === original.id
      })
      compare(selection.length, 1, "the edited source note stays selected")
      return selection[0]
  }
  // Every S010-S013 row reads through this helper, so each arrow observation
  // also carries the retained single-note selection.

Shared helpers
selectDrawnVelocityNote (src/checks/editorqml/tst_ShellWindow.qml:153) and
openTwoSongShell (:88) stage the fixture and click the drawn velocity note; they
carry no assertion of their own beyond the rows recorded in S009.
