Assertion correspondence: selectionkey/corearrows.cpp

Reference revision: acc555488c87b59885e5c58814626318039878ab
Original: src/checks/selectionkey/corearrows.cpp
Original SHA-256: 69dfaa2c94085416c2507de2b7167f5df3db2a327698490d34dda13bbe77d3e8
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftcore --verbose (controller-observed)
Result: PASS; build ok (0.15 s), ok swiftcore (10.38 s); verify 1/6 ok
(5 skipped); run_checks PASS. That run executes the paired prompt-transaction
predicates S001-S008 only.
Command: deno task verify:shell --filter shellwindow --verbose
(controller-observed)
Result: PASS; build ok (0.16 s), ok shellwindow (3.74 s); verify 1/11 ok

Command: deno task verify --filter swiftcore --verbose and deno task verify:shell --filter shellwindow --verbose
Result: PASS - task-58 implementer-recorded runs on the frozen sources: swiftcore 12/36
harnesses passed and the mounted shellwindow lane 1/26 selected harnesses passed. Evidence
build/proof-evidence/shellwindow.json (mtime 16:31:03) postdates tst_ShellWindow.qml
(16:30:46) and records test_gMultiNoteArrowSelectionAndHistory, whose QML anchors S015-S022
therefore classify executed. This task-58 ledger pass flipped the row dispositions and indexed
the added anchors; it ran no lane, rebuilt nothing and re-ran no check, so the controller
still owns the green re-run on the settled tree.

Counts: MATCHED 3, PARTIAL 0, GAP 1, RETIRED-REPRESENTATION 16
(10 skipped); run_checks PASS. That run executes the shellwindow predicates

A001 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:85
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(std::nullopt) plus the m_fixture check); the mounted lane stages its own Route 101 fixture.

A002 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:94
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick band input availability guard (non-empty bounds plus the Quick-window identity); the mounted lane resolves its roll and plot items by object name.

A003 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:124
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native click-point geometry: the probe resolves the band point from production geometry (selectedVelocityStemPoint, plainRulerPoint, band bounds centre); the mounted lane maps drawn note items into the roll.
Source context since previous assertion:
  ;

      // The click begins from the same eligible note selection every row uses.
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();

      std::optional<QPoint> point;
      QString clickDiagnostics;
      switch (kind) {
      case CoreClick::SelectedVelocityStemDrag: {
          const ClickTarget target = m_fixture->selectedVelocityStemPoint();
          point = target.point;
          clickDiagnostics = target.diagnostics;
          break;
      }
      case CoreClick::PlainRuler:
          point = m_fixture->plainRulerPoint(band);
          break;
      case CoreClick::BandCenter:
          point = band->mapToScene(band->bounds().center()).toPoint();
          break;
      }
      if (clickDiagnostics.isEmpty()) {
          clickDiagnostics = QStringLiteral("input-bounds=%1; chosen-window=%2")
                                 .arg(describeRect(band->bounds()))
                                 .arg(point ? describePoint(*point) : QStringLiteral("unavailable"));
      }
Original expression:
  QVERIFY2(point.has_value(), qPrintable(QStringLiteral("%1 band click point is unavailable: %2")
                                                 .arg(QLatin1String(probe.label), clickDiagnostics)))

A004 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:145
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native threshold-crossing pointer delivery for the selected velocity-stem drag (a startDragDistance+4 move on the native window); the mounted lane sends real mouse press/move/release.
Source context since previous assertion:
  ;

      if (kind == CoreClick::SelectedVelocityStemDrag) {
          const QPointF pressPoint(*point);
          const QPointF inputPoint = band->mapFromScene(pressPoint);
          const qreal dragY =
              qMin(band->bounds().bottom() - 2.0,
                   inputPoint.y() + QGuiApplication::styleHints()->startDragDistance() + 4.0);
          const QPoint dragPoint = band->mapToScene(QPointF(inputPoint.x(), dragY)).toPoint();
          bool delivered = checks::events::primeMouseMove(*quick, *band, *point);
          if (delivered) {
              stageMousePress(Qt::LeftButton, *point);
              stageMouseMove(dragPoint);
              delivered = QTest::qWaitFor([&] {
                  return songView.userGestureActive() &&
                         songView.selectionModel().noteSelection() == selection;
              });
              stageMouseRelease(Qt::LeftButton, dragPoint);
              selectionkey::settle();
          }
Original expression:
  QVERIFY2(delivered,
                   qPrintable(QStringLiteral("%1 band selected velocity-stem drag did not activate "
                                             "and retain the group: %2")
                                  .arg(QLatin1String(probe.label), clickDiagnostics)))

A005 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:150
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native click delivery (clickTimelineInput) into the production band; the mounted lane clicks QML items through the production surface.
Source context since previous assertion:
  ;
      } else {
Original expression:
  QVERIFY2(selectionkey::clickTimelineInput(quick, band, *point),
                   qPrintable(QStringLiteral("%1 band click did not reach its target band: %2")
                                  .arg(QLatin1String(probe.label), clickDiagnostics)))

A006 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:157
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native host focus identity (band->hasActiveFocus()); the mounted lane asserts QML activeFocus instead.
Source context since previous assertion:
  ;
      }

      // Ruler and other-events clicks intentionally do not focus.
      if (probe.clickTakesFocus)
Original expression:
  QVERIFY2(band->hasActiveFocus(), "Quick band input did not focus its band")

A007 | SelectionKeyCoreTest::incidentalBandClickPreservesSelection | src/checks/selectionkey/corearrows.cpp:159
Disposition: GAP
Mapping/reason: GAP - no Swift predicate stages an incidental band or chrome press that must preserve an already eligible note selection: the new multi-note journey's click extends the selection (S016), test_g's chrome press is a drawer resize drag whose retained selection belongs to the windowtier_gestures rows, and the empty-space clicks in the menu lane assert the time selection rather than the note selection. Missing assertion site: a band/ruler press with an eligible note selection that asserts noteSelection() is unchanged.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(songView.selectionModel().noteSelection() == selection,
               qPrintable(QStringLiteral("%1 band input disturbed the eligible selection: "
                                         "actual=%2 intended=%3 %4")
                              .arg(QLatin1String(probe.label),
                                   describeNoteIds(songView.selectionModel().noteSelection()),
                                   describeNoteIds(selection), clickDiagnostics)))

A008 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:190
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(std::nullopt) plus the m_fixture check) for the cross-band arrow fixture; the mounted lane stages its own fixture.

A009 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:199
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native Quick band input availability guard (non-empty bounds plus the Quick-window identity); the mounted lane resolves its roll item by object name.

A010 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:206
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging (focusBand(probe.band)) for the four-band arrow matrix; the mounted lane forces QML focus on the roll.
Source context since previous assertion:
  ;

      // Focus is explicit for every band's keyboard contract rather than
      // accidentally inherited from an incidental click.
Original expression:
  QVERIFY2(m_fixture->focusBand(probe.band),
               qPrintable(QStringLiteral("%1 band could not stage focus for arrows")
                              .arg(QLatin1String(probe.label))))

A011 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:213
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture note presence guard (snapshotNotes() before the arrow); the mounted lane stages and asserts its own two-note selection (S015).
Source context since previous assertion:
  ;
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();

      const auto before = m_fixture->snapshotNotes();
Original expression:
  QVERIFY2(
          before.has_value(),
          qPrintable(QStringLiteral("%1 band: a cross-band fixture note vanished before an arrow")
                         .arg(QLatin1String(probe.label))))

A012 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:227
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery (deliverKey into the Quick window) for the band arrow; the mounted lane sends a real keyClick.
Source context since previous assertion:
  ;

      // Expected per-note deltas: only the two selected notes move. Horizontal
      // arrows snap one grid step from the first selected note's tick.
      const int64_t tick = int64_t(before->at(0).tick);
      const int64_t tickDelta =
          key == Qt::Key_Right  ? int64_t(songView.grid().snapTickUp(double(tick) + 1.0)) - tick
          : key == Qt::Key_Left ? int64_t(songView.grid().snapTickDown(double(tick) - 1.0)) - tick
                                : 0;
      const int keyDelta = key == Qt::Key_Up ? 1 : key == Qt::Key_Down ? -1 : 0;

Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, key),
               qPrintable(QStringLiteral("%1 band %2 arrow: Quick window did not accept the key")
                              .arg(QLatin1String(probe.label), direction)))

A013 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:238
Disposition: MATCHED
Mapping: S017, S018, S015, S016 (only selected notes move by one key or one exact snap step, the unselected notes stay byte-identical; the four-band matrix is substituted by the mounted roll band, and the exact snap step comes from grid.snapTicks)
Source context since previous assertion:
  ;

      for (std::size_t index = 0; index < before->size(); ++index) {
          const DocNote &noteBefore = before->at(index);
          const bool isSelected = index < 2;
          const int expectedKey = int(noteBefore.key) + (isSelected ? keyDelta : 0);
          const uint64_t expectedTick =
              uint64_t(int64_t(noteBefore.tick) + (isSelected ? tickDelta : 0));
          const auto actual = m_fixture->note(index);
Original expression:
  QVERIFY2(
              actual && int(actual->key) == expectedKey && actual->tick == expectedTick,
              qPrintable(
                  QStringLiteral("%1 band %2 arrow note[%3] (%4): "
                                 "actual=(present=%5,key=%6,tick=%7) "
                                 "expected=(key=%8,tick=%9)")
                      .arg(QLatin1String(probe.label), direction, QString::number(index),
                           isSelected ? QStringLiteral("selected") : QStringLiteral("unselected"),
                           actual.has_value() ? QStringLiteral("1") : QStringLiteral("0"),
                           actual ? QString::number(int(actual->key)) : QStringLiteral("-1"),
                           actual ? QString::number(actual->tick) : QStringLiteral("0"),
                           QString::number(expectedKey), QString::number(expectedTick))))

A014 | SelectionKeyCoreTest::arrowsMoveSelectedNotesAcrossBands | src/checks/selectionkey/corearrows.cpp:251
Disposition: MATCHED
Mapping: S019, S016 (the selection vector is identical after the arrow: the selected id list is compared in order, covering the whole two-note selection rather than the single source note)
Source context since previous assertion:
  ;
      }
Original expression:
  QVERIFY2(songView.selectionModel().noteSelection() == selection,
               qPrintable(QStringLiteral("%1 band %2 arrow disturbed the selection: actual=%3 "
                                         "intended=%4")
                              .arg(QLatin1String(probe.label), direction,
                                   describeNoteIds(songView.selectionModel().noteSelection()),
                                   describeNoteIds(selection))))

A015 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:267
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture-creation guard (createFixture(std::nullopt) plus the m_fixture check) for the undo-proof fixture; the mounted lane stages its own fixture.

A016 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:274
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native focus staging (focusBand(TimelineBand::OtherEvents)) for the undo proof; the mounted lane forces QML focus on the roll.
Source context since previous assertion:
  ;
      QQuickWindow *const quick = m_fixture->window();
      SongView &songView = m_fixture->view();
      const std::vector<NoteId> selection{m_fixture->notes()[0], m_fixture->notes()[1]};

Original expression:
  QVERIFY2(m_fixture->focusBand(songview::TimelineBand::OtherEvents),
               "could not stage band focus for the undo proof")

A017 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:279
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native fixture note presence guard (snapshotNotes() before the undo proof); the mounted journey captures its own pre-arrow summary and compares byte-identical summaries (S020).
Source context since previous assertion:
  ;
      songView.selectionModel().setNoteSelection(selection);
      selectionkey::settle();
      const auto beforeUndo = m_fixture->snapshotNotes();
Original expression:
  QVERIFY2(beforeUndo.has_value(), "the fixture notes vanished before the undo proof")

A018 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:291
Disposition: MATCHED
Mapping: S020, S021, S022 (one undo restores the pre-arrow state and one redo reapplies the selected-note arrow, both as byte-identical summaries; the closing Left returns the moved selection by one snap)
Source context since previous assertion:
  ;

      // One transposing press proves increment/undo/redo preservation end to
      // end; the closing Down press cancels it through the same history.
      const auto expectShift = [this, &beforeUndo](const char *stage, int keyShift,
                                                   int64_t tickShift) {
          for (std::size_t index = 0; index < beforeUndo->size(); ++index) {
              const DocNote &noteBefore = beforeUndo->at(index);
              const int expectedKey = int(noteBefore.key) + (index < 2 ? keyShift : 0);
              const uint64_t expectedTick =
                  uint64_t(int64_t(noteBefore.tick) + (index < 2 ? tickShift : 0));
              const auto actual = m_fixture->note(index);
Original expression:
  QVERIFY2(
                  actual && int(actual->key) == expectedKey && actual->tick == expectedTick,
                  qPrintable(
                      QStringLiteral("undo proof %1 note[%2] (%3): "
                                     "actual=(present=%4,key=%5,tick=%6) "
                                     "expected=(key=%7,tick=%8)")
                          .arg(QLatin1String(stage), QString::number(index),
                               index < 2 ? QStringLiteral("selected") : QStringLiteral("unselected"),
                               actual.has_value() ? QStringLiteral("1") : QStringLiteral("0"),
                               actual ? QString::number(int(actual->key)) : QStringLiteral("-1"),
                               actual ? QString::number(actual->tick) : QStringLiteral("0"),
                               QString::number(expectedKey), QString::number(expectedTick))))

A019 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:306
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery (deliverKey Up) for the undo proof; the mounted journey sends real arrow keys.
Source context since previous assertion:
  ;
          }
      };

Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, Qt::Key_Up),
               "Quick window did not accept the undo-proof key")

A020 | SelectionKeyCoreTest::mergeableMoveHistorySupportsUndoRedoRoundTrip | src/checks/selectionkey/corearrows.cpp:313
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION - native key delivery (deliverKey Down) for the undo proof; the mounted journey sends real arrow keys.
Source context since previous assertion:
  ;
      expectShift("Up", +1, 0);
      m_fixture->document().undoStack()->undo();
      expectShift("Undo", 0, 0);
      m_fixture->document().undoStack()->redo();
      expectShift("Redo", +1, 0);
Original expression:
  QVERIFY2(selectionkey::deliverKey(quick, Qt::Key_Down),
               "Quick window did not accept the undo-proof key")

Remaining original source (closing sequence and helper bodies):
               "Quick window did not accept the undo-proof key");
      expectShift("Down", 0, 0);
  }
Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original selected fixture note exists"

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original empty-tick numeric prompt opens"

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the production prompt retains the supplied numeric draft"

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing does not mutate the song"

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing preserves note selection"

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation closes the prompt"

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation leaves the original document unchanged"

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft and cancellation preserve the full song contents"

S009 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the pointer selected one source note"

S010 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Down reverses Up"

S011 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Shift+Down reverses the octave"

S012 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Left returns the selected note"

S013 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Shift+Left restores the selected note duration"

S014 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the edited source note stays selected"


S015 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "two visible notes can form an arrow selection"

S016 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the click extends the note selection"

S017 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "only selected notes move by one key or one snap"

S018 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "unselected notes stay byte-identical"

S019 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the selection vector is identical after the arrow"

S020 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "one undo restores the pre-arrow state"

S021 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "one redo reapplies the selected-note arrow"

S022 | test_gMultiNoteArrowSelectionAndHistory | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Left moves the selected notes back by one snap"

Shared helpers
selectDrawnVelocityNote (src/checks/editorqml/tst_ShellWindow.qml) and
openTwoSongShell (:88) stage the fixture and click the drawn velocity note; they
carry no assertion of their own beyond the rows recorded in S009.
