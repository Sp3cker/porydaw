Assertion correspondence: selectionkey/windowtier_keyboard.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/selectionkey/windowtier_keyboard.cpp
Original SHA-256: f00861f60ece3fc2b9de78b2c6b886d6139a71f79c7c07aeb5d1c8a69a5730d0
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Additional Swift counterpart: src/checks/editorqml/ShellQmlTests.swift
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files

Status and evidence
The C++ file above was deleted; this inventory audits its parent-revision content (pinned by Reference revision and SHA-256) so no assertion is dropped during the Swift check migration. This is an assertion-site inventory, not a count-based equivalence claim. Every original macro/failure site appears below, including fixture guards. Source context preserves setup, rows, loops, and guards between assertions verbatim. S identifiers identify exact Swift predicates in the paired numeric-prompt transaction and in the frozen shellwindow Qt Quick lane, listed after the inventory. The prompt keyboard-ownership block's lane lookup and insertion-prompt open (volume lane, tick 5808, value 48) are MATCHED by S011/S012/S013; the remaining block sites (draft selectedText, no mutation, preserved selection, Escape close) are PARTIAL against that transaction. The window Copy/Solo case's one-selected-note setup, its window Copy/Solo single-key bindings, the Copy activation count, the decoded native clipboard, the document-dirty fact, the first and second Solo activation counts with their soloed states, and the window-Space-priority sites are MATCHED by the shellwindow lane predicates S018 through S036, with the parameter-label and tap-button sites carrying the drawer-toggle substitution recorded in their reasons. The keyboard-routing, grip, Tab-traversal, drawer-height, tap-tempo and committed-edit observations outside the sites named above stay NATIVE or GAP. MATCHED identifies equivalent model predicates; execution must still be verified. PARTIAL identifies related Swift predicates with an explicit unproved condition. NATIVE identifies retained native-window/rendering or native setup obligations; it does not establish coverage of any converted window. GAP identifies an original predicate without a replacement assertion. Unsure between PARTIAL and GAP was resolved to GAP. Native passes do not prove the Swift replacement.

Verification
Command: deno task verify --filter swiftcore --verbose (controller-observed)
Result: PASS; build ok (0.15 s), ok swiftcore (10.38 s); verify 1/6 ok (5 skipped); run_checks PASS. That run executes the paired prompt-transaction predicates S001-S017 only.
Command: deno task verify:shell --filter shellwindow --verbose (controller-observed)
Result: PASS; build ok (4.47 s), ok shellwindow (3.87 s); verify 1/11 ok (10 skipped); run_checks PASS. That run executes the shellwindow predicates S018-S036 plus the grip/chrome rows S037-S040 and the parameter-tab/tap rows S041-S060 through the shellwindow entry of ShellQmlTests.swift (src/checks/editorqml/ShellQmlTests.swift).
Result (post-fix re-pin, this checkout): PASS; build ok (6.64 s), ok swiftcore (10.58 s); verify 1/6 ok (5 skipped); run_checks PASS — the paired prompt-transaction predicates S001-S017 still pass against localinputtier_text.swift de186cca (S-line audit: all cited rows unshifted). Post-fix shellwindow: build ok (4.64 s), verify 1/11 ok (3.87 s, 10 skipped); run_checks PASS — the shellwindow predicates S018-S060 pass; tst_ShellWindow.qml unchanged at 39595ccd.
The deleted C++ harness is not registered in this checkout; its cases are reference inventories only, never current execution evidence.

drawerOriginalNumericPromptTransaction in src/checks/selectionkey/localinputtier_text.swift (called by the swiftcore session suite) executes the paired prompt-transaction predicates (S002 through S017); the window-tier Copy/Solo, clipboard, document-dirty, soloed-state and window-Space-priority sites are executed by test_cWindowShortcutsAndNumericOwnership in src/checks/editorqml/tst_ShellWindow.qml (the shellwindow entry of ShellQmlTests.swift), whose predicates are S018 through S036; the grip-locality sites are executed by test_gEditorRoutedNoteKeysAndChromeArrows (grip rows S037 through S039) and the chrome-Space selection site by test_hKeyboardFocusAndDrawerSpacePriority (S040); the parameter-tab activation and tap-cession sites are executed by test_kParameterTabActivationAndTapCession (tab/transport rows S041 through S060); the file's remaining keyboard-routing, Tab-traversal, drawer-height and committed-edit cases have no executing Swift entry point. The drawerAutomationAutomationFixture struct carries no report predicates, so no Shared helper section applies. The transaction also carries a report.fail path for fixture-note creation failure, which short-circuits before any predicate; no deleted C++ site corresponds to it.

Fixture, sequence and row constraints
Copy/solo count rows, grip-arrow rows, label-activation rows, and the prompt keyboard-ownership block (open, select-all, copy, delete, paste, Escape, then post-prompt copy/remove/paste/move rows) expand one site per delivered key. The note-move loop expands tick/key assertions per selected note id; tap-button rows expand Enter, Return, auto-repeat, and Space delivery into per-key count and document-identity sites.
Original setup, branch guards and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:97
Disposition: MATCHED
Mapping/reason: test_cWindowShortcutsAndNumericOwnership starts from a real one-note selection on the production two-song project: selectDrawnVelocityNote clicks the drawn velocity note and observes selectedCount 1 (S019), and the drawn grid model yields exactly one selected note (S023, "the real node click selected one note"), with openTwoSongShell staging that project and second song (S018). The reserved tick-960 fixture note pair is replaced by that staged velocity note; no QWidget layer exists in the Swift app.
Source context since previous assertion:
  // Selection keyboard routing through the production Quick window: parameter
  // labels keep activation local and leave shared editing commands to SongView.
  // Drawer grips and toggles keep their local Enter/Return and arrow keys while
  // bare Space stays with the window transport shortcut.

  #include "checks/selectionkey/tst_windowtier.h"

  #include "checks/automation/automationvalueprompt.h"

  #include "checks/support/timelinequickcheck.h"

  #include "ui/editordrawer/automationpage.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/layout.h"
  #include "ui/songview/clipmime.h"
  #include <QClipboard>
  #include <QGuiApplication>
  #include <QQuickItem>
  #include <QSignalSpy>
  #include <QStringList>
  #include <QStyleHints>
  #include <QUndoStack>
  #include <cstdio>

  namespace {

  constexpr int kTrack = 0;
  constexpr uint8_t kController = 10;
  constexpr uint8_t kSecondController = 1;

  // Full-control Tab traversal precondition, scoped to the keyboard traversal
  // cases. macOS hosts commonly run the text-only keyboard navigation policy
  // (AppleKeyboardUIMode 0); Qt Quick then skips every non-text control during
  // Tab traversal — QQuickItemPrivate::canAcceptTabFocus only honors
  // activeFocusOnTab for editable text and list/table roles — so plan 13's real
  // Tab delivery to the drawer grip and toggle could never arrive no
  // matter how the fixture is staged. Qt exposes the policy as per-process
  // in-memory state: this guard sets Qt::TabFocusAllControls and restores the
  // previous Qt value at scope end. It never touches the user's OS settings and
  // never substitutes focus synthesis.
  class FullControlTabTraversal final
  {
    public:
      FullControlTabTraversal() : m_previous(QGuiApplication::styleHints()->tabFocusBehavior())
      {
          QGuiApplication::styleHints()->setTabFocusBehavior(Qt::TabFocusAllControls);
      }

      ~FullControlTabTraversal() { QGuiApplication::styleHints()->setTabFocusBehavior(m_previous); }

      FullControlTabTraversal(const FullControlTabTraversal &) = delete;
      FullControlTabTraversal &operator=(const FullControlTabTraversal &) = delete;

    private:
      Qt::TabFocusBehavior m_previous;
  };

  // Real Tab traversal from the currently focused item until one of the named
  // chrome controls owns active focus. The step bound follows the live tab chain:
  // every track row contributes two header toggles plus the add-track row, and
  // the ruler controls, the three scrollbars, and the visible drawer chrome come
  // after (or wrap before) them. On exhaustion it reports where traversal ended
  // so a failure names the trap, not just "never arrived".
  QQuickItem *tabTo(QQuickWindow *window, const QStringList &targets, int maxSteps)
  {
      QString lastFocus;
      for (int step = 0; step < maxSteps; ++step) {
          QTest::keyClick(window, Qt::Key_Tab);
          selectionkey::settle();
          QQuickItem *const focus = window->activeFocusItem();
          if (!focus)
              continue;
          lastFocus = focus->objectName();
          if (targets.contains(lastFocus))
              return focus;
      }
      std::fprintf(stderr, "selectionkeywindowcheck: tab traversal stopped at '%s' after %d steps\n",
                   qPrintable(lastFocus), maxSteps);
      return nullptr;
  }

  int chromeTraversalBound(SongDocument &document)
  {
      return 2 * document.engineTrackCount() + 21;
  }

  } // namespace

  void SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce()
  {
      SongView &view = this->view();
      const selectionkey::ScenarioRollback rollback(view, document());
      view.selectTrack(kTrack);
      view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      selectionkey::settle();
      const std::optional<NotePair> pair = addNotePair(document(), kTrack, 960);

Original expression:
  QVERIFY2(pair.has_value(),
               "the reserved tick-960 note pair could not be inserted and resolved")

A002 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:99
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(focusAutomationBand(view), "could not focus the automation band")

A003 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:102
Disposition: MATCHED
Mapping/reason: The production presenter carries both single-key window bindings: actionSequences("roll.copy").length > 0 (S020) and actionSequences("roll.solo_tracks")[0] == "S" (S021). The QWidget-era binding lookup over the Quick window is replaced by the native ShellPresenter actionSequences; no QWidget layer exists in the Swift app.
Source context since previous assertion:
  ;
      const auto copy = selectionkey::firstBinding(QStringLiteral("roll.copy"));
      const auto solo = selectionkey::firstBinding(QStringLiteral("roll.solo_tracks"));

Original expression:
  QVERIFY2(copy.has_value() && solo.has_value(), "window Copy/Solo have no single-key bindings")

A004 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:108
Disposition: MATCHED
Mapping/reason: The production window Copy Shortcut (shellShortcut_roll.copy) is activated exactly once by native StandardKey.Copy delivered to the focused production roll: keySequence(StandardKey.Copy) then copyActivatedSpy.count == 1 (S026, "native Copy activates the window shortcut once"), after the roll recovers active focus from the Edit menu (S025). The QML Shortcut activated signal count replaces the QWidget QAction activation counter; QAction pointer identity is not claimed.
Source context since previous assertion:
  ;

      view.selectionModel().setNoteSelection({pair->ids[0], pair->ids[1]});
      const QByteArray before = document().smf().write();

      selectionkey::deliverKey(quickWindow(), copy->key(), copy->keyboardModifiers());

Original expression:
  QCOMPARE(m_counts.copy, 1)

A005 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:109
Disposition: MATCHED
Mapping/reason: The native clipboard written by that window Copy decodes through production pd_clipboard_read and ClipboardCodec.decode into one track, one selected note, and that note key equal to the selected pitch (S027: clip[0] == 1, clip[1] == 1, clip[2] == copied[0].pitch; helper S036, ShellQmlBootstrap.copiedClipSummary). A decodable native payload replaces the QWidget presence-only clip-MIME probe; no QWidget clipboard layer exists in the Swift app.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(songview::clipboardHasClipMime(),
               "window Copy from timeline/automation focus did not capture the selected notes")

A006 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:111
Disposition: MATCHED
Mapping/reason: The native session documentDirty is false from a clean start (S024) and remains false across the window Copy (S028, "the native window Copy never edits the song"), so that Copy edited nothing. The Swift shell reports document mutation through the session dirty flag instead of comparing SMF bytes; no QWidget document snapshot exists.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document().smf().write() == before, "window Copy mutated the document")

A007 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:114
Disposition: MATCHED
Mapping/reason: One real S key press delivered to a focused non-text window child activates the production Solo window Shortcut exactly once: soloActivatedSpy.count == 1 (S029, "first non-text S activates Solo once"). The QML Shortcut activated signal count replaces the QWidget QAction activation counter; QAction pointer identity is not claimed.
Source context since previous assertion:
  ;

      selectionkey::deliverKey(quickWindow(), solo->key(), solo->keyboardModifiers());

Original expression:
  QCOMPARE(m_counts.solo, 1)

A008 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:115
Disposition: MATCHED
Mapping/reason: That same single Solo press leaves the selected track header delegate soloChecked true (S029: tryCompare(firstTrack, "soloChecked", true)). The Swift shell exposes the soloed state on the drawn header delegate; no QWidget track widget exists.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(view.trackSoloed(kTrack), "one Solo press did not leave the track soloed once")

A009 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:118
Disposition: MATCHED
Mapping/reason: A second real S press activates Solo a second time and untoggles exactly once: soloActivatedSpy.count == 2 (S030) with soloChecked back to false (S030: tryCompare(firstTrack, "soloChecked", false)). QML Shortcut signal counts replace the QWidget QAction counter.
Source context since previous assertion:
  ;

      selectionkey::deliverKey(quickWindow(), solo->key(), solo->keyboardModifiers());

Original expression:
  QVERIFY2(m_counts.solo == 2 && !view.trackSoloed(kTrack),
               "the second Solo press did not untoggle exactly once")

A010 | SelectionWindowTierTest::windowCopySoloExecutesExactlyOnce | src/checks/selectionkey/windowtier_keyboard.cpp:122
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      selectionkey::deliverKey(quickWindow(), Qt::Key_F24);

Original expression:
  QVERIFY2(m_counts.copy == 1 && m_counts.solo == 2 && document().smf().write() == before,
               "an unrecognized key triggered a window action or mutated the song")

A011 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:138
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

      view.selectionModel().clearNoteSelection();
  }

  void SelectionWindowTierTest::chromeGripKeysStayLocal()
  {
      SongView &view = this->view();
      SongDocument &document = this->document();
      const selectionkey::ScenarioRollback rollback(view, document);
      const FullControlTabTraversal fullControlTabTraversal;
      view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      view.setDrawerSectionHeight(EditorDrawerPage::Automations, 200);
      view.selectTrack(kTrack);
      const std::optional<NotePair> pair = addNotePair(document, kTrack, 2400);

Original expression:
  QVERIFY2(pair.has_value(),
               "the reserved tick-2400 note pair could not be inserted and resolved")

A012 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:140
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(focusAutomationBand(view), "could not focus the automation band")

A013 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:146
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      view.selectionModel().setNoteSelection({pair->ids[0], pair->ids[1]});
      // The resize grip: real Tab traversal from the focused automation band to
      // the grip, whose visible focus border is activeFocus-driven.
      const QPointer<QQuickItem> grip = tabTo(
          quickWindow(), {QStringLiteral("drawerAutomationHandle")}, chromeTraversalBound(document));

Original expression:
  QVERIFY2(grip, "Tab traversal never reached the automation resize grip")

A014 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:147
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(grip->hasActiveFocus(),
               "the automation resize grip does not show its advertised active focus")

A015 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:155
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      const int copyBefore = m_counts.copy;
      const int soloBefore = m_counts.solo;
      const int gripStep = layout::space(layout::Space::Two);
      const int heightBefore = view.drawerSectionHeight(EditorDrawerPage::Automations);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Up);
      const int heightAfterUp = view.drawerSectionHeight(EditorDrawerPage::Automations);

Original expression:
  QCOMPARE(heightAfterUp, heightBefore + gripStep)

A016 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:158
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Down);
      const int heightAfterDown = view.drawerSectionHeight(EditorDrawerPage::Automations);

Original expression:
  QCOMPARE(heightAfterDown, heightBefore)

A017 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:159
Disposition: PARTIAL
Mapping/reason: Related predicates S037-S039 in src/checks/editorqml/tst_ShellWindow.qml (test_gEditorRoutedNoteKeysAndChromeArrows): the drawerHandle_velocity grip takes focus (:653-654) and resizes the drawer on Up/Down (S038) and the full note summary is unchanged across all four grip arrows (S039 against the S037 snapshot, "grip arrows never mutate selected notes"). Unproved as stated: the reserved tick-2400 note pair (the shell tracks one drawn velocity note), the drawerAutomationHandle chrome, and the exact integer grip-step equality.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(notePairUnchanged(document, *pair), "grip arrow keys moved the selected notes")

A018 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:160
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(m_counts.copy == copyBefore && m_counts.solo == soloBefore,
               "grip arrow keys triggered a window action")

A019 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:164
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Left);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Right);

Original expression:
  QCOMPARE(view.drawerSectionHeight(EditorDrawerPage::Automations), heightBefore)

A020 | SelectionWindowTierTest::chromeGripKeysStayLocal | src/checks/selectionkey/windowtier_keyboard.cpp:165
Disposition: PARTIAL
Mapping/reason: Related predicates S037 and S039 in src/checks/editorqml/tst_ShellWindow.qml (test_gEditorRoutedNoteKeysAndChromeArrows): Left/Right on the focused drawerHandle_velocity grip leave the full note summary equal to the pre-arrow snapshot (S039, "grip arrows never mutate selected notes", snapshot S037). Unproved as stated: the reserved tick-2400 note pair (the shell tracks one drawn velocity note) and the drawerAutomationHandle chrome.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(notePairUnchanged(document, *pair),
               "cross-axis arrows on the focused grip moved the selected notes")

A021 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:183
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
  }

  void SelectionWindowTierTest::parameterLabelActivationAndSharedCommands()
  {
      SongView &view = this->view();
      SongDocument &document = this->document();
      const selectionkey::ScenarioRollback rollback(view, document);
      view.selectTrack(kTrack);
      view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      view.setDrawerSectionHeight(EditorDrawerPage::Automations, 200);
      constexpr uint64_t tick = 5760;
      constexpr uint64_t pasteTick = 7680;
      document.addLanePoint(kTrack, kController, tick, 32);
      document.addLanePoint(kTrack, kSecondController, tick, 96);
      document.addLanePoint(kTrack, 7, tick, 48);
      const auto pair = addNotePair(document, kTrack, 2400);

Original expression:
  QVERIFY(pair.has_value())

A022 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:184
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(focusAutomationBand(view))

A023 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:187
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      auto *const canvas = view.editorDrawer()->automationPage()->canvas();
      auto *const quick = selectionkey::quickCanvas(view);

Original expression:
  QVERIFY(canvas && quick)

A024 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:219
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      songview::EditorSelectionModel::TimeSelection selected;
      selected.startTick = tick;
      selected.endTick = tick + 24;
      selected.scope = songview::EditorSelectionModel::TimeSelection::Lanes;
      selected.lanes = {{kTrack, kController}, {kTrack, kSecondController}};
      view.selectionModel().setTimeSelection(selected);
      const auto selectionUnchanged = [&] {
          const auto &actual = view.selectionModel().timeSelection();
          return actual.startTick == selected.startTick && actual.endTick == selected.endTick &&
                 actual.scope == selected.scope && actual.lanes == selected.lanes &&
                 actual.tempo == selected.tempo && view.selectionModel().noteSelection().empty();
      };
      const QByteArray before = document.smf().write();
      const int undoBefore = document.undoStack()->index();
      const uint64_t cursorBefore = view.editCursorTick();
      QPointer<QQuickItem> label;
      const auto focusParameter = [&](uint8_t controller) {
          const int index = checks::support::automationParameterIndex(
              *canvas, {EditorAutomationRowKind::ControlChange, kTrack, controller});
          if (index < 0 || !QTest::qWaitFor([&] {
                  label = checks::support::visualDescendant(
                      quick->rootObject(), QStringLiteral("automationParameterTab%1").arg(index));
                  return label && label->window() == quickWindow() && label->isVisible() &&
                         label->isEnabled() && label->width() > 0 && label->height() > 0;
              }))
              return false;
          label->forceActiveFocus(Qt::OtherFocusReason);
          selectionkey::settle();
          return label && label->hasActiveFocus();
      };

Original expression:
  QVERIFY(focusParameter(kController))

A025 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:225
Disposition: MATCHED
Mapping/reason: The pre-Space precondition is observed directly on the production parameter tabs: firstLabel takes keyboard focus (:977-979) while still unchecked (S041, :980, the focused label is not yet active), so it is not the active parameter before Space. The tabs are plain AutomationTabs.qml controls whose local keys are Enter/Return only; the focused-but-inactive state is stable until activation.
Source context since previous assertion:
  ;
      // Bare Space on a focused non-active label is transport input, not label
      // activation: the window-level play/pause shortcut outranks incidental
      // focus in this persistent control.
      const auto activeBeforeSpace = canvas->parameterRow(canvas->activeParameter());
      const EditorAutomationRowId pan{EditorAutomationRowKind::ControlChange, kTrack, kController};

Original expression:
  QVERIFY2(canvas->parameterRow(canvas->activeParameter()) != std::optional{pan},
               "the focused parameter label was already the active parameter before Space")

A026 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:229
Disposition: MATCHED
Mapping/reason: The window-Space-priority rule is exercised by the drawer-toggle probe: with the production drawerToggle_velocity owning active focus (S031) and the production transport.play_pause sequence bound to Space (S022), bare Space drives the transport presenter playing state while the focused section visibility and documentDirty stay unchanged (S032-S034). The original focused parameter-label chrome has no focusable Swift-shell counterpart, so the persistent drawer toggle (Rectangle with activeFocusOnTab and Return/Enter-only local keys) stands in for that retired chrome; the pinned local no-change fact is observed on that control, and the original playPause trigger count is observed as a presenter playing-state transition rather than a native signal count. Extended by S042 (:988-999): bare Space on the focused inactive parameter label drives transport playing true while the label stays unchecked, the active tab is unchanged, documentDirty stays false, and the note summary is unchanged; the second Space stops transport (:1000-1002). The AutomationTabs.qml labels claim only plain Enter/Return, so unclaimed Space cedes to the window transport shortcut.
Source context since previous assertion:
  ;
      QSignalSpy playPause(m_workspace, &WorkspaceUi::playPauseRequested);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Space);

Original expression:
  QVERIFY2(playPause.count() == 1 &&
                   canvas->parameterRow(canvas->activeParameter()) == activeBeforeSpace &&
                   document.smf().write() == before && selectionUnchanged(),
               "bare Space on a focused parameter label did not route exactly one "
               "transport play/pause request with no label, document or selection change")

A027 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:238
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      // Enter/Return are the advertised local label-activation keys: each one
      // activates exactly once without retargeting selection or editing.
      QSignalSpy parameterChanges(canvas, &AutomationCanvas::activeParameterChanged);

Original expression:
  QVERIFY(focusParameter(kSecondController))

A028 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:242
Disposition: MATCHED
Mapping/reason: Enter on the focused inactive label activates it exactly once: keyClick Enter (:1003) drives firstLabel checked true (S043, :1004-1005, Enter activates the focused label once). AutomationTabs.qml binds plain Enter with no modifiers to local activation, matching the original deliverKey Enter; the signal-count half (A029) stays GAP since no activeParameterChanged signal is staged.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Enter);
      const EditorAutomationRowId modulation{EditorAutomationRowKind::ControlChange, kTrack,
                                             kSecondController};

Original expression:
  QCOMPARE(canvas->parameterRow(canvas->activeParameter()), std::optional{modulation})

A029 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:243
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(parameterChanges.count(), 1)

A030 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:244
Disposition: PARTIAL
Mapping/reason: Partial via S044 (:1007-1009): label Enter leaves the note summary, edit cursor tick, and track index unchanged, but the time-selection half of selectionUnchanged() is unproved — the shell lane stages no time selection — and byte-level document identity is pinned only through the dirty flag (S046).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A031 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:245
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(focusParameter(7))

A032 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:248
Disposition: MATCHED
Mapping/reason: Return on the focused second label retargets activation exactly once: keyClick Return (:1013) drives secondLabel checked true (:1014-1015) while firstLabel.checked drops to false (S045, :1016, Return retargets activation exactly once). AutomationTabs.qml binds plain Return to the same local activation as Enter; the signal-count half (A033) stays GAP.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Return);
      const EditorAutomationRowId volume{EditorAutomationRowKind::ControlChange, kTrack, 7};

Original expression:
  QCOMPARE(canvas->parameterRow(canvas->activeParameter()), std::optional{volume})

A033 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:249
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(parameterChanges.count(), 2)

A034 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:250
Disposition: MATCHED
Mapping/reason: Label activation never edits the song: documentDirty stays false after Enter (S046, :1006) and after Return (:1017). The dirty flag is the Swift-shell observable for the original byte-level SMF identity; the undo-index half (A035) stays GAP since no QML undo stack is staged.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.smf().write(), before)

A035 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:251
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.undoStack()->index(), undoBefore)

A036 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:252
Disposition: MATCHED
Mapping/reason: Label activation never moves the cursor: grid.editCursorTick equals cursorBefore after Enter (S047, :1008) and after Return (:1019).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.editCursorTick(), cursorBefore)

A037 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:253
Disposition: MATCHED
Mapping/reason: Label Enter never retargets the track: grid.trackIndex equals trackBefore (S048, :1009).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.selectionModel().primaryTrack(), kTrack)

A038 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:254
Disposition: PARTIAL
Mapping/reason: Partial via S044 (:1007-1009): the Enter-side note/cursor/track no-change triple bounds the Return activation, whose own note-summary no-change is exercised at :1018 without a split S row; the time-selection half of selectionUnchanged() stays unproved.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A039 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:258
Disposition: MATCHED
Mapping/reason: Up on the focused active label never edits the song: documentDirty stays false (S049, :1022, Up on a label never edits the song).
Source context since previous assertion:
  ;

      // Up is a shared lane-selection no-op, not selector navigation.
      selectionkey::deliverKey(quickWindow(), Qt::Key_Up);

Original expression:
  QCOMPARE(document.smf().write(), before)

A040 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:259
Disposition: MATCHED
Mapping/reason: Up never retargets the active label: secondLabel.checked stays true (S049, :1021, Up never retargets the active label).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(canvas->parameterRow(canvas->activeParameter()), std::optional{volume})

A041 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:260
Disposition: MATCHED
Mapping/reason: Up on a label never moves the selection: grid note summary equals notesBefore (S049, :1023).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A042 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:265
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      const auto copy = selectionkey::firstBinding(QStringLiteral("roll.copy"));
      const auto remove = selectionkey::firstBinding(QStringLiteral("roll.delete"));
      const auto selectAll = selectionkey::firstBinding(QStringLiteral("roll.select_all"));
      const auto paste = selectionkey::firstBinding(QStringLiteral("roll.paste"));

Original expression:
  QVERIFY(copy && remove && selectAll && paste)

A043 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:274
Disposition: MATCHED
Mapping/reason: The original scans the canvas row list for the track-0 volume lane; the Swift page publishes the same catalog row stack and the predicate observes the lane's presence.
Swift counterpart expression (S011):
  report.expect(page.rows.contains { $0.parameter == fixture.volumeLane }, cppID: labelID, ...)
Source context since previous assertion:
  ;
      const int copiesBefore = m_counts.copy;
      // A prompt opened over label focus temporarily owns text commands.
      std::optional<LaneHandle> volumeLane;
      const auto &rows = canvas->rows();
      for (size_t index = 0; index < rows.size(); ++index) {
          if (rows[index].id == volume)
              volumeLane = LaneHandle{int(index)};
      }

Original expression:
  QVERIFY(volumeLane.has_value())

A044 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:275
Disposition: MATCHED
Mapping/reason: Same production entry point and arguments: the insertion prompt opens on the active volume lane at tick 5808 with value 48, and the page publishes "48" as the draft the QML field displays.
Swift counterpart expression (S012, S013):
  report.expect(page.openPrompt(tick: 5808, value: 48), cppID: labelID, ...)
  report.expectEqual("48", page.promptDraft, cppID: labelID, ...)
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(canvas->openValuePromptForInsertion(*volumeLane, tick + 48, 48))

A045 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:276
Disposition: PARTIAL
Mapping/reason: Related predicate S002 in src/checks/selectionkey/localinputtier_text.swift; unproved: QML prompt visibility plus active-focus routing versus the Swift presenter flag; focus itself has no Swift predicate.
Source context since previous assertion:
  ;

Original expression:
  QTRY_VERIFY(canvas->valuePromptVisible() && quickWindow()->activeFocusItem() != label.data() &&
                  automation_valueprompt::focusedTextInput(*quickWindow()))

A046 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:281
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      QPointer<QQuickItem> prompt = automation_valueprompt::focusedTextInput(*quickWindow());
      const int promptCopiesBefore = m_counts.copy;
      selectionkey::deliverKey(quickWindow(), selectAll->key(), selectAll->keyboardModifiers());

Original expression:
  QVERIFY(prompt)

A047 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:283
Disposition: PARTIAL
Mapping/reason: Related predicate S003 in src/checks/selectionkey/localinputtier_text.swift (the production prompt retains the supplied numeric draft "12"); unproved: QML selectedText "48" versus page.promptDraft, and native delivery.
Source context since previous assertion:
  ;
      const QString selectedText = prompt->property("selectedText").toString();

Original expression:
  QCOMPARE(selectedText, QStringLiteral("48"))

A048 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:285
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), copy->key(), copy->keyboardModifiers());

Original expression:
  QCOMPARE(QGuiApplication::clipboard()->text(), selectedText)

A049 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:286
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(m_counts.copy, promptCopiesBefore)

A050 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:288
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), remove->key(), remove->keyboardModifiers());

Original expression:
  QVERIFY(prompt && prompt->property("text").toString().isEmpty())

A051 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:290
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), paste->key(), paste->keyboardModifiers());

Original expression:
  QVERIFY(prompt && prompt->property("text").toString() == selectedText)

A052 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:291
Disposition: PARTIAL
Mapping/reason: Related predicates S004, S007 in src/checks/selectionkey/localinputtier_text.swift (draft editing and cancellation preserve the snapshot); unproved: QML canvas prompt under native key delivery versus updatePromptDraft/cancelPrompt on the Swift page.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.smf().write(), before)

A053 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:292
Disposition: PARTIAL
Mapping/reason: Related predicate S005 in src/checks/selectionkey/localinputtier_text.swift (draft editing preserves note selection); unproved: QML canvas prompt and native delivery versus the Swift page transaction.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A054 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:294
Disposition: PARTIAL
Mapping/reason: Related predicate S006 in src/checks/selectionkey/localinputtier_text.swift (cancellation closes the prompt); unproved: Escape-key delivery closing the QML prompt versus page.cancelPrompt.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Escape);

Original expression:
  QTRY_VERIFY(!canvas->valuePromptVisible())

A055 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:295
Disposition: PARTIAL
Mapping/reason: Related predicates S004, S007 in src/checks/selectionkey/localinputtier_text.swift (draft editing and cancellation preserve the snapshot); unproved: QML canvas prompt under native key delivery versus updatePromptDraft/cancelPrompt on the Swift page.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.smf().write(), before)

A056 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:296
Disposition: PARTIAL
Mapping/reason: Related predicate S005 in src/checks/selectionkey/localinputtier_text.swift (draft editing preserves note selection); unproved: QML canvas prompt and native delivery versus the Swift page transaction.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A057 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:297
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(focusParameter(7))

A058 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:299
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), copy->key(), copy->keyboardModifiers());

Original expression:
  QCOMPARE(m_counts.copy, copiesBefore + 1)

A059 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:300
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(songview::clipboardHasClipMime())

A060 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:301
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.smf().write(), before)

A061 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:302
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selectionUnchanged())

A062 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:304
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), remove->key(), remove->keyboardModifiers());

Original expression:
  QVERIFY(!document.findLanePoint(kTrack, kController, tick, nullptr))

A063 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:305
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!document.findLanePoint(kTrack, kSecondController, tick, nullptr))

A064 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:306
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(document.findLanePoint(kTrack, 7, tick, nullptr))

A065 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:307
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(notePairUnchanged(document, *pair))

A066 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:308
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(label && label->hasActiveFocus())

A067 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:311
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

      selectionkey::deliverKey(quickWindow(), selectAll->key(), selectAll->keyboardModifiers());

Original expression:
  QVERIFY(view.selectionModel().isNoteSelected(pair->ids[0]))

A068 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:312
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(view.selectionModel().isNoteSelected(pair->ids[1]))

A069 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:313
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!view.selectionModel().timeSelection().active())

A070 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:318
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      view.selectionModel().clearBothSelections();
      view.commitEditCursor(pasteTick);
      selectionkey::deliverKey(quickWindow(), paste->key(), paste->keyboardModifiers());
      DocLanePoint point;

Original expression:
  QVERIFY(document.findLanePoint(kTrack, kController, pasteTick, &point))

A071 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:319
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 32)

A072 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:320
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(document.findLanePoint(kTrack, kSecondController, pasteTick, &point))

A073 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:321
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(point.value, 96)

A074 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:322
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!document.findLanePoint(kTrack, 7, pasteTick, nullptr))

A075 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:323
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(notePairUnchanged(document, *pair))

A076 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:324
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(canvas->parameterRow(canvas->activeParameter()), std::optional{volume})

A077 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:327
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      view.selectionModel().setNoteSelection({pair->ids[0], pair->ids[1]});

Original expression:
  QVERIFY(label && label->hasActiveFocus())

A078 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:334
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const uint64_t gridStep =
          view.grid().snapTickUp(double(pair->notes[0].tick) + 1.0) - pair->notes[0].tick;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Right);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Up);
      for (size_t index = 0; index < pair->ids.size(); ++index) {
          const auto moved = selectionkey::noteById(document, pair->ids[index]);

Original expression:
  QVERIFY(moved.has_value())

A079 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:335
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(moved->tick, pair->notes[index].tick + gridStep)

A080 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:336
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(moved->key, uint8_t(pair->notes[index].key + 1))

A081 | SelectionWindowTierTest::parameterLabelActivationAndSharedCommands | src/checks/selectionkey/windowtier_keyboard.cpp:338
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      }

Original expression:
  QCOMPARE(canvas->parameterRow(canvas->activeParameter()), std::optional{volume})

A082 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:355
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
  }

  // The tempo tab's tap-tempo Tap button keeps the same activation-key contract
  // as the parameter labels above: plain Enter and Return each add exactly one
  // tap through real window delivery, tempo-row focus and selection stay
  // untouched, modified and auto-repeat Return remain shared editing input, and
  // bare Space stays with the window transport shortcut — no tap, no edit.
  void SelectionWindowTierTest::tapButtonKeysAndTransportCession()
  {
      SongView &view = this->view();
      SongDocument &document = this->document();
      const selectionkey::ScenarioRollback rollback(view, document);
      view.selectTrack(kTrack);
      view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      view.setDrawerSectionHeight(EditorDrawerPage::Automations, 200);
      const std::optional<NotePair> pair = addNotePair(document, kTrack, 2400);

Original expression:
  QVERIFY2(pair.has_value(),
               "the reserved tick-2400 note pair could not be inserted and resolved")

A083 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:357
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(focusAutomationBand(view), "could not focus the automation band")

A084 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:361
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
  ;
      view.selectionModel().setNoteSelection({pair->ids[0], pair->ids[1]});
      auto *const canvas = view.editorDrawer()->automationPage()->canvas();
      auto *const quick = selectionkey::quickCanvas(view);

Original expression:
  QVERIFY(canvas && quick)

A085 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:376
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      QPointer<QQuickItem> button;
      const auto focusTap = [&] {
          if (!QTest::qWaitFor([&] {
                  button = checks::support::visualDescendant(
                      quick->rootObject(), QStringLiteral("automationTempoTapButton"));
                  return button && button->window() == quickWindow() && button->isVisible() &&
                         button->isEnabled() && button->width() > 0 && button->height() > 0;
              }))
              return false;
          button->forceActiveFocus(Qt::OtherFocusReason);
          selectionkey::settle();
          return button && button->hasActiveFocus();
      };

Original expression:
  QVERIFY2(focusTap(), "the tempo Tap button never became focusable")

A086 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:377
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(canvas->parametersEnabled(),
               "the tap-tempo canvas session would be disabled for this staging")

A087 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:396
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      const QByteArray before = document.smf().write();
      const int undoIndexBefore = document.undoStack()->index();
      const int undoCountBefore = document.undoStack()->count();
      const uint64_t revisionBefore = document.revision();
      const int activeBefore = canvas->activeParameter();
      const auto timeSelectionBefore = view.selectionModel().timeSelection();
      const auto noteSelectionBefore = view.selectionModel().noteSelection();
      const auto selectionUnchanged = [&] {
          const auto &actual = view.selectionModel().timeSelection();
          return actual.tempo == timeSelectionBefore.tempo &&
                 actual.startTick == timeSelectionBefore.startTick &&
                 actual.endTick == timeSelectionBefore.endTick &&
                 actual.scope == timeSelectionBefore.scope &&
                 actual.lanes == timeSelectionBefore.lanes &&
                 view.selectionModel().noteSelection() == noteSelectionBefore;
      };

Original expression:
  QVERIFY(focusTap())

A088 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:398
Disposition: MATCHED
Mapping/reason: Enter on the focused production Tap button registers exactly one tap and keeps focus: tryCompare tapTempoTapCount 1 (:1028-1029) with tapButton.activeFocus still true (S050, :1030, the first tap keeps button focus). The control is the real automationTempoTapButton on the tempo tab, focused by forceActiveFocus (:1024-1026).
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Enter);

Original expression:
  QVERIFY2(canvas->tapTempoTapCount() == 1 && button && button->hasActiveFocus(),
               "Enter on the focused Tap button did not register exactly one tap")

A089 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:401
Disposition: MATCHED
Mapping/reason: Return on the focused Tap button adds the second tap: tryCompare tapTempoTapCount 2 (S051, :1032-1033, Return on the focused Tap button adds the second tap).
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Return);

Original expression:
  QVERIFY2(canvas->tapTempoTapCount() == 2,
               "Return on the focused Tap button did not add the second tap")

A090 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:403
Disposition: MATCHED
Mapping/reason: Tap keys never edit the song: documentDirty stays false after the Return tap (S052, :1037, tap keys never edit the song).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.smf().write() == before, "Return changed the document")

A091 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:404
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.revision() == revisionBefore, "Return changed the revision")

A092 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:405
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.undoStack()->index() == undoIndexBefore, "Return changed the undo index")

A093 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:406
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.undoStack()->count() == undoCountBefore, "Return changed the undo count")

A094 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:407
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(canvas->activeParameter() == activeBefore, "Return changed the active parameter")

A095 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:408
Disposition: MATCHED
Mapping/reason: Tap keys never move the selection: grid note summary equals notesBefore after the Return tap (S053, :1038, tap keys never move the selection).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(selectionUnchanged(), "Return changed the selection")

A096 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:412
Disposition: MATCHED
Mapping/reason: The two-tap session shows the draft readout with a tempo: draft visible (S054, :1035, the two-tap session shows the draft readout) with text naming BPM (:1036).
Source context since previous assertion:
  ;
      // The draft readout reflects the two-tap accumulator.
      const QPointer<QQuickItem> draft = checks::support::visualDescendant(
          quick->rootObject(), QStringLiteral("automationTempoTapDraft"));

Original expression:
  QVERIFY2(draft && draft->isVisible(),
               "the two-tap session did not make the draft tempo readout visible")

A097 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:418
Disposition: MATCHED
Mapping/reason: Modified Return is shared editing input, not button activation: keyClick Return with ShiftModifier leaves tapTempoTapCount at 2 (S055, :1039-1040, Shift+Return never registers a tap).
Source context since previous assertion:
  ;

      // Modified Return is shared editing input, not button activation: the
      // session stays where it was.
      selectionkey::deliverKey(quickWindow(), Qt::Key_Return, Qt::ShiftModifier);

Original expression:
  QVERIFY2(canvas->tapTempoTapCount() == 2,
               "Shift+Return on the focused Tap button registered a tap")

A098 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:425
Disposition: MATCHED
Mapping/reason: Bare Space on the focused persistent chrome requests transport without a local change: the drawer-toggle probe observes the transport presenter playing state toggle once with the section visibility and documentDirty unchanged (S032-S034), and the production transport.play_pause sequence is Space (S022). The original tap-button chrome has no focusable Swift-shell counterpart, so the persistent drawer toggle (Rectangle with activeFocusOnTab and Return/Enter-only local keys) stands in for that retired chrome, and the trigger count is observed as a presenter playing-state transition rather than a native signal count. Extended by S056 (:1042-1043): bare Space on the focused Tap button drives transport playing true — the tap button claims only Enter/Return locally, so unclaimed Space cedes to the window transport shortcut, matching the original play/pause request.
Source context since previous assertion:
  ;

      // Bare Space is transport input: the window-level play/pause shortcut
      // outranks incidental focus in this persistent control.
      QSignalSpy playPause(m_workspace, &WorkspaceUi::playPauseRequested);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Space);

Original expression:
  QVERIFY2(playPause.count() == 1, "bare Space did not request exactly one play/pause")

A099 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:426
Disposition: MATCHED
Mapping/reason: Transport Space never taps: tapTempoTapCount stays 2 across the Space press (S057, :1044, transport Space never taps).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(canvas->tapTempoTapCount() == 2, "bare Space changed the tap count")

A100 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:427
Disposition: MATCHED
Mapping/reason: Tap Space never edits the song: documentDirty stays false (S058, :1045, tap Space never edits the song).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.smf().write() == before, "bare Space changed the document")

A101 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:428
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.revision() == revisionBefore, "bare Space changed the revision")

A102 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:429
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(document.undoStack()->count() == undoCountBefore, "bare Space changed the undo count")

A103 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:430
Disposition: PARTIAL
Mapping/reason: Related predicate S040 in src/checks/editorqml/tst_ShellWindow.qml (test_hKeyboardFocusAndDrawerSpacePriority): bare Space on the focused drawer chrome leaves the full grid note summary, selection flags included, unchanged (S040, "chrome Space never changes musical selection"). Unproved as stated: the two-tap tempo session and draft-readout preconditions, the time-selection component of selectionUnchanged() (the shell lane stages no time selection), and byte-level document identity. Extended by the tap-Space note no-change predicate (S059-partial, :1046, tap Space never moves the selection): the grid note summary is unchanged after transport Space on the Tap button; the time-selection half stays unproved.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(selectionUnchanged(), "bare Space changed the selection")

A104 | SelectionWindowTierTest::tapButtonKeysAndTransportCession | src/checks/selectionkey/windowtier_keyboard.cpp:433
Disposition: MATCHED
Mapping/reason: resetTapTempo drops the draft: model.resetTapTempo() returns tapTempoTapCount to 0 (S060, :1050-1051, resetTapTempo drops the draft).
Source context since previous assertion:
  ;

      canvas->resetTapTempo();

Original expression:
  QVERIFY2(canvas->tapTempoTapCount() == 0, "resetTapTempo did not drop the draft")

A105 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:447
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      view.selectionModel().clearNoteSelection();
  }

  void SelectionWindowTierTest::chromeToggleRoutesNoteArrows()
  {
      SongView &view = this->view();
      SongDocument &document = this->document();
      const selectionkey::ScenarioRollback rollback(view, document);
      const FullControlTabTraversal fullControlTabTraversal;
      view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      view.setDrawerSectionHeight(EditorDrawerPage::Automations, 200);
      view.selectTrack(kTrack);
      const std::optional<NotePair> pair = addNotePair(document, kTrack, 2400);

Original expression:
  QVERIFY2(pair.has_value(),
               "the reserved tick-2400 note pair could not be inserted and resolved")

A106 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:449
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(focusAutomationBand(view), "could not focus the automation band")

A107 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:457
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      view.selectionModel().setNoteSelection({pair->ids[0], pair->ids[1]});

      // The drawer toggle: bare Space stays with the window transport shortcut,
      // Enter/Return activate locally, and the note arrows route to the
      // selected notes with the exact production effect.
      const QPointer<QQuickItem> toggle = tabTo(
          quickWindow(), {QStringLiteral("drawerAutomationToggle")}, chromeTraversalBound(document));

Original expression:
  QVERIFY2(toggle, "Tab traversal never reached the automation drawer toggle")

A108 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:458
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(toggle->hasActiveFocus(),
               "the automation drawer toggle does not show its advertised active focus")

A109 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:461
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const std::optional<DocNote> beforeRight = selectionkey::noteById(document, pair->ids[0]);

Original expression:
  QVERIFY2(beforeRight.has_value(), "the selected note vanished before Right")

A110 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:468
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Right);
      const std::optional<DocNote> afterRight = selectionkey::noteById(document, pair->ids[0]);
      // Production nudges the selection anchor to the next snap tick; both
      // selected notes advance by that same grid step, keys untouched.
      const uint64_t gridStep =
          view.grid().snapTickUp(double(beforeRight->tick) + 1.0) - beforeRight->tick;

Original expression:
  QVERIFY2(afterRight.has_value() && afterRight->tick == beforeRight->tick + gridStep &&
                   afterRight->key == beforeRight->key,
               "the routed Right arrow did not advance exactly the selected notes by one grid step")

A111 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:473
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Up);
      const std::optional<DocNote> afterUp = selectionkey::noteById(document, pair->ids[0]);

Original expression:
  QVERIFY2(afterUp.has_value() && afterUp->key == uint8_t(beforeRight->key + 1) &&
                   afterUp->tick == afterRight->tick,
               "the routed Up arrow did not transpose exactly the selected notes")

A112 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:484
Disposition: MATCHED
Mapping/reason: The drawer-toggle probe exercises the same rule on the shell persistent chrome: drawerToggle_velocity (Rectangle with activeFocusOnTab and Return/Enter-only local keys) owns active focus (S031) and one bare Space toggles playhead.playing with the focused section visibility and documentDirty unchanged (S032-S034, "bare Space on focused drawer chrome owns transport"), then a second bare Space stops transport (S035, "the second chrome Space stops transport"). Space stays with the window Shortcut; the focused chrome never activates.
Source context since previous assertion:
  ;
      // Bare Space on the focused toggle is transport input, not toggle
      // activation: the window-level play/pause shortcut outranks incidental
      // focus in this persistent control, and the drawer and selected notes
      // stay untouched.
      const std::optional<DocNote> beforeActivation = selectionkey::noteById(document, pair->ids[0]);
      const bool visibleBefore = view.drawerSectionVisible(EditorDrawerPage::Automations);
      QSignalSpy playPause(m_workspace, &WorkspaceUi::playPauseRequested);
      selectionkey::deliverKey(quickWindow(), Qt::Key_Space);

Original expression:
  QVERIFY2(playPause.count() == 1 &&
                   view.drawerSectionVisible(EditorDrawerPage::Automations) == visibleBefore,
               "bare Space on the focused drawer toggle did not route exactly one transport "
               "play/pause request while leaving the drawer section hidden or shown")

A113 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:489
Disposition: PARTIAL
Mapping/reason: Related predicate S040 in src/checks/editorqml/tst_ShellWindow.qml (test_hKeyboardFocusAndDrawerSpacePriority): with drawerToggle_velocity owning active focus, bare Space toggles transport while the focused section visibility and the full grid note summary stay unchanged (S040, "chrome Space never changes musical selection"). Unproved as stated: the reserved tick-2400 pair resolved by id with per-note tick/key identity (the shell asserts whole-summary string equality over one drawn velocity note) and the drawerAutomationToggle chrome.
Source context since previous assertion:
  ;
      const std::optional<DocNote> afterSpace = selectionkey::noteById(document, pair->ids[0]);

Original expression:
  QVERIFY2(afterSpace.has_value() && beforeActivation.has_value() &&
                   afterSpace->tick == beforeActivation->tick &&
                   afterSpace->key == beforeActivation->key,
               "bare Space on the focused toggle moved or edited the selected note")

A114 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:496
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      // Enter and Return are the advertised local activation keys: each one
      // toggles exactly once and the second press restores the drawer.
      selectionkey::deliverKey(quickWindow(), Qt::Key_Enter);

Original expression:
  QVERIFY2(view.drawerSectionVisible(EditorDrawerPage::Automations) != visibleBefore,
               "Enter did not activate the focused drawer toggle")

A115 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:499
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/selectionkey/windowtier_keyboard.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(quickWindow(), Qt::Key_Return);

Original expression:
  QVERIFY2(view.drawerSectionVisible(EditorDrawerPage::Automations) == visibleBefore,
               "Return did not restore the drawer toggle")

A116 | SelectionWindowTierTest::chromeToggleRoutesNoteArrows | src/checks/selectionkey/windowtier_keyboard.cpp:502
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const std::optional<DocNote> afterActivation = selectionkey::noteById(document, pair->ids[0]);

Original expression:
  QVERIFY2(afterActivation.has_value() && beforeActivation.has_value() &&
                   afterActivation->tick == beforeActivation->tick &&
                   afterActivation->key == beforeActivation->key,
               "toggle activation keys moved or edited the selected notes")

Remaining original source (closing sequence and helper bodies):
  ;

      view.selectionModel().clearNoteSelection();
  }


Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original selected fixture note exists"
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original empty-tick numeric prompt opens"
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the production prompt retains the supplied numeric draft"
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing does not mutate the song"
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft editing preserves note selection"
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation closes the prompt"
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "cancellation leaves the original document unchanged"
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "draft and cancellation preserve the full song contents"
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")

S009 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original CC10 lane is present in the page's row stack"
  report.expect(page.rows.contains { $0.parameter == fixture.panLane }, cppID: id,
                    message: "the original CC10 lane is present in the page's row stack")

S010 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original fixture note resolves by its inserted ID"
  report.expect(insertedNotes.first.flatMap { fixture.document.note($0) } != nil, cppID: id,
                    message: "the original fixture note resolves by its inserted ID")

S011 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original volume lane is present in the page's row stack"
  report.expect(page.rows.contains { $0.parameter == fixture.volumeLane }, cppID: labelID,
                    message: "the original volume lane is present in the page's row stack")

S012 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original volume-lane insertion prompt opens"
  report.expect(page.openPrompt(tick: 5808, value: 48), cppID: labelID,
                    message: "the original volume-lane insertion prompt opens")

S013 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the volume prompt opens with the plotted value as its draft"
  report.expectEqual("48", page.promptDraft, cppID: labelID,
                         what: "the volume prompt opens with the plotted value as its draft")

S014 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the original pan-lane insertion prompt opens"
  report.expect(page.openPrompt(tick: 5760, value: 64), cppID: lifetimeID,
                    message: "the original pan-lane insertion prompt opens")

S015 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the document-switch cancellation closes the prompt"
  report.expect(!page.promptOpen, cppID: lifetimeID,
                    message: "the document-switch cancellation closes the prompt")

S016 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "accepting a closed prompt commits nothing"
  report.expect(!page.acceptPrompt(displayedValue: 96), cppID: lifetimeID,
                    message: "accepting a closed prompt commits nothing")

S017 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift
Anchor: message "the closed prompt's late acceptance leaves the document unchanged"
  report.expectEqual(before, fixture.snapshot, cppID: lifetimeID,
                         what: "the closed prompt's late acceptance leaves the document unchanged")

S018 | openTwoSongShell | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "mus_route101"
  session.openProjectAndSong(bootstrap.projectRoot, "mus_route101")
  session.openSong("mus_littleroot_test")
  // The shellwindow entry of ShellQmlLane (src/checks/editorqml/ShellQmlTests.swift:46)
  // stages the same two-song fixture; the velocity drawer seed rows above it
  // reproduce MainWindowRoutingFixture::openSession.

S019 | selectDrawnVelocityNote | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "selectedCount"
  tryCompare(shell.shellPresenter.session.velocityPage(), "selectedCount", 1, 3000)
  // After a real mouseClick on the drawn velocityNodeFill hit target.

S020 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "native Copy is registered as a window shortcut"
  verify(shell.shellPresenter.actionSequences("roll.copy").length > 0,
               "native Copy is registered as a window shortcut")

S021 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "S"
  compare(shell.shellPresenter.actionSequences("roll.solo_tracks")[0], "S")

S022 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Space"
  compare(shell.shellPresenter.actionSequences("transport.play_pause")[0], "Space")

S023 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the real node click selected one note"
  compare(copied.length, 1, "the real node click selected one note")

S024 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Copy starts from a clean song"
  compare(session.documentDirty, false, "Copy starts from a clean song")

S025 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the Edit menu returned keyboard focus to the roll"
  roll.forceActiveFocus(Qt.OtherFocusReason)
  tryCompare(roll, "activeFocus", true, 3000,
             "the Edit menu returned keyboard focus to the roll")

S026 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "shellShortcut_roll.copy"
  keySequence(StandardKey.Copy)
  compare(copyActivatedSpy.count, 1, "native Copy activates the window shortcut once")
  // copyActivatedSpy targets the production window Shortcut objectName
  // "shellShortcut_roll.copy" selected by windowShortcut().

S027 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the copied note retains its original key"
  var clip = JSON.parse(bootstrap.copiedClipSummary())
  compare(clip.length, 3, "the native clipboard carries a decodable song clip")
  compare(clip[0], 1, "the copied clip contains one track")
  compare(clip[1], 1, "the copied track contains one selected note")
  compare(clip[2], copied[0].pitch, "the copied note retains its original key")

S028 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the native window Copy never edits the song"
  compare(session.documentDirty, false, "the native window Copy never edits the song")

S029 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "shellShortcut_roll.solo_tracks"
  keyClick(Qt.Key_S)
  tryCompare(firstTrack, "soloChecked", true, 3000)
  compare(soloActivatedSpy.count, 1, "first non-text S activates Solo once")
  // firstTrack is headers.itemAt(surface.gridModel.trackIndex); the key goes to
  // the focused non-text window child shortcutTarget, and soloActivatedSpy
  // targets the production "shellShortcut_roll.solo_tracks" Shortcut.

S030 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "second non-text S activates Solo twice total"
  keyClick(Qt.Key_S)
  tryCompare(firstTrack, "soloChecked", false, 3000)
  compare(soloActivatedSpy.count, 2, "second non-text S activates Solo twice total")

S031 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "drawerToggle_velocity"
  var velocityToggle = findChild(drawer, "drawerToggle_velocity")
  verify(velocityToggle && velocityToggle.visible, "the velocity toggle is drawn")
  velocityToggle.forceActiveFocus(Qt.OtherFocusReason)
  tryCompare(velocityToggle, "activeFocus", true, 3000,
             "drawer chrome takes keyboard focus")
  // The focused control is the production EditorDrawer toggle Rectangle
  // (src/ui/songview/quick/drawer/EditorDrawer.qml:316) with activeFocusOnTab
  // and Return/Enter-only local keys; bare Space is not consumed there.

S032 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "bare Space on focused drawer chrome owns transport"
  keyClick(Qt.Key_Space)
  tryCompare(playhead, "playing", true, 3000,
             "bare Space on focused drawer chrome owns transport")

S033 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "chrome Space never toggles the focused section"
  compare(surface.drawerPresenter.section(
              bootstrap.velocitySectionKind()).visible, toggleSectionVisible,
          "chrome Space never toggles the focused section")

S034 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "chrome Space never edits the song"
  compare(session.documentDirty, false, "chrome Space never edits the song")

S035 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the second chrome Space stops transport"
  keyClick(Qt.Key_Space)
  tryCompare(playhead, "playing", false, 3000,
             "the second chrome Space stops transport")

S036 | copiedClipSummary | src/checks/editorqml/ShellQmlTests.swift
Anchor: function
  return "[\(tracks.count),\(notes.count),\(notes.first.map { Int($0.key) } ?? -1)]"
  // Reads the native clip through production pd_clipboard_read and
  // ClipboardCodec.decode (ShellQmlTests.swift:176-180); "[]" means absent.

S037 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: function
  var beforeGrip = grid.noteSummary
  var heightBefore = surface.drawerPresenter.section(
              bootstrap.velocitySectionKind()).bodyHeight
  // Snapshot precedes every grip arrow; S039 compares against it.
S038 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "grip Down restores its height"
  keyClick(Qt.Key_Up)
  verify(surface.drawerPresenter.section(bootstrap.velocitySectionKind()).bodyHeight
         > heightBefore, "grip Up resizes the drawer locally")
  keyClick(Qt.Key_Down)
  compare(surface.drawerPresenter.section(bootstrap.velocitySectionKind()).bodyHeight,
          heightBefore, "grip Down restores its height")
S039 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "grip arrows never mutate selected notes"
  keyClick(Qt.Key_Left)
  keyClick(Qt.Key_Right)
  compare(grid.noteSummary, beforeGrip, "grip arrows never mutate selected notes")
S040 | test_hKeyboardFocusAndDrawerSpacePriority | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "chrome Space never changes musical selection"
  keyClick(Qt.Key_Space)
  tryCompare(playhead, "playing", true, 3000)
  compare(section.visible, sectionVisible, "chrome Space does not toggle the section")
  compare(surface.gridModel.noteSummary, noteSnapshot,
          "chrome Space never changes musical selection")
  // drawerToggle_velocity owns active focus (:691-692); the second Space stops
  // transport (:700-701).
S041 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the focused label is not yet active"
  verify(!firstLabel.checked, "the focused label is not yet active")
  // firstLabel holds keyboard focus (:977-979) without being active: the
  // pre-Space precondition that it is not the active parameter.
S042 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "transport Space never moves the selection"
  keyClick(Qt.Key_Space) (:987)
  tryCompare(playhead, "playing", true, 3000) (:988-989, bare Space owns transport)
  compare(firstLabel.checked, false, "transport Space never activates the label") (:990)
  compare(activeAfterSpace, activeBeforeSpace, "transport Space never retargets activation") (:997)
  compare(session.documentDirty, false, "transport Space never edits the song") (:998)
  compare(grid.noteSummary, notesBefore, "transport Space never moves the selection") (:999)
  // the second Space stops transport (:1000-1002).
S043 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Enter activates the focused label once"
  keyClick(Qt.Key_Enter) (:1003)
  tryCompare(firstLabel, "checked", true, 3000, "Enter activates the focused label once") (:1004-1005)
S044 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "label Enter never moves the selection"
  compare(grid.noteSummary, notesBefore, "label Enter never moves the selection") (:1007)
  compare(grid.editCursorTick, cursorBefore, "label Enter never moves the cursor") (:1008)
  compare(grid.trackIndex, trackBefore, "label Enter never retargets the track") (:1009)
  // Partial: the time-selection half is unproved (the shell lane stages no
  // time selection); the Return-side note no-change at :1018 rides unsplit.
S045 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Return activates the focused second label"
  tryCompare(secondLabel, "checked", true, 3000,
             "Return activates the focused second label")
  compare(firstLabel.checked, false, "Return retargets activation exactly once")
S046 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "label Enter never edits the song"
  compare(session.documentDirty, false, "label Enter never edits the song") (:1006)
  compare(session.documentDirty, false, "label Return never edits the song") (:1017)
S047 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "label Enter never moves the cursor"
  compare(grid.editCursorTick, cursorBefore, "label Enter never moves the cursor") (:1008)
  compare(grid.editCursorTick, cursorBefore, "label Return never moves the cursor") (:1019)
S048 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "label Enter never retargets the track"
  compare(grid.trackIndex, trackBefore, "label Enter never retargets the track") (:1009)
S049 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Up never retargets the active label"
  compare(secondLabel.checked, true, "Up never retargets the active label")
  compare(session.documentDirty, false, "Up on a label never edits the song")
  compare(grid.noteSummary, notesBefore, "Up on a label never moves the selection")
S050 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Enter on the focused Tap button registers exactly one tap"
  tryCompare(model, "tapTempoTapCount", 1, 3000,
             "Enter on the focused Tap button registers exactly one tap")
  compare(tapButton.activeFocus, true, "the first tap keeps button focus")
S051 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Return on the focused Tap button adds the second tap"
  keyClick(Qt.Key_Return) (:1031)
  tryCompare(model, "tapTempoTapCount", 2, 3000, "Return on the focused Tap button adds the second tap") (:1032-1033)
S052 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "tap keys never edit the song"
  compare(session.documentDirty, false, "tap keys never edit the song") (:1037)
S053 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "tap keys never move the selection"
  compare(grid.noteSummary, notesBefore, "tap keys never move the selection") (:1038)
S054 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "the two-tap session shows the draft readout"
  verify(draft && draft.visible, "the two-tap session shows the draft readout") (:1035)
  verify(draft.text.indexOf("BPM") >= 0, "the two-tap draft names a tempo") (:1036)
S055 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "Shift+Return never registers a tap"
  keyClick(Qt.Key_Return, Qt.ShiftModifier)
  compare(model.tapTempoTapCount, 2, "Shift+Return never registers a tap")
S056 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: function
  keyClick(Qt.Key_Space) (:1041)
  tryCompare(playhead, "playing", true, 3000, "bare Space on the Tap button owns transport") (:1042-1043)
S057 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "transport Space never taps"
  compare(model.tapTempoTapCount, 2, "transport Space never taps") (:1044)
S058 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "tap Space never edits the song"
  compare(session.documentDirty, false, "tap Space never edits the song") (:1045)
S059 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "tap Space never moves the selection"
  compare(grid.noteSummary, notesBefore, "tap Space never moves the selection") (:1046)
  // Partial: the time-selection half is unproved (the shell lane stages no
  // time selection).
S060 | test_kParameterTabActivationAndTapCession | src/checks/editorqml/tst_ShellWindow.qml
Anchor: message "resetTapTempo drops the draft"
      model.resetTapTempo()
      compare(model.tapTempoTapCount, 0, "resetTapTempo drops the draft")
  }

Supplemental converted-window evidence

Executable: src/checks/swiftrollgated/drawerparity.cpp
Command: deno task verify --filter swiftrollgated --verbose --qt drawerGripKeyboardIsolation

drawerGripKeyboardIsolation adapts chromeGripKeysStayLocal (A013-A020) to the
real Swift ApplicationSession and production SwiftRollOverlay.qml, mounted in
a standalone QQuickView. From the focused automation plot, real forward Tab
traversal must reach the visible automation grip. Up/Down resize by the exact
font-relative step; all four arrow presses are accepted locally; cross-axis
arrows do not resize. The document revision and selected-note summary remain
unchanged.

The staged song and selected note differ from the original reserved note pair.
This standalone check does not replace the original QWidget-window Copy/Solo
action-counter assertions; the shellwindow lane predicates S018 through S060
cover those window-tier sites, and this check does not establish them. The
grip-arrow dispositions recorded above are unchanged by that crosswalk.

Investigation also found that Tab can leave the temporary RewriteWindow's last
Quick control and fail to re-enter its QWidget container. No container workaround
was retained: the intended final application has no QWidget host. The retained
check covers complete forward traversal on that standalone Quick surface.
