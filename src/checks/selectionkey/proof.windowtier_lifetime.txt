Assertion correspondence: selectionkey/windowtier_lifetime.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/selectionkey/windowtier_lifetime.cpp
Original SHA-256: 47d589471dbc0075cb36225ef0f79df0eb818a96a61bfc963a79ab67f4e8d8d6
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Swift SHA-256: 0b1d5de7907758291583092f0d482a14eec5af37dd8d4f610689ea2e64d8c1eb
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Additional Swift SHA-256: a9c8ef79e003a9396492f2d27cadc6c88514a664ad5b45872ca79a1a593bb7b7
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files

Status and evidence
Tally: MATCHED 6, PARTIAL 5, GAP 22, NATIVE 22.
The C++ file above was deleted; this inventory audits its parent-revision content (pinned by Reference revision and SHA-256) so no assertion is dropped during the Swift check migration. This is an assertion-site inventory, not a count-based equivalence claim. Every original macro/failure site appears below, including fixture guards. Source context preserves setup, rows, loops, and guards between assertions verbatim. S identifiers identify exact Swift predicates in the paired counterparts, listed after the inventory. The pan-lane row lookup, the tick-5760 insertion-prompt open, the document-switch close, and the late-acceptance no-op sites are MATCHED by S009 and S014 through S017. The frozen shellwindow lane (src/checks/editorqml/tst_ShellWindow.qml :: test_dCopyFromOneSongTabPastesIntoAnother, S018-S021, with the real tab-retarget rows of test_eTimeAndTracksMenuContainment and test_cWindowShortcutsAndNumericOwnership, S022-S023) drives a real key-delivered Paste across the same two-song fixture (mus_route101 first, mus_littleroot_test second), so the tab-selection round trip A018 and the cross-tab document isolation A024 are MATCHED by executed predicates; A016, A017, A023 and A040 are PARTIAL, each naming its unproved condition. The remaining tab/workspace lifecycle, committed pan values, moves, undo, close, and reopen observations stay NATIVE or GAP. MATCHED identifies equivalent model predicates; execution must still be verified. PARTIAL identifies related Swift predicates with an explicit unproved condition. NATIVE identifies retained native-window/rendering or native setup obligations; it does not establish coverage of any converted window. GAP identifies an original predicate without a replacement assertion. Unsure between PARTIAL and GAP was resolved to GAP. Native passes do not prove the Swift replacement.

Verification
Command: deno task verify --filter swiftcore --verbose (controller-observed)
Result: PASS; build ok (0.15 s), ok swiftcore (10.38 s); verify 1/6 ok (5 skipped); run_checks PASS. That run executes the paired prompt-transaction predicates S002-S017 only.
Command: deno task verify:shell --filter shellwindow --verbose (controller-observed)
Result: PASS; build ok (0.16 s), ok shellwindow (3.74 s); verify 1/11 ok (10 skipped); run_checks PASS. That run executes the shellwindow predicates S018-S024 through the shellwindow entry of ShellQmlTests.swift (src/checks/editorqml/ShellQmlTests.swift:46).
The deleted C++ harness is not registered in this checkout; its cases are reference inventories only, never current execution evidence.

Execution ownership
drawerOriginalNumericPromptTransaction in src/checks/selectionkey/localinputtier_text.swift (called by the swiftcore session suite) executes the paired prompt-transaction predicates (S002 through S019); the file's tab-lifecycle, move, undo, close, and reopen cases have no executing Swift entry point. The shellwindow lane covers the tab-selection retarget and cross-tab isolation rows through ShellQmlLane (src/checks/editorqml/ShellQmlTests.swift:46), which stages mus_route101 and mus_littleroot_test and executes S018-S024. The drawerAutomationAutomationFixture struct carries no report predicates, so no Shared helper section applies. The transaction also carries a report.fail path for fixture-note creation failure, which short-circuits before any predicate; no deleted C++ site corresponds to it.

Fixture, sequence and row constraints
A two-tab sequence (prompt on tab A, work on tab B, return to A, track-1 pan prompt, close B, reopen) expands tab, window, parameter-row, and document-identity sites per lifecycle stage. The originalTrackPan comparison loop expands tick/value assertions per lane point.
Original setup, branch guards and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:61
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  // Selection keyboard routing, window tier: lifecycle scenario. A/B tab
  // switching, drawer hide/show, a primary-track change, and closing plus
  // reopening a song tab leave routing bound to the live view, with no stale
  // callback mutating a background document (plan 10). The song opens and closes
  // run production's genuine clean lifecycle — the scenario undoes every edit
  // through the real undo stack first, so no unsaved-changes prompt can mask a
  // regression, and each gate that proves the clean state stays a real
  // assertion.

  #include "checks/selectionkey/tst_windowtier.h"

  #include "checks/support/timelinequickcheck.h"
  #include "ui/editordrawer/automationpage.h"

  #include "ui/editordrawer/editordrawer.h"

  #include <QPointer>
  #include <QQuickWindow>

  namespace {

  constexpr int kTrack = 0;
  constexpr uint8_t kPan = 10;
  constexpr uint64_t kPanTick = 5760;

  AutomationCanvas *automationCanvas(SongView &view)
  {
      return view.editorDrawer()->automationPage()->canvas();
  }

  bool clickParameter(SongView &view, const EditorAutomationRowId &row)
  {
      auto *const canvas = automationCanvas(view);
      auto *const quick = selectionkey::quickCanvas(view);
      const int index = checks::support::automationParameterIndex(*canvas, row);
      if (index < 0 || !quick ||
          !checks::support::clickVisibleTab(quick->rootObject(),
                                            QStringLiteral("automationParameterTab%1").arg(index))) {
          return false;
      }
      return QTest::qWaitFor([&] { return canvas->parameterRow(canvas->activeParameter()) == row; });
  }

  std::optional<LaneHandle> panLane(const AutomationCanvas &canvas, int track)
  {
      const auto &rows = canvas.rows();
      for (size_t index = 0; index < rows.size(); ++index) {
          const auto &row = rows[index].id;
          if (row.kind == EditorAutomationRowKind::ControlChange && row.track == track &&
              row.controller == kPan)
              return LaneHandle{int(index)};
      }
      return std::nullopt;
  }

  } // namespace

  void SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime()
  {
      WorkspaceUi &workspace = *m_session.workspace;

Original expression:
  QVERIFY2(m_tab, "the first song tab is missing")

A002 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:63
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      SongTab *const tabA = workspace.songTabFor(*SongName::create(m_songA));

Original expression:
  QCOMPARE(tabA, m_tab.data())

A003 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:70
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      SongView &viewA = tabA->view();
      SongDocument &documentA = tabA->document();
      viewA.selectTrack(kTrack);
      viewA.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      songview::TimelineQuickView *const quickA = selectionkey::quickCanvas(viewA);
      QQuickWindow *const windowA = quickA ? quickA->quickWindow() : nullptr;

Original expression:
  QVERIFY2(windowA, "the first tab Quick window is missing")

A004 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:73
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      AutomationCanvas *const canvasA = automationCanvas(viewA);
      const EditorAutomationRowId panA{EditorAutomationRowKind::ControlChange, kTrack, kPan};

Original expression:
  QVERIFY(clickParameter(viewA, panA))

A005 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:81
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      // The per-case scenarios each roll their own edits back, and the explicit
      // undo-to-clean gate keeps this case self-sufficient, so the second-song
      // open runs production's genuine clean lifecycle (a dirty tab here is what
      // turns the open into an unsaved-changes prompt). The clean bytes then
      // double as the cross-tab mutation baseline.
      QString error;

Original expression:
  QVERIFY2(selectionkey::undoTabToClean(workspace, documentA,
                                            QStringLiteral("the first tab stayed dirty before the "
                                                           "second-song open"),
                                            &error),
               qUtf8Printable(error))

A006 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:89
Disposition: MATCHED
Mapping/reason: The original scans the canvas row list for the track-0 pan lane; the Swift page publishes the same catalog row stack and the predicate observes the lane's presence.
Swift counterpart expression (S009):
  report.expect(page.rows.contains { $0.parameter == fixture.panLane }, cppID: id, ...)
Source context since previous assertion:
  ;
      const int tabCountBeforeSecondOpen = workspace.openTabCount();
      const QByteArray documentAClean = documentA.smf().write();
      const auto laneA = panLane(*canvasA, kTrack);

Original expression:
  QVERIFY(laneA.has_value())

A007 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:90
Disposition: MATCHED
Mapping/reason: Same production entry point and arguments: the insertion prompt opens on the active pan lane at tick 5760 with value 64.
Swift counterpart expression (S014):
  report.expect(page.openPrompt(tick: 5760, value: 64), cppID: lifetimeID, ...)
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(canvasA->openValuePromptForInsertion(*laneA, kPanTick, 64))

A008 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:93
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      SongTab *const tabB = selectionkey::openSongTab(m_session, m_songB, true, error);

Original expression:
  QVERIFY2(tabB, qUtf8Printable(error))

A009 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:94
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(workspace.selectedSongTab() == tabB &&
                   workspace.openTabCount() == tabCountBeforeSecondOpen + 1 &&
                   workspace.songTabFor(tabA->name()) == tabA,
               "opening the second song did not add and select a distinct tab while retaining "
               "the first")

A010 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:104
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      SongView &viewB = tabB->view();
      SongDocument &documentB = tabB->document();
      viewB.selectTrack(kTrack);
      viewB.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      const std::optional<NotePair> pairB = addNotePair(documentB, kTrack, 960);

Original expression:
  QVERIFY2(pairB.has_value(),
               "the reserved tick-960 note pair could not be inserted on the second tab")

A011 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:108
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      songview::TimelineQuickView *const quickB = selectionkey::quickCanvas(viewB);
      QQuickWindow *const windowB = quickB ? quickB->quickWindow() : nullptr;

Original expression:
  QVERIFY2(windowB, "the second tab Quick window is missing")

A012 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:111
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      AutomationCanvas *const canvasB = automationCanvas(viewB);
      const EditorAutomationRowId tempo{EditorAutomationRowKind::Tempo, 0, 0};

Original expression:
  QVERIFY(clickParameter(viewB, tempo))

A013 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:112
Disposition: MATCHED
Mapping/reason: The original observes the prompt closed after the tab switch; the production document-switch route is cancelSectionInteraction, and the predicate observes the same closed prompt state.
Swift counterpart expression (S015):
  report.expect(!page.promptOpen, cppID: lifetimeID, ...)
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!canvasA->valuePromptVisible())

A014 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:114
Disposition: MATCHED
Mapping/reason: The original accepts the closed prompt and observes documentA unchanged; the Swift predicate accepts after the document-switch cancellation and observes the same no-write outcome on the same document.
Swift counterpart expression (S016, S017):
  report.expect(!page.acceptPrompt(displayedValue: 96), cppID: lifetimeID, ...)
  report.expectEqual(before, fixture.snapshot, cppID: lifetimeID, ...)
Source context since previous assertion:
  ;
      canvasA->acceptNodeValuePrompt(96);

Original expression:
  QCOMPARE(documentA.smf().write(), documentAClean)

A015 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:116
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      viewB.selectionModel().setNoteSelection({pairB->ids[0], pairB->ids[1]});

Original expression:
  QVERIFY2(focusAutomationBand(viewB), "could not focus the second tab's automation band")

A016 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:118
Disposition: PARTIAL
Mapping/reason: Related predicate S019 in src/checks/editorqml/tst_ShellWindow.qml: after a real tab click selects the destination workspace, the key-delivered Paste lands exactly one new note in that tab destination track and selects it (S019, "Paste inserts and selects the copied note in the destination track" with inserted.length == 1). Unproved as stated: the native arrow rows on the newly opened second tab and their transposed key/tick deltas (the shell rows deliver Paste, not arrows, and the shellwindow lane mounts one surface at a time).
Source context since previous assertion:
  ;
      selectionkey::deliverKey(windowB, Qt::Key_Right);

Original expression:
  QVERIFY2(!notePairUnchanged(documentB, *pairB),
               "arrows did not move the selected notes on the second tab")

A017 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:120
Disposition: PARTIAL
Mapping/reason: Related predicates S020 and S021 in src/checks/editorqml/tst_ShellWindow.qml: after the destination tab is edited, the source tab note summary is unchanged (S020, "cross-tab Paste leaves the source song unchanged") and only the pasted destination reports unsaved changes (S021). Unproved as stated: the reverse direction this site asserts — an edit delivered to the second tab leaving the first tab document untouched (the shellwindow lane edits the first tab and observes the second).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(documentA.smf().write() == documentAClean,
               "key delivery to the second tab mutated the first tab's document")

A018 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:126
Disposition: MATCHED
Mapping/reason: The shell selects tabs through real production tab controls and the presenter publishes the resulting selection: a real first-tab click followed by tryCompare(songTabs, "selectedId", firstId) (S022, S023, "a real first-tab click retargets the selected workspace"). The native selectSongTab/selectedSongTab round trip is replaced by that click-driven selected-id observation; no QWidget workspace object exists in the Swift app.
Source context since previous assertion:
  ;
      const QByteArray documentBAfterMove = documentB.smf().write();

      workspace.selectSongTab(tabA);
      selectionkey::settle();

Original expression:
  QCOMPARE(workspace.selectedSongTab(), tabA)

A019 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:127
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(canvasA->parameterRow(canvasA->activeParameter()), std::optional{panA})

A020 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:128
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(canvasB->parameterRow(canvasB->activeParameter()), std::optional{tempo})

A021 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:130
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const std::optional<NotePair> pairA = addNotePair(documentA, kTrack, 3840);

Original expression:
  QVERIFY2(pairA.has_value(),
               "the reserved tick-3840 note pair could not be inserted on the first tab")

A022 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:133
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      viewA.selectionModel().setNoteSelection({pairA->ids[0], pairA->ids[1]});

Original expression:
  QVERIFY2(focusAutomationBand(viewA),
               "could not focus the first tab's automation band after reselecting it")

A023 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:137
Disposition: PARTIAL
Mapping/reason: Related predicates S019, S023 and S024 in src/checks/editorqml/tst_ShellWindow.qml: after a real tab click retargets the selected workspace (S023), the delivered command lands in that tab and selects the inserted note (S019), and the roll Up key transposes the selected source note by one semitone (S024, test_gEditorRoutedNoteKeysAndChromeArrows). Unproved as stated: no single predicate asserts the composite — the transpose specifically after reselecting the first tab, with the resulting key observed on the note of that reselected tab.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(windowA, Qt::Key_Up);
      DocNote noteA;

Original expression:
  QVERIFY2(documentA.findNote(pairA->ids[0], &noteA) &&
                   noteA.key == uint8_t(pairA->notes[0].key + 1),
               "the Up arrow did not transpose the first tab's selection after reselecting it")

A024 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:140
Disposition: MATCHED
Mapping/reason: The frozen shellwindow lane reproduces the cross-tab isolation with a real key-delivered Paste over the same two-song fixture: the destination tab is edited (S019, exactly one inserted note) while the source tab note summary is unchanged (S020, "cross-tab Paste leaves the source song unchanged") and only the pasted destination reports unsaved changes (S021). The delivered command is Paste rather than a roll arrow and the observation is the grid note summary plus the session dirty flag instead of SMF bytes; no QWidget per-tab document snapshot exists.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(documentB.smf().write() == documentBAfterMove,
               "key delivery to the first tab mutated the second tab's document")

A025 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:147
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

      viewA.setDrawerSectionVisible(EditorDrawerPage::Automations, false);
      selectionkey::settle();
      viewA.selectionModel().setNoteSelection({pairA->ids[0], pairA->ids[1]});
      const std::optional<DocNote> beforeHide = selectionkey::noteById(documentA, pairA->ids[0]);

Original expression:
  QVERIFY2(beforeHide.has_value(),
               "the first selected note vanished before the hidden-drawer baseline")

A026 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:150
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(windowA, Qt::Key_Right);

Original expression:
  QVERIFY2(documentA.findNote(pairA->ids[0], &noteA) && noteA.tick != beforeHide->tick,
               "note arrows stopped routing while the drawer was hidden")

A027 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:155
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      viewA.setDrawerSectionVisible(EditorDrawerPage::Automations, true);

      // The primary-track change clears the selection and retargets routing.

Original expression:
  QVERIFY2(documentA.engineTrackCount() > 1,
               "the rich fixture needs a second track for primary-track routing")

A028 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:158
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const std::optional<NotePair> pairT1 = addNotePair(documentA, 1, 960);

Original expression:
  QVERIFY2(pairT1.has_value(), "the reserved track-1 tick-960 note pair could not be inserted")

A029 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:160
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      viewA.selectTrack(1);

Original expression:
  QVERIFY2(viewA.selectionModel().noteSelection().empty(),
               "the primary-track change did not clear the note selection")

A030 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:163
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const EditorAutomationRowId panTrackOne{EditorAutomationRowKind::ControlChange, 1, kPan};

Original expression:
  QCOMPARE(canvasA->parameterRow(canvasA->activeParameter()), std::optional{panTrackOne})

A031 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:165
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const auto laneTrackOne = panLane(*canvasA, 1);

Original expression:
  QVERIFY(laneTrackOne.has_value())

A032 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:166
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!canvasA->laneBody(*laneTrackOne).isEmpty())

A033 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:168
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      DocLanePoint panPoint;

Original expression:
  QVERIFY(!documentA.findLanePoint(1, kPan, kPanTick, &panPoint))

A034 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:170
Disposition: PARTIAL
Mapping/reason: Related predicate S014 in src/checks/selectionkey/localinputtier_text.swift (the pan-lane insertion prompt opens); unproved: the track-1 pan lane — the Swift fixture is single-track, so the track-1 prompt entry has no counterpart.
Source context since previous assertion:
  ;
      const auto originalTrackPan = documentA.lanePoints(kTrack, kPan);

Original expression:
  QVERIFY(canvasA->openValuePromptForInsertion(*laneTrackOne, kPanTick, 64))

A035 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:172
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      canvasA->acceptNodeValuePrompt(32);

Original expression:
  QVERIFY(documentA.findLanePoint(1, kPan, kPanTick, &panPoint))

A036 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:173
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(panPoint.value, 96)

A037 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:175
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      const auto originalTrackPanAfter = documentA.lanePoints(kTrack, kPan);

Original expression:
  QCOMPARE(originalTrackPanAfter.size(), originalTrackPan.size())

A038 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:177
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      for (size_t index = 0; index < originalTrackPan.size(); ++index) {

Original expression:
  QCOMPARE(originalTrackPanAfter[index].tick, originalTrackPan[index].tick)

A039 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:178
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(originalTrackPanAfter[index].value, originalTrackPan[index].value)

A040 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:180
Disposition: PARTIAL
Mapping/reason: Related predicates S019-S021 in src/checks/editorqml/tst_ShellWindow.qml: a real key-delivered edit in the first tab leaves the second tab note summary unchanged (S020) and only the destination tab reports unsaved changes (S021), with the destination edit observed as exactly one inserted note (S019). Unproved as stated: the same isolation for the native lane value-prompt insertion (the shell ingress is a Paste) and the byte-level document comparison.
Source context since previous assertion:
  ;
      }

Original expression:
  QCOMPARE(documentB.smf().write(), documentBAfterMove)

A041 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:182
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      viewA.selectionModel().setNoteSelection({pairT1->ids[0], pairT1->ids[1]});

Original expression:
  QVERIFY2(focusAutomationBand(viewA),
               "could not focus the automation band on the new primary track")

A042 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:186
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(windowA, Qt::Key_Up);
      DocNote trackOneNote;

Original expression:
  QVERIFY2(documentA.findNote(pairT1->ids[0], &trackOneNote) &&
                   trackOneNote.key == uint8_t(pairT1->notes[0].key + 1),
               "the Up arrow did not transpose the new primary track's selection")

A043 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:193
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

      // Return both tabs to their saved state so the close, the reopen, and the
      // final window close take production's genuine clean lifecycle instead of
      // blocking in an unsaved-changes prompt.

Original expression:
  QVERIFY2(selectionkey::undoTabToClean(workspace, documentA,
                                            QStringLiteral("the first tab stayed dirty before "
                                                           "closing the second tab"),
                                            &error),
               qUtf8Printable(error))

A044 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:200
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      workspace.selectSongTab(tabB);
      selectionkey::settle();

Original expression:
  QVERIFY2(selectionkey::undoTabToClean(workspace, documentB,
                                            QStringLiteral("the second tab stayed dirty before "
                                                           "its close"),
                                            &error),
               qUtf8Printable(error))

A045 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:213
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      const QPointer<SongTab> closingTab = tabB;
      const QPointer<AutomationCanvas> closingCanvas = canvasB;
      {
          selectionkey::DeclineModalsWithin guard(
              QStringLiteral("closing the clean second song tab"));
          workspace.requestCloseSelectedTab();
      }
      selectionkey::settle();

Original expression:
  QVERIFY2(workspace.songTabFor(*SongName::create(m_songB)) == nullptr && closingTab.isNull(),
               "the clean second tab did not close and destroy its session")

A046 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:215
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(closingCanvas.isNull())

A047 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:219
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      const QByteArray documentABeforeReopen = documentA.smf().write();
      const int tabCountBeforeReopen = workspace.openTabCount();
      SongTab *const reopened = selectionkey::openSongTab(m_session, m_songB, true, error);

Original expression:
  QVERIFY2(reopened, qUtf8Printable(error))

A048 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:220
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(workspace.selectedSongTab() == reopened &&
                   workspace.openTabCount() == tabCountBeforeReopen + 1 &&
                   workspace.songTabFor(tabA->name()) == tabA,
               "reopening the second song did not create and select its replacement while "
               "retaining the first tab")

A049 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:230
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      SongView &viewR = reopened->view();
      SongDocument &documentR = reopened->document();
      viewR.selectTrack(kTrack);
      viewR.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
      const std::optional<NotePair> pairR = addNotePair(documentR, kTrack, 960);

Original expression:
  QVERIFY2(pairR.has_value(),
               "the reserved tick-960 note pair could not be inserted on the reopened tab")

A050 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:234
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      songview::TimelineQuickView *const quickR = selectionkey::quickCanvas(viewR);
      QQuickWindow *const windowR = quickR ? quickR->quickWindow() : nullptr;

Original expression:
  QVERIFY2(windowR, "the reopened tab Quick window is missing")

A051 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:237
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      AutomationCanvas *const canvasR = automationCanvas(viewR);
      const EditorAutomationRowId volume{EditorAutomationRowKind::ControlChange, kTrack, 7};

Original expression:
  QCOMPARE(canvasR->parameterRow(canvasR->activeParameter()), std::optional{volume})

A052 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:239
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      viewR.selectionModel().setNoteSelection({pairR->ids[0], pairR->ids[1]});

Original expression:
  QVERIFY2(focusAutomationBand(viewR), "could not focus the reopened tab's automation band")

A053 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:241
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
      selectionkey::deliverKey(windowR, Qt::Key_Right);

Original expression:
  QVERIFY2(!notePairUnchanged(documentR, *pairR),
               "the reopened tab's routing is stale or dead after document replacement")

A054 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:243
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(documentA.smf().write(), documentABeforeReopen)

A055 | SelectionWindowTierTest::tabsDocumentsAndPrimaryTrackLifetime | src/checks/selectionkey/windowtier_lifetime.cpp:244
Disposition: NATIVE
Mapping/reason: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY2(selectionkey::undoTabToClean(workspace, documentR,
                                            QStringLiteral("the reopened tab stayed dirty before "
                                                           "the window close"),
                                            &error),
               qUtf8Printable(error))

Remaining original source (closing sequence and helper bodies):
  ;
  }


Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:23
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:34
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:37
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:39
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:41
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:44
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:45
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:47
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")

S009 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:53
  report.expect(page.rows.contains { $0.parameter == fixture.panLane }, cppID: id,
                    message: "the original CC10 lane is present in the page's row stack")

S010 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:55
  report.expect(insertedNotes.first.flatMap { fixture.document.note($0) } != nil, cppID: id,
                    message: "the original fixture note resolves by its inserted ID")

S011 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:62
  report.expect(page.rows.contains { $0.parameter == fixture.volumeLane }, cppID: labelID,
                    message: "the original volume lane is present in the page's row stack")

S012 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:64
  report.expect(page.openPrompt(tick: 5808, value: 48), cppID: labelID,
                    message: "the original volume-lane insertion prompt opens")

S013 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:66
  report.expectEqual("48", page.promptDraft, cppID: labelID,
                         what: "the volume prompt opens with the plotted value as its draft")

S014 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:75
  report.expect(page.openPrompt(tick: 5760, value: 64), cppID: lifetimeID,
                    message: "the original pan-lane insertion prompt opens")

S015 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:78
  report.expect(!page.promptOpen, cppID: lifetimeID,
                    message: "the document-switch cancellation closes the prompt")

S016 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:80
  report.expect(!page.acceptPrompt(displayedValue: 96), cppID: lifetimeID,
                    message: "accepting a closed prompt commits nothing")

S017 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:82
  report.expectEqual(before, fixture.snapshot, cppID: lifetimeID,
                         what: "the closed prompt's late acceptance leaves the document unchanged")

S018 | test_dCopyFromOneSongTabPastesIntoAnother | src/checks/editorqml/tst_ShellWindow.qml:405
  compare(selected.length, 1, "one source note is selected")
  keySequence(StandardKey.Copy)
  compare(JSON.parse(bootstrap.copiedClipSummary())[2], selected[0].pitch,
          "Copy places the selected source note on the host clipboard")
  // The source tab is the second song (mus_littleroot_test) opened by
  // openTwoSongShell (:88); ShellQmlLane stages both songs
  // (src/checks/editorqml/ShellQmlTests.swift:46).

S019 | test_dCopyFromOneSongTabPastesIntoAnother | src/checks/editorqml/tst_ShellWindow.qml:425
  keySequence(StandardKey.Paste)
  verify(waitForNative(function() {
      var notes = JSON.parse(destination.gridModel.noteSummary)
      return notes.some(function(note) {
          return destinationIDs.indexOf(note.id) < 0
              && note.pitch === selected[0].pitch && note.track === destinationTrack
              && note.selected
      })
  }, 5000), "Paste inserts and selects the copied note in the destination track")
  compare(inserted.length, 1, "Paste changes only the destination by one note")
  compare(destination.gridModel.editCursorTick, inserted[0].tick + inserted[0].duration,
          "Paste advances the destination edit cursor past the inserted note")

S020 | test_dCopyFromOneSongTabPastesIntoAnother | src/checks/editorqml/tst_ShellWindow.qml:446
  compare(selectedSurface().gridModel.noteSummary, sourceSummary,
          "cross-tab Paste leaves the source song unchanged")

S021 | test_dCopyFromOneSongTabPastesIntoAnother | src/checks/editorqml/tst_ShellWindow.qml:452
  compare(shell.shellPresenter.session.documentDirty, true,
          "only the pasted destination has unsaved changes")

S022 | test_eTimeAndTracksMenuContainment | src/checks/editorqml/tst_ShellWindow.qml:538
  mouseClick(firstButton, firstButton.width / 3, firstButton.height / 2)
  tryCompare(shell.shellPresenter.session.songTabs, "selectedId", firstId)

S023 | test_cWindowShortcutsAndNumericOwnership | src/checks/editorqml/tst_ShellWindow.qml:220
  mouseClick(firstButton, firstButton.width / 3, firstButton.height / 2)
  tryCompare(tabs, "selectedId", firstId, 3000,
             "a real first-tab click retargets the selected workspace")

S024 | test_gEditorRoutedNoteKeysAndChromeArrows | src/checks/editorqml/tst_ShellWindow.qml:631
  keyClick(Qt.Key_Up)
  compare(selectedNote().pitch, original.pitch + 1, "Up transposes one semitone")

Shared helpers
selectDrawnVelocityNote (:153) and openTwoSongShell (:88) stage the two-song
fixture and select the drawn velocity note; the helper itself asserts
selectedCount 1 (:166) and carries no other predicate.
