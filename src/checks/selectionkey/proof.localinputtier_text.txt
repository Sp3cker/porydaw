Assertion correspondence: selectionkey/localinputtier_text.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/selectionkey/localinputtier_text.cpp
Original SHA-256: 80adaaa8ce5a7eddfa1a779243b266c24be61bd830b111f4fd65f4f6f0d8b3d6
Original removed from current checkout; use git show at reference.
Swift counterpart: src/checks/selectionkey/localinputtier_text.swift
Swift SHA-256: 0b1d5de7907758291583092f0d482a14eec5af37dd8d4f610689ea2e64d8c1eb
Additional Swift counterpart: src/checks/editorqml/ShellQmlTests.swift
Additional Swift SHA-256: e6d919d7052150913adb2b7aea97b9f5a5c3697e259177def2c011224ba09a3f
Additional Swift counterpart: src/checks/editorqml/tst_ShellWindow.qml
Additional Swift SHA-256: 8bfc62d0ee7a984bcf7b8c03831d2922ce1587f37a2a88ea1cb8a970ce13ae26

Scope: numericPromptOwnsKeys and velocityPromptOwnsKeys. Other historical text-surface methods are not covered by this scoped proof; this is not a file-retirement certificate.

Production Swift: drawerOriginalNumericPromptTransaction reuses the original CC10 rows48=32,96=64, inserted prompt144 with initial64 and selected note24/key60/duration24/velocity100. It checks draft12, no document mutation, preserved selection, and cancellation, plus the row-stack lane presence, inserted-note ID resolution, the windowtier_keyboard volume-lane insertion prompt at tick5808 value48 and the windowtier_lifetime pan-lane insertion prompt at tick5760 value64 with document-switch cancellation and late-acceptance no-op. The original selected-note velocity prompt remains PARTIAL: its fixture lacks the parsed voice/program needed by the production prompt, so it must not be claimed through an unrelated synthetic fixture. Actual keyboard delivery, clipboard, focus, modal close and window routing require production host verification, not a fake Swift keyboard model.

Swift verification: deno task verify --filter swiftcore PASS (1 selected harness, 23 skipped). This runs drawerOriginalNumericPromptTransaction including the numeric-lane and document-switch predicates. Native keyboard delivery still requires the production host.
Keyboard routing is explicitly deferred by the user: ALL numeric inputs must yield Space to the application. The two editorqml numeric-Space rows fail and are not parity evidence. They run in a bare QuickTest window without RewriteWindow's production host arbitration. The original exception belongs in that existing host authority, not a second QML shortcut dispatcher. No host/focus implementation was changed in this task. Per-assertion reconciliation is recorded below: model-level sites whose observable contract the registered Swift transaction exercises are MATCHED with their S predicate; sites observing QML items, native key/pointer delivery, clipboard, action counters, focus, or window routing remain PARTIAL with the unproved condition named. Do not delete original reference coverage based on this file.

Original 272
Disposition: PARTIAL
Mapping: No Swift predicate: the binding registry (keymap::Registry::singleStroke for roll.solo_tracks/roll.copy) is C++-only; no production Swift keymap query exists.
QVERIFY2(solo.has_value() && copy.has_value(), "Solo/Copy have no single-key bindings")

Original 284
Disposition: PARTIAL
Mapping: No Swift predicate: the QML Quick window and the timelineAutomationInput item are native surfaces the model check does not mount.
QVERIFY2(quickWindow && automation, "the automation canvas surface is unavailable")

Original 289
Disposition: PARTIAL
Mapping: No Swift predicate: AutomationProbe locates the parameter through the QML input item's geometry; no model counterpart exists.
QVERIFY2(probe.has_value(), qUtf8Printable(coordinateDiagnostics))

Original 290
Disposition: PARTIAL
Mapping: No Swift predicate: the probe activates through the native QML input item; the page's activateParameter route is exercised by the fixture but does not prove the native activation path.
QVERIFY2(probe->activateParameter(&coordinateDiagnostics),
             qUtf8Printable(coordinateDiagnostics))

Original 295
Disposition: PARTIAL
Mapping: No Swift predicate: the QML AutomationCanvas object is a native surface; the Swift AutomationPage is the model counterpart, not the canvas itself.
QVERIFY(canvas)

Original 304
Disposition: MATCHED
Mapping: The original scans the canvas row list for the track-0 CC10 lane; the Swift page publishes the same catalog row stack and the predicate observes the lane's presence.
Swift counterpart expression (S009):
  report.expect(page.rows.contains { $0.parameter == fixture.panLane }, ...)
QVERIFY(laneIndex >= 0)

Original 309
Disposition: MATCHED
Mapping: The original inserts the fixture note and resolves it by ID; the Swift transaction inserts the identical note (track0 tick24 key60 duration24 velocity100) and resolves it through document.note.
Swift counterpart expression (S010):
  report.expect(insertedNotes.first.flatMap { fixture.document.note($0) } != nil, ...)
QVERIFY2(note.has_value(), "the fixture note was not created")

Original 322
Disposition: MATCHED
Mapping: Same production entry point, same arguments: the insertion prompt opens on the active CC10 lane at empty tick 144 with value 64.
Swift counterpart expression (S002):
  report.expect(page.openPrompt(tick: 144, value: 64), ...)
QVERIFY(canvas->openValuePromptForInsertion(LaneHandle{laneIndex}, kEmptyTick, 64))

Original 323
Disposition: PARTIAL
Mapping: Related predicates S002, S006 (prompt open/closed model flags); unproved: QML chrome prompt visibility, which the model flag does not observe.
QTRY_VERIFY(automation_valueprompt::promptVisible(chrome))

Original 325
Disposition: PARTIAL
Mapping: No Swift predicate: active-focus routing to the QML text input is a native window observation.
QTRY_VERIFY2((prompt = automation_valueprompt::focusedTextInput(*quickWindow)),
                 "the value prompt did not take active focus")

Original 332
Disposition: PARTIAL
Mapping: Related predicate S003 (the prompt retains draft "12"); unproved: native digit key delivery into the focused QML field versus updatePromptDraft.
QVERIFY2(prompt && prompt->property("text").toString() == QStringLiteral("12"),
             "the delivered digits did not land in the value prompt")

Original 339
Disposition: PARTIAL
Mapping: No Swift predicate: QMetaObject selectAll on the QML field has no model counterpart.
QVERIFY(QMetaObject::invokeMethod(prompt, "selectAll"))

Original 345
Disposition: PARTIAL
Mapping: No Swift predicate: native Copy-key delivery, the QML selectedText property, and the system clipboard are native observations.
QVERIFY2(numericText == QStringLiteral("12") && copiedFrom == numericText &&
                 copiedText == copiedFrom,
             qPrintable(QStringLiteral("numeric Copy mismatch: field='%1' selected='%2' "
                                       "clipboard='%3'")
                            .arg(numericText, copiedFrom, copiedText)))

Original 357
Disposition: PARTIAL
Mapping: Related predicate S006 (prompt open state); unproved: prompt visibility after native Up/Down key delivery.
QVERIFY2(automation_valueprompt::promptVisible(chrome),
             "the value prompt closed while arrow keys were delivered")

Original 359
Disposition: PARTIAL
Mapping: Related predicate S005 (selection preserved); unproved: note selection after native arrow-key delivery to the focused prompt.
QVERIFY2(view.selectionModel().noteSelection() == std::vector<NoteId>{note->id},
             "prompt arrow keys leaked into the note selection")

Original 361
Disposition: PARTIAL
Mapping: Related predicates S004, S007 (snapshot unchanged); unproved: song bytes after native arrow-key delivery leaking shared edit commands.
QVERIFY2(document.smf().write() == before,
             "prompt arrow keys leaked a shared edit command into the song")

Original 368
Disposition: PARTIAL
Mapping: No Swift predicate: QMetaObject selectAll on the QML field has no model counterpart.
QVERIFY(QMetaObject::invokeMethod(prompt, "selectAll"))

Original 380
Disposition: PARTIAL
Mapping: Related predicate S003 (draft text); unproved: field text after native Solo-key delivery through the QML validator contract.
QVERIFY2(prompt && prompt->property("text").toString() == expectedAfterSolo,
             qPrintable(QStringLiteral("numeric Solo-key contract mismatch: expected='%1' "
                                       "actual='%2'")
                            .arg(expectedAfterSolo,
                                 prompt ? prompt->property("text").toString() : QString())))

Original 385
Disposition: PARTIAL
Mapping: No Swift predicate: the window action counter (m_counts.solo) and view track-solo state after native Solo delivery are native routing observations.
QVERIFY2(m_counts.solo == soloBefore && !view.trackSoloed(kTrack),
             qPrintable(QStringLiteral("Solo escaped the value prompt: action-delta=%1 "
                                       "track-soloed=%2")
                            .arg(m_counts.solo - soloBefore)
                            .arg(view.trackSoloed(kTrack))))

Original 390
Disposition: PARTIAL
Mapping: Related predicates S004, S007 (snapshot unchanged); unproved: song bytes after native Solo-key delivery.
QVERIFY2(document.smf().write() == before,
             "value prompt typing mutated the automation lane or the song")

Original 396
Disposition: PARTIAL
Mapping: Related predicate S006 (prompt closed); unproved: prompt visibility after native Escape delivery.
QTRY_VERIFY(!automation_valueprompt::promptVisible(chrome))

Original 397
Disposition: PARTIAL
Mapping: Related predicates S007, S008 (cancel leaves snapshot and song unchanged); unproved: song bytes after native Escape delivery.
QVERIFY2(document.smf().write() == before, "cancelling the value prompt mutated the song")

Original 399
Disposition: PARTIAL
Mapping: No Swift predicate: native band refocus (focusTimelineBand, MouseFocusReason) is a window focus observation.
QVERIFY2(view.focusTimelineBand(songview::TimelineBand::Automation, Qt::MouseFocusReason),
             "could not refocus the automation band after the prompt")

Original 402
Disposition: PARTIAL
Mapping: No Swift predicate: Solo action counter and track-solo state after native key delivery on the refocused band.
QVERIFY2(m_counts.solo == soloBefore + 1 && view.trackSoloed(kTrack),
             "Solo did not resume after the value prompt closed")

Original 405
Disposition: PARTIAL
Mapping: No Swift predicate: track-solo state after a second native Solo delivery.
QVERIFY2(!view.trackSoloed(kTrack), "Solo did not untoggle after the prompt")

Original 416
Disposition: PARTIAL
Mapping: No Swift predicate: the binding registry (keymap::Registry::singleStroke for roll.solo_tracks/roll.copy) is C++-only; no production Swift keymap query exists.
QVERIFY2(solo.has_value() && copy.has_value(), "Solo/Copy have no single-key bindings")

Original 422
Disposition: MATCHED
Mapping: Same fixture note as numericPromptOwnsKeys (track0 tick24 key60 duration24 velocity100): the Swift transaction inserts it and resolves it through document.note.
Swift counterpart expression (S010):
  report.expect(insertedNotes.first.flatMap { fixture.document.note($0) } != nil, ...)
QVERIFY2(note.has_value(), "the fixture note was not created")

Original 432
Disposition: PARTIAL
Mapping: No Swift predicate: the QML Quick window and the timelineRollInput item are native surfaces the model check does not mount.
QVERIFY2(quickWindow && rollInput, "the roll surface is unavailable")

Original 448
Disposition: PARTIAL
Mapping: No Swift predicate: QML item bounds geometry against the live camera is a native rendering observation.
QVERIFY2(rollInput->bounds().contains(rollPoint),
             "production roll geometry did not expose the fixture note")

Original 451
Disposition: PARTIAL
Mapping: No Swift predicate: the press point inside the QML Quick window is native geometry.
QVERIFY2(QRect(QPoint{}, quickWindow->size()).contains(rollPress),
             "the note menu press left the Quick window")

Original 465
Disposition: PARTIAL
Mapping: No Swift predicate: the note menu opens through native right-click delivery into a QML popup session.
QVERIFY2(menuSession && menuSession->isOpen(), "right-click did not open the note menu")

Original 467
Disposition: PARTIAL
Mapping: No Swift predicate: the QML menu panel item is a native surface.
QVERIFY2(panel, "the note menu did not render a panel")

Original 469
Disposition: PARTIAL
Mapping: No Swift predicate: the QML menu's typed model is a native object.
QVERIFY2(model, "the note menu has no typed model")

Original 472
Disposition: PARTIAL
Mapping: No Swift predicate: the velocity row lookup runs on the QML menu model.
QVERIFY2(velocityRow >= 0, "the note menu has no velocity action")

Original 473
Disposition: PARTIAL
Mapping: No Swift predicate: the menu row receives a real native click.
QVERIFY2(quick_popup::clickMenuRow(*menuSession, velocityRow),
             "the velocity menu row did not receive a real click")

Original 479
Disposition: PARTIAL
Mapping: Related predicate S018 (the production selected-note prompt entry the menu action calls); unproved: the QML popup session's open state and window after the native menu click.
QVERIFY2(popup && popup->isOpen() && canvas,
             "the velocity action did not open the canvas prompt")

Original 481
Disposition: MATCHED
Mapping: The shell lane opens the production Set Velocity prompt through the production EditCommand.setVelocity (S032) and the numeric field reaches activeFocus (S021); the production shell item activeFocus observable replaces the original QQuickWindow focus query. No QWidget layer exists in the Swift app.
Swift counterpart expression (S021):
  tryCompare(field, "activeFocus", true, 3000)
QTRY_VERIFY2(quick_popup::inputHasActiveFocus(*canvas, QLatin1String("noteVelocityInput")),
                 "the velocity prompt text input did not take active focus")

Original 484
Disposition: MATCHED
Mapping: The lane resolves noteVelocityInput under the mounted production EditorSurface and verifies that field exists (S020), replacing the QQuickItem existence query with the production shell item lookup. No QWidget layer exists in the Swift app.
Swift counterpart expression (S020):
  var field = findChild(surface, "noteVelocityInput"); verify(field, "the original numeric prompt has a text field")
QVERIFY2(input, "the velocity prompt has no text input")

Original 486
Disposition: MATCHED
Mapping: The QML noteVelocityInput displays Number(model.promptDraft); the production page publishes the captured note velocity (100) as that draft, the same observable text.
Swift counterpart expression (S019):
  report.expectEqual("100", velocityPage.promptDraft, ...)
QCOMPARE(input->property("text").toString(), initialText)

Original 487
Disposition: MATCHED
Mapping: The lane selects the whole draft and delivers digits, and the field text becomes exactly "12" (S022, S023): only a full initial selection leaves no remnant. The QML DragInput focus leaf exposes text and selectAll but no selectedText property, so the selection fact is observed through that full replacement. No QWidget layer exists in the Swift app.
Swift counterpart expression (S022, S023):
  field.selectAll()
  keyClick(Qt.Key_1); keyClick(Qt.Key_2); compare(field.text, "12", "digits stay in the focused numeric editor")
QCOMPARE(input->property("selectedText").toString(), initialText)

Original 492
Disposition: MATCHED
Mapping: After the digit deliveries the lane compares the field text to "12" (S023); the production DragInput publishes the numeric draft exactly as the original QML field displayed it. No QWidget layer exists in the Swift app.
Swift counterpart expression (S023):
  keyClick(Qt.Key_1); keyClick(Qt.Key_2); compare(field.text, "12", "digits stay in the focused numeric editor")
QCOMPARE(input->property("text").toString(), QStringLiteral("12"))

Original 497
Disposition: MATCHED
Mapping: The lane delivers the production field own selectAll() at the digit row (S022, delivery at tst_ShellWindow.qml:334), the same QML method on the same field the original invoked through QMetaObject. No QWidget layer exists in the Swift app.
Swift counterpart expression (S022):
  field.selectAll()
QVERIFY(QMetaObject::invokeMethod(input, "selectAll"))

Original 501
Disposition: MATCHED
Mapping: The lane selects the draft, delivers native StandardKey.Copy, replaces the text and pastes, and the field text returns to "12" (S022, S024, S025): the clipboard carried the field own text. The window Copy counter stays at windowCopyBeforePrompt (S024), so Copy never left the field. No QWidget layer exists in the Swift app.
Swift counterpart expression (S024, S025):
  keySequence(StandardKey.Copy); compare(copyActivatedSpy.count, windowCopyBeforePrompt, "prompt Copy/Paste never fire the window Copy action")
  keySequence(StandardKey.Paste); compare(field.text, "12", "native Copy and Paste stay with the text editor")
QVERIFY2(!copiedFrom.isEmpty() && QApplication::clipboard()->text() == copiedFrom &&
                 m_counts.copy == copyBefore,
             "Copy while the velocity prompt owns the keys did not stay local to the field")

Original 506
Disposition: MATCHED
Mapping: The lane delivers the production field own selectAll() at the Paste row (S022, delivery at tst_ShellWindow.qml:343) before the native Paste replaces the selected draft with the copied text, the same QML method the original invoked through QMetaObject. No QWidget layer exists in the Swift app.
Swift counterpart expression (S022):
  field.selectAll()
QVERIFY(QMetaObject::invokeMethod(input, "selectAll"))

Original 510
Disposition: MATCHED
Mapping: The Paste row compares the field text to "12" and the window Copy counter to windowCopyBeforePrompt (S025, S024); the immediately following S row keeps firstTrack.soloChecked false with the prompt open (S026), the substitute for the original m_counts.solo conjunct. No QWidget layer exists in the Swift app.
Swift counterpart expression (S025, S024):
  keySequence(StandardKey.Paste); compare(field.text, "12", "native Copy and Paste stay with the text editor"); compare(copyActivatedSpy.count, windowCopyBeforePrompt, "prompt Copy/Paste never fire the window Copy action")
QVERIFY2(input->property("text").toString() == QStringLiteral("12") &&
                 m_counts.copy == copyBefore && m_counts.solo == soloBefore,
             "Paste while the velocity prompt owned focus leaked to a background command")

Original 516
Disposition: MATCHED
Mapping: The lane delivers the same production field selectAll() (S022, deliveries at tst_ShellWindow.qml:334/339/341/343) where the original re-selected the whole draft for a key-delivery row; the following S row observes firstTrack.soloChecked false with the prompt open (S026). No QWidget layer exists in the Swift app.
Swift counterpart expression (S022, S026):
  field.selectAll()
  keyClick(Qt.Key_S); compare(firstTrack.soloChecked, false, "S in a numeric editor never fires Solo")
QVERIFY(QMetaObject::invokeMethod(input, "selectAll"))

Original 528
Disposition: MATCHED
Mapping: The lane delivers S to the focused production numeric field: the window Solo Shortcut never fires (firstTrack.soloChecked stays false, the state every genuine Solo activation flips in this lane) and the prompt stays open (S026). The C++ counter conjunct is observed through that track state, and the production DragInput owns the numeric draft through its own validator instead of the original QML field text property. No QWidget layer exists in the Swift app.
Swift counterpart expression (S026):
  keyClick(Qt.Key_S); compare(firstTrack.soloChecked, false, "S in a numeric editor never fires Solo"); compare(session.velocityPage().promptOpen, true)
QVERIFY2(input->property("text").toString() == expectedAfterSolo &&
                 m_counts.solo == soloBefore && !view.trackSoloed(kTrack),
             "Solo escaped the velocity prompt")

Original 535
Disposition: MATCHED
Mapping: The same row at window level: the production window Shortcut shellShortcut_roll.solo_tracks is mounted from the real shell and this delivery leaves the track unsoloed with the prompt open (S026), the Swift form of the original window-command suppression under the prompt ShortcutOverride policy; the activation counter observable is the track Solo state that each genuine activation flips. No QWidget layer exists in the Swift app.
Swift counterpart expression (S026):
  compare(firstTrack.soloChecked, false, "S in a numeric editor never fires Solo"); compare(session.velocityPage().promptOpen, true)
QVERIFY2(m_counts.solo == soloBefore && !view.trackSoloed(kTrack) && popup->isOpen(),
             "a window command fired while the velocity prompt was open")

Original 544
Disposition: MATCHED
Mapping: Up/Down keep field.activeFocus true with the prompt open, velocityPage selectedCount 1 and session.documentDirty false (S027). The production noteVelocityInput intentionally steps the numeric draft on Up/Down (adjustmentsEnabled), so the original text-equality conjunct is not transferred; the no-leak observables (focus, popup, selection, song) are, with the shell documentDirty flag standing in for the SMF byte comparison. No QWidget layer exists in the Swift app.
Swift counterpart expression (S027):
  keyClick(Qt.Key_Up); keyClick(Qt.Key_Down); compare(field.activeFocus, true, "prompt arrows keep focus in the numeric editor"); compare(session.velocityPage().promptOpen, true); tryCompare(session.velocityPage(), "selectedCount", 1, 3000, "prompt arrows keep the musical selection"); compare(session.documentDirty, false, "prompt arrows never edit the song")
QVERIFY2(popup && popup->isOpen() && input->property("text").toString() == initialText &&
                 view.selectionModel().noteSelection() == std::vector<NoteId>{note->id} &&
                 document.smf().write() == before,
             "arrow keys leaked out of the velocity prompt")

Original 551
Disposition: MATCHED
Mapping: The lane delivers the same production field selectAll() (S022, deliveries at tst_ShellWindow.qml:334/339/341/343) for the following window-key rows; the Space row observes the field text unchanged and the field still focused while transport toggles (S028), the same field ownership the original selected the whole draft for. No QWidget layer exists in the Swift app.
Swift counterpart expression (S022, S028):
  field.selectAll()
  keyClick(Qt.Key_Space); compare(field.text, promptTextAfterArrows, "transport Space never modifies numeric text"); compare(field.activeFocus, true)
QVERIFY(QMetaObject::invokeMethod(input, "selectAll"))

Original 555
Disposition: MATCHED
Mapping: The lane delivers Space with the numeric field focused and observes playhead.playing flip true with the field text unchanged from promptTextAfterArrows and the field still focused (S028), replacing the original playPause trigger count with the playback state transition that count caused; the lane also pins the production transport.play_pause sequence to Space (S031). No QWidget layer exists in the Swift app.
Swift counterpart expression (S028, S031):
  tryCompare(playhead, "playing", true, 3000, "window Space owns transport even with numeric text focus"); compare(field.text, promptTextAfterArrows, "transport Space never modifies numeric text")
  compare(shell.shellPresenter.actionSequences("transport.play_pause")[0], "Space")
QVERIFY2(playPause.count() == 1 && input->property("text").toString() == textBeforeSpace &&
                 popup->isOpen() && input->property("activeFocus").toBool(),
             qPrintable(QStringLiteral("Space in the velocity prompt did not toggle the transport: "
                                       "triggers=%1 text='%2' open=%3 focused=%4")
                            .arg(playPause.count())
                            .arg(input->property("text").toString(), textBeforeSpace)
                            .arg(popup->isOpen())
                            .arg(input->property("activeFocus").toBool())))

Original 566
Disposition: MATCHED
Mapping: The second Space flips playhead.playing back to false and the prompt stays open (S029), the substitution for the original second playPause trigger count that leaves no playback running.
Swift counterpart expression (S029):
  tryCompare(playhead, "playing", false, 3000, "the second window Space reverses one transport activation"); compare(session.velocityPage().promptOpen, true)
QVERIFY2(playPause.count() == 2 && popup->isOpen(),
             qPrintable(QStringLiteral("Space did not toggle the transport twice: triggers=%1")
                            .arg(playPause.count())))

Original 573
Disposition: MATCHED
Mapping: Escape from the same lane closes the production prompt: the page promptOpen flag returns false (S030), replacing the QML popup visibility with the page prompt state the production prompt owns.
Swift counterpart expression (S030):
  keyClick(Qt.Key_Escape); tryCompare(session.velocityPage(), "promptOpen", false, 3000)
QTRY_VERIFY2(!popup->isOpen(), "Escape did not close the velocity prompt")

Original 574
Disposition: PARTIAL
Mapping: Related predicates S007, S008 (cancel leaves snapshot and song unchanged); unproved: song bytes after native Escape delivery.
QVERIFY2(document.smf().write() == before, "cancelling the velocity prompt mutated the song")

Original 576
Disposition: PARTIAL
Mapping: No Swift predicate: native band refocus (focusTimelineBand, MouseFocusReason) is a window focus observation.
QVERIFY2(view.focusTimelineBand(songview::TimelineBand::Roll, Qt::MouseFocusReason),
             "could not refocus the roll band after the prompt")

Original 579
Disposition: PARTIAL
Mapping: No Swift predicate: Solo action counter and track-solo state after native key delivery on the refocused band.
QVERIFY2(m_counts.solo == soloBefore + 1 && view.trackSoloed(kTrack),
             "Solo did not resume after the velocity prompt closed")

Original 582
Disposition: PARTIAL
Mapping: No Swift predicate: track-solo state after a second native Solo delivery.
QVERIFY2(!view.trackSoloed(kTrack), "Solo did not untoggle after the prompt")

Swift assertion predicates

S001 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:23
  report.expectEqual(1, notes.count, cppID: id, what: "the original selected fixture note exists")

S002 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:34
  report.expect(page.openPrompt(tick: 144, value: 64), cppID: id,
                    message: "the original empty-tick numeric prompt opens")

S003 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:37
  report.expectEqual("12", page.promptDraft, cppID: id,
                         what: "the production prompt retains the supplied numeric draft")

S004 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:39
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "draft editing does not mutate the song")

S005 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:41
  report.expectEqual(selection, fixture.session.selectedNotes, cppID: id,
                         what: "draft editing preserves note selection")

S006 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:44
  report.expect(!page.promptOpen, cppID: id, message: "cancellation closes the prompt")

S007 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:45
  report.expectEqual(before, fixture.snapshot, cppID: id,
                         what: "cancellation leaves the original document unchanged")

S008 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:47
  report.expect(fixture.document.state == state, cppID: id,
                    message: "draft and cancellation preserve the full song contents")

S009 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:53
  report.expect(page.rows.contains { $0.parameter == fixture.panLane }, cppID: id,
                    message: "the original CC10 lane is present in the page's row stack")

S010 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:55
  report.expect(insertedNotes.first.flatMap { fixture.document.note($0) } != nil, cppID: id,
                    message: "the original fixture note resolves by its inserted ID")

S011 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:62
  report.expect(page.rows.contains { $0.parameter == fixture.volumeLane }, cppID: labelID,
                    message: "the original volume lane is present in the page's row stack")

S012 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:64
  report.expect(page.openPrompt(tick: 5808, value: 48), cppID: labelID,
                    message: "the original volume-lane insertion prompt opens")

S013 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:66
  report.expectEqual("48", page.promptDraft, cppID: labelID,
                         what: "the volume prompt opens with the plotted value as its draft")

S014 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:75
  report.expect(page.openPrompt(tick: 5760, value: 64), cppID: lifetimeID,
                    message: "the original pan-lane insertion prompt opens")

S015 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:78
  report.expect(!page.promptOpen, cppID: lifetimeID,
                    message: "the document-switch cancellation closes the prompt")

S016 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:80
  report.expect(!page.acceptPrompt(displayedValue: 96), cppID: lifetimeID,
                    message: "accepting a closed prompt commits nothing")

S017 | drawerOriginalNumericPromptTransaction | src/checks/selectionkey/localinputtier_text.swift:82
  report.expectEqual(before, fixture.snapshot, cppID: lifetimeID,
                         what: "the closed prompt's late acceptance leaves the document unchanged")

S020 | test_cWindowShortcutsAndNumericOwnership prompt field | src/checks/editorqml/tst_ShellWindow.qml:331
  var field = findChild(surface, "noteVelocityInput"); verify(field, "the original numeric prompt has a text field")

S021 | test_cWindowShortcutsAndNumericOwnership field focus | src/checks/editorqml/tst_ShellWindow.qml:333
  tryCompare(field, "activeFocus", true, 3000)

S022 | test_cWindowShortcutsAndNumericOwnership selectAll deliveries | src/checks/editorqml/tst_ShellWindow.qml:334
  field.selectAll() before the digit row, and again at :339 (Copy row), :341 (draft replacement) and :343 (Paste row)

S023 | test_cWindowShortcutsAndNumericOwnership digit replacement | src/checks/editorqml/tst_ShellWindow.qml:337
  keyClick(Qt.Key_1); keyClick(Qt.Key_2); compare(field.text, "12", "digits stay in the focused numeric editor")

S024 | test_cWindowShortcutsAndNumericOwnership Copy stays in the field | src/checks/editorqml/tst_ShellWindow.qml:340
  keySequence(StandardKey.Copy) after field.selectAll(); compare(copyActivatedSpy.count, windowCopyBeforePrompt, "prompt Copy/Paste never fire the window Copy action")

S025 | test_cWindowShortcutsAndNumericOwnership Paste round trip | src/checks/editorqml/tst_ShellWindow.qml:344
  keySequence(StandardKey.Paste) after field.selectAll() and the replacing digit; compare(field.text, "12", "native Copy and Paste stay with the text editor")

S026 | test_cWindowShortcutsAndNumericOwnership S stays in the field | src/checks/editorqml/tst_ShellWindow.qml:349
  keyClick(Qt.Key_S); compare(firstTrack.soloChecked, false, "S in a numeric editor never fires Solo"); compare(session.velocityPage().promptOpen, true)

S027 | test_cWindowShortcutsAndNumericOwnership arrows stay in the prompt | src/checks/editorqml/tst_ShellWindow.qml:352
  keyClick(Qt.Key_Up); keyClick(Qt.Key_Down); compare(field.activeFocus, true, "prompt arrows keep focus in the numeric editor"); compare(session.velocityPage().promptOpen, true); tryCompare(session.velocityPage(), "selectedCount", 1, 3000, "prompt arrows keep the musical selection"); compare(session.documentDirty, false, "prompt arrows never edit the song")

S028 | test_cWindowShortcutsAndNumericOwnership first Space toggles transport | src/checks/editorqml/tst_ShellWindow.qml:362
  keyClick(Qt.Key_Space); tryCompare(playhead, "playing", true, 3000, "window Space owns transport even with numeric text focus"); compare(field.text, promptTextAfterArrows, "transport Space never modifies numeric text"); compare(field.activeFocus, true)

S029 | test_cWindowShortcutsAndNumericOwnership second Space reverses transport | src/checks/editorqml/tst_ShellWindow.qml:367
  keyClick(Qt.Key_Space); tryCompare(playhead, "playing", false, 3000, "the second window Space reverses one transport activation"); compare(session.velocityPage().promptOpen, true)

S030 | test_cWindowShortcutsAndNumericOwnership Escape closes the prompt | src/checks/editorqml/tst_ShellWindow.qml:371
  keyClick(Qt.Key_Escape); tryCompare(session.velocityPage(), "promptOpen", false, 3000)

S031 | test_cWindowShortcutsAndNumericOwnership production window bindings | src/checks/editorqml/tst_ShellWindow.qml:227
  verify(shell.shellPresenter.actionSequences("roll.copy").length > 0); compare(shell.shellPresenter.actionSequences("roll.solo_tracks")[0], "S"); compare(shell.shellPresenter.actionSequences("transport.play_pause")[0], "Space")

S032 | ShellQmlBootstrap.setVelocityCommand | src/checks/editorqml/ShellQmlTests.swift:191
  public func setVelocityCommand() -> Int { EditCommand.setVelocity.rawValue }; the row drives it through
  session.performGridCommand(bootstrap.setVelocityCommand())
