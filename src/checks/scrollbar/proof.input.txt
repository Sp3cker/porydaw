Assertion correspondence: scrollbar/input.cpp

Reference revision: 6b7e1fd989441360d234026c2d744218684966ce
Original: src/checks/scrollbar/input.cpp
Original SHA-256: 4a78343b01b892bfed84f1758538155a650775aec2543a0028ef9a3dbdc15a69
Swift counterpart: none identified
Swift SHA-256: n/a

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
No Swift counterpart file was identified in this checkout (no .swift under
src/checks/scrollbar/ or covering this suite elsewhere), so there are no Swift
predicates to cite; every site below is GAP or NATIVE.
MATCHED identifies equivalent model predicates; execution must still be verified.
PARTIAL identifies related Swift predicates with an explicit unproved condition.
NATIVE identifies retained native-window/rendering or native setup obligations;
it does not establish coverage of any converted window.
GAP identifies an original predicate without a replacement assertion.

Verification
Command: deno task verify --filter scrollbar --verbose
Result: pending integrated run; source hashes pin the tree inspected here.
No converted-window evidence is available for this suite in this checkout.

Execution ownership
No Swift entry point executes these checks. Native ownership: ScrollbarTest
methods trackPaging (4 data rows: horizontal/vertical x forward/backward),
wheelScrolling (16 data rows over orientation x wheel behavior), and
keyboardNavigation (6 data rows over orientation x key behavior), driven
through the ScrollbarTest fixture declared in
src/checks/scrollbar/tst_scrollbar.h and implemented in
src/checks/scrollbar/control.cpp (no assertion sites): view(), window(),
bar()/thumb(), press()/release(), wheel()/endWheel(), withinTrack(), plus the
camera model (SongView/TimeCamera) and QGuiApplication style hints for the
wheel notch. Each site below expands over its method's data rows.

Fixture, sequence and row constraints
trackPaging expands 4 rows, wheelScrolling 16 rows, keyboardNavigation 6 rows;
one site covers every row of its method. The wheel switch selects exactly one
stimulus branch per row, so the expected-delta sites observe row-dependent
deltas. The keyboard switch likewise selects one key per row; the Home/End
thumb-geometry sites execute only on their behavior rows (guarded by
if (behavior == ...)). QTRY_* sites poll until the camera/item state settles;
they are still one site each. Original setup, branch guards and helper
implementations at the reference revision remain authoritative; this inventory
does not replace those sources.

Original assertion inventory and source sequence

A001 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:69
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  #include "checks/scrollbar/tst_scrollbar.h"

  #include <cmath>

  #include <QGuiApplication>
  #include <QQuickItem>
  #include <QQuickWindow>
  #include <QStyleHints>
  #include <QtTest>

  #include "ui/songview.h"
  #include "ui/songview/timecamera.h"

  namespace {

  constexpr double kTolerance = 0.01;
  constexpr double kWheelMargin = 80.0;

  enum class WheelBehavior : int {
      PixelAccumulation,
      InvertedPixel,
      PixelPrecedesAngle,
      FullAngleNotch,
      FractionalAngleNotch,
      HorizontalPixel,
      HorizontalAngle,
      HorizontalDiagonal,
      Touchpad,
  };

  enum class KeyBehavior : int {
      SingleStep,
      Home,
      End,
  };

  } // namespace

  void ScrollbarTest::trackPaging_data()
  {
      QTest::addColumn<int>("orientationValue");
      QTest::addColumn<bool>("forward");

      QTest::newRow("horizontal-forward") << int(Qt::Horizontal) << true;
      QTest::newRow("horizontal-backward") << int(Qt::Horizontal) << false;
      QTest::newRow("vertical-forward") << int(Qt::Vertical) << true;
      QTest::newRow("vertical-backward") << int(Qt::Vertical) << false;
  }

  void ScrollbarTest::trackPaging()
  {
      QFETCH(int, orientationValue);
      QFETCH(bool, forward);
      const Qt::Orientation orientation =
          orientationValue == int(Qt::Horizontal) ? Qt::Horizontal : Qt::Vertical;
      SongView &songView = view();
      QQuickItem &scrollbar = bar(orientation);
      if (orientation == Qt::Horizontal)
          songView.setEditorTimeZoom(songView.pxPerBeat() * 2.0);

      const songview::TimeCamera &camera = songView.camera();
      const double page =
          orientation == Qt::Horizontal ? songView.viewportWidth() : songView.rollViewportHeight();
      const double minimum = orientation == Qt::Horizontal ? camera.minHScroll() : 0.0;
      const double maximum =
          orientation == Qt::Horizontal ? camera.maxHScroll() : camera.maxRollScroll();
      const double span = maximum - minimum;
Original expression:
  QTRY_VERIFY(std::abs(scrollbar.property("minimum").toReal() - minimum) < kTolerance);

A002 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:70
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(scrollbar.property("maximum").toReal() - maximum) < kTolerance);

A003 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:71
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(scrollbar.property("pageStep").toReal() - page) < kTolerance);

A004 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:73
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(span > page + kWheelMargin);

A005 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:83
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      const double pageStart = minimum + (span - page) / 2.0;
      const double initial = forward ? pageStart : pageStart + page;
      const double otherBefore = orientation == Qt::Horizontal ? camera.scrollY() : camera.scrollX();
      if (orientation == Qt::Horizontal)
          songView.setHScroll(initial);
      else
          songView.setVScroll(initial);

      if (orientation == Qt::Horizontal)
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - initial) < kTolerance);

A006 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:85
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      else
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - initial) < kTolerance);

A007 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:86
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(scrollbar.property("value").toReal() - initial) < kTolerance);

A008 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:101
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      QQuickItem &scrollThumb = thumb(orientation);
      const QPointF target =
          orientation == Qt::Horizontal
              ? QPointF(forward
                            ? scrollThumb.x() + scrollThumb.width() +
                                  (scrollbar.width() - scrollThumb.x() - scrollThumb.width()) / 2.0
                            : scrollThumb.x() / 2.0,
                        scrollbar.height() / 2.0)
              : QPointF(scrollbar.width() / 2.0,
                        forward
                            ? scrollThumb.y() + scrollThumb.height() +
                                  (scrollbar.height() - scrollThumb.y() - scrollThumb.height()) / 2.0
                            : scrollThumb.y() / 2.0);
Original expression:
  QVERIFY(scrollbar.boundingRect().contains(target));

A009 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:108
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      press(scrollbar.mapToScene(target));
      release();

      const double expected = forward ? pageStart + page : pageStart;
      if (orientation == Qt::Horizontal) {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - expected) < kTolerance);

A010 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:109
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - otherBefore) < kTolerance);

A011 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:111
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      } else {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - expected) < kTolerance);

A012 | ScrollbarTest::trackPaging | src/checks/scrollbar/input.cpp:112
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - otherBefore) < kTolerance);

A013 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:166
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  }

  void ScrollbarTest::wheelScrolling_data()
  {
      QTest::addColumn<int>("orientationValue");
      QTest::addColumn<int>("behaviorValue");

      QTest::newRow("horizontal-pixel-accumulation")
          << int(Qt::Horizontal) << int(WheelBehavior::PixelAccumulation);
      QTest::newRow("vertical-pixel-accumulation")
          << int(Qt::Vertical) << int(WheelBehavior::PixelAccumulation);
      QTest::newRow("horizontal-inverted-natural-sign")
          << int(Qt::Horizontal) << int(WheelBehavior::InvertedPixel);
      QTest::newRow("vertical-inverted-natural-sign")
          << int(Qt::Vertical) << int(WheelBehavior::InvertedPixel);
      QTest::newRow("horizontal-pixel-precedes-angle")
          << int(Qt::Horizontal) << int(WheelBehavior::PixelPrecedesAngle);
      QTest::newRow("vertical-pixel-precedes-angle")
          << int(Qt::Vertical) << int(WheelBehavior::PixelPrecedesAngle);
      QTest::newRow("horizontal-full-angle-notch")
          << int(Qt::Horizontal) << int(WheelBehavior::FullAngleNotch);
      QTest::newRow("vertical-full-angle-notch")
          << int(Qt::Vertical) << int(WheelBehavior::FullAngleNotch);
      QTest::newRow("horizontal-fractional-angle-notch")
          << int(Qt::Horizontal) << int(WheelBehavior::FractionalAngleNotch);
      QTest::newRow("vertical-fractional-angle-notch")
          << int(Qt::Vertical) << int(WheelBehavior::FractionalAngleNotch);
      QTest::newRow("horizontal-only-pixel")
          << int(Qt::Horizontal) << int(WheelBehavior::HorizontalPixel);
      QTest::newRow("horizontal-only-angle")
          << int(Qt::Horizontal) << int(WheelBehavior::HorizontalAngle);
      QTest::newRow("horizontal-diagonal-precedence")
          << int(Qt::Horizontal) << int(WheelBehavior::HorizontalDiagonal);
      QTest::newRow("horizontal-touchpad") << int(Qt::Horizontal) << int(WheelBehavior::Touchpad);
      QTest::newRow("vertical-touchpad") << int(Qt::Vertical) << int(WheelBehavior::Touchpad);
  }

  void ScrollbarTest::wheelScrolling()
  {
      QFETCH(int, orientationValue);
      QFETCH(int, behaviorValue);
      const Qt::Orientation orientation =
          orientationValue == int(Qt::Horizontal) ? Qt::Horizontal : Qt::Vertical;
      const WheelBehavior behavior = WheelBehavior(behaviorValue);
      const songview::TimeCamera &camera = view().camera();
      const double minimum = orientation == Qt::Horizontal ? camera.minHScroll() : 0.0;
      const double maximum =
          orientation == Qt::Horizontal ? camera.maxHScroll() : camera.maxRollScroll();
      const double midpoint = minimum + (maximum - minimum) / 2.0;
      const double otherBefore = orientation == Qt::Horizontal ? camera.scrollY() : camera.scrollX();
      const double notch = QGuiApplication::styleHints()->wheelScrollLines();
Original expression:
  QVERIFY(maximum - minimum > 2.0 * kWheelMargin);

A014 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:167
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(notch > 0.0);

A015 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:174
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      if (orientation == Qt::Horizontal)
          view().setHScroll(midpoint);
      else
          view().setVScroll(midpoint);

      if (orientation == Qt::Horizontal)
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - midpoint) < kTolerance);

A016 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:176
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      else
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - midpoint) < kTolerance);

A017 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:226
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      double expected = midpoint;
      switch (behavior) {
      case WheelBehavior::PixelAccumulation:
          wheel(orientation, QPoint(0, -10), {});
          wheel(orientation, QPoint(0, -10), {});
          wheel(orientation, QPoint(0, -10), {});
          expected += 30.0;
          break;
      case WheelBehavior::InvertedPixel:
          wheel(orientation, QPoint(0, -10), {}, true);
          expected += 10.0;
          break;
      case WheelBehavior::PixelPrecedesAngle:
          wheel(orientation, QPoint(0, -10), QPoint(0, 120));
          expected += 10.0;
          break;
      case WheelBehavior::FullAngleNotch:
          wheel(orientation, {}, QPoint(0, 120));
          expected -= notch;
          break;
      case WheelBehavior::FractionalAngleNotch:
          wheel(orientation, {}, QPoint(0, 50));
          expected -= notch * 50.0 / 120.0;
          break;
      case WheelBehavior::HorizontalPixel:
          wheel(orientation, QPoint(10, 0), {});
          expected -= 10.0;
          break;
      case WheelBehavior::HorizontalAngle:
          wheel(orientation, {}, QPoint(120, 0));
          expected -= notch;
          break;
      case WheelBehavior::HorizontalDiagonal:
          wheel(orientation, QPoint(20, -10), {});
          expected -= 20.0;
          break;
      case WheelBehavior::Touchpad:
          if (orientation == Qt::Horizontal) {
              wheel(orientation, QPoint(8, 0), {}, false, true);
              expected -= 8.0;
          } else {
              wheel(orientation, QPoint(0, -6), {}, false, true);
              expected += 6.0;
          }
          break;
      }

      if (orientation == Qt::Horizontal) {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - expected) < kTolerance);

A018 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:227
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - otherBefore) < kTolerance);

A019 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:229
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      } else {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - expected) < kTolerance);

A020 | ScrollbarTest::wheelScrolling | src/checks/scrollbar/input.cpp:230
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - otherBefore) < kTolerance);

A021 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:265
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      endWheel(orientation, behavior == WheelBehavior::Touchpad);
  }

  void ScrollbarTest::keyboardNavigation_data()
  {
      QTest::addColumn<int>("orientationValue");
      QTest::addColumn<int>("behaviorValue");

      QTest::newRow("horizontal-right-single-dip")
          << int(Qt::Horizontal) << int(KeyBehavior::SingleStep);
      QTest::newRow("vertical-down-single-dip") << int(Qt::Vertical) << int(KeyBehavior::SingleStep);
      QTest::newRow("horizontal-home-negative-preroll")
          << int(Qt::Horizontal) << int(KeyBehavior::Home);
      QTest::newRow("vertical-home-minimum") << int(Qt::Vertical) << int(KeyBehavior::Home);
      QTest::newRow("horizontal-end-maximum") << int(Qt::Horizontal) << int(KeyBehavior::End);
      QTest::newRow("vertical-end-maximum") << int(Qt::Vertical) << int(KeyBehavior::End);
  }

  void ScrollbarTest::keyboardNavigation()
  {
      QFETCH(int, orientationValue);
      QFETCH(int, behaviorValue);
      const Qt::Orientation orientation =
          orientationValue == int(Qt::Horizontal) ? Qt::Horizontal : Qt::Vertical;
      const KeyBehavior behavior = KeyBehavior(behaviorValue);
      const songview::TimeCamera &camera = view().camera();
      const double minimum = orientation == Qt::Horizontal ? camera.minHScroll() : 0.0;
      const double maximum =
          orientation == Qt::Horizontal ? camera.maxHScroll() : camera.maxRollScroll();
      const double midpoint = minimum + (maximum - minimum) / 2.0;
      const double otherBefore = orientation == Qt::Horizontal ? camera.scrollY() : camera.scrollX();

      if (orientation == Qt::Horizontal)
Original expression:
  QVERIFY(minimum < 0.0);

A022 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:266
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(maximum - minimum > 2.0);

A023 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:273
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      if (orientation == Qt::Horizontal)
          view().setHScroll(midpoint);
      else
          view().setVScroll(midpoint);

      if (orientation == Qt::Horizontal)
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - midpoint) < kTolerance);

A024 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:275
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      else
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - midpoint) < kTolerance);

A025 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:279
Disposition: NATIVE
Mapping/reason: Original native fixture/window obligation: observes the live QQuickWindow active-focus item, which a presenter-level Swift check cannot establish.
Source context since previous assertion:
      QQuickItem &scrollbar = bar(orientation);
      scrollbar.forceActiveFocus(Qt::TabFocusReason);
Original expression:
  QTRY_VERIFY(window().activeFocusItem() == &scrollbar && scrollbar.hasActiveFocus());

A026 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:300
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      Qt::Key key = Qt::Key_Right;
      double expected = midpoint;
      switch (behavior) {
      case KeyBehavior::SingleStep:
          key = orientation == Qt::Horizontal ? Qt::Key_Right : Qt::Key_Down;
          expected += 1.0;
          break;
      case KeyBehavior::Home:
          key = Qt::Key_Home;
          expected = minimum;
          break;
      case KeyBehavior::End:
          key = Qt::Key_End;
          expected = maximum;
          break;
      }

      QTest::keyClick(&window(), key);
      if (orientation == Qt::Horizontal) {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - expected) < kTolerance);

A027 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:301
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - otherBefore) < kTolerance);

A028 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:303
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
      } else {
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollY() - expected) < kTolerance);

A029 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:304
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(std::abs(camera.scrollX() - otherBefore) < kTolerance);

A030 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:309
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::Home); executes only on Home rows.
Source context since previous assertion:
      if (behavior == KeyBehavior::Home) {
          if (orientation == Qt::Horizontal)
Original expression:
  QTRY_VERIFY(std::abs(thumb(orientation).x()) < kTolerance);

A031 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:311
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::Home); executes only on Home rows.
Source context since previous assertion:
          else
Original expression:
  QTRY_VERIFY(std::abs(thumb(orientation).y()) < kTolerance);

A032 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:312
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::Home); executes only on Home rows.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(withinTrack(orientation));

A033 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:316
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::End); executes only on End rows.
Source context since previous assertion:
      if (behavior == KeyBehavior::End) {
          if (orientation == Qt::Horizontal) {
Original expression:
  QTRY_VERIFY(std::abs(thumb(orientation).x() + thumb(orientation).width() -
                       scrollbar.width()) < kTolerance);

A034 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:319
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::End); executes only on End rows.
Source context since previous assertion:
          } else {
Original expression:
  QTRY_VERIFY(std::abs(thumb(orientation).y() + thumb(orientation).height() -
                       scrollbar.height()) < kTolerance);

A035 | ScrollbarTest::keyboardNavigation | src/checks/scrollbar/input.cpp:322
Disposition: GAP
Mapping/reason: No equivalent predicate in the existing paired Swift case has been established; original predicate and setup below remain an open obligation. Guarded by if (behavior == KeyBehavior::End); executes only on End rows.
Source context since previous assertion:
          }
Original expression:
  QVERIFY(withinTrack(orientation));

Remaining original source (closing sequence and helper bodies):
      }
  }

Swift assertion predicates
No Swift counterpart was identified, so no S entries exist.

Shared helper: none (no Swift fixture files carrying expects were identified).
