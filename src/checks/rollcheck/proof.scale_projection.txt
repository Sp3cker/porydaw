Assertion correspondence: rollcheck/scale_projection.cpp

Reference revision: 996c6446f2cbfaa5b88ed721df4fa831129bd7bc
Original: src/checks/rollcheck/scale_projection.cpp
Original SHA-256: 4dc0fed604392d4a02f9a9f67f8b95fedf68363320ffba60658062c3f437c36d
Swift counterpart: src/checks/rollcheck/static/geometry.swift (checkScaleProjectionInvariants, checkLiveFoldProjection, checkScaleHighlightRasterDocumentGuards)
Additional Swift counterpart: src/checks/rollqml/tst_SwiftRollPlots.qml (mounted Highlight tint probe, test_scaleHighlightRaster)
Additional Swift counterpart: src/checks/rollcheck/scale_editing.swift (Fold/Highlight flag composition, runFoldScaleIntegrationChecks)
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftcore --qt projectSession
Result: green post-refactor controller run; SwiftCoreTest::projectSession 3 passed, 0 failed.
Note: Task 42 evidence supplied to this ledger pass (not re-run here): the implementer reported the swiftcore projectSession lane green with the production-fold rows and verify:qml-roll swiftroll-window green with the raster probe; the controller's shell-transport fix (tst_ShellTransport.qml:503/534 fold.Accessible.checked) passed. This ledger pass ran only `deno task proof check`.

A001 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:46
Disposition: MATCHED
Mapping: S001

A002 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:52
Disposition: MATCHED
Mapping: S002

A003 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:57
Disposition: MATCHED
Mapping: S003

A004 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:74
Disposition: MATCHED
Mapping/reason: S018 counts the live session's Fold rows against the selected track's distinct occupied pitches with production Fold enabled, so the law is observed on the production binding rather than a constructed projection.
Source context since previous assertion:
  ;
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);

          // A3. Fold geometry is the selected track's exact occupied-pitch
          // set, strictly descending and unique.
          bool initialOccupancy[128] = {};
          int occupiedPitchCount = 0;
          for (const DocNote &n : doc.notesForTrack(scaleTrack)) {
              if (!initialOccupancy[n.key]) {
                  initialOccupancy[n.key] = true;
                  occupiedPitchCount++;
              }
          }
          view.setScaleHighlight(false);
          view.setScaleFold(true);
          if (proj.visibleRowCount() != occupiedPitchCount)
Original expression:
  QFAIL("Fold row count does not match selected-track occupancy")

A005 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:79
Disposition: MATCHED
Mapping: S005

A006 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:86
Disposition: MATCHED
Mapping/reason: S019 compares the live Fold projection's per-pitch visibility with the selected-track occupancy for all 128 pitches.
Source context since previous assertion:
  ;
                  break;
              }
              prevPitch = p;
          }
          for (int p = 0; p < 128; p++) {
              if ((proj.rowForPitch(p) != projHidden) != initialOccupancy[p]) {
Original expression:
  QFAIL("Fold visible pitches do not exactly match selected-track occupancy")

A007 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:94
Disposition: MATCHED
Mapping: S007

A008 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:101
Disposition: MATCHED
Mapping: S008

A009 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:120
Disposition: RETIRED-REPRESENTATION
Mapping/reason: fixture-precondition branch: the native occupancy probe searched for a free C-sharp-class octave pair before inserting; the Swift probe runs the same search but reports a fixture failure rather than asserting, so the guard has no counterpart assertion.
Source context since previous assertion:
  ;
                  break;
              }
          }

          // A5-A7. Occupancy is exact per MIDI pitch; adding a note exposes
          // only that pitch, while every unused pitch stays hidden.
          bool noteOnSelected[128] = {};
          for (const DocNote &n : doc.notesForTrack(scaleTrack))
              noteOnSelected[n.key] = true;
          const Tick aTick = check.timeline().lengthTicks + doc.ticksPerClock() * 8;
          int exceptBase = -1;
          for (int k = 1; k + 12 < 128; k += 12) { // C#/Db classes
              if (!noteOnSelected[k] && !noteOnSelected[k + 12]) {
                  exceptBase = k;
                  break;
              }
          }
          if (exceptBase < 0) {
Original expression:
  QFAIL("no free C#-class octave pair for the Fold occupancy exception")

A010 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:125
Disposition: MATCHED
Mapping/reason: S020 adds the unoccupied off-scale note and asserts the live Fold projection exposes its row.
Source context since previous assertion:
  ;
          } else {
              const int cmd0 = doc.undoStack()->index();
              doc.addNote(scaleTrack, aTick, uint8_t(exceptBase), doc.ticksPerClock(), 100);
              if (proj.rowForPitch(exceptBase) == projHidden)
Original expression:
  QFAIL("Fold did not expose an occupied off-scale pitch")

A011 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:127
Disposition: MATCHED
Mapping/reason: S021 asserts the added note's unused off-scale octave stays hidden in the live Fold projection.
Source context since previous assertion:
  ;
              if (proj.rowForPitch(exceptBase + 12) != projHidden)
Original expression:
  QFAIL("Fold exposed an unoccupied off-scale octave")

A012 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:138
Disposition: MATCHED
Mapping/reason: S022 asserts every unused off-scale pitch stays hidden in the live Fold projection.
Source context since previous assertion:
  ;
              int freePitch = -1;
              for (int p = 1; p < 128; p += 12) {
                  if (porydaw_scale::isScalePitch(scaleMajor, 0, p))
                      continue;
                  if (!noteOnSelected[p] && proj.rowForPitch(p) == projHidden) {
                      freePitch = p;
                      break;
                  }
              }
              if (freePitch >= 0 && proj.rowForPitch(freePitch) != projHidden)
Original expression:
  QFAIL("Fold revealed an off-scale unused pitch")

A013 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:150
Disposition: MATCHED
Mapping: S012

A014 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:163
Disposition: MATCHED
Mapping: S013

A015 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:171
Disposition: MATCHED
Mapping/reason: S014 undoes the occupancy probe's insertion and asserts the document history identity is restored.
Source context since previous assertion:
  ;
          }

          view.setScaleHighlight(false);
          view.setScaleFold(false);
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);
      }
Original expression:
  QCOMPARE(doc.undoStack()->index(), undo)

A016 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:172
Disposition: MATCHED
Mapping/reason: S015 asserts the same probe restores serialized MIDI after the undo.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(doc.smf().write(), before)

A017 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:235
Disposition: PARTIAL
Mapping/reason: Related predicates S025 and S026 execute the mounted raster observations (the scale row carries the fork composite, the accidental row is untouched); unproved: the probe's fixture precondition - the mounted lane derives its probe rows from camera geometry instead of searching for an octave whose C, C-sharp and D are unused, so no predicate asserts this guard.
Source context since previous assertion:
  ;
  }

  void PianoRollTest::scaleHighlightRaster()
  {
      PianoRollFixture &check = *m_fixture;
      SongDocument &doc = check.document();
      SongView &view = check.view();
      songview::TimelineInputItem *roll = &check.rollInput();
      const int pianoKeyboardWidth = check.pianoKeyboardWidth();
      const int pianoRollDefaultKeyHeight = check.pianoRollDefaultKeyHeight();
      const SnappedRows rows{view, *roll};
      const auto scaleMajor = porydaw_scale::ScaleId::major;
      const auto projHidden = songview::PitchProjection::cHiddenRow;
      const int scaleTrack = view.selectionModel().primaryTrack();
      const auto foldCenterY = [&](int pitch) -> int {
          const int row = view.pitchProjection().rowForPitch(pitch);
          if (row == songview::PitchProjection::cHiddenRow)
              return -1;
          const qreal dpr = roll->devicePixelRatio();
          const qreal top =
              std::round((double(row) * view.camera().keyHeight() - view.camera().scrollY()) * dpr) /
              dpr;
          const qreal bottom =
              std::round((double(row + 1) * view.camera().keyHeight() - view.camera().scrollY()) *
                         dpr) /
              dpr;
          return int(std::floor((top + bottom) / 2.0));
      };
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();

      // Block B — Highlight pixel checks (Wave 5): the tint lands on scale
      // rows only, and never on the keyboard or a painted note face.
      {
          const auto pixelAt = [](const QImage &image, int x, int y) -> QRgb {
              const qreal dpr = image.devicePixelRatio();
              return image.pixel(qRound(x * dpr), qRound(y * dpr));
          };
          const int plotLaneX = qRound(view.camera().leadPadPx()) + 20; // off the gridlines
          const int bandLaneX = pianoKeyboardWidth + plotLaneX;
          const int kbdX = pianoKeyboardWidth - 10;

          view.setScaleHighlight(false);
          view.setScaleFold(false);
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);

          bool noteOnSelected[128] = {};
          for (const DocNote &n : doc.notesForTrack(scaleTrack))
              noteOnSelected[n.key] = true;

          // Pick an octave whose C, C# and D are all empty on the selected
          // scaleTrack, then center its rows in the viewport.
          int rootKey = -1, scaleKey = -1, nonScaleKey = -1;
          for (int base = 0; base + 2 <= 127 && rootKey < 0; base += 12) {
              if (!noteOnSelected[base] && !noteOnSelected[base + 1] && !noteOnSelected[base + 2]) {
                  rootKey = base;
                  scaleKey = base + 2;
                  nonScaleKey = base + 1;
              }
          }
          if (rootKey < 0) {
Original expression:
  QFAIL("no empty octave for the Highlight pixel probe")

A018 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:261
Disposition: MATCHED
Mapping/reason: S025 compares the highlighted scale row against the alpha=51/255 fork composite of that row's own un-highlighted pixels.
Source context since previous assertion:
  ;
          } else {
              const double kh = pianoRollDefaultKeyHeight;
              const double scrollY =
                  (127 - nonScaleKey) * kh + kh / 2.0 - double(roll->height()) / 2.0;
              SongView::ViewState st = view.viewState();
              st.valid = true;
              st.keyHeight = kh;
              st.scrollY = scrollY;
              view.applyViewState(st);
              checks::support::pumpQuick();

              const QImage offPm = check.captureQuickFramebuffer();
              const QRgb offScale = pixelAt(offPm, bandLaneX, rows.centerY(scaleKey));
              const QRgb offNon = pixelAt(offPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb offKbd = pixelAt(offPm, kbdX, rows.centerY(scaleKey));

              view.setScaleHighlight(true);
              view.setScaleFold(false);
              const QImage hlPm = check.captureQuickFramebuffer();
              const QRgb hlScale = pixelAt(hlPm, bandLaneX, rows.centerY(scaleKey));
              const QRgb hlNon = pixelAt(hlPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb hlKbd = pixelAt(hlPm, kbdX, rows.centerY(scaleKey));
              const QRgb hlRoot = pixelAt(hlPm, bandLaneX, rows.centerY(rootKey));

              if (hlScale == offScale)
Original expression:
  QFAIL("Highlight did not tint the scale row")

A019 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:271
Disposition: MATCHED
Mapping/reason: S025 builds the expectation as the #b595fc blend (channels 0xb5/0x95/0xfc) at alpha 51/255, and S024 asserts the palette alpha itself is 51/255.
Source context since previous assertion:
  ;
              const auto blendedChannel = [](int background, int source) {
                  return (source * 51 + background * 204 + 127) / 255;
              };
              const QRgb expectedTint =
                  qRgb(blendedChannel(qRed(offScale), 0xb5), blendedChannel(qGreen(offScale), 0x95),
                       blendedChannel(qBlue(offScale), 0xfc));
              if (std::abs(qRed(hlScale) - qRed(expectedTint)) > 1 ||
                  std::abs(qGreen(hlScale) - qGreen(expectedTint)) > 1 ||
                  std::abs(qBlue(hlScale) - qBlue(expectedTint)) > 1) {
Original expression:
  QFAIL("Highlight tint is not #b595fc at 20% opacity")

A020 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:274
Disposition: MATCHED
Mapping/reason: S026 asserts the accidental (non-scale) row's pixels are unchanged by Highlight.
Source context since previous assertion:
  ;
              }
              if (hlNon != offNon)
Original expression:
  QFAIL("Highlight tinted a non-scale row")

A021 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:276
Disposition: MATCHED
Mapping/reason: S027 asserts the keyboard column's pixels are unchanged by Highlight.
Source context since previous assertion:
  ;
              if (hlKbd != offKbd)
Original expression:
  QFAIL("Highlight repainted the keyboard column")

A022 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:278
Disposition: MATCHED
Mapping/reason: S028 asserts two distinct scale degrees (the C root row and the D row) receive the identical composite, so no degree is emphasized.
Source context since previous assertion:
  ;
              if (hlRoot != hlScale)
Original expression:
  QFAIL("Highlight emphasized the root degree differently")

A023 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:289
Disposition: MATCHED
Mapping/reason: S029 asserts the former accidental row carries the composite after the root moves and the other probe row loses it.
Source context since previous assertion:
  ;

              view.setScaleRoot(1); // C# Major: the former non-scale C# row becomes the root.
              const QImage shiftedPm = check.captureQuickFramebuffer();
              const QRgb shiftedRoot = pixelAt(shiftedPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb expectedShiftedTint =
                  qRgb(blendedChannel(qRed(offNon), 0xb5), blendedChannel(qGreen(offNon), 0x95),
                       blendedChannel(qBlue(offNon), 0xfc));
              if (std::abs(qRed(shiftedRoot) - qRed(expectedShiftedTint)) > 1 ||
                  std::abs(qGreen(shiftedRoot) - qGreen(expectedShiftedTint)) > 1 ||
                  std::abs(qBlue(shiftedRoot) - qBlue(expectedShiftedTint)) > 1) {
Original expression:
  QFAIL("changing the scale root did not update Highlight lanes")

A024 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:299
Disposition: MATCHED
Mapping/reason: S030 asserts changing the scale type moves which row carries the composite.
Source context since previous assertion:
  ;
              }
              view.setScaleRoot(0);
              view.setScaleId(porydaw_scale::ScaleId::phrygian);
              const QImage changedScalePm = check.captureQuickFramebuffer();
              const QRgb changedScalePitch =
                  pixelAt(changedScalePm, bandLaneX, rows.centerY(nonScaleKey));
              if (std::abs(qRed(changedScalePitch) - qRed(expectedShiftedTint)) > 1 ||
                  std::abs(qGreen(changedScalePitch) - qGreen(expectedShiftedTint)) > 1 ||
                  std::abs(qBlue(changedScalePitch) - qBlue(expectedShiftedTint)) > 1) {
Original expression:
  QFAIL("changing the scale type did not update Highlight lanes")

A025 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:313
Disposition: PARTIAL
Mapping/reason: Related predicate S019 asserts the production Fold projection's per-pitch visibility equals occupancy, so an occupied pitch's row is visible; unproved: the tint probe's own folded fixture - the mounted lane never asserts the occupied Fold row exists before probing its pixel.
Source context since previous assertion:
  ;
              }
              view.setScaleId(scaleMajor);

              // Highlight and Fold compose: a visible, occupied scale row keeps
              // Fold's projection and receives the same Highlight tint.
              const int cmd0 = doc.undoStack()->index();
              constexpr int foldTintKey = 127; // G9 in C Major; always the top Fold row.
              const Tick tintTick = check.timeline().lengthTicks + doc.ticksPerClock() * 8;
              doc.addNote(scaleTrack, tintTick, uint8_t(foldTintKey), doc.ticksPerClock(), 100);
              view.setScaleHighlight(false);
              view.setScaleFold(true);
              const int foldRow = view.pitchProjection().rowForPitch(foldTintKey);
              if (foldRow == projHidden) {
Original expression:
  QFAIL("Fold hid the occupied scale row for the Highlight tint probe")

A026 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:323
Disposition: PARTIAL
Mapping/reason: Related predicate S031 requires the folded row's center to be a projectable pixel of the passing tint lane; unproved: no predicate asserts the scrolled Fold row lies inside the roll viewport (the original guard).
Source context since previous assertion:
  ;
              } else {
                  SongView::ViewState folded = view.viewState();
                  folded.valid = true;
                  folded.keyHeight = kh;
                  folded.scrollY = std::max(0.0, double(foldRow) * kh + kh / 2.0 - 100.0);
                  view.applyViewState(folded);
                  checks::support::pumpQuick();
                  const int foldY = foldCenterY(foldTintKey);
                  if (foldY < 0 || foldY >= roll->height()) {
Original expression:
  QFAIL("Fold did not make the occupied scale row visible for the Highlight tint "
                            "probe")

A027 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:330
Disposition: MATCHED
Mapping/reason: S033 asserts Fold and Highlight are both set on the live projection, so enabling Highlight does not clear Fold.
Source context since previous assertion:
  ;
                  } else {
                      const QImage foldOffPm = check.captureQuickFramebuffer();
                      const QRgb foldOff = pixelAt(foldOffPm, bandLaneX, foldY);
                      view.setScaleHighlight(true);
                      if (!view.scaleHighlight() || !view.scaleFold())
Original expression:
  QFAIL("enabling Highlight disabled Fold")

A028 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:334
Disposition: MATCHED
Mapping/reason: S031 asserts the tint lands on a visible occupied Fold row at the fork composite.
Source context since previous assertion:
  ;
                      const QImage foldHighlightPm = check.captureQuickFramebuffer();
                      const QRgb foldHighlight = pixelAt(foldHighlightPm, bandLaneX, foldY);
                      if (foldHighlight == foldOff)
Original expression:
  QFAIL("Highlight did not tint a visible occupied Fold scale row")

A029 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:341
Disposition: MATCHED
Mapping/reason: S031 compares the Fold row's pixels against the alpha=51/255 fork composite, and S024 asserts the palette alpha is 51/255.
Source context since previous assertion:
  ;
                      const QRgb expectedFoldTint = qRgb(blendedChannel(qRed(foldOff), 0xb5),
                                                         blendedChannel(qGreen(foldOff), 0x95),
                                                         blendedChannel(qBlue(foldOff), 0xfc));
                      if (std::abs(qRed(foldHighlight) - qRed(expectedFoldTint)) > 1 ||
                          std::abs(qGreen(foldHighlight) - qGreen(expectedFoldTint)) > 1 ||
                          std::abs(qBlue(foldHighlight) - qBlue(expectedFoldTint)) > 1) {
Original expression:
  QFAIL("Highlight tint on a Fold row is not #b595fc at 20% opacity")

A030 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:370
Disposition: MATCHED
Mapping/reason: S032 asserts the painted note face's pixels are unchanged by Highlight.
Source context since previous assertion:
  ;
                      }
                  }
              }
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              while (doc.undoStack()->index() > cmd0)
                  doc.undoStack()->undo();
          }

          // Note face is untouched: paint the same note in Off and Highlight
          // and compare the pixels inside its rect.
          if (scaleKey >= 0) {
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              view.ensureKeyVisible(scaleKey);
              checks::support::pumpQuick();
              const QPoint notePos(plotLaneX, rows.centerY(scaleKey));
              const int cmd0 = doc.undoStack()->index();
              drawNote(*roll, notePos); // commits a C-major-scale note (empty row)
              const QImage offPm = check.captureQuickFramebuffer();
              const QRgb offFace = pixelAt(offPm, bandLaneX, notePos.y());
              view.setScaleHighlight(true);
              view.setScaleFold(false);
              const QImage hlPm = check.captureQuickFramebuffer();
              const QRgb hlFace = pixelAt(hlPm, bandLaneX, notePos.y());
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              if (hlFace != offFace)
Original expression:
  QFAIL("Highlight changed the painted note face")

A031 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:378
Disposition: MATCHED
Mapping/reason: S023 asserts the Highlight view passes leave document history and bytes unchanged; S016 asserts the highlight probe's note insertion is undone and history restored.
Source context since previous assertion:
  ;
              while (doc.undoStack()->index() > cmd0)
                  doc.undoStack()->undo();
              view.selectionModel().clearNoteSelection();
          }
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);
      }
Original expression:
  QCOMPARE(doc.undoStack()->index(), undo)

A032 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:379
Disposition: MATCHED
Mapping/reason: S023 covers the view passes' bytes; S017 asserts the probe restores serialized MIDI after the undo.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(doc.smf().write(), before)

Remaining original source (closing sequence and helper bodies):
  ;
  }

Swift assertion predicates

S001 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A001 Off projects all 128 pitches"

S002 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A002 Off hides no pitch"

S003 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A003 Off remains chromatic after root and type change"

S004 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A004 folded row count equals distinct selected-track pitches"

S005 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A005 folded rows descend strictly"

S006 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A006 folded visible pitches exactly equal selected-track occupancy"

S007 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A007 each visible folded row maps back to itself"

S008 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A008 each visible folded pitch maps back to itself"

S009 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A010 occupied off-scale C-sharp becomes visible"

S010 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A011 unused off-scale octave remains hidden"

S011 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A012 unused off-scale pitch remains hidden"

S012 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A013 equal-distance nearest pitch chooses lower pitch"

S013 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A014 folded scroll remains inside its projected height"

S014 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A015 undo restores the pre-probe document history"

S015 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift
Anchor: message "A016 undo restores serialized MIDI"

S016 | checkScaleHighlightRasterDocumentGuards | src/checks/rollcheck/static/geometry.swift
Anchor: message "A031 highlight note probe restores document history"

S017 | checkScaleHighlightRasterDocumentGuards | src/checks/rollcheck/static/geometry.swift
Anchor: message "A032 highlight note probe restores serialized MIDI"

S018 | checkLiveFoldProjection | src/checks/rollcheck/static/geometry.swift
Anchor: message "production fold row count equals selected-track occupancy"

S019 | checkLiveFoldProjection | src/checks/rollcheck/static/geometry.swift
Anchor: message "production fold visibility exactly matches occupancy"

S020 | checkLiveFoldProjection | src/checks/rollcheck/static/geometry.swift
Anchor: message "production fold exposes an occupied off-scale pitch after add"

S021 | checkLiveFoldProjection | src/checks/rollcheck/static/geometry.swift
Anchor: message "production fold keeps the unused off-scale octave hidden"

S022 | checkLiveFoldProjection | src/checks/rollcheck/static/geometry.swift
Anchor: message "production fold hides every unused off-scale pitch"

S023 | checkScaleHighlightRasterDocumentGuards | src/checks/rollcheck/static/geometry.swift
Anchor: message "highlight view passes leave history and bytes unchanged"

S024 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight retains the fork's translucent tint"

S025 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight tints the scale row at the fork composite"

S026 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight leaves the non-scale row untouched"

S027 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight leaves the keyboard column untouched"

S028 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight tints every scale degree identically"

S029 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "changing the scale root moves the Highlight lane"

S030 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "changing the scale type moves the Highlight lane"

S031 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight tints a visible occupied Fold row"

S032 | SwiftRollPlots::test_scaleHighlightRaster | src/checks/rollqml/tst_SwiftRollPlots.qml
Anchor: message "Highlight leaves the painted note face unchanged"

S033 | runFoldScaleIntegrationChecks | src/checks/rollcheck/scale_editing.swift
Anchor: message "track selection keeps per-tab Fold and Highlight settings"
