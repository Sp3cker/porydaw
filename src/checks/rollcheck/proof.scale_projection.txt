Assertion correspondence: rollcheck/scale_projection.cpp

Reference revision: 996c6446f2cbfaa5b88ed721df4fa831129bd7bc
Original: src/checks/rollcheck/scale_projection.cpp
Original SHA-256: 4dc0fed604392d4a02f9a9f67f8b95fedf68363320ffba60658062c3f437c36d
Swift counterpart: src/checks/rollcheck/static/geometry.swift
Swift SHA-256: 9e7a6c1d3dec6fb682d93d17aaa4f0d3e61cc001116f9f37b96b488e8c635259

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below. Source context preserves setup, rows,
loops, and guards between assertions verbatim. MATCHED identifies an equivalent
Swift predicate executed green; PARTIAL a related predicate with an explicit
unproved condition; NATIVE a retained native-window/rendering/input or harness
obligation a presenter-level Swift check cannot establish; GAP an original
predicate without an equivalent executed replacement. The geometry.swift
predicates S001-S017 ran green in projectSession: seven model sites are MATCHED
and ten sites remain PARTIAL because their counterpart is only related. S003 does
not exercise a root/scale transition because Swift projection has no root
or scale parameter. Folded checks construct a projection from selected-track
notes instead of switching production Fold; that binding remains unproved.
S016-S017 check isolated note insertion and undo, not the native Highlight
sequence or its raster observations. None of these labels authorizes
deletion or claims whole-case equivalence.
Block A audits the model row-set/inverse/nearest/scroll-bound sites and the
undo-index/smf guards. Block B tint/lane/compose/face sites read rendered
pixels and remain NATIVE. Model predicates in geometry.swift are not native
SongView window proof.

Verification
Command: deno task verify --filter swiftcore --qt projectSession
Result: green post-refactor controller run; SwiftCoreTest::projectSession 3 passed, 0 failed.

Execution and ownership
runGeometryChecks ran green in projectSession. The original slots run under
PianoRollTest (checks/rollcheck/tst_pianoroll.h):
scaleProjectionInvariants and scaleHighlightRaster, on the per-slot PianoRollFixture
(checks/rollcheck/rollcheck.h, m_fixture) with the SnappedRows sampler, Quick
pumping and framebuffer capture (checks/support/quickframebuffer.h), and
file-local foldCenterY/pixelAt lambdas.

Fixture, sequence and row constraints
Block A loops over all 128 pitches and all visible rows (Off chromatic row
count, Fold occupancy/descending/inverse maps, nearestVisiblePitch tie on a
{60, 62} projection, folded scroll bound) with an undo-index plus smf guard.
Block B probes one empty C/C#/D octave centered in the viewport, asserts the
20% #b595fc blend within +-1 per channel, re-checks lanes after root and scale
type changes, composes Highlight over a visible occupied Fold row (key 127),
compares note-face pixels, and closes with the same undo-index plus smf guard.

Original assertion inventory and source sequence

A001 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:46
Disposition: MATCHED
Mapping/reason: Executed S001 in geometry.swift confirms the chromatic PitchProjection exposes exactly 128 rows; projectSession green.
Source context since previous assertion:
  #include "checks/rollcheck/tst_pianoroll.h"

  #include "checks/rollcheck/rollcheck.h"

  #include <QByteArray>
  #include <QCoreApplication>
  #include <QEvent>
  #include <QImage>
  #include <QPoint>
  #include <QtTest>
  #include <algorithm>
  #include <cmath>
  #include <cstdint>
  #include <vector>

  #include "checks/support/quickframebuffer.h"
  #include "core/songdocument.h"
  #include "porydaw_scale.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/timelineinputitem.h"

  using namespace checks::rollcheck;

  void PianoRollTest::scaleProjectionInvariants()
  {
      PianoRollFixture &check = *m_fixture;
      SongDocument &doc = check.document();
      SongView &view = check.view();
      songview::TimelineInputItem *roll = &check.rollInput();
      const auto scaleMajor = porydaw_scale::ScaleId::major;
      const auto projHidden = songview::PitchProjection::cHiddenRow;
      const int scaleTrack = view.selectionModel().primaryTrack();
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();

      // Block A — Projection invariants (Wave 3): pure queries, no gestures.
      {
          const auto &proj = view.pitchProjection();
          view.setScaleHighlight(false);
          view.setScaleFold(false);
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);

          // A1. Off is full chromatic whatever the root/scale.
          if (proj.visibleRowCount() != 128)
Original expression:
  QFAIL("Off projection does not expose 128 rows")

A002 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:52
Disposition: MATCHED
Mapping/reason: Executed S002 in geometry.swift confirms every MIDI pitch 0-127 has a chromatic visible row; projectSession green.
Source context since previous assertion:
  ;
          bool anyHidden = false;
          for (int p = 0; p < 128; p++)
              if (proj.rowForPitch(p) == projHidden)
                  anyHidden = true;
          if (anyHidden)
Original expression:
  QFAIL("Off projection hides a pitch")

A003 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:57
Disposition: PARTIAL
Mapping/reason: Executed related S003 in geometry.swift constructs another chromatic projection, but cannot perform the original root/scale transition; that condition remains unproved after the green run. Feature not yet ported: Swift roll scale projection UI (no production Fold view accepts a root/scale transition).
Source context since previous assertion:
  ;
          // A2. Drift the root/scale; Off must stay chromatic.
          view.setScaleRoot(2);
          view.setScaleId(porydaw_scale::ScaleId::dorian);
          if (proj.visibleRowCount() != 128)
Original expression:
  QFAIL("Off mode root/scale change altered the row count")

A004 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:74
Disposition: PARTIAL
Mapping/reason: Executed related S004 in geometry.swift counts explicitly constructed selected-track PitchProjection rows, but cannot observe production Fold binding; that condition remains unproved. Feature not yet ported: Swift roll scale projection UI (no production Fold binding exists).
Source context since previous assertion:
  ;
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);

          // A3. Fold geometry is the selected track's exact occupied-pitch
          // set, strictly descending and unique.
          bool initialOccupancy[128] = {};
          int occupiedPitchCount = 0;
          for (const DocNote &n : doc.notesForTrack(scaleTrack)) {
              if (!initialOccupancy[n.key]) {
                  initialOccupancy[n.key] = true;
                  occupiedPitchCount++;
              }
          }
          view.setScaleHighlight(false);
          view.setScaleFold(true);
          if (proj.visibleRowCount() != occupiedPitchCount)
Original expression:
  QFAIL("Fold row count does not match selected-track occupancy")

A005 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:79
Disposition: MATCHED
Mapping/reason: Executed S005 in geometry.swift checks every folded PitchProjection row is strictly lower-pitched than its predecessor; projectSession green.
Source context since previous assertion:
  ;
          int prevPitch = 128; // above the highest MIDI pitch; rows descend
          for (int r = 0; r < proj.visibleRowCount(); r++) {
              const int p = proj.visiblePitchAt(r);
              if (p >= prevPitch) { // row 0 is the HIGHEST pitch: descending
Original expression:
  QFAIL("Fold visible pitches are not strictly descending")

A006 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:86
Disposition: PARTIAL
Mapping/reason: Executed related S006 in geometry.swift compares constructed projection visibility with selected-track occupancy, but production Fold binding remains unproved. Feature not yet ported: Swift roll scale projection UI (no production Fold binding exists).
Source context since previous assertion:
  ;
                  break;
              }
              prevPitch = p;
          }
          for (int p = 0; p < 128; p++) {
              if ((proj.rowForPitch(p) != projHidden) != initialOccupancy[p]) {
Original expression:
  QFAIL("Fold visible pitches do not exactly match selected-track occupancy")

A007 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:94
Disposition: MATCHED
Mapping/reason: Executed S007 in geometry.swift verifies each folded visible row maps to a pitch that maps back to that row; projectSession green.
Source context since previous assertion:
  ;
                  break;
              }
          }

          // A4. Inverses: the row<->pitch maps round-trip.
          for (int r = 0; r < proj.visibleRowCount(); r++) {
              if (proj.rowForPitch(proj.visiblePitchAt(r)) != r) {
Original expression:
  QFAIL("Fold visible row does not map back to itself")

A008 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:101
Disposition: MATCHED
Mapping/reason: Executed S008 in geometry.swift verifies every non-hidden folded pitch maps to a row that maps back to that pitch; projectSession green.
Source context since previous assertion:
  ;
                  break;
              }
          }
          for (int p = 0; p < 128; p++) {
              const int r = proj.rowForPitch(p);
              if (r != projHidden && proj.visiblePitchAt(r) != p) {
Original expression:
  QFAIL("Fold visible pitch does not map back to itself")

A009 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:120
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
                  break;
              }
          }

          // A5-A7. Occupancy is exact per MIDI pitch; adding a note exposes
          // only that pitch, while every unused pitch stays hidden.
          bool noteOnSelected[128] = {};
          for (const DocNote &n : doc.notesForTrack(scaleTrack))
              noteOnSelected[n.key] = true;
          const Tick aTick = check.timeline().lengthTicks + doc.ticksPerClock() * 8;
          int exceptBase = -1;
          for (int k = 1; k + 12 < 128; k += 12) { // C#/Db classes
              if (!noteOnSelected[k] && !noteOnSelected[k + 12]) {
                  exceptBase = k;
                  break;
              }
          }
          if (exceptBase < 0) {
Original expression:
  QFAIL("no free C#-class octave pair for the Fold occupancy exception")

A010 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:125
Disposition: PARTIAL
Mapping/reason: Executed related S009 in geometry.swift reconstructs PitchProjection after isolated note insertion, but cannot observe a Fold-bound view update; that condition remains unproved. Feature not yet ported: Swift roll scale projection UI (no Fold-bound production view exists).
Source context since previous assertion:
  ;
          } else {
              const int cmd0 = doc.undoStack()->index();
              doc.addNote(scaleTrack, aTick, uint8_t(exceptBase), doc.ticksPerClock(), 100);
              if (proj.rowForPitch(exceptBase) == projHidden)
Original expression:
  QFAIL("Fold did not expose an occupied off-scale pitch")

A011 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:127
Disposition: PARTIAL
Mapping/reason: Executed related S010 in geometry.swift keeps an unused octave hidden in a reconstructed projection, but the original Fold-bound view observation remains unproved. Feature not yet ported: Swift roll scale projection UI (no Fold-bound production view exists).
Source context since previous assertion:
  ;
              if (proj.rowForPitch(exceptBase + 12) != projHidden)
Original expression:
  QFAIL("Fold exposed an unoccupied off-scale octave")

A012 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:138
Disposition: PARTIAL
Mapping/reason: Executed related S011 in geometry.swift hides an unused C-sharp-class pitch in a reconstructed projection; the C-sharp-class membership sub-condition is now covered by the Swift PorydawCore ScaleID membership predicates (src/checks/editcheck/ScaleChecks.swift membershipAndNeighbors rows under cppID scalecheck/ScaleCheckTest::membershipAndNeighbors), while the Fold-bound view observation remains unproved. Feature not yet ported: Swift roll scale projection UI.
Source context since previous assertion:
  ;
              int freePitch = -1;
              for (int p = 1; p < 128; p += 12) {
                  if (porydaw_scale::isScalePitch(scaleMajor, 0, p))
                      continue;
                  if (!noteOnSelected[p] && proj.rowForPitch(p) == projHidden) {
                      freePitch = p;
                      break;
                  }
              }
              if (freePitch >= 0 && proj.rowForPitch(freePitch) != projHidden)
Original expression:
  QFAIL("Fold revealed an off-scale unused pitch")

A013 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:150
Disposition: MATCHED
Mapping/reason: Executed S012 in geometry.swift verifies nearestVisiblePitch(61) chooses 60 on the visible {60,62} pitch subset; projectSession green.
Source context since previous assertion:
  ;
              while (doc.undoStack()->index() > cmd0)
                  doc.undoStack()->undo();
          }

          // A8. nearestVisiblePitch uses the lower pitch on an equal-distance
          // tie, independent of the current track's occupied set.
          {
              songview::PitchProjection tieProjection;
              const uint8_t tiePitches[] = {60, 62};
              tieProjection.buildFromPitches(tiePitches);
              if (tieProjection.nearestVisiblePitch(61) != 60)
Original expression:
  QFAIL("nearestVisiblePitch tie did not pick the lower pitch")

A014 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:163
Disposition: MATCHED
Mapping/reason: Executed S013 in geometry.swift verifies an extreme EditorCamera vertical scroll clamps within folded projection height and viewport bounds; projectSession green.
Source context since previous assertion:
  ;
          }

          // A9. Fold vertical scroll is bounded by the folded content height.
          {
              SongView::ViewState big = view.viewState();
              big.valid = true;
              big.scrollY = 1.0e9;
              view.applyViewState(big);
              double maxScroll = proj.totalHeight(view.camera().keyHeight()) - double(roll->height());
              if (maxScroll < 0.0)
                  maxScroll = 0.0;
              if (view.camera().scrollY() < -1e-9 || view.camera().scrollY() > maxScroll + 1e-9)
Original expression:
  QFAIL("Fold scroll Y is not bounded by the folded content height")

A015 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:171
Disposition: PARTIAL
Mapping/reason: Executed related S014 in geometry.swift restores cloned document identity after undo, but the original Fold-bound view's undo sequence remains unproved. Feature not yet ported: Swift roll scale projection UI (no Fold-bound production view has an undo sequence).
Source context since previous assertion:
  ;
          }

          view.setScaleHighlight(false);
          view.setScaleFold(false);
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);
      }
Original expression:
  QCOMPARE(doc.undoStack()->index(), undo)

A016 | PianoRollTest::scaleProjectionInvariants | src/checks/rollcheck/scale_projection.cpp:172
Disposition: PARTIAL
Mapping/reason: Executed related S015 in geometry.swift restores cloned MIDI bytes after undo, but the original Fold-bound view's undo sequence remains unproved. Feature not yet ported: Swift roll scale projection UI (no Fold-bound production view has an undo sequence).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(doc.smf().write(), before)

A017 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:235
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
  }

  void PianoRollTest::scaleHighlightRaster()
  {
      PianoRollFixture &check = *m_fixture;
      SongDocument &doc = check.document();
      SongView &view = check.view();
      songview::TimelineInputItem *roll = &check.rollInput();
      const int pianoKeyboardWidth = check.pianoKeyboardWidth();
      const int pianoRollDefaultKeyHeight = check.pianoRollDefaultKeyHeight();
      const SnappedRows rows{view, *roll};
      const auto scaleMajor = porydaw_scale::ScaleId::major;
      const auto projHidden = songview::PitchProjection::cHiddenRow;
      const int scaleTrack = view.selectionModel().primaryTrack();
      const auto foldCenterY = [&](int pitch) -> int {
          const int row = view.pitchProjection().rowForPitch(pitch);
          if (row == songview::PitchProjection::cHiddenRow)
              return -1;
          const qreal dpr = roll->devicePixelRatio();
          const qreal top =
              std::round((double(row) * view.camera().keyHeight() - view.camera().scrollY()) * dpr) /
              dpr;
          const qreal bottom =
              std::round((double(row + 1) * view.camera().keyHeight() - view.camera().scrollY()) *
                         dpr) /
              dpr;
          return int(std::floor((top + bottom) / 2.0));
      };
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();

      // Block B — Highlight pixel checks (Wave 5): the tint lands on scale
      // rows only, and never on the keyboard or a painted note face.
      {
          const auto pixelAt = [](const QImage &image, int x, int y) -> QRgb {
              const qreal dpr = image.devicePixelRatio();
              return image.pixel(qRound(x * dpr), qRound(y * dpr));
          };
          const int plotLaneX = qRound(view.camera().leadPadPx()) + 20; // off the gridlines
          const int bandLaneX = pianoKeyboardWidth + plotLaneX;
          const int kbdX = pianoKeyboardWidth - 10;

          view.setScaleHighlight(false);
          view.setScaleFold(false);
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);

          bool noteOnSelected[128] = {};
          for (const DocNote &n : doc.notesForTrack(scaleTrack))
              noteOnSelected[n.key] = true;

          // Pick an octave whose C, C# and D are all empty on the selected
          // scaleTrack, then center its rows in the viewport.
          int rootKey = -1, scaleKey = -1, nonScaleKey = -1;
          for (int base = 0; base + 2 <= 127 && rootKey < 0; base += 12) {
              if (!noteOnSelected[base] && !noteOnSelected[base + 1] && !noteOnSelected[base + 2]) {
                  rootKey = base;
                  scaleKey = base + 2;
                  nonScaleKey = base + 1;
              }
          }
          if (rootKey < 0) {
Original expression:
  QFAIL("no empty octave for the Highlight pixel probe")

A018 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:261
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
          } else {
              const double kh = pianoRollDefaultKeyHeight;
              const double scrollY =
                  (127 - nonScaleKey) * kh + kh / 2.0 - double(roll->height()) / 2.0;
              SongView::ViewState st = view.viewState();
              st.valid = true;
              st.keyHeight = kh;
              st.scrollY = scrollY;
              view.applyViewState(st);
              checks::support::pumpQuick();

              const QImage offPm = check.captureQuickFramebuffer();
              const QRgb offScale = pixelAt(offPm, bandLaneX, rows.centerY(scaleKey));
              const QRgb offNon = pixelAt(offPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb offKbd = pixelAt(offPm, kbdX, rows.centerY(scaleKey));

              view.setScaleHighlight(true);
              view.setScaleFold(false);
              const QImage hlPm = check.captureQuickFramebuffer();
              const QRgb hlScale = pixelAt(hlPm, bandLaneX, rows.centerY(scaleKey));
              const QRgb hlNon = pixelAt(hlPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb hlKbd = pixelAt(hlPm, kbdX, rows.centerY(scaleKey));
              const QRgb hlRoot = pixelAt(hlPm, bandLaneX, rows.centerY(rootKey));

              if (hlScale == offScale)
Original expression:
  QFAIL("Highlight did not tint the scale row")

A019 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:271
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              const auto blendedChannel = [](int background, int source) {
                  return (source * 51 + background * 204 + 127) / 255;
              };
              const QRgb expectedTint =
                  qRgb(blendedChannel(qRed(offScale), 0xb5), blendedChannel(qGreen(offScale), 0x95),
                       blendedChannel(qBlue(offScale), 0xfc));
              if (std::abs(qRed(hlScale) - qRed(expectedTint)) > 1 ||
                  std::abs(qGreen(hlScale) - qGreen(expectedTint)) > 1 ||
                  std::abs(qBlue(hlScale) - qBlue(expectedTint)) > 1) {
Original expression:
  QFAIL("Highlight tint is not #b595fc at 20% opacity")

A020 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:274
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              }
              if (hlNon != offNon)
Original expression:
  QFAIL("Highlight tinted a non-scale row")

A021 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:276
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              if (hlKbd != offKbd)
Original expression:
  QFAIL("Highlight repainted the keyboard column")

A022 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:278
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              if (hlRoot != hlScale)
Original expression:
  QFAIL("Highlight emphasized the root degree differently")

A023 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:289
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;

              view.setScaleRoot(1); // C# Major: the former non-scale C# row becomes the root.
              const QImage shiftedPm = check.captureQuickFramebuffer();
              const QRgb shiftedRoot = pixelAt(shiftedPm, bandLaneX, rows.centerY(nonScaleKey));
              const QRgb expectedShiftedTint =
                  qRgb(blendedChannel(qRed(offNon), 0xb5), blendedChannel(qGreen(offNon), 0x95),
                       blendedChannel(qBlue(offNon), 0xfc));
              if (std::abs(qRed(shiftedRoot) - qRed(expectedShiftedTint)) > 1 ||
                  std::abs(qGreen(shiftedRoot) - qGreen(expectedShiftedTint)) > 1 ||
                  std::abs(qBlue(shiftedRoot) - qBlue(expectedShiftedTint)) > 1) {
Original expression:
  QFAIL("changing the scale root did not update Highlight lanes")

A024 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:299
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              }
              view.setScaleRoot(0);
              view.setScaleId(porydaw_scale::ScaleId::phrygian);
              const QImage changedScalePm = check.captureQuickFramebuffer();
              const QRgb changedScalePitch =
                  pixelAt(changedScalePm, bandLaneX, rows.centerY(nonScaleKey));
              if (std::abs(qRed(changedScalePitch) - qRed(expectedShiftedTint)) > 1 ||
                  std::abs(qGreen(changedScalePitch) - qGreen(expectedShiftedTint)) > 1 ||
                  std::abs(qBlue(changedScalePitch) - qBlue(expectedShiftedTint)) > 1) {
Original expression:
  QFAIL("changing the scale type did not update Highlight lanes")

A025 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:313
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              }
              view.setScaleId(scaleMajor);

              // Highlight and Fold compose: a visible, occupied scale row keeps
              // Fold's projection and receives the same Highlight tint.
              const int cmd0 = doc.undoStack()->index();
              constexpr int foldTintKey = 127; // G9 in C Major; always the top Fold row.
              const Tick tintTick = check.timeline().lengthTicks + doc.ticksPerClock() * 8;
              doc.addNote(scaleTrack, tintTick, uint8_t(foldTintKey), doc.ticksPerClock(), 100);
              view.setScaleHighlight(false);
              view.setScaleFold(true);
              const int foldRow = view.pitchProjection().rowForPitch(foldTintKey);
              if (foldRow == projHidden) {
Original expression:
  QFAIL("Fold hid the occupied scale row for the Highlight tint probe")

A026 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:323
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
              } else {
                  SongView::ViewState folded = view.viewState();
                  folded.valid = true;
                  folded.keyHeight = kh;
                  folded.scrollY = std::max(0.0, double(foldRow) * kh + kh / 2.0 - 100.0);
                  view.applyViewState(folded);
                  checks::support::pumpQuick();
                  const int foldY = foldCenterY(foldTintKey);
                  if (foldY < 0 || foldY >= roll->height()) {
Original expression:
  QFAIL("Fold did not make the occupied scale row visible for the Highlight tint "
                            "probe")

A027 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:330
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
                  } else {
                      const QImage foldOffPm = check.captureQuickFramebuffer();
                      const QRgb foldOff = pixelAt(foldOffPm, bandLaneX, foldY);
                      view.setScaleHighlight(true);
                      if (!view.scaleHighlight() || !view.scaleFold())
Original expression:
  QFAIL("enabling Highlight disabled Fold")

A028 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:334
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
                      const QImage foldHighlightPm = check.captureQuickFramebuffer();
                      const QRgb foldHighlight = pixelAt(foldHighlightPm, bandLaneX, foldY);
                      if (foldHighlight == foldOff)
Original expression:
  QFAIL("Highlight did not tint a visible occupied Fold scale row")

A029 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:341
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
                      const QRgb expectedFoldTint = qRgb(blendedChannel(qRed(foldOff), 0xb5),
                                                         blendedChannel(qGreen(foldOff), 0x95),
                                                         blendedChannel(qBlue(foldOff), 0xfc));
                      if (std::abs(qRed(foldHighlight) - qRed(expectedFoldTint)) > 1 ||
                          std::abs(qGreen(foldHighlight) - qGreen(expectedFoldTint)) > 1 ||
                          std::abs(qBlue(foldHighlight) - qBlue(expectedFoldTint)) > 1) {
Original expression:
  QFAIL("Highlight tint on a Fold row is not #b595fc at 20% opacity")

A030 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:370
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/scale_projection.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering/input observation. A presenter-level Swift check cannot establish the event delivery, surface state, pixels, sounding preview, or harness precondition below.
Source context since previous assertion:
  ;
                      }
                  }
              }
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              while (doc.undoStack()->index() > cmd0)
                  doc.undoStack()->undo();
          }

          // Note face is untouched: paint the same note in Off and Highlight
          // and compare the pixels inside its rect.
          if (scaleKey >= 0) {
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              view.ensureKeyVisible(scaleKey);
              checks::support::pumpQuick();
              const QPoint notePos(plotLaneX, rows.centerY(scaleKey));
              const int cmd0 = doc.undoStack()->index();
              drawNote(*roll, notePos); // commits a C-major-scale note (empty row)
              const QImage offPm = check.captureQuickFramebuffer();
              const QRgb offFace = pixelAt(offPm, bandLaneX, notePos.y());
              view.setScaleHighlight(true);
              view.setScaleFold(false);
              const QImage hlPm = check.captureQuickFramebuffer();
              const QRgb hlFace = pixelAt(hlPm, bandLaneX, notePos.y());
              view.setScaleHighlight(false);
              view.setScaleFold(false);
              if (hlFace != offFace)
Original expression:
  QFAIL("Highlight changed the painted note face")

A031 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:378
Disposition: PARTIAL
Mapping/reason: Executed related S016 in geometry.swift restores cloned document identity after undo, but the original Highlight sequence and renderer remain unproved. Feature not yet ported: Swift roll scale projection UI (no Highlight production view or renderer exists).
Source context since previous assertion:
  ;
              while (doc.undoStack()->index() > cmd0)
                  doc.undoStack()->undo();
              view.selectionModel().clearNoteSelection();
          }
          view.setScaleRoot(0);
          view.setScaleId(scaleMajor);
      }
Original expression:
  QCOMPARE(doc.undoStack()->index(), undo)

A032 | PianoRollTest::scaleHighlightRaster | src/checks/rollcheck/scale_projection.cpp:379
Disposition: PARTIAL
Mapping/reason: Executed related S017 in geometry.swift restores cloned MIDI bytes after undo, but the original Highlight sequence and renderer remain unproved. Feature not yet ported: Swift roll scale projection UI (no Highlight production view or renderer exists).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(doc.smf().write(), before)

Remaining original source (closing sequence and helper bodies):
  ;
  }

Swift assertion predicates

S001 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:63
  report.expect(chromatic.visibleRowCount == 128, cppID: id,
                  message: "A001 Off projects all 128 pitches")

S002 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:65
  report.expect((0..<128).allSatisfy { chromatic.row(forPitch: $0) != PitchProjection.hiddenRow },
                  cppID: id, message: "A002 Off hides no pitch")

S003 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:68
  report.expect(PitchProjection().visibleRowCount == chromatic.visibleRowCount, cppID: id,
                  message: "A003 related chromatic construction remains 128 rows")

S004 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:74
  report.expect(folded.visibleRowCount == occupied.count, cppID: id,
                  message: "A004 folded row count equals distinct selected-track pitches")

S005 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:85
  report.expect(descending, cppID: id, message: "A005 folded rows descend strictly")

S006 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:86
  report.expect((0..<128).allSatisfy {
          (folded.row(forPitch: $0) != PitchProjection.hiddenRow) == occupied.contains(UInt8($0))
      }, cppID: id, message: "A006 folded visible pitches exactly equal selected-track occupancy")

S007 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:89
  report.expect((0..<folded.visibleRowCount).allSatisfy { row in
          folded.visiblePitch(at: row).map { folded.row(forPitch: $0) == row } == true
      }, cppID: id, message: "A007 each visible folded row maps back to itself")

S008 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:92
  report.expect((0..<128).allSatisfy { pitch in
          let row = folded.row(forPitch: pitch)
          return row == PitchProjection.hiddenRow || folded.visiblePitch(at: row) == pitch
      }, cppID: id, message: "A008 each visible folded pitch maps back to itself")

S009 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:122
  report.expect(inserted.row(forPitch: insertedBase) != PitchProjection.hiddenRow,
                          cppID: id, message: "A010 occupied off-scale C-sharp becomes visible")

S010 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:124
  report.expect(inserted.row(forPitch: insertedBase + 12) == PitchProjection.hiddenRow,
                          cppID: id, message: "A011 unused off-scale octave remains hidden")

S011 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:129
  report.expect(freePitch.map { inserted.row(forPitch: $0) == PitchProjection.hiddenRow }
                          == true, cppID: id, message: "A012 unused off-scale pitch remains hidden")

S012 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:134
  report.expect(tieProjection.nearestVisiblePitch(to: 61) == 60, cppID: id,
                  message: "A013 equal-distance nearest pitch chooses lower pitch")

S013 | checkScaleProjectionInvariants | src/checks/rollcheck/static/geometry.swift:140
  report.expect(camera.snapshot.scrollY >= -1e-9 && camera.snapshot.scrollY <= maximum + 1e-9,
                  cppID: id, message: "A014 folded scroll remains inside its projected height")

S014 | checkDocumentUndoProbe | src/checks/rollcheck/static/geometry.swift:211
  report.expect(document.history.currentIdentity == identity, cppID: id,
                  message: messages.history)
  checkScaleProjectionInvariants supplies history: "A015 undo restores the pre-probe document history"

S015 | checkDocumentUndoProbe | src/checks/rollcheck/static/geometry.swift:213
  report.expect(before != nil && (try? document.captureSave().bytes) == before,
                  cppID: id, message: messages.bytes)
  checkScaleProjectionInvariants supplies bytes: "A016 undo restores serialized MIDI"

S016 | checkDocumentUndoProbe | src/checks/rollcheck/static/geometry.swift:211
  report.expect(document.history.currentIdentity == identity, cppID: id,
                  message: messages.history)
  checkScaleHighlightRasterDocumentGuards supplies history: "A031 highlight note probe restores document history"

S017 | checkDocumentUndoProbe | src/checks/rollcheck/static/geometry.swift:213
  report.expect(before != nil && (try? document.captureSave().bytes) == before,
                  cppID: id, message: messages.bytes)
  checkScaleHighlightRasterDocumentGuards supplies bytes: "A032 highlight note probe restores serialized MIDI"
