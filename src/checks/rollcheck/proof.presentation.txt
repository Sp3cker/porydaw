Assertion correspondence: rollcheck/presentation.cpp

Reference revision: 02752345fa3a1dbaf11ee6f1cd3ad5da475f0e06
Original: src/checks/rollcheck/presentation.cpp
Original SHA-256: 898cf2f8c3470a0ebcee65764bb17ac67081ad0d659ec53bd4d850efe741e38d
Swift counterpart: src/checks/rollcheck/presentation.swift
Swift SHA-256: 592f516980fe1e10bfa1861326aea56309034273b059db437f11047fb338de2b
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
S identifiers identify exact counterpart predicates, listed after the inventory.
MATCHED identifies equivalent Swift predicates executed by the swiftcore suite.
PARTIAL identifies related counterpart predicates with an explicit unproved condition.
NATIVE identifies retained native fixture/input/host obligations; it does not
establish coverage of the converted Swift surface.
GAP identifies an original predicate without a replacement assertion.
checkHeaderPanFollow in presentation.swift suspends and resumes SharedPlayheadPolicy
follow around a real PianoGrid pan and restores the camera snapshot.
checkHeaderRename drives the TrackHeadersPresenter rename (open, draft, Return commit,
Escape discard, loop-marker refusal, single-undo restoration). checkHeaderKeyboardMuteSolo
dispatches the mute/solo EditCommands through PianoGrid and asserts the header roles and
counters. headerRevealNote's polyphony-dock reveal has no owner in the Swift roll and
stays GAP. Disposition tally: 40 sites: 22 MATCHED, 4 PARTIAL, 8 GAP, 6 NATIVE.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: PASS — 1/14 ok (controller-recorded post-change run); the swiftcore
projectSession suite executed runPresentationChecks and the S001-S024 predicates.
Command: deno task verify:qml-roll --verbose
Result: PASS — 1/1 ok (controller-recorded post-change run); no presentation site
depends on the roll QML lane.
Command: deno task verify:shell --verbose
Result: PASS — 11/11 ok (controller-recorded post-change run); no presentation site
depends on the shell lane. The shell and roll QML runs predate only a
swiftcore-checks-only commit; no production or QML source changed after them.

Execution and ownership
runPresentationChecks executes checkHeaderPanFollow, checkHeaderRename,
checkHeaderKeyboardMuteSolo and checkHeaderReconciliation in the projectSession Swift
suite (swiftcore). It drives TrackHeadersPresenter, PianoGrid and EditorCamera directly;
it does not replay the original native/QML input sequences, so native focus/field
preconditions stay NATIVE. Original headercheck::model/renameInput and trackheaders
rowForTrack helpers have no counterpart in this ledger.

Fixture, sequence and row constraints
The original headerPanFollow probed both the roll input and the automation-lanes input
with a middle-button pan while the playhead followed; headerRename typed drafts through
the Quick rename field with Return/Escape and a '[' loop-marker guard; headerRevealNote
jumped to the middle view-model note then probed an unused key; headerKeyboardMuteSolo
toggled M/masks, triggered the canonical Solo action and probed a mixed mute scope while
counting model resets. Every slot ends with undo-index and SMF comparisons. The Swift
fixture instead seeds a one/two-track song and drives the presenter and grid commands.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.cpp:45
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/presentation.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
#include "checks/rollcheck/tst_pianoroll.h"

#include <QAbstractItemModel>
#include <QByteArray>
#include <QCoreApplication>
#include <QEvent>
#include <QObject>
#include <QPointF>
#include <QtTest>
#include <algorithm>
#include <cstdint>
#include <optional>

#include "checks/rollcheck/headerchecksupport.h"
#include "checks/support/eventsynth.h"
#include "checks/trackheaders/trackheaderoracles.h"
#include "core/songdocument.h"
#include "ui/songview.h"
#include "ui/songview/editactions.h"
#include "ui/songview/quick/timelineinputitem.h"
#include "ui/songview/quick/timelinequickview.h"
#include "ui/songview/trackheadermodel.h"
using namespace checks::rollcheck;

using checks::rollcheck::headercheck::model;
using checks::rollcheck::headercheck::renameInput;
using trackheaders_test::rowForTrack;

void PianoRollTest::headerPanFollow()
{
    PianoRollFixture &check = *m_fixture;
    SongDocument &doc = check.document();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    SongView &view = check.view();
    songview::TimelineInputItem &roll = check.rollInput();
    const qreal width = std::max<qreal>(50, roll.width());
    auto *automationQuick =
        view.findChild<songview::TimelineQuickView *>(QStringLiteral("timelineQuickCanvas"));
    auto *lanes = automationQuick && automationQuick->rootObject()
                      ? automationQuick->rootObject()->findChild<songview::TimelineInputItem *>(
                            QStringLiteral("timelineAutomationInput"))
                      : nullptr;
    if (!lanes) {

Original expression:
QFAIL("automation input item not found")

A002 | PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.cpp:57
Disposition: MATCHED
Mapping: S001 (src/checks/rollcheck/presentation.swift:30-32) asserts that while the roll pan is live the SharedPlayheadPolicy follow is blocked and the camera scrollX is unchanged — the follow-suspension observable of this site.
Source context since previous assertion:
;
        return;
    }
    const auto probe = [&](auto &surface) {
        const int home = view.camera().contentX(0.0);
        const Tick farTick =
            CoreTimeDefaults::tickFromDouble(view.camera().tickAtContentX(width * 2));
        const QPointF middle(surface.width() / 2.0, surface.height() / 2.0);
        checks::events::sendMouse(surface, QEvent::MouseButtonPress, middle, Qt::MiddleButton,
                                  Qt::MiddleButton, Qt::NoModifier);
        view.setPlayheadSample(check.timeline().sampleForTick(farTick), true);
        if (view.camera().contentX(0.0) != home)

Original expression:
QFAIL("playhead follow-scroll moved the view during a pan gesture")

A003 | PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.cpp:62
Disposition: MATCHED
Mapping: S002 (presentation.swift:37-38) asserts follow becomes eligible once the pan ends, and S003 (presentation.swift:41-42) asserts the resumed follow scrolls the shared roll camera.
Source context since previous assertion:
;
        checks::events::sendMouse(surface, QEvent::MouseButtonRelease, middle, Qt::MiddleButton,
                                  Qt::NoButton, Qt::NoModifier);
        view.setPlayheadSample(check.timeline().sampleForTick(farTick), true);
        if (view.camera().contentX(0.0) == home)

Original expression:
QFAIL("playhead follow-scroll did not resume after the pan ended")

A004 | PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.cpp:68
Disposition: MATCHED
Mapping: S005 (presentation.swift:51-53) asserts the document state and history identity are unchanged after the pan/follow probe — the state/identity public-API form of the unchanged undo index. S004 (presentation.swift:49-50) additionally asserts the incoming camera snapshot is restored.
Source context since previous assertion:
;
        view.setPlayheadSample(0, false);
        view.scrollByPx(view.camera().contentX(0.0) - home);
    };
    probe(roll);
    probe(*lanes);

Original expression:
QCOMPARE(doc.undoStack()->index(), undo)

A005 | PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.cpp:69
Disposition: MATCHED
Mapping: S005 (presentation.swift:51-53) asserts session.document.state == before after the pan/follow probe; SongState equality includes the serialized file, the observable this SMF byte comparison pins.
Source context since previous assertion:
;

Original expression:
QCOMPARE(doc.smf().write(), before)

A006 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:79
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/presentation.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
}

void PianoRollTest::headerRename()
{
    PianoRollFixture &check = *m_fixture;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto *headers = model(view);
    if (!headers) {

Original expression:
QFAIL("Quick track-header model was not found")

A007 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:85
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/presentation.cpp) no longer builds or runs. Previous native note: Original native input precondition: focusTimelineBand on the Quick SongView. The swiftcore roll lane has no focusable timeline band.
Source context since previous assertion:
;
        return;
    }
    const int track = check.track();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();

Original expression:
QVERIFY2(view.focusTimelineBand(songview::TimelineBand::TrackHeaders, Qt::OtherFocusReason),
             "the Quick track-header input was unavailable")

A008 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:87
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/presentation.cpp) no longer builds or runs. Previous native note: Original native focus observation: focusedTimelineBand() == TrackHeaders after the Quick focus route. No Swift roll predicate observes focus routing.
Source context since previous assertion:
;

Original expression:
QTRY_VERIFY2(view.focusedTimelineBand() == songview::TimelineBand::TrackHeaders,
                 "the Quick track-header input did not take active focus")

A009 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:99
Disposition: PARTIAL
Mapping/reason: S007 (presentation.swift:85-86) asserts the presenter publishes the typed draft (renameDraft == "Rolled"). Unproved: the native Quick rename field receiving the model draft (its text property and focus).
Source context since previous assertion:
;
    const auto renameIsOpen = [&] {
        QCoreApplication::processEvents();
        QObject *const field = renameInput(view);
        return headers->renamingTrack() == track && field && field->property("visible").toBool() &&
               field->property("activeFocus").toBool();
    };
    const auto typeDraft = [&](QObject &field, const QString &draft) {
        headers->setRenameDraft(draft);
        QCoreApplication::processEvents();
        if (field.property("text").toString() != draft)

Original expression:
QFAIL("Quick rename input did not receive the model draft")

A010 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:102
Disposition: PARTIAL
Mapping/reason: S006 (presentation.swift:82-83) asserts the presenter rename opens with renamingTrack == the selected track. Unproved: the Quick rename field's visible/activeFocus state that the original renameIsOpen() also requires.
Source context since previous assertion:
;
    };
    view.renameTrack(track);

Original expression:
QTRY_VERIFY2(renameIsOpen(), "Quick rename input did not open")

A011 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:108
Disposition: MATCHED
Mapping: S008 (presentation.swift:88-90) asserts the committed name reaches the document and the editor closes (trackName == "Rolled" && renamingTrack == -1) after finishRename(commit: true).
Source context since previous assertion:
;
    QObject *field = renameInput(view);
    typeDraft(*field, QStringLiteral("Rolled"));
    sendKeyStroke(*field, Qt::Key_Return, Qt::NoModifier, false);
    QCoreApplication::processEvents();
    if (doc.trackName(track) != QStringLiteral("Rolled")) {

Original expression:
QFAIL("Quick Return did not commit the inline rename")

A012 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:112
Disposition: PARTIAL
Mapping/reason: S009 (presentation.swift:91-92) asserts the renamed header can reopen its editor (renamingTrack == track). Unproved: the Quick rename field's visible/activeFocus state.
Source context since previous assertion:
;
        return;
    }
    view.renameTrack(track);

Original expression:
QTRY_VERIFY2(renameIsOpen(), "Quick rename input did not reopen")

A013 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:118
Disposition: MATCHED
Mapping: S010 (presentation.swift:95-96) asserts Escape discards the draft: the name stays "Rolled" and the editor closes.
Source context since previous assertion:
;
    field = renameInput(view);
    typeDraft(*field, QStringLiteral("Discarded"));
    sendKeyStroke(*field, Qt::Key_Escape, Qt::NoModifier, false);
    QCoreApplication::processEvents();
    if (doc.trackName(track) != QStringLiteral("Rolled") || headers->renamingTrack() != -1) {

Original expression:
QFAIL("Quick Escape did not discard the inline rename draft")

A014 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:122
Disposition: PARTIAL
Mapping/reason: S011 (presentation.swift:98-99) asserts the editor can reopen for the loop-marker guard. Unproved: the Quick rename field's visible/activeFocus state.
Source context since previous assertion:
;
        return;
    }
    view.renameTrack(track);

Original expression:
QTRY_VERIFY2(renameIsOpen(), "Quick rename input did not reopen for the loop-marker guard")

A015 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:129
Disposition: MATCHED
Mapping: S012 (presentation.swift:104-106) asserts the loop-marker draft "[" is refused: the name stays "Rolled", and state and history identity are unchanged (no transaction); S013 (presentation.swift:107-108) asserts the single accepted rename undoes.
Source context since previous assertion:
;
    const int commands = doc.undoStack()->count();
    field = renameInput(view);
    typeDraft(*field, QStringLiteral("["));
    sendKeyStroke(*field, Qt::Key_Return, Qt::NoModifier, false);
    QCoreApplication::processEvents();
    if (doc.trackName(track) != QStringLiteral("Rolled") || doc.undoStack()->count() != commands)

Original expression:
QFAIL("loop-marker name was not refused")

A016 | PianoRollTest::headerRename | src/checks/rollcheck/presentation.cpp:132
Disposition: MATCHED
Mapping: S014 (presentation.swift:109-110) asserts the document state and history identity are back to the pre-rename snapshot after the undo drain — the state/identity form of the unchanged undo index and the restored SMF bytes.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo)
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A017 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:149
Disposition: GAP
Mapping/reason: Feature not yet ported: the polyphony-dock revealNote jump (view.revealNote selecting the losing track and the lost note) has no owner in the Swift roll; no predicate asserts a reveal-note selection or its viewport jump.
Source context since previous assertion:
;
}

void PianoRollTest::headerRevealNote()
{
    PianoRollFixture &check = *m_fixture;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();

    // Polyphony-dock jump target: revealNote selects the losing track and
    // the lost note itself (the last note on (track, key) starting at or
    // before the event tick), without touching the undo stack.
    {
        const auto &notes = view.model().notes;
        if (notes.empty()) {

Original expression:
QFAIL("no notes in the view model for revealNote")

A018 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:153
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote's note-lookup outcome (the note at or before the event tick is found) is not implemented in the Swift roll.
Source context since previous assertion:
;
        } else {
            const ViewNote target = notes[notes.size() / 2];
            if (!view.revealNote(target.track, target.key, target.startTick))

Original expression:
QFAIL("revealNote did not find the note")

A019 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:155
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote's track-selection outcome (primaryTrack becomes the revealed track) has no Swift roll predicate.
Source context since previous assertion:
;
            if (view.selectionModel().primaryTrack() != int(target.track))

Original expression:
QFAIL("revealNote did not select the track")

A020 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:158
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote's single-note selection payload has no Swift roll predicate.
Source context since previous assertion:
;
            const auto &sel = view.selectionModel().noteSelection();
            if (sel.size() != 1 || !(sel[0] == target.noteId))

Original expression:
QFAIL("revealNote did not select the note")

A021 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:174
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote miss on an unused key (returns false without selecting a note) has no Swift roll predicate.
Source context since previous assertion:
;

            // A key the track never plays: no note found, but the track
            // selection sticks (the dock still switches context).
            bool used[128] = {};
            for (const ViewNote &note : notes) {
                if (note.track == target.track)
                    used[note.key] = true;
            }
            int freeKey = -1;
            for (int k = 0; k < 128 && freeKey < 0; k++) {
                if (!used[k])
                    freeKey = k;
            }
            if (freeKey >= 0) {
                if (view.revealNote(target.track, uint8_t(freeKey), target.startTick))

Original expression:
QFAIL("revealNote found a note on an unused key")

A022 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:176
Disposition: GAP
Mapping/reason: Feature not yet ported: the revealNote miss keeps the track selection — no Swift roll predicate observes it.
Source context since previous assertion:
;
                if (view.selectionModel().primaryTrack() != int(target.track))

Original expression:
QFAIL("revealNote miss dropped the track selection")

A023 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:180
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote's no-undo-stack-change guarantee has no Swift roll predicate because revealNote itself is unported.
Source context since previous assertion:
;
            }
        }
    }

Original expression:
QCOMPARE(doc.undoStack()->index(), undo)

A024 | PianoRollTest::headerRevealNote | src/checks/rollcheck/presentation.cpp:181
Disposition: GAP
Mapping/reason: Feature not yet ported: revealNote's no-SMF-write guarantee has no Swift roll predicate because revealNote itself is unported.
Source context since previous assertion:
;

Original expression:
QCOMPARE(doc.smf().write(), before)

A025 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:193
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/presentation.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
}

void PianoRollTest::headerKeyboardMuteSolo()
{
    PianoRollFixture &check = *m_fixture;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    songview::TimelineInputItem &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    auto *headers = model(view);

Original expression:
QVERIFY2(headers, "Quick track-header model was not found")

A026 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:213
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/presentation.cpp) no longer builds or runs. Previous native note: Original native action-binding precondition: the canonical SoloTracks QAction bound to the Quick view. The Swift check dispatches EditCommand.soloTracks through PianoGrid.performCommand and does not assert the action binding.
Source context since previous assertion:
;

    // Keyboard mute/solo changes view masks and the matching model roles
    // without resetting the retained Quick header structure.
    {
        const int preCount = doc.undoStack()->count();
        int headerResets = 0;
        const QMetaObject::Connection resetConnection = QObject::connect(
            headers, &QAbstractItemModel::modelReset, &view, [&headerResets] { ++headerResets; });
        const auto roleChecked = [&](int track, int role) {
            const std::optional<int> row = rowForTrack(*headers, track);
            return row && headers->data(headers->index(*row, 0), role).toBool();
        };
        // S is a Window-scope command: the isolated fixture owns no window
        // shortcut, so the canonical action is the stimulus (the same
        // treatment the spec gives the isolated Copy fixture stimulus).
        const songview::EditActions *const actions = view.editActions();
        QAction *const soloAction =
            actions ? actions->action(SongView::EditCommand::SoloTracks) : nullptr;
        if (!soloAction)

Original expression:
QFAIL("canonical solo action was not bound to the view")

A027 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:217
Disposition: MATCHED
Mapping: S015 (presentation.swift:157-158) asserts mute and solo scopes are clean before the keyboard toggles.
Source context since previous assertion:
;

        const int selectedTrack = view.selectionModel().primaryTrack();
        if (view.muteMask() != 0 || view.soloMask() != 0)

Original expression:
QFAIL("mute/solo masks not clean before the keyboard toggles")

A028 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:220
Disposition: MATCHED
Mapping: S016 (presentation.swift:160-161) asserts M mutes the selected track (mutedTracks == [0]).
Source context since previous assertion:
;
        sendKeyStroke(roll, Qt::Key_M, Qt::NoModifier, false);
        if (!view.trackMuted(selectedTrack))

Original expression:
QFAIL("M did not mute the selected track")

A029 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:222
Disposition: MATCHED
Mapping: S016 (presentation.swift:160-161) asserts the same site publishes the checked header mute role (headers.rows[0].muteChecked).
Source context since previous assertion:
;
        if (!roleChecked(selectedTrack, songview::TrackHeaderModel::MuteCheckedRole))

Original expression:
QFAIL("keyboard mute did not publish the checked Quick header role")

A030 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:225
Disposition: MATCHED
Mapping: S017 (presentation.swift:163-164) asserts the second M clears the selected track mute.
Source context since previous assertion:
;
        sendKeyStroke(roll, Qt::Key_M, Qt::NoModifier, false);
        if (view.muteMask() != 0)

Original expression:
QFAIL("second M did not unmute the selected track")

A031 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:227
Disposition: MATCHED
Mapping: S017 (presentation.swift:163-164) asserts the second M clears the checked header mute role.
Source context since previous assertion:
;
        if (roleChecked(selectedTrack, songview::TrackHeaderModel::MuteCheckedRole))

Original expression:
QFAIL("keyboard unmute did not clear the checked Quick header role")

A032 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:231
Disposition: MATCHED
Mapping: S018 (presentation.swift:166-167) asserts the Solo command marks the selected track solo and publishes the checked header solo role.
Source context since previous assertion:
;
        soloAction->trigger();
        if (!view.trackSoloed(selectedTrack) ||
            !roleChecked(selectedTrack, songview::TrackHeaderModel::SoloCheckedRole)) {

Original expression:
QFAIL("S did not publish the selected track's solo role")

A033 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:236
Disposition: MATCHED
Mapping: S019 (presentation.swift:169-170) asserts the second Solo action clears both the solo set and the checked header solo role.
Source context since previous assertion:
;
        }
        soloAction->trigger();
        if (view.soloMask() != 0 ||
            roleChecked(selectedTrack, songview::TrackHeaderModel::SoloCheckedRole)) {

Original expression:
QFAIL("second S did not clear the selected track's solo role")

A034 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:244
Disposition: MATCHED
Mapping: S020 (presentation.swift:171-173) guards that a second header row exists for the mixed-scope probe (report.fail when missing).
Source context since previous assertion:
;
        }

        // Multi-track scope + mixed state: with another track Ctrl-scoped
        // in and already muted, M mutes the rest (on wins), and the next M
        // clears the whole scope.
        const int other = selectedTrack == 0 ? 1 : 0;
        if (!rowForTrack(*headers, other)) {

Original expression:
QFAIL("Quick header record was missing for the scoped keyboard probe")

A035 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:252
Disposition: MATCHED
Mapping: S021 (presentation.swift:178-181) asserts M over the mixed scope mutes every scoped track (mutedTracks == [0, other] with both header roles checked).
Source context since previous assertion:
;
        } else {
            view.trackHeaderClicked(other, Qt::ControlModifier);
            view.setTrackMute(other, true);
            sendKeyStroke(roll, Qt::Key_M, Qt::NoModifier, false);
            if (!view.trackMuted(selectedTrack) || !view.trackMuted(other) ||
                !roleChecked(selectedTrack, songview::TrackHeaderModel::MuteCheckedRole) ||
                !roleChecked(other, songview::TrackHeaderModel::MuteCheckedRole)) {

Original expression:
QFAIL("M over a mixed scope did not mute every scoped track")

A036 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:256
Disposition: MATCHED
Mapping: S022 (presentation.swift:182-183) asserts the second M unmutes the whole scoped selection (mutedTracks.isEmpty).
Source context since previous assertion:
;
            }
            sendKeyStroke(roll, Qt::Key_M, Qt::NoModifier, false);
            if (view.muteMask() != 0)

Original expression:
QFAIL("second M did not unmute the whole scope")

A037 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:260
Disposition: MATCHED
Mapping: S023 (presentation.swift:185-186) asserts mute/solo role updates never reset the header rows (rowRebuildCount unchanged).
Source context since previous assertion:
;
            view.trackHeaderClicked(selectedTrack, Qt::NoModifier); // collapse scope
        }
        if (headerResets != 0)

Original expression:
QFAIL("keyboard mute/solo reset the TrackHeaderModel")

A038 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:262
Disposition: MATCHED
Mapping: S024 (presentation.swift:187-188) asserts the keyboard toggles leave the document state and history identity untouched (no undo entry).
Source context since previous assertion:
;
        if (doc.undoStack()->count() != preCount)

Original expression:
QFAIL("keyboard mute/solo touched the undo stack")

A039 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:266
Disposition: MATCHED
Mapping: S024 (presentation.swift:187-188) asserts the document and history identity are unchanged by the mute/solo sequence; the check's defer additionally asserts state == before (presentation.swift:136-138).
Source context since previous assertion:
;
        QObject::disconnect(resetConnection);
    }


Original expression:
QCOMPARE(doc.undoStack()->index(), undo)

A040 | PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.cpp:267
Disposition: MATCHED
Mapping: S024 (presentation.swift:187-188) asserts the document state is untouched (SongState equality includes the serialized file), corroborated by the defer assertion at presentation.swift:136-138.
Source context since previous assertion:
;

Original expression:
QCOMPARE(doc.smf().write(), before)

Remaining original source (closing sequence and helper bodies):
;
}

Swift assertion predicates
The S entries are exact report predicates (report.expect except the S020 report.fail guard) already executed by
runPresentationChecks in the swiftcore projectSession suite (deno task verify
--filter swiftcore --verbose, PASS 1/14 ok, controller-recorded).

S001 | swiftcore/PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.swift:30
  report.expect(grid.interactionActive && blocked == nil
                && session.camera.snapshot.scrollX == home, cppID: id,
                message: "live roll pan suspends follow without moving the camera")

S002 | swiftcore/PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.swift:37
  report.expect(!grid.interactionActive && resumed != nil, cppID: id,
                message: "follow becomes eligible once the pan ends")

S003 | swiftcore/PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.swift:41
  report.expect(session.camera.snapshot.scrollX != home, cppID: id,
                message: "resumed follow scrolls the shared roll camera")

S004 | swiftcore/PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.swift:49
  report.expectEqual(oldCamera, session.camera.snapshot, cppID: id,
                     what: "pan probe restores the incoming camera viewport and scroll")

S005 | swiftcore/PianoRollTest::headerPanFollow | src/checks/rollcheck/presentation.swift:51
  report.expect(session.document.state == before && session.document.history.currentIdentity == identity,
                cppID: id, message: "pan and follow do not edit the song or its undo history")

S006 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:82
  report.expectEqual(track, headers.renamingTrack, cppID: id,
                     what: "inline rename opens on the selected header")

S007 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:85
  report.expectEqual("Rolled", headers.renameDraft, cppID: id,
                     what: "header publishes the typed draft")

S008 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:88
  report.expect(document.trackName(track) == "Rolled" && headers.renamingTrack == -1,
                cppID: id, message: "Return commits the inline rename and closes the editor")

S009 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:91
  report.expectEqual(track, headers.renamingTrack, cppID: id,
                     what: "the renamed header can reopen its editor")

S010 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:95
  report.expect(document.trackName(track) == "Rolled" && headers.renamingTrack == -1,
                cppID: id, message: "Escape discards the draft without changing the name")

S011 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:98
  report.expectEqual(track, headers.renamingTrack, cppID: id,
                     what: "editor can reopen for the marker-name guard")

S012 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:104
  report.expect(document.trackName(track) == "Rolled" && document.state == accepted
                && document.history.currentIdentity == acceptedIdentity, cppID: id,
                message: "loop-marker name is refused without a transaction")

S013 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:107
  report.expect(document.history.undoDocument(), cppID: id,
                message: "one undo reverts the only accepted rename")

S014 | swiftcore/PianoRollTest::headerRename | src/checks/rollcheck/presentation.swift:109
  report.expect(document.state == before && document.history.currentIdentity == identity,
                cppID: id, message: "cancellation and invalid marker add no undo steps")

S015 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:157
  report.expect(session.mutedTracks.isEmpty && session.soloedTracks.isEmpty,
                cppID: id, message: "keyboard mute and solo begin with clear scopes")

S016 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:160
  report.expect(session.mutedTracks == [0] && headers.rows[0].muteChecked,
                cppID: id, message: "M mutes the selected track and publishes the header role")

S017 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:163
  report.expect(session.mutedTracks.isEmpty && !headers.rows[0].muteChecked,
                cppID: id, message: "second M clears the selected header mute role")

S018 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:166
  report.expect(session.soloedTracks == [0] && headers.rows[0].soloChecked,
                cppID: id, message: "Solo action publishes the selected header solo role")

S019 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:169
  report.expect(session.soloedTracks.isEmpty && !headers.rows[0].soloChecked,
                cppID: id, message: "second Solo action clears the solo role")

S020 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:171
  guard headers.rows.count > other else {
      report.fail(id, "second header row is missing for mixed-scope mute")
      return
  }

S021 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:178
  report.expect(session.mutedTracks == [0, other]
                && headers.rows[0].muteChecked && headers.rows[other].muteChecked,
                cppID: id, message: "M over a mixed scope mutes every scoped track")

S022 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:182
  report.expect(session.mutedTracks.isEmpty, cppID: id,
                message: "second M unmutes the whole scoped selection")

S023 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:185
  report.expect(headers.rowRebuildCount == rebuilds, cppID: id,
                message: "mute and solo role updates never reset header rows")

S024 | swiftcore/PianoRollTest::headerKeyboardMuteSolo | src/checks/rollcheck/presentation.swift:187
  report.expect(document.state == noEdit && document.history.currentIdentity == noEditIdentity,
                cppID: id, message: "keyboard toggles do not touch MIDI or the undo stack")
