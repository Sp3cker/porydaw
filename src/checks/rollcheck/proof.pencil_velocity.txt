Assertion correspondence: rollcheck/pencil_velocity.cpp

Reference revision: 4ae6619dda9405bf0a3352e4171d9da54ef04f1f
Original: src/checks/rollcheck/pencil_velocity.cpp
Original SHA-256: c054a6feb791c8e18ac192d8450bf84066d19c6ef237298098df4244219dd9de
Swift counterpart: src/checks/rollcheck/selection.swift
Additional Swift counterpart: src/checks/rollcheck/pencil.swift
Additional Swift counterpart: src/checks/rollcheck/interlock.swift
Additional Swift counterpart: src/checks/rollcheck/resize.swift
Additional Swift counterpart: src/checks/editorqml/tst_ShellGridInput.qml
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
S identifiers identify exact counterpart predicates, listed after the inventory.
MATCHED identifies equivalent Swift predicates executed by the swiftcore suite or the
shell lane.
PARTIAL identifies related counterpart predicates with an explicit unproved condition.
NATIVE identifies retained native popup/menu/host-input obligations (note-menu frame,
window, click delivery and Quick-window focus); it does not establish coverage of the
converted Swift surface.
GAP identifies an original predicate without a replacement assertion.
checkGroupedVelocityDrag in selection.swift covers the modifier velocity drag (hover pin,
preview before release, grouped commit, one undo entry, restoration); checkVelocityDoubleClickDelete
and checkPointerDrawCancellation in pencil.swift cover the double-click delete and the
ungrab/window-deactivate/focus-out cancellation loop; interlock.swift covers the staged
velocity preview and its abort; resize.swift publishes the hover cursor kinds; the
shell-grid-input lane covers a real click selection and the ungrab cancel slot. The
velocity click-latch and note-menu retarget/stale-activation families are not ported.
Disposition tally: 53 sites: 12 MATCHED, 5 PARTIAL, 21 GAP, 15 NATIVE.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: PASS — 1/14 ok (controller-recorded post-change run); the swiftcore
projectSession suite executed checkGroupedVelocityDrag, checkVelocityDoubleClickDelete,
checkPointerDrawCancellation, checkGestureInterlock and the hover-cursor predicates.
Command: deno task verify:qml-roll --verbose
Result: PASS — 1/1 ok (controller-recorded post-change run); no pencil-velocity site
depends on the roll QML lane.
Command: deno task verify:shell --verbose
Result: PASS — 11/11 ok (controller-recorded post-change run); the shell-grid-input
lane ran test_escapeCancel and test_ungrabCancel with real pointer input (S027/S028).
The shell and roll QML runs predate only a swiftcore-checks-only commit; no production
or QML source changed after them.

Execution and ownership
runSelectionChecks executes checkGroupedVelocityDrag and runPencilChecks executes
checkVelocityDoubleClickDelete and checkPointerDrawCancellation (cppID
swiftcore/PianoRoll::quickLifecycle) in the projectSession swiftcore suite;
runInterlockChecks executes the staged-preview/abort sequence; runResizeChecks publishes
the hover cursor kinds; runEditorGridCameraChecks supplies the free-cell/draw predicates;
the shell lane runs test_escapeCancel/test_ungrabCancel with real input. The retired gated
GridFixture entry points are dead citations and are no longer indexed. The original
PencilPaintingFixture/PencilVelocityFixture (makePaintingSeed/makeVelocitySeed) have no
Swift-surface equivalent.

Fixture, sequence and row constraints
The original makeVelocitySeed staged two notes (a/b) with seeded velocities; click-latch
set velocity 73 then drew; retarget opened the note menu with a right press and re-pressed
outside the menu frame; stale-activation edited the document under an open menu;
drag-commit staged preview deltas (73->93) before release; cancel-ungrab primed a real
window drag then ungrabbed; quickLifecycle staged three cancellation paths (ungrab,
WindowDeactivate, FocusOut) through one lambda executed three times. Every method ended
with an undo-stack drain plus an SMF byte comparison. The Swift fixture instead seeds the
93/85 velocity pair (velocityPairSeed) and stages an equivalent preview delta (78) with
GroupedVelocityDrag's control-modifier pointer sequence.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | PianoRollTest::velocityClickLatch | src/checks/rollcheck/pencil_velocity.cpp:56
Disposition: GAP
Mapping/reason: Feature not yet ported: the velocity click-latch (drawing with the last accepted/clicked velocity) does not exist in the Swift roll. Pointer draws always use the fixture latch (velocity 100), so no predicate can assert the 73-velocity click-latch draw.
Source context since previous assertion:
#include "checks/rollcheck/tst_pianoroll.h"

#include <QCoreApplication>
#include <QEvent>
#include <QFocusEvent>
#include <QMouseEvent>
#include <QPoint>
#include <QQuickItem>
#include <QQuickWindow>
#include <QtTest>
#include <algorithm>
#include <optional>
#include <vector>

#include "checks/quickpopupguard.h"
#include "checks/rollcheck/rollcheck.h"
#include "checks/support/eventsynth.h"
#include "core/songdocument.h"
#include "ui/songview.h"
#include "ui/songview/pianoroll.h"
#include "ui/songview/quick/quickmenumodel.h"
#include "ui/songview/quick/timelineinputitem.h"
#include "ui/songview/quick/timelinequickview.h"

using ::DocNote;
using checks::rollcheck::Cell;
using checks::rollcheck::drawNote;
using checks::rollcheck::makePaintingSeed;
using checks::rollcheck::makeVelocitySeed;
using checks::rollcheck::PencilPaintingFixture;
using checks::rollcheck::PencilVelocityFixture;

namespace {

void sendWindowMouse(SongView &view, QQuickItem &rollInput, QEvent::Type type,
                     const QPointF &position, Qt::MouseButton button, Qt::MouseButtons buttons,
                     Qt::KeyboardModifiers modifiers)
{
    auto *quick =
        view.findChild<songview::TimelineQuickView *>(QStringLiteral("timelineQuickCanvas"));
    QQuickWindow *const window = quick ? quick->quickWindow() : nullptr;
    if (!window)
        return;
    const QPointF windowPosition = rollInput.mapToScene(position);
    QMouseEvent event(type, windowPosition, QPointF(window->mapToGlobal(windowPosition.toPoint())),
                      button, buttons, modifiers);
    QCoreApplication::sendEvent(window, &event);
}

} // namespace

void PianoRollTest::velocityClickLatch()
{
    auto &check = *m_fixture;
    const std::optional<PencilPaintingFixture> seed = makePaintingSeed(check);

Original expression:
QVERIFY(seed.has_value())

A002 | PianoRollTest::velocityClickLatch | src/checks/rollcheck/pencil_velocity.cpp:64
Disposition: GAP
Mapping/reason: Feature not yet ported: the click-latch scenario's free-cell probe has no Swift counterpart because the latch itself is unported.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    doc.setNotesVelocity({seed->noteA}, 73);
    checks::rollcheck::click(roll, seed->a.center);
    const Cell b = check.findFreeCell();

Original expression:
QVERIFY2(b.key >= 0, "no free grid cell for the click-latch draw")

A003 | PianoRollTest::velocityClickLatch | src/checks/rollcheck/pencil_velocity.cpp:67
Disposition: GAP
Mapping/reason: Feature not yet ported: the click-latch draw produced-note observation is unreachable in the Swift roll, whose draws never consume a clicked velocity.
Source context since previous assertion:
;
    drawNote(roll, b.center);
    DocNote noteB;

Original expression:
QVERIFY2(doc.findNote(check.track(), b.tick, uint8_t(b.key), &noteB),
             "click-latch draw produced no note")

A004 | PianoRollTest::velocityClickLatch | src/checks/rollcheck/pencil_velocity.cpp:69
Disposition: GAP
Mapping/reason: Feature not yet ported: the click-latched velocity (73) is unassertable; Swift pointer draws use the fixture latch of 100 (pencil.swift).
Source context since previous assertion:
;

Original expression:
QCOMPARE(noteB.velocity, 73)

A005 | PianoRollTest::velocityClickLatch | src/checks/rollcheck/pencil_velocity.cpp:72
Disposition: GAP
Mapping/reason: Feature not yet ported: the click-latch scenario's no-mutation guard has no Swift counterpart because the scenario is unported.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A006 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:79
Disposition: GAP
Mapping/reason: Feature not yet ported: the note-menu retarget scenario requires the QML note menu, which the Swift roll does not host; the shared velocity seed guard has no predicate in this scenario.
Source context since previous assertion:
;
}

void PianoRollTest::velocityNoteMenuRetarget()
{
    auto &check = *m_fixture;
    const std::optional<PencilVelocityFixture> seed = makeVelocitySeed(check);

Original expression:
QVERIFY(seed.has_value())

A007 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:93
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: a right press must open the Quick note menu. No Swift roll predicate observes note-menu opening.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const int undoCount = doc.undoStack()->count();
    const checks::rollcheck::SnappedRows rows{view, roll};
    checks::events::sendMouse(roll, QEvent::MouseButtonPress, seed->b.center, Qt::RightButton,
                              Qt::RightButton, Qt::NoModifier);
    checks::events::sendMouse(roll, QEvent::MouseButtonRelease, seed->b.center, Qt::RightButton,
                              Qt::NoButton, Qt::NoModifier);
    QCoreApplication::processEvents();
    songview::QuickPopupSession *const menu = quick_popup::popupSession(view);

Original expression:
QVERIFY2(menu && menu->isOpen(), "right-click did not open the note menu")

A008 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:95
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the note menu must render a frame. Native QML surface observation.
Source context since previous assertion:
;
    QQuickItem *const frame = quick_popup::menuFrame(*menu);

Original expression:
QVERIFY2(frame, "the note menu rendered no frame")

A009 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:97
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the retarget point must fall outside the rendered menu frame (native frame geometry).
Source context since previous assertion:
;
    const QPointF retargetLocal(seed->a.center);

Original expression:
QVERIFY2(!frame->contains(frame->mapFromScene(roll.mapToScene(retargetLocal))),
             "the retarget note fell inside the open note menu")

A010 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:100
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the note menu must own a canvas window.
Source context since previous assertion:
;
    QQuickWindow *const window = menu->window();

Original expression:
QVERIFY2(window, "the note menu has no canvas window")

A011 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:106
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the open note menu must stay open while retargeting. Menu session state is not modelled in the Swift roll.
Source context since previous assertion:
;
    const QPoint retargetPoint = roll.mapToScene(retargetLocal).toPoint();

    // The outside right press retargets the selection to the note under the
    // cursor and reopens the menu for it on the same owner.
    QTest::mousePress(window, Qt::RightButton, Qt::NoModifier, retargetPoint);

Original expression:
QTRY_VERIFY2(menu->isOpen(), "retargeting hid the open note menu")

A012 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:107
Disposition: GAP
Mapping/reason: Feature not yet ported: the note-menu retarget selection (re-pressing outside the frame re-selects the first note) has no Swift predicate; the Swift roll hosts no note menu.
Source context since previous assertion:
;

Original expression:
QTRY_COMPARE(view.selectionModel().noteSelection(), std::vector<NoteId>{seed->noteA.noteId})

A013 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:110
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the retarget release must not reach the timeline (native event delivery to the open menu).
Source context since previous assertion:
;
    QTest::mouseRelease(window, Qt::RightButton, Qt::NoModifier, retargetPoint);
    QCoreApplication::processEvents();

Original expression:
QVERIFY2(menu->isOpen(), "the retarget release reached the timeline")

A014 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:111
Disposition: GAP
Mapping/reason: Feature not yet ported: the post-release retarget selection has no Swift predicate because the note-menu retarget sequence is unported.
Source context since previous assertion:
;

Original expression:
QCOMPARE(view.selectionModel().noteSelection(), std::vector<NoteId>{seed->noteA.noteId})

A015 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:112
Disposition: GAP
Mapping/reason: Feature not yet ported: the retarget no-mutation guard belongs to the unported note-menu sequence.
Source context since previous assertion:
;

Original expression:
QVERIFY2(doc.smf().write() == before && doc.undoStack()->index() == undo &&
                 doc.undoStack()->count() == undoCount,
             "the retarget press/release mutated the song")

A016 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:139
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: locating a visible empty row outside the reopened menu frame is native frame geometry.
Source context since previous assertion:
;

    // The reopened menu anchors on the retargeted note; an outside right
    // press on a verified-empty visible row dismisses it without mutation.
    QQuickItem *const reopened = quick_popup::menuFrame(*menu);
    // The piano spans 0..127 in both directions from the anchor. Rows below
    // the anchor sit lower on screen and may hold the only unoccupied visible
    // area outside the reopened frame, so the scan is finite and
    // bidirectional, ordered by distance from the anchor.
    int clearKey = -1;
    QPointF clearLocal;
    for (int offset = 1; offset <= 127 && clearKey < 0; ++offset) {
        for (const int key : {seed->a.key + offset, seed->a.key - offset}) {
            if (key < 0 || key > 127)
                continue;
            const QPointF candidate(seed->a.center.x(), rows.centerY(key));
            const bool visible = rows.top(key) >= 0.0 && rows.bottom(key) <= roll.bounds().height();
            if (visible && !check.isOccupied(seed->a.tick, seed->a.dur, key) &&
                !reopened->contains(reopened->mapFromScene(roll.mapToScene(candidate)))) {
                clearKey = key;
                clearLocal = candidate;
                break;
            }
        }
    }

Original expression:
QVERIFY2(clearKey >= 0, "no visible empty row outside the reopened note menu")

A017 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:142
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: an empty-space right click must dismiss the note menu.
Source context since previous assertion:
;
    const QPoint clearPoint = roll.mapToScene(clearLocal).toPoint();
    QTest::mousePress(window, Qt::RightButton, Qt::NoModifier, clearPoint);

Original expression:
QTRY_VERIFY2(!menu->isOpen(), "empty-space right-click did not dismiss the note menu")

A018 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:145
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the dismissed release must not reopen a popup (native paired-event delivery).
Source context since previous assertion:
;
    QTest::mouseRelease(window, Qt::RightButton, Qt::NoModifier, clearPoint);
    QCoreApplication::processEvents();

Original expression:
QVERIFY2(!menu->isOpen(), "the dismissed release reopened a popup")

A019 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:146
Disposition: GAP
Mapping/reason: Feature not yet ported: the selection after the menu dismissal has no Swift predicate; the Swift roll hosts no note menu.
Source context since previous assertion:
;

Original expression:
QCOMPARE(view.selectionModel().noteSelection(), std::vector<NoteId>{seed->noteA.noteId})

A020 | PianoRollTest::velocityNoteMenuRetarget | src/checks/rollcheck/pencil_velocity.cpp:149
Disposition: GAP
Mapping/reason: Feature not yet ported: the dismissal no-mutation guard belongs to the unported note-menu sequence.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A021 | PianoRollTest::velocityNoteMenuStaleActivation | src/checks/rollcheck/pencil_velocity.cpp:160
Disposition: GAP
Mapping/reason: Feature not yet ported: the stale-activation scenario requires the QML note menu; the shared velocity seed guard has no predicate in this scenario.
Source context since previous assertion:
;
}

// The note menu's rows are projections of the canonical commands, so a
// document edit retires the open menu synchronously through
// contextMenusInvalidated instead of leaving a stale row to click. The
// dismissal must write nothing and leave the edit's undo entry intact.
void PianoRollTest::velocityNoteMenuStaleActivation()
{
    auto &check = *m_fixture;
    const std::optional<PencilVelocityFixture> seed = makeVelocitySeed(check);

Original expression:
QVERIFY(seed.has_value())

A022 | PianoRollTest::velocityNoteMenuStaleActivation | src/checks/rollcheck/pencil_velocity.cpp:174
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: a right press must open the Quick note menu before the stale edit.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const int undoCount = doc.undoStack()->count();
    const uint64_t revision = doc.revision();
    checks::events::sendMouse(roll, QEvent::MouseButtonPress, seed->b.center, Qt::RightButton,
                              Qt::RightButton, Qt::NoModifier);
    checks::events::sendMouse(roll, QEvent::MouseButtonRelease, seed->b.center, Qt::RightButton,
                              Qt::NoButton, Qt::NoModifier);
    QCoreApplication::processEvents();
    songview::QuickPopupSession *const menu = quick_popup::popupSession(view);

Original expression:
QVERIFY2(menu && menu->isOpen(), "right-click did not open the note menu")

A023 | PianoRollTest::velocityNoteMenuStaleActivation | src/checks/rollcheck/pencil_velocity.cpp:178
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native popup obligation: the document edit must dismiss the open note menu (native menu session lifetime).
Source context since previous assertion:
;

    doc.setNotesVelocity({seed->noteB}, 40);
    QCoreApplication::processEvents();

Original expression:
QVERIFY2(!menu->isOpen(), "the document edit did not dismiss the note menu")

A024 | PianoRollTest::velocityNoteMenuStaleActivation | src/checks/rollcheck/pencil_velocity.cpp:180
Disposition: GAP
Mapping/reason: Feature not yet ported: no Swift predicate asserts the stale menu activation committing the foreign edit's velocity 40 with its revision and undo-count/index increments; the Swift roll has no note-menu activation path.
Source context since previous assertion:
;
    DocNote stale;

Original expression:
QVERIFY2(doc.findNote(check.track(), seed->b.tick, uint8_t(seed->b.key), &stale) &&
                 stale.velocity == 40 && doc.revision() == revision + 1 &&
                 doc.undoStack()->count() == undoCount + 1 && doc.undoStack()->index() == undo + 1,
             "the note menu dismissal wrote to the song")

A025 | PianoRollTest::velocityNoteMenuStaleActivation | src/checks/rollcheck/pencil_velocity.cpp:186
Disposition: GAP
Mapping/reason: Feature not yet ported: the stale-activation no-mutation guard belongs to the unported note-menu sequence.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A026 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:193
Disposition: GAP
Mapping/reason: GAP — fixture seed guard (QVERIFY seed presence) with no executing Swift predicate; the Swift check builds its own synthetic velocity-pair fixture, so the guard fires only on Swift seeding failure and observes nothing on the passing path.
Source context since previous assertion:
;
}

void PianoRollTest::velocityDragCommit()
{
    auto &check = *m_fixture;
    const std::optional<PencilVelocityFixture> seed = makeVelocitySeed(check);

Original expression:
QVERIFY(seed.has_value())

A027 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:205
Disposition: MATCHED
Mapping: S006 (src/checks/rollcheck/selection.swift) asserts the modifier press pins the hover mark to the anchor (grabbed) note's row.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const uint64_t revision = doc.revision();
    const int undoCount = doc.undoStack()->count();
    checks::events::sendMouse(roll, QEvent::MouseButtonPress, seed->b.center, Qt::LeftButton,
                              Qt::LeftButton, Qt::ControlModifier);
    checks::events::sendMouse(roll, QEvent::MouseMove, seed->b.center - QPoint(0, 20), Qt::NoButton,
                              Qt::LeftButton, Qt::ControlModifier);

Original expression:
QCOMPARE(check.roll().property("hoverKey").toInt(), seed->b.key)

A028 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:208
Disposition: PARTIAL
Mapping/reason: PARTIAL — S007 (selection.swift) asserts the drag stages preview 78 before release, S008 (selection.swift) asserts the "Changing velocity" status, and S009 (selection.swift) asserts the document velocity is still 93 (no pre-release commit). Unproved: the original revision-unchanged and undoCount-unchanged conjuncts have no Swift predicate, and the held/preview values differ (73/93 native vs 93-seed/78-preview Swift).
Source context since previous assertion:
;
    const auto preview = view.previewVelocity(seed->noteB.noteId);
    DocNote held;

Original expression:
QVERIFY2(doc.findNote(check.track(), seed->b.tick, uint8_t(seed->b.key), &held) &&
                 held.velocity == 73 && preview && *preview == 93 && doc.revision() == revision &&
                 doc.undoStack()->count() == undoCount,
             "velocity drag moves must update preview without changing document history")

A029 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:215
Disposition: PARTIAL
Mapping/reason: S010 (selection.swift) asserts the released drag lands the anchor at 78, S011 (selection.swift) applies the same delta to the grouped note, S012 (selection.swift) preserves the selection and S013 (selection.swift) asserts exactly one new undo entry (undoCount == preCount + 1). Unproved: the exact revision +1 and the exact undo index +1 that this original expression also pins; the Swift predicate proves the count delta and the committed value.
Source context since previous assertion:
;
    checks::events::sendMouse(roll, QEvent::MouseButtonRelease, seed->b.center - QPoint(0, 20),
                              Qt::LeftButton, Qt::NoButton, Qt::ControlModifier);
    DocNote dragged;

Original expression:
QVERIFY2(doc.findNote(check.track(), seed->b.tick, uint8_t(seed->b.key), &dragged) &&
                 dragged.velocity == 93 && doc.revision() == revision + 1 &&
                 doc.undoStack()->count() == undoCount + 1 &&
                 !view.previewVelocity(seed->noteB.noteId),
             "velocity drag release must commit one batch and clear its preview")

A030 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:221
Disposition: GAP
Mapping/reason: Feature not yet ported: the drag-latch draw cell probe belongs to the unported latch (Swift draws always use the fixture latch of 100).
Source context since previous assertion:
;
    const Cell c = check.findFreeCell();

Original expression:
QVERIFY2(c.key >= 0, "no free grid cell for the drag-latch draw")

A031 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:224
Disposition: GAP
Mapping/reason: Feature not yet ported: the drag-latch draw produced-note observation is unreachable because the Swift roll never consumes a dragged velocity latch.
Source context since previous assertion:
;
    drawNote(roll, c.center);
    DocNote noteC;

Original expression:
QVERIFY2(doc.findNote(check.track(), c.tick, uint8_t(c.key), &noteC),
             "drag-latch draw produced no note")

A032 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:226
Disposition: GAP
Mapping/reason: Feature not yet ported: the drag-latched draw velocity (93) is unassertable; Swift pointer draws use the fixture latch of 100 (pencil.swift).
Source context since previous assertion:
;

Original expression:
QCOMPARE(noteC.velocity, 93)

A033 | PianoRollTest::velocityDragCommit | src/checks/rollcheck/pencil_velocity.cpp:229
Disposition: PARTIAL
Mapping/reason: S014 (selection.swift) asserts one undo restores both fixture velocities at once and S015 (selection.swift, selectionRestore) asserts the drags unwind to the pre-seed MIDI bytes and history identity. Unproved: the drag-latch draw that this site's comparison also unwinds; the latch is not ported.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A034 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:236
Disposition: MATCHED
Mapping: S001 (src/checks/rollcheck/interlock.swift) asserts the two-note A/B velocity fixture seeded as exactly two notes, the Swift counterpart of makeVelocitySeed.
Source context since previous assertion:
;
}

void PianoRollTest::velocityCancelUngrab()
{
    auto &check = *m_fixture;
    const std::optional<PencilVelocityFixture> seed = makeVelocitySeed(check);

Original expression:
QVERIFY(seed.has_value())

A035 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:247
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/pencil_velocity.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const int undoCount = doc.undoStack()->count();
    const uint64_t revision = doc.revision();
    auto *quick =
        view.findChild<songview::TimelineQuickView *>(QStringLiteral("timelineQuickCanvas"));
    QQuickWindow *const window = quick ? quick->quickWindow() : nullptr;

Original expression:
QVERIFY2(window, "no Quick window for mouse-ungrab cancellation")

A036 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:248
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/pencil_velocity.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

Original expression:
QVERIFY2(QTest::qWaitForWindowExposed(window),
             "Quick window did not become exposed for the mouse-ungrab cancellation drag")

A037 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:252
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Original native event priming: primeMouseMove must stage the real drag before the ungrab. Host event-delivery obligation.
Source context since previous assertion:
;
    const QPoint pressPosition = roll.mapToScene(QPointF(seed->b.center)).toPoint();
    const QPoint dragPosition = roll.mapToScene(QPointF(seed->b.center - QPoint(0, 15))).toPoint();

Original expression:
QVERIFY2(checks::events::primeMouseMove(*window, roll, pressPosition),
             "could not prime the mouse-ungrab cancellation drag")

A038 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:256
Disposition: MATCHED
Mapping: S002 (src/checks/rollcheck/interlock.swift) asserts the Ctrl-left drag stages a velocity preview, and S004 (interlock.swift) asserts the MIDI bytes, undo index and undo count are unchanged while it is staged. The Swift abort stimulus is the right press/release pair rather than QEvent::UngrabMouse.
Source context since previous assertion:
;
    QTest::mousePress(window, Qt::LeftButton, Qt::ControlModifier, pressPosition);
    QTest::mouseMove(window, dragPosition);

Original expression:
QVERIFY2(view.previewVelocity(seed->noteB.noteId).has_value() && doc.revision() == revision &&
                 doc.undoStack()->index() == undo && doc.undoStack()->count() == undoCount,
             "mouse-ungrab cancellation must stage its changed velocity preview")

A039 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:262
Disposition: PARTIAL
Mapping/reason: S003 (interlock.swift) asserts the staged preview is aborted and S004 (interlock.swift) asserts no bytes/index/count change; S028 (src/checks/editorqml/tst_ShellGridInput.qml) runs the production inputCancelled(1) ungrab slot and asserts noteSummary restoration with reason 1. Unproved: a QEvent::UngrabMouse delivered to a live velocity drag (QML cannot synthesize UngrabMouse; the shell lane exercises the band, not the velocity drag).
Source context since previous assertion:
;
    roll.ungrabMouse();
    const bool cancelledOnUngrab = !view.previewVelocity(seed->noteB.noteId).has_value();
    QTest::mouseRelease(window, Qt::LeftButton, Qt::ControlModifier, dragPosition);

Original expression:
QVERIFY2(cancelledOnUngrab && !view.previewVelocity(seed->noteB.noteId) &&
                 doc.revision() == revision && doc.undoStack()->index() == undo &&
                 doc.undoStack()->count() == undoCount,
             "mouse ungrab must cancel the local velocity drag without mutation")

A040 | PianoRollTest::velocityCancelUngrab | src/checks/rollcheck/pencil_velocity.cpp:266
Disposition: MATCHED
Mapping: S004 (src/checks/rollcheck/interlock.swift) asserts the aborted velocity drag preserves the MIDI bytes, undo index and undo count for the slot.
Source context since previous assertion:
;

Original expression:
QCOMPARE(doc.smf().write(), before)

A041 | PianoRollTest::velocityDoubleClickDelete | src/checks/rollcheck/pencil_velocity.cpp:273
Disposition: GAP
Mapping/reason: GAP — fixture seed guard (QVERIFY seed presence) with no executing Swift predicate; the Swift check builds its own synthetic velocity fixture, so the guard fires only on Swift seeding failure and observes nothing on the passing path.
Source context since previous assertion:
;
}

void PianoRollTest::velocityDoubleClickDelete()
{
    auto &check = *m_fixture;
    const std::optional<PencilVelocityFixture> seed = makeVelocitySeed(check);

Original expression:
QVERIFY(seed.has_value())

A042 | PianoRollTest::velocityDoubleClickDelete | src/checks/rollcheck/pencil_velocity.cpp:279
Disposition: GAP
Mapping/reason: GAP — free-cell seed guard (QVERIFY2 c.key >= 0) with no executing Swift predicate; the Swift check locates its own synthetic delete cell, so the guard fires only on Swift seeding failure and observes nothing on the passing path.
Source context since previous assertion:
;
    SongDocument &doc = check.document();
    auto &roll = check.rollInput();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const Cell c = check.findFreeCell();

Original expression:
QVERIFY2(c.key >= 0, "no free grid cell for the double-click delete")

A043 | PianoRollTest::velocityDoubleClickDelete | src/checks/rollcheck/pencil_velocity.cpp:286
Disposition: MATCHED
Mapping: S019 (src/checks/rollcheck/pencil.swift) asserts the double press deletes the note under the pointer (session.document.note(id) == nil).
Source context since previous assertion:
;
    drawNote(roll, c.center);
    checks::events::sendMouse(roll, QEvent::MouseButtonDblClick, c.center, Qt::LeftButton,
                              Qt::LeftButton, Qt::NoModifier);
    checks::events::sendMouse(roll, QEvent::MouseButtonRelease, c.center, Qt::LeftButton,
                              Qt::NoButton, Qt::NoModifier);
    DocNote deleted;

Original expression:
QVERIFY2(!doc.findNote(check.track(), c.tick, uint8_t(c.key), &deleted),
             "double-click on a note did not delete it")

A044 | PianoRollTest::velocityDoubleClickDelete | src/checks/rollcheck/pencil_velocity.cpp:290
Disposition: MATCHED
Mapping: S016 (src/checks/rollcheck/pencil.swift, pencilRestore) drains the double-click sequence and asserts the restored serialized MIDI bytes equal the pre-draw bytes and the history identity is restored.
Source context since previous assertion:
;
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

A045 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:304
Disposition: GAP
Mapping/reason: GAP — free-cell seed guard (QVERIFY2 cell.key >= 0) with no executing Swift predicate; the Swift check locates its own synthetic lifecycle cell, so the guard fires only on Swift seeding failure and observes nothing on the passing path.
Source context since previous assertion:
;
}

void PianoRollTest::quickLifecycle()
{
    auto &check = *m_fixture;
    SongDocument &doc = check.document();
    SongView &view = check.view();
    auto &rollInput = check.rollInput();
    auto &rollGutterInput = check.rollGutterInput();
    const int track = check.track();
    const QByteArray before = doc.smf().write();
    const int undo = doc.undoStack()->index();
    const Cell cell = check.findFreeCell();

Original expression:
QVERIFY2(cell.key >= 0, "no free grid cell for the Quick lifecycle scenarios")

A046 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:307
Disposition: MATCHED
Mapping: S017 (src/checks/rollcheck/pencil.swift) asserts the pointer draw creates a note at the selected cell — the lifecycle-draw observation of this site.
Source context since previous assertion:
;
    drawNote(rollInput, cell.center);
    DocNote note;

Original expression:
QVERIFY2(doc.findNote(track, cell.tick, uint8_t(cell.key), &note),
             "lifecycle pencil draw produced no note")

A047 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:318
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/pencil_velocity.cpp) no longer builds or runs. Previous native note: Retained native host-input obligation: pressing the roll through the Quick window must grant the roll input focus. The shell lane forces focus (tst_ShellGridInput.qml/421) rather than asserting the press-grants-focus chain, so no executing predicate covers it.
Source context since previous assertion:
;
    view.selectionModel().setNoteSelection({note.noteId});
    QCoreApplication::processEvents();
    sendWindowMouse(view, rollInput, QEvent::MouseButtonPress, QPointF(cell.center), Qt::LeftButton,
                    Qt::LeftButton, Qt::NoModifier);
    QCoreApplication::processEvents();
    const bool focused = rollInput.hasFocus();
    sendWindowMouse(view, rollInput, QEvent::MouseButtonRelease, QPointF(cell.center),
                    Qt::LeftButton, Qt::NoButton, Qt::NoModifier);
    QCoreApplication::processEvents();

Original expression:
QVERIFY2(focused,
             "pressing the roll through the Quick window did not focus the roll input item")

A048 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:320
Disposition: MATCHED
Mapping: S027 (src/checks/editorqml/tst_ShellGridInput.qml) presses a note with a real left click on the production shell and asserts the note becomes selected (noteById(target.id).selected).
Source context since previous assertion:
;

Original expression:
QCOMPARE(view.selectionModel().noteSelection(), std::vector<NoteId>{note.noteId})

A049 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:347
Disposition: MATCHED
Mapping: S021 (src/checks/rollcheck/pencil.swift) asserts the pointerUngrabReason cancellation clears the staged draw preview, leaves no interaction active, records the reason and changes no document revision.
Source context since previous assertion:
;

    const auto cancellationLeavesNoMutation = [&](const auto &cancel) {
        const QPointF press(cell.center);
        const uint64_t revision = doc.revision();
        const int index = doc.undoStack()->index();
        const int count = doc.undoStack()->count();
        sendWindowMouse(view, rollInput, QEvent::MouseButtonPress, press, Qt::LeftButton,
                        Qt::LeftButton, Qt::ControlModifier);
        sendWindowMouse(view, rollInput, QEvent::MouseMove, press - QPointF(0, 10), Qt::NoButton,
                        Qt::LeftButton, Qt::ControlModifier);
        QCoreApplication::processEvents();
        const auto staged = view.previewVelocity(note.noteId);
        if (!staged || *staged == note.velocity || doc.revision() != revision ||
            doc.undoStack()->index() != index || doc.undoStack()->count() != count)
            return false;
        cancel();
        QCoreApplication::processEvents();
        sendWindowMouse(view, rollInput, QEvent::MouseButtonRelease, press - QPointF(0, 10),
                        Qt::LeftButton, Qt::NoButton, Qt::ControlModifier);
        QCoreApplication::processEvents();
        DocNote after;
        return doc.revision() == revision && doc.undoStack()->index() == index &&
               doc.undoStack()->count() == count && !view.previewVelocity(note.noteId) &&
               doc.findNote(track, cell.tick, uint8_t(cell.key), &after) &&
               after.velocity == note.velocity;
    };

Original expression:
QVERIFY2(cancellationLeavesNoMutation([&] { rollInput.ungrabMouse(); }),
             "pointer ungrab must cancel the velocity drag without mutation")

A050 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:349
Disposition: MATCHED
Mapping: S021 (src/checks/rollcheck/pencil.swift) asserts the windowDeactivated cancellation clears the staged draw preview, leaves no interaction active, records the reason and changes no document revision.
Source context since previous assertion:
;

Original expression:
QVERIFY2(cancellationLeavesNoMutation([&] {
                 QEvent deactivate(QEvent::WindowDeactivate);
                 auto *quick = view.findChild<songview::TimelineQuickView *>(
                     QStringLiteral("timelineQuickCanvas"));
                 if (quick && quick->quickWindow())
                     QCoreApplication::sendEvent(quick->quickWindow(), &deactivate);
             }),
             "window deactivation must cancel the velocity drag without mutation")

A051 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:357
Disposition: MATCHED
Mapping: S021 (src/checks/rollcheck/pencil.swift) asserts the focusLost cancellation clears the staged draw preview, leaves no interaction active, records the reason and changes no document revision.
Source context since previous assertion:
;

Original expression:
QVERIFY2(cancellationLeavesNoMutation([&] {
                 QFocusEvent focusOut(QEvent::FocusOut, Qt::OtherFocusReason);
                 QCoreApplication::sendEvent(&rollInput, &focusOut);
             }),
             "focus loss must cancel the velocity drag without mutation")

A052 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:369
Disposition: PARTIAL
Mapping/reason: PARTIAL — S024 (src/checks/rollcheck/resize.swift) asserts the note's right edge grip publishes cursorKind 3, S025 (resize.swift) the left edge grip, and S026 (resize.swift) asserts the note body publishes cursorKind 0 (arrow). Unproved: the host QCursor pixmap/shape chain (non-null pixmap, ArrowCursor shape) has no Swift predicate; cursorKind zones are an analogous model, not the same observation.
Source context since previous assertion:
;
    const qreal edgeX =
        view.camera().displayX(double(note.tick), 0.0, rollInput.devicePixelRatio()) - 1.0;
    checks::events::sendMouse(rollInput, QEvent::MouseMove, QPointF(edgeX, cell.center.y()),
                              Qt::NoButton, Qt::NoButton, Qt::NoModifier);
    const bool edgeCursor = !rollInput.cursor().pixmap().isNull();
    checks::events::sendMouse(rollGutterInput, QEvent::MouseMove, QPointF(1, cell.center.y()),
                              Qt::NoButton, Qt::NoButton, Qt::NoModifier);

Original expression:
QVERIFY2(edgeCursor && rollInput.cursor().shape() == Qt::ArrowCursor,
             "host cursor publication did not track the note edge and arrow zones")

A053 | PianoRollTest::quickLifecycle | src/checks/rollcheck/pencil_velocity.cpp:375
Disposition: MATCHED
Mapping: S021 (src/checks/rollcheck/pencil.swift) asserts each lifecycle cancellation changes no document revision, and S016 (pencil.swift, pencilRestore) restores the pre-draw MIDI bytes and history position for the slot.
Source context since previous assertion:
;
    checks::events::sendMouse(rollInput, QEvent::Leave, QPointF(1, cell.center.y()), Qt::NoButton,
                              Qt::NoButton, Qt::NoModifier);
    while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
        doc.undoStack()->undo();

Original expression:
QCOMPARE(doc.smf().write(), before)

Remaining original source (closing sequence and helper bodies):
;
}

Swift assertion predicates
The S entries are exact report.expect/fail predicates already executed by the swiftcore
projectSession suite (selection.swift, pencil.swift, interlock.swift, resize.swift) or by
the shell-grid-input lane (tst_ShellGridInput.qml). deno task verify --filter swiftcore
--verbose PASS 1/14 ok and deno task verify:shell --verbose PASS 11/11 (both
controller-recorded).

S001 | checkGestureInterlock | src/checks/rollcheck/interlock.swift
Anchor: message "A001 velocity fixture seeds notes A and B"
  report.expect(seeded?.count == 2, cppID: id,
                message: "A001 velocity fixture seeds notes A and B")

S002 | checkGestureInterlock | src/checks/rollcheck/interlock.swift
Anchor: function
  report.expect(grid.previewVelocity(seeded[1]) != nil, cppID: id,
                message: attempt == 0 ? "A014 Ctrl velocity drag stages a preview" :
                    "A016 later Ctrl velocity drag stages a preview")

S003 | checkGestureInterlock | src/checks/rollcheck/interlock.swift
Anchor: function
  report.expect(grid.previewVelocity(seeded[1]) == nil, cppID: id,
                message: attempt == 0 ? "A015 right release aborts velocity preview" :
                    "A017 second right release aborts velocity preview")

S004 | checkGestureInterlock | src/checks/rollcheck/interlock.swift
Anchor: message "A018 velocity aborts preserve MIDI bytes and undo history"
  report.expect(unchanged(before), cppID: id,
                message: "A018 velocity aborts preserve MIDI bytes and undo history")

S005 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: function
  report.fail(id, "could not seed the velocity-drag note pair")

S006 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "the modifier press pins the hover mark to the anchor row"
  report.expect(session.document.note(seed.ids[0]).map { grid.hoverKey == Int($0.pitch) } == true,
                cppID: id, message: "the modifier press pins the hover mark to the anchor row")

S007 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "the drag previews 78 before release"
  report.expect(grid.previewVelocity(seed.ids[0]) == 78, cppID: id,
                message: "the drag previews 78 before release")

S008 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "the velocity drag publishes its preview status"
  report.expect(grid.statusText.contains("Changing velocity"), cppID: id,
                message: "the velocity drag publishes its preview status")

S009 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "the velocity preview commits nothing before release"
  report.expect(session.document.note(seed.ids[0]).map { Int($0.velocity) } == 93, cppID: id,
                message: "the velocity preview commits nothing before release")

S010 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "a 15px modifier drag lands the anchor at 78 from 93"
  report.expect(session.document.note(seed.ids[0]).map { Int($0.velocity) } == 78, cppID: id,
                message: "a 15px modifier drag lands the anchor at 78 from 93")

S011 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "the grouped drag applies the same delta to the other selected note"
  report.expect(session.document.note(seed.ids[1]).map { Int($0.velocity) } == 85, cppID: id,
                message: "the grouped drag applies the same delta to the other selected note")

S012 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "a grouped velocity drag preserves the selected notes"
  report.expect(Set(session.selectedNoteOrder) == Set(seed.ids), cppID: id,
                message: "a grouped velocity drag preserves the selected notes")

S013 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "one grouped velocity drag commits one undo entry"
  report.expect(session.document.history.undoCount == preCount + 1, cppID: id,
                message: "one grouped velocity drag commits one undo entry")

S014 | checkGroupedVelocityDrag | src/checks/rollcheck/selection.swift
Anchor: message "undo restores both fixture velocities at once"
  report.expect(session.document.history.undoDocument()
      && session.document.history.currentIdentity == plantedIdentity
      && (try? session.document.captureSave().bytes) == plantedBytes, cppID: id,
      message: "undo restores both fixture velocities at once")

S015 | selectionRestore helper (shared by selection.swift checks) | src/checks/rollcheck/selection.swift
Anchor: function
  report.expect(document.history.currentIdentity == baseline.identity
      && restored.bytes == baseline.bytes, cppID: id, message: message)

S016 | pencilRestore helper (shared by pencil.swift checks) | src/checks/rollcheck/pencil.swift
Anchor: message "undo restores the original SMF bytes and history position"
  report.expect(document.history.currentIdentity == baseline.identity
                    && restored.bytes == baseline.bytes,
                cppID: id, message: "undo restores the original SMF bytes and history position")

S017 | checkPencilPlacement | src/checks/rollcheck/pencil.swift
Anchor: message "pointer draw creates a note at the selected cell"
  report.expect(note != nil, cppID: id, message: "pointer draw creates a note at the selected cell")

S018 | checkVelocityDoubleClickDelete | src/checks/rollcheck/pencil.swift
Anchor: message "a free cell accepts the note to delete"
  report.expect(drawn != nil, cppID: id, message: "a free cell accepts the note to delete")

S019 | checkVelocityDoubleClickDelete | src/checks/rollcheck/pencil.swift
Anchor: message "double press deletes the note under the pointer"
  report.expect(drawn.map { session.document.note($0.id) == nil } == true,
                cppID: id, message: "double press deletes the note under the pointer")

S020 | checkPointerDrawCancellation | src/checks/rollcheck/pencil.swift
Anchor: function
  report.fail(id, "no free grid cell for the pointer lifecycle")

S021 | checkPointerDrawCancellation | src/checks/rollcheck/pencil.swift
Anchor: message "\(reason) discards the staged draw without a document write"
  report.expect(preview && grid.scene.pianoDrawPreviewFill.count == 0
                    && !grid.interactionActive && grid.lastCancelReason == reason.rawValue
                    && session.document.revision == revision,
                cppID: id,
                message: "\(reason) discards the staged draw without a document write")

S022 | checkVelocityDoubleClickDelete | src/checks/rollcheck/pencil.swift
Anchor: function
  report.fail(id, "could not seed the first velocity fixture note")

S023 | checkVelocityDoubleClickDelete | src/checks/rollcheck/pencil.swift
Anchor: function
  report.fail(id, "could not seed the second velocity note or locate a free delete cell")

S024 | checkResizeHoverCursor | src/checks/rollcheck/resize.swift
Anchor: message "the right edge grip publishes the horizontal resize cursor"
  report.expect(grid.cursorKind == 3, cppID: id,
                message: "the right edge grip publishes the horizontal resize cursor")

S025 | checkResizeHoverCursor | src/checks/rollcheck/resize.swift
Anchor: message "the left edge grip publishes the horizontal resize cursor"
  report.expect(grid.cursorKind == 3, cppID: id,
                message: "the left edge grip publishes the horizontal resize cursor")

S026 | checkResizeHoverCursor | src/checks/rollcheck/resize.swift
Anchor: message "the note body publishes the arrow cursor"
  report.expect(grid.cursorKind == 0, cppID: id,
                message: "the note body publishes the arrow cursor")

S027 | test_escapeCancel idle click | src/checks/editorqml/tst_ShellGridInput.qml
Anchor: message "idle click selects the note"
  A real left click on the production roll selects the note (noteById(target.id).selected);
  the same slot later forces roll focus and asserts roll.activeFocus.

S028 | test_ungrabCancel | src/checks/editorqml/tst_ShellGridInput.qml
Anchor: function
  Real right press/move plus the production grid.inputCancelled(1) slot restores
grid.noteSummary with lastCancelReason 1 (QML cannot synthesize QEvent::UngrabMouse).
