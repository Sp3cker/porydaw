Assertion correspondence: rollcheck/identity.cpp

Reference revision: 85b97239ce94dc4c4cf5f3f4fb66fc96ff2cf27e
Original: src/checks/rollcheck/identity.cpp
Original SHA-256: 14b6f45a36b2cf769bc6c886c20980ef133de0ba504524ab8a14c7416813bddc
Swift counterpart: src/checks/rollcheck/identity.swift
Additional Swift counterpart: src/checks/rollcheck/presentation.swift
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

Disposition tally: 14 sites: 5 MATCHED, 0 PARTIAL, 6 GAP, 3 NATIVE.

Command: deno task verify --filter swiftcore --verbose
Result: PASS — 1/14 ok (controller-recorded post-change run); the swiftcore
projectSession suite executed runIdentityChecks and the S001-S005 predicates.
Command: deno task verify:qml-roll --verbose
Result: PASS — 1/1 ok (controller-recorded post-change run); no identity site
depends on the roll QML lane.
Command: deno task verify:shell --verbose
Result: PASS — 11/11 ok (controller-recorded post-change run); no identity site
depends on the shell lane. The shell and roll QML runs predate only a
swiftcore-checks-only commit; no production or QML source changed after them.

PianoRollTest::timelineProjection and PianoRollTest::viewStateRoundTrip slots have
no counterpart in the Swift roll lane and stay GAP below. Shared fixture:
the DocumentSession supplied by the projectSession suite; the original slot seeded
its own LoadedSong/SongView from the staged project root.

A001 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:27
Disposition: RETIRED-REPRESENTATION

A002 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:32
Disposition: RETIRED-REPRESENTATION

A003 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:55
Disposition: MATCHED
Mapping: S001

A004 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:61
Disposition: RETIRED-REPRESENTATION

A005 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:74
Disposition: MATCHED
Mapping: S002

A006 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:97
Disposition: MATCHED
Mapping: S003

A007 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:109
Disposition: MATCHED
Mapping: S004

A008 | PianoRollTest::duplicateNoteIdentity | src/checks/rollcheck/identity.cpp:117
Disposition: PARTIAL
Mapping: S005 selects both duplicates, switches the active track header and asserts the duplicate-note selection clears, but like the original it is guarded by a second used track and the single-track fixture never enters that branch, so the predicate does not execute; unproved condition: the track-switch clear on a multi-track song.
Source context since previous assertion:
      }

      identityView.selectionModel().setNoteSelection({duplicates[0].noteId, duplicates[1].noteId});
      const int otherTrack = track == 0 ? 1 : 0;
      if (otherTrack < projectionDoc.engineTrackCount()) {
          identityView.trackHeaderClicked(otherTrack, Qt::NoModifier);
          if (!identityView.selectionModel().noteSelection().empty())
Original expression:
  QFAIL("switching track headers did not clear note selection");

A009 | PianoRollTest::timelineProjection | src/checks/rollcheck/identity.cpp:131
Disposition: GAP
Mapping/reason: Feature not yet ported: no executing Swift roll predicate projects a hand-built MidiTimeline with an unassigned note ID. DocumentProjectionCache and NoteProjection are internal (no public projection seam) and TimeAxis is a coordinate axis, so the ordinary-timeline note-count observable has no equivalent assertion.
Source context since previous assertion:
      }
  }

  void PianoRollTest::timelineProjection()
  {
      MidiTimeline ordinaryTimeline;
      ordinaryTimeline.lengthTicks = 288;
      ordinaryTimeline.events = {
          {0, 240, 0x9, 2, 65, 83, NoteId{}},
          {0, 288, 0x8, 2, 65, 0, NoteId{}},
      };
      const SongViewModel ordinary = buildSongViewModel(ordinaryTimeline);
      if (ordinary.notes.size() != 1) {
Original expression:
  QFAIL("ordinary unassigned timeline note did not project");

A010 | PianoRollTest::timelineProjection | src/checks/rollcheck/identity.cpp:137
Disposition: GAP
Mapping/reason: Feature not yet ported: the same unported projection step. No executing Swift predicate asserts the projected note's unassigned ID, startTick 240, endTick 288, key 65, velocity 83 and track 2.
Source context since previous assertion:
      }

      const ViewNote &note = ordinary.notes.front();
      if (note.noteId.isAssigned() || note.startTick != 240 || note.endTick() != 288 ||
          note.key != 65 || note.velocity != 83 || note.track != 2) {
Original expression:
  QFAIL("ordinary unassigned timeline note changed during projection");

A011 | PianoRollTest::viewStateRoundTrip | src/checks/rollcheck/identity.cpp:155
Disposition: GAP
Mapping/reason: Feature not yet ported: the typed cosmetic EditorViewState (laneHeight, per-lane heights and ranges, empty lanes) has no Swift type and no predicate asserts its retention.
Source context since previous assertion:
      }
  }

  void PianoRollTest::viewStateRoundTrip()
  {
      SongView &view = m_fixture->view();
      songview::TimelineInputItem &rollInput = m_fixture->rollInput();
      const SongView::ViewState before = view.viewState();
      const int track = m_fixture->track();
      const EditorAutomationRowId lane{EditorAutomationRowKind::ControlChange, uint8_t(track), 7};
      EditorViewState cosmetics;
      cosmetics.laneHeight = 64;
      cosmetics.laneHeights.emplace(lane, 96);
      cosmetics.laneRanges.emplace(lane, 91);
      cosmetics.emptyLanes.emplace(lane);
      view.applyEditorViewState(cosmetics);
      if (view.editorViewState() != cosmetics)
Original expression:
  QFAIL("SongView did not retain typed cosmetic EditorViewState");

A012 | PianoRollTest::viewStateRoundTrip | src/checks/rollcheck/identity.cpp:215
Disposition: GAP
Mapping/reason: The perturbation guard spans SongView::ViewState fields (pxPerBeat, keyHeight, scroll, selectedTrack, editCursorTick, gridSelection, gridTriplet, eventList) that the Swift EditorCamera value type does not model; no counterpart predicate asserts the guard.
Source context since previous assertion:
      view.setEditCursorTick(view.grid().snapTick(96.0));
      const SongView::ViewState snapshot = view.viewState();
      SongView::ViewState perturbed = snapshot;
      perturbed.pxPerBeat = snapshot.pxPerBeat < 64.0 ? 64.0 : 16.0;
      perturbed.keyHeight = snapshot.keyHeight < 16.0 ? 16.0 : 8.0;
      perturbed.scrollPx = snapshot.scrollPx == 0.0 ? 1.0 : 0.0;
      perturbed.scrollY = snapshot.scrollY == 0.0 ? 1.0 : 0.0;
      int alternateTrack = snapshot.selectedTrack;
      for (int candidate = 0; candidate < 16; ++candidate) {
          if (candidate != snapshot.selectedTrack && m_fixture->timeline().tracks[candidate].used) {
              alternateTrack = candidate;
              break;
          }
      }
      perturbed.selectedTrack = alternateTrack;
      perturbed.editCursorTick = snapshot.editCursorTick == 0 ? 1 : 0;
      perturbed.gridSelection = snapshot.gridSelection == songview::GridSelection::musical(4)
                                    ? songview::GridSelection::musical(8)
                                    : songview::GridSelection::musical(4);
      perturbed.gridTriplet = !snapshot.gridTriplet;
      perturbed.eventList = !snapshot.eventList;
      SongView::ViewState expected = perturbed;
      expected.pxPerBeat = std::clamp(perturbed.pxPerBeat, 4.0, 640.0);
      expected.keyHeight = std::clamp(perturbed.keyHeight, 4.0, 32.0);
      const double tpb = double(m_fixture->timeline().ticksPerBeat);
      const double maxHScroll =
          std::max(0.0, double(m_fixture->timeline().lengthTicks) * expected.pxPerBeat / tpb + 100.0 -
                            std::max<qreal>(50, rollInput.width()));
      expected.scrollPx = std::clamp(perturbed.scrollPx, 0.0, maxHScroll);
      const double maxRollScroll = std::max(0.0, 128.0 * expected.keyHeight - rollInput.height());
      expected.scrollY = std::clamp(perturbed.scrollY, 0.0, maxRollScroll);
      expected.selectedTrack = snapshot.selectedTrack;
      if (perturbed.selectedTrack >= 0 && perturbed.selectedTrack < 16 &&
          m_fixture->timeline().tracks[perturbed.selectedTrack].used) {
          expected.selectedTrack = perturbed.selectedTrack;
      }
      expected.editCursorTick = std::min(perturbed.editCursorTick, m_fixture->timeline().lengthTicks);
      expected.gridSelection = perturbed.gridSelection;
      view.applyViewState(perturbed);
      const SongView::ViewState applied = view.viewState();
      const auto sameViewState = [](const SongView::ViewState &lhs, const SongView::ViewState &rhs) {
          return lhs.valid == rhs.valid && std::abs(lhs.pxPerBeat - rhs.pxPerBeat) <= 1e-12 &&
                 std::abs(lhs.keyHeight - rhs.keyHeight) <= 1e-12 &&
                 std::abs(lhs.scrollPx - rhs.scrollPx) <= 1e-12 &&
                 std::abs(lhs.scrollY - rhs.scrollY) <= 1e-12 &&
                 lhs.selectedTrack == rhs.selectedTrack && lhs.editCursorTick == rhs.editCursorTick &&
                 lhs.gridSelection == rhs.gridSelection && lhs.gridTriplet == rhs.gridTriplet &&
                 lhs.eventList == rhs.eventList;
      };
      const auto differs = [](double lhs, double rhs) { return std::abs(lhs - rhs) > 1e-12; };
      if (!differs(expected.pxPerBeat, snapshot.pxPerBeat) ||
          !differs(expected.keyHeight, snapshot.keyHeight) ||
          !differs(expected.scrollPx, snapshot.scrollPx) ||
          !differs(expected.scrollY, snapshot.scrollY) ||
          expected.selectedTrack == snapshot.selectedTrack ||
          expected.editCursorTick == snapshot.editCursorTick ||
          expected.gridSelection == snapshot.gridSelection ||
          expected.gridTriplet == snapshot.gridTriplet || expected.eventList == snapshot.eventList) {
Original expression:
  QFAIL("ViewState perturbation did not change every retained field");

A013 | PianoRollTest::viewStateRoundTrip | src/checks/rollcheck/identity.cpp:218
Disposition: GAP
Mapping/reason: Feature not yet ported: applyViewState normalization and clamping over the whole ViewState has no Swift counterpart; EditorCamera exposes setHScroll/setVScroll/updateViewport with its own reclamp and no predicate asserts a full apply-viewstate retention.
Source context since previous assertion:
      }
      if (!sameViewState(applied, expected))
Original expression:
  QFAIL("ViewState apply did not retain every normalized perturbed field");

A014 | PianoRollTest::viewStateRoundTrip | src/checks/rollcheck/identity.cpp:223
Disposition: GAP
Mapping/reason: Feature not yet ported: EditorCamera is a value type with no view-state round-trip; no Swift predicate captures, perturbs, restores and compares a full SongView::ViewState with its typed cosmetic EditorViewState.
Source context since previous assertion:
      view.applyViewState(snapshot);
      const SongView::ViewState restored = view.viewState();
      if (view.editorViewState() != cosmetics || !sameViewState(restored, snapshot))
Original expression:
  QFAIL("ViewState capture/apply did not restore runtime state without changing cosmetics");

Remaining original source (closing sequence and helper bodies):
      view.applyViewState(before);
  }

Counterpart executable predicate index
S001..S005 are report.expect predicates in the registered swiftcore check identity.swift,
executed by deno task verify --filter swiftcore --verbose (PASS, 1/14 ok,
controller-recorded). Their presence alone does not close any A entry.

S001 | checkDuplicateNoteIdentity | src/checks/rollcheck/identity.swift
Anchor: message "equal-visible notes receive distinct assigned IDs"

S002 | checkDuplicateNoteIdentity | src/checks/rollcheck/identity.swift
Anchor: message "plain active-track header clears note selection"

S003 | checkDuplicateNoteIdentity | src/checks/rollcheck/identity.swift
Anchor: message "one-ID pitch edit leaves the other duplicate untouched and selected ID stable"

S004 | checkDuplicateNoteIdentity | src/checks/rollcheck/identity.swift
Anchor: message "undo restores both duplicate values without replacing their identities"

S005 | checkDuplicateNoteIdentity | src/checks/rollcheck/identity.swift
Anchor: message "switching track headers clears duplicate-note selection"
