Assertion correspondence: rollcheck/static/camera.cpp

Reference revision: f3069ef693542bdb63564b80a29773e2f5b2a360
Original: src/checks/rollcheck/static/camera.cpp
Original SHA-256: cd54f262cc3b30c64e2b1ff8ee7bc1b843c4cad469b58b5aea08c7ac4644fe78
Swift counterpart: src/checks/rollcheck/static/camera.swift
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift counterpart: src/checks/rollcheck/keyboard.swift
Additional Swift counterpart: src/checks/rollcheck/note_commands.swift
Additional Swift counterpart: src/checks/rollcheck/pencil.swift
Additional Swift counterpart: src/checks/workspace/session_playback.swift
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftcore --verbose
Result: PASS; 1/14 ok (controller run recorded before this ledger edit; the edit changes no Swift source).
Command: deno task verify:qml-roll --verbose
Result: PASS; 1/1 ok (controller run recorded before this ledger edit; the edit changes no QML source).
Command: deno task verify:shell --verbose
Result: PASS; 11/11 ok (controller run recorded before this ledger edit; the edit changes no shell source).
Verified Swift SHA-256: 95fb2e80f95bf57e41250632cb8b3e7378fa32be143d02f5bd092313f6029808
Verified QML SHA-256: 7a11ea7ca9a6f0450bdc2db5ef318ffedd325c4d527d488a4dded0349dfd5397 (src/checks/rollqml/tst_TimelinePan.qml); 4b6f582c6bf9e4ee8b15166f6e631167171421b1a454b413198c398531aef519 (src/checks/rollqml/tst_SwiftRollPlots.qml)
Narrow form used while iterating: deno task verify --filter swiftcore --qt projectSession.

A001 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:70
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(NaN, 100.0) returns an empty window.
Source context since previous assertion:
  #include "checks/rollcheck/static/tst_pianorollstatic.h"

  #include <QCoreApplication>
  #include <QEvent>
  #include <QImage>
  #include <QtTest>

  #include <cmath>
  #include <cstdint>
  #include <limits>
  #include <vector>

  #include "checks/rollcheck/static/fixtures.h"
  #include "checks/support/eventsynth.h"
  #include "checks/support/quickframebuffer.h"
  #include "core/songdocument.h"
  #include "ui/songtab.h"
  #include "ui/songview.h"
  #include "ui/songview/detail.h"
  #include "ui/songview/pianoroll.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"

  namespace checks::rollcheck::staticcheck {
  namespace {

  int keyAt(const SongView &view, qreal y)
  {
      const qreal dpr = view.quickView() ? view.quickView()->quickDevicePixelRatio() : 1.0;
      for (int row = 0; row < 128; ++row) {
          const qreal edge =
              std::round((double(row + 1) * view.camera().keyHeight() - view.camera().scrollY()) *
                         dpr) /
              dpr;
          if (y < edge)
              return 127 - row;
      }
      return 0;
  }

  qreal rowCenter(const SongView &view, int key)
  {
      const qreal dpr = view.quickView() ? view.quickView()->quickDevicePixelRatio() : 1.0;
      const auto edge = [&view, dpr](int row) {
          return std::round((double(row) * view.camera().keyHeight() - view.camera().scrollY()) *
                            dpr) /
                 dpr;
      };
      return std::floor((edge(127 - key) + edge(128 - key)) / 2.0);
  }

  void wheel(songview::TimelineInputItem &input, const QPointF &position, QPoint pixel, QPoint angle,
             Qt::KeyboardModifiers modifiers = Qt::NoModifier,
             Qt::ScrollPhase phase = Qt::NoScrollPhase)
  {
      checks::events::sendWheel(input, position, pixel, angle, Qt::NoButton, modifiers, phase, false);
  }
  } // namespace

  PianoRollStaticTest::PianoRollStaticTest(QString projectRoot, QString songLabel)
      : m_projectRoot(std::move(projectRoot))
      , m_songLabel(std::move(songLabel))
  {}

  void PianoRollStaticTest::tickRangeRejectsInvalidBounds()
  {
      using songview::detail::tickRange;
      const double nan = std::numeric_limits<double>::quiet_NaN();
      const double infinity = std::numeric_limits<double>::infinity();
Original expression:
  QVERIFY(tickRange(nan, 100.0).empty())

A002 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:71
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(0.0, NaN) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(0.0, nan).empty())

A003 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:72
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(+infinity, 100.0) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(infinity, 100.0).empty())

A004 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:73
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(0.0, -infinity) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(0.0, -infinity).empty())

A005 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:74
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(-64.0, -1.0) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(-64.0, -1.0).empty())

A006 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:75
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(48.0, 48.0) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(48.0, 48.0).empty())

A007 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:76
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(64.0, 32.0) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(64.0, 32.0).empty())

A008 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:77
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(1.0, 0x1p64) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(1.0, 0x1p64).empty())

A009 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:78
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(0x1p64, 0x1p64 + 4096.0) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(0x1p64, 0x1p64 + 4096.0).empty())

A010 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:79
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(1.0, DBL_MAX) returns an empty window.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(tickRange(1.0, std::numeric_limits<double>::max()).empty())

A011 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:81
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(-24.0, 72.5).begin == 0.
Source context since previous assertion:
  ;
      const songview::detail::TickRange clipped = tickRange(-24.0, 72.5);
Original expression:
  QCOMPARE(clipped.begin, uint64_t(0))

A012 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:82
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(-24.0, 72.5).end == 72.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(clipped.end, uint64_t(72))

A013 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:84
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(96.75, 289.25).begin == 96.
Source context since previous assertion:
  ;
      const songview::detail::TickRange fractional = tickRange(96.75, 289.25);
Original expression:
  QCOMPARE(fractional.begin, uint64_t(96))

A014 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:85
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(96.75, 289.25).end == 289.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(fractional.end, uint64_t(289))

A015 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:87
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(0.0, nextafter(kNoTick, 0.0)).end == Tick(largestBelowCeiling).
Source context since previous assertion:
  ;
      const double largestBelowCeiling = std::nextafter(double(CoreTimeDefaults::kNoTick), 0.0);
Original expression:
  QCOMPARE(tickRange(0.0, largestBelowCeiling).end, Tick(largestBelowCeiling))

A016 | PianoRollStaticTest::tickRangeRejectsInvalidBounds | src/checks/rollcheck/static/camera.cpp:88
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane observes songview::detail::tickRange; the Swift grid consumes already-clamped Tick ranges (GridGeometry.forEachSubdivision, src/swift/app/timeline/GridGeometry.swift), and the nearest executed predicates S005 (src/checks/rollcheck/static/camera.swift) and S006 (:36-37) assert EditorCamera scroll bounds only. Unproved: tickRange(kMaxTick - 2048.0, kMaxTick - 1024.0).begin == kMaxTick - 2048.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(tickRange(double(CoreTimeDefaults::kMaxTick) - 2048.0,
                         double(CoreTimeDefaults::kMaxTick) - 1024.0)
                   .begin,
               CoreTimeDefaults::kMaxTick - 2048)

A017 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:98
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/camera.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::tickRangeWalksFractionalLattice()
  {
      CameraFixture fixture(m_projectRoot, m_songLabel);
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A018 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:106
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: at pxPerBeat 384 the automatic-grid segment at tick 96 starts at or before 96.
Source context since previous assertion:
  ;
      SongView &view = *fixture.view();
      const SongView::ViewState original = view.viewState();
      SongView::ViewState zoomed = original;
      zoomed.pxPerBeat = 384.0;
      zoomed.gridSelection = songview::GridSelection::automatic();
      view.applyViewState(zoomed);
      const songview::Grid::Segment segment = view.grid().segmentAt(96);
Original expression:
  QVERIFY(segment.start <= 96)

A019 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:107
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: that segment next boundary is at or beyond 289.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(segment.next >= 289)

A020 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:109
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the segment lattice stride is positive.
Source context since previous assertion:
  ;
      const Tick lattice = view.grid().gridTicksAt(96);
Original expression:
  QVERIFY(lattice > 0)

A021 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:110
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the lattice stride stays below the segment beat width.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(lattice < segment.beatTicks)

A022 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:129
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the fractional walk over 96.75..289.25 equals the independent membership expectation for 96..289.
Source context since previous assertion:
  ;
      const auto walk = [&view](double begin, double end) {
          std::vector<Tick> ticks;
          songview::detail::forEachSubGridLine(view.grid(), songview::detail::tickRange(begin, end),
                                               [&ticks](Tick tick, int) { ticks.push_back(tick); });
          return ticks;
      };
      // Independent membership expectation on each segment's sub-beat lattice.
      const auto expectedLines = [&view](Tick begin, Tick end) {
          std::vector<Tick> ticks;
          for (Tick tick = begin; tick < end; ++tick) {
              const songview::Grid::Segment s = view.grid().segmentAt(tick);
              const Tick step = view.grid().gridTicksAt(tick);
              if (step > 0 && step < s.beatTicks && (tick - s.start) % step == 0 &&
                  (tick - s.start) % s.beatTicks != 0 && view.grid().drawsSubGridIn(s))
                  ticks.push_back(tick);
          }
          return ticks;
      };
Original expression:
  QCOMPARE(walk(96.75, 289.25), expectedLines(96, 289))

A023 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:130
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the walk over a NaN begin is empty.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(walk(std::numeric_limits<double>::quiet_NaN(), 100.0).empty())

A024 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:138
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the musical-16 coarse lattice stride exceeds one tick.
Source context since previous assertion:
  ;
      // A coarser lattice keeps both drawn and snap strides above the clock floor.
      SongView::ViewState coarse = original;
      coarse.pxPerBeat = 192.0;
      coarse.gridSelection = songview::GridSelection::musical(16);
      view.applyViewState(coarse);
      const Tick latticeG = view.grid().gridTicksAt(0);
      const Tick snapG = view.grid().snapTicksAt(0);
Original expression:
  QVERIFY(latticeG > 1)

A025 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:139
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the coarse lattice stride stays below the segment beat width.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(latticeG < segment.beatTicks)

A026 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:140
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: tick 96 is a multiple of the coarse lattice stride.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(96 % latticeG == 0)

A027 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:141
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the coarse snap stride is at least three ticks.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(snapG >= 3)

A028 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:142
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the segment beat width is a multiple of the coarse snap stride.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(segment.beatTicks % snapG == 0)

A029 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:144
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the coarse lattice steps at least twice below the segment end.
Source context since previous assertion:
  ;
      // Aligned and interior ticks advance to the same next subdivision.
Original expression:
  QVERIFY2(2 * uint64_t(latticeG) < uint64_t(segment.next - 96),
               "lattice must step at least twice below the segment end")

A030 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:146
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: nextSubdivisionTickAfter(96) is 96 plus latticeG.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().nextSubdivisionTickAfter(Tick(96)), Tick(96 + latticeG))

A031 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:147
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: nextSubdivisionTickAfter(97) is 96 plus latticeG.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().nextSubdivisionTickAfter(Tick(97)), Tick(96 + latticeG))

A032 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:151
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: a walk starting one tick after the last aligned candidate is empty.
Source context since previous assertion:
  ;
      // The first ceiling-aligned candidate (96 + latticeG) already lies at
      // or beyond the range end: the segment contributes nothing.
      const std::vector<Tick> firstOutside = walk(97, 98);
Original expression:
  QVERIFY2(firstOutside.empty(), "a first aligned candidate past the range end must be skipped")

A033 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:154
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the walk over 95..110 equals its membership expectation.
Source context since previous assertion:
  ;
      // The first candidate is the beat line at 96: only its callback is
      // skipped; the walk still advances through the rest of the range.
Original expression:
  QCOMPARE(walk(95, 110), expectedLines(95, 110))

A034 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:157
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTick at the exact midpoint 96 + snapG/2 returns 96 (lower candidate wins the tie).
Source context since previous assertion:
  ;
      // Fractional nearest tie on either side of the midpoint: the lower
      // candidate wins exactly at the tie.
Original expression:
  QCOMPARE(view.grid().snapTick(96.0 + snapG / 2.0), Tick(96))

A035 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:158
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTick a quarter tick below the midpoint returns 96.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTick(96.0 + snapG / 2.0 - 0.25), Tick(96))

A036 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:159
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTick a quarter tick above the midpoint returns 96 + snapG.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTick(96.0 + snapG / 2.0 + 0.25), Tick(96 + snapG))

A037 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:160
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTickDown a quarter tick above the midpoint returns 96.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTickDown(96.0 + snapG / 2.0 + 0.25), Tick(96))

A038 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:161
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTickUp a quarter tick below the midpoint returns 96 + snapG.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTickUp(96.0 + snapG / 2.0 - 0.25), Tick(96 + snapG))

A039 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:167
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the mid-cell 3/4 signature at tick 102 re-anchors segmentAt(102).start to 102.
Source context since previous assertion:
  ;
      // A mid-cell signature change re-anchors the lattice: the first
      // segment's walk stops at the seam (its next candidate lies at the
      // segment end), and the new segment restarts its own lattice and
      // beat-line omission from its start.
      fixture.tab()->document().setTimeSig(102, 4, 2);
Original expression:
  QTRY_VERIFY(view.grid().segmentAt(102).start == 102)

A040 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:168
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: segmentAt(101).next is 102 at the signature seam.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().segmentAt(101).next, Tick(102))

A041 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:169
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the walk over 95..130 after the signature change equals its membership expectation.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(walk(95, 130), expectedLines(95, 130))

A042 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:170
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: the walk over 100..110 after the signature change equals its membership expectation.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(walk(100, 110), expectedLines(100, 110))

A043 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:171
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTickDown(103.5) returns 102 on the post-seam lattice.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTickDown(103.5), Tick(102))

A044 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:172
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTickUp(101.5) returns 102 on the post-seam lattice.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTickUp(101.5), Tick(102))

A045 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:175
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: nextSubdivisionTickAfter(101) stops at the signature seam 102.
Source context since previous assertion:
  ;
      // Stop at the signature seam, then restart on its new lattice.
      const Tick latticeSeam = view.grid().gridTicksAt(102);
Original expression:
  QCOMPARE(view.grid().nextSubdivisionTickAfter(Tick(101)), Tick(102))

A046 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:176
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: nextSubdivisionTickAfter(102) restarts at 102 plus latticeSeam.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().nextSubdivisionTickAfter(Tick(102)), Tick(102 + latticeSeam))

A047 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:177
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTick(103.5) returns 102.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTick(103.5), Tick(102))

A048 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:178
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: snapTick(103.5 + snapG) returns 102 + snapG.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTick(103.5 + double(snapG)), Tick(102 + snapG))

A049 | PianoRollStaticTest::tickRangeWalksFractionalLattice | src/checks/rollcheck/static/camera.cpp:180
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane enumerates TimeAxis segments or the sub-grid lattice; the nearest executed lattice predicates assert derived strides instead - keyboard.swift (Right nudges one snap cell), note_commands.swift (split commits three grid-length fragments), and the snapTickDown guard inside the pencil fixture (pencil.swift) - while the mounted grid is repainted for the palette case only (src/checks/rollqml/tst_SwiftRollWindowing.qml). Unproved: deleting the 3/4 signature restores segmentAt(102).start to the original segment start.
Source context since previous assertion:
  ;
      fixture.tab()->document().deleteTimeSig(102);
Original expression:
  QTRY_VERIFY(view.grid().segmentAt(102).start == segment.start)

A050 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:188
Disposition: RETIRED-REPRESENTATION

A051 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:203
Disposition: MATCHED
Mapping: S046

A052 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:204
Disposition: MATCHED
Mapping: S046

A053 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:208
Disposition: MATCHED
Mapping: S047

A054 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:209
Disposition: MATCHED
Mapping: S047

A055 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:213
Disposition: MATCHED
Mapping: S048

A056 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:218
Disposition: MATCHED
Mapping: S049

A057 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:221
Disposition: MATCHED
Mapping: S050

A058 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:225
Disposition: MATCHED
Mapping: S051

A059 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:231
Disposition: MATCHED
Mapping: S052

A060 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:232
Disposition: MATCHED
Mapping: S052

A061 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:236
Disposition: MATCHED
Mapping: S053

A062 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:237
Disposition: MATCHED
Mapping: S053

A063 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:245
Disposition: MATCHED
Mapping: S054

A064 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:249
Disposition: MATCHED
Mapping: S055

A065 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:253
Disposition: MATCHED
Mapping: S057

A066 | PianoRollStaticTest::verticalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:254
Disposition: MATCHED
Mapping: S057

A067 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:262
Disposition: RETIRED-REPRESENTATION

A068 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:279
Disposition: MATCHED
Mapping: S061

A069 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:280
Disposition: MATCHED
Mapping: S061

A070 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:281
Disposition: MATCHED
Mapping: S061

A071 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:286
Disposition: MATCHED
Mapping: S062

A072 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:287
Disposition: MATCHED
Mapping: S062

A073 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:292
Disposition: MATCHED
Mapping: S063

A074 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:293
Disposition: MATCHED
Mapping: S063

A075 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:297
Disposition: MATCHED
Mapping: S064

A076 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:303
Disposition: MATCHED
Mapping: S065

A077 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:304
Disposition: MATCHED
Mapping: S065

A078 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:308
Disposition: MATCHED
Mapping: S066

A079 | PianoRollStaticTest::horizontalCameraWheelContract | src/checks/rollcheck/static/camera.cpp:309
Disposition: MATCHED
Mapping: S066

A080 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:328
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/camera.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      view.applyViewState(original);
  }

  void PianoRollStaticTest::affineCameraProjection_data()
  {
      QTest::addColumn<double>("pxPerBeat");
      QTest::addColumn<double>("scrollPx");
      QTest::newRow("low") << 37.125 << .375;
      QTest::newRow("fractional") << 37.375 << 13.625;
      QTest::newRow("high") << 512.5 << 71.3125;
  }

  void PianoRollStaticTest::affineCameraProjection()
  {
      QFETCH(double, pxPerBeat);
      QFETCH(double, scrollPx);
      CameraFixture fixture(m_projectRoot, m_songLabel);
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A081 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:338
Disposition: MATCHED
Mapping: S042, S043

A082 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:351
Disposition: MATCHED
Mapping: S044

A083 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:352
Disposition: PARTIAL
Mapping/reason: Related predicate S044 (src/checks/rollcheck/static/camera.swift) asserts the display-projection half of this pair over the same three affine fixture rows; the snapped-tick inverse half is not asserted. Unproved: snapTick(tickAtContentX(displayed - origin)) == tick for each origin/DPR pair.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.grid().snapTick(view.camera().tickAtContentX(displayed - origin)),
                               tick)

A084 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:358
Disposition: PARTIAL
Mapping/reason: Related predicate S043 (:209-211) asserts the fractional tick/content round-trip inside the same walk; no executed predicate asserts forward progress of the walk. Unproved: every snapped candidate strictly exceeds the previous tick.
Source context since previous assertion:
  ;
                  }
              }
          }
          const Tick next = view.grid().snapTickUp(double(tick) + 1.0);
Original expression:
  QVERIFY(next > tick)

A085 | PianoRollStaticTest::affineCameraProjection | src/checks/rollcheck/static/camera.cpp:361
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane counts the visible ticks; S044 (:217-218) enumerates the same display projections without counting. Unproved: at least two ticks land inside the visible plot width.
Source context since previous assertion:
  ;
          tick = next;
      }
Original expression:
  QVERIFY(visible >= 2)

A086 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:369
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/camera.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      view.applyViewState(original);
  }

  void PianoRollStaticTest::cameraRangeAndPreRollRaster()
  {
      CameraFixture fixture(m_projectRoot, m_songLabel);
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A087 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:374
Disposition: MATCHED
Mapping: S045

A088 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:378
Disposition: MATCHED
Mapping: S005

A089 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:381
Disposition: MATCHED
Mapping: S006

A090 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:384
Disposition: MATCHED
Mapping: S005

A091 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:385
Disposition: PARTIAL
Mapping/reason: Related predicate: the executing lane observes the edit cursor through grid.editCursorTick (src/checks/rollcheck/EditorGridCameraChecks.swift) and cursor publication (src/checks/workspace/session_playback.swift); neither drives SongView::goToStart. Unproved: goToStart leaves the edit cursor at tick 0.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(view.editCursorTick(), uint64_t(0))

A092 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:391
Disposition: PARTIAL
Mapping/reason: Related predicate: the mounted roll band lifecycle and canonical rect are asserted by src/checks/rollqml/tst_SwiftRollPlots.qml; no Swift predicate observes view.timelineBandLayout().geometry(Roll). Unproved: the roll band layout exists for the pre-roll-bound view.
Source context since previous assertion:
  ;
      state.scrollPx = 0.0;
      view.applyViewState(state);
      state.scrollPx = -pad;
      view.applyViewState(state);
      const auto rollBand = view.timelineBandLayout().geometry(songview::TimelineBand::Roll);
Original expression:
  QVERIFY(rollBand.has_value())

A093 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:393
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/camera.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      const QImage raster = checks::support::captureQuickBand(view, rollBand->rect);
Original expression:
  QVERIFY(!raster.isNull())

A094 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:395
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane reads the raster device pixel ratio; the band raster capture itself is NATIVE (A093). Unproved: the captured roll-band raster reports a positive device pixel ratio.
Source context since previous assertion:
  ;
      const qreal dpr = raster.devicePixelRatio();
Original expression:
  QVERIFY(dpr > 0.0)

A095 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:417
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane probes raster key rows; the row/pitch projection is exercised by S019 (src/checks/rollcheck/static/camera.swift) and S020 (:119-124). Unproved: a natural key row lies fully inside the roll band for the raster probe.
Source context since previous assertion:
  ;
      const auto isBlackKey = [](int key) {
          switch (key % 12) {
          case 1:
          case 3:
          case 6:
          case 8:
          case 10:
              return true;
          default:
              return false;
          }
      };
      const int midKey = keyAt(view, roll.bounds().height() / 2.0);
      int naturalKey = -1;
      int accidentalKey = -1;
      for (int key = midKey - 4; key <= midKey + 4; ++key) {
          if (key < 0 || key > 127 || rowCenter(view, key) < 0 ||
              rowCenter(view, key) >= roll.bounds().height())
              continue;
          (isBlackKey(key) ? accidentalKey : naturalKey) = key;
      }
Original expression:
  QVERIFY(naturalKey >= 0)

A096 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:418
Disposition: PARTIAL
Mapping/reason: Related predicate: none in the executing lane probes raster key rows; the row/pitch projection is exercised by S019 (src/checks/rollcheck/static/camera.swift) and S020 (:119-124). Unproved: an accidental key row lies fully inside the roll band for the raster probe.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(accidentalKey >= 0)

A097 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:426
Disposition: PARTIAL
Mapping/reason: Related predicate: the mounted pre-roll pad is observed by src/checks/rollqml/tst_TimelinePan.qml (positive pad, tick-zero home); no predicate compares pad pixels across key rows. Unproved: the pre-roll pad paints natural and accidental rows identically.
Source context since previous assertion:
  ;
      const qreal plotOffset = rollBand->plotRect.x() - rollBand->rect.x();
      const auto pixel = [&raster, dpr](qreal x, qreal y) {
          return raster.pixel(qRound(x * dpr), qRound(y * dpr));
      };
      const QRgb padNatural = pixel(plotOffset + pad / 2.0, rowCenter(view, naturalKey));
      const QRgb padAccidental = pixel(plotOffset + pad / 2.0, rowCenter(view, accidentalKey));
      const QRgb plainNatural = pixel(plotOffset + pad + 20.0, rowCenter(view, naturalKey));
Original expression:
  QCOMPARE(padNatural, padAccidental)

A098 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:427
Disposition: PARTIAL
Mapping/reason: Related predicate: the mounted pre-roll pad position is observed by src/checks/rollqml/tst_TimelinePan.qml; no predicate compares pre-roll pad pixels with plot pixels. Unproved: the pre-roll pad pixel differs from the plain plot pixel.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(padNatural != plainNatural)

A099 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:431
Disposition: PARTIAL
Mapping/reason: Related predicate S063 (src/checks/rollcheck/static/camera.swift) asserts the same 8px horizontal pixel-wheel pan through grid.handleWheel from the 23.625 fixture offset; the bound-view fixture from scrollPx 0 is not driven. Unproved: the wheel leaves scrollX at -8 on this fixture.
Source context since previous assertion:
  ;
      state.scrollPx = 0.0;
      view.applyViewState(state);
      wheel(roll, {40.0, 200.0}, {8, 0}, {});
Original expression:
  QVERIFY(std::abs(view.camera().scrollX() + 8.0) <= 1e-12)

A100 | PianoRollStaticTest::cameraRangeAndPreRollRaster | src/checks/rollcheck/static/camera.cpp:434
Disposition: PARTIAL
Mapping/reason: Related predicate S003 (:28-29) asserts fractional camera scroll restoration; no executed predicate reads the production view-state scrollPx field. Unproved: viewState().scrollPx equals -pad/2 after the apply.
Source context since previous assertion:
  ;
      state.scrollPx = -pad / 2.0;
      view.applyViewState(state);
Original expression:
  QVERIFY(std::abs(view.viewState().scrollPx + pad / 2.0) <= 1e-12)

A101 | PianoRollStaticTest::scratchSpaceDrawGrowsTimeline | src/checks/rollcheck/static/camera.cpp:442
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/camera.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation; see converted-window evidence below where available.
Source context since previous assertion:
  ;
      view.applyViewState(original);
  }

  void PianoRollStaticTest::scratchSpaceDrawGrowsTimeline()
  {
      CameraFixture fixture(m_projectRoot, m_songLabel);
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A102 | PianoRollStaticTest::scratchSpaceDrawGrowsTimeline | src/checks/rollcheck/static/camera.cpp:465
Disposition: PARTIAL
Mapping/reason: Related predicate: timeline extent growth is asserted by src/checks/rollcheck/EditorGridCameraChecks.swift (a far addNotes grows lengthTicks; deletion shrinks and reclamps) and the draw commit by :947-950; neither drives the double-click scratch draw. Unproved: the drawn tick lands at or beyond the pre-draw timeline length.
Source context since previous assertion:
  ;
      SongView &view = *fixture.view();
      SongDocument &document = fixture.tab()->document();
      auto &roll = *fixture.rollInput();
      const SongView::ViewState original = view.viewState();
      const Tick length = fixture.tab()->timeline()->lengthTicks;
      const QByteArray bytes = document.smf().write();
      const int undo = document.undoStack()->index();
      SongView::ViewState state = original;
      state.scrollPx = 1e9;
      view.applyViewState(state);
      const Tick tick =
          view.grid().snapTickDown(view.camera().tickAtContentX(roll.bounds().width() / 2.0));
      const int key = keyAt(view, roll.bounds().height() / 2.0);
      const qreal begin = view.camera().contentX(double(tick));
      const qreal end = view.camera().contentX(double(tick + view.grid().snapTicksAt(tick)));
      checks::events::sendMouse(roll, QEvent::MouseButtonDblClick,
                                QPointF((begin + end) / 2.0, rowCenter(view, key)), Qt::LeftButton,
                                Qt::LeftButton, Qt::NoModifier);
      checks::events::sendMouse(roll, QEvent::MouseButtonRelease,
                                QPointF((begin + end) / 2.0, rowCenter(view, key)), Qt::LeftButton,
                                Qt::NoButton, Qt::NoModifier);
      DocNote note;
Original expression:
  QVERIFY(tick >= length)

A103 | PianoRollStaticTest::scratchSpaceDrawGrowsTimeline | src/checks/rollcheck/static/camera.cpp:466
Disposition: PARTIAL
Mapping/reason: Related predicate: the scratch draw commit is asserted at src/checks/rollcheck/EditorGridCameraChecks.swift (revision advance and latched velocity); note existence at the drawn cell is not asserted. Unproved: the drawn key resolves at the drawn tick in the document.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(document.findNote(fixture.track(), tick, uint8_t(key), &note))

A104 | PianoRollStaticTest::scratchSpaceDrawGrowsTimeline | src/checks/rollcheck/static/camera.cpp:467
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts grow-then-shrink extent through addNotes/deleteNotes; the scratch-draw growth path is not driven. Unproved: the scratch draw strictly grows the timeline length.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.tab()->timeline()->lengthTicks > length)

A105 | PianoRollStaticTest::scratchSpaceDrawGrowsTimeline | src/checks/rollcheck/static/camera.cpp:470
Disposition: PARTIAL
Mapping/reason: Related predicate: undo-to-exact-bytes is asserted for other commands (src/checks/rollcheck/note_commands.swift; src/checks/rollcheck/keyboard.swift restores song, camera, and history position); the scratch-draw undo round-trip is not asserted. Unproved: undoing the scratch draw re-encodes the original SMF bytes.
Source context since previous assertion:
  ;
      while (document.undoStack()->index() > undo && document.undoStack()->canUndo())
          document.undoStack()->undo();
Original expression:
  QCOMPARE(document.smf().write(), bytes)

A106 | PianoRollStaticTest::keyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.cpp:478
Disposition: RETIRED-REPRESENTATION

A107 | PianoRollStaticTest::keyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.cpp:485
Disposition: MATCHED
Mapping: S058

A108 | PianoRollStaticTest::keyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.cpp:489
Disposition: MATCHED
Mapping: S059

A109 | PianoRollStaticTest::keyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.cpp:492
Disposition: MATCHED
Mapping: S060

Remaining original source (closing sequence and helper bodies):
  ;
  }

  } // namespace checks::rollcheck::staticcheck


Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "caller-resolved minimum viewport produces observable camera bounds"

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick/content transforms round-trip with a fractional offset"

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal restoration preserves fractional offsets"

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "display projection snaps to a physical pixel"

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal scrolling clamps to production pre-roll"

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound timeline end reaches the plot origin"

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport shrink reclamps the changed pre-roll floor"

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport updates preserve a legal fractional offset"

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "interactive time zoom preserves its tick anchor"

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "time zoom reaches its bound when the pointer anchor is unreachable"

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unanchored restoration keeps valid fractional camera state"

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "restoration sanitizes non-finite transient view-state values independently"

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "content shrink reclamps the horizontal offset"

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "viewport growth reclamps vertical content"

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "an unbound timeline homes to its lead pad"

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unbound viewport updates follow the resolved home"

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "chromatic pitch mapping remains bijective across the full MIDI range"

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch rows and nearest lookup preserve production ordering"

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row and pitch inverses agree"

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional DPR snapping defines the inverse row boundary"

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch projection shrink reclamps the vertical offset"

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "vertical bounds derive from projected row height"

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom preserves its row anchor when bounds permit"

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom reaches its bound when the row anchor is unreachable"

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "key reveal scrolls only enough to show its row"

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "hidden pitches do not move the camera"

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick reveal uses the production one-third anchor"

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "a fitting range on the physical right boundary stays visible"

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal prefers its requested leading edge"

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal honors the requested trailing edge"

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "phase \(phase.rawValue) performs one anchored time zoom publication"

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses time zoom without publishing"

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "pixel deltas receive five-times angle weighting"

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "effective angle and pixel zooms each publish once"

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift wheel pans by negative selected delta exactly once"

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at minimum and repeated clamped pan is silent"

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at maximum and repeated clamped pan is silent"

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal-only momentum still pans exactly once"

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Ctrl wheel performs one row-anchored pitch zoom publication"

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses pitch zoom without publishing"

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter wheel scrolls by negative half-delta exactly once"

S042 | checkAffineCameraProjection | src/checks/rollcheck/static/camera.swift
Anchor: message "recorded fractional affine projection at tick 24"

S043 | checkAffineCameraProjection | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional affine tick round-trip at \(pixelsPerBeat)"

S044 | checkAffineCameraProjection | src/checks/rollcheck/static/camera.swift
Anchor: message "physical-pixel affine projection"

S045 | checkFallbackCamera | src/checks/rollcheck/static/camera.swift
Anchor: message "unbound viewport homes tick zero at its lead pad"

S046 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "four quarter-notch pitch zooms equal one full notch"

S047 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch-zoom momentum leaves height and scroll unchanged"

S048 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "quarter-notch zoom holds the fractional pitch row"

S049 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "ten pitch notches clamp at the font-scaled 16px maximum"

S050 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "240px wheel delta reaches the same pitch-height maximum"

S051 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "one gutter pixel pans pitch by a negative half-pixel"

S052 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "opposite quarter-notches restore pitch height and offset"

S053 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional pitch height and scroll restore without rounding"

S054 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row boundary minus a quarter pixel selects upper pitch"

S055 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row boundary plus a quarter pixel selects lower pitch"

S056 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "leaving the keyboard clears the projected hover pitch"

S057 | checkVerticalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "integral pitch height and scroll restore exactly"

S058 | checkKeyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter midpoint hover selects the projected pitch"

S059 | checkKeyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.swift
Anchor: message "adjacent lower gutter row changes hover pitch by one"

S060 | checkKeyboardGutterHoverTracksRows | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter leave clears the hover pitch"

S061 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "four quarter-notch time zooms equal a full notch"

S062 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "24px wheel equals one full angle notch"

S063 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal pixel wheel pans without changing scale"

S064 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "quarter-notch time zoom preserves fractional tick anchor"

S065 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "opposite quarter-notches restore scale and offset"

S066 | checkHorizontalCameraWheelContract | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional time scale and offset restore exactly"
