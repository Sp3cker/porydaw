Assertion correspondence: rollcheck/static/gate.cpp

Reference revision: 90446f5dbbb9f9d9f9000d7819c42c1b8b2c5389
Original: src/checks/rollcheck/static/gate.cpp
Original SHA-256: 78ee6803e1c397aaa0e035fdcda0ae63f87e72ac8be9336e62aa1ee4718b4620
Swift counterpart: src/checks/rollcheck/static/camera.swift
Ready-wheel Swift counterpart: src/checks/rollcheck/static/gate.swift
Ready-wheel Swift SHA-256: f848be0e07c37edd0f644d93a836391c1e59f540fa088cd96b3e229cd12718f2
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift counterpart: src/checks/workspace/session_playback.swift
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftcore --verbose
Result: PASS; 1/14 ok (controller run recorded before this ledger edit; the edit changes no Swift source).
Command: deno task verify:qml-roll --verbose
Result: PASS; 1/1 ok (controller run recorded before this ledger edit; the edit changes no QML source); test_readyRollWheelZoom exercises A068.
Command: deno task verify:shell --verbose
Result: PASS; 11/11 ok (controller run recorded before this ledger edit; the edit changes no shell source).
Verified Swift SHA-256: 95fb2e80f95bf57e41250632cb8b3e7378fa32be143d02f5bd092313f6029808 (camera.swift)
Verified QML SHA-256: e7f78598b6eefcfa4b2e120273fe74c89b3b2a13ad78d286d0dbba11c6478da0 (tst_SwiftRollWindowing.qml)
Narrow form used while iterating: deno task verify --filter swiftcore --qt projectSession.

A001 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:197
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  #include "checks/rollcheck/static/tst_pianorollstatic.h"

  #include <QCoreApplication>
  #include <QCursor>
  #include <QEnterEvent>
  #include <QPointer>
  #include <QQuickItem>
  #include <QQuickWindow>
  #include <QScopeGuard>
  #include <QWheelEvent>
  #include <QWindow>
  #include <QtTest>

  #include <array>

  #include "checks/rollcheck/static/fixtures.h"
  #include "checks/support/eventsynth.h"
  #include "core/smf.h"
  #include "core/tracklimits.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/songtab.h"
  #include "ui/songview.h"
  #include "ui/songview/quick/eventlistcontroller.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"
  #include "ui/songview/timeruler.h"
  #include "ui/songview/trackheadermodel.h"

  namespace checks::rollcheck::staticcheck {
  namespace {

  bool bindMidiStage(GateFixture &fixture)
  {
      SmfFile smf;
      SongInfo info;
      info.label = fixture.tab()->name().value();
      fixture.tab()->applyMidiStage(info, std::move(smf), track_limits::kHardwareCapacity);
      return fixture.tab()->presentationError().isEmpty();
  }

  bool bindVoicegroup(GateFixture &fixture)
  {
      const auto identity =
          VoicegroupId::create(QStringLiteral("sound/voicegroups/loading_probe.inc"), QString());
      if (!identity)
          return false;
      fixture.tab()->applyVoicegroupBound(*identity);
      return true;
  }

  void sendRulerClick(songview::TimelineInputItem &input, SongView &view)
  {
      const auto band = view.timelineBandLayout().geometry(songview::TimelineBand::Ruler);
      QQuickWindow *const window = input.window();
      if (!band || !window)
          return;
      const QPointF point(view.camera().leadPadPx() + 140.0, band->rect.height() * 3.0 / 4.0);
      QTest::mouseClick(window, Qt::LeftButton, Qt::NoModifier, input.mapToScene(point).toPoint());
  }

  void sendScrollbarWheel(QQuickItem &bar)
  {
      QQuickWindow *const window = bar.window();
      if (!window)
          return;
      const QPointF local(bar.width() / 2.0, bar.height() / 2.0);
      const QPointF scene = bar.mapToScene(local);
      QWheelEvent event(scene, window->mapToGlobal(scene.toPoint()), QPoint(), QPoint(0, -120),
                        Qt::NoButton, Qt::NoModifier, Qt::NoScrollPhase, false);
      QCoreApplication::sendEvent(window, &event);
  }

  void sendRollWheel(songview::TimelineInputItem &input)
  {
      QQuickWindow *const window = input.window();
      if (!window)
          return;
      const QPointF scene = input.mapToScene(QPointF(80.0, 100.0));
      QWheelEvent event(scene, window->mapToGlobal(scene.toPoint()), QPoint(), QPoint(0, 120),
                        Qt::NoButton, Qt::NoModifier, Qt::NoScrollPhase, false);
      QCoreApplication::sendEvent(window, &event);
  }

  bool controlsMatchRuler(const GateFixture &fixture)
  {
      const auto *ruler = fixture.ruler();
      const auto *division = fixture.divisionControl();
      const auto *feel = fixture.feelControl();
      return ruler && division && feel && ruler->gridControlsEnabled() &&
             division->property("controlText").toString() == ruler->divisionText() &&
             feel->property("controlText").toString() == ruler->feelText() &&
             division->property("controlToolTip").toString() == ruler->divisionToolTip() &&
             feel->property("controlToolTip").toString() == ruler->feelToolTip();
  }
  bool allFixedSurfacesEnabled(const GateFixture &fixture)
  {
      if (!fixture.view() || !fixture.rollInput() || !fixture.rulerInput() ||
          !fixture.horizontalScrollbar() || !fixture.eventListController() || !fixture.drawer() ||
          !fixture.headers() || !fixture.headersInput() || !fixture.controls() ||
          !fixture.divisionControl() || !fixture.feelControl())
          return false;
      const auto *quick = fixture.view()->findChild<songview::TimelineQuickView *>(
          QStringLiteral("timelineQuickCanvas"));
      QQuickItem *const root = quick ? quick->rootObject() : nullptr;
      if (!root || !fixture.rollInput()->isEnabled() || !fixture.rulerInput()->isEnabled() ||
          !fixture.horizontalScrollbar()->isVisible() ||
          !fixture.horizontalScrollbar()->isEnabled() ||
          fixture.headersInput()->interaction() != fixture.headers() ||
          !fixture.headersInput()->isEnabled() || !fixture.controls()->isEnabled() ||
          !fixture.divisionControl()->isEnabled() || !fixture.feelControl()->isEnabled())
          return false;
      const auto *verticalScrollbar =
          root->findChild<QQuickItem *>(QStringLiteral("timelineRollScrollBar"));
      const auto *otherEvents =
          root->findChild<songview::TimelineInputItem *>(QStringLiteral("timelineOtherEventsInput"));
      const auto *eventListInput =
          root->findChild<songview::TimelineInputItem *>(QStringLiteral("timelineEventListInput"));
      if (!verticalScrollbar || !verticalScrollbar->isVisible() || !verticalScrollbar->isEnabled() ||
          !otherEvents || !otherEvents->isEnabled() || !eventListInput ||
          !eventListInput->isEnabled())
          return false;
      constexpr std::array drawerNames{
          "drawerVoiceChangesHandleInput",
          "drawerVelocityHandleInput",
          "drawerAutomationHandleInput",
          "drawerBarInput",
          "drawerDetentInput",
      };
      for (const char *name : drawerNames) {
          const auto *input =
              root->findChild<songview::TimelineInputItem *>(QString::fromLatin1(name));
          if (!input || !input->isEnabled())
              return false;
      }
      return true;
  }

  struct GridControlState {
      QString divisionText;
      QString feelText;
      QString divisionToolTip;
      QString feelToolTip;
      QString divisionControlText;
      QString feelControlText;
      QString divisionControlToolTip;
      QString feelControlToolTip;
      QVariantMap appearance;
      bool controlsEnabled = false;
      bool divisionEnabled = false;
      bool feelEnabled = false;
  };

  GridControlState gridControlState(const GateFixture &fixture)
  {
      const auto *ruler = fixture.ruler();
      const auto *controls = fixture.controls();
      const auto *division = fixture.divisionControl();
      const auto *feel = fixture.feelControl();
      if (!ruler || !controls || !division || !feel)
          return {};
      return {ruler->divisionText(),
              ruler->feelText(),
              ruler->divisionToolTip(),
              ruler->feelToolTip(),
              division->property("controlText").toString(),
              feel->property("controlText").toString(),
              division->property("controlToolTip").toString(),
              feel->property("controlToolTip").toString(),
              ruler->gridControlAppearance(),
              controls->isEnabled(),
              division->isEnabled(),
              feel->isEnabled()};
  }

  bool unchangedGridControlState(const GateFixture &fixture, const GridControlState &before)
  {
      const GridControlState after = gridControlState(fixture);
      return after.divisionText == before.divisionText && after.feelText == before.feelText &&
             after.divisionToolTip == before.divisionToolTip &&
             after.feelToolTip == before.feelToolTip &&
             after.divisionControlText == before.divisionControlText &&
             after.feelControlText == before.feelControlText &&
             after.divisionControlToolTip == before.divisionControlToolTip &&
             after.feelControlToolTip == before.feelControlToolTip &&
             after.appearance == before.appearance &&
             after.controlsEnabled == before.controlsEnabled &&
             after.divisionEnabled == before.divisionEnabled &&
             after.feelEnabled == before.feelEnabled;
  }

  } // namespace

  void PianoRollStaticTest::freshTabStaysGated()
  {
      GateFixture fixture;
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A002 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:198
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!fixture.tab()->isReady())

A003 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:199
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.rollInput()->isEnabled())

A004 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:200
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.rulerInput()->isEnabled())

A005 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:201
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.horizontalScrollbar()->isVisible())

A006 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:202
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.horizontalScrollbar()->isEnabled())

A007 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:203
Disposition: RETIRED-REPRESENTATION

A008 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:204
Disposition: RETIRED-REPRESENTATION

A009 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:205
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.headersInput()->isEnabled())

A010 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:206
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(fixture.headersInput()->interaction(), fixture.headers())

A011 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:207
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.controls()->isEnabled())

A012 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:208
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.divisionControl()->isEnabled())

A013 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:209
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.feelControl()->isEnabled())

A014 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:210
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(controlsMatchRuler(fixture))

A015 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:214
Disposition: RETIRED-REPRESENTATION

A016 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:221
Disposition: RETIRED-REPRESENTATION

A017 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:222
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(verticalScrollbar->isVisible())

A018 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:223
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(verticalScrollbar->isEnabled())

A019 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:224
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(otherEvents)

A020 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:225
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(otherEvents->isEnabled())

A021 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:226
Disposition: RETIRED-REPRESENTATION

A022 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:227
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(eventListInput->isEnabled())

A023 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:238
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      constexpr std::array drawerNames{
          "drawerVoiceChangesHandleInput",
          "drawerVelocityHandleInput",
          "drawerAutomationHandleInput",
          "drawerBarInput",
          "drawerDetentInput",
      };
      for (const char *name : drawerNames) {
          auto *const input =
              root->findChild<songview::TimelineInputItem *>(QString::fromLatin1(name));
Original expression:
  QVERIFY2(input, name)

A024 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:239
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(input->isEnabled())

A025 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:242
Disposition: GAP
Mapping/reason: GAP; no executing predicate reads the edit cursor of a MIDI-only gated tab. The nearest executed cursor predicates are src/checks/rollcheck/EditorGridCameraChecks.swift (direct cursor change on the ready session) and src/checks/workspace/session_playback.swift (cursor publication); neither drives a gated ruler click, so the resulting 96-tick cursor stays unproved.
Source context since previous assertion:
  ;
      }
      fixture.view()->setEditCursorTick(96);
Original expression:
  QCOMPARE(fixture.view()->editCursorTick(), uint64_t(96))

A026 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:245
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Tick before = fixture.view()->editCursorTick();
      sendRulerClick(*fixture.rulerInput(), *fixture.view());
Original expression:
  QCOMPARE(fixture.view()->editCursorTick(), before)

A027 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:246
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!fixture.view()->userGestureActive())

A028 | PianoRollStaticTest::freshTabStaysGated | src/checks/rollcheck/static/gate.cpp:249
Disposition: PARTIAL
Mapping/reason: Related predicates S031 (src/checks/rollcheck/static/camera.swift) and S032 (:274-276) assert the ready-session time-zoom publication and its momentum suppression; no predicate drives a wheel while this tab is gated. Unproved: the gated roll wheel leaves pxPerBeat at its pre-wheel zoom.
Source context since previous assertion:
  ;
      const double zoom = fixture.view()->camera().pxPerBeat();
      sendRollWheel(*fixture.rollInput());
Original expression:
  QVERIFY(qFuzzyCompare(fixture.view()->camera().pxPerBeat(), zoom))

A029 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:256
Disposition: RETIRED-REPRESENTATION

A030 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:257
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(bindMidiStage(fixture), qPrintable(fixture.tab()->presentationError()))

A031 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:258
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!fixture.tab()->isReady())

A032 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:259
Disposition: RETIRED-REPRESENTATION

A033 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:260
Disposition: GAP
Mapping/reason: GAP; no executing predicate reads the edit cursor of a MIDI-only staged tab. The nearest executed cursor predicates are src/checks/rollcheck/EditorGridCameraChecks.swift and src/checks/workspace/session_playback.swift; neither drives a MIDI-only staged tab, so the zero cursor stays unproved.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(fixture.view()->editCursorTick(), uint64_t(0))

A034 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:261
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(controlsMatchRuler(fixture))

A035 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:262
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(allFixedSurfacesEnabled(fixture))

A036 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:265
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Tick cursor = fixture.view()->editCursorTick();
      sendRulerClick(*fixture.rulerInput(), *fixture.view());
Original expression:
  QCOMPARE(fixture.view()->editCursorTick(), cursor)

A037 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:268
Disposition: PARTIAL
Mapping/reason: Related predicates S035 (src/checks/rollcheck/static/camera.swift), S038 (:347-349) and S005/S006 (:33-37) assert ready-session pan and bounds behavior; no predicate drives a scrollbar wheel while the tab is gated. Unproved: the gated scrollbar wheel leaves scrollX at its pre-wheel offset (midiStageStaysGated replay).
Source context since previous assertion:
  ;
      const double scroll = fixture.view()->camera().scrollX();
      sendScrollbarWheel(*fixture.horizontalScrollbar());
Original expression:
  QVERIFY(qFuzzyCompare(fixture.view()->camera().scrollX(), scroll))

A038 | PianoRollStaticTest::midiStageStaysGated | src/checks/rollcheck/static/gate.cpp:271
Disposition: PARTIAL
Mapping/reason: Related predicates S031 (src/checks/rollcheck/static/camera.swift) and S032 (:274-276) assert the ready-session time-zoom publication and its momentum suppression; no predicate drives a wheel while the tab is gated. Unproved: the gated roll wheel leaves pxPerBeat at its pre-wheel zoom (midiStageStaysGated replay).
Source context since previous assertion:
  ;
      const double zoom = fixture.view()->camera().pxPerBeat();
      sendRollWheel(*fixture.rollInput());
Original expression:
  QVERIFY(qFuzzyCompare(fixture.view()->camera().pxPerBeat(), zoom))

A039 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:278
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::voicegroupBoundReadiesTab()
  {
      GateFixture fixture;
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A040 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:279
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(bindMidiStage(fixture), qPrintable(fixture.tab()->presentationError()))

A041 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:280
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(bindVoicegroup(fixture))

A042 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:281
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.tab()->isReady())

A043 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:282
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(controlsMatchRuler(fixture))

A044 | PianoRollStaticTest::voicegroupBoundReadiesTab | src/checks/rollcheck/static/gate.cpp:283
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QTRY_VERIFY(allFixedSurfacesEnabled(fixture))

A045 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:290
Disposition: RETIRED-REPRESENTATION

A046 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:291
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(bindMidiStage(fixture), qPrintable(fixture.tab()->presentationError()))

A047 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:293
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const GridControlState before = gridControlState(fixture);
Original expression:
  QVERIFY(!before.divisionText.isEmpty())

A048 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:294
Disposition: RETIRED-REPRESENTATION

A049 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:295
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.tab()->isReady())

A050 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:296
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(unchangedGridControlState(fixture, before))

A051 | PianoRollStaticTest::gridControlsDoNotRestyleAcrossReadiness | src/checks/rollcheck/static/gate.cpp:297
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(controlsMatchRuler(fixture))

A052 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:304
Disposition: RETIRED-REPRESENTATION

A053 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:305
Disposition: RETIRED-REPRESENTATION

A054 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:308
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Tick gated = fixture.view()->editCursorTick();
      sendRulerClick(*fixture.rulerInput(), *fixture.view());
Original expression:
  QCOMPARE(fixture.view()->editCursorTick(), gated)

A055 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:309
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!fixture.view()->userGestureActive())

A056 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:310
Disposition: RETIRED-REPRESENTATION

A057 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:313
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Tick ready = fixture.view()->editCursorTick();
      sendRulerClick(*fixture.rulerInput(), *fixture.view());
Original expression:
  QVERIFY(fixture.view()->editCursorTick() != ready)

A058 | PianoRollStaticTest::gatedAndReadyRulerScrub | src/checks/rollcheck/static/gate.cpp:314
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!fixture.view()->userGestureActive())

A059 | PianoRollStaticTest::gatedAndReadyScrollbarWheel | src/checks/rollcheck/static/gate.cpp:321
Disposition: RETIRED-REPRESENTATION

A060 | PianoRollStaticTest::gatedAndReadyScrollbarWheel | src/checks/rollcheck/static/gate.cpp:322
Disposition: RETIRED-REPRESENTATION

A061 | PianoRollStaticTest::gatedAndReadyScrollbarWheel | src/checks/rollcheck/static/gate.cpp:325
Disposition: PARTIAL
Mapping/reason: Related predicates S035 (src/checks/rollcheck/static/camera.swift), S038 (:347-349) and S005/S006 (:33-37) assert ready-session pan and bounds behavior; no predicate drives a scrollbar wheel while the tab is gated. Unproved: the gated scrollbar wheel leaves scrollX at its pre-wheel offset (gatedAndReadyScrollbarWheel replay).
Source context since previous assertion:
  ;
      const double gated = fixture.view()->camera().scrollX();
      sendScrollbarWheel(*fixture.horizontalScrollbar());
Original expression:
  QVERIFY(qFuzzyCompare(fixture.view()->camera().scrollX(), gated))

A062 | PianoRollStaticTest::gatedAndReadyScrollbarWheel | src/checks/rollcheck/static/gate.cpp:326
Disposition: RETIRED-REPRESENTATION

A063 | PianoRollStaticTest::gatedAndReadyScrollbarWheel | src/checks/rollcheck/static/gate.cpp:329
Disposition: PARTIAL
Mapping/reason: Related predicates S035 (src/checks/rollcheck/static/camera.swift) and S038 (:347-349) assert ready-session pans; the ready scrollbar-wheel path itself is not driven. Unproved: the ready scrollbar wheel moves scrollX off the gated value.
Source context since previous assertion:
  ;
      const double ready = fixture.view()->camera().scrollX();
      sendScrollbarWheel(*fixture.horizontalScrollbar());
Original expression:
  QVERIFY(!qFuzzyCompare(fixture.view()->camera().scrollX(), ready))

A064 | PianoRollStaticTest::gatedAndReadyRollZoom | src/checks/rollcheck/static/gate.cpp:336
Disposition: RETIRED-REPRESENTATION

A065 | PianoRollStaticTest::gatedAndReadyRollZoom | src/checks/rollcheck/static/gate.cpp:337
Disposition: RETIRED-REPRESENTATION

A066 | PianoRollStaticTest::gatedAndReadyRollZoom | src/checks/rollcheck/static/gate.cpp:340
Disposition: PARTIAL
Mapping/reason: Related predicates S031 (src/checks/rollcheck/static/camera.swift) and S032 (:274-276) assert the ready wheel zoom and its momentum suppression, and S042 (src/checks/rollcheck/static/gate.swift) the ready zoom movement; the MIDI-only loading stage has no Swift representation. Unproved: the gated roll wheel leaves pxPerBeat at its pre-wheel zoom before the voicegroup bind.
Source context since previous assertion:
  ;
      const double gated = fixture.view()->camera().pxPerBeat();
      sendRollWheel(*fixture.rollInput());
Original expression:
  QVERIFY(qFuzzyCompare(fixture.view()->camera().pxPerBeat(), gated))

A067 | PianoRollStaticTest::gatedAndReadyRollZoom | src/checks/rollcheck/static/gate.cpp:341
Disposition: RETIRED-REPRESENTATION

A068 | PianoRollStaticTest::gatedAndReadyRollZoom | src/checks/rollcheck/static/gate.cpp:344
Disposition: MATCHED
Mapping: S042

A069 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:359
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::tooltipFloatsBelowRuler_data()
  {
      QTest::addColumn<bool>("division");
      QTest::newRow("division") << true;
      QTest::newRow("feel") << false;
  }

  void PianoRollStaticTest::tooltipFloatsBelowRuler()
  {
      QFETCH(bool, division);
      GateFixture fixture;
      QString error;
Original expression:
  QVERIFY2(fixture.create(error), qPrintable(error))

A070 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:360
Disposition: RETIRED-REPRESENTATION

A071 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:361
Disposition: RETIRED-REPRESENTATION

A072 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:390
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      auto *const quick = fixture.view()->findChild<songview::TimelineQuickView *>(
          QStringLiteral("timelineQuickCanvas"));
      const QPointer<songview::TimelineQuickView> guardedQuick = quick;
      const QPointer<QQuickItem> guardedCanvasRoot = quick ? quick->rootObject() : nullptr;
      const QPointer<QQuickWindow> guardedQuickWindow = quick ? quick->quickWindow() : nullptr;
      const QPointer<QWindow> guardedHostWindow = fixture.tab()->windowHandle();
      const QPointer<QQuickItem> guardedControls = fixture.controls();
      const QPointer<QQuickItem> guardedDivisionControl = fixture.divisionControl();
      const QPointer<QQuickItem> guardedFeelControl = fixture.feelControl();
      const QPointer<QQuickItem> guardedToolTip = fixture.toolTip();
      const auto sceneReady = [&] {
          return guardedQuick && guardedCanvasRoot && guardedQuickWindow && guardedHostWindow &&
                 guardedControls && guardedDivisionControl && guardedFeelControl && guardedToolTip &&
                 fixture.tab()->isVisible() && fixture.tab()->windowHandle() == guardedHostWindow &&
                 guardedHostWindow->isExposed() && guardedQuickWindow->isVisible() &&
                 guardedQuickWindow->isExposed() && guardedCanvasRoot->isVisible() &&
                 guardedCanvasRoot->width() > 0.0 && guardedCanvasRoot->height() > 0.0 &&
                 guardedToolTip->window() == guardedQuickWindow;
      };
      const auto usableControl = [&sceneReady,
                                  &guardedQuickWindow](const QPointer<QQuickItem> &control) {
          return sceneReady() && control->isVisible() && control->isEnabled() &&
                 control->width() > 0.0 && control->height() > 0.0 &&
                 control->window() == guardedQuickWindow;
      };
      if (!QTest::qWaitFor([&] {
              return usableControl(guardedDivisionControl) && usableControl(guardedFeelControl);
          })) {
Original expression:
  QFAIL("ready ruler tooltip Quick surface did not become input-ready")

A073 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:451
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      const QPointer<QQuickItem> guardedControl =
          division ? guardedDivisionControl : guardedFeelControl;
  #ifdef Q_OS_WIN
      const QPoint originalCursorPosition = QCursor::pos();
      const auto restoreCursor =
          qScopeGuard([originalCursorPosition] { QCursor::setPos(originalCursorPosition); });
  #endif
      bool windowEntered = false;
      const auto setHover = [&guardedCanvasRoot, &guardedQuickWindow,
                             &windowEntered](const QPointer<QQuickItem> &control, bool entering) {
          if (!guardedCanvasRoot || !guardedQuickWindow || !control)
              return false;
          QQuickItem *const canvasRoot = guardedCanvasRoot.data();
          QQuickWindow *const quickWindow = guardedQuickWindow.data();
          const QPoint windowPosition =
              (entering
                   ? control->mapToScene(QPointF(control->width() / 2.0, control->height() / 2.0))
                   : canvasRoot->mapToScene(
                         QPointF(canvasRoot->width() / 2.0, canvasRoot->height() / 2.0)))
                  .toPoint();
  #ifdef Q_OS_WIN
          // QTest's QWindow mouse moves do not move the native cursor. Keep it
          // over the control so Windows cannot send a leave during the tooltip delay.
          QCursor::setPos(quickWindow->mapToGlobal(windowPosition));
  #endif
          if (entering) {
              if (!windowEntered) {
                  const QPointF point(windowPosition);
                  QEnterEvent enter(point, point, QPointF(quickWindow->mapToGlobal(windowPosition)));
                  QCoreApplication::sendEvent(quickWindow, &enter);
                  windowEntered = true;
              }
              if (!checks::events::primeMouseMove(*quickWindow, *control, windowPosition))
                  return false;
          }
          QTest::mouseMove(quickWindow, windowPosition);
          return true;
      };
      const auto hoverDiagnostic = [&sceneReady, &guardedQuickWindow,
                                    &guardedToolTip](const QPointer<QQuickItem> &control) {
          return QObject::tr(
                     "ruler tooltip did not appear while hovering the %1 control "
                     "(scene-ready=%2 hovered=%3 visible=%4 enabled=%5 size=%6x%7 window=%8 "
                     "tooltip-visibleForControl=%9 tooltip-text-empty=%10 tooltip-size=%11x%12)")
              .arg(control ? control->objectName() : QStringLiteral("<destroyed>"))
              .arg(sceneReady())
              .arg(control && control->property("hovered").toBool())
              .arg(control && control->isVisible())
              .arg(control && control->isEnabled())
              .arg(control ? control->width() : 0.0)
              .arg(control ? control->height() : 0.0)
              .arg(control && control->window() == guardedQuickWindow)
              .arg(guardedToolTip && guardedToolTip->property("visibleForControl").toBool())
              .arg(guardedToolTip && guardedToolTip->property("toolTipText").toString().isEmpty())
              .arg(guardedToolTip ? guardedToolTip->width() : 0.0)
              .arg(guardedToolTip ? guardedToolTip->height() : 0.0);
      };

      if (!usableControl(guardedControl)) {
Original expression:
  QFAIL("ready ruler tooltip Quick control was destroyed before hover delivery")

A074 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:455
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      if (!setHover(guardedControl, true)) {
          setHover(guardedControl, false);
Original expression:
  QFAIL(qUtf8Printable(QObject::tr("could not deliver ruler hover to the %1 control")
                                   .arg(guardedControl->objectName())))

A075 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:461
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      if (!QTest::qWaitFor(
              [&] { return sceneReady() && guardedControl && guardedToolTip->isVisible(); })) {
          setHover(guardedControl, false);
Original expression:
  QFAIL(qUtf8Printable(hoverDiagnostic(guardedControl)))

A076 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:464
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      if (!sceneReady() || !guardedControl || !guardedToolTip->isVisible()) {
Original expression:
  QFAIL("ready ruler tooltip Quick scene was destroyed after hover delivery")

A077 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:475
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      const QRectF tooltipRect(guardedToolTip->mapToItem(guardedCanvasRoot, QPointF(0, 0)),
                               guardedToolTip->size());
      const qreal rowBottom = guardedControls->mapToItem(guardedCanvasRoot, QPointF(0, 0)).y() +
                              rulerBandHeight(*fixture.view());
      // Room below the row exists, so the tooltip must float there instead
      // of covering the row or spilling past the canvas.
      if (rowBottom + tooltipRect.height() <= guardedCanvasRoot->height()) {
          if (tooltipRect.top() < rowBottom - 0.5) {
              setHover(guardedControl, false);
Original expression:
  QFAIL("ruler tooltip covered the ruler row instead of floating below it")

A078 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:479
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          }
          if (tooltipRect.bottom() > guardedCanvasRoot->height() + 0.5) {
              setHover(guardedControl, false);
Original expression:
  QFAIL("ruler tooltip spilled past the Quick canvas below the ruler row")

A079 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:483
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          }
          if (tooltipRect.left() < -0.5 || tooltipRect.right() > guardedCanvasRoot->width() + 0.5) {
              setHover(guardedControl, false);
Original expression:
  QFAIL("ruler tooltip left the Quick canvas while floating below the row")

A080 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:487
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          }
      }
      if (!setHover(guardedControl, false)) {
Original expression:
  QFAIL(qUtf8Printable(QObject::tr("could not deliver ruler hover leave from the %1 control")
                                   .arg(guardedControl->objectName())))

A081 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:493
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      if (!QTest::qWaitFor(
              [&] { return sceneReady() && guardedControl && !guardedToolTip->isVisible(); })) {
          if (!sceneReady() || !guardedControl || !guardedToolTip) {
Original expression:
  QFAIL("ready ruler tooltip Quick scene was destroyed while awaiting hover leave")

A082 | PianoRollStaticTest::tooltipFloatsBelowRuler | src/checks/rollcheck/static/gate.cpp:495
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/gate.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          } else {
Original expression:
  QFAIL("ruler tooltip stayed visible after the hover left the control")

Remaining original source (closing sequence and helper bodies):
  ;
          }
      }
  }

  } // namespace checks::rollcheck::staticcheck


Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "caller-resolved minimum viewport produces observable camera bounds"

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick/content transforms round-trip with a fractional offset"

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal restoration preserves fractional offsets"

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "display projection snaps to a physical pixel"

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal scrolling clamps to production pre-roll"

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound timeline end reaches the plot origin"

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport shrink reclamps the changed pre-roll floor"

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport updates preserve a legal fractional offset"

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "interactive time zoom preserves its tick anchor"

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "time zoom reaches its bound when the pointer anchor is unreachable"

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unanchored restoration keeps valid fractional camera state"

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "restoration sanitizes non-finite transient view-state values independently"

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "content shrink reclamps the horizontal offset"

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "viewport growth reclamps vertical content"

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "an unbound timeline homes to its lead pad"

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unbound viewport updates follow the resolved home"

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "chromatic pitch mapping remains bijective across the full MIDI range"

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch rows and nearest lookup preserve production ordering"

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row and pitch inverses agree"

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional DPR snapping defines the inverse row boundary"

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch projection shrink reclamps the vertical offset"

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "vertical bounds derive from projected row height"

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom preserves its row anchor when bounds permit"

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom reaches its bound when the row anchor is unreachable"

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "key reveal scrolls only enough to show its row"

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "hidden pitches do not move the camera"

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick reveal uses the production one-third anchor"

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "a fitting range on the physical right boundary stays visible"

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal prefers its requested leading edge"

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal honors the requested trailing edge"

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "phase \(phase.rawValue) performs one anchored time zoom publication"

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses time zoom without publishing"

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "pixel deltas receive five-times angle weighting"

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "effective angle and pixel zooms each publish once"

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift wheel pans by negative selected delta exactly once"

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at minimum and repeated clamped pan is silent"

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at maximum and repeated clamped pan is silent"

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal-only momentum still pans exactly once"

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Ctrl wheel performs one row-anchored pitch zoom publication"

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses pitch zoom without publishing"

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter wheel scrolls by negative half-delta exactly once"

S042 | checkReadyRollZoom | src/checks/rollcheck/static/gate.swift
Anchor: message "a roll wheel at (80, 100) changes time zoom on a ready song"
