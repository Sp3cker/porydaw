Assertion correspondence: rollcheck/static/geometry.cpp

Reference revision: 85b97239ce94dc4c4cf5f3f4fb66fc96ff2cf27e
Original: src/checks/rollcheck/static/geometry.cpp
Original SHA-256: 9c3dcb0a9fdbab901eaeac2d1271951e0d6c7b648564b15a53175d989e465979
Swift counterpart: src/checks/rollcheck/static/camera.swift
Additional Swift counterpart: src/checks/rollcheck/static/geometry.swift
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift counterpart: src/checks/rollcheck/time_signature_prompt.swift
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter swiftcore --verbose
Result: PASS; 1/14 ok (controller run recorded before this ledger edit; the edit changes no Swift source).
Command: deno task verify:qml-roll --verbose
Result: PASS; 1/1 ok (controller run recorded before this ledger edit; the edit changes no QML source).
Command: deno task verify:shell --verbose
Result: PASS; 11/11 ok (controller run recorded before this ledger edit; the edit changes no shell source).
Verified Swift SHA-256: 95fb2e80f95bf57e41250632cb8b3e7378fa32be143d02f5bd092313f6029808 (camera.swift)
Narrow form used while iterating: deno task verify --filter swiftcore --qt projectSession.
Converted-window case cited: src/checks/rollqml/tst_SwiftRollPlots.qml (sha256 b5d3e154b76f6c4a523667124d7d46c08a5dff9d62f5b30d918d674596c1118d, re-pinned for the task-27 other-events band revision: the canonical band layout now subtracts the mounted other-events band height).
Command: deno task verify:qml-roll --verbose (task-27 other-events band revision)
Result: PASS; 1/1 ok. Lane-owner observation on the revision now pinned; this bookkeeping pass ran no lane.

A001 | PianoRollStaticTest::fallbackCamera | src/checks/rollcheck/static/geometry.cpp:117
Disposition: RETIRED-REPRESENTATION

A002 | PianoRollStaticTest::fallbackCamera | src/checks/rollcheck/static/geometry.cpp:121
Disposition: PARTIAL
Mapping/reason: Related predicate S045 (src/checks/rollcheck/static/camera.swift) observes the same fallback-camera lead-pad home over the 1280/1000/1500 width rows under synthetic EditorCamera limits, with S005 (:33-34) covering the pre-roll floor. Unproved: the native BareView fallback camera reports a positive leadPadPx with no song bound.
Source context since previous assertion:
  ;
      fixture.view.quickView()->quickWindow()->resize(QSize(width, 800));
      QCoreApplication::processEvents();
      const double leadPad = fixture.view.camera().leadPadPx();
Original expression:
  QVERIFY2(leadPad > 0.0, "fallback lead pad is not positive")

A003 | PianoRollStaticTest::fallbackCamera | src/checks/rollcheck/static/geometry.cpp:122
Disposition: PARTIAL
Mapping/reason: Related predicate S045 (src/checks/rollcheck/static/camera.swift) asserts contentX(tick: 0) equals the lead pad under synthetic limits, and S002 (:26-27) the tick/content round-trip. Unproved: the native fallback camera tick-zero content position sits within 0.5px of its lead pad.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY2(std::abs(fixture.view.camera().contentX(0.0) - leadPad) <= 0.5,
               "fresh view camera is not at the pre-roll home")

A004 | PianoRollStaticTest::fallbackCamera | src/checks/rollcheck/static/geometry.cpp:124
Disposition: PARTIAL
Mapping/reason: Related predicate S062 (src/checks/rollcheck/static/geometry.swift) observes the default beat zoom retained across a 24-TPB bind and S064 (:46-47) across a 48-TPB bind under EditorCamera.Limits defaults. Unproved condition: the unbound native BareView default pxPerBeat equals the default ViewState zoom.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(fixture.view.camera().pxPerBeat(), SongView::ViewState{}.pxPerBeat)

A005 | PianoRollStaticTest::opaqueResizeBacking | src/checks/rollcheck/static/geometry.cpp:131
Disposition: RETIRED-REPRESENTATION

A006 | PianoRollStaticTest::opaqueResizeBacking | src/checks/rollcheck/static/geometry.cpp:133
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const QColor background = themes::color(themes::Role::window_background);
Original expression:
  QCOMPARE(window->color(), background)

A007 | PianoRollStaticTest::opaqueResizeBacking | src/checks/rollcheck/static/geometry.cpp:134
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(window->color().alpha(), 255)

A008 | PianoRollStaticTest::opaqueResizeBacking | src/checks/rollcheck/static/geometry.cpp:138
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;

      window->resize(QSize(1000, 700));
      QCoreApplication::processEvents();
Original expression:
  QCOMPARE(window->color(), background)

A009 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:155
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts the published grid time marks stay inside one culling window and cover the visible plot (suite 10). Unproved condition: the unsignatured grid publishes exactly 16 lines over 0..96 ticks.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::fallbackGrid()
  {
      BareView fixture;
      struct Line {
          Tick tick;
          bool bar;
          int barNumber;
          int beatNumber;
      };
      std::vector<Line> lines;
      fixture.view.forEachGridLine(0, 4 * kBeatsPerBar * kTicksPerBeat,
                                   [&lines](Tick tick, bool bar, int barNumber, int beatNumber) {
                                       lines.push_back({tick, bar, barNumber, beatNumber});
                                   });
Original expression:
  QCOMPARE(lines.size(), size_t(16))

A010 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:158
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts the published grid time marks positions (suite 10). Unproved condition: each fallback line sits at index * kTicksPerBeat (24).
Source context since previous assertion:
  ;
      for (int index = 0; index < 16; ++index) {
          const Line line = lines[size_t(index)];
Original expression:
  QCOMPARE(line.tick, uint64_t(index) * kTicksPerBeat)

A011 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:159
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts the published grid time marks positions (suite 10). Unproved condition: every fourth fallback line is a bar line.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(line.bar, index % 4 == 0)

A012 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:160
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts the published grid time marks positions (suite 10). Unproved condition: fallback bar numbers are index / 4 + 1.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(line.barNumber, index / 4 + 1)

A013 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:161
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/EditorGridCameraChecks.swift asserts the published grid time marks positions (suite 10). Unproved condition: fallback beat numbers are index % 4 + 1.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(line.beatNumber, index % 4 + 1)

A014 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:164
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.start/beatsPerBar/beatTicks on a 7/8 axis built from the rebuilt timeline (suite 10). Unproved condition: the unsignatured fallback segment at tick 0 starts at 0.
Source context since previous assertion:
  ;
      }
      const songview::Grid::Segment segment = fixture.view.grid().segmentAt(0);
Original expression:
  QCOMPARE(segment.start, uint64_t(0))

A015 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:165
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.start/beatsPerBar/beatTicks on a 7/8 axis (suite 10); it does not assert the next boundary. Unproved condition: the unsignatured fallback segment next is the kNoTick sentinel.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.next, CoreTimeDefaults::kNoTick)

A016 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:166
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.beatTicks on a 7/8 axis (suite 10). Unproved condition: the unsignatured fallback segment beatTicks is 24.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.beatTicks, kTicksPerBeat)

A017 | PianoRollStaticTest::fallbackGrid | src/checks/rollcheck/static/geometry.cpp:167
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.beatsPerBar on a 7/8 axis (suite 10). Unproved condition: the unsignatured fallback segment beatsPerBar is 4.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.beatsPerBar, kBeatsPerBar)

A018 | PianoRollStaticTest::tickCeilingDoesNotWrap | src/checks/rollcheck/static/geometry.cpp:177
Disposition: GAP
Mapping/reason: GAP; no executing predicate walks grid lines at the tick ceiling. The nearest executed grid-mark predicate (src/checks/rollcheck/EditorGridCameraChecks.swift) culls generated marks inside one viewport and never approaches CoreTimeDefaults kMaxTick, so the ceiling traversal stays unproved.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::tickCeilingDoesNotWrap()
  {
      BareView fixture;
      std::vector<Tick> ticks;
      fixture.view.forEachGridLine(CoreTimeDefaults::kMaxTick - kTicksPerBeat,
                                   CoreTimeDefaults::kNoTick,
                                   [&ticks](Tick tick, bool, int, int) { ticks.push_back(tick); });
Original expression:
  QVERIFY(ticks.size() <= 1)

A019 | PianoRollStaticTest::tickCeilingDoesNotWrap | src/checks/rollcheck/static/geometry.cpp:178
Disposition: GAP
Mapping/reason: GAP; no executing predicate excludes a wrapped ceiling beat. The nearest executed grid-mark predicate (src/checks/rollcheck/EditorGridCameraChecks.swift) never approaches CoreTimeDefaults kMaxTick, so the wrap exclusion stays unproved.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(ticks.empty() || ticks.front() >= CoreTimeDefaults::kMaxTick - kTicksPerBeat)

A020 | PianoRollStaticTest::preRollRulerShade | src/checks/rollcheck/static/geometry.cpp:185
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::preRollRulerShade()
  {
      BareView fixture;
      const Raster raster = captureRuler(fixture.view);
Original expression:
  QVERIFY2(raster.valid(), "fresh ruler raster could not be captured")

A021 | PianoRollStaticTest::preRollRulerShade | src/checks/rollcheck/static/geometry.cpp:192
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const qreal plotOffset = rulerPlotOffset(fixture.view);
      const qreal tickZero = plotOffset + fixture.view.camera().contentX(0.0);
      const QRgb padUpper = raster.at(plotOffset + 4.0, raster.image.height() / raster.dpr * 0.5);
      const QRgb padLower = raster.at(plotOffset + 4.0 + (tickZero - plotOffset) * 0.5,
                                      raster.image.height() / raster.dpr - 4.0);
      const QRgb chrome = raster.at(tickZero + 80.0, 2.0);
Original expression:
  QVERIFY(channelDelta(padUpper, padLower) <= 2)

A022 | PianoRollStaticTest::preRollRulerShade | src/checks/rollcheck/static/geometry.cpp:193
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(channelDelta(padUpper, chrome) > 1)

A023 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:200
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
  }

  void PianoRollStaticTest::fallbackRulerStemAndBars()
  {
      BareView fixture;
      const Raster raster = captureRuler(fixture.view);
Original expression:
  QVERIFY2(raster.valid(), "fresh ruler raster could not be captured")

A024 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:214
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const qreal offset = rulerPlotOffset(fixture.view);
      const int tickZero = raster.deviceX(offset + fixture.view.camera().contentX(0.0));
      const QRgb placeholder = QGuiApplication::palette().color(QPalette::PlaceholderText).rgb();
      const int top = int(raster.image.height() * 0.05);
      const int bottom = int(raster.image.height() * 0.45);
      int longestStem = 0;
      for (int x = tickZero - 1; x <= tickZero + 1; ++x) {
          int matching = 0;
          for (int y = top; y <= bottom; ++y)
              if (channelDelta(raster.image.pixel(x, y), placeholder) <= 12)
                  ++matching;
          longestStem = std::max(longestStem, matching);
      }
Original expression:
  QVERIFY(longestStem >= (bottom - top) * 7 / 10)

A025 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:218
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const int captionLeft = std::clamp(raster.deviceX(offset + 4.0), 0, raster.image.width());
      const int captionRight =
          std::clamp(tickZero - qRound(4.0 * raster.dpr), 0, raster.image.width());
Original expression:
  QVERIFY(captionRight > captionLeft)

A026 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:224
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      int captionPixels = 0;
      for (int y = 0; y < raster.image.height(); ++y)
          for (int x = captionLeft; x < captionRight; ++x)
              if (channelDelta(raster.image.pixel(x, y), placeholder) <= 12)
                  ++captionPixels;
Original expression:
  QCOMPARE(captionPixels, 0)

A027 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:233
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const QRgb backdrop = raster.at(offset + fixture.view.camera().leadPadPx() + 80.0, 2.0);
      int paintedBars = 0;
      for (int bar = 0; bar < 6; ++bar) {
          const Tick tick = Tick(bar) * kBeatsPerBar * kTicksPerBeat;
          const int expected =
              raster.deviceX(fixture.view.camera().displayX(double(tick), offset, raster.dpr));
          const int line = detectedLine(raster, expected, int(raster.image.height() * 0.55),
                                        raster.image.height() - 3, backdrop);
Original expression:
  QVERIFY2(line >= 0, "fresh ruler is missing a fallback bar line")

A028 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:240
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          ++paintedBars;
          int labelPixels = 0;
          for (int x = expected + 3; x <= expected + int(34 * raster.dpr); ++x)
              for (int y = int(raster.image.height() * 0.55); y < raster.image.height() - 3; ++y)
                  if (channelDelta(raster.image.pixel(x, y), backdrop) > 30)
                      ++labelPixels;
Original expression:
  QVERIFY(labelPixels >= 2)

A029 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:242
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
Original expression:
  QVERIFY(paintedBars >= 2)

A030 | PianoRollStaticTest::fallbackRulerStemAndBars | src/checks/rollcheck/static/geometry.cpp:246
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      for (int beat = 1; beat <= 3; ++beat) {
          const int expected = raster.deviceX(
              fixture.view.camera().displayX(double(beat * kTicksPerBeat), offset, raster.dpr));
Original expression:
  QVERIFY(lineAt(raster, expected, int(raster.image.height() * 0.72),
                         raster.image.height() - 3, backdrop, 6, 1))

A031 | PianoRollStaticTest::defaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:255
Disposition: RETIRED-REPRESENTATION

A032 | PianoRollStaticTest::defaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:270
Disposition: MATCHED
Mapping: S062

A033 | PianoRollStaticTest::defaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:272
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Raster after = captureRuler(fixture.view);
Original expression:
  QVERIFY(after.valid())

A034 | PianoRollStaticTest::defaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:277
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      for (int bar = 0; bar < 6; ++bar) {
          const Tick tick = Tick(bar) * kBeatsPerBar * kTicksPerBeat;
          const int expected =
              after.deviceX(fixture.view.camera().displayX(double(tick), offset, after.dpr));
Original expression:
  QCOMPARE(detectedLine(after, expected, int(after.image.height() * 0.55),
                                after.image.height() - 3, backdrop),
                   columns[size_t(bar)])

A035 | PianoRollStaticTest::defaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:280
Disposition: MATCHED
Mapping: S063

A036 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:291
Disposition: RETIRED-REPRESENTATION

A037 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:312
Disposition: MATCHED
Mapping: S064

A038 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:314
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Raster after = captureRuler(fixture.view);
Original expression:
  QVERIFY(after.valid())

A039 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:318
Disposition: MATCHED
Mapping: S065

A040 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:319
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(lineAt(after, expected, int(after.image.height() * .72), after.image.height() - 3,
                         backdrop, 6, 1))

A041 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:326
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      }
      for (int bar = 0; bar < 6; ++bar) {
          const Tick tick = Tick(bar) * kBeatsPerBar * 48;
          const int expected =
              after.deviceX(fixture.view.camera().displayX(double(tick), offset, after.dpr));
Original expression:
  QCOMPARE(detectedLine(after, expected, int(after.image.height() * .55),
                                after.image.height() - 3, backdrop),
                   barColumns[size_t(bar)])

A042 | PianoRollStaticTest::ticksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.cpp:331
Disposition: MATCHED
Mapping: S066

A043 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:341
Disposition: RETIRED-REPRESENTATION

A044 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:359
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.start on a 7/8 axis built from the rebuilt timeline (suite 10). Unproved condition: the 3/4-bound view segment at tick 0 starts at 0.
Source context since previous assertion:
  ;
      const qreal offset = rulerPlotOffset(fixture.view);
      const QRgb backdrop = before.at(offset + fixture.view.camera().leadPadPx() + 80.0, 2.0);
      std::array<int, 6> fallbackBars{};
      std::array<double, 8> beats{};
      for (int bar = 0; bar < 6; ++bar) {
          const Tick tick = Tick(bar) * kBeatsPerBar * kTicksPerBeat;
          fallbackBars[size_t(bar)] = detectedLine(
              before,
              before.deviceX(fixture.view.camera().displayX(double(tick), offset, before.dpr)),
              int(before.image.height() * .55), before.image.height() - 3, backdrop);
      }
      for (int beat = 1; beat <= 8; ++beat)
          beats[size_t(beat - 1)] = fixture.view.camera().contentX(double(beat * kTicksPerBeat));
      const int height = rulerBandHeight(fixture.view);
      fixture.view.setSong(&fixture.t34, nullptr);
      QCoreApplication::processEvents();
      const songview::Grid::Segment segment = fixture.view.grid().segmentAt(0);
Original expression:
  QCOMPARE(segment.start, uint64_t(0))

A045 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:360
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.start/beatsPerBar/beatTicks on a 7/8 axis (suite 10); it does not assert the next boundary. Unproved condition: the 3/4-bound segment next is the kNoTick sentinel.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.next, CoreTimeDefaults::kNoTick)

A046 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:361
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.beatTicks on a 7/8 axis (suite 10). Unproved condition: the 3/4-bound segment beatTicks is 24.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.beatTicks, kTicksPerBeat)

A047 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:362
Disposition: PARTIAL
Mapping/reason: Related predicate: src/checks/rollcheck/time_signature_prompt.swift asserts GridSegment.beatsPerBar on a 7/8 axis (suite 10). Unproved condition: the 3/4-bound segment beatsPerBar is 3.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(segment.beatsPerBar, 3)

A048 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:363
Disposition: GAP
Mapping/reason: GAP; the mounted cases consume gridModel.rulerHeight only as a layout input (src/checks/rollqml/tst_SwiftRollPlots.qml) and none compares the bound view's ruler height across a 3/4 bind, so the transition stays unproved.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(rulerBandHeight(fixture.view), height)

A049 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:364
Disposition: PARTIAL
Mapping/reason: Related predicate S045 (src/checks/rollcheck/static/camera.swift) asserts a positive lead pad under synthetic limits, and S015 (:93-94) the unbound home. Unproved: the 3/4-bound native view reports a positive leadPadPx.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(fixture.view.camera().leadPadPx() > 0.0)

A050 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:366
Disposition: PARTIAL
Mapping/reason: Related predicate S066 (src/checks/rollcheck/static/geometry.swift) asserts beat content positions preserved across a 48-TPB bind. Unproved condition: the 3/4 signature bind preserves the view's beat content positions within 1e-6.
Source context since previous assertion:
  ;
      for (int beat = 1; beat <= 8; ++beat)
Original expression:
  QVERIFY(std::abs(fixture.view.camera().contentX(double(beat * kTicksPerBeat)) -
                           beats[size_t(beat - 1)]) <= 1e-6)

A051 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:369
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      const Raster after = captureRuler(fixture.view);
Original expression:
  QVERIFY(after.valid())

A052 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:377
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
      bool moved = false;
      for (int bar = 0; bar < 6; ++bar) {
          const Tick tick = Tick(bar) * 3 * kTicksPerBeat;
          const int expected =
              after.deviceX(fixture.view.camera().displayX(double(tick), offset, after.dpr));
          const int line = detectedLine(after, expected, int(after.image.height() * .55),
                                        after.image.height() - 3, backdrop);
Original expression:
  QVERIFY(line >= 0)

A053 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:378
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(std::abs(line - expected) <= 1)

A054 | PianoRollStaticTest::signatureGroupingKeepsBeatsAndMovesBars | src/checks/rollcheck/static/geometry.cpp:381
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/static/geometry.cpp) no longer builds or runs. Previous native note: Original native fixture/window/rendering obligation. A direct presenter check cannot establish this observation.
Source context since previous assertion:
  ;
          moved = moved || line != fallbackBars[size_t(bar)];
      }
Original expression:
  QVERIFY2(moved, "3/4 binding did not move the signature-dependent bar lines")

Remaining original source (closing sequence and helper bodies):
  ;
  }

  } // namespace checks::rollcheck::staticcheck


Swift assertion predicates

S001 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "caller-resolved minimum viewport produces observable camera bounds"

S002 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick/content transforms round-trip with a fractional offset"

S003 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal restoration preserves fractional offsets"

S004 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "display projection snaps to a physical pixel"

S005 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal scrolling clamps to production pre-roll"

S006 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound timeline end reaches the plot origin"

S007 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport shrink reclamps the changed pre-roll floor"

S008 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "bound viewport updates preserve a legal fractional offset"

S009 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "interactive time zoom preserves its tick anchor"

S010 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "time zoom reaches its bound when the pointer anchor is unreachable"

S011 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unanchored restoration keeps valid fractional camera state"

S012 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "restoration sanitizes non-finite transient view-state values independently"

S013 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "content shrink reclamps the horizontal offset"

S014 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "viewport growth reclamps vertical content"

S015 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "an unbound timeline homes to its lead pad"

S016 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "unbound viewport updates follow the resolved home"

S017 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "chromatic pitch mapping remains bijective across the full MIDI range"

S018 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch rows and nearest lookup preserve production ordering"

S019 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "DPR-snapped row and pitch inverses agree"

S020 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "fractional DPR snapping defines the inverse row boundary"

S021 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch projection shrink reclamps the vertical offset"

S022 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "vertical bounds derive from projected row height"

S023 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom preserves its row anchor when bounds permit"

S024 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "pitch zoom reaches its bound when the row anchor is unreachable"

S025 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "key reveal scrolls only enough to show its row"

S026 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "hidden pitches do not move the camera"

S027 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "tick reveal uses the production one-third anchor"

S028 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "a fitting range on the physical right boundary stays visible"

S029 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal prefers its requested leading edge"

S030 | runEditorCameraChecks | src/checks/rollcheck/static/camera.swift
Anchor: message "wide range reveal honors the requested trailing edge"

S031 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "phase \(phase.rawValue) performs one anchored time zoom publication"

S032 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses time zoom without publishing"

S033 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "pixel deltas receive five-times angle weighting"

S034 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "effective angle and pixel zooms each publish once"

S035 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift wheel pans by negative selected delta exactly once"

S036 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at minimum and repeated clamped pan is silent"

S037 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Shift pan clamps at maximum and repeated clamped pan is silent"

S038 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "horizontal-only momentum still pans exactly once"

S039 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "Ctrl wheel performs one row-anchored pitch zoom publication"

S040 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "momentum suppresses pitch zoom without publishing"

S041 | checkGridCameraWheel | src/checks/rollcheck/static/camera.swift
Anchor: message "gutter wheel scrolls by negative half-delta exactly once"

S062 | checkDefaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.swift
Anchor: message "A032 binding 4/4 preserves the default beat zoom"

S063 | checkDefaultBindKeepsGeometry | src/checks/rollcheck/static/geometry.swift
Anchor: message "A035 bar \(bar) keeps unbound content geometry"

S064 | checkTicksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.swift
Anchor: message "A037 binding 48 TPB retains the default beat zoom"

S065 | checkTicksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.swift
Anchor: message "A039 beat \(beat) retains its display column"

S066 | checkTicksPerBeatKeepsGeometry | src/checks/rollcheck/static/geometry.swift
Anchor: message "A042 beat \(beat) retains its content coordinate"
