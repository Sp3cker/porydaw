Assertion correspondence: rollcheck/keyboard.cpp

Reference revision: a1244957bb05a59d62b8912e053d49b2a6f3d771
Original: src/checks/rollcheck/keyboard.cpp
Original SHA-256: 35206c7f5c5ea64c6eca1fcc455d8559248b5f974a71989f8306cc2bfe9ebaee
Swift counterpart: src/checks/rollcheck/keyboard.swift
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift counterpart: src/checks/rollcheck/selection.swift (A056 click-clears-time-selection predicate, cppID swiftcore/PianoRollTest::keyboardTimeSelectionShortcuts)
Additional Swift counterpart: src/checks/drawerpresentation/other_events_band.swift
Additional Swift counterpart: src/checks/editorqml/tst_EditorDrawer.qml (converted QML drawer lane)
Retained native counterpart: src/checks/swiftrollgated/gesturechecks.cpp
Retained native SHA-256: 4ade53282b20a324a89d990e0f8fbc61c927a534b022bce0affc843b178e445a
Retired in: 4d7fa617 Delete C++ window host, main.cpp and legacy keymap/layout/typography/theme C++; the pinned SHA is the 5f0924e8 blob, resolvable via git show 5f0924e8:src/checks/swiftrollgated/gesturechecks.cpp. S001-S028 index that pinned revision and close no A entry.
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources


Command: deno task verify --filter projectstore --filter swiftcore --verbose
Result: PASS — 24 applicable entries, 7 filtered skips; runtime swiftcore 20.39s (controller-recorded run; frozen keyboard.swift source unchanged).
The swiftcore lane executes the four checkKeyboardSkipsGhosts report.expect predicates indexed as S078–S081 under cppID swiftcore/PianoRoll::keyboardTranspose, and build/proof-evidence/swiftcore.json records all four anchor literals.
Command: deno task verify --filter swiftcore --verbose; deno task verify:qml --verbose (editorqml-drawer); deno task verify:qml-roll --verbose
Result: PASS — swiftcore 12 of 37 ok (25 filtered skips), editorqml-drawer 1/1 ok, verify:qml-roll 1/1 ok. Lane-owner observations on the task-27 other-events band revision; this bookkeeping pass ran no lane. build/proof-evidence/editorqml-drawer.json, swiftcore-projectsession.json and swiftroll-window.json record the predicates anchored as S083–S086.
Command: deno task verify --filter swiftcore --verbose (task-35 roll-pointer handoff, locked at /tmp/porydaw-build.lock)
Result: PASS — implementer-reported 12/37 ok, 25 skipped on the frozen tree; S087's row is recorded in build/proof-evidence/swiftcore-projectsession.json under cppID swiftcore/PianoRollTest::keyboardTimeSelectionShortcuts. This bookkeeping pass ran no lane.
Command: deno task verify --filter swiftcore --qt projectSession --verbose, and
deno task verify:shell --filter shell-grid-menu --verbose (task-53 sources)
Result: evidence re-read, lane green status not established here. build/proof-evidence/
swiftcore-projectsession.json (5710 pass rows across 515 cppIDs, mtime 2026-09-26 14:20) and
build/proof-evidence/shell-grid-menu.json (0 pass rows, 23 test functions, mtime 14:19) postdate
the frozen keyboard.swift (13:55), ruler_loop_menu.swift (14:03) and tst_ShellGridMenu.qml (14:16)
edits, so the new S088-S110 anchor literals classify executed under
`deno task proof check --executed`. The ruler-scope presenter predicates run under
swiftcore/PianoRoll::timelineRulerScope and keyboardTimeSelectionShortcuts in the
swiftcore projectSession lane, which the controller re-runs on the settled tree. This proof-only
reconciliation ran no lane, rebuilt nothing and re-ran no check.

A001 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:38
Disposition: MATCHED
Mapping: S043

A002 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:53
Disposition: MATCHED
Mapping: S044

A003 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:56
Disposition: MATCHED
Mapping: S045

A004 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:59
Disposition: PARTIAL
Mapping/reason: S046 keyboard.swift checks the one-snap Right nudge after the same semitone/octave transpose sequence; PARTIAL remains because native key-event delivery and the original rollcheck song and fixture are not established.
Source context since previous assertion:
      sendKeyStroke(roll, Qt::Key_Right, Qt::NoModifier, false);
Original expression:
  QVERIFY2(doc.findNote(check.track(), d.tick + snapCell, uint8_t(d.key - 11), &note),
               "Right did not nudge one snap cell right");

A005 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:64
Disposition: MATCHED
Mapping: S047

A006 | PianoRollTest::keyboardTranspose | src/checks/rollcheck/keyboard.cpp:68
Disposition: GAP
Mapping/reason: Swift S048 keyboard.swift restores the pre-seed document, whereas this original comparison restores the post-seed snapshot; that transaction boundary remains unproved.
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A007 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:75
Disposition: MATCHED
Mapping: S043

A008 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:86
Disposition: MATCHED
Mapping: S061

A009 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:89
Disposition: MATCHED
Mapping: S061

A010 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:95
Disposition: MATCHED
Mapping: S062

A011 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:98
Disposition: MATCHED
Mapping: S063

A012 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:100
Disposition: MATCHED
Mapping: S064

A013 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:108
Disposition: MATCHED
Mapping: S065

A014 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:112
Disposition: MATCHED
Mapping: S066

A015 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:120
Disposition: MATCHED
Mapping: S067

A016 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:125
Disposition: MATCHED
Mapping: S068

A017 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:129
Disposition: MATCHED
Mapping: S069

A018 | PianoRollTest::keyboardKeepVisible | src/checks/rollcheck/keyboard.cpp:133
Disposition: MATCHED
Mapping: S070

A019 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:140
Disposition: GAP
Mapping/reason: GAP: harness fixture guard (makeResizeSeed) — the ruler-scope counterpart (checkRulerSweepScope/checkRulerTapAndCancel in ruler_loop_menu.swift) builds its fixture from the session document and asserts no seeded-note predicate; S043/S048 guard the transpose slot's seed instead.
Source context since previous assertion:
  }

  void PianoRollTest::timelineRulerScope()
  {
      PianoRollFixture &check = *m_fixture;
      const std::optional<ResizeFixture> seed = makeResizeSeed(check);
Original expression:
  QVERIFY(seed.has_value());

A020 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:151
Disposition: GAP
Mapping/reason: GAP: harness fixture guard — the seed-note lookup for the ruler-scope slot has no counterpart predicate; the transpose behavior it stages is proved by S044/S045 in the transpose slot.
Source context since previous assertion:
      SongDocument &doc = check.document();
      SongView &view = check.view();
      songview::TimelineInputItem &roll = check.rollInput();
      const int track = check.track();
      const int pianoKeyboardWidth = check.pianoKeyboardWidth();
      const Cell &d = seed->cell;
      const Tick snapCell = seed->snapCell;
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();
      DocNote transposed;
Original expression:
  QVERIFY2(doc.findNote(track, d.tick, uint8_t(d.key), &transposed),
               "timeline scope seed note was not found");

A021 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:154
Disposition: GAP
Mapping/reason: GAP: harness fixture guard — the post-transpose seed state of the ruler-scope slot has no counterpart predicate; the transpose behaviors are proved by S044/S045 in the transpose slot.
Source context since previous assertion:
      doc.moveNotes({transposed}, int64_t(snapCell), -11);
Original expression:
  QVERIFY2(doc.findNote(track, d.tick + snapCell, uint8_t(d.key - 11), &transposed),
               "timeline scope seed did not reach the expected post-transpose state");

A022 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:166
Disposition: GAP
Mapping/reason: GAP — no Quick track-header model predicate in Swift: no predicate looks up header records (task-66 owns the headers surface).
Source context since previous assertion:
      auto *quick =
          view.findChild<songview::TimelineQuickView *>(QStringLiteral("timelineQuickCanvas"));
      auto *rulerInput = quick && quick->rootObject()
                             ? quick->rootObject()->findChild<songview::TimelineInputItem *>(
                                   QStringLiteral("timelineRulerInput"))
                             : nullptr;
      const std::optional<songview::TimelineBandGeometry> rulerBand =
          view.timelineBandLayout().geometry(songview::TimelineBand::Ruler);
      auto *headers = headercheck::model(view);
Original expression:
  QVERIFY2(headers, "could not find the Quick track-header model");

A023 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:167
Disposition: GAP
Mapping/reason: GAP — the Quick header records-match-timeline reconciliation has no executing counterpart predicate (task-66 owns the headers surface).
Source context since previous assertion:
  (adjacent to previous assertion; no intervening source lines)
Original expression:
  QVERIFY2(trackheaders_test::recordsMatchTimeline(*headers, check.timeline(), doc.canAddTrack()),
               "Quick header records did not match the current timeline");

A024 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:169
Disposition: GAP
Mapping/reason: GAP: harness existence guard — the mounted lanes fetch the ruler input and band by objectName (control() in tst_ShellGridMenu.qml) with no message argument, so no predicate observes the lookup.
Source context since previous assertion:
  (adjacent to previous assertion; no intervening source lines)
Original expression:
  QVERIFY2(rulerInput && rulerBand, "could not find the time ruler");

A025 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:197
Disposition: MATCHED
Mapping: S072 (exact swept range with primary-only scope), S096

A026 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:232
Disposition: PARTIAL
Mapping/reason: PARTIAL: S073 asserts the modified sweep's exact range with a scope containing the primary and the intersecting track and S101 the mounted Control drag's added track.
Unproved: the original exact track-bitmask equality, which the predicates replace with scope containment.
Source context since previous assertion:
      uint32_t expectedScope = uint32_t{1} << track;
      const ViewNote *scopedGhost = nullptr;
      for (const ViewNote &note : view.model().notes) {
          if (note.startTick >= endTick)
              break;
          if (startTick < note.endTick()) {
              expectedScope |= uint32_t{1} << note.track;
              if (note.track != track && !scopedGhost)
                  scopedGhost = &note;
          }
      }
      const std::optional<int> secondaryRecord =
          scopedGhost ? trackheaders_test::rowForTrack(*headers, scopedGhost->track) : std::nullopt;
      const QColor plainOverlay = secondaryRecord
                                      ? headers
                                            ->data(headers->index(*secondaryRecord, 0),
                                                   songview::TrackHeaderModel::OverlayColorRole)
                                            .value<QColor>()
                                      : QColor{};
      const QImage plainHeaderFrame =
          secondaryRecord ? headercheck::captureBand(check, view) : QImage{};
      checks::events::sendMouse(*rulerInput, QEvent::MouseButtonPress, start, Qt::LeftButton,
                                Qt::LeftButton, Qt::ControlModifier);
      checks::events::sendMouse(*rulerInput, QEvent::MouseMove, activate, Qt::NoButton,
                                Qt::LeftButton, Qt::ControlModifier);
      checks::events::sendMouse(*rulerInput, QEvent::MouseMove, end, Qt::NoButton, Qt::LeftButton,
                                Qt::ControlModifier);
      checks::events::sendMouse(*rulerInput, QEvent::MouseButtonRelease, end, Qt::LeftButton,
                                Qt::NoButton, Qt::ControlModifier);
Original expression:
  QVERIFY2(view.selectionModel().timeSelection().startTick == startTick &&
                   view.selectionModel().timeSelection().endTick == endTick &&
                   view.selectionModel().storedTrackScope() == expectedScope,
               "modified ruler drag did not derive the overlapping note-track scope");

A027 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:236
Disposition: MATCHED
Mapping: S103 (the mounted fixture presents an overlapping secondary-track note inside the ruler viewport)

A028 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:251
Disposition: GAP
Mapping/reason: GAP — ghost-note viewport placement has no executing counterpart predicate; the Swift predicates assert the projected boxes, never their visibility in the roll viewport.
Source context since previous assertion:
      const SongView::ViewState priorViewState = view.viewState();
      SongView::ViewState ghostViewState = priorViewState;
      ghostViewState.scrollY = std::max(
          0.0, (127.5 - double(scopedGhost->key)) * ghostViewState.keyHeight - roll.height() / 2.0);
      view.applyViewState(ghostViewState);
      const SnappedRows ghostRows{view, roll};
      const QRectF ghostPlotBox = ghostRows.noteBox(ghostRows.noteRect(
          view.camera().displayX(double(scopedGhost->startTick), 0.0, ghostRows.dpr()),
          view.camera().displayX(double(scopedGhost->endTick()), 0.0, ghostRows.dpr()),
          scopedGhost->key));
      const QRectF ghostBandBox = ghostPlotBox.translated(pianoKeyboardWidth, 0.0);
      const QRectF visibleGhostBox = ghostBandBox.intersected(
          QRectF(pianoKeyboardWidth, 0.0, qreal(roll.width()), qreal(roll.height())));
      const QImage scopedImage = check.captureQuickFramebuffer();
Original expression:
  QVERIFY2(!visibleGhostBox.isEmpty(),
               "time-scoped ghost note is outside the horizontal viewport");

A029 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:256
Disposition: PARTIAL
Mapping/reason: PARTIAL: S076 rings the covered note and its time-scoped ghost at dpr 2 through the same track-scoped provider the roll reads.
Unproved: the framebuffer ring pixels this row samples at device pixels; the predicate asserts published scene frames.
Source context since previous assertion:
      const qreal scopedDpr = scopedImage.devicePixelRatio();
      const int centerX = qRound(visibleGhostBox.center().x() * scopedDpr);
      const int bottomY = qRound(visibleGhostBox.bottom() * scopedDpr) - 1;
Original expression:
  QVERIFY2(isSelectionRingColor(scopedImage.pixel(centerX, bottomY)),
               "time-scoped ghost note did not render its selection ring");

A030 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:262
Disposition: MATCHED
Mapping: S104 (the note's secondary header row is mounted)

A031 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:267
Disposition: MATCHED
Mapping: S102 (the time-scoped secondary header publishes its selection overlay)

A032 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:270
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION — the row guards the native header-band framebuffer capture (headercheck::captureBand) validity for the selection indicator; the Swift surface asserts the published overlay role (S102) and captures no band pixels.

A033 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:275
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION — the row recaptures the native header-band framebuffer (headercheck::captureBand) to prove the indicator repainted; the Swift surface publishes the overlay role instead, asserted by S102, and no Swift predicate captures band pixels.

A034 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:277
Disposition: RETIRED-REPRESENTATION
Mapping/reason: RETIRED-REPRESENTATION — the row compares two captured native header-band rasters (pixel equality) to prove the selection indicator rendered; the Swift surface asserts the published overlay value (S102) and has no framebuffer repaint observation.

A035 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:288
Disposition: MATCHED
Mapping: S074, S075, S097

A036 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:295
Disposition: MATCHED
Mapping: S098

A037 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:302
Disposition: MATCHED
Mapping: S099

A038 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:307
Disposition: MATCHED
Mapping: S100

A039 | PianoRollTest::timelineRulerScope | src/checks/rollcheck/keyboard.cpp:310
Disposition: PARTIAL
Mapping/reason: PARTIAL: S089 asserts the sweep scenario restores its fixture bytes after undoing the intersecting-note insertion.
Unproved: the slot-level unwind to the post-seed snapshot (the ruler-scope slot is not driven through withKeyboardSeed).
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A040 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:317
Disposition: RETIRED-REPRESENTATION
Mapping/reason: the retired native lane seeded its resize fixture through makeResizeSeed; the Swift roll builds its own synthetic document, so the native strip-slot seed guard has no counterpart.

A041 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:326
Disposition: MATCHED
Mapping: S083, S084

A042 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:334
Disposition: MATCHED
Mapping: S085

A043 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:351
Disposition: RETIRED-REPRESENTATION
Mapping/reason: the retired native lane compared two captured other-events band rasters around a moved time selection; the Swift surface asserts the strip projection (S085, S086), never band pixels.

A044 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:371
Disposition: RETIRED-REPRESENTATION
Mapping/reason: the retired native lane searched the captured band raster for the projected marker x; the projected marker position is now asserted by S086 (src/checks/drawerpresentation/other_events_band.swift) and no Swift predicate captures band pixels.

A045 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:373
Disposition: RETIRED-REPRESENTATION
Mapping/reason: the retired native lane searched the captured band raster for the rendered track-colored diamond; the marker color is now asserted by S085 (src/checks/drawerpresentation/other_events_band.swift) and no Swift predicate captures band pixels.

A046 | PianoRollTest::timelineOtherEventsStrip | src/checks/rollcheck/keyboard.cpp:378
Disposition: RETIRED-REPRESENTATION
Mapping/reason: the retired native lane unwound its seeded strip slot and compared the fixture's SMF bytes; the Swift roll seeds no native slot, so the harness restore has no counterpart.

A047 | PianoRollTest::timelinePartialSelectionRepaint | src/checks/rollcheck/keyboard.cpp:385
Disposition: GAP
Mapping/reason: GAP — no time-selection or repaint model in the Swift roll; so this repaint-slot seed guard has no executing counterpart.
Source context since previous assertion:
  }

  void PianoRollTest::timelinePartialSelectionRepaint()
  {
      PianoRollFixture &check = *m_fixture;
      const std::optional<ResizeFixture> seed = makeResizeSeed(check);
Original expression:
  QVERIFY(seed.has_value());

A048 | PianoRollTest::timelinePartialSelectionRepaint | src/checks/rollcheck/keyboard.cpp:404
Disposition: GAP
Mapping/reason: No executing check: the native C++ lane (src/checks/rollcheck/keyboard.cpp) no longer builds or runs. Previous native note: NATIVE — full-framebuffer repaint comparison: Swift asserts document and history state, never framebuffer pixels.
Source context since previous assertion:
      SongDocument &doc = check.document();
      SongView &view = check.view();
      const Cell &d = seed->cell;
      const Tick snapCell = seed->snapCell;
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();
      view.selectionModel().setTimeSelection({d.tick + snapCell,
                                              d.tick + 2 * snapCell,
                                              songview::EditorSelectionModel::TimeSelection::Tracks,
                                              {}});
      QCoreApplication::processEvents();
      auto movedSelection = view.selectionModel().timeSelection();
      ++movedSelection.endTick;
      view.selectionModel().setTimeSelection(movedSelection);
      QCoreApplication::processEvents();
      const QImage partialSelectionImage = check.captureQuickFramebuffer();
      check.roll().requestQuickUpdate(songview::PianoRollQuickDirty::All);
      QCoreApplication::processEvents();
Original expression:
  QCOMPARE(partialSelectionImage, check.captureQuickFramebuffer());

A049 | PianoRollTest::timelinePartialSelectionRepaint | src/checks/rollcheck/keyboard.cpp:407
Disposition: GAP
Mapping/reason: GAP — S048 (keyboard.swift) restores the pre-seed document, camera, and history identity, whereas this unwind targets the post-seed snapshot; the repaint slot SMF restoration is unproved.
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A050 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:414
Disposition: MATCHED
Mapping: S043, S105

A051 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:426
Disposition: MATCHED
Mapping: S092

A052 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:429
Disposition: MATCHED
Mapping: S093

A053 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:447
Disposition: PARTIAL
Mapping/reason: PARTIAL: S076 asserts the time-covered note and its time-scoped ghost publish the selection ring frame.
Unproved: the framebuffer ring pixel this row samples; the predicate asserts published scene frames.
Source context since previous assertion:
      songview::EditorSelectionModel::TimeSelection band;
      band.startTick = d.tick + snapCell;
      band.endTick = d.tick + 2 * snapCell;
      view.selectionModel().setTimeSelection(band);
      const QRectF selectedByTimeBox =
          rows.noteBox(
                  rows.noteRect(view.camera().displayX(double(transposed.tick), 0.0, rows.dpr()),
                                view.camera().displayX(double(transposed.tick + transposed.duration),
                                                       0.0, rows.dpr()),
                                transposed.key))
              .translated(pianoKeyboardWidth, 0.0);
      const QImage selectedByTimeImage = check.captureQuickFramebuffer();
      const qreal selectedByTimeDpr = selectedByTimeImage.devicePixelRatio();
      const int centerX = qRound(selectedByTimeBox.center().x() * selectedByTimeDpr);
      const int bottomY = qRound(selectedByTimeBox.bottom() * selectedByTimeDpr) - 1;
Original expression:
  QVERIFY2(isSelectionRingColor(selectedByTimeImage.pixel(centerX, bottomY)),
               "time-selected note did not show the normal selection ring");

A054 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:449
Disposition: MATCHED
Mapping: S077

A055 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:466
Disposition: MATCHED
Mapping: S109 (the mounted in-band click targets asserted empty note space)

A056 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:470
Disposition: MATCHED
Mapping: S087
Source context since previous assertion:
      const QPoint emptyInside(qRound(view.camera().displayX(double(emptyTick), 0.0, rows.dpr())),
                               rows.centerY(emptyKey));
      click(roll, emptyInside);
Original expression:
  QVERIFY2(!view.selectionModel().timeSelection().active(),
               "left-clicking empty space inside the time selection did not clear it");

A057 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:474
Disposition: MATCHED
Mapping: S093, S106

A058 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:477
Disposition: MATCHED
Mapping: S094, S107

A059 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:479
Disposition: MATCHED
Mapping: S094, S108

A060 | PianoRollTest::keyboardTimeSelectionShortcuts | src/checks/rollcheck/keyboard.cpp:482
Disposition: MATCHED
Mapping: S091 (the scenario unwind restores the slot's post-seed bytes)

A061 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:489
Disposition: GAP
Mapping/reason: GAP — no time-range duplication in PianoGrid; so this duplicate-time seed guard has no executing counterpart.
Source context since previous assertion:
  }

  void PianoRollTest::timelineDuplicateTime()
  {
      PianoRollFixture &check = *m_fixture;
      const std::optional<ResizeFixture> seed = makeResizeSeed(check);
Original expression:
  QVERIFY(seed.has_value());

A062 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:499
Disposition: GAP
Mapping/reason: GAP — no time duplication in PianoGrid: the duplicate-time seed lookup is unmodelled.
Source context since previous assertion:
      SongDocument &doc = check.document();
      SongView &view = check.view();
      songview::TimelineInputItem &roll = check.rollInput();
      const int track = check.track();
      const Cell &d = seed->cell;
      const Tick snapCell = seed->snapCell;
      const QByteArray before = doc.smf().write();
      const int undo = doc.undoStack()->index();
      DocNote transposed;
Original expression:
  QVERIFY2(doc.findNote(track, d.tick, uint8_t(d.key), &transposed),
               "duplicate-time seed note was not found");

A063 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:502
Disposition: GAP
Mapping/reason: GAP — no time duplication in PianoGrid: the duplicate-time post-transpose setup is unmodelled.
Source context since previous assertion:
      doc.moveNotes({transposed}, int64_t(snapCell), -11);
Original expression:
  QVERIFY2(doc.findNote(track, d.tick + snapCell, uint8_t(d.key - 11), &transposed),
               "duplicate-time seed did not reach the expected post-transpose state");

A064 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:522
Disposition: GAP
Mapping/reason: GAP — native host key delivery (sendKeyStroke Ctrl+D) into the roll input: the retired C++ lane owns this observation. The command-router arm is asserted at S008 (src/checks/rollcheck/note_commands.swift: the active time range owns the duplicate, advances one span and moves the cursor), but no Swift predicate delivers Ctrl+D through the mounted surface, and the exact undo-index +1 stays unproved (S008 asserts a changed history identity).
Source context since previous assertion:
      view.selectionModel().setTimeSelection({d.tick + snapCell,
                                              d.tick + 2 * snapCell,
                                              songview::EditorSelectionModel::TimeSelection::Tracks,
                                              {}});
      const songview::EditorSelectionModel::TimeSelection duplicateSource =
          view.selectionModel().timeSelection();
      const Tick duplicateSpan = duplicateSource.endTick - duplicateSource.startTick;
      const int duplicateUndoIndex = doc.undoStack()->index();
      const uint8_t duplicateKey = transposed.key;
      const auto hasNoteAt = [&](Tick tick) {
          DocNote note;
          return doc.findNote(track, tick, duplicateKey, &note);
      };
      sendKeyStroke(roll, Qt::Key_D, Qt::ControlModifier, false);
      const Tick firstStart = duplicateSource.endTick;
      const Tick firstEnd = firstStart + duplicateSpan;
      const songview::EditorSelectionModel::TimeSelection firstSelection =
          view.selectionModel().timeSelection();
Original expression:
  QVERIFY2(doc.undoStack()->index() == duplicateUndoIndex + 1 && firstSelection.active() &&
                   firstSelection.startTick == firstStart && firstSelection.endTick == firstEnd &&
                   view.editCursorTick() == firstEnd && hasNoteAt(firstStart),
               "Ctrl+D did not duplicate once and advance the time selection");

A065 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:528
Disposition: GAP
Mapping/reason: GAP — no duplicate-time camera follow in the Swift roll: making the duplicated range visible via displayX has no executing counterpart.
Source context since previous assertion:
      const qreal duplicateDpr = roll.devicePixelRatio();
      const qreal duplicateViewport = std::max<qreal>(50, roll.width());
Original expression:
  QVERIFY2(view.camera().displayX(double(firstStart), 0.0, duplicateDpr) >= 0.0 &&
                   view.camera().displayX(double(firstEnd), 0.0, duplicateDpr) <= duplicateViewport,
               "first duplicated range was not made visible");

A066 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:536
Disposition: GAP
Mapping/reason: GAP — native host key delivery (repeated sendKeyStroke Ctrl+D): the retired C++ lane owns this observation, and no Swift predicate repeats the time-range duplicate; S008 covers one duplicate through the command router (the session owns the time selection, PianoGrid holds none of its own).
Source context since previous assertion:
      sendKeyStroke(roll, Qt::Key_D, Qt::ControlModifier, false);
      const Tick secondStart = firstEnd;
      const Tick secondEnd = secondStart + duplicateSpan;
      const songview::EditorSelectionModel::TimeSelection secondSelection =
          view.selectionModel().timeSelection();
Original expression:
  QVERIFY2(doc.undoStack()->index() == duplicateUndoIndex + 2 && secondSelection.active() &&
                   secondSelection.startTick == secondStart && secondSelection.endTick == secondEnd &&
                   view.editCursorTick() == secondEnd && hasNoteAt(secondStart),
               "repeating Ctrl+D did not duplicate the newest copy");

A067 | PianoRollTest::timelineDuplicateTime | src/checks/rollcheck/keyboard.cpp:542
Disposition: GAP
Mapping/reason: GAP — S048 (keyboard.swift) restores the pre-seed document, camera, and history identity, whereas this unwind targets the post-seed snapshot; the duplicate-time slot SMF restoration is unproved.
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A068 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:549
Disposition: MATCHED
Mapping: S043

A069 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:558
Disposition: MATCHED
Mapping: S043

A070 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:561
Disposition: MATCHED
Mapping: S056

A071 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:568
Disposition: GAP
Mapping/reason: GAP — the fewer-than-two-tracks early-return path is unasserted in Swift: the guard at keyboard.swift returns without a report.expect.
Source context since previous assertion:
      const Tick insertStart = d.tick + 2 * snapCell;
      const Tick insertEnd = insertStart + snapCell;
      if (doc.engineTrackCount() < 2) {
          while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
              doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A072 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:579
Disposition: GAP
Mapping/reason: GAP — the free-key reservation loop is guard-only in Swift (keyboard.swift report.fail, no report.expect pins the reservation).
Source context since previous assertion:
          return;
      }
      const int otherTrack = track == 0 ? 1 : 0;
      int otherKey = 12;
      while (otherKey < 128) {
          DocNote existing;
          if (!doc.findNote(otherTrack, insertStart, uint8_t(otherKey), &existing))
              break;
          ++otherKey;
      }
Original expression:
  QVERIFY2(otherKey < 128, "could not reserve an unselected-track insert fixture");

A073 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:583
Disposition: GAP
Mapping/reason: GAP — the unselected-track fixture creation is guard-only in Swift (keyboard.swift report.fail, no report.expect pins otherBefore).
Source context since previous assertion:
      const uint8_t otherPitch = uint8_t(otherKey);
      doc.addNote(otherTrack, insertStart, otherPitch, uint32_t(snapCell), 91);
      DocNote otherBefore;
Original expression:
  QVERIFY2(doc.findNote(otherTrack, insertStart, otherPitch, &otherBefore),
               "could not create the unselected-track insert fixture");

A074 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:593
Disposition: GAP
Mapping/reason: GAP — no edit cursor in the Swift session: parking the cursor past the range end is unmodelled.
Source context since previous assertion:
      view.selectTrack(track);
      songview::EditorSelectionModel::TimeSelection trackSelection;
      trackSelection.startTick = insertStart;
      trackSelection.endTick = insertEnd;
      view.selectionModel().setTimeSelectionAndTrackScope(trackSelection, 1u << track);
      // The unified command must anchor on the selection span, not the edit
      // cursor parked past the range end.
      view.commitEditCursor(insertEnd + snapCell);
Original expression:
  QVERIFY2(view.editCursorTick() == insertEnd + snapCell,
               "could not park the edit cursor away from the range start");

A075 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:606
Disposition: GAP
Mapping/reason: S057 (keyboard.swift) proves the document half — the selected note shifts to insertEnd and the other track stays byte-identical. GAP remains: the undo-index +1 increment, the retained Tracks band, and the cursor reset to range start are unasserted in Swift.
Source context since previous assertion:
      const int insertUndoIndex = doc.undoStack()->index();
      view.insertTime();
      DocNote otherAfter;
      DocNote selectedAfter;
      const bool otherStable =
          doc.findNote(otherTrack, insertStart, otherPitch, &otherAfter) &&
          otherAfter.tick == otherBefore.tick && otherAfter.duration == otherBefore.duration &&
          otherAfter.key == otherBefore.key && otherAfter.velocity == otherBefore.velocity;
      const bool selectedShifted = doc.findNote(track, insertEnd, transposed.key, &selectedAfter);
      const songview::EditorSelectionModel::TimeSelection insertedSelection =
          view.selectionModel().timeSelection();
Original expression:
  QVERIFY2(doc.undoStack()->index() == insertUndoIndex + 1 && otherStable && selectedShifted &&
                   insertedSelection.active() && insertedSelection.startTick == insertStart &&
                   insertedSelection.endTick == insertEnd &&
                   insertedSelection.scope == songview::EditorSelectionModel::TimeSelection::Tracks &&
                   view.editCursorTick() == insertStart,
               "Insert Time changed scope, cursor, or an unselected track");

A076 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:614
Disposition: GAP
Mapping/reason: GAP — S058 (keyboard.swift) restores the scenario-local post-fixture baseline, whereas this unwind targets the slot-level post-seed snapshot; the slot SMF restoration is unproved.
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A077 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:624
Disposition: GAP
Mapping/reason: GAP — no timeline-unused-track concept in the Swift roll: the rejected-insert track precondition is unmodelled.
Source context since previous assertion:
      // An active but unresolved scope is a rejected command: the unified
      // entry must refuse without falling through to the whole-song prompt.
      int unusedTrack = -1;
      for (int candidate = 0;
           candidate < static_cast<int>(std::size(check.timeline().tracks)) && unusedTrack < 0;
           ++candidate)
          if (!check.timeline().tracks[candidate].used)
              unusedTrack = candidate;
Original expression:
  QVERIFY2(unusedTrack >= 0, "the insert fixture has no timeline-unused track");

A078 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:631
Disposition: GAP
Mapping/reason: GAP — no primaryTrack, storedTrackScope, or time selection in the Swift session: the invalid active-scope setup is unmodelled.
Source context since previous assertion:
      view.selectTrack(unusedTrack);
      songview::EditorSelectionModel::TimeSelection rejectedSelection;
      rejectedSelection.startTick = insertStart;
      rejectedSelection.endTick = insertEnd;
      rejectedSelection.scope = songview::EditorSelectionModel::TimeSelection::Tracks;
      view.selectionModel().setTimeSelection(rejectedSelection);
Original expression:
  QVERIFY2(view.selectionModel().primaryTrack() == unusedTrack &&
                   view.selectionModel().storedTrackScope() == (uint32_t{1} << unusedTrack) &&
                   !check.timeline().tracks[unusedTrack].used &&
                   view.selectionModel().timeSelection().active() &&
                   view.selectionModel().timeSelection().startTick == insertStart &&
                   view.selectionModel().timeSelection().endTick == insertEnd &&
                   view.selectionModel().timeSelection().scope ==
                       songview::EditorSelectionModel::TimeSelection::Tracks,
               "could not build the invalid active time-selection scope");

A079 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:649
Disposition: GAP
Mapping/reason: GAP — the rejected Insert Time path is unmodelled in Swift: no prompt UI exists and no scenario asserts refusal without a prompt.
Source context since previous assertion:
      const quick_popup::PromptGuard guard(view);
      const QByteArray rejectedBefore = doc.smf().write();
      const int rejectedUndoIndex = doc.undoStack()->index();
      const int rejectedUndoCount = doc.undoStack()->count();
      const uint64_t rejectedRevision = doc.revision();
      const uint64_t rejectedCursor = view.editCursorTick();
      view.insertTime();
      QCoreApplication::processEvents();
Original expression:
  QVERIFY2(!quick_popup::popupSession(view)->isOpen(),
               "the rejected Insert Time opened a prompt");

A080 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:651
Disposition: GAP
Mapping/reason: GAP — the rejected-insert invariance (bytes, revision, undo index/count, cursor, selection, scope) is unmodelled in Swift: no scenario drives the invalid-scope path.
Source context since previous assertion:
  (adjacent to previous assertion; no intervening source lines)
Original expression:
  QVERIFY2(doc.smf().write() == rejectedBefore && doc.revision() == rejectedRevision &&
                   doc.undoStack()->index() == rejectedUndoIndex &&
                   doc.undoStack()->count() == rejectedUndoCount &&
                   view.editCursorTick() == rejectedCursor &&
                   view.selectionModel().timeSelection().active() &&
                   view.selectionModel().timeSelection().startTick == insertStart &&
                   view.selectionModel().timeSelection().endTick == insertEnd &&
                   view.selectionModel().primaryTrack() == unusedTrack &&
                   view.selectionModel().storedTrackScope() == (uint32_t{1} << unusedTrack),
               "the rejected Insert Time changed document, cursor, or selection state");

A081 | PianoRollTest::timelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.cpp:666
Disposition: GAP
Mapping/reason: GAP — S058 (keyboard.swift) restores the scenario-local post-fixture baseline, whereas this restore targets the slot-level post-seed snapshot; the slot SMF restoration is unproved.
Source context since previous assertion:
      // Restore the fixture state after the rejected scenario.
      view.selectTrack(track);
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A082 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:673
Disposition: MATCHED
Mapping: S043

A083 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:682
Disposition: MATCHED
Mapping: S043

A084 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:685
Disposition: GAP
Mapping/reason: GAP — the lane-seed shortcut position has no report.expect in Swift: checkTimelineInsertBlankTimeLanes performs the same nudgeNotes (keyboard.swift) but only fail-guards the fixture (keyboard.swift); S056 belongs to the tracks scenario.
Source context since previous assertion:
      doc.moveNotes({transposed}, int64_t(2 * snapCell), -10);
Original expression:
  QVERIFY2(doc.findNote(track, d.tick + 2 * snapCell, uint8_t(d.key - 10), &transposed),
               "lane insertion seed did not reach the expected shortcut state");

A085 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:694
Disposition: GAP
Mapping/reason: GAP — the lane-point fixture (findLanePoint plus findNote) is guard-only in Swift (keyboard.swift report.fail, no report.expect).
Source context since previous assertion:
      const Tick insertStart = d.tick + 2 * snapCell;
      const Tick insertEnd = insertStart + snapCell;
      const uint8_t laneCc = 7;
      const Tick lanePointTick = insertStart + snapCell / 2;
      doc.addLanePoint(track, laneCc, lanePointTick, 80);
      DocLanePoint laneBefore;
      DocNote laneNoteBefore;
Original expression:
  QVERIFY2(doc.findLanePoint(track, laneCc, lanePointTick, &laneBefore) &&
                   doc.findNote(track, insertStart, transposed.key, &laneNoteBefore),
               "could not create the lane-scoped insert fixture");

A086 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:706
Disposition: GAP
Mapping/reason: GAP — no edit cursor in the Swift session: parking the cursor past the range end is unmodelled.
Source context since previous assertion:
      songview::EditorSelectionModel::TimeSelection laneSelection;
      laneSelection.startTick = insertStart;
      laneSelection.endTick = insertEnd;
      laneSelection.scope = songview::EditorSelectionModel::TimeSelection::Lanes;
      laneSelection.lanes = {{track, laneCc}};
      view.selectionModel().setTimeSelection(laneSelection);
      // The unified command must anchor on the selection span, not the edit
      // cursor parked past the range end.
      view.commitEditCursor(insertEnd + snapCell);
Original expression:
  QVERIFY2(view.editCursorTick() == insertEnd + snapCell,
               "could not park the edit cursor away from the range start");

A087 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:721
Disposition: GAP
Mapping/reason: S059 (keyboard.swift) proves the document half — CC7 shifts by one snap and the note stays byte-identical. GAP remains: the undo-index +1 increment, the retained Lanes band and lane list, and the cursor reset to range start are unasserted in Swift.
Source context since previous assertion:
      const int insertUndoIndex = doc.undoStack()->index();
      view.insertTime();
      DocLanePoint shiftedLanePoint;
      DocNote laneNoteAfter;
      const bool laneShifted =
          doc.findLanePoint(track, laneCc, lanePointTick + snapCell, &shiftedLanePoint);
      const bool noteStable = doc.findNote(track, insertStart, transposed.key, &laneNoteAfter) &&
                              laneNoteAfter.tick == laneNoteBefore.tick &&
                              laneNoteAfter.duration == laneNoteBefore.duration &&
                              laneNoteAfter.key == laneNoteBefore.key &&
                              laneNoteAfter.velocity == laneNoteBefore.velocity;
      const songview::EditorSelectionModel::TimeSelection insertedSelection =
          view.selectionModel().timeSelection();
Original expression:
  QVERIFY2(doc.undoStack()->index() == insertUndoIndex + 1 && laneShifted && noteStable &&
                   insertedSelection.active() &&
                   insertedSelection.scope == songview::EditorSelectionModel::TimeSelection::Lanes &&
                   insertedSelection.lanes == laneSelection.lanes &&
                   insertedSelection.startTick == insertStart &&
                   insertedSelection.endTick == insertEnd && view.editCursorTick() == insertStart,
               "lane-scoped Insert Time widened its scope or moved the band");

A088 | PianoRollTest::timelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.cpp:730
Disposition: GAP
Mapping/reason: GAP — S060 (keyboard.swift) restores the scenario-local post-fixture baseline, whereas this unwind targets the slot-level post-seed snapshot; the slot SMF restoration is unproved.
Source context since previous assertion:
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

A089 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:737
Disposition: MATCHED
Mapping: S043

A090 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:747
Disposition: MATCHED
Mapping: S043

A091 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:762
Disposition: GAP
Mapping/reason: GAP — the free-row search for the second resize note is guard-only in Swift (keyboard.swift report.fail, no report.expect).
Source context since previous assertion:
      // A second selected note starts strictly after the seed's onset and
      // carries a different duration: the batch delta must come from the grid
      // snap at the earliest onset and apply uniformly to both.
      const Tick laterTick = d.tick + 2 * d.dur + snapCell;
      const uint32_t laterDur = uint32_t(snapCell) + (d.dur == snapCell + 1 ? 2 : 1);
      int laterKey = -1;
      for (int key = 115; key >= 24 && laterKey < 0; --key) {
          if (key == d.key)
              continue;
          if (!check.isOccupied(laterTick, laterDur, key, true))
              laterKey = key;
      }
Original expression:
  QVERIFY2(laterKey >= 0, "could not find a free row for the second resize note");

A092 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:765
Disposition: PARTIAL
Mapping/reason: PARTIAL — S049 (keyboard.swift) proves the second note exists with its intended different duration. Unproved: the laterTick/key placement asserted here (S049 addresses the note by ID, not by tick and key).
Source context since previous assertion:
      doc.addNote(track, laterTick, uint8_t(laterKey), laterDur, 100);
      DocNote noteB;
Original expression:
  QVERIFY2(doc.findNote(track, laterTick, uint8_t(laterKey), &noteB),
               "could not create the second resize note");

A093 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:767
Disposition: MATCHED
Mapping: S049

A094 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:777
Disposition: MATCHED
Mapping: S050

A095 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:782
Disposition: PARTIAL
Mapping/reason: PARTIAL — S050 (keyboard.swift) proves the merged two-snap delta and S051 (keyboard.swift) proves one undo restores the baseline. Unproved: the exact undo index +1 and count +1 arithmetic asserted here.
Source context since previous assertion:
  (adjacent to previous assertion; no intervening source lines)
Original expression:
  QVERIFY2(doc.undoStack()->index() == resizeUndo + 1 &&
                   doc.undoStack()->count() == resizeUndoCount + 1,
               "repeated Shift+Right presses did not merge into one undo entry");

A096 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:786
Disposition: MATCHED
Mapping: S051

A097 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:790
Disposition: MATCHED
Mapping: S051

A098 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:800
Disposition: GAP
Mapping/reason: GAP — per-press note survival is guard/fail-only in Swift (keyboard.swift); no report.expect pins it (S052 asserts the shrink deltas, not survival). Re-executes once per Shift+Left press in the shrink loop.
Source context since previous assertion:
      view.selectionModel().setNoteSelection({noteA.noteId, noteB.noteId});
      const int floorUndo = doc.undoStack()->index();
      for (int press = 0; press < 256 && std::min(noteA.duration, noteB.duration) > 1; ++press) {
          const uint32_t previousA = noteA.duration;
          const uint32_t previousB = noteB.duration;
          const uint32_t previousMin = std::min(previousA, previousB);
          sendKeyStroke(roll, Qt::Key_Left, Qt::ShiftModifier, false);
Original expression:
  QVERIFY2(doc.findNote(track, d.tick, uint8_t(d.key), &noteA) &&
                       doc.findNote(track, laterTick, uint8_t(laterKey), &noteB),
                   "Shift+Left lost a resized note");

A099 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:804
Disposition: MATCHED
Mapping: S052

A100 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:808
Disposition: PARTIAL
Mapping/reason: S052 (keyboard.swift) verifies every uniform Shift+Left delta and S053 (keyboard.swift) verifies the one-tick floor. PARTIAL remains: host key-event delivery is unproved (Swift drives grid.performCommand directly).
Source context since previous assertion:
      }
Original expression:
  QVERIFY2(std::min(noteA.duration, noteB.duration) == 1,
               "Shift+Left did not stop at the one-tick duration floor");

A101 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:814
Disposition: PARTIAL
Mapping/reason: S054 (keyboard.swift) asserts the extra Shift+Left at the floor leaves document, history identity, and revision unchanged. PARTIAL remains: host key-event delivery and the exact undo-count arithmetic are unproved.
Source context since previous assertion:
      const QByteArray floorBytes = doc.smf().write();
      const int floorIndex = doc.undoStack()->index();
      const int floorCount = doc.undoStack()->count();
      sendKeyStroke(roll, Qt::Key_Left, Qt::ShiftModifier, false);
Original expression:
  QVERIFY2(doc.smf().write() == floorBytes && doc.undoStack()->index() == floorIndex &&
                   doc.undoStack()->count() == floorCount,
               "Shift+Left at the duration floor changed the document or history");

A102 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:819
Disposition: MATCHED
Mapping: S055

A103 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:823
Disposition: MATCHED
Mapping: S055

A104 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:835
Disposition: GAP
Mapping/reason: GAP — the active-time-selection block refusing Shift+Right/Shift+Left is unmodelled: the session owns the time selection since task 38a, but no Swift predicate drives a shift-nudge while a range is active.
Source context since previous assertion:
      view.selectionModel().setNoteSelection({noteA.noteId, noteB.noteId});
      view.selectionModel().setTimeSelection(
          {d.tick, d.tick + snapCell, songview::EditorSelectionModel::TimeSelection::Tracks, {}});
      const QByteArray blockedBytes = doc.smf().write();
      const int blockedIndex = doc.undoStack()->index();
      const int blockedCount = doc.undoStack()->count();
      const uint64_t blockedRevision = doc.revision();
      sendKeyStroke(roll, Qt::Key_Right, Qt::ShiftModifier, false);
      sendKeyStroke(roll, Qt::Key_Left, Qt::ShiftModifier, false);
Original expression:
  QVERIFY2(doc.smf().write() == blockedBytes && doc.revision() == blockedRevision &&
                   doc.undoStack()->index() == blockedIndex &&
                   doc.undoStack()->count() == blockedCount &&
                   view.selectionModel().timeSelection().active(),
               "an active time selection did not block the note resize keys");

A105 | PianoRollTest::keyboardResizeNotes | src/checks/rollcheck/keyboard.cpp:843
Disposition: GAP
Mapping/reason: GAP — S048 (keyboard.swift) restores the pre-seed document, camera, and history identity, whereas this final unwind targets the post-seed snapshot; the slot SMF restoration is unproved.
Source context since previous assertion:
      view.selectionModel().clearTimeSelection();
      while (doc.undoStack()->index() > undo && doc.undoStack()->canUndo())
          doc.undoStack()->undo();
Original expression:
  QCOMPARE(doc.smf().write(), before);

Remaining original source (closing sequence and helper bodies):
  }

Counterpart executable predicate index
S001..S028 index the RETIRED gated Swift-surface suite (C++ QTest checks that drove the Swift grid; source retired in 4d7fa617, pinned blob at 5f0924e8): they execute nowhere in the current tree and close no A entry. S029..S042 are report.expect predicates in the registered Swift check swiftcore/EditorGridCamera::commandRouting, which runs under the swiftcore filter. S043–S071 are report.expect predicates in the registered swiftcore checks keyboard.swift and its seed helper swiftcore/PianoRoll::withKeyboardSeed. S072–S075 are the ruler sweep, tap and cursor-publication predicates of src/checks/rollcheck/ruler_loop_menu.swift (checkRulerSweepScopeTapAndChip), cited here for the timelineRulerScope sites; the two sweep predicates assert the exact swept range. S076–S077 are the checkTimeSelectionHighlights predicates in keyboard.swift, executed once that check provisions its own second track. S078–S081 are the four checkKeyboardSkipsGhosts predicates in keyboard.swift — second-track setup, ghost projection, selected-note transpose+nudge, and ghost-note conservation — executing under the same swiftcore filter; no original keyboard.cpp assertion covers keyboard remaps with a projected other-track ghost, so they close no A entry. Their presence alone does not close any A entry. Preserve their surrounding setup and order in the paired source. S082 is the seed guard of checkRulerSweepSingleTrackScope in note_rendering.swift (swiftcore/PianoRoll::timelineRulerScope), which fails unless the intersecting other-track note is provisioned; it closes A027.

S001 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S002 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S003 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S004 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S005 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S006 | SwiftRollGatedTest::resizeEdgesRespectMinimumDuration | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S007 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S008 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S009 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S010 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S011 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S012 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S013 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S014 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S015 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S016 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S017 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S018 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S019 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S020 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S021 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S022 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S023 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S024 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S025 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S026 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S027 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S028 | SwiftRollGatedTest::focusedGridCommandRouting | src/checks/swiftrollgated/gesturechecks.cpp
Anchor: deleted

S029 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "nudge right advances the selected note one snap cell"

S030 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "nudge left returns the selected note one snap cell"

S031 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "nudge right snaps an off-grid note forward to the lattice"

S032 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "nudge left snaps an off-grid note back to the lattice"

S033 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "octave transpose raises the selected note twelve keys"

S034 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "octave transpose down restores the selected note pitch"

S035 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "shorten clamps the shorter note at one tick"

S036 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "repeated shorten at the floor is a no-op"

S037 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "merged shorten presses undo in one step"

S038 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "copy leaves the document and published summary untouched"

S039 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "delete is refused while a pointer gesture owns the grid"

S040 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "pencil mode still toggles mid-gesture without touching notes"

S041 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "pencil mode toggles back mid-gesture without touching notes"

S042 | checkCommandRouting | src/checks/rollcheck/note_commands.swift
Anchor: message "releasing a zero-delta press commits nothing"

S043 | swiftcore/PianoRoll::withKeyboardSeed | src/checks/rollcheck/keyboard.swift
Anchor: message "the seeded keyboard note is present"

S044 | checkKeyboardTranspose | src/checks/rollcheck/keyboard.swift
Anchor: message "Up transposes the selected note one semitone"

S045 | checkKeyboardTranspose | src/checks/rollcheck/keyboard.swift
Anchor: message "Shift+Down transposes down an octave"

S046 | checkKeyboardTranspose | src/checks/rollcheck/keyboard.swift
Anchor: message "Right nudges one snap cell without changing pitch"

S047 | checkKeyboardTranspose | src/checks/rollcheck/keyboard.swift
Anchor: message "Left snaps the off-grid note back to the lattice"

S048 | swiftcore/PianoRoll::withKeyboardSeed | src/checks/rollcheck/keyboard.swift
Anchor: message "undo restores the original song, camera, and history position"

S049 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "the batch fixture has two different durations"

S050 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "two Shift+Right presses extend both notes by two snap cells"

S051 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "one undo restores both different durations"

S052 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "Shift+Left shortens both notes by the same step"

S053 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "repeated Shift+Left reaches the one-tick floor"

S054 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "an extra Shift+Left at the floor is a document and history no-op"

S055 | checkKeyboardResizeNotes | src/checks/rollcheck/keyboard.swift
Anchor: message "the shrink sequence merges and one undo restores the fixture"

S056 | checkTimelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.swift
Anchor: message "track insertion seed reaches its shortcut position"

S057 | checkTimelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.swift
Anchor: message "track-scoped blank insertion shifts only the selected track"

S058 | checkTimelineInsertBlankTimeTracks | src/checks/rollcheck/keyboard.swift
Anchor: message "undo restores the track-scoped insertion"

S059 | checkTimelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.swift
Anchor: message "lane-scoped blank insertion shifts CC7 without moving the note"

S060 | checkTimelineInsertBlankTimeLanes | src/checks/rollcheck/keyboard.swift
Anchor: message "undo restores the lane-scoped insertion"

S061 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "the keep-visible seed reaches its parked pitch and tick"

S062 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "the selected note is parked above the roll"

S063 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "Up transposes the parked note one semitone"

S064 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "Up keeps the parked note fully within the roll"

S065 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "the next nudge starts left of the viewport"

S066 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "Right reveals the parked note at the left edge"

S067 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "the note rides right by every requested snap step"

S068 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "every Right nudge keeps the whole note in the viewport"

S069 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "riding left returns the same note to its parked position"

S070 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "undo restores the pre-gesture song bytes"

S071 | checkKeyboardKeepsEditedNoteVisible | src/checks/rollcheck/keyboard.swift
Anchor: message "every Left nudge keeps the whole note in the viewport"

S072 | checkRulerSweepScope | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "A025: a plain ruler drag sweeps the exact range with primary-only scope"

S073 | checkRulerSweepScope | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "A026: a Control ruler drag sweeps the exact range with intersecting-track scope"

S074 | checkRulerTapAndCancel | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "A035: tapping the ruler outside the selection commits the cursor without clearing it"

S075 | checkRulerTapAndCancel | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "A035: the tap commit publishes the cursor through the session observer"

S076 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "A053/A029 covered notes ring, including the time-scoped ghost"

S077 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "A054 time-covered notes never leak into the note selection"

S078 | checkKeyboardSkipsGhosts | src/checks/rollcheck/keyboard.swift
Anchor: message "a distinct second track exists for keyboard ghost editing"

S079 | checkKeyboardSkipsGhosts | src/checks/rollcheck/keyboard.swift
Anchor: message "the other-track note projects as a ghost"

S080 | checkKeyboardSkipsGhosts | src/checks/rollcheck/keyboard.swift
Anchor: message "transpose and nudge remap the selected note with ghosts present"

S081 | checkKeyboardSkipsGhosts | src/checks/rollcheck/keyboard.swift
Anchor: message "keyboard remaps leave other-track ghost notes untouched"

S082 | checkRulerSweepSingleTrackScope | src/checks/rollcheck/note_rendering.swift
Anchor: message "single-track sweep fixture cannot seed the intersecting other-track note"

S083 | test_otherEventsBandMountsBetweenDrawerAndScrollbar | src/checks/editorqml/tst_EditorDrawer.qml
Anchor: message "the other-events band mounts in the production editor"

S084 | test_otherEventsBandMountsBetweenDrawerAndScrollbar | src/checks/editorqml/tst_EditorDrawer.qml
Anchor: message "the other-events band starts at the drawer bottom"

S085 | runOtherEventsBandChecks | src/checks/drawerpresentation/other_events_band.swift
Anchor: message "track markers use the stable track-identity fill"

S086 | runOtherEventsBandChecks | src/checks/drawerpresentation/other_events_band.swift
Anchor: message "marker x is the shared camera content position of its tick"

S087 | checkThresholdDrawCell | src/checks/rollcheck/selection.swift
Anchor: message "A056 clicking inside the primary time selection clears its active span"

S088 | checkRulerSweepScope | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "releasing a swept range retains the time selection"

S089 | checkRulerSweepScope | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "undoing the note and track restores the fixture bytes"

S090 | checkRulerTapAndCancel | src/checks/rollcheck/ruler_loop_menu.swift
Anchor: message "cancelling a sweep keeps the selection and cursor without committing"

S091 | swiftcore/PianoRoll::withKeyboardSeed | src/checks/rollcheck/keyboard.swift
Anchor: message "the scenario unwind restores the slot's post-seed bytes"

S092 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "the time-shortcut fixture seeds its covered note"

S093 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "Up over an active time selection transposes the covered notes"

S094 | checkTimeSelectionHighlights | src/checks/rollcheck/keyboard.swift
Anchor: message "Right over an active time selection nudges the covered notes and advances the band start"

S095 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a sub-threshold ruler press releases as a cursor tap, not a selection"

S096 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a ruler drag past the drag threshold creates the exact snapped selection"

S097 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a left click outside the time selection keeps it"

S098 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a ruler right-drag creates no time selection"

S099 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "releasing a ruler right-drag opens the ruler menu"

S100 | test_rulerDragThresholdAndRightDragMenu | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "Escape dismisses the right-drag ruler menu"

S101 | test_controlRulerSweepPublishesSecondaryHeaderOverlay | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a Control ruler drag adds the overlapping track to the scope"

S102 | test_controlRulerSweepPublishesSecondaryHeaderOverlay | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "the time-scoped secondary header publishes its selection overlay"

S103 | test_controlRulerSweepPublishesSecondaryHeaderOverlay | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "the fixture presents a secondary-track note inside the ruler viewport"

S104 | test_controlRulerSweepPublishesSecondaryHeaderOverlay | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "the note's secondary header row is mounted"

S105 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "the selection covers a mounted primary-track note"

S106 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "Up with an active time selection transposes the covered note"

S107 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "Right with an active time selection nudges the covered note"

S108 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "Right over an active time selection advances the band start"

S109 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "the in-band roll click targets empty note space"

S110 | test_rollKeysEditTimeScopedNotesAndEmptyClickClearsBand | src/checks/editorqml/tst_ShellGridMenu.qml
Anchor: message "a left click on empty roll space inside the selection clears it"

S088-S110 are the task-53 additions: S088/S089 the swept-range retention and the fixture-byte restoration of checkRulerSweepScope, S090 the cancelled-sweep law of checkRulerTapAndCancel, S091 the slot post-seed unwind of swiftcore/PianoRoll::withKeyboardSeed, S092-S094 the time-shortcut seed and the time-covered Up/Right outcomes of checkTimeSelectionHighlights, and S095-S110 the mounted threshold/right-drag, Control-sweep secondary-header and roll-key/empty-click predicates of tst_ShellGridMenu.qml. They execute under swiftcore/PianoRoll::* in the swiftcore projectSession lane and under the shell-grid-menu lane.
