Assertion correspondence: rollcheck/remap.cpp

Reference revision: 85b97239ce94dc4c4cf5f3f4fb66fc96ff2cf27e
Original: src/checks/rollcheck/remap.cpp
Original SHA-256: f0c18ebf9ff091b783f1892f9ccbb39ea8760142f97a3a740811dffb5f9e0441
Swift counterpart: src/checks/rollcheck/remap.swift
Swift SHA-256: bed5eaa92b96b15a37fcf75ed0ab35cf651073753d86dc270ba0683cf0883b16
Additional Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Additional Swift SHA-256: 3865b7618bb681a7720785153c04ce764b67d2780e09dfcf6e833fc6a20c62fd
Additional Swift counterpart: src/checks/editorqml/tst_ShellGridInput.qml
Additional Swift SHA-256: 4538a7863acc6e39fbed17606e2bc1402eed7607058685780b7e0f1f6f5db726

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards. Source
context preserves setup, rows, loops, and guards between assertions verbatim.
S identifiers identify exact counterpart predicates, listed after the inventory.
MATCHED identifies equivalent Swift predicates executed by the swiftcore suite.
PARTIAL identifies related counterpart predicates with an explicit unproved condition.
NATIVE identifies retained native fixture/loader staging obligations; it does not
establish coverage of the converted Swift surface.
GAP identifies an original predicate without a replacement assertion.
remap.swift runs one scenario per original family under the same cppIDs and asserts the
single-publication remap payload, the remapped owner state and each undo/redo;
EditorGridCameraChecks ::trackOwnerRemap supplies the remap-before-publication payload
predicate (S025) and the per-owner sequences. The original two-signal emission order
(tracksRemapped before documentChanged) has no Swift counterpart — SongView Qt signals
do not exist in Swift — so the twelve pure-ordering sites stay GAP; the shell-grid-input lane runs test_trackFollowReload for header
follow and session replacement. The original SongView editorViewState cosmetics and
time-selection lanes are not modelled in the Swift roll, so every site whose original
conjunction includes them stays PARTIAL. The raw metadata-to-engine promotion family
stays GAP because insertRawEvent never promotes or remaps a conductor in Swift.
Disposition tally: 67 sites: 19 MATCHED, 17 PARTIAL, 19 GAP, 12 NATIVE.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: PASS — 1/14 ok (controller-recorded post-change run); the swiftcore
projectSession suite executed runRemapChecks, runEditorGridCameraChecks
(trackOwnerRemap) and the S001-S029 predicates.
Command: deno task verify:qml-roll --verbose
Result: PASS — 1/1 ok (controller-recorded post-change run); no remap site depends
on the roll QML lane.
Command: deno task verify:shell --verbose
Result: PASS — 11/11 ok (controller-recorded post-change run); the shell-grid-input
lane ran test_trackFollowReload (S030). The shell and roll QML runs predate only a
swiftcore-checks-only commit; no production or QML source changed after them.

Execution and ownership
runRemapChecks executes checkRemapMove, checkRemapInsert, checkRemapDuplicate,
checkRemapDelete and checkRemapMetadata in the projectSession Swift suite; the paired
checkTrackOwnerRemap in EditorGridCameraChecks.swift runs in the same suite; the shell
lane runs test_trackFollowReload against the production shell with real input. The
original local RemapFixture (remapCosmetics/hasMovedTrackState/hasOriginalTrackState/
hasTimeSelectionLanes/hasNoOwnerCosmetics) and the gated
trackFollowAndSessionReplacement slot are dead citations and are no longer indexed.

Fixture, sequence and row constraints
Each original method loaded the song fixture, prepared a local RemapFixture, applied
remapCosmetics (mute/solo/scopes/lanes), performed the structural edit, then asserted the
same triple for the edit, its undo and its redo: remapPrecedesDocument,
headersFollowTimeline and the expected owner state. Delete additionally seeded removable
cosmetics on the removed owner; metadata asserted documentOnly invariance; engine
promotion built a raw two-track SMF with a metadata track. The Swift checks seed the
same two-track owners on the session document and assert the published remap payload per
phase instead of the retired two-signal ordering.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:170
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
#include "checks/rollcheck/tst_pianoroll.h"

#include <QtTest>

#include <QTemporaryDir>
#include <algorithm>
#include <memory>
#include <utility>
#include <vector>

#include "checks/rollcheck/headerchecksupport.h"
#include "checks/support/quickframebuffer.h"
#include "checks/support/songfixture.h"
#include "checks/support/support.h"
#include "checks/trackheaders/trackheaderoracles.h"
#include "core/smf.h"
#include "core/songdocument.h"
#include "ui/songview.h"

namespace {
using checks::rollcheck::headercheck::model;
using trackheaders_test::recordsMatchTimeline;

struct RemapFixture {
    SongDocument document;
    std::unique_ptr<MidiTimeline> timeline;
    SongView view{document};
    std::vector<QString> order;

    bool prepare(const SongInfo &song, QString *error)
    {
        if (!document.load(song, error))
            return false;
        timeline = document.buildTimeline(48000.0);
        if (!checks::support::showQuickViewport(view, QSize(800, 480)))
            return false;
        view.setSong(timeline.get(), nullptr);
        checks::support::bindEditActionsForTest(view);
        QObject::connect(&document, &SongDocument::tracksRemapped, &view,
                         [this] { order.push_back(QStringLiteral("remap")); });
        QObject::connect(&document, &SongDocument::documentChanged, &view, [this] {
            auto rebuilt = document.buildTimeline(48000.0);
            view.updateSong(rebuilt.get());
            timeline = std::move(rebuilt);
            order.push_back(QStringLiteral("document"));
        });
        return true;
    }

    bool remapPrecedesDocument() const
    {
        return order.size() == 2 && order[0] == QStringLiteral("remap") &&
               order[1] == QStringLiteral("document");
    }

    bool documentOnly() const
    {
        return order.size() == 1 && order.front() == QStringLiteral("document");
    }

    bool headersFollowTimeline()
    {
        auto *headers = model(view);
        return headers && recordsMatchTimeline(*headers, *timeline, document.canAddTrack());
    }

    void clearOrder() { order.clear(); }
};

EditorAutomationRowId controllerRow(int owner, uint8_t cc)
{
    return {EditorAutomationRowKind::ControlChange, uint8_t(owner), cc};
}

bool hasEmptyLane(const EditorViewState &state, int owner, uint8_t cc)
{
    return state.emptyLanes.find(controllerRow(owner, cc)) != state.emptyLanes.end();
}

bool hasRowValue(const auto &rows, const EditorAutomationRowId &row, int value)
{
    const auto found = rows.find(row);
    return found != rows.end() && int(found->second) == value;
}

bool hasNoOwnerCosmetics(const EditorViewState &state, int owner)
{
    const auto owns = [owner](const auto &entry) {
        return entry.first.kind != EditorAutomationRowKind::Tempo && entry.first.track == owner;
    };
    return std::none_of(state.laneHeights.cbegin(), state.laneHeights.cend(), owns) &&
           std::none_of(state.laneRanges.cbegin(), state.laneRanges.cend(), owns) &&
           !hasEmptyLane(state, owner, 7) && !hasEmptyLane(state, owner, 10) &&
           !hasEmptyLane(state, owner, 74);
}

EditorViewState remapCosmetics()
{
    const EditorAutomationRowId tempo{EditorAutomationRowKind::Tempo, 0, 0};
    EditorViewState cosmetics;
    cosmetics.laneHeight = 64;
    cosmetics.laneHeights.emplace(tempo, 94);
    cosmetics.laneHeights.emplace(controllerRow(0, 7), 67);
    cosmetics.laneHeights.emplace(controllerRow(1, 10), 77);
    cosmetics.laneRanges.emplace(tempo, 116);
    cosmetics.laneRanges.emplace(controllerRow(0, 7), 102);
    cosmetics.laneRanges.emplace(controllerRow(1, 10), 92);
    cosmetics.emptyLanes.emplace(controllerRow(0, 7));
    cosmetics.emptyLanes.emplace(controllerRow(1, 10));
    return cosmetics;
}

bool hasRemappedCosmetics(const EditorViewState &state, int zeroOwner, int oneOwner)
{
    const EditorAutomationRowId tempo{EditorAutomationRowKind::Tempo, 0, 0};
    return state.laneHeight == 64 && state.laneHeights.size() == 3 &&
           state.laneRanges.size() == 3 && state.emptyLanes.size() == 2 &&
           hasRowValue(state.laneHeights, tempo, 94) &&
           hasRowValue(state.laneHeights, controllerRow(zeroOwner, 7), 67) &&
           hasRowValue(state.laneHeights, controllerRow(oneOwner, 10), 77) &&
           hasRowValue(state.laneRanges, tempo, 116) &&
           hasRowValue(state.laneRanges, controllerRow(zeroOwner, 7), 102) &&
           hasRowValue(state.laneRanges, controllerRow(oneOwner, 10), 92) &&
           hasEmptyLane(state, zeroOwner, 7) && hasEmptyLane(state, oneOwner, 10);
}

bool hasRawCosmetics(const EditorViewState &state, int owner)
{
    const EditorAutomationRowId tempo{EditorAutomationRowKind::Tempo, 0, 0};
    return state.laneHeight == 64 && state.laneHeights.size() == 2 &&
           state.laneRanges.size() == 2 && state.emptyLanes.size() == 1 &&
           hasRowValue(state.laneHeights, tempo, 94) &&
           hasRowValue(state.laneHeights, controllerRow(owner, 7), 67) &&
           hasRowValue(state.laneRanges, tempo, 116) &&
           hasRowValue(state.laneRanges, controllerRow(owner, 7), 102) &&
           hasEmptyLane(state, owner, 7);
}

bool hasTimeSelectionLanes(const SongView &view,
                           const std::vector<std::pair<int, uint8_t>> &expected)
{
    const songview::EditorSelectionModel::TimeSelection &selection =
        view.selectionModel().timeSelection();
    return selection.scope == songview::EditorSelectionModel::TimeSelection::Lanes &&
           selection.startTick == 24 && selection.endTick == 48 && selection.lanes == expected;
}

bool hasMovedTrackState(const SongView &view)
{
    return view.selectionModel().primaryTrack() == 0 &&
           view.selectionModel().storedTrackScope() == 0x3 && view.trackMuted(1) &&
           !view.trackMuted(0) && view.trackSoloed(0) && !view.trackSoloed(1) &&
           hasRemappedCosmetics(view.editorViewState(), 1, 0);
}

bool hasOriginalTrackState(const SongView &view)
{
    return view.selectionModel().primaryTrack() == 1 &&
           view.selectionModel().storedTrackScope() == 0x3 && view.trackMuted(0) &&
           !view.trackMuted(1) && view.trackSoloed(1) && !view.trackSoloed(0) &&
           hasRemappedCosmetics(view.editorViewState(), 0, 1);
}
} // namespace

void PianoRollTest::trackRemapMove()
{
    QString error;
    const std::unique_ptr<checks::LoadedSong> source =
        checks::LoadedSong::load(m_project->root(), m_songLabel, error);

Original expression:
QVERIFY2(source, qPrintable(error))

A002 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:174
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(source->songInfo(), &error))

Original expression:
QFAIL("could not load track-remap fixture")

A003 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:176
Disposition: MATCHED
Mapping: S001 (src/checks/rollcheck/remap.swift:35-46) fails the scenario unless the session can be provisioned to two engine tracks with an insertion slot (usedTrackCount == 1, canAddTrack, addTrack(voice:) == 1).
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() < 2 || !fixture.document.canAddTrack())

Original expression:
QFAIL("track-remap fixture lacks two tracks and an insertion slot")

A004 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:198
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals (tracksRemapped/documentChanged) do not exist in Swift, so the two-signal emission order has no executing counterpart. S025 (EditorGridCameraChecks.swift:1054-1058) and S003 body (remap.swift:86-92) at the move call (remap.swift:127-128) prove the remap is reconciled in the single document publication, not the signal order.
Source context since previous assertion:
;

    fixture.view.applyEditorViewState(remapCosmetics());
    fixture.view.selectionModel().clearNoteSelection();
    fixture.view.selectionModel().clearTimeSelection();
    fixture.view.setTrackMute(0, false);
    fixture.view.setTrackMute(1, false);
    fixture.view.setTrackSolo(0, false);
    fixture.view.setTrackSolo(1, false);
    fixture.view.selectTrack(1);
    fixture.view.trackHeaderClicked(0, Qt::ControlModifier);
    fixture.view.setTrackMute(0, true);
    fixture.view.setTrackSolo(1, true);
    songview::EditorSelectionModel::TimeSelection lanes;
    lanes.scope = songview::EditorSelectionModel::TimeSelection::Lanes;
    lanes.startTick = 24;
    lanes.endTick = 48;
    lanes.lanes = {{0, 7}, {1, 10}};
    fixture.view.selectionModel().setTimeSelection(lanes);

    fixture.document.moveTrack(0, 1);
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("move did not remap before documentChanged")

A005 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:200
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the move call (remap.swift:127-128) asserts the presenter rows follow the current track order with titles and the trailing add row; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track follow plus in-place session replacement on the production shell.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A006 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:204
Disposition: PARTIAL
Mapping/reason: S003 (remap.swift:86-92), S008 (remap.swift:129-130) and S024 (EditorGridCameraChecks.swift:1050-1053) assert the moved mute/solo/selection owners and the moved note owners, with S007 (remap.swift:125) for the accepted move and S002 (remap.swift:29-31) for the fixture restoration. Unproved: the original hasMovedTrackState cosmetic remap (editorViewState lane heights/ranges) and the time-selection lanes ({{1,7},{0,10}}), which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasMovedTrackState(fixture.view) ||
        !hasTimeSelectionLanes(fixture.view, {{1, 7}, {0, 10}})) {

Original expression:
QFAIL("move did not re-address complete SongView track state")

A007 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:209
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the undo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the move-undo call (remap.swift:134-135) proves the remap is reconciled in the single undo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->undo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("move undo did not remap before documentChanged")

A008 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:211
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the move-undo call (remap.swift:134-135) asserts the presenter rows follow the rebuilt track order after the undo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates header/track follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A009 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:215
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the move-undo call (remap.swift:134-135), S009 (remap.swift:136-137) and S026 (EditorGridCameraChecks.swift:1060-1063) assert the inverse remap and the restored document state/identity. Unproved: the original hasOriginalTrackState cosmetic remap and the time-selection lane remap, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasOriginalTrackState(fixture.view) ||
        !hasTimeSelectionLanes(fixture.view, {{0, 7}, {1, 10}})) {

Original expression:
QFAIL("move undo did not restore remapped owners")

A010 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:220
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the redo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the move-redo call (remap.swift:139-140) proves the remap is reconciled in the single redo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->redo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("move redo did not remap before documentChanged")

A011 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:222
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the move-redo call (remap.swift:139-140) asserts the presenter rows follow the rebuilt track order after the redo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates header/track follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A012 | PianoRollTest::trackRemapMove | src/checks/rollcheck/remap.cpp:226
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the move-redo call (remap.swift:139-140), S010 (remap.swift:141) and S024 (EditorGridCameraChecks.swift:1050-1053) assert the redo owner state. Unproved: the original hasMovedTrackState cosmetic remap and the time-selection lane remap, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasMovedTrackState(fixture.view) ||
        !hasTimeSelectionLanes(fixture.view, {{1, 7}, {0, 10}})) {

Original expression:
QFAIL("move redo did not restore remapped owners")

A013 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:235
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
    }
}

void PianoRollTest::trackRemapInsert()
{
    QString error;
    const std::unique_ptr<checks::LoadedSong> source =
        checks::LoadedSong::load(m_project->root(), m_songLabel, error);

Original expression:
QVERIFY2(source, qPrintable(error))

A014 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:239
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(source->songInfo(), &error))

Original expression:
QFAIL("could not load track-remap fixture")

A015 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:241
Disposition: MATCHED
Mapping: S001 (remap.swift:35-46) fails the scenario unless the two-track fixture with an insertion slot can be provisioned.
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() < 2 || !fixture.document.canAddTrack())

Original expression:
QFAIL("track-remap fixture lacks two tracks and an insertion slot")

A016 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:252
Disposition: MATCHED
Mapping: S005 (src/checks/rollcheck/remap.swift:157-160) fails the scenario when the insertion is rejected although a slot is available, and asserts the inserted owner index.
Source context since previous assertion:
;

    fixture.view.applyEditorViewState(remapCosmetics());
    fixture.view.selectTrack(1);
    fixture.view.trackHeaderClicked(0, Qt::ControlModifier);
    fixture.view.setTrackMute(0, true);
    fixture.view.setTrackSolo(1, true);
    fixture.view.selectionModel().clearTimeSelection();
    fixture.view.selectionModel().clearNoteSelection();
    const int inserted = fixture.document.addTrack(0);
    if (inserted < 0)

Original expression:
QFAIL("track-remap fixture could not insert a track")

A017 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:254
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the insert two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the insert call (remap.swift:162-163) proves the remap is reconciled in the single publication, not the signal order.
Source context since previous assertion:
;
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("insert did not remap before documentChanged")

A018 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:256
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the insert call (remap.swift:162-163) asserts the presenter rows follow the rebuilt track order after the insert; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A019 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:262
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the insert call (remap.swift:162-163), S011 (remap.swift:164-167) and S023 (EditorGridCameraChecks.swift:1034-1037) assert the inserted track inherits no mute/solo/selection owner state and the pre-existing owners are retained. Unproved: the original hasNoOwnerCosmetics(inserted) cosmetic conjunct, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.trackMuted(inserted) || fixture.view.trackSoloed(inserted) ||
        (fixture.view.selectionModel().storedTrackScope() & (1u << inserted)) ||
        !hasOriginalTrackState(fixture.view) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), inserted)) {

Original expression:
QFAIL("inserted track inherited existing SongView state")

A020 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:267
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the insert-undo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the insert-undo call (remap.swift:171-172) proves the remap is reconciled in the single undo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->undo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("insert undo did not remap before documentChanged")

A021 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:269
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the insert-undo call (remap.swift:171-172) asserts the presenter rows follow the rebuilt track order after the insert undo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A022 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:272
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the insert-undo call (remap.swift:171-172) and S012 (remap.swift:173-174) assert the insert undo restores the document, history identity and the surviving owners. Unproved: the original hasNoOwnerCosmetics conjunct, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasOriginalTrackState(fixture.view))

Original expression:
QFAIL("insert undo did not restore existing SongView state")

A023 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:276
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the insert-redo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the insert-redo call (remap.swift:176-177) proves the remap is reconciled in the single redo publication, not the signal order.
Source context since previous assertion:
;

    fixture.document.undoStack()->redo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("insert redo did not remap before documentChanged")

A024 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:278
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the insert-redo call (remap.swift:176-177) asserts the presenter rows follow the rebuilt track order after the insert redo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A025 | PianoRollTest::trackRemapInsert | src/checks/rollcheck/remap.cpp:281
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the insert-redo call (remap.swift:176-177) and S013 (remap.swift:178) assert the redo reinstates the inserted track with the pre-existing owner state. Unproved: the original hasNoOwnerCosmetics conjunct, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasOriginalTrackState(fixture.view))

Original expression:
QFAIL("insert redo did not preserve existing SongView state")

A026 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:289
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
}

void PianoRollTest::trackRemapDuplicate()
{
    QString error;
    const std::unique_ptr<checks::LoadedSong> source =
        checks::LoadedSong::load(m_project->root(), m_songLabel, error);

Original expression:
QVERIFY2(source, qPrintable(error))

A027 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:293
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(source->songInfo(), &error))

Original expression:
QFAIL("could not load track-remap fixture")

A028 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:295
Disposition: MATCHED
Mapping: S001 (remap.swift:35-46) fails the scenario unless the two-track fixture with an insertion slot can be provisioned.
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() < 2 || !fixture.document.canAddTrack())

Original expression:
QFAIL("track-remap fixture lacks two tracks and an insertion slot")

A029 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:306
Disposition: MATCHED
Mapping: S006 (src/checks/rollcheck/remap.swift:194-196) fails the scenario when duplication is rejected although a slot is available, and asserts the duplicated owner index.
Source context since previous assertion:
;

    fixture.view.applyEditorViewState(remapCosmetics());
    fixture.view.selectTrack(1);
    fixture.view.trackHeaderClicked(0, Qt::ControlModifier);
    fixture.view.setTrackMute(0, true);
    fixture.view.setTrackSolo(1, true);
    fixture.view.selectionModel().clearTimeSelection();
    fixture.view.selectionModel().clearNoteSelection();
    const int duplicate = fixture.document.duplicateTrack(0);
    if (duplicate < 0)

Original expression:
QFAIL("track-remap fixture could not duplicate a track")

A030 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:308
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the duplicate two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the duplicate call (remap.swift:199-200) proves the remap is reconciled in the single publication, not the signal order.
Source context since previous assertion:
;
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("duplicate did not remap before documentChanged")

A031 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:310
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the duplicate call (remap.swift:199-200) asserts the presenter rows follow the rebuilt track order after the duplicate; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A032 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:316
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the duplicate call (remap.swift:199-200), S014 (remap.swift:201-204) and S027 (EditorGridCameraChecks.swift:1065-1068) assert the duplicated track keeps the owners on the source track and inherits none. Unproved: the original hasNoOwnerCosmetics(duplicate) conjunct, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.trackMuted(duplicate) || fixture.view.trackSoloed(duplicate) ||
        (fixture.view.selectionModel().storedTrackScope() & (1u << duplicate)) ||
        !hasOriginalTrackState(fixture.view) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), duplicate)) {

Original expression:
QFAIL("duplicated track inherited existing SongView state")

A033 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:321
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the duplicate-undo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the duplicate-undo call (remap.swift:210-211) proves the remap is reconciled in the single undo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->undo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("duplicate undo did not remap before documentChanged")

A034 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:323
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the duplicate-undo call (remap.swift:210-211) asserts the presenter rows follow the rebuilt track order after the duplicate undo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A035 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:326
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the duplicate-undo call (remap.swift:210-211) and S015 (remap.swift:212-213) assert the duplicate undo restores the document, history identity and the source-owner state. Unproved: the original cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasOriginalTrackState(fixture.view))

Original expression:
QFAIL("duplicate undo did not restore existing SongView state")

A036 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:330
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the duplicate-redo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the duplicate-redo call (remap.swift:215-216) proves the remap is reconciled in the single redo publication, not the signal order.
Source context since previous assertion:
;

    fixture.document.undoStack()->redo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("duplicate redo did not remap before documentChanged")

A037 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:332
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the duplicate-redo call (remap.swift:215-216) asserts the presenter rows follow the rebuilt track order after the duplicate redo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A038 | PianoRollTest::trackRemapDuplicate | src/checks/rollcheck/remap.cpp:335
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the duplicate-redo call (remap.swift:215-216) and S016 (remap.swift:217) assert the duplicate redo reinstates the duplicate with the source-owner state. Unproved: the original cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!hasOriginalTrackState(fixture.view))

Original expression:
QFAIL("duplicate redo did not preserve existing SongView state")

A039 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:343
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
}

void PianoRollTest::trackRemapDelete()
{
    QString error;
    const std::unique_ptr<checks::LoadedSong> source =
        checks::LoadedSong::load(m_project->root(), m_songLabel, error);

Original expression:
QVERIFY2(source, qPrintable(error))

A040 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:347
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(source->songInfo(), &error))

Original expression:
QFAIL("could not load track-remap fixture")

A041 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:349
Disposition: MATCHED
Mapping: S001 (remap.swift:35-46) fails the scenario unless the two-track fixture can be provisioned (the delete family's counterpart of the two-track guard).
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() < 2)

Original expression:
QFAIL("track-remap fixture lacks two tracks and an insertion slot")

A042 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:374
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the delete two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the delete call (remap.swift:240-241) proves the remap is reconciled in the single publication, not the signal order.
Source context since previous assertion:
;

    constexpr int removed = 1;
    fixture.view.selectTrack(0);
    fixture.view.trackHeaderClicked(removed, Qt::ControlModifier);
    fixture.view.setTrackMute(0, true);
    fixture.view.setTrackMute(removed, true);
    fixture.view.setTrackSolo(removed, true);
    EditorViewState deletedCosmetics = fixture.view.editorViewState();
    deletedCosmetics.emptyLanes.insert(controllerRow(removed, 74));
    deletedCosmetics.laneHeights.emplace(controllerRow(removed, 74), 123);
    deletedCosmetics.laneRanges.emplace(controllerRow(removed, 74), 120);
    fixture.view.applyEditorViewState(deletedCosmetics);
    songview::EditorSelectionModel::TimeSelection deletedLanes;
    deletedLanes.scope = songview::EditorSelectionModel::TimeSelection::Lanes;
    deletedLanes.startTick = 24;
    deletedLanes.endTick = 48;
    deletedLanes.lanes = {{removed, 74}};
    fixture.view.selectionModel().setTimeSelection(deletedLanes);
    const std::vector<DocNote> duplicateNotes = fixture.document.notesForTrack(removed);
    if (!duplicateNotes.empty())
        fixture.view.selectionModel().setNoteSelection({duplicateNotes.front().noteId});

    fixture.document.deleteTrack(removed);
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("delete did not remap before documentChanged")

A043 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:376
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the delete call (remap.swift:240-241) asserts the presenter rows follow the rebuilt track order after the delete; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A044 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:383
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the delete call (remap.swift:240-241), S017 (remap.swift:242-243) and S028 (EditorGridCameraChecks.swift:1071-1074) assert the removed owner's note and note-selection clear and the mute/solo/scope fallback. Unproved: the original inactive time-selection and removed-owner empty-lane cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.selectionModel().primaryTrack() == removed ||
        !fixture.view.selectionModel().noteSelection().empty() ||
        fixture.view.selectionModel().timeSelection().active() ||
        fixture.view.trackMuted(removed) || fixture.view.trackSoloed(removed) ||
        hasEmptyLane(fixture.view.editorViewState(), removed, 74)) {

Original expression:
QFAIL("deleted track left SongView-owned state behind")

A045 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:390
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the delete call (remap.swift:240-241) and S028 (EditorGridCameraChecks.swift:1071-1074) assert the fallback selection, the single-owner scope and the mute/solo drop. Unproved: the original hasNoOwnerCosmetics conjunct, which the Swift roll does not model.
Source context since previous assertion:
;
    }
    const int fallback = std::min(removed, fixture.document.engineTrackCount() - 1);
    if (fixture.view.selectionModel().primaryTrack() != fallback ||
        fixture.view.selectionModel().storedTrackScope() != (1u << fallback) ||
        !fixture.view.trackMuted(0) || fixture.view.trackSoloed(0) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), removed)) {

Original expression:
QFAIL("delete did not drop cosmetic state from its removed owner")

A046 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:395
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the delete-undo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the delete-undo call (remap.swift:247-248) proves the remap is reconciled in the single undo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->undo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("delete undo did not remap before documentChanged")

A047 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:397
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the delete-undo call (remap.swift:247-248) asserts the presenter rows follow the rebuilt track order after the delete undo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A048 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:403
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the delete-undo call (remap.swift:247-248), S018 (remap.swift:249-251) and S029 (EditorGridCameraChecks.swift:1076-1080) assert the restored track does not revive the dropped note selection and that the surviving owner state returns. Unproved: the original inactive time-selection and cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (!fixture.view.selectionModel().noteSelection().empty() ||
        fixture.view.selectionModel().timeSelection().active() ||
        fixture.view.trackMuted(removed) || fixture.view.trackSoloed(removed) ||
        hasEmptyLane(fixture.view.editorViewState(), removed, 74)) {

Original expression:
QFAIL("restored track inherited dropped SongView state")

A049 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:409
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the delete-undo call (remap.swift:247-248), S018 (remap.swift:249-251) and S029 (EditorGridCameraChecks.swift:1076-1080) assert the restored document, history identity and surviving owners. Unproved: the original cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    }
    if (fixture.view.selectionModel().primaryTrack() != 0 ||
        fixture.view.selectionModel().storedTrackScope() != 0x1 || !fixture.view.trackMuted(0) ||
        fixture.view.trackSoloed(0) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), removed)) {

Original expression:
QFAIL("delete undo did not restore surviving SongView state")

A050 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:414
Disposition: GAP
Mapping/reason: GAP — SongView Qt signals do not exist in Swift, so the delete-redo two-signal emission order has no executing counterpart. S003 body (remap.swift:86-92) at the delete-redo call (remap.swift:253-254) proves the remap is reconciled in the single redo publication, not the signal order.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->redo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("delete redo did not remap before documentChanged")

A051 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:416
Disposition: MATCHED
Mapping: S004 body (remap.swift:96-105) at the delete-redo call (remap.swift:253-254) asserts the presenter rows follow the rebuilt track order after the delete redo; S030 (tst_ShellGridInput.qml:476-486 and 492-499) corroborates track/header follow on the shell lane.
Source context since previous assertion:
;
    if (!fixture.headersFollowTimeline())

Original expression:
QFAIL("track header list did not follow the rebuilt timeline")

A052 | PianoRollTest::trackRemapDelete | src/checks/rollcheck/remap.cpp:422
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the delete-redo call (remap.swift:253-254), S019 (remap.swift:255) and S028 (EditorGridCameraChecks.swift:1071-1074) assert the redo removes the owner again without reviving its state. Unproved: the original cosmetic conjuncts, which the Swift roll does not model.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.selectionModel().primaryTrack() != 0 ||
        fixture.view.selectionModel().storedTrackScope() != 0x1 || !fixture.view.trackMuted(0) ||
        fixture.view.trackSoloed(0) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), removed)) {

Original expression:
QFAIL("delete redo did not keep dropped SongView state absent")

A053 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:431
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
    }
}

void PianoRollTest::trackRemapMetadata()
{
    QString error;
    const std::unique_ptr<checks::LoadedSong> source =
        checks::LoadedSong::load(m_project->root(), m_songLabel, error);

Original expression:
QVERIFY2(source, qPrintable(error))

A054 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:435
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(source->songInfo(), &error))

Original expression:
QFAIL("could not load track-remap fixture")

A055 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:437
Disposition: MATCHED
Mapping: S001 (remap.swift:35-46) fails the scenario unless the two-track fixture can be provisioned (the metadata family's counterpart of the two-track guard).
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() < 2)

Original expression:
QFAIL("track-remap fixture lacks two tracks and an insertion slot")

A056 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:464
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the metadata-edit call (remap.swift:274-275) asserts the metadata publication reports remapped == false with every owner value unchanged, and S020 (remap.swift:276-277) asserts the header rows are not reset. Unproved: the original documentOnly() timeline-rebuild observation and the editorViewState cosmetic equality, which the Swift roll does not model.
Source context since previous assertion:
;

    fixture.view.applyEditorViewState(remapCosmetics());
    fixture.view.selectTrack(1);
    fixture.view.trackHeaderClicked(0, Qt::ControlModifier);
    fixture.view.setTrackMute(0, true);
    fixture.view.setTrackSolo(1, true);
    const int selected = fixture.view.selectionModel().primaryTrack();
    const uint32_t headerMask = fixture.view.selectionModel().storedTrackScope();
    const EditorViewState cosmetics = fixture.view.editorViewState();
    const bool mute0 = fixture.view.trackMuted(0);
    const bool mute1 = fixture.view.trackMuted(1);
    const bool mute2 = fixture.view.trackMuted(2);
    const bool solo0 = fixture.view.trackSoloed(0);
    const bool solo1 = fixture.view.trackSoloed(1);
    const bool solo2 = fixture.view.trackSoloed(2);
    const auto stateUnchanged = [&] {
        return fixture.view.selectionModel().primaryTrack() == selected &&
               fixture.view.selectionModel().storedTrackScope() == headerMask &&
               fixture.view.editorViewState() == cosmetics && fixture.view.trackMuted(0) == mute0 &&
               fixture.view.trackMuted(1) == mute1 && fixture.view.trackMuted(2) == mute2 &&
               fixture.view.trackSoloed(0) == solo0 && fixture.view.trackSoloed(1) == solo1 &&
               fixture.view.trackSoloed(2) == solo2;
    };

    fixture.document.renameTrack(0, QStringLiteral("rollcheck remap metadata"));
    if (!fixture.documentOnly() || !stateUnchanged())

Original expression:
QFAIL("metadata-only edit changed SongView owner state")

A057 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:468
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the metadata-undo call (remap.swift:281-282) asserts the metadata-undo publication reports remapped == false with every owner value unchanged; S021 (remap.swift:283-284) asserts the restored document. Unproved: the original documentOnly() observation and the editorViewState cosmetic equality.
Source context since previous assertion:
;
    fixture.clearOrder();
    fixture.document.undoStack()->undo();
    if (!fixture.documentOnly() || !stateUnchanged())

Original expression:
QFAIL("metadata-only undo changed SongView owner state")

A058 | PianoRollTest::trackRemapMetadata | src/checks/rollcheck/remap.cpp:472
Disposition: PARTIAL
Mapping/reason: S003 body (remap.swift:86-92) at the metadata-redo call (remap.swift:286-287) asserts the metadata-redo publication reports remapped == false with every owner value unchanged; S022 (remap.swift:288) asserts the restored name. Unproved: the original documentOnly() observation and the editorViewState cosmetic equality.
Source context since previous assertion:
;
    fixture.clearOrder();
    fixture.document.undoStack()->redo();
    if (!fixture.documentOnly() || !stateUnchanged())

Original expression:
QFAIL("metadata-only redo changed SongView owner state")

A059 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:497
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;
}

void PianoRollTest::trackRemapEnginePromotion()
{
    QTemporaryDir rawDir;
    SmfFile rawSmf;
    rawSmf.tracks.resize(2);
    rawSmf.tracks[0].endTick = 96;
    rawSmf.tracks[1].endTick = 96;
    SmfEvent metadata;
    metadata.status = 0xFF;
    metadata.metaType = 0x01;
    metadata.blob = QByteArrayLiteral("metadata");
    rawSmf.tracks[0].events.push_back(metadata);
    SmfEvent originalProgram;
    originalProgram.status = 0xC1;
    originalProgram.data0 = 4;
    rawSmf.tracks[1].events.push_back(originalProgram);
    SongInfo rawInfo;
    rawInfo.label = QStringLiteral("rollcheck_raw_remap");
    rawInfo.midPath = rawDir.filePath(QStringLiteral("raw-remap.mid"));
    rawInfo.hasMid = true;
    QString error;
    if (!rawDir.isValid() || !rawSmf.writeFile(rawInfo.midPath, &error))

Original expression:
QFAIL("could not write raw metadata-to-engine remap fixture")

A060 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:501
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/rollcheck/remap.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
Source context since previous assertion:
;

    RemapFixture fixture;
    if (!fixture.prepare(rawInfo, &error))

Original expression:
QFAIL("could not load raw metadata-to-engine remap fixture")

A061 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:503
Disposition: GAP
Mapping/reason: No Swift predicate asserts that a raw metadata-only track becomes the sole engine owner; the Swift raw-insert path never promotes or reclassifies a conductor track.
Source context since previous assertion:
;
    if (fixture.document.engineTrackCount() != 1)

Original expression:
QFAIL("raw metadata-to-engine fixture did not start with one engine owner")

A062 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:525
Disposition: GAP
Mapping/reason: No Swift predicate observes a remap publication for a raw metadata-to-engine edit: insertRawEvent in Swift never emits TrackRemap.
Source context since previous assertion:
;

    const EditorAutomationRowId tempo{EditorAutomationRowKind::Tempo, 0, 0};
    EditorViewState cosmetics;
    cosmetics.laneHeight = 64;
    cosmetics.laneHeights.emplace(tempo, 94);
    cosmetics.laneHeights.emplace(controllerRow(0, 7), 67);
    cosmetics.laneRanges.emplace(tempo, 116);
    cosmetics.laneRanges.emplace(controllerRow(0, 7), 102);
    cosmetics.emptyLanes.emplace(controllerRow(0, 7));
    fixture.view.applyEditorViewState(cosmetics);
    fixture.view.setTrackSolo(0, true);
    songview::EditorSelectionModel::TimeSelection tracks;
    tracks.startTick = 24;
    tracks.endTick = 48;
    fixture.view.selectionModel().setTimeSelection(tracks);

    SmfEvent promotedProgram;
    promotedProgram.status = 0xC0;
    promotedProgram.data0 = 3;
    fixture.document.insertRawEvent(0, promotedProgram);
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("metadata-to-engine raw edit did not remap before documentChanged")

A063 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:533
Disposition: GAP
Mapping/reason: No Swift predicate observes the engine-to-metadata owner rebinding after a raw edit; the promotion mechanism is absent from the Swift raw-event path.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.selectionModel().primaryTrack() != 1 ||
        fixture.view.selectionModel().storedTrackScope() != 0x2 || fixture.view.trackMuted(0) ||
        fixture.view.trackMuted(1) || fixture.view.trackSoloed(0) || !fixture.view.trackSoloed(1) ||
        !fixture.view.selectionModel().timeSelection().active() ||
        !hasRawCosmetics(fixture.view.editorViewState(), 1) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), 0)) {

Original expression:
QFAIL("metadata-to-engine raw edit did not remap SongView owners")

A064 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:541
Disposition: GAP
Mapping/reason: No Swift predicate observes a raw-undo remap publication; insertRawEvent undo carries no TrackRemap in Swift.
Source context since previous assertion:
;
    }

    fixture.view.selectTrack(0);
    fixture.view.setTrackMute(0, true);
    fixture.view.selectionModel().setTimeSelection(tracks);
    fixture.document.undoStack()->undo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("engine-to-metadata raw undo did not remap before documentChanged")

A065 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:548
Disposition: GAP
Mapping/reason: No Swift predicate observes the engine-to-metadata fallback rebinding of owner state; the promotion mechanism is absent from the Swift raw-event path.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.selectionModel().primaryTrack() != 0 ||
        fixture.view.selectionModel().storedTrackScope() != 0x1 || fixture.view.trackMuted(0) ||
        !fixture.view.trackSoloed(0) || fixture.view.selectionModel().timeSelection().active() ||
        !hasRawCosmetics(fixture.view.editorViewState(), 0) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), 1)) {

Original expression:
QFAIL("engine-to-metadata fallback rebound SongView state to the wrong owner")

A066 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:553
Disposition: GAP
Mapping/reason: No Swift predicate observes a raw-redo remap publication; insertRawEvent redo carries no TrackRemap in Swift.
Source context since previous assertion:
;
    }

    fixture.document.undoStack()->redo();
    if (!fixture.remapPrecedesDocument())

Original expression:
QFAIL("metadata-to-engine raw redo did not remap before documentChanged")

A067 | PianoRollTest::trackRemapEnginePromotion | src/checks/rollcheck/remap.cpp:561
Disposition: GAP
Mapping/reason: No Swift predicate observes the re-applied engine owner state after a raw redo; the dynamic conductor promotion that this site pins does not exist in the Swift raw-event path.
Source context since previous assertion:
;
    fixture.clearOrder();
    if (fixture.view.selectionModel().primaryTrack() != 1 ||
        fixture.view.selectionModel().storedTrackScope() != 0x2 || fixture.view.trackMuted(0) ||
        fixture.view.trackMuted(1) || fixture.view.trackSoloed(0) || !fixture.view.trackSoloed(1) ||
        fixture.view.selectionModel().timeSelection().active() ||
        !hasRawCosmetics(fixture.view.editorViewState(), 1) ||
        !hasNoOwnerCosmetics(fixture.view.editorViewState(), 0)) {

Original expression:
QFAIL("metadata-to-engine redo did not preserve remapped SongView owners")

Remaining original source (closing sequence and helper bodies):
;
    }
}

Swift assertion predicates
The S entries are exact report.expect/fail predicates already executed by
runRemapChecks and runEditorGridCameraChecks (swiftcore projectSession suite), plus the
shell-grid-input lane predicate (deno task verify --filter swiftcore --verbose PASS
1/14 ok; deno task verify:shell --verbose PASS 11/11; both controller-recorded).

S001 | RemapProbe fixture provisioning guard | src/checks/rollcheck/remap.swift:35
  report.fail("swiftcore/PianoRollTest::trackRemapMove",
              "supplied session cannot provision the two-track remap fixture")

S002 | runRemapChecks fixture restoration | src/checks/rollcheck/remap.swift:29
  report.expectEqual(originalState, document.state,
                     cppID: "swiftcore/PianoRollTest::trackRemapMove",
                     what: "remap fixture leaves the supplied song unchanged")

S003 | RemapProbe.expectPublication publication payload | src/checks/rollcheck/remap.swift:86
  report.expect(documentChanges.count == 1 && documentChanges[0].remapped == remapped
                && documentChanges[0].selected == selected
                && documentChanges[0].scope == scope
                && documentChanges[0].muted == muted
                && documentChanges[0].soloed == soloed,
                cppID: cppID,
                message: "\(phase): owner state is reconciled before the single document publication")

S004 | RemapProbe.expectPublication header records | src/checks/rollcheck/remap.swift:96
  report.expect(headers.rows.count == count + (document.canAddTrack ? 1 : 0)
                && (0..<headers.rows.count).map { headers.rows[$0] }
                    .filter { !$0.isAddTrack }.map(\.track) == Array(0..<count)
                && (0..<count).allSatisfy {
                    let name = document.trackName($0)
                    return headers.rows[$0].title == "\($0 + 1) · \(name.isEmpty ? "Track \($0 + 1)" : name)"
                }
                && (!document.canAddTrack || headers.rows[count].isAddTrack),
                cppID: cppID, message: "\(phase): header records follow current track order")

S005 | checkRemapInsert insertion guard | src/checks/rollcheck/remap.swift:158
  report.fail(id, "insertion rejected with an available track slot")

S006 | checkRemapDuplicate duplication guard | src/checks/rollcheck/remap.swift:195
  report.fail(id, "duplicate rejected with an available track slot")

S007 | checkRemapMove accepted | src/checks/rollcheck/remap.swift:125
  report.expect(document.moveTrack(0, to: 1), cppID: id, message: "move accepts the source track")

S008 | checkRemapMove note owners | src/checks/rollcheck/remap.swift:129
  report.expect(document.note(firstNote.id)?.track == 1 && document.note(secondNote.id)?.track == 0,
                cppID: id, message: "move carries both note owners to their new slots")

S009 | checkRemapMove undo | src/checks/rollcheck/remap.swift:136
  report.expect(document.state == before && document.history.currentIdentity == identity,
                cppID: id, message: "one undo restores the original document and history position")

S010 | checkRemapMove redo | src/checks/rollcheck/remap.swift:141
  report.expectEqual(moved, document.state, cppID: id, what: "redo restores moved note owners")

S011 | checkRemapInsert owner inheritance | src/checks/rollcheck/remap.swift:164
  report.expect(inserted == 2 && !session.mutedTracks.contains(inserted)
                && !session.soloedTracks.contains(inserted)
                && !session.selectedTracks.contains(inserted), cppID: id,
                message: "new track inherits no pre-existing owner state")

S012 | checkRemapInsert undo | src/checks/rollcheck/remap.swift:173
  report.expect(document.state == before && document.history.currentIdentity == identity,
                cppID: id, message: "one undo removes the inserted track")

S013 | checkRemapInsert redo | src/checks/rollcheck/remap.swift:178
  report.expectEqual(added, document.state, cppID: id, what: "redo reinstates the inserted track")

S014 | checkRemapDuplicate owner inheritance | src/checks/rollcheck/remap.swift:201
  report.expect(duplicate == 2 && !session.mutedTracks.contains(duplicate)
                && !session.soloedTracks.contains(duplicate)
                && !session.selectedTracks.contains(duplicate), cppID: id,
                message: "duplicate copies musical notes but not owner-only state")

S015 | checkRemapDuplicate undo | src/checks/rollcheck/remap.swift:212
  report.expect(document.state == before && document.history.currentIdentity == identity,
                cppID: id, message: "one undo removes the duplicate")

S016 | checkRemapDuplicate redo | src/checks/rollcheck/remap.swift:217
  report.expectEqual(duplicated, document.state, cppID: id, what: "redo reinstates the duplicate")

S017 | checkRemapDelete note and selection | src/checks/rollcheck/remap.swift:242
  report.expect(document.note(removedNote.id) == nil && session.selectedNotes.isEmpty,
                cppID: id, message: "deleting an owner removes its note and selected-note reference")

S018 | checkRemapDelete undo | src/checks/rollcheck/remap.swift:249
  report.expect(document.state == before && document.history.currentIdentity == identity
                && session.selectedNotes.isEmpty, cppID: id,
                message: "one undo restores the document, not the dropped session selection")

S019 | checkRemapDelete redo | src/checks/rollcheck/remap.swift:255
  report.expectEqual(deleted, document.state, cppID: id, what: "redo removes the owner again")

S020 | checkRemapMetadata header stability | src/checks/rollcheck/remap.swift:276
  report.expect(probe.headers.rowRebuildCount == rebuilds, cppID: id,
                message: "metadata edit updates the header without resetting its records")

S021 | checkRemapMetadata undo | src/checks/rollcheck/remap.swift:283
  report.expect(document.state == before && document.history.currentIdentity == identity,
                cppID: id, message: "one metadata undo restores the original name")

S022 | checkRemapMetadata redo | src/checks/rollcheck/remap.swift:288
  report.expectEqual(renamed, document.state, cppID: id, what: "redo restores the metadata name")

S023 | swiftcore/EditorGridCamera::trackOwnerRemap insert | src/checks/rollcheck/EditorGridCameraChecks.swift:1035-1037
  session.mutedTracks == [0] && session.soloedTracks == [0]
      && session.selectedTrack == 0,
  cppID: id, message: "inserted track inherits no mute, solo, or selection owner state")

S024 | swiftcore/EditorGridCamera::trackOwnerRemap move | src/checks/rollcheck/EditorGridCameraChecks.swift:1051-1053
  session.mutedTracks == [1] && session.soloedTracks == [0]
      && session.selectedTrack == 0,
  cppID: id, message: "move remaps mute, solo, and selection owners to the new index")

S025 | swiftcore/EditorGridCamera::trackOwnerRemap remap-before-publication | src/checks/rollcheck/EditorGridCameraChecks.swift:1056-1058
      $0.muted == [1] && $0.soloed == [0] && $0.selected == 0
  } == true,
  cppID: id, message: "owner remap lands before the document change publishes")

S026 | swiftcore/EditorGridCamera::trackOwnerRemap undo | src/checks/rollcheck/EditorGridCameraChecks.swift:1061-1063
  session.mutedTracks == [0] && session.soloedTracks == [1]
      && session.selectedTrack == 1,
  cppID: id, message: "undo applies the inverse remap to every owner")

S027 | swiftcore/EditorGridCamera::trackOwnerRemap duplicate | src/checks/rollcheck/EditorGridCameraChecks.swift:1066-1068
  session.mutedTracks == [0] && session.soloedTracks == [1]
      && session.selectedTrack == 1,
  cppID: id, message: "duplicate keeps owner identity on the source track")

S028 | swiftcore/EditorGridCamera::trackOwnerRemap delete | src/checks/rollcheck/EditorGridCameraChecks.swift:1072-1074
  session.soloedTracks.isEmpty && session.mutedTracks == [0]
      && session.selectedTrack == 0,
  cppID: id, message: "delete drops the removed owner and falls selection back")

S029 | swiftcore/EditorGridCamera::trackOwnerRemap delete undo | src/checks/rollcheck/EditorGridCameraChecks.swift:1077-1080
  session.soloedTracks.isEmpty && session.mutedTracks == [0]
      && session.selectedTrack == 0
      && session.document.engineTracks.usedTrackCount == added + 1,
  cppID: id, message: "undo restores the track without reviving dropped owner state")

S030 | test_trackFollowReload | src/checks/editorqml/tst_ShellGridInput.qml:476
  verify(waitForNative(function() { if (grid.trackIndex !== 1) return false; ... every rendered note on track 1 ... }, 5000), "setTrack follows track 1 in notes and faces")
  plus tst_ShellGridInput.qml:492 verify(waitForNative(function() { ... replacement !== oldGrid ... }, 15000), "re-opening the selected song replaces its grid in place")
