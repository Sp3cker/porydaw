Assertion correspondence: onboardcheck/action.cpp

Reference revision: a7fcaa3e9ea51a057c85bc1959e541c69fe4a3ea
Deleted in: S6 flip cutover (feature/swift-project-store, base 2d1c92d0)
Retirement: historical deletion record only; the deleted C++ source above is recoverable from the Reference revision and Original SHA-256, never from HEAD. The dispositions and S citations below were established while the file was live and were not re-verified in this cutover; no execution or parity claim is made by this record.
Original: src/checks/onboardcheck/action.cpp
Original SHA-256: fd4ade0d9a60e73aeec99865631d42849d1ac83257b78311c9ae6e7bf29c24e7
Swift counterpart: none identified
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

Site tally: 49 sites (9 PARTIAL, 40 GAP).



Command: deno task verify --filter onboardcheck --verbose
Result: pending integrated run.

OnboardingTest::import* methods, none in this file).

A001 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:66
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:
  #include "checks/onboardcheck/onboardingtest.h"

  #include <QAbstractButton>
  #include <QApplication>
  #include <QFile>
  #include <QMessageBox>
  #include <QSettings>
  #include <QTimer>
  #include <QVariant>
  #include <QWidget>

  #include "checks/support/asyncwait.h"
  #include "checks/support/songfixture.h"
  #include "mainwindow.h"
  #include "ui/workspaceui.h"
  #include <optional>
  #include <utility>
  #include <vector>

  namespace {
  QString midPath(const QString &root, const QString &label)
  {
      return root + QStringLiteral("/sound/songs/midi/%1.mid").arg(label);
  }

  class SettingsGuard final
  {
    public:
      SettingsGuard()
      {
          for (const QString &key : m_settings.allKeys())
              m_values.emplace_back(key, m_settings.value(key));
          m_settings.clear();
          m_settings.sync();
      }

      ~SettingsGuard()
      {
          m_settings.clear();
          for (const auto &[key, value] : m_values)
              m_settings.setValue(key, value);
          m_settings.sync();
      }

      SettingsGuard(const SettingsGuard &) = delete;
      SettingsGuard &operator=(const SettingsGuard &) = delete;

    private:
      QSettings m_settings;
      std::vector<std::pair<QString, QVariant>> m_values;
  };

  QAbstractButton *buttonWithRole(QMessageBox &dialog, QMessageBox::ButtonRole role)
  {
      for (QAbstractButton *button : dialog.buttons())
          if (dialog.buttonRole(button) == role)
              return button;
      return nullptr;
  }
  } // namespace

  void OnboardingTest::registerAction()
  {
      QString error;
      std::unique_ptr<checks::ProjectFixture> fixture = copyProject(error);

Original expression:
  QVERIFY2(fixture, qPrintable(error));

A002 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:71
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const QString root = fixture->root();
      const QString label = QStringLiteral("mus_onboardcheck_action_register");
      const QString constant = SongRegistry::constantForLabel(label);
      SongCfg cfg;

Original expression:
  QVERIFY2(defaultCfg(root, cfg, error), qPrintable(error));

A003 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:72
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY2(SongRegistry::blankSong().writeFile(midPath(root, label), &error), qPrintable(error));

A004 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:73
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY2(SongRegistry::writeSongFlags(midiDirectory(root), label, cfg.rawFlags, &error),
               qPrintable(error));

A005 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:76
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      int id = -1;

Original expression:
  QVERIFY2(SongRegistry::registerSong(root, label, constant, QStringLiteral("MUSIC_PLAYER_BGM"),
                                          &error, &id),
               qPrintable(error));

A006 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:81
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const RegistrationPlan plan =
          SongRegistry::makePlan(root, label, constant, QStringLiteral("MUSIC_PLAYER_BGM"));

Original expression:
  QVERIFY(plan.charmapApplicable);

A007 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:84
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const QString charmap = root + QStringLiteral("/charmap.txt");
      const QByteArray complete = readFile(charmap, error);

Original expression:
  QVERIFY2(error.isEmpty(), qPrintable(error));

A008 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:86
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const int at = complete.indexOf(plan.charmapLine.toUtf8());

Original expression:
  QVERIFY(at >= 0);

A009 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:88
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const int end = complete.indexOf('\n', at);

Original expression:
  QVERIFY(end >= at);

A010 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:91
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      QByteArray stripped = complete;
      stripped.remove(at, end - at + 1);

Original expression:
  QVERIFY2(writeFile(charmap, stripped, error), qPrintable(error));

A011 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:95
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


      SettingsGuard settings;
      MainWindow window;

Original expression:
  QVERIFY2(openSong(window, root, label, error), qPrintable(error));

A012 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:96
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY(window.m_registerAction->isEnabled());

A013 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:101
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const SongInfo *partial = nullptr;
      for (const SongInfo &song : window.m_workspace->projectState().snapshot.songs())
          if (song.label == label)
              partial = &song;

Original expression:
  QVERIFY(partial);

A014 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:102
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY(partial->registered);

A015 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:103
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QCOMPARE(partial->registrationGaps, QStringList{QStringLiteral("charmap.txt")});

A016 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:104
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY(window.m_workspace->isSongListed(label));

A017 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:107
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips opens mounted QML Register Song dialog from a real staged plan; original QMessageBox identity and integrated execution are not asserted.
Source context since previous assertion:

      window.m_workspace->registerSelectedSong();
      QMessageBox *dialog = waitForDialog(window, OnboardingDialog::Register, error);

Original expression:
  QVERIFY2(dialog, qPrintable(error));

A018 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:109
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips locates and clicks the mounted Register dialog action; QMessageBox AcceptRole is retired widget mechanics and integrated execution remains pending.
Source context since previous assertion:

      QAbstractButton *button = buttonWithRole(*dialog, QMessageBox::AcceptRole);

Original expression:
  QVERIFY(button);

A019 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:111
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips confirms registration and waits for canRegister to turn false after the project feed refresh; original ProjectOpenState::Ready predicate and integrated execution remain unproved.
Source context since previous assertion:

      button->click();

Original expression:
  QCOMPARE(checks::async_wait::waitUntil(
                   [&window] {
                       return window.m_workspace->projectState().state == ProjectOpenState::Ready;
                   },
                   [&window, &label] {
                       for (const SongInfo &song :
                            window.m_workspace->projectState().snapshot.songs())
                           if (song.label == label)
                               return song.registered && song.registrationGaps.isEmpty() &&
                                      window.m_workspace->isSongListed(label) &&
                                      !window.m_registerAction->isEnabled();
                       return false;
                   }),
               checks::async_wait::Result::Ready);

A020 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:125
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QCOMPARE(readFile(charmap, error), complete);

A021 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:126
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY2(error.isEmpty(), qPrintable(error));

A022 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:128
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const std::optional<SongName> name = SongName::create(label);

Original expression:
  QVERIFY(name.has_value());

A023 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:130
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      window.m_workspace->requestSongOpen(*name);

Original expression:
  QCOMPARE(
          checks::async_wait::waitUntil(
              [] { return true; }, [&window] { return window.m_workspace->openProjectEnabled(); }),
          checks::async_wait::Result::Ready);

A024 | OnboardingTest::registerAction | src/checks/onboardcheck/action.cpp:134
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY(!window.m_registerAction->isEnabled());

A025 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:150
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      closeWorkspace(window);
  }

  void OnboardingTest::deleteAction_data()
  {
      QTest::addColumn<bool>("fallback");
      QTest::newRow("delete-open-song") << false;
      QTest::newRow("fallback-refusal") << true;
  }

  void OnboardingTest::deleteAction()
  {
      QFETCH(bool, fallback);
      QString error;
      std::unique_ptr<checks::ProjectFixture> fixture = copyProject(error);

Original expression:
  QVERIFY2(fixture, qPrintable(error));

A026 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:153
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const QString root = fixture->root();
      SongCfg cfg;

Original expression:
  QVERIFY2(defaultCfg(root, cfg, error), qPrintable(error));

A027 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:157
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      QString label;
      if (!fallback) {
          label = QStringLiteral("mus_onboardcheck_action_delete");

Original expression:
  QVERIFY2(SongRegistry::blankSong().writeFile(midPath(root, label), &error),
                   qPrintable(error));

A028 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:159
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY2(SongRegistry::writeSongFlags(midiDirectory(root), label, cfg.rawFlags, &error),
                   qPrintable(error));

A029 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:162
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          int id = -1;

Original expression:
  QVERIFY2(SongRegistry::registerSong(root, label, SongRegistry::constantForLabel(label),
                                              QStringLiteral("MUSIC_PLAYER_BGM"), &error, &id),
                   qPrintable(error));

A030 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:167
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      } else {
          DecompProject project;

Original expression:
  QVERIFY2(project.open(root, &error), qPrintable(error));

A031 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:171
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          for (const SongInfo &song : project.songs())
              if (song.registered && song.id == 0)
                  label = song.label;

Original expression:
  QVERIFY(!label.isEmpty());

A032 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:173
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          if (!QFile::exists(midPath(root, label)))

Original expression:
  QVERIFY2(SongRegistry::blankSong().writeFile(midPath(root, label), &error),
                       qPrintable(error));

A033 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:179
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      }

      SettingsGuard settings;
      MainWindow window;

Original expression:
  QVERIFY2(openSong(window, root, label, error), qPrintable(error));

A034 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:189
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      const QStringList files = {root + QStringLiteral("/sound/song_table.inc"),
                                 root + QStringLiteral("/include/constants/songs.h"),
                                 root + QStringLiteral("/ld_script.ld"),
                                 root + QStringLiteral("/charmap.txt"),
                                 midiDirectory(root) + QStringLiteral("/midi.cfg"),
                                 root + QStringLiteral("/src/debug.c")};
      QList<QByteArray> before;
      for (const QString &path : files) {
          before.append(readFile(path, error));

Original expression:
  QVERIFY2(error.isEmpty(), qPrintable(error));

A035 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:220
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

      }
      const qsizetype listed = window.m_workspace->listedSongCount();
      if (fallback) {
          bool refusalHandled = false;
          QTimer refusalCloser;
          refusalCloser.setInterval(0);
          connect(&refusalCloser, &QTimer::timeout, &window, [&] {
              QWidget *const modal = QApplication::activeModalWidget();
              if (!modal)
                  return;
              auto *const dialog = qobject_cast<QMessageBox *>(modal);
              if (!dialog || dialog->parent() != &window) {
                  error = QStringLiteral("unexpected modal dialog during fallback refusal");
                  modal->close();
                  refusalHandled = true;
                  return;
              }
              if (!dialog->text().contains(label))
                  error = QStringLiteral("fallback refusal did not identify the song");
              QAbstractButton *const button = buttonWithRole(*dialog, QMessageBox::AcceptRole);
              refusalHandled = true;
              if (button)
                  button->click();
              else {
                  error = QStringLiteral("fallback refusal has no accepting action");
                  dialog->reject();
              }
          });
          refusalCloser.start();
          window.m_workspace->deleteSelectedSong();

Original expression:
  QVERIFY2(QTest::qWaitFor([&] { return refusalHandled; }),
                   "fallback refusal did not appear");

A036 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:223
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          refusalCloser.stop();

Original expression:
  QVERIFY2(error.isEmpty(), qPrintable(error));

A037 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:225
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          const std::optional<SongName> name = SongName::create(label);

Original expression:
  QVERIFY(name.has_value());

A038 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:226
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY(window.m_workspace->songTabFor(*name));

A039 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:228
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          for (int i = 0; i < files.size(); ++i) {

Original expression:
  QCOMPARE(readFile(files[i], error), before[i]);

A040 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:229
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QVERIFY2(error.isEmpty(), qPrintable(error));

A041 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:231
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          }

Original expression:
  QVERIFY(QFile::exists(midPath(root, label)));

A042 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:235
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips opens mounted QML Delete Song dialog from a real staged deletion plan; original QMessageBox identity and integrated execution remain unproved.
Source context since previous assertion:

      } else {
          window.m_workspace->deleteSelectedSong();
          QMessageBox *dialog = waitForDialog(window, OnboardingDialog::Delete, error);

Original expression:
  QVERIFY2(dialog, qPrintable(error));

A043 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:237
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips clicks the mounted Delete confirmation button; original QMessageBox DestructiveRole is retired widget mechanics and integrated execution remains pending.
Source context since previous assertion:

          QAbstractButton *button = buttonWithRole(*dialog, QMessageBox::DestructiveRole);

Original expression:
  QVERIFY(button);

A044 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:240
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:

          button->click();
          const std::optional<SongName> name = SongName::create(label);

Original expression:
  QVERIFY(name.has_value());

A045 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:241
Disposition: GAP
Mapping/reason: feature not yet ported: onboarding register/delete dialog flow; the QML Register/Delete confirmation dialogs are absent (ShellPresenter's file.register_song is unwired) and the MainWindow/QMessageBox mechanics are unexecuted.
Source context since previous assertion:


Original expression:
  QCOMPARE(checks::async_wait::waitUntil(
                       [&window] {
                           return window.m_workspace->projectState().state == ProjectOpenState::Ready;
                       },
                       [&window, &name] { return window.m_workspace->songTabFor(*name) == nullptr; }),
                   checks::async_wait::Result::Ready);

A046 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:247
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips asserts the deleted row disappears from the mounted dock; original WorkspaceUi.isSongListed and integrated execution remain unproved.
Source context since previous assertion:


Original expression:
  QVERIFY(!window.m_workspace->isSongListed(label));

A047 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:248
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips asserts mounted song count drops from ten to nine after deletion; original WorkspaceUi count and integrated execution remain unproved.
Source context since previous assertion:


Original expression:
  QCOMPARE(window.m_workspace->listedSongCount(), listed - 1);

A048 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:249
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips asserts the staged source MIDI no longer exists after QML Delete; original fixture identity and integrated execution remain unproved.
Source context since previous assertion:


Original expression:
  QVERIFY(!QFile::exists(midPath(root, label)));

A049 | OnboardingTest::deleteAction | src/checks/onboardcheck/action.cpp:250
Disposition: PARTIAL
Mapping/reason: PARTIAL: shell-songs/test_mountedSongDockAndConfirmationRoundTrips asserts the deleted MIDI exists under .porydaw/trash after QML confirmation; original fixture identity and integrated execution remain unproved.
Source context since previous assertion:


Original expression:
  QVERIFY(QFile::exists(root + QStringLiteral("/.porydaw/trash/%1.mid").arg(label)));

Remaining original source (closing sequence and helper bodies):

      }
      closeWorkspace(window);
  }


Swift assertion predicates
None. No Swift counterpart exists and no report.expect* call covers this file, so there are no S entries.

Shared helper:
None. No Swift fixture file carries expects for this file.
