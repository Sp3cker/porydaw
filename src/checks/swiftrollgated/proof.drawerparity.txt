Assertion correspondence: swiftrollgated/drawerparity.cpp

Reference revision: 084e6049ec7ebff04cf0465ca22b165172774aeb
Original: src/checks/swiftrollgated/drawerparity.cpp
Original SHA-256: 0699e32c19617c3dd1d0ef0033b3cbbbc0c194b84e29fe5bdaf14a35dda74519
Swift counterpart: src/checks/automation/automationcanvasediting.swift
Swift SHA-256: fb7a43e443e50b3ad2a25a7d0028488d05e0a938c4c5dab9d9cbf658eeac1e34
Swift counterpart: src/checks/automation/presentation/tst_automationpresentation.swift
Swift SHA-256: 95c1eb92e5823c7dded6c1058a059e47f398f7a55c5f88f8ba7dc5cadb89cd91
Swift counterpart: src/checks/drawerpresentation/voice_projection.swift
Swift SHA-256: b6310047f752aad72cc0c7932bad47ccd18d3d4c5a10c284cdc3de1efc201c87
Swift counterpart: src/checks/drawerpresentation/voice_interaction.swift
Swift SHA-256: 60df6e0cab39e68765bdb2c4627549f518763bc671c6bf8f12aad62bb2969fb8
Swift counterpart: src/checks/drawerpresentation/drawer_resize.swift
Swift SHA-256: 50f6d45af28663e889bc4116297ad2774f20ad2fc70f80c7180be6d6824ccc28
Swift counterpart: src/checks/rollcheck/EditorGridCameraChecks.swift
Swift SHA-256: 1589104de593a1b0ba252e96a432c89d677f94ae4d97fd59665a33a5e812ce05

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below, including fixture guards and mode gates.
Source context preserves setup, rows, loops, and guards between assertions verbatim.
S identifiers identify exact Swift predicates, listed after the inventory.
MATCHED means a corresponding Swift predicate establishes the same condition;
PARTIAL means a related Swift predicate exists but the stated native sequence or
observation is unproved; GAP means no equivalent Swift assertion is established;
NATIVE means the site observes real input delivery, focus, delegates, cursor state,
or rendered pixels; NATIVE-SETUP means native mode/fixture/window/QML prerequisites.
Unsure between PARTIAL and GAP is recorded as GAP. Bookkeeping must not overclaim.
The three cases cover automation hover raster restoration, voice drag commit/cancel
transactions, and resize-grip keyboard isolation on the final Quick-only surface.

Verification
Command: integrated native swiftrollgated check run
Result: pending integrated run; source hashes pin the tree inspected here.

Execution and ownership
drawerAutomationHoverRaster, drawerVoicePreviewTransaction and drawerGripKeyboardIsolation
are registered through src/checks/CMakeLists.txt and checkcatalog.cpp. The Swift predicates
run from runAutomationPageChecks, runVoiceChangesPageChecks, runEditorDrawerChecks and
runEditorGridCameraChecks; they establish presenter/domain conditions, not native raster,
window focus, QTest delivery or production QML delegate behavior.

Fixture, sequence and row constraints
Automation runs Pan and Tempo rows, captures idle/hover/ring frames, and requires exact
pixel restoration with no revision change. Voice runs release-commits and escape-cancels
rows while holding a real mouse button across the activation threshold. Keyboard routing
selects a real note before Tab traversal so leaked arrows would mutate observable roll state.
Original setup, branch guards and nativefixture.h remain authoritative; this inventory
does not replace those sources.

Original assertion inventory

A001 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:34
Disposition: NATIVE-SETUP
Mapping/reason: Mode gate for the registered native swiftrollgated surface.
Source context since previous assertion:
#include "tst_swiftrollgated.h"

#include "nativefixture.h"
#include "ui/layout.h"

#include <QGuiApplication>
#include <QImage>
#include <QJsonArray>
#include <QJsonDocument>
#include <QJsonObject>
#include <QKeyEvent>
#include <QPointer>
#include <QQuickItem>
#include <QQuickView>
#include <QScopeGuard>
#include <QStyleHints>
#include <QtTest/QTest>

#include <algorithm>
// Ports the sibling automation raster and keyboard contracts onto the real
// Swift session's final Quick-only surface.
using gridcheck::NativeScene;

void SwiftRollGatedTest::drawerAutomationHoverRaster_data()
{
    QTest::addColumn<QString>("parameter");
    QTest::newRow("pan") << QStringLiteral("Pan");
    QTest::newRow("tempo") << QStringLiteral("Tempo");
}

void SwiftRollGatedTest::drawerAutomationHoverRaster()
{
    if (m_mode != QStringLiteral("swiftrollgated"))
Original expression:
QSKIP("drawer raster parity belongs to swiftrollgated");

A002 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:38
Disposition: NATIVE-SETUP
Mapping/reason: Creates the real Swift session, production Quick composition, active window and first rendered drawer frame.
Source context since previous assertion:
    QFETCH(QString, parameter);
    NativeScene scene;
    QString error;
Original expression:
QVERIFY2(scene.open(m_projectRoot, m_songLabel, &error), qPrintable(error));

A003 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:42
Disposition: NATIVE-SETUP
Mapping/reason: Requires the production QML automation page, input surface and exported page model.
Source context since previous assertion:
    auto *page = scene.item("automationPage");
    auto *input = scene.item("automationPlotInput");
    auto *model = scene.model("automationPage");
Original expression:
QVERIFY(page && input && model);

A004 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:50
Disposition: NATIVE-SETUP
Mapping/reason: Requires the requested Pan/Tempo tab delegate in the live selector.
Source context since previous assertion:
    QPointer<QQuickItem> tab;
    for (int index = 0; index < model->property("tabCount").toInt(); ++index) {
        auto *candidate = gridcheck::visualDescendant(
            page, QStringLiteral("automationParameterTab%1").arg(index));
        if (candidate && candidate->property("text").toString() == parameter)
            tab = candidate;
    }
Original expression:
QVERIFY2(tab, qPrintable(QStringLiteral("missing parameter tab: %1").arg(parameter)));

A005 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:55
Disposition: PARTIAL
Mapping/reason: S001 proves the presenter switches to the requested parameter without mutation; it does not prove the live tab delegate receives the pointer and paints checked.
Source context since previous assertion:
    // Focus scrolls the real selector into view; activation still arrives by pointer.
    tab->forceActiveFocus(Qt::TabFocusReason);
    QTest::mouseClick(scene.view, Qt::LeftButton, Qt::NoModifier,
                      tab->mapToScene(QPointF(tab->width() * 0.2, tab->height() * 0.5)).toPoint());
Original expression:
QTRY_VERIFY(tab && tab->property("checked").toBool());

A006 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:57
Disposition: MATCHED
Mapping/reason: S004 proves leaving the automation plot clears the published hover.
Source context since previous assertion:
    scene.move(scene.item("swiftRollInput"), QPointF(20, 20));
Original expression:
QTRY_VERIFY(!model->property("hoverVisible").toBool());

A007 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:59
Disposition: NATIVE-SETUP
Mapping/reason: Requires a live session revision token before native interaction.
Source context since previous assertion:
    const QString before = scene.revision();
Original expression:
QVERIFY(!before.isEmpty());

A008 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:61
Disposition: NATIVE
Mapping/reason: Captures the real idle automation framebuffer.
Source context since previous assertion:
    const QImage idle = scene.capture(input);
Original expression:
QVERIFY(!idle.isNull());

A009 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:67
Disposition: MATCHED
Mapping/reason: S003 proves a background pointer move publishes a non-point hover.
Source context since previous assertion:

    // Background insertion hover must actually paint, remain read-only, and be
    // pixel-stable when the same pointer position is delivered twice.
    const QPointF insertion(input->width() * 0.55, input->height() * 0.5);
    scene.move(input, insertion);
Original expression:
QTRY_VERIFY(model->property("hoverVisible").toBool());

A010 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:69
Disposition: NATIVE
Mapping/reason: Proves the published background hover changes actual raster pixels.
Source context since previous assertion:
    QImage hovered;
Original expression:
QTRY_VERIFY(!(hovered = scene.capture(input)).isNull() && hovered != idle);

A011 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:70
Disposition: MATCHED
Mapping/reason: S013 drives background pointerMove through AutomationPage and proves the SongDocument revision is unchanged. Verification evidence: pending orchestrator run
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A012 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:72
Disposition: NATIVE
Mapping/reason: Proves repeated delivery at the same point is pixel-stable in the real window.
Source context since previous assertion:
    scene.move(input, insertion);
Original expression:
QCOMPARE(scene.capture(input), hovered);

A013 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:75
Disposition: MATCHED
Mapping/reason: S004 proves leaving the automation plot clears the published hover.
Source context since previous assertion:

    scene.move(scene.item("swiftRollInput"), QPointF(20, 20));
Original expression:
QTRY_VERIFY(!model->property("hoverVisible").toBool());

A014 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:76
Disposition: NATIVE
Mapping/reason: Proves the live automation raster returns exactly to its idle frame.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(scene.capture(input), idle);

A015 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:77
Disposition: MATCHED
Mapping/reason: S014 completes the background-hover sequence with pointerLeave and proves the revision remains unchanged. Verification evidence: pending orchestrator run
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A016 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:90
Disposition: NATIVE-SETUP
Mapping/reason: Requires a visible written-node delegate away from clipped plot edges.
Source context since previous assertion:

    // A written node must paint a ring, not merely flip a presenter flag.
    const auto fills = scene.descendants(page, "automationNodeFill");
    QQuickItem *node = nullptr;
    for (auto *fill : fills) {
        const QPointF center = fill->mapToItem(input, fill->boundingRect().center());
        if (fill->isVisible() &&
            input->boundingRect().adjusted(12, 12, -12, -12).contains(center)) {
            node = fill;
            break;
        }
    }
Original expression:
QVERIFY2(node, "the fixture must expose a written node away from plot edges");

A017 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:94
Disposition: NATIVE
Mapping/reason: Requires the production node-hover ring delegate.
Source context since previous assertion:
    const QPoint nodeCenter = node->mapToScene(node->boundingRect().center()).toPoint();
    auto *ring =
        gridcheck::visualDescendant(node->parentItem(), QStringLiteral("automationNodeHover"));
Original expression:
QVERIFY(ring);

A018 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:97
Disposition: NATIVE
Mapping/reason: Captures the ring bounds before hover.
Source context since previous assertion:
    const QRectF ringBounds = ring->mapRectToScene(ring->boundingRect());
    const QImage ringIdle = scene.capture(ringBounds);
Original expression:
QVERIFY(!ringIdle.isNull());

A019 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:101
Disposition: NATIVE
Mapping/reason: Proves real pointer delivery makes a freshly resolved ring delegate visible.
Source context since previous assertion:
    // Hover publication may replace delegates. Keep coordinates, not pointers,
    // across the input boundary, and inspect freshly resolved render items.
    QTest::mouseMove(scene.view, nodeCenter);
Original expression:
QTRY_VERIFY([&] {
        const auto rings = scene.descendants(page, "automationNodeHover");
        return std::any_of(rings.cbegin(), rings.cend(),
                           [](auto *item) { return item->isVisible(); });
    }());

A020 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:107
Disposition: NATIVE
Mapping/reason: Proves the visible ring changes real framebuffer pixels.
Source context since previous assertion:
    QImage ringFrame;
Original expression:
QTRY_VERIFY(!(ringFrame = scene.capture(ringBounds)).isNull() && ringFrame != ringIdle);

A021 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:108
Disposition: MATCHED
Mapping/reason: S015 drives pointerMove over the concrete written pan node and proves the revision is unchanged. Verification evidence: pending orchestrator run
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A022 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:110
Disposition: NATIVE
Mapping/reason: Proves the ring pixels return to idle after pointer leave.
Source context since previous assertion:
    scene.move(scene.item("swiftRollInput"), QPointF(20, 20));
Original expression:
QTRY_COMPARE(scene.capture(ringBounds), ringIdle);

A023 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:111
Disposition: NATIVE
Mapping/reason: Proves the entire automation plot raster returns to idle after pointer leave.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(scene.capture(input), idle);

A024 | SwiftRollGatedTest::drawerAutomationHoverRaster | src/checks/swiftrollgated/drawerparity.cpp:112
Disposition: MATCHED
Mapping/reason: S016 completes the node-hover sequence with pointerLeave and proves the revision remains unchanged. Verification evidence: pending orchestrator run
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A025 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:125
Disposition: NATIVE-SETUP
Mapping/reason: Mode gate for the registered native swiftrollgated surface.
Source context since previous assertion:
}

void SwiftRollGatedTest::drawerVoicePreviewTransaction_data()
{
    QTest::addColumn<bool>("cancel");
    QTest::newRow("release-commits") << false;
    QTest::newRow("escape-cancels") << true;
}

void SwiftRollGatedTest::drawerVoicePreviewTransaction()
{
    if (m_mode != QStringLiteral("swiftrollgated"))
Original expression:
QSKIP("drawer raster parity belongs to swiftrollgated");

A026 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:129
Disposition: NATIVE-SETUP
Mapping/reason: Creates the real Swift session, production Quick composition, active window and first rendered drawer frame.
Source context since previous assertion:
    QFETCH(bool, cancel);
    NativeScene scene;
    QString error;
Original expression:
QVERIFY2(scene.open(m_projectRoot, m_songLabel, &error), qPrintable(error));

A027 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:134
Disposition: NATIVE-SETUP
Mapping/reason: Requires the production voice page, input surface, page model and drag-preview delegate.
Source context since previous assertion:
    auto *input = scene.item("voicePlotInput");
    auto *page = scene.item("voiceChangesPage");
    auto *model = scene.model("voiceChangesPage");
    auto *preview = scene.item("voiceDragPreview");
Original expression:
QVERIFY(input && page && model && preview);

A028 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:137
Disposition: NATIVE-SETUP
Mapping/reason: Requires a published voice-change marker delegate in the retained fixture.
Source context since previous assertion:
    scene.move(scene.item("swiftRollInput"), QPointF(20, 20));
    auto *line = scene.item("voiceChangeMarkerLine");
Original expression:
QVERIFY2(line, "the fixture must publish a voice-change marker");

A029 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:139
Disposition: NATIVE-SETUP
Mapping/reason: Requires that marker to map inside the live voice input bounds.
Source context since previous assertion:
    const QPointF source = line->mapToItem(input, line->boundingRect().center());
Original expression:
QVERIFY(input->boundingRect().contains(source));

A030 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:141
Disposition: MATCHED
Mapping/reason: S005 proves marker hover publishes hoverHintProfile 21.
Source context since previous assertion:
    scene.move(input, source);
Original expression:
QTRY_COMPARE(model->property("hoverHintProfile").toInt(), 21);

A031 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:143
Disposition: NATIVE-SETUP
Mapping/reason: Requires a live session revision token before native interaction.
Source context since previous assertion:
    const QString before = scene.revision();
Original expression:
QVERIFY(!before.isEmpty());

A032 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:145
Disposition: NATIVE
Mapping/reason: Captures the real idle voice framebuffer.
Source context since previous assertion:
    const QImage idle = scene.capture(input);
Original expression:
QVERIFY(!idle.isNull());

A033 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:154
Disposition: PARTIAL
Mapping/reason: S007 proves activated drag motion is preview-only; it does not isolate the earlier native press-only boundary.
Source context since previous assertion:
    const QPoint press = input->mapToScene(source).toPoint();
    QTest::mousePress(scene.view, Qt::LeftButton, Qt::NoModifier, press);
    bool buttonHeld = true;
    const auto release = qScopeGuard([&] {
        if (buttonHeld)
            QTest::mouseRelease(scene.view, Qt::LeftButton);
    });
    QCoreApplication::processEvents();
Original expression:
QCOMPARE(scene.revision(), before);

A034 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:155
Disposition: NATIVE
Mapping/reason: Observes the real preview delegate remains hidden before the activation distance.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(!preview->isVisible());

A035 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:157
Disposition: NATIVE
Mapping/reason: Proves the press-only native framebuffer remains identical to idle.
Source context since previous assertion:
    const QImage pressed = scene.capture(input);
Original expression:
QCOMPARE(pressed, idle);

A036 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:160
Disposition: NATIVE-SETUP
Mapping/reason: Requires the selected drag target to remain inside the live input bounds.
Source context since previous assertion:

    const QPointF target(source.x() + scene.grid->property("beatWidth").toDouble() * 2, source.y());
Original expression:
QVERIFY(input->boundingRect().contains(target));

A037 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:162
Disposition: PARTIAL
Mapping/reason: S006-S007 prove an activated presenter drag and preview; delegate visibility in the live scene remains native.
Source context since previous assertion:
    scene.move(input, target);
Original expression:
QTRY_VERIFY(preview->isVisible());

A038 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:163
Disposition: NATIVE
Mapping/reason: Observes the production QQuickItem cursor shape during native drag.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(input->cursor().shape(), Qt::SizeHorCursor);

A039 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:165
Disposition: NATIVE
Mapping/reason: Proves activated preview changes the actual framebuffer.
Source context since previous assertion:
    QImage draft;
Original expression:
QTRY_VERIFY(!(draft = scene.capture(input)).isNull() && draft != idle);

A040 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:166
Disposition: NATIVE
Mapping/reason: Proves the native draft capture preserves framebuffer dimensions.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(draft.size(), idle.size());

A041 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:167
Disposition: MATCHED
Mapping/reason: S007 proves activated drag motion is preview-only and leaves the document snapshot unchanged.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A042 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:168
Disposition: MATCHED
Mapping/reason: S006 proves a live voice drag publishes interactionActive.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(model->property("interactionActive").toBool());

A043 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:173
Disposition: PARTIAL
Mapping/reason: S009 proves cancellation ends the presenter gesture; hidden preview delegate state remains native.
Source context since previous assertion:

    if (cancel) {
        input->parentItem()->forceActiveFocus(Qt::OtherFocusReason);
        QTest::keyClick(scene.view, Qt::Key_Escape);
Original expression:
QTRY_VERIFY(!preview->isVisible());

A044 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:177
Disposition: MATCHED
Mapping/reason: S009 proves cancellation commits nothing.
Source context since previous assertion:
        QTest::mouseRelease(scene.view, Qt::LeftButton, Qt::NoModifier,
                            input->mapToScene(target).toPoint());
        buttonHeld = false;
Original expression:
QCOMPARE(scene.revision(), before);

A045 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:178
Disposition: PARTIAL
Mapping/reason: S009 proves cancellation restores the complete baseline snapshot, including its undo state; it does not independently assert that the fixture baseline has no undo entry.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(!scene.session->property("canUndo").toBool());

A046 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:183
Disposition: MATCHED
Mapping/reason: S008 proves release commits exactly one document revision.
Source context since previous assertion:
    } else {
        QTest::mouseRelease(scene.view, Qt::LeftButton, Qt::NoModifier,
                            input->mapToScene(target).toPoint());
        buttonHeld = false;
Original expression:
QTRY_VERIFY(scene.revision() != before);

A047 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:184
Disposition: PARTIAL
Mapping/reason: S008 proves release ends the presenter gesture; hidden preview delegate state remains native.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_VERIFY(!preview->isVisible());

A048 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:185
Disposition: MATCHED
Mapping/reason: S008 proves the committed move records an undo entry.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_VERIFY(scene.session->property("canUndo").toBool());

A049 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:187
Disposition: NATIVE
Mapping/reason: Exercises the real session QObject requestUndo bridge.
Source context since previous assertion:
        const QString committed = scene.revision();
Original expression:
QVERIFY(QMetaObject::invokeMethod(scene.session, "requestUndo"));

A050 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:190
Disposition: PARTIAL
Mapping/reason: S008-S009 prove the document transition and restoration semantics, but not this asynchronous native revision-token completion boundary.
Source context since previous assertion:
        // canUndo clears before asynchronous replay starts; the published
        // revision, not the disabled action, establishes completion.
Original expression:
QTRY_VERIFY(scene.revision() != committed);

A051 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:191
Disposition: GAP
Mapping/reason: No identified Swift predicate proves canUndo is false after this exact native one-edit undo sequence.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_VERIFY(!scene.session->property("canUndo").toBool());

A052 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:193
Disposition: MATCHED
Mapping/reason: S008 and S009 prove release and cancellation both end interactionActive.
Source context since previous assertion:
    }
Original expression:
QTRY_VERIFY(!model->property("interactionActive").toBool());

A053 | SwiftRollGatedTest::drawerVoicePreviewTransaction | src/checks/swiftrollgated/drawerparity.cpp:195
Disposition: NATIVE
Mapping/reason: Proves the live voice framebuffer returns exactly to idle after commit/cancel completion.
Source context since previous assertion:
    scene.move(input, source);
Original expression:
QTRY_COMPARE(scene.capture(input), idle);

A054 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:201
Disposition: NATIVE-SETUP
Mapping/reason: Mode gate for the registered native swiftrollgated surface.
Source context since previous assertion:
}

void SwiftRollGatedTest::drawerGripKeyboardIsolation()
{
    if (m_mode != QStringLiteral("swiftrollgated"))
Original expression:
QSKIP("drawer window keyboard routing belongs to swiftrollgated");

A055 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:204
Disposition: NATIVE-SETUP
Mapping/reason: Creates the real Swift session, production Quick composition, active window and first rendered drawer frame.
Source context since previous assertion:
    NativeScene scene;
    QString error;
Original expression:
QVERIFY2(scene.open(m_projectRoot, m_songLabel, &error), qPrintable(error));

A056 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:207
Disposition: NATIVE-SETUP
Mapping/reason: Requires the production automation resize grip and plot delegates.
Source context since previous assertion:
    auto *grip = scene.item("drawerHandle_automation");
    auto *plot = scene.item("automationPlot");
Original expression:
QVERIFY(grip && plot);

A057 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:212
Disposition: NATIVE-SETUP
Mapping/reason: Requires the live noteSummary payload to decode as an array.
Source context since previous assertion:
    // Without selected notes, leaked arrows could be harmless and the check
    // would miss the regression. Select a real visible note before traversal.
    const auto notesBefore =
        QJsonDocument::fromJson(scene.grid->property("noteSummary").toString().toUtf8());
Original expression:
QVERIFY(notesBefore.isArray());

A058 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:214
Disposition: NATIVE-SETUP
Mapping/reason: Requires the live roll input delegate.
Source context since previous assertion:
    auto *roll = scene.item("swiftRollInput");
Original expression:
QVERIFY(roll);

A059 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:226
Disposition: NATIVE-SETUP
Mapping/reason: Requires a visible note delegate for the arrow-routing negative control.
Source context since previous assertion:
    QQuickItem *note = nullptr;
    for (const auto value : notesBefore.array()) {
        const auto id = quint64(value.toObject().value(QStringLiteral("id")).toDouble());
        auto *candidate =
            gridcheck::visualDescendant(scene.root, QStringLiteral("gridNote_%1").arg(id));
        if (candidate && roll->boundingRect().contains(
                             candidate->mapToItem(roll, candidate->boundingRect().center()))) {
            note = candidate;
            break;
        }
    }
Original expression:
QVERIFY2(note, "the fixture must expose a note for the routing negative control");

A060 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:229
Disposition: PARTIAL
Mapping/reason: S012 proves projected-note pointer selection; this site additionally depends on real QTest delivery and delegate lookup.
Source context since previous assertion:
    QTest::mouseClick(scene.view, Qt::LeftButton, Qt::NoModifier,
                      note->mapToScene(note->boundingRect().center()).toPoint());
Original expression:
QTRY_VERIFY([&] {
        const auto selected =
            QJsonDocument::fromJson(scene.grid->property("noteSummary").toString().toUtf8())
                .array();
        return std::any_of(selected.cbegin(), selected.cend(), [](const auto &entry) {
            return entry.toObject().value(QStringLiteral("selected")).toBool();
        });
    }());

A061 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:242
Disposition: NATIVE
Mapping/reason: Proves the production automation plot accepts active focus.
Source context since previous assertion:
    const auto previous = QGuiApplication::styleHints()->tabFocusBehavior();
    const auto restore =
        qScopeGuard([&] { QGuiApplication::styleHints()->setTabFocusBehavior(previous); });
    QGuiApplication::styleHints()->setTabFocusBehavior(Qt::TabFocusAllControls);
    plot->forceActiveFocus(Qt::OtherFocusReason);
Original expression:
QTRY_VERIFY(plot->hasActiveFocus());

A062 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:243
Disposition: NATIVE
Mapping/reason: Proves the focused native Quick window owns application focus.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(QGuiApplication::focusWindow(), scene.view);

A063 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:251
Disposition: NATIVE
Mapping/reason: Proves real Tab traversal reaches the production drawer grip.
Source context since previous assertion:
    QStringList focusPath;
    for (int count = 0; count < 80 && !grip->hasActiveFocus(); ++count) {
        QTest::keyClick(scene.view, Qt::Key_Tab);
        QCoreApplication::processEvents();
        auto *focus = scene.view->activeFocusItem();
        focusPath.append(focus ? focus->objectName() : QStringLiteral("<none>"));
    }
Original expression:
QVERIFY2(grip->hasActiveFocus(), qPrintable(QStringLiteral("Tab traversal missed grip: %1")
                                                    .arg(focusPath.join(QStringLiteral(" -> ")))));

A064 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:264
Disposition: NATIVE
Mapping/reason: Proves the production grip accepts a real Up key event.
Source context since previous assertion:
    const QString before = scene.revision();
    const QString notes = scene.grid->property("noteSummary").toString();
    const qreal height = plot->height();
    const int step = layout::space(layout::Space::Two);
    const auto arrow = [&](Qt::Key code) {
        QKeyEvent press(QEvent::KeyPress, code, Qt::NoModifier);
        QCoreApplication::sendEvent(scene.view, &press);
        QKeyEvent release(QEvent::KeyRelease, code, Qt::NoModifier);
        QCoreApplication::sendEvent(scene.view, &release);
        return press.isAccepted();
    };
Original expression:
QVERIFY(arrow(Qt::Key_Up));

A065 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:265
Disposition: PARTIAL
Mapping/reason: S010 proves one positive resize-handle step changes automation height by resizeStep; this site additionally proves the QML plot follows the native key route.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(plot->height(), height + step);

A066 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:266
Disposition: NATIVE
Mapping/reason: Proves the production grip accepts a real Down key event.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(arrow(Qt::Key_Down));

A067 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:267
Disposition: GAP
Mapping/reason: No identified Swift predicate proves a positive step followed by a negative step returns exactly to the original height.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QTRY_COMPARE(plot->height(), height);

A068 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:268
Disposition: NATIVE
Mapping/reason: Proves the production grip consumes Left rather than leaking it to the roll.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(arrow(Qt::Key_Left));

A069 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:269
Disposition: NATIVE
Mapping/reason: Proves the production grip consumes Right rather than leaking it to the roll.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QVERIFY(arrow(Qt::Key_Right));

A070 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:270
Disposition: GAP
Mapping/reason: No identified Swift predicate proves Left and Right leave drawer height unchanged.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(plot->height(), height);

A071 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:271
Disposition: GAP
Mapping/reason: No identified Swift predicate proves the complete native grip-key sequence leaves the song revision unchanged.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.revision(), before);

A072 | SwiftRollGatedTest::drawerGripKeyboardIsolation | src/checks/swiftrollgated/drawerparity.cpp:272
Disposition: GAP
Mapping/reason: No identified Swift predicate proves selected noteSummary is unchanged by the complete native grip-key sequence.
Source context since previous assertion:
(adjacent assertion sites; no intervening source lines.)
Original expression:
QCOMPARE(scene.grid->property("noteSummary").toString(), notes);

Remaining original source (closing sequence and helper bodies):
}


Swift assertion predicates

S001 | drawerAutomationParameterSwitchAndGhosts | src/checks/automation/presentation/tst_automationpresentation.swift:19
    report.expect(fixture.page.activateParameter(index: panIndex), cppID: drawerAutomationSwitchID,
                  message: "a parameter switch is accepted")
    report.expectEqual(fixture.panLane, fixture.page.activeParameter, cppID: drawerAutomationSwitchID,
                       what: "the switched parameter becomes active")
    report.expectEqual(panIndex, fixture.page.activeParameterIndex, cppID: drawerAutomationSwitchID,
                       what: "the catalog index follows the switch")
    report.expectEqual(before, fixture.snapshot, cppID: drawerAutomationSwitchID,
                       what: "switching a parameter mutates nothing")

S002 | drawerAutomationContextAndPublicationDiagnostics | src/checks/automation/automationcanvasediting.swift:190
    _ = fixture.page.pointerMove(x: fixture.x(96), y: fixture.y(fixture.volumeLane, 64),
                                 buttons: 0)
    report.expect(fixture.page.hover?.hasPoint == true, cppID: drawerAutomationContextID,
                  message: "a hover on a node publishes the node")
    report.expectEqual("64", fixture.page.hover?.text ?? "", cppID: drawerAutomationContextID,
                       what: "a node hover reads out the node's value")
    report.expectEqual(15, fixture.page.hoverHintProfile, cppID: drawerAutomationContextID,
                       what: "a written node advertises node movement")

S003 | drawerAutomationContextAndPublicationDiagnostics | src/checks/automation/automationcanvasediting.swift:198
    _ = fixture.page.pointerMove(x: fixture.x(150), y: fixture.y(fixture.volumeLane, 20),
                                 buttons: 0)
    report.expect(fixture.page.hover?.hasPoint == false, cppID: drawerAutomationContextID,
                  message: "a hover on the background publishes the tick")
    report.expectEqual("64", fixture.page.hover?.text ?? "", cppID: drawerAutomationContextID,
                       what: "a background hover reads the value the lane holds there")
    report.expect(fixture.page.hoverBuildCount > hoverBuilds, cppID: drawerAutomationContextID,
                  message: "the hover publications are counted")
    report.expectEqual(17, fixture.page.hoverHintProfile, cppID: drawerAutomationContextID,
                       what: "the background advertises sweep and ramp input")

S004 | drawerAutomationContextAndPublicationDiagnostics | src/checks/automation/automationcanvasediting.swift:214
    fixture.page.pointerLeave()
    report.expect(fixture.page.hover == nil, cppID: drawerAutomationContextID,
                  message: "leaving the plot clears the hover")

S005 | drawerVoiceProjectionAndHover | src/checks/drawerpresentation/voice_projection.swift:79
    _ = page.pointerMove(x: fixture.markerX(48), y: 10, buttons: 0)
    report.expect(!page.hoverVisible, cppID: drawerVoiceProjectionID,
                  message: "a marker hover publishes no background label")
    report.expectEqual(48, Tick(page.hoverTick), cppID: drawerVoiceProjectionID,
                       what: "the marker hover publishes its own tick")
    report.expectEqual(21, page.hoverHintProfile, cppID: drawerVoiceProjectionID,
                       what: "a marker hover advertises fine marker movement")

S006 | drawerVoiceMarkerDragTransactions | src/checks/drawerpresentation/voice_interaction.swift:53
    _ = page.pointerPress(x: startX, y: 10, surface: 1, button: 1, modifiers: 0)
    report.expect(page.hasGesture, cppID: drawerVoiceMoveID, message: "the press owns a gesture")
    report.expectEqual(48, page.frozenOccurrence?.tick, cppID: drawerVoiceMoveID,
                       what: "the frozen occurrence is the pressed marker")
    report.expect(page.interactionActive, cppID: drawerVoiceMoveID,
                  message: "the live gesture reports an active interaction")
    _ = page.pointerMove(x: startX + 60, y: 10, buttons: 1)
    report.expect(page.dragActive, cppID: drawerVoiceMoveID,
                  message: "the drag activates past its activation distance")

S007 | drawerVoiceMarkerDragTransactions | src/checks/drawerpresentation/voice_interaction.swift:62
    guard let preview = page.dragPreviewTick else {
        report.fail(drawerVoiceMoveID, "the activated drag published no preview tick")
        return
    }
    report.expect(preview != 48, cppID: drawerVoiceMoveID,
                  message: "the preview tick drafts away from the frozen tick")
    report.expectEqual(baseline, fixture.snapshot, cppID: drawerVoiceMoveID,
                       what: "motion is preview only and mutates nothing")
    report.expectEqual(preview, page.markerTicks[1], cppID: drawerVoiceMoveID,
                       what: "the projection draws the marker at the preview tick")

S008 | drawerVoiceMarkerDragTransactions | src/checks/drawerpresentation/voice_interaction.swift:72
    _ = page.pointerRelease(x: startX + 60, y: 10, button: 1)
    report.expect(!page.hasGesture && !page.interactionActive, cppID: drawerVoiceMoveID,
                  message: "the release ends the gesture and its interaction")
    report.expectEqual(preview, fixture.lanePoints()[1].tick, cppID: drawerVoiceMoveID,
                       what: "the release commits the preview tick")
    report.expectEqual(baseline.revision + 1, fixture.snapshot.revision, cppID: drawerVoiceMoveID,
                       what: "the move is one revision")
    report.expect(fixture.snapshot.canUndo, cppID: drawerVoiceMoveID,
                  message: "the move records one history entry")

S009 | drawerVoiceCancellationPaths | src/checks/drawerpresentation/voice_interaction.swift:150
    // A live drag cancelled mid-motion restores presentation and commits nothing.
    let startX = fixture.markerX(48)
    _ = page.pointerPress(x: startX, y: 10, surface: 1, button: 1, modifiers: 0)
    _ = page.pointerMove(x: startX + 60, y: 10, buttons: 1)
    report.expect(page.dragActive, cppID: drawerVoiceCancellationID, message: "the drag is live")
    page.cancelSectionInteraction()
    report.expect(!page.hasGesture && !page.interactionActive, cppID: drawerVoiceCancellationID,
                  message: "cancellation ends the live drag")
    report.expectEqual([0, 48, 120], page.markerTicks, cppID: drawerVoiceCancellationID,
                       what: "the cancelled drag restores the projection from the document")
    report.expectEqual(baseline, fixture.snapshot, cppID: drawerVoiceCancellationID,
                       what: "the cancelled drag commits nothing")

S010 | drawerLayoutCheckDrawerResizeClampsAndSessions | src/checks/drawerpresentation/drawer_resize.swift:113
    let stepped = drawerLayoutMakeStoredDrawerHarness().harness
    let step = stepped.apply {
        $0.adjustResizeHandle(.automation, direction: 1, pages: $1)
    }
    report.expect(step.published &&
                  stepped.layout.storedBodyHeight(.automation) == 100 + drawerLayoutDrawerResizeStep &&
                  stepped.layout.resizeKind == nil, cppID: drawerLayoutResizeID,
                  message: "a handle step applies one resize step through the resize path and ends its own session")
    drawerLayoutExpectDrawerPreference(report, cppID: drawerLayoutResizeID, step, .automation, visible: true,
                           storedBodyHeight: 107,
                           message: "a handle step records the changed section preference")

S011 | drawerLayoutCheckDrawerResizeClampsAndSessions | src/checks/drawerpresentation/drawer_resize.swift:125
    let flooredStep = drawerLayoutMakeStoredDrawerHarness().harness
    flooredStep.apply {
        $0.setSectionBodyHeight(
            .automation, height: drawerLayoutDrawerMinimumBody, pages: $1)
    }
    let floorStep = flooredStep.apply {
        $0.adjustResizeHandle(.automation, direction: -1, pages: $1)
    }
    report.expect(!floorStep.published && floorStep.isEmpty &&
                  flooredStep.layout.storedBodyHeight(.automation) == drawerLayoutDrawerMinimumBody,
                  cppID: drawerLayoutResizeID,
                  message: "a handle step that resolves to the drag start publishes nothing")

S012 | checkProjection | src/checks/rollcheck/EditorGridCameraChecks.swift:326
    grid.beginPointer(
        x: originalRect.x + originalRect.width / 2,
        y: originalRect.y + originalRect.height / 2, modifiers: 0)
    report.expect(
        session.selectedNotes.contains(originalNote.id),
        cppID: projectionID, message: "beginPointer selects the note under the projected rectangle")

Shared helper predicates are included only where an inventory entry cites them.

S013 | drawerSwiftRollGatedPortableGaps | src/checks/swiftrollgated/gated_gap.swift:154
Swift expression:
  report.expectEqual(revision, automation.document.revision, cppID: drawerParityGapID,
                     what: "A011 background hover leaves revision unchanged")

S014 | drawerSwiftRollGatedPortableGaps | src/checks/swiftrollgated/gated_gap.swift:157
Swift expression:
  report.expectEqual(revision, automation.document.revision, cppID: drawerParityGapID,
                     what: "A015 hover-away leaves revision unchanged")

S015 | drawerSwiftRollGatedPortableGaps | src/checks/swiftrollgated/gated_gap.swift:162
Swift expression:
  report.expectEqual(revision, automation.document.revision, cppID: drawerParityGapID,
                     what: "A021 written-node hover leaves revision unchanged")

S016 | drawerSwiftRollGatedPortableGaps | src/checks/swiftrollgated/gated_gap.swift:165
Swift expression:
  report.expectEqual(revision, automation.document.revision, cppID: drawerParityGapID,
                     what: "A024 complete node-hover sequence leaves revision unchanged")
