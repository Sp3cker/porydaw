Assertion correspondence: host/tst_hostadapter.cpp

Reference revision: c17d966fbf56b1cccb2c1112b7eef84bec1dc7ac
Original: src/checks/host/tst_hostadapter.cpp
Original SHA-256: 6e63b0766e5899601bb5a68b18a516715fe0669549a1929c4bc43ddb550c1bbe
Swift counterpart: none identified
Swift SHA-256: n/a
Deleted in: 31ea635fc360f9a248be427dadea12f5948757bb T2: boundary sweep — delete automation dead-code twins + strip shared files

Status and evidence
The C++ file above was deleted; this inventory audits its parent-revision content (pinned by Reference revision and SHA-256) so no assertion is dropped during the Swift check migration. This is an assertion-site inventory, not a count-based equivalence claim. Every original macro/failure site appears below, including fixture guards. Source context preserves setup, rows, loops, and guards between assertions verbatim. No Swift counterpart was identified for this deleted host suite. MATCHED identifies equivalent model predicates; execution must still be verified. PARTIAL identifies related Swift predicates with an explicit unproved condition. NATIVE identifies retained native-window/rendering or native setup obligations; it does not establish coverage of any converted window. GAP identifies an original predicate without a replacement assertion. Nothing here is marked MATCHED or PARTIAL; unsure between PARTIAL and GAP was resolved to GAP. Native passes do not prove any Swift replacement.

Dispositions and no-ingress evidence
NATIVE is no longer used: every site that observed a native-window, embedding, delivery, framebuffer, settings, popup, or harness obligation is RETIRED-REPRESENTATION, because the mechanism that carried it is gone (this C++ file was deleted at 31ea635f, appears in no CMake target, and checkcatalog.cpp registers no host entry; re-verified 2026-09-24). GAP sites are behavior assertions whose Swift owners exist but whose original predicates have no executed Swift equivalent; each mapping names the owner verified under src/swift/app. The Reference revision and Original SHA-256 below were re-verified against each other (git show <revision>:<path> | shasum -a 256) before any retirement claim.

Verification
Command: deno task proof check
Result: PASS - structure, entry IDs, metadata, and present tallies only; not source freshness, execution, or parity. No integrated verification result was provided for this snapshot, and no lane executes this deleted harness, so execution evidence is unknown.

Execution ownership
No Swift entry point executes the deleted host checks; no Swift fixture is paired.

Fixture, sequence and row constraints
Every case opens with the host.prepare guard; band-geometry cases expand one QCOMPARE site per published band row (rect, plot, visibility entries). The four physicalInputsMatchCanonical calls expand input-parity obligations across pointer surfaces. Velocity/roll plot assertions expand over the track-headers, keyboard, and drawer-body geometry rows.
Original setup, branch guards and helper implementations at the reference revision remain authoritative; this inventory does not replace those sources.

Tally: MATCHED 0, PARTIAL 0, GAP 91, NATIVE 0, RETIRED-REPRESENTATION 104.

Original assertion inventory and source sequence

A001 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:63
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  #include "checks/fwd.hpp"
  #include "checks/host/hosttestsupport.h"

  #include <memory>
  #include <optional>

  #include <QEnterEvent>
  #include <QEvent>
  #include <QFocusEvent>
  #include <QImage>
  #include <QKeyEvent>
  #include <QPalette>
  #include <QPoint>
  #include <QQuickItem>
  #include <QQuickWindow>
  #include <QTimer>
  #include <QtTest>
  #include <algorithm>
  #include <array>
  #include <cmath>

  #include "checks/support/asyncwait.h"
  #include "checks/support/eventsynth.h"
  #include "checks/support/quickframebuffer.h"
  #include "checks/support/songfixture.h"
  #include "checks/support/timelinequickcheck.h"
  #include "ui/editordrawer/automationcanvas.h"
  #include "ui/editordrawer/automationpage.h"
  #include "ui/editordrawer/drawerchrome.h"
  #include "ui/editordrawer/editordrawer.h"
  #include "ui/editordrawer/velocityarea/velocityarea.h"
  #include "ui/editordrawer/voicechangearea/voicechangearea.h"
  #include "ui/layout.h"
  #include "ui/songview/otherstrip.h"
  #include "ui/songview/pianoroll.h"
  #include "ui/songview/quick/quickpopupsession.h"
  #include "ui/songview/quick/timelineinputitem.h"
  #include "ui/songview/quick/timelinequickview.h"
  #include "ui/songview/timelinebandlayout.h"
  #include "ui/songview/timeruler.h"
  #include "ui/songview/trackheadermodel.h"

  #include "ui/editorviewstate.h"
  namespace checks::host {
  namespace {

  class HostAdapterTest final : public QObject
  {
      Q_OBJECT
      Q_DISABLE_COPY_MOVE(HostAdapterTest)

    public:
      HostAdapterTest(QString projectRoot, QString songLabel)
          : m_projectRoot(std::move(projectRoot))
          , m_songLabel(std::move(songLabel))
      {}

    private slots:
      void syntheticFixtureConstructsHost()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A002 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:64
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old host.view().timeline() identity accessor (host-to-timeline seam); uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift DocumentSession/PlaybackTimeline owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(host.view().timeline())

A003 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:65
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SongView editorDrawer() QWidget accessor; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EditorDrawerPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(host.view().editorDrawer())

A004 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:66
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old editorDrawer()->velocityArea() QWidget handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(host.view().editorDrawer()->velocityArea())

A005 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:67
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quickView() TimelineQuickView handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(host.view().quickView())

A006 | syntheticFixtureConstructsHost | src/checks/host/tst_hostadapter.cpp:68
Disposition: GAP
Mapping/reason: Swift DocumentSession/SongDocument owns the document note projection for track 0; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(host.document().notesForTrack(0).size(), size_t{2})

A007 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:75
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old LoadedSong staging fixture; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession.openProject/DocumentWorkspace owns the behavior).
Source context since previous assertion:
  ;
      }

      void route101FixtureAttachesHostedVelocityMarker()
      {
          QString error;
          auto loaded = checks::LoadedSong::load(m_projectRoot, m_songLabel, error);

Original expression:
  QVERIFY2(loaded, qPrintable(error))

A008 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:77
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SongViewRig host quick-canvas rig construction; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          auto rig = checks::SongViewRig::create(std::move(loaded), 44100.0, error);

Original expression:
  QVERIFY2(rig, qPrintable(error))

A009 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:86
Disposition: GAP
Mapping/reason: Swift DocumentSession/SongDocument owns fixture note discovery from the loaded document; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          SongDocument &document = rig->document();
          const MidiTimeline &timeline = rig->timeline();
          std::optional<DocNote> fixtureNote;
          for (int track = 0; track < document.engineTrackCount() && !fixtureNote; ++track) {
              const std::vector<DocNote> notes = document.notesForTrack(track);
              if (!notes.empty())
                  fixtureNote = notes.front();
          }

Original expression:
  QVERIFY(fixtureNote.has_value())

A010 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:87
Disposition: GAP
Mapping/reason: Swift PlaybackTimeline/DocumentSession owns the timeline note-on event published for the fixture note; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(std::any_of(timeline.events.cbegin(), timeline.events.cend(),
                              [id = fixtureNote->noteId](const TimelineEvent &event) {
                                  return event.type == 0x9 && event.noteId == id;
                              }))

A011 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:92
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old showQuickViewport() native viewport staging; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = rig->view();

Original expression:
  QVERIFY(checks::support::showQuickViewport(view, QSize(720, 520)))

A012 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:100
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old editorDrawer()->velocityArea() QWidget handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage owns the behavior).
Source context since previous assertion:
  ;
          view.selectTrack(fixtureNote->engineTrack);
          view.selectionModel().setNoteSelection({fixtureNote->noteId});
          view.setEditCursorTick(fixtureNote->tick);
          view.setDrawerActivePage(EditorDrawerPage::Velocity);
          view.setDrawerSectionVisible(EditorDrawerPage::Velocity, true);
          settle();
          auto *area = view.editorDrawer()->velocityArea();

Original expression:
  QVERIFY(area)

A013 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:101
Disposition: GAP
Mapping/reason: Swift DocumentSession/SongDocument owns the loaded song label identity; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(document.label(), m_songLabel)

A014 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:102
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old view.timeline() address identity (host-to-timeline seam); uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift DocumentSession/PlaybackTimeline owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.timeline(), &timeline)

A015 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:103
Disposition: GAP
Mapping/reason: Swift DocumentSession selected track owns the primary track selection after selectTrack; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.selectionModel().primaryTrack(), fixtureNote->engineTrack)

A016 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:104
Disposition: GAP
Mapping/reason: Swift DocumentSession note selection owns the note selection set after setNoteSelection; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.selectionModel().noteSelection(), (std::vector<NoteId>{fixtureNote->noteId}))

A017 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:105
Disposition: GAP
Mapping/reason: Swift VelocityPage/VelocityAxisModel owns the velocity axis marker count for the selected note; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(area->axis().markerCount(), 1)

A018 | route101FixtureAttachesHostedVelocityMarker | src/checks/host/tst_hostadapter.cpp:106
Disposition: GAP
Mapping/reason: Swift VelocityPage/VelocityAxisModel owns the velocity axis marker value for the selected note; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(area->axis().markers().front().velocity, fixtureNote->velocity)

A019 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:113
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void canonicalGeometryProjectsToQuick()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A020 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:116
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A021 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:118
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A022 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:121
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old track-headers quick item discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift TrackHeadersPresenter owns the behavior).
Source context since previous assertion:
  ;
          auto *headers = view.findChild<songview::TrackHeaderModel *>(
              QStringLiteral("trackHeaderModel"), Qt::FindDirectChildrenOnly);

Original expression:
  QVERIFY(headers)

A023 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:124
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old embedded QQuickWindow discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          QString captureError;
          QQuickWindow *const window = quick->quickWindow();

Original expression:
  QVERIFY(window)

A024 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:127
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old published-frame QImage capture validity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          const QImage publishedFrame =
              checks::support::captureQuickBand(view, QRect(QPoint{}, window->size()), &captureError);

Original expression:
  QVERIFY2(!publishedFrame.isNull(), qPrintable(captureError))

A025 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:130
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter/TrackHeadersGeometry owns the track-headers band geometry presence; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const songview::TimelineBandLayout &layout = view.timelineBandLayout();
          const auto trackHeaders = layout.geometry(songview::TimelineBand::TrackHeaders);

Original expression:
  QVERIFY(trackHeaders.has_value())

A026 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:131
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter/TrackHeadersGeometry owns the track-headers plot-rect emptiness; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(trackHeaders->plotRect.isEmpty())

A027 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:136
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the band layout geometry presence across the ruler/roll/other-events/velocity rows; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          for (songview::TimelineBand band :
               {songview::TimelineBand::Ruler, songview::TimelineBand::Roll,
                songview::TimelineBand::OtherEvents, songview::TimelineBand::Velocity}) {
              const auto geometry = layout.geometry(band);

Original expression:
  QVERIFY(geometry.has_value())

A028 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:137
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the band layout rect non-emptiness; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!geometry->rect.isEmpty())

A029 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:138
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the band layout plot-rect non-emptiness; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!geometry->plotRect.isEmpty())

A030 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:139
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the band plot origin x aligned to the timeline split; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(geometry->plotRect.x(), view.timelineSplitX())

A031 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:140
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the band plot right edge aligned to the band rect; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(geometry->plotRect.right(), geometry->rect.right())

A032 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:141
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter/GridGeometry owns the track-headers/band plot non-intersection; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!trackHeaders->rect.intersects(geometry->plotRect))

A033 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:169
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band rect published to the QML root property per band entry; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          }
          struct BandProperties {
              songview::TimelineBand band;
              const char *rect;
              const char *plot;
              const char *visible;
          };
          constexpr std::array<BandProperties, 7> properties{{
              {songview::TimelineBand::Ruler, "rulerBandRect", "rulerBandPlotRect",
               "rulerBandVisible"},
              {songview::TimelineBand::Roll, "rollBandRect", "rollBandPlotRect", "rollBandVisible"},
              {songview::TimelineBand::OtherEvents, "otherEventsBandRect", "otherEventsBandPlotRect",
               "otherEventsBandVisible"},
              {songview::TimelineBand::Automation, "automationBandRect", "automationBandPlotRect",
               "automationBandVisible"},
              {songview::TimelineBand::Velocity, "velocityBandRect", "velocityBandPlotRect",
               "velocityBandVisible"},
              {songview::TimelineBand::VoiceChanges, "voiceChangesBandRect",
               "voiceChangesBandPlotRect", "voiceChangesBandVisible"},
              {songview::TimelineBand::TrackHeaders, "trackHeadersBandRect",
               "trackHeadersBandPlotRect", "trackHeadersBandVisible"},
          }};
          for (const BandProperties &entry : properties) {
              const auto geometry = layout.geometry(entry.band);
              const QRectF expectedRect = geometry ? QRectF(geometry->rect) : QRectF{};
              const QRectF expectedPlot =
                  geometry && !geometry->plotRect.isEmpty() ? QRectF(geometry->plotRect) : QRectF{};

Original expression:
  QCOMPARE(root->property(entry.rect).toRectF(), expectedRect)

A034 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:170
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band plot published to the QML root property per band entry; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(root->property(entry.plot).toRectF(), expectedPlot)

A035 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:171
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band visibility published to the QML root property per band entry; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(root->property(entry.visible).toBool(), geometry.has_value())

A036 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:173
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old physical quick-input parity for the ruler band; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          }

Original expression:
  QVERIFY(checks::support::physicalInputsMatchCanonical(
              layout, *root, songview::TimelineBand::Ruler, QStringLiteral("timelineRulerInput"),
              QStringLiteral("timelineRulerGutterInput")))

A037 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:176
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old physical quick-input parity for the roll band; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(checks::support::physicalInputsMatchCanonical(
              layout, *root, songview::TimelineBand::Roll, QStringLiteral("timelineRollInput"),
              QStringLiteral("timelineRollGutterInput")))

A038 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:179
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old physical quick-input parity for the other-events band; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(checks::support::physicalInputsMatchCanonical(
              layout, *root, songview::TimelineBand::OtherEvents,
              QStringLiteral("timelineOtherEventsInput"),
              QStringLiteral("timelineOtherEventsGutterInput")))

A039 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:183
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old physical quick-input parity for the velocity band; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(checks::support::physicalInputsMatchCanonical(
              layout, *root, songview::TimelineBand::Velocity,
              QStringLiteral("timelineVelocityInput"),
              QStringLiteral("timelineVelocityGutterInput")))

A040 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:189
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const auto velocity = layout.geometry(songview::TimelineBand::Velocity);
          const auto roll = layout.geometry(songview::TimelineBand::Roll);

Original expression:
  QVERIFY(velocity.has_value())

A041 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:190
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the roll band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(roll.has_value())

A042 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:191
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity band left edge aligned to the track-headers width; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(velocity->rect.x(), trackHeaders->rect.width())

A043 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:192
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity plot origin x aligned to the timeline split; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(velocity->plotRect.x(), view.timelineSplitX())

A044 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:193
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the roll plot origin x aligned to the timeline split; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(roll->plotRect.x(), view.timelineSplitX())

A045 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:194
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity plot offset equal to the piano-keyboard width; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(velocity->plotRect.x() - velocity->rect.x(), view.pianoKeyboardWidth())

A046 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:195
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the roll plot offset equal to the piano-keyboard width; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(roll->plotRect.x() - roll->rect.x(), view.pianoKeyboardWidth())

A047 | canonicalGeometryProjectsToQuick | src/checks/host/tst_hostadapter.cpp:196
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer body rect matched to the velocity band rect; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(view.editorDrawer()->bodyRect(EditorDrawerPage::Velocity),
                   std::optional<QRect>{velocity->rect})

A048 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:204
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void quickInputsOwnTheirInteractions()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A049 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:207
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A050 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:209
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A051 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:225
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick band input-item identity across six bands; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          auto input = [root](const char *name) {
              return root->findChild<songview::TimelineInputItem *>(QString::fromLatin1(name));
          };
          auto *ruler = input("timelineRulerInput");
          auto *rulerGutter = input("timelineRulerGutterInput");
          auto *roll = input("timelineRollInput");
          auto *rollGutter = input("timelineRollGutterInput");
          auto *other = input("timelineOtherEventsInput");
          auto *otherGutter = input("timelineOtherEventsGutterInput");
          auto *automation = input("timelineAutomationInput");
          auto *automationGutter = input("timelineAutomationGutterInput");
          auto *velocity = input("timelineVelocityInput");
          auto *velocityGutter = input("timelineVelocityGutterInput");
          auto *voice = input("timelineVoiceChangesInput");
          auto *voiceGutter = input("timelineVoiceChangesGutterInput");

Original expression:
  QVERIFY(ruler && rulerGutter && roll && rollGutter && other && otherGutter && automation &&
                  automationGutter && velocity && velocityGutter && voice && voiceGutter)

A052 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:227
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old ruler plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift PianoGrid/GridGesture owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(ruler->interaction(), rulerGutter->interaction())

A053 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:228
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old dynamic_cast<TimeRuler*> host interaction seam; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift PianoGrid/GridGesture owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(dynamic_cast<songview::TimeRuler *>(ruler->interaction()))

A054 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:229
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old roll plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift PianoGrid owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(roll->interaction(), rollGutter->interaction())

A055 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:230
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old dynamic_cast<PianoRoll*> host interaction seam; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift PianoGrid owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(dynamic_cast<songview::PianoRoll *>(roll->interaction()))

A056 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:231
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old other-events plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(other->interaction(), otherGutter->interaction())

A057 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:232
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old dynamic_cast<OtherStrip*> host interaction seam; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(dynamic_cast<songview::OtherStrip *>(other->interaction()))

A058 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:233
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(automation->interaction(), automationGutter->interaction())

A059 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:234
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation interaction bound to the host automation canvas; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(automation->interaction(), view.editorDrawer()->automationPage()->canvas())

A060 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:235
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old velocity plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(velocity->interaction(), velocityGutter->interaction())

A061 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:236
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old velocity interaction bound to the host velocityArea widget; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(velocity->interaction(), view.editorDrawer()->velocityArea())

A062 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:237
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old voice plot/gutter interaction identity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VoiceChangesPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(voice->interaction(), voiceGutter->interaction())

A063 | quickInputsOwnTheirInteractions | src/checks/host/tst_hostadapter.cpp:238
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old voice interaction bound to the host voiceChangeArea widget; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VoiceChangesPage owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(voice->interaction(), view.editorDrawer()->voiceChangeArea())

A064 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:245
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void drawerChromeAndQuickHeadersFollowCanonicalGeometry()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A065 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:248
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A066 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:250
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A067 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:252
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old drawer QWidget handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EditorDrawerPresenter owns the behavior).
Source context since previous assertion:
  ;
          EditorDrawer *drawer = view.editorDrawer();

Original expression:
  QVERIFY(drawer)

A068 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:255
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the other-events band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const auto other = view.timelineBandLayout().geometry(songview::TimelineBand::OtherEvents);
          const auto velocity = view.timelineBandLayout().geometry(songview::TimelineBand::Velocity);

Original expression:
  QVERIFY(other.has_value())

A069 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:256
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(velocity.has_value())

A070 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:259
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the velocity drawer body rect presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const DrawerChrome &chrome = drawer->chrome();
          const std::optional<QRect> body = drawer->bodyRect(EditorDrawerPage::Velocity);

Original expression:
  QVERIFY(body.has_value())

A071 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:260
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome velocity handle visibility; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(chrome.velocityHandleVisible())

A072 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:261
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome automation handle hidden state; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!chrome.automationHandleVisible())

A073 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:262
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome voice-changes handle hidden state; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!chrome.voiceChangesHandleVisible())

A074 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:263
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer overlay rect above the other-events band (vertical non-intersection); no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(drawer->overlayRect().bottom() < other->rect.top())

A075 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:264
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome handle rect aligned to the drawer body; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.velocityHandleRect().bottom(), body->top())

A076 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:265
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer body bottom aligned to the chrome bar top; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(body->bottom() + 1, qRound(chrome.barRect().top()))

A077 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:266
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome automation/voice-changes toggle spacing; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.automationToggleRect().x(), chrome.voiceChangesToggleRect().x() +
                                                          chrome.voiceChangesToggleRect().width() +
                                                          layout::space(layout::Space::One))

A078 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:269
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome velocity/automation toggle spacing; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.velocityToggleRect().x(), chrome.automationToggleRect().x() +
                                                        chrome.automationToggleRect().width() +
                                                        layout::space(layout::Space::One))

A079 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:276
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome toggles centred on the piano keyboard; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const QRectF toggles = chrome.voiceChangesToggleRect()
                                     .united(chrome.automationToggleRect())
                                     .united(chrome.velocityToggleRect());
          const qreal pianoCenter = (velocity->rect.x() + velocity->plotRect.x()) / 2.0;

Original expression:
  QVERIFY(std::abs(toggles.center().x() - pianoCenter) <= layout::singlePixel())

A080 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:288
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old track-headers quick rows/band/scrollbar item discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift TrackHeadersPresenter owns the behavior).
Source context since previous assertion:
  ;
          const auto *headers = view.findChild<songview::TrackHeaderModel *>(
              QStringLiteral("trackHeaderModel"), Qt::FindDirectChildrenOnly);
          auto *headerBand =
              root->findChild<QQuickItem *>(QStringLiteral("timelineQuickTrackHeaders"));
          auto *headerInput = root->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineTrackHeadersInput"));
          auto *headerRows = root->findChild<QObject *>(QStringLiteral("timelineTrackHeaderRows"));
          auto *headerScroll =
              root->findChild<QQuickItem *>(QStringLiteral("timelineTrackHeaderScrollBar"));
          auto *headerThumb =
              root->findChild<QQuickItem *>(QStringLiteral("timelineTrackHeaderScrollThumb"));

Original expression:
  QVERIFY(headers && headerBand && headerInput && headerRows && headerScroll && headerThumb)

A081 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:291
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter/TrackHeadersGeometry owns the track-headers band geometry presence; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const auto headersGeometry =
              view.timelineBandLayout().geometry(songview::TimelineBand::TrackHeaders);

Original expression:
  QVERIFY(headersGeometry.has_value())

A082 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:292
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old track-headers input bound to the headers presenter; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift TrackHeadersPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(headerInput->interaction(), headers)

A083 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:293
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old track-headers row-count projection into the quick row model; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift TrackHeadersPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(headerRows->property("count").toInt(), headers->rowCount())

A084 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:294
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter owns the track-headers scrollbar width projection; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(headerScroll->width(), headers->scrollbarWidth())

A085 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:295
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter owns the track-headers scrollbar visibility projection; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(headerScroll->isVisible(), headers->maximumScrollY() > 0.0)

A086 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:296
Disposition: GAP
Mapping/reason: Swift TrackHeadersPresenter owns the track-headers scrollbar thumb visibility projection; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(headerThumb->isVisible(), headers->maximumScrollY() > 0.0)

A087 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:297
Disposition: GAP
Mapping/reason: Swift GridGeometry/EditorDrawerPresenter owns the quick ruler plot origin x aligned to the timeline split; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(std::abs(quick->rulerPlotOrigin() - view.timelineSplitX()) <=
                  layout::singlePixel())

A088 | drawerChromeAndQuickHeadersFollowCanonicalGeometry | src/checks/host/tst_hostadapter.cpp:299
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick window device pixel ratio; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(quick->quickWindow()->devicePixelRatio() > 0.0)

A089 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:306
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void bandGeometryPublishesWithTheChoreography()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A090 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:309
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A091 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:311
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A092 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:313
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old drawer QWidget handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EditorDrawerPresenter owns the behavior).
Source context since previous assertion:
  ;
          EditorDrawer *drawer = view.editorDrawer();

Original expression:
  QVERIFY(drawer)

A093 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:316
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band layout geometry before the choreography change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const DrawerChrome &chrome = drawer->chrome();
          const auto before = view.timelineBandLayout().geometry(songview::TimelineBand::Velocity);

Original expression:
  QVERIFY(before.has_value())

A094 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:320
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band layout geometry after the choreography change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

          view.setDrawerSectionHeight(EditorDrawerPage::Velocity, 260);
          const auto after = view.timelineBandLayout().geometry(songview::TimelineBand::Velocity);

Original expression:
  QVERIFY(after.has_value())

A095 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:321
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the band geometry rect change re-published to the QML root properties; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(after->rect != before->rect)

A096 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:327
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity band rect re-published to the QML root property; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          // No event-loop pump: the drawer chrome snapshot publishes through
          // chromeChanged inside the same GUI-thread pass as the choreography,
          // so the published band rectangle must be current in that same pass.
          // A queued publication left a live-resize frame rendering moved resize
          // handles against unmoved panes, showing the roll through the gap.

Original expression:
  QCOMPARE(root->property("velocityBandRect").toRectF(), QRectF(after->rect))

A097 | bandGeometryPublishesWithTheChoreography | src/checks/host/tst_hostadapter.cpp:328
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the drawer chrome handle rect following the published band rect; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(chrome.velocityHandleRect().bottom(), QRectF(after->rect).top())

A098 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:357
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void hiddenBandsClearEveryProjection_data()
      {
          QTest::addColumn<EditorDrawerPage>("page");
          QTest::addColumn<songview::TimelineBand>("band");
          QTest::addColumn<QString>("visibleProperty");
          QTest::addColumn<QString>("rectProperty");
          QTest::newRow("velocity") << EditorDrawerPage::Velocity << songview::TimelineBand::Velocity
                                    << QStringLiteral("velocityBandVisible")
                                    << QStringLiteral("velocityBandRect");
          QTest::newRow("automation")
              << EditorDrawerPage::Automations << songview::TimelineBand::Automation
              << QStringLiteral("automationBandVisible") << QStringLiteral("automationBandRect");
          QTest::newRow("voice") << EditorDrawerPage::VoiceChanges
                                 << songview::TimelineBand::VoiceChanges
                                 << QStringLiteral("voiceChangesBandVisible")
                                 << QStringLiteral("voiceChangesBandRect");
      }

      void hiddenBandsClearEveryProjection()
      {
          QFETCH(EditorDrawerPage, page);
          QFETCH(songview::TimelineBand, band);
          QFETCH(QString, visibleProperty);
          QFETCH(QString, rectProperty);
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A099 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:360
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A100 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:362
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A101 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:365
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the hidden band cleared from the band layout (geometry presence); no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setDrawerSectionVisible(page, false);
          settle();

Original expression:
  QVERIFY(!view.timelineBandLayout().geometry(band).has_value())

A102 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:366
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the hidden band visible property cleared on the QML root; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!root->property(qPrintable(visibleProperty)).toBool())

A103 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:367
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the hidden band rect property cleared on the QML root; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(root->property(qPrintable(rectProperty)).toRectF().isEmpty())

A104 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:382
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old hidden band plot/gutter quick item discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QString inputName;
          QString gutterName;
          if (band == songview::TimelineBand::Velocity) {
              inputName = QStringLiteral("timelineVelocityInput");
              gutterName = QStringLiteral("timelineVelocityGutterInput");
          } else if (band == songview::TimelineBand::Automation) {
              inputName = QStringLiteral("timelineAutomationInput");
              gutterName = QStringLiteral("timelineAutomationGutterInput");
          } else {
              inputName = QStringLiteral("timelineVoiceChangesInput");
              gutterName = QStringLiteral("timelineVoiceChangesGutterInput");
          }
          auto *plot = root->findChild<songview::TimelineInputItem *>(inputName);
          auto *gutter = root->findChild<songview::TimelineInputItem *>(gutterName);

Original expression:
  QVERIFY(plot && gutter)

A105 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:383
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the hidden band plot item visibility; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!plot->isVisible())

A106 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:384
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the hidden band gutter item visibility; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!gutter->isVisible())

A107 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:387
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the restored band present in the band layout; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setDrawerSectionVisible(page, true);
          settle();

Original expression:
  QVERIFY(view.timelineBandLayout().geometry(band).has_value())

A108 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:388
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EditorDrawerLayout owns the restored band visible property on the QML root; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(root->property(qPrintable(visibleProperty)).toBool())

A109 | hiddenBandsClearEveryProjection | src/checks/host/tst_hostadapter.cpp:389
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old restored band physical quick-input parity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(checks::support::physicalInputsMatchCanonical(view.timelineBandLayout(), *root,
                                                                band, inputName, gutterName))

A110 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:397
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void eventListHidesOnlyRollProjection()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A111 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:400
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A112 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:402
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A113 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:405
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EventListPresenter owns the roll band cleared from the band layout under the event-list toggle; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setEventListVisible(true);
          settle();

Original expression:
  QVERIFY(!view.timelineBandLayout().geometry(songview::TimelineBand::Roll).has_value())

A114 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:410
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old roll plot/gutter quick band-input item presence; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          auto *roll =
              root->findChild<songview::TimelineInputItem *>(QStringLiteral("timelineRollInput"));
          auto *gutter = root->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineRollGutterInput"));

Original expression:
  QVERIFY(roll && gutter)

A115 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:411
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EventListPresenter owns the roll plot item visibility under the event-list toggle; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!roll->isVisible())

A116 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:412
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EventListPresenter owns the roll gutter item visibility under the event-list toggle; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!gutter->isVisible())

A117 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:413
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the velocity band presence while roll is hidden; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(view.timelineBandLayout().geometry(songview::TimelineBand::Velocity).has_value())

A118 | eventListHidesOnlyRollProjection | src/checks/host/tst_hostadapter.cpp:416
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/EventListPresenter owns the roll band restored in the band layout; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setEventListVisible(false);
          settle();

Original expression:
  QVERIFY(view.timelineBandLayout().geometry(songview::TimelineBand::Roll).has_value())

A119 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:423
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void otherEventsTooltipUsesTheQuickInputAndClearsOnLeave()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A120 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:426
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A121 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:428
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick root object discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *root = quick->rootObject();

Original expression:
  QVERIFY(root)

A122 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:433
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the other-events band geometry presence (with input discovery); no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          auto *input = root->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineOtherEventsInput"));
          const auto geometry =
              view.timelineBandLayout().geometry(songview::TimelineBand::OtherEvents);

Original expression:
  QVERIFY(input && geometry.has_value())

A123 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:437
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared tooltip QQuickItem and OtherStrip widget discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;
          QQuickItem *toolTip =
              root->findChild<QQuickItem *>(QStringLiteral("timelineOtherEventsToolTip"));
          auto *otherStrip = dynamic_cast<songview::OtherStrip *>(input->interaction());

Original expression:
  QVERIFY(toolTip && otherStrip && !toolTip->isVisible())

A124 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:438
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired OtherStrip tooltip surface: feature not yet ported: other-strip tooltip/loop markers; the retired strip model had to expose the hovered marker before any tooltip text could be observed; no equivalent predicate established.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!view.model().strip.empty())

A125 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:444
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old input containment of the tooltip hover point; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;
          const StripItem &marker = view.model().strip.front();
          const qreal x = view.camera().displayX(double(marker.tick), geometry->plotRect.x(),
                                                 input->devicePixelRatio());
          const QPointF inputPoint =
              input->mapFromScene(QPointF{x, qreal(geometry->rect.center().y())});

Original expression:
  QVERIFY(input->contains(inputPoint))

A126 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:454
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old OtherStrip tooltip visibility after a hover enter; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;
          const QPointF windowPoint = input->mapToScene(inputPoint);
          QEnterEvent enter(windowPoint, windowPoint,
                            QPointF(quick->quickWindow()->mapToGlobal(windowPoint.toPoint())));
          QCoreApplication::sendEvent(quick->quickWindow(), &enter);
          QMouseEvent move(QEvent::MouseMove, windowPoint, windowPoint,
                           quick->quickWindow()->mapToGlobal(windowPoint.toPoint()), Qt::NoButton,
                           Qt::NoButton, Qt::NoModifier);
          QCoreApplication::sendEvent(quick->quickWindow(), &move);
          settle();

Original expression:
  QVERIFY(otherStrip->toolTipVisible())

A127 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:455
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired OtherStrip tooltip surface: feature not yet ported: other-strip tooltip/loop markers; the retired tooltip text had to contain the hovered marker label; no equivalent predicate established.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(otherStrip->toolTipText().contains(marker.label))

A128 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:456
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old tooltip position matched to the hover point; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(otherStrip->toolTipPosition(), inputPoint)

A129 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:457
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old tooltip QQuickItem visibility after the hover; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(toolTip->isVisible())

A130 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:464
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old rendered tooltip text containing the marker label; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;
          const auto renderedText = [](QQuickItem &item) {
              for (QQuickItem *child : item.childItems())
                  if (QByteArray(child->metaObject()->className()).contains("QQuickText"))
                      return child->property("text").toString();
              return QString();
          };

Original expression:
  QVERIFY(renderedText(*toolTip).contains(marker.label))

A131 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:478
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired OtherStrip tooltip surface: feature not yet ported: other-strip tooltip/loop markers; the retired tooltip had to clear on leave (async wait result); no equivalent predicate established.
Source context since previous assertion:
  ;
          const QPointF outside = root->property("rulerBandRect").toRectF().center();
          QMouseEvent leave(QEvent::MouseMove, outside, outside,
                            quick->quickWindow()->mapToGlobal(outside.toPoint()), Qt::NoButton,
                            Qt::NoButton, Qt::NoModifier);
          QCoreApplication::sendEvent(quick->quickWindow(), &leave);
          const auto hidden =
              checks::async_wait::waitUntil([] { return true; },
                                            [&] {
                                                return !otherStrip->toolTipVisible() &&
                                                       otherStrip->toolTipText().isEmpty() &&
                                                       !toolTip->isVisible();
                                            },
                                            1000);

Original expression:
  QCOMPARE(hidden, checks::async_wait::Result::Ready)

A132 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:480
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old OtherStrip tooltip cleared after leave; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;
          settle();

Original expression:
  QVERIFY(!otherStrip->toolTipVisible() && otherStrip->toolTipText().isEmpty())

A133 | otherEventsTooltipUsesTheQuickInputAndClearsOnLeave | src/checks/host/tst_hostadapter.cpp:481
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old tooltip QQuickItem hidden after leave; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift EventListPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!toolTip->isVisible())

A134 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:488
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void loopMarkersReachTimelineButNotTheOtherEventsRaster()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A135 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:492
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the other-events band geometry presence for the raster probe; no equivalent Swift/QML geometry predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          SongView &view = host.view();
          const auto geometry =
              view.timelineBandLayout().geometry(songview::TimelineBand::OtherEvents);

Original expression:
  QVERIFY(geometry.has_value())

A136 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:496
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old baseline other-events band raster capture validity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          QString captureError;
          const QImage baseline =
              checks::support::captureQuickBand(view, geometry->rect, &captureError);

Original expression:
  QVERIFY2(!baseline.isNull(), qPrintable(captureError))

A137 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:500
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the loop timeline had to exist for the loop-marker scenario; no equivalent predicate established.
Source context since previous assertion:
  ;
          host.document().setLoopTick(false, 6);
          host.document().setLoopTick(true, 18);
          std::unique_ptr<MidiTimeline> loopTimeline = host.document().buildTimeline(44100.0);

Original expression:
  QVERIFY(loopTimeline)

A138 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:501
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the loop start tick reaching the timeline; no equivalent predicate established.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(loopTimeline->loopStartTick, uint64_t{6})

A139 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:502
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the loop end tick reaching the timeline; no equivalent predicate established.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(loopTimeline->loopEndTick, uint64_t{18})

A140 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:507
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the other-events band geometry after the loop update; no equivalent predicate established.
Source context since previous assertion:
  ;
          view.updateSong(loopTimeline.get());
          settle();
          const auto updated =
              view.timelineBandLayout().geometry(songview::TimelineBand::OtherEvents);

Original expression:
  QVERIFY(updated.has_value())

A141 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:509
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the post-update band raster capture validity guard; no equivalent predicate established.
Source context since previous assertion:
  ;
          const QImage after = checks::support::captureQuickBand(view, updated->rect, &captureError);

Original expression:
  QVERIFY2(!after.isNull(), qPrintable(captureError))

A142 | loopMarkersReachTimelineButNotTheOtherEventsRaster | src/checks/host/tst_hostadapter.cpp:512
Disposition: GAP
Mapping/reason: Swift has no counterpart for the retired loop-marker raster surface: feature not yet ported: other-strip tooltip/loop markers; the other-events raster equality after the loop markers changed; no equivalent predicate established.
Source context since previous assertion:
  ;
          // The timeline fields above are primary; this raster comparison guards
          // the absence of loop-marker leakage into the Other Events strip.

Original expression:
  QCOMPARE(after, baseline)

A143 | automationTempoRangeDelegatesTheSelectionScope | src/checks/host/tst_hostadapter.cpp:519
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
          view.updateSong(&host.timeline());
      }
      void automationTempoRangeDelegatesTheSelectionScope()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A144 | automationTempoRangeDelegatesTheSelectionScope | src/checks/host/tst_hostadapter.cpp:527
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view and automation page widget handles; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
          view.setDrawerActivePage(EditorDrawerPage::Automations);
          view.setDrawerSectionHeight(EditorDrawerPage::Automations, 180);
          settle();
          auto *quick = view.quickView();
          auto *page = view.editorDrawer()->automationPage();

Original expression:
  QVERIFY(quick && page)

A145 | automationTempoRangeDelegatesTheSelectionScope | src/checks/host/tst_hostadapter.cpp:531
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation canvas and automation input item handles; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          auto *canvas = page->canvas();
          auto *input = quick->rootObject()->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineAutomationInput"));

Original expression:
  QVERIFY(canvas && input)

A146 | automationTempoRangeDelegatesTheSelectionScope | src/checks/host/tst_hostadapter.cpp:534
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationParameter owns the automation tempo parameter index discovery; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const int tempoIndex = checks::support::automationParameterIndex(
              *canvas, {EditorAutomationRowKind::Tempo, 0, 0});

Original expression:
  QVERIFY(tempoIndex >= 0)

A147 | automationTempoRangeDelegatesTheSelectionScope | src/checks/host/tst_hostadapter.cpp:538
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationLaneProjection owns the activated automation lane body rect; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          canvas->activateParameter(tempoIndex);
          settle();
          const QRect tempo = canvas->laneBody(LaneHandle{0});

Original expression:
  QVERIFY(!tempo.isEmpty())

A148 | events::sendMouse | src/checks/host/tst_hostadapter.cpp:549
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationSelectionCommands owns the time selection active state after the tempo-range drag; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const qreal y = tempo.center().y();
          const QPointF start(layout::space(layout::Space::One), y);
          const QPointF end = start + QPointF(layout::fontPx(12.0), 0.0);
          checks::events::sendMouse(*input, QEvent::MouseButtonPress, start, Qt::RightButton,
                                    Qt::RightButton, Qt::NoModifier);
          checks::events::sendMouse(*input, QEvent::MouseMove, end, Qt::NoButton, Qt::RightButton,
                                    Qt::NoModifier);
          checks::events::sendMouse(*input, QEvent::MouseButtonRelease, end, Qt::RightButton,
                                    Qt::NoButton, Qt::NoModifier);
          const auto selection = view.selectionModel().timeSelection();

Original expression:
  QVERIFY(selection.active())

A149 | events::sendMouse | src/checks/host/tst_hostadapter.cpp:550
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationSelectionCommands owns the time selection scope after the tempo-range drag; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(selection.scope, songview::EditorSelectionModel::TimeSelection::Lanes)

A150 | events::sendMouse | src/checks/host/tst_hostadapter.cpp:551
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationSelectionCommands owns the tempo flag on the time selection; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selection.tempo)

A151 | events::sendMouse | src/checks/host/tst_hostadapter.cpp:552
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationSelectionCommands owns the empty lane list on a tempo-scoped time selection; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(selection.lanes.empty())

A152 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:559
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void automationPanRoutesThroughQuickWindow()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A153 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:566
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view and automation input item handles; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
          view.setDrawerActivePage(EditorDrawerPage::Automations);
          settle();
          auto *quick = view.quickView();
          auto *automation = view.editorDrawer()->automationPage()->canvas();

Original expression:
  QVERIFY(quick && automation)

A154 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:569
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation input item handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          auto *input = quick->rootObject()->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineAutomationInput"));

Original expression:
  QVERIFY(input)

A155 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:578
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old focused timeline band after the pan press (focus plumbing); uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage/AutomationInteraction owns the behavior).
Source context since previous assertion:
  ;
          const uint64_t revision = host.document().revision();
          const int undo = host.document().undoStack()->count();
          const QPointF point(input->width() / 2.0, input->height() / 2.0);
          const QPointF inWindow = input->mapToScene(point);
          QMouseEvent press(QEvent::MouseButtonPress, inWindow, inWindow, Qt::MiddleButton,
                            Qt::MiddleButton, Qt::NoModifier);
          QCoreApplication::sendEvent(quick->quickWindow(), &press);
          settle();

Original expression:
  QCOMPARE(view.focusedTimelineBand(),
                   std::optional<songview::TimelineBand>{songview::TimelineBand::Automation})

A156 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:580
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick-window mouse-grabber identity during the pan; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage/AutomationInteraction owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(quick->quickWindow()->mouseGrabberItem(), static_cast<QQuickItem *>(input))

A157 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:581
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation canvas isPanning while the pointer is held; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage/AutomationInteraction owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(automation->isPanning())

A158 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:584
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation canvas isPanning after a further move; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage/AutomationInteraction owns the behavior).
Source context since previous assertion:
  ;
          QFocusEvent focusOut(QEvent::FocusOut, Qt::OtherFocusReason);
          QApplication::sendEvent(input, &focusOut);

Original expression:
  QVERIFY(automation->isPanning())

A159 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:587
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation canvas isPanning cleared after release; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage/AutomationInteraction owns the behavior).
Source context since previous assertion:
  ;
          QEvent deactivate(QEvent::WindowDeactivate);
          QApplication::sendEvent(quick->quickWindow(), &deactivate);

Original expression:
  QVERIFY(!automation->isPanning())

A160 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:588
Disposition: GAP
Mapping/reason: Swift DocumentSession.revision owns the document revision unchanged by the pan gesture; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(host.document().revision(), revision)

A161 | automationPanRoutesThroughQuickWindow | src/checks/host/tst_hostadapter.cpp:589
Disposition: GAP
Mapping/reason: Swift DocumentSession history (coreEditHistoryCountAtTip) owns the history depth unchanged by the pan gesture; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(host.document().undoStack()->count(), undo)

A162 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:596
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void velocityVoiceRoutingUsesPresentationOnlyForSteadyContext()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A163 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:599
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old editorDrawer()->velocityArea() QWidget handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *area = view.editorDrawer()->velocityArea();

Original expression:
  QVERIFY(area)

A164 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:602
Disposition: GAP
Mapping/reason: Swift VelocityPage/VelocityAxisModel owns the velocity axis voice name at the steady cursor; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.selectionModel().clearNoteSelection();
          view.setEditCursorTick(0);

Original expression:
  QCOMPARE(QString::fromLatin1(area->axis().map().voiceName()), QStringLiteral("Square 1"))

A165 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:605
Disposition: GAP
Mapping/reason: Swift VelocityPage/VelocityAxisModel owns the velocity axis voice name after the voice change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setPlayheadSample(host.timeline().sampleForTick(24), true);
          settle();

Original expression:
  QCOMPARE(QString::fromLatin1(area->axis().map().voiceName()), QStringLiteral("Noise"))

A166 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:610
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old velocity-area diagnostics content-build counter; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage presentation counters owns the behavior).
Source context since previous assertion:
  ;
          const auto before = area->diagnostics();
          view.setPlayheadSample(host.timeline().sampleForTick(25), true);
          view.setPlayheadSample(host.timeline().sampleForTick(26), true);
          settle();

Original expression:
  QCOMPARE(area->diagnostics().contentBuildCount, before.contentBuildCount)

A167 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:611
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old velocity-area diagnostics playhead-presentation counter; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift VelocityPage/SharedPlayheadPresenter owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QCOMPARE(area->diagnostics().playheadPresentationCount,
                   before.playheadPresentationCount + 2)

A168 | velocityVoiceRoutingUsesPresentationOnlyForSteadyContext | src/checks/host/tst_hostadapter.cpp:615
Disposition: GAP
Mapping/reason: Swift VelocityPage/VelocityAxisModel owns the velocity axis voice name after the second voice change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setPlayheadSample(host.timeline().sampleForTick(26), false);
          settle();

Original expression:
  QCOMPARE(QString::fromLatin1(area->axis().map().voiceName()), QStringLiteral("Square 1"))

A169 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:622
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void drawerSoloAndTrackRemapReachTheHost()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A170 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:625
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A171 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:628
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old velocity band TimelineInputItem handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          auto *velocity = quick->rootObject()->findChild<songview::TimelineInputItem *>(
              QStringLiteral("timelineVelocityInput"));

Original expression:
  QVERIFY(velocity)

A172 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:635
Disposition: GAP
Mapping/reason: Swift DocumentSession soloed tracks/TrackHeadersPresenter owns the track solo state after the drawer solo toggle; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          const int selectedTrack = view.selectionModel().primaryTrack();
          view.setTrackSolo(selectedTrack, false);
          QKeyEvent soloKey(QEvent::KeyPress, Qt::Key_S, Qt::NoModifier);
          QApplication::sendEvent(velocity, &soloKey);
          if (!soloKey.isAccepted())
              QApplication::sendEvent(&view, &soloKey);

Original expression:
  QVERIFY(view.trackSoloed(selectedTrack))

A173 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:642
Disposition: GAP
Mapping/reason: Swift SongDocument moveTrack/DocumentWorkspace owns the track move acceptance (moveTrack(0, 1)); no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setTrackSolo(selectedTrack, false);
          EditorViewState state = view.editorViewState();
          const EditorAutomationRowId oldLane{EditorAutomationRowKind::ControlChange, 0, 74};
          const EditorAutomationRowId movedLane{EditorAutomationRowKind::ControlChange, 1, 74};
          state.emptyLanes.insert(oldLane);
          view.applyEditorViewState(state);

Original expression:
  QVERIFY(host.document().moveTrack(0, 1))

A174 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:643
Disposition: GAP
Mapping/reason: Swift DocumentSession/DocumentWorkspace empty-lane state owns the moved lane flagged empty in the editor view state; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(view.editorViewState().emptyLanes.contains(movedLane))

A175 | drawerSoloAndTrackRemapReachTheHost | src/checks/host/tst_hostadapter.cpp:644
Disposition: GAP
Mapping/reason: Swift DocumentSession/DocumentWorkspace empty-lane state owns the old lane no longer flagged empty in the editor view state; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(!view.editorViewState().emptyLanes.contains(oldLane))

A176 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:651
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void voiceChangesRefreshWithoutInvalidatingAutomationRaster()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A177 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:660
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the automation band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          SongView &view = host.view();
          view.setDrawerSectionVisible(EditorDrawerPage::Automations, true);
          view.setDrawerSectionVisible(EditorDrawerPage::VoiceChanges, true);
          view.setDrawerActivePage(EditorDrawerPage::VoiceChanges);
          settle();
          const auto automation =
              view.timelineBandLayout().geometry(songview::TimelineBand::Automation);
          const auto voice = view.timelineBandLayout().geometry(songview::TimelineBand::VoiceChanges);

Original expression:
  QVERIFY(automation.has_value())

A178 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:661
Disposition: GAP
Mapping/reason: Swift EditorDrawerPresenter/GridGeometry owns the voice-changes band layout geometry presence; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(voice.has_value())

A179 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:663
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation canvas QWidget handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          auto *automationArea = view.editorDrawer()->automationPage()->canvas();

Original expression:
  QVERIFY(automationArea)

A180 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:668
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old voice band raster capture validity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          const auto automationRows = automationArea->rows();
          QString captureError;
          const QImage voiceBefore =
              checks::support::captureQuickBand(view, voice->rect, &captureError);

Original expression:
  QVERIFY2(!voiceBefore.isNull(), qPrintable(captureError))

A181 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:671
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation band raster capture validity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          const QImage automationBefore =
              checks::support::captureQuickBand(view, automation->rect, &captureError);

Original expression:
  QVERIFY2(!automationBefore.isNull(), qPrintable(captureError))

A182 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:674
Disposition: GAP
Mapping/reason: Swift AutomationPage/AutomationProjection owns the automation canvas rows unchanged across a voice change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;
          view.setPlayheadSample(host.timeline().sampleForTick(24), true);
          settle();

Original expression:
  QVERIFY(automationRowsEqual(automationArea->rows(), automationRows))

A183 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:679
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old post-change voice/automation raster capture validity; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;
          const QImage voiceAfter =
              checks::support::captureQuickBand(view, voice->rect, &captureError);
          const QImage automationAfter =
              checks::support::captureQuickBand(view, automation->rect, &captureError);

Original expression:
  QVERIFY2(!voiceAfter.isNull() && !automationAfter.isNull(), qPrintable(captureError))

A184 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:680
Disposition: GAP
Mapping/reason: Swift VoiceChangesPage/VoiceChangesPublication owns the voice band raster changed by the voice change; no equivalent predicate established for this deleted host suite, so the original predicate remains an open obligation.
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(voiceAfter != voiceBefore)

A185 | voiceChangesRefreshWithoutInvalidatingAutomationRaster | src/checks/host/tst_hostadapter.cpp:683
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old automation band raster equality across a voice change; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift AutomationPage owns the behavior).
Source context since previous assertion:
  ;
          // The image check is a retained negative control. The primary
          // assertion above proves Automation's model did not rebuild.

Original expression:
  QCOMPARE(automationAfter, automationBefore)

A186 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:690
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old SyntheticHost fixture construction and prepare() guard; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift SongTabsController/SongTabSession owns the behavior).
Source context since previous assertion:
  ;
      }

      void sharedPopupDiesOnHideAndReadinessReset()
      {
          SyntheticHost host;
          QString error;

Original expression:
  QVERIFY2(host.prepare(&error), qPrintable(error))

A187 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:693
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick view handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          SongView &view = host.view();
          auto *quick = view.quickView();

Original expression:
  QVERIFY(quick)

A188 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:694
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old quick window handle discovery; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession shell window owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(quick->quickWindow())

A189 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:696
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared popup session handle; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          auto *session = quick->popupSession();

Original expression:
  QVERIFY(session)

A190 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:698
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old beginMenu acceptance on the shared popup session; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          QObject menuOwner;

Original expression:
  QVERIFY(session->beginMenu(&menuOwner))

A191 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:699
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared popup session reported open; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(session->isOpen())

A192 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:704
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared popup session closed when the host hides; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          // Tab switches hide the embedded child while the top level stays
          // active, so Deactivate may never fire: Hide alone must dismiss.
          QEvent hide(QEvent::Hide);
          QCoreApplication::sendEvent(quick->quickWindow(), &hide);

Original expression:
  QVERIFY(!session->isOpen())

A193 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:707
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old beginMenu acceptance after the readiness reset; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          // Readiness/document resets route through cancelTransientInput: any
          // surviving menu or form dies before the gate eats new input.

Original expression:
  QVERIFY(session->beginMenu(&menuOwner))

A194 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:708
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared popup session reported open after the readiness reset; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;

Original expression:
  QVERIFY(session->isOpen())

A195 | sharedPopupDiesOnHideAndReadinessReset | src/checks/host/tst_hostadapter.cpp:710
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Retired host representation: old shared popup session closed after the readiness reset; uncompiled in all CMake targets and unregistered in checkcatalog; no Swift ingress (Swift ApplicationSession/EditorSurface.qml shell owns the behavior).
Source context since previous assertion:
  ;
          view.cancelTransientInput();

Original expression:
  QVERIFY(!session->isOpen())

Remaining original source (closing sequence and helper bodies):
  ;
      }

    private:
      QString m_projectRoot;
      QString m_songLabel;
  };
  } // namespace
  } // namespace checks::host

  int runHostAdapterCheck(const QString &projectRoot, const QString &songLabel,
                          const QStringList &qtArguments)
  {
      checks::host::HostAdapterTest test(projectRoot, songLabel);
      QStringList arguments{QStringLiteral("host-adapter")};
      arguments.append(qtArguments);
      return QTest::qExec(&test, arguments);
  }

  #include "tst_hostadapter.moc"
