Assertion correspondence: themelayout/tst_themelayout_font.cpp

Reference revision: a7fcaa3e9ea51a057c85bc1959e541c69fe4a3ea
Original: src/checks/themelayout/tst_themelayout_font.cpp
Original SHA-256: aa416bc26d730a3c4625e5861bcd80df4ee1cdad299db685d4ef851e2ac893b8
Swift counterpart: src/checks/themelayout/TypographyLayoutChecks.swift
Swift SHA-256: 68d30462170a47f1ad1587abc1a57676ce37889ea5f9c3f5a927b909882d6aee
Swift counterpart: src/checks/editorqml/tst_Typography.qml
Swift SHA-256: 9822c4d1e4bf47474932b2d560e2418609106f384c3f5388db037c7c67585ecf

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site in this file appears below. The file exercises the
typography module (base size, body/mono/table/caption faces, fitted sizing,
note-name face, glyph centering) and layout initialization against the lane's
expected base font size, plus one theme-apply font-restore case. Source context
preserves setup, loops, and guards between assertions verbatim.
MATCHED means a corresponding Swift predicate was identified and would execute;
PARTIAL means a related Swift predicate exists with a stated unproved condition;
GAP means no equivalent Swift assertion is established;
NATIVE means the site observes native window/rendering/harness state a
presenter-level Swift check cannot establish. Pure policy (base, spaces,
body/mono/caption faces, tnum, fitted bounds and maximality) executes in
TypographyLayoutChecks.swift; the resolved Next family, shell font, title
centering and organization identity execute in tst_Typography.qml. All faces
render and measure unhinted (S036-S040). Faces with no Swift/QML surface
(tableMono, italic, bold-caption) and native rasterization metrics stay
non-MATCHED with "feature not yet ported" reasons naming the missing surface.

Verification
Command: deno task verify --filter swiftcore --verbose
Command: deno task verify:shell --filter shell-typography --verbose
Command: deno task verify:shell --filter shellwindow --verbose
Command: deno task verify --filter fontcheck --verbose
Result: swiftcore PASS (10.19s, 1/14 harness) and shell-typography PASS
(0.59s, 1/9) on feature/swift-keybindings with the counterparts below;
source hashes pin the tree inspected here.

Execution and ownership
Native ownership only. runThemeLayoutFontCheck (defined in
src/checks/themelayout/themelayoutcheck.cpp) launches the fontcheck lane with
the expected base size sampled from the application font before
ui::initializeApplication; ThemeLayoutFontTest::init applies the vanilla theme
and saves font/palette/stylesheet. Swift execution: runTypographyLayoutChecks
(src/checks/themelayout/TypographyLayoutChecks.swift:34) via
runProjectSessionSuite (src/checks/workspace/SessionChecks.swift:16) into the
projectSession slot (swiftcore suite 10 via pdcSuiteRun); the counterpart is
compiled in src/checks/CMakeLists.txt :: swift_core_check. QML execution: the
shell-typography entry (src/checks/editorqml/ShellQmlTests.swift) stages
mus_route101 and runs tst_Typography.qml against the production ShellWindow.
Suite declarations live in src/checks/themelayout/tst_themelayout.h; no shared
Swift fixture file exists. The file-local helpers hasTabularNumbers and
resolvedSpace are value instruments, inventoried via the assertions that use
them.

Fixture, sequence and row constraints
No data rows: each case runs once per lane. typographyAndLayoutInitialization
expands A004 and A009 over the same 8-entry spaces table (Zero/0.0 through
Eight/2.0). typographyFaceContracts derives expectedBodySize from the live base
size (qMax(1, qRound(base * 1.125))) and checks every face against it.
fittedTypographyContracts loops heights 1..captionHeight+4, skipping
unfittable heights; A042 fires only when the fitted size is below the caption
size (maximality: size+1 must overflow). The note-name style expectation is
platform-guarded (Regular on macOS, SemiBold elsewhere). The Q_OS_MACOS branch
is a compile-time guard, not a runtime row. Swift rows: lane bases 12, 16, 18
with pinned literal tables; the fitted loop mirrors heights 1 through face
height + 4 at base 16. QML rows: one song (mus_route101), one body-font case
and one selected-title centering case.
Original setup, branch guards, and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory

A001 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:37
Disposition: MATCHED
Mapping/reason: The base is captured before faces or spaces derive: the lane
base is retained and the single seed threads every owner. S003, S035.
Source context since previous assertion:
  #include "checks/themelayout/tst_themelayout.h"

  #include "ui/layout.h"
  #include "ui/theme/themeresolver.h"
  #include "ui/theme/themeruntime.h"
  #include "ui/typography.h"

  #include <QApplication>
  #include <QFontInfo>
  #include <QFontMetrics>
  #include <QFontMetricsF>
  #include <QLabel>
  #include <QtTest>

  #include <array>
  #include <optional>
  #include <utility>

  namespace {

  bool hasTabularNumbers(const QFont &font)
  {
      constexpr QFont::Tag tag{"tnum"};
      return font.isFeatureSet(tag) && font.featureValue(tag) == 1;
  }

  int resolvedSpace(int baseFontPx, double multiplier)
  {
      return multiplier == 0.0 ? 0 : qMax(1, qRound(baseFontPx * multiplier));
  }

  } // namespace

  void ThemeLayoutFontTest::typographyAndLayoutInitialization()
  {
      const std::optional<int> baseFontPx = typography::baseFontPx();
Original expression:
  QVERIFY(baseFontPx.has_value());

A002 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:38
Disposition: MATCHED
Mapping/reason: Lane bases are positive and degenerate inputs fall back to the
seed instead of latching. S004, S005, S035.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(m_expectedBaseFontPx > 0);

A003 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:39
Disposition: MATCHED
Mapping/reason: The lane base reaches the grid metrics unchanged. S003.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(*baseFontPx, m_expectedBaseFontPx);

A004 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:47
Disposition: MATCHED
Mapping/reason: The 8 space tokens execute in Swift at the lane base. S001.
Source context since previous assertion:
  ;

      const std::array spaces{
          std::pair{layout::Space::Zero, 0.0},   std::pair{layout::Space::Half, 0.125},
          std::pair{layout::Space::One, 0.25},   std::pair{layout::Space::Two, 0.5},
          std::pair{layout::Space::Three, 0.75}, std::pair{layout::Space::Four, 1.0},
          std::pair{layout::Space::Six, 1.5},    std::pair{layout::Space::Eight, 2.0}};
      for (const auto &[token, multiplier] : spaces)
Original expression:
  QCOMPARE(layout::space(token), resolvedSpace(*baseFontPx, multiplier));

A005 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:50
Disposition: GAP
Mapping/reason: feature not yet ported: process-scoped layout::initialize; Swift
layout is a pure value with no initialize call, and stable re-resolution is
covered for A009 by S001/S002.
Source context since previous assertion:
  ;

      const QString styleSheet = m_application->styleSheet();
Original expression:
  QVERIFY(layout::initialize(*m_application, *baseFontPx));

A006 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:51
Disposition: MATCHED
Mapping/reason: Degenerate bases never latch in Swift: non-positive and
non-finite bases fall back to the seed. S004, S005.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!layout::initialize(*m_application, 0));

A007 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:52
Disposition: GAP
Mapping/reason: feature not yet ported: process-scoped layout::initialize
second-call conflict rejection; Swift has no process-global base to conflict
with because each surface carries its own base.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!layout::initialize(*m_application, *baseFontPx + 1));

A008 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:53
Disposition: NATIVE
Mapping/reason: Original native application-state obligation: the successful
re-initialize must leave the native stylesheet byte-identical. No stylesheet
layer exists in the Swift shell; a presenter check cannot observe the native
stylesheet. Stable styling is covered by S001 and the shell-typography
geometry predicates S029/S030.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(m_application->styleSheet(), styleSheet);

A009 | ThemeLayoutFontTest::typographyAndLayoutInitialization | src/checks/themelayout/tst_themelayout_font.cpp:55
Disposition: MATCHED
Mapping/reason: The 8 space tokens still match after re-resolution: values are
stable across resolutions. S001, S002.
Source context since previous assertion:
  ;
      for (const auto &[token, multiplier] : spaces)
Original expression:
  QCOMPARE(layout::space(token), resolvedSpace(*baseFontPx, multiplier));

A010 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:61
Disposition: MATCHED
Mapping/reason: The base is present before faces derive: bodyPx derives from
the lane base. S003, S006.
Source context since previous assertion:
  ;
  }

  void ThemeLayoutFontTest::typographyFaceContracts()
  {
      const std::optional<int> baseFontPx = typography::baseFontPx();
Original expression:
  QVERIFY(baseFontPx.has_value());

A011 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:65
Disposition: MATCHED
Mapping/reason: Body PreferNoHinting executes in the shell root font. S026.
Source context since previous assertion:
  ;
      const QFont body = QApplication::font();
      const QFontInfo bodyInfo(body);
      const int expectedBodySize = qMax(1, qRound(*baseFontPx * 1.125));
Original expression:
  QCOMPARE(body.hintingPreference(), QFont::PreferNoHinting);

A012 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:66
Disposition: MATCHED
Mapping/reason: The body family executes as the Next chip face and the resolved
shell font. S007, S024.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(bodyInfo.family(), QStringLiteral("Atkinson Hyperlegible Next"));

A013 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:67
Disposition: MATCHED
Mapping/reason: Body size executes as base times 1.125 in the sig face and the
shell bodyFontPx binding. S006, S025.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(bodyInfo.pixelSize(), expectedBodySize);

A014 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:68
Disposition: MATCHED
Mapping/reason: Body Normal weight executes on the chip face and in the shell
font. S007, S027.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(bodyInfo.weight(), static_cast<int>(QFont::Normal));

A015 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:69
Disposition: MATCHED
Mapping/reason: Body tnum executes in Swift: the spec default, the QML emission
and uniform measured advances. S014, S015, S017-S019.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(hasTabularNumbers(body));

A016 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:73
Disposition: MATCHED
Mapping/reason: Mono PreferNoHinting executes on the published maps: every
spec face pins the unhinted preference (S036), with the fallback caption maps
covered (S037-S039), and the rendered title carries it (S040). The native
measurement path sets the same preference in sgf_create.
Source context since previous assertion:
  ;

      const QFont mono = typography::bodyMono(body);
      const QFontInfo monoInfo(mono);
Original expression:
  QCOMPARE(mono.hintingPreference(), QFont::PreferNoHinting);

A017 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:74
Disposition: MATCHED
Mapping/reason: The mono family executes as the Mono ruler face. S008.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(monoInfo.family(), QStringLiteral("Atkinson Hyperlegible Mono"));

A018 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:75
Disposition: GAP
Mapping/reason: feature not yet ported: body-sized mono face (Mono at the body
size); no Swift/QML surface renders it because ruler mono is ruler-sized by
GridTypography design (S008).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(monoInfo.pixelSize(), expectedBodySize);

A019 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:76
Disposition: MATCHED
Mapping/reason: Mono Normal weight executes on the ruler and beat faces. S008, S009.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(monoInfo.weight(), static_cast<int>(QFont::Normal));

A020 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:77
Disposition: MATCHED
Mapping/reason: Mono tnum executes: every mono face carries the default (S016)
and the shared native path measures uniform advances (S017-S019).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(hasTabularNumbers(mono));

A021 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:80
Disposition: GAP
Mapping/reason: feature not yet ported: tableMono face (typography::tableMono
family); no Swift/QML surface renders it. Mono at rendered sizes is covered by
S008 and S009.
Source context since previous assertion:
  ;

      const QFont tableMono = typography::tableMono(body);
Original expression:
  QCOMPARE(QFontInfo(tableMono).family(), monoInfo.family());

A022 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:81
Disposition: GAP
Mapping/reason: feature not yet ported: tableMono face (absolute letter
spacing); no Swift/QML surface renders it.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(tableMono.letterSpacingType(), QFont::AbsoluteSpacing);

A023 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:82
Disposition: GAP
Mapping/reason: feature not yet ported: tableMono face (letter spacing -0.5);
no Swift/QML surface renders it.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(tableMono.letterSpacing(), -0.5);

A024 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:86
Disposition: MATCHED
Mapping/reason: Caption PreferNoHinting executes on the published maps: the
caption-size chip face pins the unhinted preference (S036), with the fallback
caption maps covered (S037-S039), and the rendered title carries it (S040).
Source context since previous assertion:
  ;

      const QFont caption = typography::caption(body);
      const QFontInfo captionInfo(caption);
Original expression:
  QCOMPARE(caption.hintingPreference(), QFont::PreferNoHinting);

A025 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:87
Disposition: MATCHED
Mapping/reason: The caption family executes as the Next chip face. S007.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(captionInfo.family(), QStringLiteral("Atkinson Hyperlegible Next"));

A026 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:88
Disposition: MATCHED
Mapping/reason: The caption size executes as the base size on the chip face. S007.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(captionInfo.pixelSize(), *baseFontPx);

A027 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:89
Disposition: MATCHED
Mapping/reason: The caption weight executes as Normal 400 on the chip face. S007.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(captionInfo.weight(), static_cast<int>(QFont::Normal));

A028 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:90
Disposition: MATCHED
Mapping/reason: The caption tnum executes in Swift: the spec default, the QML
emission and uniform measured advances. S014, S015, S017-S019.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(hasTabularNumbers(caption));

A029 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:93
Disposition: MATCHED
Mapping/reason: The regular-caption family is the caption family by
construction; the chip face pins Next. S007.
Source context since previous assertion:
  ;

      const QFontInfo regularCaptionInfo(typography::regular(caption));
Original expression:
  QCOMPARE(regularCaptionInfo.family(), captionInfo.family());

A030 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:94
Disposition: MATCHED
Mapping/reason: The regular-caption size is the caption size by construction;
the chip face pins Next at the base size. S007.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(regularCaptionInfo.pixelSize(), captionInfo.pixelSize());

A031 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:95
Disposition: MATCHED
Mapping/reason: Constructed-Regular triple: caption faces are built
upright-Normal with no style mutation; the chip face pins Next/base/400. S007.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(regularCaptionInfo.weight(), static_cast<int>(QFont::Normal));

A032 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:97
Disposition: GAP
Mapping/reason: feature not yet ported: italic caption face (typography::italic
family); no Swift/QML surface renders it, and caption faces are constructed
upright with no style-mutation API.
Source context since previous assertion:
  ;
      const QFontInfo italicCaptionInfo(typography::italic(caption));
Original expression:
  QCOMPARE(italicCaptionInfo.family(), captionInfo.family());

A033 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:98
Disposition: GAP
Mapping/reason: feature not yet ported: italic caption face (pixel size); no
Swift/QML surface renders it, and caption faces are constructed upright with no
style-mutation API.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(italicCaptionInfo.pixelSize(), captionInfo.pixelSize());

A034 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:99
Disposition: GAP
Mapping/reason: feature not yet ported: italic caption face (StyleItalic); no
Swift/QML surface renders it, and caption faces are constructed upright with no
style-mutation API.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(italicCaptionInfo.style(), QFont::StyleItalic);

A035 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:101
Disposition: MATCHED
Mapping/reason: Bold preserves size: the body-size sig face and the ruler bold
face pin their sizes with DemiBold weight. S006, S010.
Source context since previous assertion:
  ;
      const QFontInfo bodyBoldInfo(typography::bold(body));
Original expression:
  QCOMPARE(bodyBoldInfo.pixelSize(), bodyInfo.pixelSize());

A036 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:102
Disposition: MATCHED
Mapping/reason: DemiBold weight executes on the bold face preserving the ruler
size. S010.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(bodyBoldInfo.weight(), static_cast<int>(QFont::DemiBold));

A037 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:104
Disposition: GAP
Mapping/reason: feature not yet ported: bold caption face (bold(caption)
pixel size); no Swift/QML surface renders it, and DemiBold at body size is
covered by S006 and S010.
Source context since previous assertion:
  ;
      const QFontInfo captionBoldInfo(typography::bold(caption));
Original expression:
  QCOMPARE(captionBoldInfo.pixelSize(), captionInfo.pixelSize());

A038 | ThemeLayoutFontTest::typographyFaceContracts | src/checks/themelayout/tst_themelayout_font.cpp:105
Disposition: GAP
Mapping/reason: feature not yet ported: bold caption face (DemiBold weight);
no Swift/QML surface renders it, and the DemiBold policy at rendered sizes is
covered by S010.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(captionBoldInfo.weight(), static_cast<int>(QFont::DemiBold));

A039 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:114
Disposition: MATCHED
Mapping/reason: Fitting at a generous height succeeds and keeps the full size. S020.
Source context since previous assertion:
  ;
  }

  void ThemeLayoutFontTest::fittedTypographyContracts()
  {
      const QFont body = QApplication::font();
      const QFont caption = typography::caption(body);
      const QFontInfo captionInfo(caption);
      const int captionHeight = QFontMetrics(caption).height();
Original expression:
  QVERIFY(typography::fitted(body, captionHeight).has_value());

A040 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:121
Disposition: MATCHED
Mapping/reason: The fit stays within the face size across the mirrored height
loop. S021.
Source context since previous assertion:
  ;
      for (int height = 1; height <= captionHeight + 4; ++height) {
          const std::optional<QFont> fitted = typography::fitted(body, height);
          if (!fitted)
              continue;
          const int size = QFontInfo(*fitted).pixelSize();
          const QFontMetrics metrics(*fitted);
Original expression:
  QVERIFY(size <= captionInfo.pixelSize());

A041 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:122
Disposition: PARTIAL
Mapping/reason: S021/S022 prove bounds and maximality only: the Swift fit keeps
1 <= fitted <= face size with one pixel larger overflowing the height across
the mirrored loop (TypographyLayoutChecks.swift:250-263). Unproved: the
per-height ascent-plus-descent fit itself -- SGFontExtents exposes no descent
for the per-height expression.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(metrics.ascent() + metrics.descent() <= height);

A042 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:127
Disposition: MATCHED
Mapping/reason: Maximality executes in Swift: one pixel larger overflows the
height across the mirrored height loop. S022.
Source context since previous assertion:
  ;
          if (size < captionInfo.pixelSize()) {
              auto larger = *fitted;
              larger.setPixelSize(size + 1);
              const QFontMetrics largerMetrics(larger);
Original expression:
  QVERIFY(largerMetrics.ascent() + largerMetrics.descent() > height);

A043 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:130
Disposition: GAP
Mapping/reason: feature not yet ported: missing-face branch of
typography::fitted (std::nullopt at height 0); Swift has an owned-but-different
floor at the minimum size instead (S023, TypographyLayoutChecks.swift:265-267).
Source context since previous assertion:
  ;
          }
      }
Original expression:
  QVERIFY(!typography::fitted(body, 0).has_value());

A044 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:140
Disposition: MATCHED
Mapping/reason: The note-name family executes as the Next key-label face. S011.
Source context since previous assertion:
  ;

      const QFont noteName = typography::noteName(body);
      const QFontInfo noteNameInfo(noteName);
      const QFontMetrics noteNameMetrics(noteName);
  #ifdef Q_OS_MACOS
      const QString expectedNoteNameStyle = QStringLiteral("Regular");
  #else
      const QString expectedNoteNameStyle = QStringLiteral("SemiBold");
  #endif
Original expression:
  QCOMPARE(noteNameInfo.family(), QStringLiteral("Atkinson Hyperlegible Next"));

A045 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:141
Disposition: MATCHED
Mapping/reason: The macOS Regular style executes as Normal weight 400 on the
key-label face (this lane runs on macOS). S011.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(noteNameInfo.styleName(), expectedNoteNameStyle);

A046 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:142
Disposition: MATCHED
Mapping/reason: The note size equals the caption size: the key-label face
stays Next at the base size. S011.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(noteNameInfo.pixelSize(), captionInfo.pixelSize());

A047 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:143
Disposition: NATIVE
Mapping/reason: Original native font-metric obligation: the note-name
ascent plus descent must fit the caption height, measured with native font
metrics.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(noteNameMetrics.ascent() + noteNameMetrics.descent() <= captionHeight);

A048 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:147
Disposition: MATCHED
Mapping/reason: A smaller already-fitting face still fits: the canvas fit
keeps the full size at a generous height. S020.
Source context since previous assertion:
  ;
      auto smallerNoteName = noteName;
      smallerNoteName.setPixelSize(noteNameInfo.pixelSize() - 2);
      const std::optional<QFont> fittedNoteName = typography::fitted(smallerNoteName, captionHeight);
Original expression:
  QVERIFY(fittedNoteName.has_value());

A049 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:148
Disposition: MATCHED
Mapping/reason: An already-fitting face comes back unchanged: the canvas fit
keeps the full size at a generous height. S020.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(QFontInfo(*fittedNoteName).pixelSize(), QFontInfo(smallerNoteName).pixelSize());

A050 | ThemeLayoutFontTest::fittedTypographyContracts | src/checks/themelayout/tst_themelayout_font.cpp:154
Disposition: MATCHED
Mapping/reason: Selected-title centering executes in QML: the band publishes
the normal-minus-bold tight-box delta and the title renders translated with
coinciding centers, mirroring the original probe string shape. S032.
Source context since previous assertion:
  ;

      const QFont selected = typography::bold(body);
      const QString text = QStringLiteral("1 · Tést g̦");
      const QRectF referenceBounds = QFontMetricsF(body).tightBoundingRect(text);
      const QRectF displayedBounds = QFontMetricsF(selected).tightBoundingRect(text);
Original expression:
  QCOMPARE(
          displayedBounds.translated(typography::glyphCenteringOffset(body, selected, text)).center(),
          referenceBounds.center());

A051 | ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont | src/checks/themelayout/tst_themelayout_font.cpp:161
Disposition: MATCHED
Mapping/reason: The installed body font executes in QML: the production
window resolves Next and body text renders it. S024, S031.
Source context since previous assertion:
  ;
  }

  void ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont()
  {
Original expression:
  QVERIFY(typography::bodyFont().has_value());

A052 | ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont | src/checks/themelayout/tst_themelayout_font.cpp:165
Disposition: NATIVE
Mapping/reason: QWidget window-visibility mechanics: no QWidget layer exists
in the Swift app. The observable behavior (body text adopts the Next family
once mounted) is covered by S031.
Source context since previous assertion:
  ;
      QLabel probe(QStringLiteral("probe"));
      probe.show();
      probe.ensurePolished();
Original expression:
  QTRY_VERIFY(probe.isVisible());

A053 | ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont | src/checks/themelayout/tst_themelayout_font.cpp:166
Disposition: MATCHED
Mapping/reason: The Next family before any reset executes in QML: the
resolved root font carries Next with no reset step in the way. S024.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(QFontInfo(probe.font()).family(), QStringLiteral("Atkinson Hyperlegible Next"));

A054 | ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont | src/checks/themelayout/tst_themelayout_font.cpp:171
Disposition: PARTIAL
Mapping/reason: S024/S031 prove the restored-Next end-state only: the root font
holds Next through the bundled-loader binding. Unproved: the imperative
external-reset sequence -- the declarative shell has no apply step, so the
restore-after-reset transition has no counterpart.
Source context since previous assertion:
  ;
      auto reset = QApplication::font();
      reset.setFamily(QStringLiteral("Atkinson Hyperlegible Mono"));
      QApplication::setFont(reset);
      themes::apply(*m_application, themes::vanilla());
Original expression:
  QCOMPARE(QFontInfo(QApplication::font()).family(),
           QStringLiteral("Atkinson Hyperlegible Next"));

A055 | ThemeLayoutFontTest::themeApplyRestoresExternallyResetFont | src/checks/themelayout/tst_themelayout_font.cpp:173
Disposition: PARTIAL
Mapping/reason: S031 proves the probe end-state only: body text shows Next
through the root-font binding. Unproved: the imperative external-reset
sequence -- no reset is sabotaged and the declarative shell has no apply step,
so the restore-after-reset transition has no counterpart.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(QFontInfo(probe.font()).family(), QStringLiteral("Atkinson Hyperlegible Next"));

Remaining original source (closing sequence and helper bodies):
  }

Swift assertion predicates

S001 | typographyLayoutCheckScaleTables | src/checks/themelayout/TypographyLayoutChecks.swift:52
Swift expression:
  report.expectEqual(
          typographyLayoutSpaceExpected[lane][token], fontPx(base, multiplier), ...)

S002 | typographyLayoutCheckBasePropagation | src/checks/themelayout/TypographyLayoutChecks.swift:90
Swift expression:
  report.expect(
          again.baseFontPx == metrics.baseFontPx && again.spaceHalf == metrics.spaceHalf &&
              again.spaceTwo == metrics.spaceTwo, ...)

S003 | typographyLayoutCheckBasePropagation | src/checks/themelayout/TypographyLayoutChecks.swift:80
Swift expression:
  report.expectEqual(
          base, metrics.baseFontPx, cppID: typographyLayoutBaseID,
          what: "the lane base reaches the grid metrics unchanged")

S004 | typographyLayoutCheckBasePropagation | src/checks/themelayout/TypographyLayoutChecks.swift:105
Swift expression:
  report.expectEqual(
          seeded, EditorDrawerMetrics.resolve(baseFontPx: degenerate, appFontLineSpacing: 0), ...)

S005 | typographyLayoutCheckBasePropagation | src/checks/themelayout/TypographyLayoutChecks.swift:110
Swift expression:
  report.expectEqual(
          seed, VelocityPage(baseFontPx: 0).baseFontPx, ...)

S006 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:127
Swift expression:
  report.expect(
          sig?.family == "Atkinson Hyperlegible Next" &&
              sig?.pixelSize == Int(typographyLayoutBodyPxExpected[lane]), ...)

S007 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:121
Swift expression:
  report.expect(
          chip?.family == "Atkinson Hyperlegible Next" && chip?.pixelSize == Int(base) &&
              chip?.weight == 400, ...)

S008 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:133
Swift expression:
  report.expect(
          ruler?.family == "Atkinson Hyperlegible Mono" &&
              ruler?.pixelSize == Int(typographyLayoutRulerPxExpected[lane]) &&
              ruler?.weight == 400, ...)

S009 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:140
Swift expression:
  report.expect(
          beat?.family == "Atkinson Hyperlegible Mono" &&
              beat?.pixelSize == Int(base) && beat?.weight == 400, ...)

S010 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:146
Swift expression:
  report.expect(
          bold?.family == "Atkinson Hyperlegible Mono" &&
              bold?.pixelSize == Int(typographyLayoutRulerPxExpected[lane]) &&
              bold?.weight == 600, ...)

S011 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:153
Swift expression:
  report.expect(
          keyLabel?.family == "Atkinson Hyperlegible Next" &&
              keyLabel?.pixelSize == Int(base) && keyLabel?.weight == 400, ...)

S012 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:159
Swift expression:
  report.expect(
          abs(spacing - base * (-1.0 / 24.0)) < 1e-9, ...)

S013 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:174
Swift expression:
  report.expect(
          resolved.barHeight == pin.barHeight && resolved.handleHeight == pin.handleHeight &&
              resolved.minimumBody == pin.minimumBody && ..., ...)

S014 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:212
Swift expression:
  report.expect(
          chip.features["tnum"] as? Int == 1, ...)

S015 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:219
Swift expression:
  report.expect(
          emissionMatches, ...,
          message: "the QML map emits the tabular feature")

S016 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:226
Swift expression:
  report.expect(
          faces[kind]?.features["tnum"] as? Int == 1, ...,
          message: "the mono face enables tabular figures")

S017 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:231
Swift expression:
  report.expectEqual(
          measured.advance("1"), measured.advance("8"), ...,
          what: "single digits share one advance")

S018 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:234
Swift expression:
  report.expectEqual(
          measured.advance("111"), measured.advance("777"), ...,
          what: "equal-length digit runs share one advance")

S019 | typographyLayoutCheckTabularFeatures | src/checks/themelayout/TypographyLayoutChecks.swift:237
Swift expression:
  report.expectEqual(
          measured.advance("2026"), measured.advance("1975"), ...,
          what: "year-like digit runs share one advance")

S020 | typographyLayoutCheckFittedMaximality | src/checks/themelayout/TypographyLayoutChecks.swift:250
Swift expression:
  report.expectEqual(
          16, full.fittedSize(rowHeight: fullHeight), ...,
          what: "a generous height keeps the full face size")

S021 | typographyLayoutCheckFittedMaximality | src/checks/themelayout/TypographyLayoutChecks.swift:255
Swift expression:
  report.expect(
          fitted >= 1 && fitted <= 16, ...,
          message: "the fit at height \(height) stays within the face size")

S022 | typographyLayoutCheckFittedMaximality | src/checks/themelayout/TypographyLayoutChecks.swift:260
Swift expression:
  report.expect(
          larger.extents.height > Double(height), ...,
          message: "one pixel larger overflows height \(height)")

S023 | typographyLayoutCheckFittedMaximality | src/checks/themelayout/TypographyLayoutChecks.swift:265
Swift expression:
  report.expectEqual(
          1, full.fittedSize(rowHeight: 0), ...,
          what: "a zero height floors at the minimum size")

S024 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:104
QML expression:
  tryVerify(function() {
      return shell.font.family === "Atkinson Hyperlegible Next"
  }, 5000, "the resolved base font carries the bundled Next family")

S025 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:107
QML expression:
  compare(shell.font.pixelSize, shell.bodyFontPx,
          "the base font resolves to the threaded body size")

S026 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:109
QML expression:
  compare(shell.font.hintingPreference, Font.PreferNoHinting,
          "the base font prefers no hinting")

S027 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:111
QML expression:
  compare(shell.font.weight, Font.Normal, "the base font keeps Normal weight")

S028 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:112
QML expression:
  compare(shell.font.features["tnum"], 1, "the base font enables tabular figures")

S029 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:114
QML expression:
  compare(shell.width, shell.bodyFontPx * 72,
          "window width threads the body size into geometry")

S030 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:116
QML expression:
  compare(shell.height, shell.bodyFontPx * 48,
          "window height threads the body size into geometry")

S031 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:121
QML expression:
  compare(label.font.family, "Atkinson Hyperlegible Next",
          "body text shows the restored Next family")

S032 | test_selectedTitleCentering | src/checks/editorqml/tst_Typography.qml:134
QML expression:
  tryVerify(function() { ... centersClose(displayed, reference) ... }, 5000,
      "the bold title stays centered on the regular title")

S033 | test_selectedTitleCentering | src/checks/editorqml/tst_Typography.qml:166
QML expression:
  compare(row.titleFont.features["tnum"], 1,
          "the title map emits tabular figures")

S034 | test_bodyFontAndMetrics | src/checks/editorqml/tst_Typography.qml:101
QML expression:
  compare(Qt.application.organization, "sp3cker",
          "the shell keeps the production organization identity")

S035 | typographyLayoutCheckBasePropagation | src/checks/themelayout/TypographyLayoutChecks.swift:97
Swift expression:
  report.expectEqual(13.0, seed, ...) // grid, velocity and automation seeds agree

S036 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:185
Swift expression:
  for kind in [GridFontKind.ruler, GridFontKind.beat, GridFontKind.bold, GridFontKind.sig, GridFontKind.chip, GridFontKind.keyLabel] {
      report.expect(
              unhinted[kind]?.map["hintingPreference"] as? Int == fontPreferNoHinting, ...)

S037 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:190
Swift expression:
  report.expect(
          VelocityScene.fontMap(emphasized: false, typography: nil, baseFontPx: 16)["hintingPreference"] as? Int == fontPreferNoHinting, ...)

S038 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:194
Swift expression:
  report.expect(
          AutomationCaption(pixelSize: 16, weight: 400).fontMap["hintingPreference"] as? Int == fontPreferNoHinting, ...)

S039 | typographyLayoutCheckFaceContracts | src/checks/themelayout/TypographyLayoutChecks.swift:198
Swift expression:
  report.expect(
          VoiceCaption(pixelSize: 16, weight: 400).fontMap["hintingPreference"] as? Int == fontPreferNoHinting, ...)

S040 | test_selectedTitleCentering | src/checks/editorqml/tst_Typography.qml:171
QML expression:
  compare(rendered.font.hintingPreference, Font.PreferNoHinting,
          "the rendered title carries the unhinted preference")

Shared helper
Suite entry runTypographyLayoutChecks wires the scale, base-propagation,
face-contract, tabular-feature and fitted-maximality checks into
runProjectSessionSuite; the shell-typography QML entry observes the
production window. No shared fixture file carries expects for this suite.
