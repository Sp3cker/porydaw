Assertion correspondence: themelayout/tst_themelayout_color.cpp

Reference revision: 97dc7fea819cb7c4a97a0dac819244ee570e993f
Original: src/checks/themelayout/tst_themelayout_color.cpp
Original SHA-256: a594fe67298004f6cbb8bc76f8dce1a2173b91b408a0b5b9b3414c78ec72fbaf
Swift counterpart: src/checks/themecolor/ThemeColorChecks.swift
Swift SHA-256: 33fdddcda2114a17a7047c58cab7ab7c062647b3e4e720d50ace2e2b858089a1
Additional Swift counterpart: src/checks/editorqml/tst_Theme.qml
Additional Swift SHA-256: c99a86a08b0e926ebc93d63e14f6b3e4d3d6f9f6407c9c158611eb619eb37db9

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site in this file appears below, including the
file-local helper guard and the menu/control contract helper whose sites fan
out through themeCompleteness. The file exercises color math (Oklab and
relative luminance against closed-form references), per-theme completeness and
role contracts, grid-line contrast adjustment, lane/waveform legibility floors,
and ThemeController settings repair/persistence. Source context preserves
setup, data rows, loops, and guards between assertions verbatim.
MATCHED means a corresponding Swift predicate was identified and executes;
PARTIAL means a related Swift predicate exists with a stated unproved condition;
GAP means no equivalent Swift assertion is established;
NATIVE means the site observes native harness state a Swift check cannot
establish (here: the two QTemporaryDir validity guards). Disposition tally:
50 sites: 47 MATCHED, 1 PARTIAL, 0 GAP, 2 NATIVE. There is no QWidget layer
in the Swift app, now or later: where a role has no Swift control, the
shared-fill or legibility observable is asserted on the live Swift controls
instead, and the mapping reason says so.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: PASS swiftcore (1/24 ok); SwiftCoreTest::themeColor PASS, 0 failed.
Direct slot run: build/porydaw_checks --swiftcore <scratch> build/mid2agb --qt themeColor
  -> PASS SwiftCoreTest::themeColor(), 3 passed, 0 failed.
Command: deno task verify:shell --filter shell-theme --verbose
Result: PASS shell-theme (1/2 ok, 1 skipped); Finished testing of Theme.
Command: deno task verify:shell --filter shellwindow --verbose
Result: PASS shellwindow (1/2 ok, 1 skipped); Finished testing of ShellWindow.
All cases in this file execute under themecheck via runThemeLayoutThemeCheck;
the Swift predicates execute under swiftcore suite 12 (PDC_SUITE_THEME_COLOR)
and the shell-theme QML entry.

Execution and ownership
Native ownership: runThemeLayoutThemeCheck (defined in
src/checks/themelayout/themelayoutcheck.cpp) launches the themecheck lane;
ThemeLayoutTest::init applies the vanilla theme and saves font/palette/
stylesheet for every case. Swift ownership: runThemeColorChecks
(src/checks/themecolor/ThemeColorChecks.swift:67, wired via
CoreCheckSupport.swift pdcSuiteRun case 12 and SwiftCoreTest::themeColor)
covers the pure value rules; the shell-theme entry
(src/checks/editorqml/ShellQmlTests.swift entries table, tst_Theme.qml)
covers persisted settings and applied chrome through the production
ShellWindow. Suite declarations live in src/checks/themelayout/tst_themelayout.h.

Fixture, sequence and row constraints
colorMath expands A019-A022 over 9 color rows (black, white, mid gray, pure
RGB primaries, and three production tones). themeCompleteness expands A002-A018
(the contract helper) plus A023-A030 over 3 theme rows (vanilla,
dark-neutral-high, immaterial); A028-A030 additionally guard on the row's
theme name. gridContrast expands A031-A038 over 9 rows (3 themes x contrast
0/50/100) with branch guards: the defaultGridLineContrast row asserts identity
and returns (A032); contrast 0 asserts reduced contrast and alpha (A033, A035);
nonzero asserts increased contrast (A034) and alpha (A036); contrast 100
additionally asserts the luminance moved away from the background (A037/A038).
laneAndWaveformLegibility expands A039 over 24 rows (3 themes x 8
role/surface pairs) with a 3.0 contrast floor. The last four entries expand
over 3 mode rows. settingsRepair has no rows.
Original setup, branch guards, and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory

A001 | themeForName (file-local helper) | src/checks/themelayout/tst_themelayout_color.cpp:31
Disposition: MATCHED
Mapping/reason: Swift predicate S002 asserts the same repair rule: ShellAppearance.mode maps the unknown "custom" (and empty) mode to vanilla instead of substituting a wrong theme (ThemeColorChecks.swift:122).
Source context since previous assertion:
  #include "checks/themelayout/tst_themelayout.h"

  #include "ui/theme/color_math.h"
  #include "ui/theme/themecontroller.h"

  #include "ui/theme/themeresolver.h"

  #include <QColor>
  #include <QSettings>
  #include <QTemporaryDir>
  #include <QtTest>

  #include <cstddef>

  #include <array>
  #include <cmath>
  #include <utility>

  namespace {

  constexpr auto kContrastEpsilon = 1e-12;

  const themes::Theme themeForName(const QString &name)
  {
      if (name == QStringLiteral("vanilla"))
          return themes::vanilla();
      if (name == QStringLiteral("dark-neutral-high"))
          return themes::darkNeutralHigh();
      if (name == QStringLiteral("immaterial"))
          return themes::immaterial();
Original expression:
  QTest::qFail(qPrintable(QStringLiteral("unknown theme name: %1").arg(name)), __FILE__,
               __LINE__);

A002 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:49
Disposition: MATCHED
Mapping/reason: Swift predicate S003 asserts the same 4.5 floor on the applied Swift pair windowText/chromeBackground for every theme row; S010 observes the pair on the production shell (tst_Theme.qml:119).
Source context since previous assertion:
  ;
      return themes::vanilla();
  }

  bool completeTheme(const themes::Theme &theme)
  {
      for (std::size_t index = 0; index < theme.colors.size(); ++index) {
          const QColor color = theme.colors[index];
          const auto role = static_cast<themes::Role>(index);
          if (!color.isValid() || (role != themes::Role::song_view_grid && color.alpha() != 255))
              return false;
      }
      return true;
  }

  void verifyMenuAndControlContracts(const themes::Theme &theme)
  {
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::menu_bar_text),
                                theme.color(themes::Role::menu_bar_background)) >= 4.5);

A003 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:51
Disposition: MATCHED
Mapping/reason: Swift predicate S003 asserts the same 4.5 floor on windowText/buttonHoverBackground for every theme row (ThemeColorChecks.swift:241).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::button_hover_text),
                                theme.color(themes::Role::button_hover_background)) >= 4.5);

A004 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:53
Disposition: MATCHED
Mapping/reason: Swift predicate S003 asserts the same 4.5 floor on buttonPressedText/buttonPressedBackground for every theme row; S010 observes the pressed pair on the production shell (tst_Theme.qml:119).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::button_pressed_text),
                                theme.color(themes::Role::button_pressed_background)) >= 4.5);

A005 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:55
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so the combo drop-down lane has no counterpart; the shared pressed-fill observable is covered by S003, which pins buttonPressedBackground == tabPressedBackground on every theme row.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::combo_drop_down_pressed_background),
           theme.color(themes::Role::button_pressed_background));

A006 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:57
Disposition: MATCHED
Mapping/reason: S003 pins menuBackground to the item literal on every theme row, and S010 observes the resting production menu item showing that surface (tst_Theme.qml:193).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::menu_background),
           theme.color(themes::Role::item_background));

A007 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:59
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app; menu/item text resolve to window_text, whose value S003 pins and whose menu rendering S010 observes through the delegate foreground binding (tst_Theme.qml:193).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::menu_text), theme.color(themes::Role::item_text));

A008 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:60
Disposition: MATCHED
Mapping/reason: S003 pins menuHoverBackground to the item-hover literal, and S010 observes the highlighted production menu item showing that surface (tst_Theme.qml:193).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::menu_item_hover_background),
           theme.color(themes::Role::item_hover_background));

A009 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:62
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app; menu/item hover text resolve to window_text, whose value S003 pins and whose menu rendering S010 observes through the delegate foreground binding (tst_Theme.qml:193).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::menu_item_hover_text),
           theme.color(themes::Role::item_hover_text));

A010 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:64
Disposition: MATCHED
Mapping/reason: Swift predicate S003 asserts the same 4.5 floor on windowText/menuHoverBackground for every theme row (ThemeColorChecks.swift:241).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::menu_item_hover_text),
                                theme.color(themes::Role::menu_item_hover_background)) >= 4.5);

A011 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:66
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so the splitter handle has no counterpart; the shared pressed-fill observable is covered by S003, which pins buttonPressedBackground on every theme row.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::splitter_handle_hover_background),
           theme.color(themes::Role::button_pressed_background));

A012 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:68
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so input/combo surfaces have no counterpart; the shared control-hover value is pinned by S003 (buttonHoverBackground literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::input_background),
           theme.color(themes::Role::combo_background));

A013 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:70
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so input/combo text has no counterpart; the shared window_text value is pinned by S003 (buttonText literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::input_text), theme.color(themes::Role::combo_text));

A014 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:71
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so input/combo outlines have no counterpart; the shared outline value is pinned by S003 (outline literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::input_outline), theme.color(themes::Role::combo_outline));

A015 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:72
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so spin-box/combo surfaces have no counterpart; the shared control value is pinned by S003 (buttonBackground literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::spin_box_background),
           theme.color(themes::Role::combo_background));

A016 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:74
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so spin-box/combo text has no counterpart; the shared window_text value is pinned by S003 (buttonText literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::spin_box_text), theme.color(themes::Role::combo_text));

A017 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:75
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so spin-box/combo outlines have no counterpart; the shared outline value is pinned by S003 (outline literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::spin_box_outline), theme.color(themes::Role::combo_outline));

A018 | verifyMenuAndControlContracts (file-local helper, via ThemeLayoutTest::themeCompleteness) | src/checks/themelayout/tst_themelayout_color.cpp:76
Disposition: MATCHED
Mapping/reason: Swift predicate S003 asserts the same 4.5 floor on buttonText/buttonHoverBackground for every theme row (ThemeColorChecks.swift:241).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::combo_text),
                                theme.color(themes::Role::combo_background)) >= 4.5);

A019 | ThemeLayoutTest::colorMath | src/checks/themelayout/tst_themelayout_color.cpp:116
Disposition: MATCHED
Mapping/reason: S001 judges PaletteMath.oklab lightness against the independent closed-form reference over the same nine color rows (ThemeColorChecks.swift:81).
Source context since previous assertion:
  ;
  }

  double srgbToLinearReference(double component)
  {
      return component <= 0.04045 ? component / 12.92 : std::pow((component + 0.055) / 1.055, 2.4);
  }

  themes::Oklab oklabReference(const QColor &color)
  {
      const QColor rgb = color.toRgb();
      const double red = srgbToLinearReference(static_cast<double>(rgb.red()) / 255.0);
      const double green = srgbToLinearReference(static_cast<double>(rgb.green()) / 255.0);
      const double blue = srgbToLinearReference(static_cast<double>(rgb.blue()) / 255.0);
      const double l = std::cbrt(0.4122214708 * red + 0.5363325363 * green + 0.0514459929 * blue);
      const double m = std::cbrt(0.2119034982 * red + 0.6806995451 * green + 0.1073969566 * blue);
      const double s = std::cbrt(0.0883024619 * red + 0.2817188376 * green + 0.6299787005 * blue);
      return {0.2104542553 * l + 0.7936177850 * m - 0.0040720468 * s,
              1.9779984951 * l - 2.4285922050 * m + 0.4505937099 * s,
              0.0259040371 * l + 0.7827717662 * m - 0.8086757660 * s};
  }

  } // namespace

  void ThemeLayoutTest::colorMath_data()
  {
      QTest::addColumn<QColor>("color");
      const std::array colors{QColor(0, 0, 0),     QColor(255, 255, 255), QColor(128, 128, 128),
                              QColor(255, 0, 0),   QColor(0, 255, 0),     QColor(0, 0, 255),
                              QColor(24, 88, 192), QColor(200, 150, 50),  QColor(33, 33, 33)};
      for (const QColor &color : colors)
          QTest::newRow(color.name().toLatin1().constData()) << color;
  }

  void ThemeLayoutTest::colorMath()
  {
      QFETCH(QColor, color);
      const themes::Oklab actual = themes::oklabFromColor(color);
      const themes::Oklab expected = oklabReference(color);
Original expression:
  QVERIFY(std::abs(actual.lightness - expected.lightness) < kContrastEpsilon);

A020 | ThemeLayoutTest::colorMath | src/checks/themelayout/tst_themelayout_color.cpp:117
Disposition: MATCHED
Mapping/reason: S001 judges the Oklab a channel against the independent reference over the same nine rows.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(std::abs(actual.a - expected.a) < kContrastEpsilon);

A021 | ThemeLayoutTest::colorMath | src/checks/themelayout/tst_themelayout_color.cpp:118
Disposition: MATCHED
Mapping/reason: S001 judges the Oklab b channel against the independent reference over the same nine rows.
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(std::abs(actual.b - expected.b) < kContrastEpsilon);

A022 | ThemeLayoutTest::colorMath | src/checks/themelayout/tst_themelayout_color.cpp:124
Disposition: MATCHED
Mapping/reason: S001 judges PaletteMath.relativeLuminance against the Rec.709 reference over the same nine rows, and checks contrastRatio against the reference on three production pairs.
Source context since previous assertion:
  ;
      const QColor rgb = color.toRgb();
      const double expectedLuminance =
          0.2126 * srgbToLinearReference(static_cast<double>(rgb.red()) / 255.0) +
          0.7152 * srgbToLinearReference(static_cast<double>(rgb.green()) / 255.0) +
          0.0722 * srgbToLinearReference(static_cast<double>(rgb.blue()) / 255.0);
Original expression:
  QVERIFY(std::abs(themes::relativeLuminance(color) - expectedLuminance) < kContrastEpsilon);

A023 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:140
Disposition: MATCHED
Mapping/reason: S004 sweeps every exposed GridPalette field for the row theme: valid hex, fully opaque except the by-design translucent grid/selection/hover-chip set (ThemeColorChecks.swift:222).
Source context since previous assertion:
  ;
  }

  void ThemeLayoutTest::themeCompleteness_data()
  {
      QTest::addColumn<QString>("themeName");
      const std::array names{QStringLiteral("vanilla"), QStringLiteral("dark-neutral-high"),
                             QStringLiteral("immaterial")};
      for (const QString &name : names)
          QTest::newRow(name.toLatin1().constData()) << name;
  }

  void ThemeLayoutTest::themeCompleteness()
  {
      QFETCH(QString, themeName);
      const themes::Theme theme = themeForName(themeName);
Original expression:
  QVERIFY(completeTheme(theme));

A024 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:142
Disposition: MATCHED
Mapping/reason: S003 asserts the 1.3 disabled-text floor on the applied palette; S010 observes the disabled pair on the production shell (tst_Theme.qml:119).
Source context since previous assertion:
  ;
      verifyMenuAndControlContracts(theme);
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(themes::Role::disabled_text),
                                theme.color(themes::Role::window_text)) >= 1.3);

A025 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:144
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so the combo lane has no counterpart; the shared hover-fill value is pinned by S003 (buttonHoverBackground literal).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::combo_drop_down_hover_background),
           theme.color(themes::Role::button_hover_background));

A026 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:146
Disposition: MATCHED
Mapping/reason: S003 asserts focusOutline == outline on the applied palette directly for every theme row.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::focus_outline), theme.color(themes::Role::palette_outline));

A027 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:147
Disposition: MATCHED
Mapping/reason: No QWidget layer exists in the Swift app, so tab-pane/toolbar roles have no counterpart; the shared chrome value is pinned by S003 (chromeBackground literal) with tab/button surface sharing asserted.
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(theme.color(themes::Role::tab_pane_background),
           theme.color(themes::Role::toolbar_background));

A028 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:150
Disposition: MATCHED
Mapping/reason: S003 pins the vanilla grid role to (0x04,0x00,0x00,0x3F) via the gridLine literal.
Source context since previous assertion:
  ;
      if (themeName == QStringLiteral("vanilla"))
Original expression:
  QCOMPARE(theme.color(themes::Role::song_view_grid),
           QColor::fromRgb(0x04, 0x00, 0x00, 0x3F));

A029 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:153
Disposition: MATCHED
Mapping/reason: S003 pins the dark-neutral-high grid role to (0x03,0x03,0x03,0x54) via the gridLine literal.
Source context since previous assertion:
  ;
      if (themeName == QStringLiteral("dark-neutral-high"))
Original expression:
  QCOMPARE(theme.color(themes::Role::song_view_grid),
           QColor::fromRgb(0x03, 0x03, 0x03, 0x54));

A030 | ThemeLayoutTest::themeCompleteness | src/checks/themelayout/tst_themelayout_color.cpp:156
Disposition: MATCHED
Mapping/reason: S003 pins the immaterial grid role to (0x03,0x06,0x06,0x54) via the gridLine literal.
Source context since previous assertion:
  ;
      if (themeName == QStringLiteral("immaterial"))
Original expression:
  QCOMPARE(theme.color(themes::Role::song_view_grid),
           QColor::fromRgb(0x03, 0x06, 0x06, 0x54));

A031 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:180
Disposition: MATCHED
Mapping/reason: S005 runs the completeness sweep on the contrast-adjusted palette for every theme/contrast row (ThemeColorChecks.swift:348).
Source context since previous assertion:
  ;
  }

  void ThemeLayoutTest::gridContrast_data()
  {
      QTest::addColumn<QString>("themeName");
      QTest::addColumn<int>("contrast");
      const std::array names{QStringLiteral("vanilla"), QStringLiteral("dark-neutral-high"),
                             QStringLiteral("immaterial")};
      for (const QString &name : names) {
          for (const int contrast : {0, 50, 100})
              QTest::newRow(
                  (name + QStringLiteral("-") + QString::number(contrast)).toLatin1().constData())
                  << name << contrast;
      }
  }

  void ThemeLayoutTest::gridContrast()
  {
      QFETCH(QString, themeName);
      QFETCH(int, contrast);
      const themes::Theme theme = themeForName(themeName);
      const themes::Theme adjusted = themes::withGridLineContrast(theme, contrast);
Original expression:
  QVERIFY(completeTheme(adjusted));

A032 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:185
Disposition: MATCHED
Mapping/reason: S005 asserts default-contrast identity; S009 observes the pinned grid restored on the production shell (tst_Theme.qml:157).
Source context since previous assertion:
  ;
      const QColor originalGrid = theme.color(themes::Role::song_view_grid);
      const QColor adjustedGrid = adjusted.color(themes::Role::song_view_grid);
      const QColor background = theme.color(themes::Role::song_view_piano_roll_background);
      if (contrast == themes::defaultGridLineContrast) {
Original expression:
  QCOMPARE(adjustedGrid, originalGrid);

A033 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:191
Disposition: MATCHED
Mapping/reason: S005 asserts contrast 0 loses grid contrast against the background (independent reference); S009 observes it on the production shell.
Source context since previous assertion:
  ;
          return;
      }
      const double originalContrast = themes::contrastRatio(originalGrid, background);
      const double adjustedContrast = themes::contrastRatio(adjustedGrid, background);
      if (contrast == 0)
Original expression:
  QVERIFY(adjustedContrast < originalContrast);

A034 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:193
Disposition: MATCHED
Mapping/reason: S005 asserts nonzero contrast gains grid contrast; S009 observes it on the production shell.
Source context since previous assertion:
  ;
      else
Original expression:
  QVERIFY(adjustedContrast > originalContrast);

A035 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:195
Disposition: MATCHED
Mapping/reason: S005 asserts contrast 0 lowers grid alpha; S009 observes the lowered alpha on the production shell.
Source context since previous assertion:
  ;
      if (contrast == 0)
Original expression:
  QVERIFY(adjustedGrid.alpha() < originalGrid.alpha());

A036 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:197
Disposition: MATCHED
Mapping/reason: S005 asserts nonzero contrast raises grid alpha; S009 observes the raised alpha on the production shell.
Source context since previous assertion:
  ;
      else
Original expression:
  QVERIFY(adjustedGrid.alpha() > originalGrid.alpha());

A037 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:203
Disposition: MATCHED
Mapping/reason: S005 asserts the contrast-100 luminance moves darker away from the background on dark-side rows.
Source context since previous assertion:
  ;
      if (contrast == 100) {
          const double originalLuminance = themes::relativeLuminance(originalGrid);
          const double backgroundLuminance = themes::relativeLuminance(background);
          const double adjustedLuminance = themes::relativeLuminance(adjustedGrid);
          if (originalLuminance <= backgroundLuminance)
Original expression:
  QVERIFY(adjustedLuminance < originalLuminance);

A038 | ThemeLayoutTest::gridContrast | src/checks/themelayout/tst_themelayout_color.cpp:205
Disposition: MATCHED
Mapping/reason: S005 asserts the contrast-100 luminance moves lighter away from the background on light-side rows.
Source context since previous assertion:
  ;
          else
Original expression:
  QVERIFY(adjustedLuminance > originalLuminance);

A039 | ThemeLayoutTest::laneAndWaveformLegibility | src/checks/themelayout/tst_themelayout_color.cpp:245
Disposition: PARTIAL
Mapping/reason: S003 asserts the same 3.0 floor for the three live-editor pairs per theme row (ThemeColorChecks.swift:333-339: edit-preview/roll, add-lane/roll, selected-tab/active-automation). feature not yet ported: sample editor (sample_waveform_ink, sample_crop_handle, sample_loop_handle x2, sample_seam_end_ink); the five sample-editor pairs have no Swift owner -- no sample-editor surface exists in the Swift app.
Source context since previous assertion:
  ;
      }
  }

  void ThemeLayoutTest::laneAndWaveformLegibility_data()
  {
      QTest::addColumn<QString>("themeName");
      QTest::addColumn<int>("role");
      QTest::addColumn<int>("surface");
      const std::array names{QStringLiteral("vanilla"), QStringLiteral("dark-neutral-high"),
                             QStringLiteral("immaterial")};
      const std::array checks{
          std::pair{themes::Role::song_view_edit_preview_outline,
                    themes::Role::song_view_piano_roll_background},
          std::pair{themes::Role::song_view_add_automation_lane_action,
                    themes::Role::song_view_piano_roll_background},
          std::pair{themes::Role::tab_selected_text,
                    themes::Role::song_view_automation_tab_active_background},
          std::pair{themes::Role::sample_waveform_ink, themes::Role::item_background},
          std::pair{themes::Role::sample_crop_handle, themes::Role::item_background},
          std::pair{themes::Role::sample_loop_handle, themes::Role::item_background},
          std::pair{themes::Role::sample_loop_handle, themes::Role::item_alternate_background},
          std::pair{themes::Role::sample_seam_end_ink, themes::Role::item_alternate_background},
      };
      for (const QString &name : names) {
          for (const auto &[role, surface] : checks) {
              const auto row = name + QStringLiteral("-") + QString::number(static_cast<int>(role)) +
                               QStringLiteral("-") + QString::number(static_cast<int>(surface));
              QTest::newRow(row.toLatin1().constData())
                  << name << static_cast<int>(role) << static_cast<int>(surface);
          }
      }
  }

  void ThemeLayoutTest::laneAndWaveformLegibility()
  {
      QFETCH(QString, themeName);
      QFETCH(int, role);
      QFETCH(int, surface);
      const themes::Theme theme = themeForName(themeName);
Original expression:
  QVERIFY(themes::contrastRatio(theme.color(static_cast<themes::Role>(role)),
                                theme.color(static_cast<themes::Role>(surface))) >= 3.0);

A040 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:252
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/themelayout/tst_themelayout_color.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
must be valid before the repair case writes into it. A presenter check cannot
establish the native temporary directory.
Source context since previous assertion:
  ;
  }

  void ThemeLayoutTest::settingsRepair()
  {
      QTemporaryDir directory;
Original expression:
  QVERIFY(directory.isValid());

A041 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:262
Disposition: MATCHED
Mapping/reason: Covered by tst_ShellWindow.qml test_bThemeRepairThroughProductionShell (tst_ShellWindow.qml:183; see proof.shellwindow.txt:50-56), which seeds custom mode through genuine QtCore.Settings and observes themeMode repair to vanilla. S002 asserts the same validation rule in Swift (S012).
Source context since previous assertion:
  ;
      QSettings settings(directory.filePath(QStringLiteral("settings.ini")), QSettings::IniFormat);
      settings.setValue(QStringLiteral("theme/mode"), QStringLiteral("custom"));
      settings.setValue(QStringLiteral("theme/primary"), QStringLiteral("#000000"));
      settings.setValue(QStringLiteral("theme/accent"), QStringLiteral("#FFFFFF"));
      settings.setValue(QStringLiteral("theme/grid-line-contrast"), QStringLiteral("80"));

      themes::ThemeController controller(*m_application, settings);
      controller.restore();

Original expression:
  QCOMPARE(controller.committedSelection().mode, themes::ThemeMode::Vanilla);

A042 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:263
Disposition: MATCHED
Mapping/reason: Covered by test_bThemeRepairThroughProductionShell, which observes the stored contrast 80 surviving the repair. S002 asserts contrast("80") == 80 in Swift (S012).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(controller.committedSelection().gridLineContrast, 80);

A043 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:264
Disposition: MATCHED
Mapping/reason: Covered by test_bThemeRepairThroughProductionShell, which observes the stored mode rewritten to vanilla (S012).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(settings.value(QStringLiteral("theme/mode")).toString(), QStringLiteral("vanilla"));

A044 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:265
Disposition: MATCHED
Mapping/reason: Covered by test_bThemeRepairThroughProductionShell, which observes the stale theme/primary key dropped (S012).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!settings.contains(QStringLiteral("theme/primary")));

A045 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:266
Disposition: MATCHED
Mapping/reason: Covered by test_bThemeRepairThroughProductionShell, which observes the stale theme/accent key dropped (S012).
Source context since previous assertion:
  ;
Original expression:
  QVERIFY(!settings.contains(QStringLiteral("theme/accent")));

A046 | ThemeLayoutTest::settingsRepair | src/checks/themelayout/tst_themelayout_color.cpp:267
Disposition: MATCHED
Mapping/reason: Covered by test_bThemeRepairThroughProductionShell, which observes the stored contrast reading back as 80 (S012).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(settings.value(QStringLiteral("theme/grid-line-contrast")).toInt(), 80);

A047 | ThemeLayoutTest::themePersistence | src/checks/themelayout/tst_themelayout_color.cpp:287
Disposition: RETIRED-REPRESENTATION
Mapping/reason: Test-harness precondition of the retired native C++ lane (src/checks/themelayout/tst_themelayout_color.cpp, no longer built or run); it asserts only fixture/object/window scaffolding with no consumer-visible behavior, and that harness was its only ingress.
must be valid before the write/read round-trip.
Source context since previous assertion:
  ;
  }

  void ThemeLayoutTest::themePersistence_data()
  {
      QTest::addColumn<int>("mode");
      QTest::addColumn<QString>("storedName");
      QTest::newRow("vanilla") << static_cast<int>(themes::ThemeMode::Vanilla)
                               << QStringLiteral("vanilla");
      QTest::newRow("dark-neutral-high") << static_cast<int>(themes::ThemeMode::DarkNeutralHigh)
                                         << QStringLiteral("dark-neutral-high");
      QTest::newRow("immaterial") << static_cast<int>(themes::ThemeMode::Immaterial)
                                  << QStringLiteral("immaterial");
  }

  void ThemeLayoutTest::themePersistence()
  {
      QFETCH(int, mode);
      QFETCH(QString, storedName);
      QTemporaryDir directory;
Original expression:
  QVERIFY(directory.isValid());

A048 | ThemeLayoutTest::themePersistence | src/checks/themelayout/tst_themelayout_color.cpp:294
Disposition: MATCHED
Mapping/reason: S008 seeds each mode through genuine QtCore.Settings and observes the production shell restore it with canonical writeback (tst_Theme.qml:119); the commit-success observable is the restored plus canonical state.
Source context since previous assertion:
  ;
      const QString settingsPath = directory.filePath(QStringLiteral("settings.ini"));
      QSettings writeSettings(settingsPath, QSettings::IniFormat);
      themes::ThemeController writeController(*m_application, writeSettings);
      writeController.restore();
      const auto themeMode = static_cast<themes::ThemeMode>(mode);
      const themes::ThemeSelection selection{themeMode};
Original expression:
  QVERIFY(writeController.commit(selection));

A049 | ThemeLayoutTest::themePersistence | src/checks/themelayout/tst_themelayout_color.cpp:301
Disposition: MATCHED
Mapping/reason: S008 observes themeMode per mode row on the production shell (tst_Theme.qml:119).
Source context since previous assertion:
  ;
      writeSettings.sync();

      QSettings readSettings(settingsPath, QSettings::IniFormat);
      themes::ThemeController readController(*m_application, readSettings);
      readController.restore();
      const themes::ThemeSelection &restored = readController.committedSelection();
Original expression:
  QCOMPARE(restored.mode, themeMode);

A050 | ThemeLayoutTest::themePersistence | src/checks/themelayout/tst_themelayout_color.cpp:302
Disposition: MATCHED
Mapping/reason: S008 reads back the stored mode string per mode row (tst_Theme.qml:119).
Source context since previous assertion:
  ;
Original expression:
  QCOMPARE(readSettings.value(QStringLiteral("theme/mode")).toString(), storedName);

Remaining original source (closing sequence and helper bodies):
  }

Swift assertion predicates
Each predicate below executes under swiftcore suite 12 (themeColor slot) or
the shell-theme QML entry. Preserve their surrounding setup and order in the
paired source.

S001 | themeColorMathChecks | src/checks/themecolor/ThemeColorChecks.swift:93
Counterpart expression:
  report.expect(abs(actual.lightness - expected.l) < themeColorEpsilon, ...)
  over the same nine color rows, plus contrastRatio against the reference.

S002 | themeModeAndContrastValidation | src/checks/themecolor/ThemeColorChecks.swift:129
Counterpart expression:
  report.expectEqual("vanilla", ShellAppearance.mode("custom"), ...)
  with contrast fallback/clamp rows (themecontroller.cpp:89-104 rules).

S003 | themePresetValueChecks | src/checks/themecolor/ThemeColorChecks.swift:246
Counterpart expression:
  report.expectEqual(row.window, palette.windowBackground, ...)
  per theme row for every applied GridPalette field (:246-281), plus the
  4.5/1.3/3.0 menu/control floors (:315-328) and the three lane-legibility
  3.0 floors (:333-339) judged by the independent reference.

S004 | themeAssertComplete | src/checks/themecolor/ThemeColorChecks.swift:222
Counterpart expression:
  report.expect(opaque, ..., message: themed field opacity)
  sweeping every exposed GridPalette field.

S005 | themeGridContrastChecks | src/checks/themecolor/ThemeColorChecks.swift:348
Counterpart expression:
  report.expect(strengthened grid contrast > base grid contrast, ...)
  with identity, alpha and luminance-direction rows.

S006 | themeTrackIdentityChecks | src/checks/themecolor/ThemeColorChecks.swift:394
Counterpart expression:
  report.expect(onLight || onDark, ..., message: piano-key surface floor)
  over all sixteen identity fills.

S007 | themeCommitPreviewRevertChecks | src/checks/themecolor/ThemeColorChecks.swift:432
Counterpart expression:
  report.expectEqual("#037384", reverted.selectionEdge, ...)
  with commit, preview and determinism rows.

S008 | test_themePersistence | src/checks/editorqml/tst_Theme.qml:126
Counterpart expression:
  tryCompare(shell.shellPresenter, "themeMode", data.mode)
  with canonical settings writeback per mode row (vanilla/50,
  dark-neutral-high/80, immaterial/100).

S009 | test_gridContrastDirection | src/checks/editorqml/tst_Theme.qml:157
Counterpart expression:
  verify(strengthened contrast > baseline contrast, ...)
  with alpha-direction and pinned-value restoration rows.

S010 | test_menuChromeFollowsTheme | src/checks/editorqml/tst_Theme.qml:209
Counterpart expression:
  compare(copyItem.background.color.toString().toUpperCase(), "#D2D0CA", ...)
  with highlight and cross-theme switch rows.

S011 | test_themeRepair | src/checks/editorqml/tst_Theme.qml:149
Counterpart expression:
  tryCompare(shell.shellPresenter, "themeMode", "vanilla")
  repairing empty mode and unparseable contrast to vanilla/50.

S012 | test_bThemeRepairThroughProductionShell | src/checks/editorqml/tst_ShellWindow.qml:225
Counterpart expression:
  tryCompare(shell.shellPresenter, "themeMode", "vanilla")
  repairing custom/#000000/#FFFFFF/80 (see proof.shellwindow.txt:50-56).

Shared helper
Theme rows and the independent reference math live with the predicates in
src/checks/themecolor/ThemeColorChecks.swift:18-64 (references) and :153-201
(preset literals). QML reference math lives in tst_Theme.qml:82-110.
