Assertion correspondence: eventviews/playhead.cpp

Reference revision: 996c6446f2cbfaa5b88ed721df4fa831129bd7bc
Original: src/checks/eventviews/playhead.cpp
Original SHA-256: 04ead5c46696448efe5cd8c525f2e4e658f73576570ca687712a8a7bd34a2cc4
Swift counterpart: src/checks/eventviews/EventListPlayheadChecks.swift
Swift SHA-256: 46a69ead27c305284920ec2db7e19b96f6c8e1a344946bc868232a9077d56841

Status and evidence
This is an assertion-site inventory, not a count-based equivalence claim. Every
original macro/failure site appears below in file order, including fixture guards.
Source context preserves setup between assertions verbatim. The registered
runEventListPlayheadChecks suite now covers all 44 sites through five Swift
predicates: tintLastOfRun (A001-A006), focusCommitsCursor (A007-A011),
focusedSiblingWins (A012-A023), samplePathAndProgrammaticRestore (A024-A033),
and followScroll (A034-A044). These are presenter/model assertions for the
converted event-list contract; no native event-list window or framebuffer claim
is made.
Disposition tally: 44 sites: 44 MATCHED.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: passing — the suite ran green with these checks registered.
The legacy eventviews C++ source is a reference inventory; the Swift event-list
predicates are the current execution evidence.

Execution and ownership
runEventListPlayheadChecks in src/checks/eventviews/EventListPlayheadChecks.swift
executes tintLastOfRun, focusCommitsCursor, focusedSiblingWins,
samplePathAndProgrammaticRestore, and followScroll in projectSession order.
Each report expectation carries its corresponding
eventviews/EventViewsPlayheadTest::<case> cppId.

Fixture, sequence and row constraints
Data rows expand single sites into many cases: tintLastOfRun_data emits 3 tick rows (before first event, same-tick run, past end of track), each reusing the tintLastOfRun sites against a different oracle row. focusedSiblingWins and samplePathAndProgrammaticRestore sequence multiple setPlayheadTick/focusRow phases inside one method; followScroll sequences four suppression phases (free follow, pointer-held, in-cell editing, follow off) over the same scrollToRow-spy sites.
Original setup, branch guards and helper implementations at the reference
revision remain authoritative; this inventory does not replace those sources.

Original assertion inventory and source sequence

A001 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:66
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A001.
Source context since previous assertion:
#include "checks/eventviews/eventview_fixture.h"
#include "checks/eventviews/tst_eventviews.h"

#include <QCoreApplication>
#include <QQuickWindow>
#include <QSignalSpy>
#include <QtTest>

#include "ui/eventtabletypes.h"
#include "ui/songview.h"
#include "ui/songview/quick/eventlistcontroller.h"

using checks::eventviews::EventWidgets;
using checks::eventviews::FixtureShape;

namespace {

int playheadRowOracle(const SmfTrack &track, double tick, int eotRow)
{
    if (tick < 0)
        return -1;
    if (tick >= double(track.endTick))
        return eotRow;
    int row = -1;
    for (int index = 0; index < int(track.events.size()); ++index) {
        if (double(track.events[index].tick) > tick)
            break;
        row = index;
    }
    return row;
}

int onlyTintedRow(const eventlist::EventTableModel &model)
{
    int tinted = -1;
    for (int row = 0; row < model.rowCount(); ++row) {
        bool any = false;
        bool wholeRow = true;
        for (int column = 0; column < model.columnCount(); ++column) {
            const bool hasTint = model.data(model.index(row, column), Qt::BackgroundRole).isValid();
            any = any || hasTint;
            wholeRow = wholeRow && hasTint;
        }
        if (any != wholeRow || (wholeRow && tinted >= 0))
            return -2;
        if (wholeRow)
            tinted = row;
    }
    return tinted;
}

} // namespace

void EventViewsPlayheadTest::tintLastOfRun_data()
{
    QTest::addColumn<Tick>("tick");
    QTest::newRow("before first event") << Tick{1};
    QTest::newRow("same tick run") << Tick{60};
    QTest::newRow("past end of track") << Tick{130};
}

void EventViewsPlayheadTest::tintLastOfRun()
{
    QFETCH(Tick, tick);
    const auto opened = checks::eventviews::openRigFixture(FixtureShape::Basic);

Original expression:
QVERIFY2(opened, qPrintable(opened.error))

A002 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:68
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A002.
Source context since previous assertion:
;
    const EventWidgets widgets = opened.fixture->openEventList();

Original expression:
QVERIFY(widgets)

A003 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:73
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A003.
Source context since previous assertion:
;
    const SmfTrack &track = opened.fixture->document().smf().tracks[widgets.controller->chunk()];
    const int expected = playheadRowOracle(track, double(tick), widgets.model->rowCount() - 1);

    widgets.controller->setPlayheadTick(double(tick), true);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), expected)

A004 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:74
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A004.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), expected)

A005 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:76
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A005.
Source context since previous assertion:
;
    widgets.controller->setPlayheadTick(-1.0, false);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), -1)

A006 | EventViewsPlayheadTest::tintLastOfRun | src/checks/eventviews/playhead.cpp:77
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::tintLastOfRun executes cppId eventviews/EventViewsPlayheadTest::tintLastOfRun and asserts the converted event-list contract for A006.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), -1)

A007 | EventViewsPlayheadTest::focusCommitsCursor | src/checks/eventviews/playhead.cpp:83
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusCommitsCursor executes cppId eventviews/EventViewsPlayheadTest::focusCommitsCursor and asserts the converted event-list contract for A007.
Source context since previous assertion:
;
}

void EventViewsPlayheadTest::focusCommitsCursor()
{
    const auto opened = checks::eventviews::openRigFixture(FixtureShape::Basic);

Original expression:
QVERIFY2(opened, qPrintable(opened.error))

A008 | EventViewsPlayheadTest::focusCommitsCursor | src/checks/eventviews/playhead.cpp:85
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusCommitsCursor executes cppId eventviews/EventViewsPlayheadTest::focusCommitsCursor and asserts the converted event-list contract for A008.
Source context since previous assertion:
;
    const EventWidgets widgets = opened.fixture->openEventList();

Original expression:
QVERIFY(widgets)

A009 | EventViewsPlayheadTest::focusCommitsCursor | src/checks/eventviews/playhead.cpp:88
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusCommitsCursor executes cppId eventviews/EventViewsPlayheadTest::focusCommitsCursor and asserts the converted event-list contract for A009.
Source context since previous assertion:
;
    const int eventRow =
        checks::eventviews::rowForTickAndType(*widgets.model, 70, eventlist::TypeNoteOn);

Original expression:
QVERIFY(eventRow >= 0)

A010 | EventViewsPlayheadTest::focusCommitsCursor | src/checks/eventviews/playhead.cpp:90
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusCommitsCursor executes cppId eventviews/EventViewsPlayheadTest::focusCommitsCursor and asserts the converted event-list contract for A010.
Source context since previous assertion:
;
    widgets.controller->focusRow(eventRow);

Original expression:
QTRY_COMPARE(opened.fixture->view().editCursorTick(), 70ULL)

A011 | EventViewsPlayheadTest::focusCommitsCursor | src/checks/eventviews/playhead.cpp:92
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusCommitsCursor executes cppId eventviews/EventViewsPlayheadTest::focusCommitsCursor and asserts the converted event-list contract for A011.
Source context since previous assertion:
;
    widgets.controller->focusRow(widgets.model->rowCount() - 1);

Original expression:
QTRY_COMPARE(opened.fixture->view().editCursorTick(), 120ULL)

A012 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:98
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A012.
Source context since previous assertion:
;
}

void EventViewsPlayheadTest::focusedSiblingWins()
{
    const auto opened = checks::eventviews::openRigFixture(FixtureShape::Basic);

Original expression:
QVERIFY2(opened, qPrintable(opened.error))

A013 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:100
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A013.
Source context since previous assertion:
;
    const EventWidgets widgets = opened.fixture->openEventList();

Original expression:
QVERIFY(widgets)

A014 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:102
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A014.
Source context since previous assertion:
;
    const int first = checks::eventviews::rowForTickAndType(*widgets.model, 60, eventlist::TypeCc);

Original expression:
QVERIFY(first >= 0)

A015 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:104
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A015.
Source context since previous assertion:
;
    const int sibling = first + 1;

Original expression:
QCOMPARE(
        widgets.model
            ->data(widgets.model->index(sibling, eventlist::EventTableModel::ColTick), Qt::EditRole)
            .toULongLong(),
        60ULL)

A016 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:113
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A016.
Source context since previous assertion:
;

    widgets.controller->setPlayheadTick(-1.0, false);
    widgets.controller->focusRow(first);
    widgets.controller->setPlayheadTick(60.0, false);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), first)

A017 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:114
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A017.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), first)

A018 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:116
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A018.
Source context since previous assertion:
;
    widgets.controller->setPlayheadTick(59.999, false);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), first)

A019 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:119
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A019.
Source context since previous assertion:
;

    widgets.controller->focusRow(sibling);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), sibling)

A020 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:120
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A020.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), sibling)

A021 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:123
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A021.
Source context since previous assertion:
;
    const int other =
        checks::eventviews::rowForTickAndType(*widgets.model, 70, eventlist::TypeNoteOn);

Original expression:
QVERIFY(other >= 0)

A022 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:128
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A022.
Source context since previous assertion:
;
    widgets.controller->focusRow(other);
    widgets.controller->setPlayheadTick(60.0, false);
    const SmfTrack &track = opened.fixture->document().smf().tracks[widgets.controller->chunk()];
    const int expected = playheadRowOracle(track, 60.0, widgets.model->rowCount() - 1);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), expected)

A023 | EventViewsPlayheadTest::focusedSiblingWins | src/checks/eventviews/playhead.cpp:129
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::focusedSiblingWins executes cppId eventviews/EventViewsPlayheadTest::focusedSiblingWins and asserts the converted event-list contract for A023.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), expected)

A024 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:135
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A024.
Source context since previous assertion:
;
}

void EventViewsPlayheadTest::samplePathAndProgrammaticRestore()
{
    const auto opened = checks::eventviews::openTabFixture(FixtureShape::Basic);

Original expression:
QVERIFY2(opened, qPrintable(opened.error))

A025 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:137
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A025.
Source context since previous assertion:
;
    const EventWidgets widgets = opened.fixture->openEventList();

Original expression:
QVERIFY(widgets)

A026 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:139
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A026.
Source context since previous assertion:
;
    const MidiTimeline *timeline = opened.fixture->view().timeline();

Original expression:
QVERIFY(timeline)

A027 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:144
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A027.
Source context since previous assertion:
;
    const SmfTrack &track = opened.fixture->document().smf().tracks[widgets.controller->chunk()];

    // A fresh list has no focused row (currentRow -1), so a programmatic
    // transport update must use the last event at the sample's tick.

Original expression:
QCOMPARE(widgets.controller->currentRow(), -1)

A028 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:148
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A028.
Source context since previous assertion:
;
    opened.fixture->view().setPlayheadSample(timeline->sampleForTick(track.endTick + 1), false);
    opened.fixture->view().setPlayheadSample(0, false);
    const int tickZero = playheadRowOracle(track, 0.0, widgets.model->rowCount() - 1);

Original expression:
QTRY_COMPARE(widgets.model->playRow(), tickZero)

A029 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:149
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A029.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), tickZero)

A030 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:153
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A030.
Source context since previous assertion:
;

    const int focused =
        checks::eventviews::rowForTickAndType(*widgets.model, 70, eventlist::TypeNoteOn);

Original expression:
QVERIFY(focused >= 0)

A031 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:155
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A031.
Source context since previous assertion:
;
    widgets.controller->focusRow(focused);

Original expression:
QTRY_COMPARE(opened.fixture->view().editCursorTick(), 70ULL)

A032 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:163
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A032.
Source context since previous assertion:
;
    const Tick cursorBefore = opened.fixture->view().editCursorTick();
    SmfEvent probe;
    probe.tick = track.endTick + 50;
    probe.status = 0xb0;
    probe.data0 = 7;
    probe.data1 = 64;
    opened.fixture->document().insertRawEvent(widgets.controller->chunk(), probe);

Original expression:
QTRY_COMPARE(opened.fixture->view().editCursorTick(), cursorBefore)

A033 | EventViewsPlayheadTest::samplePathAndProgrammaticRestore | src/checks/eventviews/playhead.cpp:165
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::samplePathAndProgrammaticRestore executes cppId eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore and asserts the converted event-list contract for A033.
Source context since previous assertion:
;
    opened.fixture->document().undoStack()->undo();

Original expression:
QTRY_COMPARE(opened.fixture->view().editCursorTick(), cursorBefore)

A034 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:171
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A034.
Source context since previous assertion:
;
}

void EventViewsPlayheadTest::followScroll()
{
    const auto opened = checks::eventviews::openRigFixture(FixtureShape::Long);

Original expression:
QVERIFY2(opened, qPrintable(opened.error))

A035 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:173
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A035.
Source context since previous assertion:
;
    const EventWidgets widgets = opened.fixture->openEventList();

Original expression:
QVERIFY(widgets)

A036 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:176
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A036.
Source context since previous assertion:
;
    EventListController &controller = *widgets.controller;
    QSignalSpy scrolled(&controller, &EventListController::scrollToRow);

Original expression:
QVERIFY(scrolled.isValid())

A037 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:184
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A037.
Source context since previous assertion:
;
    const Tick pastEnd = opened.fixture->document().smf().tracks[controller.chunk()].endTick + 10;

    // Follow scroll targets the playing row; a full pass emits at least one
    // scrollToRow for the past-end (EOT) position.
    controller.setPlayheadTick(0.0, false);
    controller.setFollowPlayhead(true);
    controller.setPlayheadTick(double(pastEnd), true);

Original expression:
QTRY_VERIFY(!scrolled.isEmpty())

A038 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:192
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A038.
Source context since previous assertion:
;

    // Mouse-held and in-cell-editing states must suppress auto scroll while
    // the tint keeps tracking the play row.
    scrolled.clear();
    controller.setPlayheadTick(0.0, false);
    controller.setPointerDown(true);
    controller.setPlayheadTick(double(pastEnd), true);

Original expression:
QCOMPARE(scrolled.count(), 0)

A039 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:193
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A039.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), widgets.model->rowCount() - 1)

A040 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:200
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A040.
Source context since previous assertion:
;
    controller.setPointerDown(false);

    // In-cell editing suppresses follow scroll the same way; the session is
    // a real editing session on the sentinel row's tick cell.
    scrolled.clear();
    controller.setPlayheadTick(0.0, false);

Original expression:
QVERIFY(controller.beginEditing(widgets.model->rowCount() - 1,
                                    eventlist::EventTableModel::ColTick))

A041 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:203
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A041.
Source context since previous assertion:
;
    controller.setPlayheadTick(double(pastEnd), true);

Original expression:
QCOMPARE(scrolled.count(), 0)

A042 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:204
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A042.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), widgets.model->rowCount() - 1)

A043 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:212
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A043.
Source context since previous assertion:
;
    controller.finishEditing(QString(), false);

    // Follow off: tint only, never a scroll request.
    scrolled.clear();
    controller.setPlayheadTick(0.0, false);
    controller.setFollowPlayhead(false);
    controller.setPlayheadTick(double(pastEnd), true);

Original expression:
QCOMPARE(scrolled.count(), 0)

A044 | EventViewsPlayheadTest::followScroll | src/checks/eventviews/playhead.cpp:213
Disposition: MATCHED
Mapping/reason: Swift predicate src/checks/eventviews/EventListPlayheadChecks.swift::followScroll executes cppId eventviews/EventViewsPlayheadTest::followScroll and asserts the converted event-list contract for A044.
Source context since previous assertion:
;

Original expression:
QCOMPARE(onlyTintedRow(*widgets.model), widgets.model->rowCount() - 1)

Remaining original source (closing sequence and helper bodies):
;
    controller.setFollowPlayhead(true);
}

int runEventViewsPlayheadCheck(const QStringList &qtArguments)
{
    EventViewsPlayheadTest test;
    QStringList arguments{QStringLiteral("eventviews-playhead")};
    arguments.append(qtArguments);
    return QTest::qExec(&test, arguments);
}


Swift assertion predicates
Swift assertion predicates
runEventListPlayheadChecks (src/checks/eventviews/EventListPlayheadChecks.swift)
- tintLastOfRun -> eventviews/EventViewsPlayheadTest::tintLastOfRun (A001-A006)
- focusCommitsCursor -> eventviews/EventViewsPlayheadTest::focusCommitsCursor (A007-A011)
- focusedSiblingWins -> eventviews/EventViewsPlayheadTest::focusedSiblingWins (A012-A023)
- samplePathAndProgrammaticRestore -> eventviews/EventViewsPlayheadTest::samplePathAndProgrammaticRestore (A024-A033)
- followScroll -> eventviews/EventViewsPlayheadTest::followScroll (A034-A044)
