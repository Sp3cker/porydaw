Assertion correspondence: visual/browsers.cpp

Reference revision: 4346c26abdcab317ccf97e178c959a82301081c9
Deleted in: S6 flip cutover (feature/swift-project-store, base 2d1c92d0)
Retirement: historical deletion record only; the deleted C++ source above is recoverable from the Reference revision and Original SHA-256, never from HEAD. The dispositions and S citations below were established while the file was live and were not re-verified in this cutover; no execution or parity claim is made by this record.
Original: src/checks/visual/browsers.cpp
Original SHA-256: 3f358911eebfb7b37f1327fccbf2449627c1be34ca20e2c6595a0b199d17a59d
Swift counterpart: none identified
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

This file: 18 assertion sites: 8 GAP, 10 RETIRED-REPRESENTATION. Dispositions tally: 8 GAP, 10 RETIRED-REPRESENTATION. The voicegroup-browser population and captures (A007/A009/A011) and the sample-picker popup observations and baseline (A013/A014/A015/A017/A018) are GAP, features not yet ported with no converted-window owner; fixture and loader preconditions (A001-A006), theme plumbing (A008/A010/A012) and the popup size pin (A016) stay RETIRED-REPRESENTATION of an uncompiled QWidget harness whose runVisualBrowsersCheck entry point has no callsite and which is unregistered in checkcatalog, so no lane executes them.

Command: deno task verify --filter visual --verbose
Result: pending integrated run.

A001 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:286
Disposition: RETIRED-REPRESENTATION

A002 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:289
Disposition: RETIRED-REPRESENTATION

A003 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:293
Disposition: RETIRED-REPRESENTATION

A004 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:298
Disposition: RETIRED-REPRESENTATION

A005 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:301
Disposition: RETIRED-REPRESENTATION

A006 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:303
Disposition: RETIRED-REPRESENTATION

A007 | VisualBrowsersTest::initTestCase | src/checks/visual/browsers.cpp:305
Disposition: PARTIAL
Mapping/reason: tst_ShellVoicegroup.qml::test_128RowsSelectionAndAudition opens mus_route101 from the staged fixture and observes 128 live bank rows, a loaded DirectSound sample and read-only cry slot in VoicegroupPanel; unproved: native sample-set loop metadata and full type-icon/used-mark pixel parity.
Source context since previous assertion:

Original expression:
  QVERIFY2(populateBrowser(error), qPrintable(error));

A008 | VisualBrowsersTest::browserBaseline | src/checks/visual/browsers.cpp:433
Disposition: RETIRED-REPRESENTATION

A009 | VisualBrowsersTest::browserBaseline | src/checks/visual/browsers.cpp:437
Disposition: PARTIAL
Mapping/reason: tst_ShellVoicegroup.qml::test_referenceProfileCapture mounts VoicegroupPanel, compares selector/tree/header/first-row geometry to the macos-dpr1-font12 vanilla widget JSON and records a PNG; unproved: native pixel colors/icons, alternate theme and DPR/font profiles.
Source context since previous assertion:

      m_browser->selectSlot(kDirectSoundSlot);
      m_browser->revealSlot(0);
      QApplication::processEvents();
Original expression:
  QVERIFY(grabBrowser(QStringLiteral("voicegroupbrowser/%1").arg(theme)));

A010 | VisualBrowsersTest::editorVariants | src/checks/visual/browsers.cpp:462
Disposition: RETIRED-REPRESENTATION

A011 | VisualBrowsersTest::editorVariants | src/checks/visual/browsers.cpp:465
Disposition: PARTIAL
Mapping/reason: tst_ShellVoicegroup.qml::test_referenceProfileCapture captures the mounted Square 1 and read-only editor variants against the macos-dpr1-font12 vanilla widget JSON; unproved: per-field pixel/colors, darkneutralhigh and other font/DPR profiles.
Source context since previous assertion:

      m_browser->selectSlot(slot);
      QApplication::processEvents();
Original expression:
  QVERIFY(grabBrowser(QStringLiteral("voicegroupbrowser/%1/%2").arg(variant, theme)));

A012 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:480
Disposition: RETIRED-REPRESENTATION

A013 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:485
Disposition: GAP
Mapping/reason: GAP: feature not yet ported: sample picker. No-ingress fact retained: browsers.cpp uncompiled in all CMake targets, runVisualBrowsersCheck entry point uncalled, unregistered in checkcatalog; frozen baseline under src/checks/fixtures/visual/ is a legacy QWidget render with no converted-window owner. Original observation: Real opened SamplePickerButton popup must exist in the native widget tree.
Source context since previous assertion:

      m_browser->selectSlot(kDirectSoundSlot);
      QApplication::processEvents();

      auto *picker = m_browser->findChild<SamplePickerButton *>();
Original expression:
  QVERIFY(picker);

A014 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:488
Disposition: GAP
Mapping/reason: GAP: feature not yet ported: sample picker. No-ingress fact retained: browsers.cpp uncompiled in all CMake targets, runVisualBrowsersCheck entry point uncalled, unregistered in checkcatalog; frozen baseline under src/checks/fixtures/visual/ is a legacy QWidget render with no converted-window owner. Original observation: Popup widget must be found; native widget-tree observation.
Source context since previous assertion:

      picker->openPopup();
      QWidget *popup = pickerPopup();
Original expression:
  QVERIFY(popup);

A015 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:489
Disposition: GAP
Mapping/reason: GAP: feature not yet ported: sample picker. No-ingress fact retained: browsers.cpp uncompiled in all CMake targets, runVisualBrowsersCheck entry point uncalled, unregistered in checkcatalog; frozen baseline under src/checks/fixtures/visual/ is a legacy QWidget render with no converted-window owner. Original observation: Popup becomes visible within 5 s; native async window state.
Source context since previous assertion:

Original expression:
  QTRY_VERIFY_WITH_TIMEOUT(popup->isVisible(), 5000);

A016 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:494
Disposition: RETIRED-REPRESENTATION

A017 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:515
Disposition: GAP
Mapping/reason: GAP: feature not yet ported: sample picker. No-ingress fact retained: browsers.cpp uncompiled in all CMake targets, runVisualBrowsersCheck entry point uncalled, unregistered in checkcatalog; frozen baseline under src/checks/fixtures/visual/ is a legacy QWidget render with no converted-window owner. Original observation: Popup closes after hide; native popup state.
Source context since previous assertion:

      QApplication::processEvents();
      // The search field takes focus on open; a blinking caret is transient
      // state the baseline must not depend on.
      if (QWidget *focused = QApplication::focusWidget())
          focused->clearFocus();
      QApplication::processEvents();

      QString error;
      const bool ok = visual::compareWidget(QStringLiteral("samplepicker/%1").arg(theme), *popup,
                                            popupRegions(*popup), &error);
      if (!ok)
          qWarning().noquote() << error;

      popup->hide();
      QApplication::processEvents();
Original expression:
  QVERIFY2(!picker->popupVisible(), "sample picker popup did not close");

A018 | VisualBrowsersTest::samplePickerPopup | src/checks/visual/browsers.cpp:516
Disposition: GAP
Mapping/reason: GAP: feature not yet ported: sample picker. No-ingress fact retained: browsers.cpp uncompiled in all CMake targets, runVisualBrowsersCheck entry point uncalled, unregistered in checkcatalog; frozen baseline under src/checks/fixtures/visual/ is a legacy QWidget render with no converted-window owner. Original observation: Stored result of the samplepicker/<theme> screenshot comparison; native pixels.
Source context since previous assertion:

Original expression:
  QVERIFY(ok);

Remaining original source (closing sequence and helper bodies):

  }

  } // namespace checks

  int runVisualBrowsersCheck(QApplication &application, const QStringList &qtArguments)
  {
      // The registry stages the decomp fixture into a scratch directory and
      // exports its root; without it the checked-in copy is used read-only.
      QString projectRoot = qEnvironmentVariable("PORYDAW_VISUAL_PROJECT_ROOT");
      if (projectRoot.isEmpty()) {
          const QDir sourceRoot(QStringLiteral(PORYDAW_CHECK_SOURCE_DIR));
          projectRoot = sourceRoot.filePath(QStringLiteral("src/checks/fixtures/decompproject"));
      }
      if (!QFileInfo(projectRoot).isDir()) {
          std::fprintf(stderr, "visual-browsers: project fixture root '%s' is not a directory\n",
                       qPrintable(projectRoot));
          return 2;
      }

      checks::visual::prepare(application);
      checks::VisualBrowsersTest test(projectRoot);
      QStringList exec{QStringLiteral("visual-browsers")};
      exec.append(qtArguments);
      return QTest::qExec(&test, exec);
  }

  #include "browsers.moc"

Swift assertion predicates

No Swift counterpart identified; no S-items.
