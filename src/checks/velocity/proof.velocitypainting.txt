Assertion correspondence: velocity/velocitypainting.cpp

Reference revision: f842f7e128adea2958d0967e2afbd834893f21b4
Original: src/checks/velocity/velocitypainting.cpp
Original SHA-256: 33747463a4aa2a8dbce55094dddaa5f68e218c834913dac14936c932b578f428
Swift counterpart: src/checks/velocity/tst_velocityediting.swift
Swift SHA-256: 322d230eb82710575393d0bd85634f224390f7eb353154e88e2caae7cb6261b0
Additional Swift counterpart: src/checks/velocity/velocitydetentdragging.swift
Additional Swift SHA-256: e411048c4631faad6dc3fa9cb27ea7f471fd6ba266776ba248fb5ad4a2fac12b
Additional Swift counterpart: src/checks/velocity/VelocityPageChecks.swift
Additional Swift SHA-256: cb49bddf735ed93df3d0faaa0e9a76e46cf989d3c6498ac05faff54de3d48b6e
Additional Swift counterpart: src/checks/velocity/VelocityPaintDetentChecks.swift
Additional Swift SHA-256: 9c6f9389d6da12297ae6600087b5d8ffb5ab60b5a7275b422b4e655e6afebe8d
Deleted in: 67544720bed2e63b0093314273d15ef5d11a2be5 checks: retire unexecuted native rows, refresh proof pins, delete dead C++ check sources

Purpose and status
Each A entry is one original assertion or failure site, including helper and setup sites.
Repeated rows and loops execute a site more than once. The source SHA pins the entire
original sequence; source context preserves setup, rows, loops, and guards between
assertions verbatim, and the tail preserves the closing sequence verbatim.
MATCHED requires identified equivalent predicates and execution evidence; PARTIAL means
a specific related scenario differs; GAP means exact assertion correspondence is not
established; NATIVE-SETUP identifies a retained C++ loader/window/input precondition.
RETIRED-REPRESENTATION marks a C++ mechanism with no Swift ingress whose
consumer-visible effect is carried by the predicates cited in its mapping.
A method name or related Swift case alone never establishes an assertion match.
Unresolved entries remain work.

Execution and ownership
drawerVelocityGestureTransactions (velocity/tst_velocityediting.swift),
drawerVelocityFrozenGesturePolicy (velocity/velocitydetentdragging.swift),
drawerVelocityCommandAvailability (swiftcore/VelocityPageChecks.swift), and
drawerVelocityPaintCommitsOnce / drawerVelocityRampCommitsOnce
(swiftcore/VelocityPaintDetentChecks.swift, cppIDs
swiftcore/VelocityPaintDetent::paintCommitsOnce and
swiftcore/VelocityPaintDetent::rampCommitsOnce) execute the paired checks.
runVelocityPageChecks calls the two paint functions at
src/checks/velocity/VelocityPageChecks.swift:176-177; that file is compiled by
src/checks/CMakeLists.txt:198. Shared setup lives in swiftcore/VelocityPageChecks.swift
(drawerVelocityVelocityPageFixture, drawerVelocityVelocityFixture); per-case document
and gesture state live in the paired Swift sources. Their helper expectations execute
on every call and are inventoried below.

Verification
Command: deno task verify --filter swiftcore
Result: PASS — 1/14 harnesses selected, 13 sibling harnesses skipped by the filter.
Provenance: provided by the controller with the settled handoff for the pre-freeze source,
and re-reported PASS by the GapsVelocity lane after the final edit of
src/checks/velocity/VelocityPaintDetentChecks.swift (SHA-256 9c6f9389…). This ledger did
not rerun the build or the suite; structural proof checking is not execution evidence
and no parity or source-freshness claim follows from it.

Fixture, sequence and row constraints
No QTest data rows; each method runs once.
Cases run on the shared VelocityEditingTest in-memory fixture: three notes resolved
in SMF event order (quiet stacked duplicate, loud stacked duplicate, tick-60 later
note); assertions read the documentVelocity/timelineVelocity/noteSelectionIs helpers.
See proof.tst_velocityediting.txt for the init-site inventory.
paintCommitsOnce and rampCommitsOnce on the Continuous-axis fixture: a press at an
empty tick paints across the selected drag pair, staging per-note previews without
document mutation (revision frozen, zero signals), then committing exactly once on
release with per-note painted velocities and preserved selection. No data rows.
Methods in file order: paintCommitsOnce, rampCommitsOnce.
The Swift counterparts run the same shape on the shared synthetic fixture
(src/checks/velocity/VelocityPageChecks.swift:57-77): notes at tick 0 (pitch 60,
velocity 100, square program), tick 24 (pitch 67, velocity 64) and tick 96 (pitch 72,
velocity 32, after the program change to noise), so the selected pair spans two voice
maps and the ruler is continuous only once setUseDetents(enabled: false) disables the
detent ladder. drawerVelocityPaintCommitsOnce selects notes 0 and 2;
drawerVelocityRampCommitsOnce adds a third note at tick 48 (pitch 60, duration 12,
velocity 56) and selects all three. Both drive page.pointerPress / pointerMove /
pointerRelease (surface 1, button 1; the ramp with Shift bit 0x0200_0000) and read
page.frozenPreview, document.note(id)?.velocity, document.revision,
document.history.currentIdentity and session.selectedNoteOrder.
Exact original source anchors:
src/checks/velocity/velocitypainting.cpp:16 | selectDragPair();
src/checks/velocity/velocitypainting.cpp:27 | QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
src/checks/velocity/velocitypainting.cpp:28 | QSignalSpy edited(m_tab.get(), &SongTab::edited);
src/checks/velocity/velocitypainting.cpp:32 | focusVelocityBand();
src/checks/velocity/velocitypainting.cpp:33 | mousePress(Qt::LeftButton, paintStart);
src/checks/velocity/velocitypainting.cpp:34 | mouseMove(paintEnd);
src/checks/velocity/velocitypainting.cpp:59 | mouseRelease(Qt::LeftButton, paintEnd);
src/checks/velocity/velocitypainting.cpp:91 | m_tab->document().addNote(0, kMiddleTick, m_quietStacked.key, kMiddleDuration,
src/checks/velocity/velocitypainting.cpp:99 | m_tab->view().selectionModel().setNoteSelection(
src/checks/velocity/velocitypainting.cpp:130 | QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
src/checks/velocity/velocitypainting.cpp:131 | QSignalSpy edited(m_tab.get(), &SongTab::edited);
src/checks/velocity/velocitypainting.cpp:135 | focusVelocityBand();
src/checks/velocity/velocitypainting.cpp:136 | mousePress(Qt::LeftButton, rampStart, Qt::ShiftModifier);
src/checks/velocity/velocitypainting.cpp:137 | mouseMove(rampEnd, Qt::ShiftModifier);
src/checks/velocity/velocitypainting.cpp:167 | mouseRelease(Qt::LeftButton, rampEnd, Qt::ShiftModifier);

Original assertion inventory

A001 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:15
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:49 asserts page.axisMode is the continuous ruler for the mixed square/noise selection after setUseDetents(false); the same observable as the original Continuous mode check. Executed by runVelocityPageChecks (src/checks/velocity/VelocityPageChecks.swift:176); cppID swiftcore/VelocityPaintDetent::paintCommitsOnce.
Swift counterpart expression (S080):
  report.expectEqual(VelocityAxisModel.Mode.continuous.rawValue, page.axisMode, cppID: drawerVelocityPaintID, what: "the mixed square and noise selection presents the continuous ruler")
Source context since previous assertion:
#include "checks/velocity/tst_velocityediting.h"

#include <cmath>
#include <optional>

#include <QtTest>

#include "ui/songview.h"

void VelocityEditingTest::paintCommitsOnce()
{
    constexpr uint8_t kQuietPaintVelocity = 37;
    constexpr uint8_t kLaterPaintVelocity = 91;


Original expression:
  QCOMPARE(m_area->axis().mode(), VelocityAxis::Mode::Continuous)

A002 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:23
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:58 asserts pressX != endX: the two selected note columns give distinct paint endpoints, the observable of the original paintStart != paintEnd guard. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S081):
  report.expect(pressX != endX, cppID: drawerVelocityPaintID, message: "the paint endpoints span two note columns")
Source context since previous assertion:
;
    selectDragPair();

    // Each endpoint uses a selected note's x coordinate but a distant velocity
    // y coordinate, so the initial press is blank—not either stacked circle or
    // its duration stem—and therefore begins the paint interaction.
    const QPoint paintStart = windowPoint(nodePoint(kQuietPaintVelocity, m_quietStacked.tick));
    const QPoint paintEnd = windowPoint(nodePoint(kLaterPaintVelocity, m_later.tick));

Original expression:
  QVERIFY(paintStart != paintEnd)

A003 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:29
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityPaintCommitsOnce (S085, S086)
Mapping/reason: QSignalSpy construction validity is a Qt test-harness mechanism with no Swift ingress: the Swift check holds no spy and counts no emissions, and src/checks/velocity/velocitypainting.cpp is uncompiled in every target and unregistered in checkcatalog (spec section 7). The consumer-visible effect the spy would observe is asserted through public document state by S085 (revision frozen) and S086 (history identity frozen).
Source context since previous assertion:
;

    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();
    QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
    QSignalSpy edited(m_tab.get(), &SongTab::edited);

Original expression:
  QVERIFY(documentChanged.isValid())

A004 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:30
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityPaintCommitsOnce (S085, S086, S091, S093)
Mapping/reason: the second QSignalSpy observes the native SongTab::edited signal; the Swift page has no tab QObject and no emission counter, so the spy-validity mechanism has no ingress (src/checks/velocity/velocitypainting.cpp is uncompiled and unregistered, spec section 7). The deferred-then-committed publication it observed is carried by S085/S086 (nothing staged) and S091/S093 (one release, undoable).
Source context since previous assertion:
;

Original expression:
  QVERIFY(edited.isValid())

A005 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:42
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:62 asserts the press column's frozen preview equals 37; a present 37 entry entails the has_value() observation the original makes for the first selected note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S082):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityPaintID, what: "the press column previews its literal paint velocity")
Source context since previous assertion:
;

    focusVelocityBand();
    mousePress(Qt::LeftButton, paintStart);
    mouseMove(paintEnd);

    // Painting is deferred: both selected notes receive literal positive
    // previews while the document, projection, and command history stay at
    // their pre-gesture values.
    const std::optional<uint8_t> quietPreview =
        m_tab->view().previewVelocity(m_quietStacked.noteId);
    const std::optional<uint8_t> laterPreview = m_tab->view().previewVelocity(m_later.noteId);

Original expression:
  QVERIFY(quietPreview.has_value())

A006 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:43
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:63 asserts the release column's frozen preview equals 91, which entails the has_value() observation for the second selected note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S083):
  report.expectEqual(91, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityPaintID, what: "the release column previews its literal paint velocity")
Source context since previous assertion:
;

Original expression:
  QVERIFY(laterPreview.has_value())

A007 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:44
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:62 asserts the first selected note's frozen preview is the literal 37; the same predicate carries the presence site A005. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S082):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityPaintID, what: "the press column previews its literal paint velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*quietPreview), int(kQuietPaintVelocity))

A008 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:45
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:63 asserts the second selected note's frozen preview is the literal 91; the same predicate carries the presence site A006. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S083):
  report.expectEqual(91, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityPaintID, what: "the release column previews its literal paint velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*laterPreview), int(kLaterPaintVelocity))

A009 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:46
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:64 asserts page.frozenPreview has no entry for notes[1], the unselected loud sibling, which is the original's absent preview for m_loudStacked. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S084):
  report.expect(page.frozenPreview[notes[1].id] == nil, cppID: drawerVelocityPaintID, message: "the unselected note previews nothing")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A010 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:47
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:65 asserts document.revision still equals the snapshot taken before the press while the paint preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S085):
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityPaintID, what: "the deferred paint stages no revision before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A011 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:48
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:66 asserts document.history.currentIdentity still equals the pre-gesture identity, so no history entry was staged; the Swift suite replaces the private QUndoStack::count() read with public history identity, exactly as proof.tst_velocityediting.txt does for the same gesture. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S086):
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityPaintID, what: "the deferred paint stages no history entry before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A012 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:49
Disposition: PARTIAL
Mapping/reason: Related predicates S085 (src/checks/velocity/VelocityPaintDetentChecks.swift:65, revision frozen) and S086 (:66, history identity frozen) establish that the deferred paint committed nothing, which is the no-op the original counts. Unproved: the documentChanged emission count itself - the Swift check attaches no document.onChange observer and counts no publications, so the exact zero-count observation is not executed. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S085):
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityPaintID, what: "the deferred paint stages no revision before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentChanged.count(), 0)

A013 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:50
Disposition: PARTIAL
Mapping/reason: Related predicates S085 (:65) and S086 (:66) show the deferred paint committed nothing. Unproved: the SongTab::edited emission count - the Swift page has no tab object and no edited/dirty publication counter to read. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S086):
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityPaintID, what: "the deferred paint stages no history entry before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(edited.count(), 0)

A014 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:51
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:67 asserts document.note(notes[0])?.velocity still equals the captured first-note velocity while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S087):
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the first velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A015 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:52
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:68 asserts the second selected note's document velocity still equals its captured value while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S088):
  report.expectEqual(Int(notes[2].velocity), Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the last velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(m_later.velocity))

A016 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:53
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:69 asserts the unselected sibling's document velocity still equals its captured value while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S089):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the unselected velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A017 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:54
Disposition: PARTIAL
Mapping/reason: S087 (src/checks/velocity/VelocityPaintDetentChecks.swift:67) asserts the first selected note's document velocity during the preview. Unproved: the timeline projection value - the original reacquires the rebuilt MidiTimeline and reads its note-on velocity (tst_velocityediting.cpp:431-442), while the Swift check never re-reads the page's published projection.
Swift counterpart expression (S087):
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the first velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A018 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:55
Disposition: PARTIAL
Mapping/reason: S088 (src/checks/velocity/VelocityPaintDetentChecks.swift:68) asserts the second selected note's document velocity during the preview. Unproved: its timeline projection value (original helper tst_velocityediting.cpp:431-442); the Swift check never re-reads the published projection.
Swift counterpart expression (S088):
  report.expectEqual(Int(notes[2].velocity), Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the last velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(m_later.velocity))

A019 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:56
Disposition: PARTIAL
Mapping/reason: S089 (src/checks/velocity/VelocityPaintDetentChecks.swift:69) asserts the unselected note's document velocity during the preview. Unproved: its timeline projection value (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S089):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the unselected velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A020 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:57
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:70 asserts session.selectedNoteOrder is exactly the paint pair while the preview is live, the observable of the original frozen noteSelectionIs check. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S090):
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, notes[2].id], cppID: drawerVelocityPaintID, message: "the preview keeps the paint selection")
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A021 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:64
Disposition: PARTIAL
Mapping/reason: Related predicates S091 (src/checks/velocity/VelocityPaintDetentChecks.swift:72, revision advanced by exactly one) and S092 (:73, a new history identity) establish the single release commit. Unproved: the documentChanged emission count - no document.onChange observer is attached, so exactly-one publication is corroborated but not executed.
Swift counterpart expression (S091):
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityPaintID, what: "one paint release commits one revision")
Source context since previous assertion:
;

    mouseRelease(Qt::LeftButton, paintEnd);

    // Release commits the two independent literal values as one history entry,
    // updates the actual timeline projection, and does not touch the unselected
    // stacked sibling.

Original expression:
  QCOMPARE(documentChanged.count(), 1)

A022 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:65
Disposition: PARTIAL
Mapping/reason: Related predicates S091 (:72) and S092 (:73) establish the single release commit. Unproved: the SongTab::edited emission count, which has no Swift tab counterpart. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S092):
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityPaintID, message: "one paint release makes one history entry")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(edited.count(), 1)

A023 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:66
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:72 asserts document.revision is exactly one past the pre-gesture snapshot after the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S091):
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityPaintID, what: "one paint release commits one revision")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore + 1)

A024 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:67
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:73 asserts the history identity changed on release, i.e. exactly one newly committed entry for the single-release transaction; this replaces the private QUndoStack::count() delta. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S092):
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityPaintID, message: "one paint release makes one history entry")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore + 1)

A025 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:68
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:74 asserts document.history.canUndo after the release, the public form of the original history().canUndo(). Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S093):
  report.expect(document.history.canUndo, cppID: drawerVelocityPaintID, message: "the paint entry is undoable")
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->history().canUndo())

A026 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:69
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:75 asserts page.frozenPreview.isEmpty (and no live gesture) after the release, so the first selected note keeps no preview. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S094):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityPaintID, message: "the paint release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A027 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:70
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:75 asserts the whole frozen preview map is empty after release, which covers the second selected note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S094):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityPaintID, message: "the paint release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A028 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:71
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:75 asserts the whole frozen preview map is empty after release, which covers the unselected sibling. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S094):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityPaintID, message: "the paint release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A029 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:72
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:76 asserts document.note(notes[0])?.velocity == 37 after the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S095):
  report.expectEqual(37, Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the first literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietPaintVelocity))

A030 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:73
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:77 asserts document.note(notes[2])?.velocity == 91 after the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S096):
  report.expectEqual(91, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the last literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLaterPaintVelocity))

A031 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:74
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:78 asserts the unselected sibling's document velocity is unchanged by the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S097):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release leaves the unselected note alone")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A032 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:75
Disposition: PARTIAL
Mapping/reason: S095 (src/checks/velocity/VelocityPaintDetentChecks.swift:76) asserts the committed document velocity of the first selected note. Unproved: its post-commit timeline projection (original helper tst_velocityediting.cpp:431-442); the Swift check never re-reads the published projection.
Swift counterpart expression (S095):
  report.expectEqual(37, Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the first literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(kQuietPaintVelocity))

A033 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:76
Disposition: PARTIAL
Mapping/reason: S096 (src/checks/velocity/VelocityPaintDetentChecks.swift:77) asserts the committed document velocity of the second selected note. Unproved: its post-commit timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S096):
  report.expectEqual(91, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the last literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(kLaterPaintVelocity))

A034 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:77
Disposition: PARTIAL
Mapping/reason: S097 (src/checks/velocity/VelocityPaintDetentChecks.swift:78) asserts the unselected note's document velocity after the release. Unproved: its post-commit timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S097):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release leaves the unselected note alone")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A035 | VelocityEditingTest::paintCommitsOnce | src/checks/velocity/velocitypainting.cpp:78
Disposition: MATCHED
Mapping/reason: drawerVelocityPaintCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:79 asserts session.selectedNoteOrder is still the paint pair after the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:176).
Swift counterpart expression (S098):
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, notes[2].id], cppID: drawerVelocityPaintID, message: "the release keeps the paint selection")
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A036 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:90
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:101 asserts page.axisMode is the continuous ruler for the mixed square/noise selection after setUseDetents(false); the same observable as the original Continuous mode check. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177); cppID swiftcore/VelocityPaintDetent::rampCommitsOnce.
Swift counterpart expression (S099):
  report.expectEqual(VelocityAxisModel.Mode.continuous.rawValue, page.axisMode, cppID: drawerVelocityRampID, what: "the mixed ramp selection presents the continuous ruler")
Source context since previous assertion:
;
}

void VelocityEditingTest::rampCommitsOnce()
{
    constexpr uint64_t kMiddleTick = 36;
    constexpr uint32_t kMiddleDuration = 12;
    constexpr uint8_t kMiddleOriginalVelocity = 42;
    constexpr uint8_t kRampStartVelocity = 37;
    constexpr uint8_t kRampMiddleVelocity = 65;
    constexpr uint8_t kRampEndVelocity = 93;


Original expression:
  QCOMPARE(m_area->axis().mode(), VelocityAxis::Mode::Continuous)

A037 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:95
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce asserts the freshly added middle note's placed geometry in src/checks/velocity/VelocityPaintDetentChecks.swift: its tick at :96 (S120) and its pitch at :97 (S121), the observable of the original (track 0, tick, key) findNote probe; S079 (:19) still fails the check when the identity addNotes returned does not resolve, and :25 when the add is rejected. The track-0 arm is fixed by the fixture construction (NewNote(track: 0) read back through notes(in: 0)) rather than re-probed. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S120):
  report.expectEqual(48, Int(middle.tick), cppID: drawerVelocityRampID, what: "the added middle note resolves at its placed tick")
Swift counterpart expression (S121):
  report.expectEqual(60, Int(middle.pitch), cppID: drawerVelocityRampID, what: "the added middle note resolves at its placed pitch")
Swift counterpart expression (S079):
  report.fail(cppID, "the added note published no identity")
Source context since previous assertion:
;
    m_tab->document().addNote(0, kMiddleTick, m_quietStacked.key, kMiddleDuration,
                              kMiddleOriginalVelocity);

    DocNote middle;

Original expression:
  QVERIFY(m_tab->document().findNote(0, kMiddleTick, m_quietStacked.key, &middle))

A038 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:96
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:123 asserts document.note(middle)?.velocity == 56 while the ramp preview is live, i.e. the added middle note keeps the velocity it was constructed with - the same observable as the original middle.velocity == kMiddleOriginalVelocity. The Swift fixture constructs that note at 56 (tick 48) instead of the original 42 (tick 36).
Swift counterpart expression (S109):
  report.expectEqual(56, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the middle velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(middle.velocity), int(kMiddleOriginalVelocity))

A039 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:97
Disposition: PARTIAL
Mapping/reason: S109 (src/checks/velocity/VelocityPaintDetentChecks.swift:123) asserts the added middle note's document velocity. Unproved: its timeline projection - the original's QTRY re-read of the rebuilt timeline (tst_velocityediting.cpp:431-442) is not executed.
Swift counterpart expression (S109):
  report.expectEqual(56, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the middle velocity during preview")
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(timelineVelocity(middle.noteId), int(kMiddleOriginalVelocity))

A040 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:101
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:126 asserts session.selectedNoteOrder is exactly the three-note ramp selection, the observable of the original noteSelectionIs check. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S111):
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, middle.id, notes[2].id], cppID: drawerVelocityRampID, message: "the preview keeps the ramp selection")
Source context since previous assertion:
;

    m_tab->view().selectionModel().setNoteSelection(
        {m_quietStacked.noteId, middle.noteId, m_later.noteId});

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, middle.noteId, m_later.noteId}))

A041 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:121
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityRampCommitsOnce (S101, S103)
Mapping/reason: the original asserts that the native QTest window's integral QPoint endpoints balance around a half-pixel middle (2*middleExact.x() rounding parity). Swift pointer input is double-precision (VelocityPage.pointerPress/pointerMove take Double coordinates), so QPointF::toPoint rounding has no ingress; the intent - a swept span symmetric about the middle note - is carried by S101 (:111, the middle handle lies strictly inside the span) and S103 (:117, the middle preview equals the ramp value interpolated at that x). src/checks/velocity/velocitypainting.cpp is uncompiled and unregistered (spec section 7).
Source context since previous assertion:
;

    const QPointF firstExact =
        m_velocityInput->mapToScene(nodePoint(kRampStartVelocity, m_quietStacked.tick));
    const QPointF middleExact =
        m_velocityInput->mapToScene(nodePoint(kRampMiddleVelocity, kMiddleTick));
    const QPointF lastExact =
        m_velocityInput->mapToScene(nodePoint(kRampEndVelocity, m_later.tick));
    QPoint rampStart = firstExact.toPoint();
    QPoint rampEnd = lastExact.toPoint();

    // Native QTest window positions are integral. Keep both endpoint samples
    // on the inward side of their rendered note centers, and require those
    // representable coordinates to balance exactly around the middle node.
    // Rounding both half-pixel centers with QPointF::toPoint() instead shifts
    // the sampled middle away from one half even though the endpoint values
    // still clamp to their requested velocities.
    rampStart.setX(static_cast<int>(std::ceil(firstExact.x())));
    rampEnd.setX(static_cast<int>(std::floor(lastExact.x())));
    const qreal doubledMiddleExactX = 2.0 * middleExact.x();

Original expression:
  QCOMPARE(doubledMiddleExactX, std::round(doubledMiddleExactX))

A042 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:122
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityRampCommitsOnce (S101, S103)
Mapping/reason: same native integral-coordinate mechanism as A041 for the endpoint-symmetry equality: Swift drives double-precision page coordinates and grounds no QPoint rounding, so the exact endpoint balance is not representable; the behavioural remnant (the middle sample inside the swept span, interpolated at its own x) is asserted by S101 and S103. src/checks/velocity/velocitypainting.cpp is uncompiled and unregistered (spec section 7).
Source context since previous assertion:
;

Original expression:
  QCOMPARE(rampStart.x() + rampEnd.x(), static_cast<int>(doubledMiddleExactX))

A043 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:123
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:110 asserts pressX != endX: the two endpoint note columns give distinct ramp endpoints, the observable of the original rampStart != rampEnd guard. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S100):
  report.expect(pressX != endX, cppID: drawerVelocityRampID, message: "the ramp endpoints span two note columns")
Source context since previous assertion:
;

Original expression:
  QVERIFY(rampStart != rampEnd)

A044 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:132
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityRampCommitsOnce (S106, S107)
Mapping/reason: QSignalSpy construction validity is a Qt test-harness mechanism with no Swift ingress; the Swift check holds no spy and counts no emissions (src/checks/velocity/velocitypainting.cpp uncompiled and unregistered, spec section 7). The deferred ramp's consumer-visible effect is asserted by S106 (:120, revision frozen) and S107 (:121, history identity frozen).
Source context since previous assertion:
;

    // The midpoint is exactly halfway in both input-coordinate and target
    // velocity space: 37 + (93 - 37) / 2 == 65. The asserted literal is
    // intentionally not inferred from a preview or the production quantizer.
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();
    QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
    QSignalSpy edited(m_tab.get(), &SongTab::edited);

Original expression:
  QVERIFY(documentChanged.isValid())

A045 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:133
Disposition: RETIRED-REPRESENTATION
Swift: src/checks/velocity/VelocityPaintDetentChecks.swift :: drawerVelocityRampCommitsOnce (S106, S107, S112, S114)
Mapping/reason: same mechanism as A044 for the tab-level edited spy; the Swift page has no tab QObject or emission counter. The publication it observed is carried by S106/S107 (nothing staged during the ramp) and S112/S114 (one release revision, undoable).
Source context since previous assertion:
;

Original expression:
  QVERIFY(edited.isValid())

A046 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:146
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:116 asserts the ramp press endpoint's frozen preview equals 37, which entails the has_value() observation for the first selected note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S102):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp press endpoint previews its literal velocity")
Source context since previous assertion:
;

    focusVelocityBand();
    mousePress(Qt::LeftButton, rampStart, Qt::ShiftModifier);
    mouseMove(rampEnd, Qt::ShiftModifier);

    // Shift paints the frozen selection as a linear ramp while remaining a
    // preview-only transaction. In particular, the added midpoint is 65,
    // independently derived from the two literal endpoints above.
    const std::optional<uint8_t> quietPreview =
        m_tab->view().previewVelocity(m_quietStacked.noteId);
    const std::optional<uint8_t> middlePreview = m_tab->view().previewVelocity(middle.noteId);
    const std::optional<uint8_t> laterPreview = m_tab->view().previewVelocity(m_later.noteId);

Original expression:
  QVERIFY(quietPreview.has_value())

A047 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:147
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:117 asserts the added middle note's frozen preview equals middleExpected (the ramp value at that note's x, quantized by the axis), which entails the has_value() observation. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S103):
  report.expectEqual(middleExpected, Int(page.frozenPreview[middle.id] ?? 0), cppID: drawerVelocityRampID, what: "the halfway middle note previews the interpolated velocity")
Source context since previous assertion:
;

Original expression:
  QVERIFY(middlePreview.has_value())

A048 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:148
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:118 asserts the ramp release endpoint's frozen preview equals 93, which entails the has_value() observation for the last selected note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S104):
  report.expectEqual(93, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp release endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QVERIFY(laterPreview.has_value())

A049 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:149
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:116 asserts the press endpoint's preview is the literal 37, matching the original's literal endpoint value. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S102):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp press endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*quietPreview), int(kRampStartVelocity))

A050 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:150
Disposition: PARTIAL
Mapping/reason: S103 (src/checks/velocity/VelocityPaintDetentChecks.swift:117) asserts the middle preview equals page.axisModel.yToVelocity(velocityRampValue(at: middleHandle.x, x0: pressX, y0: pressY, x1: endX, y1: endY)) computed at :114 - the same interpolation observable over the gesture's own span. Unproved: equality with the original's deliberately non-inferred literal 65, because the expectation is derived from the production ramp helper and axis quantizer rather than an independent constant.
Swift counterpart expression (S103):
  report.expectEqual(middleExpected, Int(page.frozenPreview[middle.id] ?? 0), cppID: drawerVelocityRampID, what: "the halfway middle note previews the interpolated velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*middlePreview), int(kRampMiddleVelocity))

A051 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:151
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:118 asserts the release endpoint's preview is the literal 93, matching the original's literal endpoint value. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S104):
  report.expectEqual(93, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp release endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*laterPreview), int(kRampEndVelocity))

A052 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:152
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:119 asserts page.frozenPreview has no entry for the note outside the selection, the original's absent preview for m_loudStacked. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S105):
  report.expect(page.frozenPreview[notes[1].id] == nil, cppID: drawerVelocityRampID, message: "the note outside the selection previews nothing")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A053 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:153
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:120 asserts document.revision still equals the pre-press snapshot while the ramp preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S106):
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityRampID, what: "the deferred ramp stages no revision before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A054 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:154
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:121 asserts document.history.currentIdentity still equals the pre-gesture identity, so no history entry was staged; public history identity replaces the private QUndoStack::count() read. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S107):
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityRampID, what: "the deferred ramp stages no history entry before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A055 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:155
Disposition: PARTIAL
Mapping/reason: Related predicates S106 (src/checks/velocity/VelocityPaintDetentChecks.swift:120) and S107 (:121) establish that the deferred ramp committed nothing, the no-op the original counts. Unproved: the documentChanged emission count - no document.onChange observer is attached. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S106):
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityRampID, what: "the deferred ramp stages no revision before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentChanged.count(), 0)

A056 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:156
Disposition: PARTIAL
Mapping/reason: Related predicates S106 (:120) and S107 (:121) establish that the deferred ramp committed nothing. Unproved: the SongTab::edited emission count, which has no Swift tab counterpart.
Swift counterpart expression (S107):
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityRampID, what: "the deferred ramp stages no history entry before release")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(edited.count(), 0)

A057 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:157
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:122 asserts the first ramp note's document velocity is unchanged while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S108):
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the first velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A058 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:158
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:123 asserts the added middle note's document velocity is 56 while the preview is live - unchanged and equal to its constructed velocity, the observable of the original middle == kMiddleOriginalVelocity (the Swift fixture uses 56 rather than 42). Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S109):
  report.expectEqual(56, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the middle velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(middle.noteId), int(kMiddleOriginalVelocity))

A059 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:159
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:125 (S122) asserts the ramp-end note's document velocity still equals its captured value while the ramp preview is live, which is the documentVelocity read the original makes before the release; S117 (:134) covers its committed value. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S122):
  report.expectEqual(Int(notes[2].velocity), Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the last velocity during preview")
Swift counterpart expression (S104):
  report.expectEqual(93, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp release endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(m_later.velocity))

A060 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:160
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:124 asserts the unselected sibling's document velocity is unchanged while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S110):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the outside velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A061 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:161
Disposition: PARTIAL
Mapping/reason: S108 (src/checks/velocity/VelocityPaintDetentChecks.swift:122) asserts the first ramp note's document velocity during the preview. Unproved: its timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S108):
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the first velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A062 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:162
Disposition: PARTIAL
Mapping/reason: S109 (src/checks/velocity/VelocityPaintDetentChecks.swift:123) asserts the middle note's document velocity during the preview. Unproved: its timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S109):
  report.expectEqual(56, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the middle velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(middle.noteId), int(kMiddleOriginalVelocity))

A063 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:163
Disposition: PARTIAL
Mapping/reason: Related predicates S104 (src/checks/velocity/VelocityPaintDetentChecks.swift:118, preview) and S117 (:134, commit) for the ramp-end note. Unproved: its pre-release timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S104):
  report.expectEqual(93, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp release endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(m_later.velocity))

A064 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:164
Disposition: PARTIAL
Mapping/reason: S110 (src/checks/velocity/VelocityPaintDetentChecks.swift:124) asserts the unselected note's document velocity during the preview. Unproved: its timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S110):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the outside velocity during preview")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A065 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:165
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:126 asserts session.selectedNoteOrder is exactly the three-note ramp selection while the preview is live. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S111):
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, middle.id, notes[2].id], cppID: drawerVelocityRampID, message: "the preview keeps the ramp selection")
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, middle.noteId, m_later.noteId}))

A066 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:171
Disposition: PARTIAL
Mapping/reason: Related predicates S112 (src/checks/velocity/VelocityPaintDetentChecks.swift:128, revision advanced by exactly one) and S113 (:129, a new history identity) establish the single ramp commit. Unproved: the documentChanged emission count - no document.onChange observer is attached.
Swift counterpart expression (S112):
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityRampID, what: "one ramp release commits one revision")
Source context since previous assertion:
;

    mouseRelease(Qt::LeftButton, rampEnd, Qt::ShiftModifier);

    // The ramp becomes one document command on release, clears every preview,
    // and leaves the non-selected stacked note unchanged in document and view.

Original expression:
  QCOMPARE(documentChanged.count(), 1)

A067 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:172
Disposition: PARTIAL
Mapping/reason: Related predicates S112 (:128) and S113 (:129) establish the single ramp commit. Unproved: the SongTab::edited emission count, which has no Swift tab counterpart.
Swift counterpart expression (S113):
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityRampID, message: "one ramp release makes one history entry")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(edited.count(), 1)

A068 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:173
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:128 asserts document.revision is exactly one past the pre-gesture snapshot after the ramp release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S112):
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityRampID, what: "one ramp release commits one revision")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore + 1)

A069 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:174
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:129 asserts the history identity changed on release, i.e. one newly committed entry for the ramp transaction; this replaces the private QUndoStack::count() delta. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S113):
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityRampID, message: "one ramp release makes one history entry")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore + 1)

A070 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:175
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:130 asserts document.history.canUndo after the ramp release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S114):
  report.expect(document.history.canUndo, cppID: drawerVelocityRampID, message: "the ramp entry is undoable")
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->history().canUndo())

A071 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:176
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:131 asserts the frozen preview map is empty (and no live gesture) after release, so the first ramp note keeps no preview. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S115):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityRampID, message: "the ramp release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A072 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:177
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:131 asserts the whole frozen preview map is empty after release, which covers the middle note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S115):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityRampID, message: "the ramp release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(middle.noteId).has_value())

A073 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:178
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:131 asserts the whole frozen preview map is empty after release, which covers the last ramp note. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S115):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityRampID, message: "the ramp release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A074 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:179
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:131 asserts the whole frozen preview map is empty after release, which covers the unselected sibling. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S115):
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityRampID, message: "the ramp release clears its preview")
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A075 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:180
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:132 (S123) asserts the first ramp note's committed document velocity is the literal 37 after the release, the original's post-release documentVelocity read; S102 (:116) covers its preview. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S123):
  report.expectEqual(37, Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the first ramp velocity")
Swift counterpart expression (S102):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp press endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kRampStartVelocity))

A076 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:181
Disposition: PARTIAL
Mapping/reason: S116 (src/checks/velocity/VelocityPaintDetentChecks.swift:133) asserts the committed middle velocity equals middleExpected, the value derived from the production ramp helper and axis quantizer at :114. Unproved: equality with the original's independent literal 65.
Swift counterpart expression (S116):
  report.expectEqual(middleExpected, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the interpolated middle velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(middle.noteId), int(kRampMiddleVelocity))

A077 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:182
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:134 asserts document.note(notes[2])?.velocity == 93 after the ramp release, the original's literal last-ramp value. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S117):
  report.expectEqual(93, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the last ramp velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kRampEndVelocity))

A078 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:183
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:135 asserts the unselected note's document velocity is unchanged by the ramp release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S118):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release leaves the outside note alone")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A079 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:184
Disposition: PARTIAL
Mapping/reason: Related predicate S102 (src/checks/velocity/VelocityPaintDetentChecks.swift:116, the first note's preview 37). Unproved: any post-commit read of the first ramp note - the release side asserts only the committed middle (S116, :133), the committed last note (S117, :134) and the untouched outside note (S118, :135), so neither its document nor its projected value is re-read.
Swift counterpart expression (S102):
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp press endpoint previews its literal velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(kRampStartVelocity))

A080 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:185
Disposition: PARTIAL
Mapping/reason: S116 (src/checks/velocity/VelocityPaintDetentChecks.swift:133) asserts the committed middle note's document value (production-derived). Unproved: its post-commit timeline projection and an independent literal check of 65.
Swift counterpart expression (S116):
  report.expectEqual(middleExpected, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the interpolated middle velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(middle.noteId), int(kRampMiddleVelocity))

A081 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:186
Disposition: PARTIAL
Mapping/reason: S117 (src/checks/velocity/VelocityPaintDetentChecks.swift:134) asserts the committed last ramp note's document value 93. Unproved: its timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S117):
  report.expectEqual(93, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the last ramp velocity")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(kRampEndVelocity))

A082 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:187
Disposition: PARTIAL
Mapping/reason: S118 (src/checks/velocity/VelocityPaintDetentChecks.swift:135) asserts the unselected note's document value is unchanged after the release. Unproved: its timeline projection (original helper tst_velocityediting.cpp:431-442).
Swift counterpart expression (S118):
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release leaves the outside note alone")
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A083 | VelocityEditingTest::rampCommitsOnce | src/checks/velocity/velocitypainting.cpp:188
Disposition: MATCHED
Mapping/reason: drawerVelocityRampCommitsOnce at src/checks/velocity/VelocityPaintDetentChecks.swift:136 asserts session.selectedNoteOrder is still the three-note ramp selection after the release. Executed by runVelocityPageChecks (VelocityPageChecks.swift:177).
Swift counterpart expression (S119):
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, middle.id, notes[2].id], cppID: drawerVelocityRampID, message: "the release keeps the ramp selection")
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, middle.noteId, m_later.noteId}))

Remaining original source (closing sequence and helper bodies):
;
}

Swift executable assertion index
The S entries are exact predicates already called by the suite. Their presence alone
does not close any A entry. Preserve their surrounding setup and order in the paired source.

S001 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:14
  report.fail(drawerVelocityTransactionID, "the synthetic fixture published fewer than three notes")

S002 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:26
  report.expectEqual(baseline.revision + 1, committed.revision, cppID: drawerVelocityTransactionID,
                     what: "one released drag advances the document revision once")

S003 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:28
  report.expect(committed.identity != baseline.identity, cppID: drawerVelocityTransactionID,
                message: "one released drag makes exactly one history entry")

S004 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:30
  report.expect(committed.canUndo && !baseline.canUndo, cppID: drawerVelocityTransactionID,
                message: "the drag's entry becomes undoable")

S005 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:32
  report.expect(document.note(notes[0].id)?.velocity != notes[0].velocity, cppID: drawerVelocityTransactionID,
                message: "the drag's commit reached the document")

S006 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:34
  report.expect(document.note(notes[1].id)?.velocity != notes[1].velocity, cppID: drawerVelocityTransactionID,
                message: "every selected target moved with the gesture")

S007 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:36
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityTransactionID,
                message: "a released gesture clears its preview")

S008 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:38
  report.expect(fixture.handle(notes[0])?.y != before, cppID: drawerVelocityProjectionID,
                message: "the published handle follows the committed value (before \(before) "
                    + "after \(fixture.handle(notes[0])?.y ?? -1) velocity "
                    + "\(document.note(notes[0].id)?.velocity ?? 0) level "
                    + "\(fixture.handle(notes[0])?.level ?? -99)")

S009 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:45
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores the captured velocity")

S010 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:47
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores every target of the transaction")

S011 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:50
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityHistoryID,
                     what: "Undo rebuilds the page without losing its handles")

S012 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:53
  report.expectEqual(committed.identity, drawerVelocityDocumentSnapshot(document).identity, cppID: drawerVelocityHistoryID,
                     what: "Redo restores the committed history identity")

S013 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:61
  report.expectEqual(pointerBaseline.revision, drawerVelocityDocumentSnapshot(document).revision,
                     cppID: drawerVelocityTransactionID, what: "a stationary click records no history")

S014 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:63
  report.expect(fixture.session.selectedNotes == [notes[0].id], cppID: drawerVelocityTransactionID,
                message: "a stationary click selects only its own note")

S015 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:74
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "a live drag previews before release")

S016 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:76
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                    message: "a live drag reports an active interaction")

S017 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:78
  report.expect(page.handleEscape(), cppID: drawerVelocityCancellationID,
                    message: "Escape is claimed while a gesture is live")

S018 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:81
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityCancellationID,
                message: "Escape clears the frozen preview")

S019 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:83
  report.expect(drawerVelocityDocumentSnapshot(document) == cancelBaseline, cppID: drawerVelocityCancellationID,
                message: "Escape writes nothing at all")

S020 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:85
  report.expect(fixture.session.selectedNoteOrder == [notes[2].id, notes[0].id], cppID: drawerVelocityCancellationID,
                message: "Escape restores selection membership and insertion order")

S021 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:96
  report.expectEqual(foreignRevision + 1, document.revision, cppID: drawerVelocityCancellationID,
                         what: "a release after a foreign change writes nothing")

S022 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:98
  report.expect(!page.hasGesture && page.frozenPreview.isEmpty, cppID: drawerVelocityCancellationID,
                    message: "a stale release ends the gesture")

S023 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:109
  report.expect(page.pointerPress(x: 10, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "the ruler consumes its own press")

S024 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:112
  report.expectEqual(rulerBaseline.revision + 1, document.revision, cppID: drawerVelocityTransactionID,
                     what: "one ruler click makes one revision")

S025 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:114
  report.expect(document.history.currentIdentity != rulerBaseline.identity, cppID: drawerVelocityTransactionID,
                message: "one ruler click makes one history entry")

S026 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:116
  report.expect(!page.pointerPress(x: 200, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "a press inside the plot is not the ruler's")

S027 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:130
  report.expect(fixture.session.camera.snapshot.scrollX > panScroll, cppID: drawerVelocityTransactionID,
                message: "a middle drag pans the shared camera")

S028 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:132
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                message: "a live pan suspends follow through the page's own interaction")

S029 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:135
  report.expect(!page.hasGesture, cppID: drawerVelocityTransactionID, message: "the pan ends on release")

S030 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:136
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "panning writes no document state")

S031 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:143
  report.expect(page.detentsAvailable && page.detentsEnabled, cppID: drawerVelocityTransactionID,
                message: "a PSG context offers the detent control, enabled by default")

S032 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:145
  report.expectEqual(VelocityAxisModel.Mode.intrinsic.rawValue, page.axisMode,
                     cppID: drawerVelocityTransactionID,
                     what: "the PSG context presents the intrinsic ruler")

S033 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:148
  report.expect(page.axisGraduationsVisible, cppID: drawerVelocityTransactionID,
                message: "an enabled detent set draws the level graduations")

S034 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:153
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "the detent case starts from a live preview")

S035 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:156
  report.expect(!page.detentsEnabled && page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "toggling detents cancels the live interaction")

S036 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:159
  report.expect(page.axisGraduationsVisible == false, cppID: drawerVelocityTransactionID,
                message: "disabled detents put the ruler on the continuous rendering")

S037 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:161
  report.expect(page.axisModel.mode == .intrinsic, cppID: drawerVelocityTransactionID,
                message: "the voice's own map stays intrinsic under the ruler switch")

S038 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:165
  report.expect(abs(handle.y - exactY) < 0.001, cppID: drawerVelocityTransactionID,
                    message: "a disabled detent set places nodes at their exact velocity")

S039 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:168
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "the detent toggle writes no document state")

S040 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:181
  report.expectEqual(bandBaseline.revision, document.revision, cppID: drawerVelocityTransactionID,
                     what: "a band selection writes nothing")

S041 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:183
  report.expectEqual(3, fixture.session.selectedNotes.count, cppID: drawerVelocityTransactionID,
                     what: "the band selected every note it covered")

S042 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:14
  report.fail(drawerVelocityGestureID, "the synthetic fixture published fewer than three notes")

S043 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:18
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityProjectionID,
                     what: "every note of the primary track publishes one handle")

S044 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:23
  report.fail(drawerVelocityProjectionID, "the fixture's notes have no published handles")

S045 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:27
  report.expect(!firstHandle.selected, cppID: drawerVelocityProjectionID,
                message: "a fresh selection publishes no selected handle")

S046 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:29
  report.expectEqual(openingMap.level(of: Int(notes[0].velocity)) ?? -1, firstHandle.level,
                     cppID: drawerVelocityProjectionID,
                     what: "an intrinsic handle publishes the level of its displayed value")

S047 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:32
  report.expect(abs(firstHandle.x - page.axisModel.geometry.labelWidth - 56) < 1.0
                    || firstHandle.x <= 400, cppID: drawerVelocityProjectionID,
                message: "the handle's x is a plot-local camera projection")

S048 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:35
  report.expect(fixture.handle(notes[0])!.y < firstHandle.hitRadius * 2
                    || firstHandle.y > 0, cppID: drawerVelocityProjectionID,
                message: "the handle publishes a drawn y inside the body")

S049 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:52
  report.expect(!gesture.relativeActivated && gesture.preview.isEmpty, cppID: drawerVelocityGestureID,
                message: "a drag inside the activation distance previews nothing")

S050 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:55
  report.expect(gesture.relativeActivated, cppID: drawerVelocityGestureID,
                message: "leaving the activation distance arms the relative drag")

S051 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:57
  report.expectEqual(30, Int(gesture.preview[NoteID(1)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the low note takes the whole delta")

S052 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:59
  report.expectEqual(127, Int(gesture.preview[NoteID(2)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the high note clamps to the maximum instead of wrapping")

S053 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:61
  report.expectEqual(2, VelocityGesturePolicy.updates(gesture).count, cppID: drawerVelocityGestureID,
                     what: "the commit payload carries one update per frozen note")

S054 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:78
  report.expectEqual(Int(psgMap.representative(9)), Int(psgGesture.preview[NoteID(3)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the first level follows the level delta")

S055 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:80
  report.expectEqual(Int(psgMap.representative(11)), Int(psgGesture.preview[NoteID(4)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the second level keeps its own offset")

S056 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:82
  report.expectEqual(127,
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: true, y: 0)),
                     cppID: drawerVelocityGestureID,
                     what: "the detent unlock takes exact MIDI velocity")

S057 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:87
  report.expectEqual(Int(psgMap.canonicalize(76)),
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: false,
                                                                y: psgAxis.levelToY(9))),
                     cppID: drawerVelocityGestureID,
                     what: "a locked intrinsic drag canonicalizes onto the note's own map")

S058 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:104
  report.expectEqual(2, painted.count, cppID: drawerVelocityGestureID,
                     what: "the sweep covers every node the column reaches")

S059 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:106
  report.expect(painted[0].velocity < painted[1].velocity, cppID: drawerVelocityGestureID,
                message: "the painted ramp follows the swept line's own direction")

S060 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:111
  report.expectEqual(1, partial.count, cppID: drawerVelocityGestureID,
                     what: "a sweep that stops short leaves the far node alone")

S061 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:116
  report.expectEqual(1, singleColumn.count, cppID: drawerVelocityGestureID,
                     what: "a stationary paint only takes the nodes in its own column")

S062 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:128
  report.expectEqual(100, Int(ramp.preview[NoteID(5)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the note at the press position keeps the press velocity")

S063 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:130
  report.expectEqual(75, Int(ramp.preview[NoteID(7)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note inside the ramp span takes the swept line's value")

S064 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:132
  report.expectEqual(40, Int(ramp.preview[NoteID(6)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note outside the ramp span keeps its captured velocity")

S065 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:142
  report.expect(VelocityGesturePolicy.updates(flatState).isEmpty, cppID: drawerVelocityGestureID,
                message: "a preview that matches the captured values commits nothing")

S066 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:148
  report.expect(moved.count == 1 && moved[0].noteID == movedNote.noteID
                && moved[0].velocity == Int(movedNote.velocity) + 7,
                cppID: drawerVelocityGestureID,
                message: "only the values that differ from the captured ones are committed")

S067 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:152
  report.expect(abs(velocityRampValue(at: 20, x0: 0, y0: 0, x1: 40, y1: 40) - 20) < 1e-9,
                cppID: drawerVelocityGestureID, message: "the ramp interpolates linearly")

S068 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:154
  report.expect(abs(velocityRampValue(at: 5, x0: 10, y0: 3, x1: 10, y1: 9) - 9) < 1e-9,
                cppID: drawerVelocityGestureID, message: "a vertical span resolves to the near endpoint")

S069 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:156
  report.expect(thirdHandle.hitRadius > 0, cppID: drawerVelocityGestureID,
                message: "the published hit radius is font-relative and positive")

S070 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:225
  report.expect(!grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity is unavailable with no selection")

S071 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:239
  report.expect(grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity becomes available with a selection")

S072 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:243
  report.expectEqual(1, requested, cppID: drawerVelocityCommandID,
                     what: "the existing command row asks its owner for the prompt")

S073 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:245
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "the command commits no value before prompt acceptance")

S074 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:250
  report.expectEqual(2, requested, cppID: drawerVelocityCommandID,
                     what: "the command routes every execution through the same owner")

S075 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:252
  report.expect(fixture.page.promptOpen, cppID: drawerVelocityCommandID,
                message: "the routed command opened the page's captured prompt")

S076 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:255
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "cancelling the routed prompt still commits nothing")

S077 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:259
  report.expectEqual(EditNotesOperation.setVelocity.rawValue,
                     row?.notesOperation.rawValue ?? -1, cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity on the notes arm")

S078 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:262
  report.expectEqual(EditKeyRoute.alwaysConsume.rawValue, row?.keyRoute.rawValue ?? -1,
                     cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity's consume route")

S079 | drawerVelocityPaintAddNote | src/checks/velocity/VelocityPaintDetentChecks.swift:19
  report.fail(cppID, "the added note published no identity")

S080 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:49
  report.expectEqual(VelocityAxisModel.Mode.continuous.rawValue, page.axisMode, cppID: drawerVelocityPaintID, what: "the mixed square and noise selection presents the continuous ruler")

S081 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:58
  report.expect(pressX != endX, cppID: drawerVelocityPaintID, message: "the paint endpoints span two note columns")

S082 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:62
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityPaintID, what: "the press column previews its literal paint velocity")

S083 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:63
  report.expectEqual(91, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityPaintID, what: "the release column previews its literal paint velocity")

S084 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:64
  report.expect(page.frozenPreview[notes[1].id] == nil, cppID: drawerVelocityPaintID, message: "the unselected note previews nothing")

S085 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:65
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityPaintID, what: "the deferred paint stages no revision before release")

S086 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:66
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityPaintID, what: "the deferred paint stages no history entry before release")

S087 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:67
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the first velocity during preview")

S088 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:68
  report.expectEqual(Int(notes[2].velocity), Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the last velocity during preview")

S089 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:69
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the document keeps the unselected velocity during preview")

S090 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:70
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, notes[2].id], cppID: drawerVelocityPaintID, message: "the preview keeps the paint selection")

S091 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:72
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityPaintID, what: "one paint release commits one revision")

S092 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:73
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityPaintID, message: "one paint release makes one history entry")

S093 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:74
  report.expect(document.history.canUndo, cppID: drawerVelocityPaintID, message: "the paint entry is undoable")

S094 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:75
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityPaintID, message: "the paint release clears its preview")

S095 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:76
  report.expectEqual(37, Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the first literal velocity")

S096 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:77
  report.expectEqual(91, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release commits the last literal velocity")

S097 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:78
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityPaintID, what: "the release leaves the unselected note alone")

S098 | drawerVelocityPaintCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:79
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, notes[2].id], cppID: drawerVelocityPaintID, message: "the release keeps the paint selection")

S099 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:101
  report.expectEqual(VelocityAxisModel.Mode.continuous.rawValue, page.axisMode, cppID: drawerVelocityRampID, what: "the mixed ramp selection presents the continuous ruler")

S100 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:110
  report.expect(pressX != endX, cppID: drawerVelocityRampID, message: "the ramp endpoints span two note columns")

S101 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:111
  report.expect(middleHandle.x > min(pressX, endX) && middleHandle.x < max(pressX, endX), cppID: drawerVelocityRampID, message: "the middle note sits inside the swept span")

S102 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:116
  report.expectEqual(37, Int(page.frozenPreview[notes[0].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp press endpoint previews its literal velocity")

S103 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:117
  report.expectEqual(middleExpected, Int(page.frozenPreview[middle.id] ?? 0), cppID: drawerVelocityRampID, what: "the halfway middle note previews the interpolated velocity")

S104 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:118
  report.expectEqual(93, Int(page.frozenPreview[notes[2].id] ?? 0), cppID: drawerVelocityRampID, what: "the ramp release endpoint previews its literal velocity")

S105 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:119
  report.expect(page.frozenPreview[notes[1].id] == nil, cppID: drawerVelocityRampID, message: "the note outside the selection previews nothing")

S106 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:120
  report.expectEqual(baseline.revision, document.revision, cppID: drawerVelocityRampID, what: "the deferred ramp stages no revision before release")

S107 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:121
  report.expectEqual(baseline.identity, document.history.currentIdentity, cppID: drawerVelocityRampID, what: "the deferred ramp stages no history entry before release")

S108 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:122
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the first velocity during preview")

S109 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:123
  report.expectEqual(56, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the middle velocity during preview")

S110 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:124
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the outside velocity during preview")

S111 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:126
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, middle.id, notes[2].id], cppID: drawerVelocityRampID, message: "the preview keeps the ramp selection")

S112 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:128
  report.expectEqual(baseline.revision + 1, document.revision, cppID: drawerVelocityRampID, what: "one ramp release commits one revision")

S113 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:129
  report.expect(document.history.currentIdentity != baseline.identity, cppID: drawerVelocityRampID, message: "one ramp release makes one history entry")

S114 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:130
  report.expect(document.history.canUndo, cppID: drawerVelocityRampID, message: "the ramp entry is undoable")

S115 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:131
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityRampID, message: "the ramp release clears its preview")

S116 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:133
  report.expectEqual(middleExpected, Int(document.note(middle.id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the interpolated middle velocity")

S117 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:134
  report.expectEqual(93, Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the last ramp velocity")

S118 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:135
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release leaves the outside note alone")

S119 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:136
  report.expect(fixture.session.selectedNoteOrder == [notes[0].id, middle.id, notes[2].id], cppID: drawerVelocityRampID, message: "the release keeps the ramp selection")

S120 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:96
  report.expectEqual(48, Int(middle.tick), cppID: drawerVelocityRampID, what: "the added middle note resolves at its placed tick")

S121 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:97
  report.expectEqual(60, Int(middle.pitch), cppID: drawerVelocityRampID, what: "the added middle note resolves at its placed pitch")

S122 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:125
  report.expectEqual(Int(notes[2].velocity), Int(document.note(notes[2].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the document keeps the last velocity during preview")

S123 | drawerVelocityRampCommitsOnce | src/checks/velocity/VelocityPaintDetentChecks.swift:132
  report.expectEqual(37, Int(document.note(notes[0].id)?.velocity ?? 0), cppID: drawerVelocityRampID, what: "the release commits the first ramp velocity")

Tally: MATCHED 52, PARTIAL 25, GAP 0, RETIRED-REPRESENTATION 6
