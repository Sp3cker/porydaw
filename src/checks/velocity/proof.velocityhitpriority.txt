Assertion correspondence: velocity/velocityhitpriority.cpp

Reference revision: f842f7e128adea2958d0967e2afbd834893f21b4
Original: src/checks/velocity/velocityhitpriority.cpp
Original SHA-256: cc68c41551cf9a30c8f38d18633615a1870080984336a78cf738addea4f782df
Swift counterpart: src/checks/velocity/tst_velocityediting.swift
Swift SHA-256: d10757673b1ecbbd0e49c6e6592b4415990d6e986f0cd6a555d2c155661d8e62
Additional Swift counterpart: src/checks/velocity/velocitydetentdragging.swift
Additional Swift SHA-256: 8449b61bab2cd9f39f86b2769ad6f0165307d877e188c190acff337829dc26cb
Additional Swift counterpart: src/checks/velocity/VelocityPageChecks.swift
Additional Swift SHA-256: 0aabd70533f5efc4ce77ceb10d2470f0ea2fd619fd2e4e407fb9d8beb326737b

Purpose and status
Each A entry is one original assertion or failure site, including helper and setup sites.
Repeated rows and loops execute a site more than once. The source SHA pins the entire
original sequence; source context preserves setup, rows, loops, and guards between
assertions verbatim, and the tail preserves the closing sequence verbatim.
MATCHED requires identified equivalent predicates and execution evidence; PARTIAL means
a specific related scenario differs; GAP means exact assertion correspondence is not
established; NATIVE-SETUP identifies a retained C++ loader/window/input precondition.
A method name or related Swift case alone never establishes an assertion match.
This inventory does not authorize removing native checks. Unresolved entries remain work.

Execution and ownership
drawerVelocityGestureTransactions (velocity/tst_velocityediting.swift),
drawerVelocityFrozenGesturePolicy (velocity/velocitydetentdragging.swift), and
drawerVelocityCommandAvailability (swiftcore/VelocityPageChecks.swift) execute the
paired checks. Shared setup lives in swiftcore/VelocityPageChecks.swift
(drawerVelocityVelocityPageFixture, drawerVelocityVelocityFixture); per-case document
and gesture state live in the paired Swift sources. Their helper expectations execute
on every call and are inventoried below.

Verification evidence: pending orchestrator run

Fixture, sequence and row constraints
No QTest data rows; each method runs once.
Cases run on the shared VelocityEditingTest in-memory fixture: three notes resolved
in SMF event order (quiet stacked duplicate, loud stacked duplicate, tick-60 later
note); assertions read the documentVelocity/timelineVelocity/noteSelectionIs helpers.
See proof.tst_velocityediting.txt for the init-site inventory.
Hit-priority cases around an added overlap note (tick 20, key 60, duration 24,
velocity 20). selectedCircleWinsOverStem, unselectedCircleWinsOverSelectedStem,
selectedStemWinsAtStackedStem, movedNodeDoesNotClickThrough, and
rightPressPreservesSelectedGroup press node points and must preserve selection,
revision, undo depth, and every fixture velocity. No data rows.
Methods in file order: selectedCircleWinsOverStem, unselectedCircleWinsOverSelectedStem, selectedStemWinsAtStackedStem, movedNodeDoesNotClickThrough, rightPressPreservesSelectedGroup.
Exact original source anchors:
src/checks/velocity/velocityhitpriority.cpp:24 | m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);
src/checks/velocity/velocityhitpriority.cpp:30 | m_tab->view().selectionModel().setNoteSelection({overlap.noteId});
src/checks/velocity/velocityhitpriority.cpp:35 | focusVelocityBand();
src/checks/velocity/velocityhitpriority.cpp:36 | mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));
src/checks/velocity/velocityhitpriority.cpp:39 | mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));
src/checks/velocity/velocityhitpriority.cpp:52 | m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);
src/checks/velocity/velocityhitpriority.cpp:58 | m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId});
src/checks/velocity/velocityhitpriority.cpp:63 | focusVelocityBand();
src/checks/velocity/velocityhitpriority.cpp:64 | mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));
src/checks/velocity/velocityhitpriority.cpp:67 | mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));
src/checks/velocity/velocityhitpriority.cpp:81 | m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);
src/checks/velocity/velocityhitpriority.cpp:88 | m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId});
src/checks/velocity/velocityhitpriority.cpp:93 | focusVelocityBand();
src/checks/velocity/velocityhitpriority.cpp:94 | mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kStemOnlyTick)));
src/checks/velocity/velocityhitpriority.cpp:97 | mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kStemOnlyTick)));
src/checks/velocity/velocityhitpriority.cpp:114 | focusVelocityBand();
src/checks/velocity/velocityhitpriority.cpp:115 | mousePress(Qt::LeftButton, windowPoint(nodePoint(kLoudVelocity, 60)));
src/checks/velocity/velocityhitpriority.cpp:118 | mouseMove(windowPoint(nodePoint(kLoudVelocity, 12)));
src/checks/velocity/velocityhitpriority.cpp:123 | mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kLoudVelocity, 12)));
src/checks/velocity/velocityhitpriority.cpp:139 | m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId, m_later.noteId});
src/checks/velocity/velocityhitpriority.cpp:144 | focusVelocityBand();
src/checks/velocity/velocityhitpriority.cpp:145 | mousePress(Qt::RightButton, windowPoint(nodePoint(kQuietVelocity, 12)));
src/checks/velocity/velocityhitpriority.cpp:148 | mouseRelease(Qt::RightButton, windowPoint(nodePoint(kQuietVelocity, 12)));

Original assertion inventory

A001 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:27
Disposition: MATCHED
Mapping/reason: S079 executes `("A001", selected.document.note(selectedOverlap) != nil)`; the real SongDocument loads the same tick-20, key-60, duration-24, velocity-20 overlap, so the assigned NoteID is the Swift admission predicate and the following production page press sequence is unchanged. Verification evidence: pending orchestrator run
Source context since previous assertion:
#include "checks/velocity/tst_velocityediting.h"

#include <QtTest>

#include <cstdint>
#include <optional>

#include "core/songdocument.h"
#include "ui/songview.h"

namespace {

constexpr uint64_t kOverlapTick = 20;
constexpr uint64_t kStemOnlyTick = 16;
constexpr uint8_t kOverlapKey = 60;
constexpr uint32_t kOverlapDuration = 24;
constexpr uint8_t kQuietVelocity = 20;
constexpr uint8_t kLoudVelocity = 70;

} // namespace

void VelocityEditingTest::selectedCircleWinsOverStem()
{
    m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);

    DocNote overlap;

Original expression:
  QVERIFY(m_tab->document().findNote(0, kOverlapTick, kOverlapKey, &overlap))

A002 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:28
Disposition: MATCHED
Mapping/reason: S080 executes `("A002", priorityTimelineVelocity(selected, selectedOverlap) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(timelineVelocity(overlap.noteId), int(kQuietVelocity))

A003 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:31
Disposition: MATCHED
Mapping/reason: S081 executes `("A003", selected.session.selectedNoteOrder == [selectedOverlap])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    m_tab->view().selectionModel().setNoteSelection({overlap.noteId});

Original expression:
  QVERIFY(noteSelectionIs({overlap.noteId}))

A004 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:37
Disposition: NATIVE
Mapping/reason: Asserts that the native QQuickWindow acquired a mouse grab after a real QTest mouse press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();

    focusVelocityBand();
    mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));

Original expression:
  QTRY_VERIFY(m_quickWindow->mouseGrabberItem())

A005 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:38
Disposition: MATCHED
Mapping/reason: S082 executes `("A005", selected.page.observation.targetNoteID == selectedOverlap && selected.session.selectedNoteOrder == [selectedOverlap])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({overlap.noteId}))

A006 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:41
Disposition: MATCHED
Mapping/reason: S083 executes `("A006", selected.document.revision == selectedBefore.revision)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));


Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A007 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:42
Disposition: MATCHED
Mapping/reason: S084 executes `("A007", selected.document.history.currentIdentity == selectedBefore.identity)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A008 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:43
Disposition: MATCHED
Mapping/reason: S085 executes `("A008", selected.document.note(selectedOverlap)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(overlap.noteId), int(kQuietVelocity))

A009 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:44
Disposition: MATCHED
Mapping/reason: S086 executes `("A009", selected.document.note(selectedNotes[0].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A010 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:45
Disposition: MATCHED
Mapping/reason: S087 executes `("A010", selected.document.note(selectedNotes[1].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A011 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:46
Disposition: MATCHED
Mapping/reason: S088 executes `("A011", selected.document.note(selectedNotes[2].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLoudVelocity))

A012 | VelocityEditingTest::selectedCircleWinsOverStem | src/checks/velocity/velocityhitpriority.cpp:47
Disposition: MATCHED
Mapping/reason: S089 executes `("A012", selected.session.selectedNoteOrder == [selectedOverlap])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({overlap.noteId}))

A013 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:55
Disposition: MATCHED
Mapping/reason: S090 executes `("A013", unselected.document.note(unselectedOverlap) != nil)`; the real SongDocument loads the same overlap alongside the selected quiet stem, so the assigned NoteID establishes the original admission precondition before the identical page press. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::unselectedCircleWinsOverSelectedStem()
{
    m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);

    DocNote overlap;

Original expression:
  QVERIFY(m_tab->document().findNote(0, kOverlapTick, kOverlapKey, &overlap))

A014 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:56
Disposition: MATCHED
Mapping/reason: S091 executes `("A014", priorityTimelineVelocity(unselected, unselectedOverlap) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(timelineVelocity(overlap.noteId), int(kQuietVelocity))

A015 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:59
Disposition: MATCHED
Mapping/reason: S092 executes `("A015", unselected.session.selectedNoteOrder == [unselectedNotes[0].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId});

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId}))

A016 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:65
Disposition: NATIVE
Mapping/reason: Asserts that the native QQuickWindow acquired a mouse grab after a real QTest mouse press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();

    focusVelocityBand();
    mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));

Original expression:
  QTRY_VERIFY(m_quickWindow->mouseGrabberItem())

A017 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:66
Disposition: MATCHED
Mapping/reason: S093 executes `("A017", unselected.page.observation.targetNoteID == unselectedOverlap && unselected.session.selectedNoteOrder == [unselectedOverlap])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({overlap.noteId}))

A018 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:69
Disposition: MATCHED
Mapping/reason: S094 executes `("A018", unselected.document.revision == unselectedBefore.revision)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kOverlapTick)));


Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A019 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:70
Disposition: MATCHED
Mapping/reason: S095 executes `("A019", unselected.document.history.currentIdentity == unselectedBefore.identity)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A020 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:71
Disposition: MATCHED
Mapping/reason: S096 executes `("A020", unselected.document.note(unselectedOverlap)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(overlap.noteId), int(kQuietVelocity))

A021 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:72
Disposition: MATCHED
Mapping/reason: S097 executes `("A021", unselected.document.note(unselectedNotes[0].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A022 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:73
Disposition: MATCHED
Mapping/reason: S098 executes `("A022", unselected.document.note(unselectedNotes[1].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A023 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:74
Disposition: MATCHED
Mapping/reason: S099 executes `("A023", unselected.document.note(unselectedNotes[2].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLoudVelocity))

A024 | VelocityEditingTest::unselectedCircleWinsOverSelectedStem | src/checks/velocity/velocityhitpriority.cpp:75
Disposition: MATCHED
Mapping/reason: S100 executes `("A024", unselected.session.selectedNoteOrder == [unselectedOverlap])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({overlap.noteId}))

A025 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:84
Disposition: MATCHED
Mapping/reason: S101 executes `("A025", stem.document.note(stemOverlap) != nil)`; the real SongDocument loads the same overlap with both stacked stems at velocity 20, so the assigned NoteID establishes the original admission precondition before the identical stem press. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::selectedStemWinsAtStackedStem()
{
    m_tab->document().setNotesVelocity({m_loudStacked}, kQuietVelocity);
    m_tab->document().addNote(0, kOverlapTick, kOverlapKey, kOverlapDuration, kQuietVelocity);

    DocNote overlap;

Original expression:
  QVERIFY(m_tab->document().findNote(0, kOverlapTick, kOverlapKey, &overlap))

A026 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:85
Disposition: MATCHED
Mapping/reason: S102 executes `("A026", priorityTimelineVelocity(stem, stemNotes[1].id) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(timelineVelocity(m_loudStacked.noteId), int(kQuietVelocity))

A027 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:86
Disposition: MATCHED
Mapping/reason: S103 executes `("A027", priorityTimelineVelocity(stem, stemOverlap) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(timelineVelocity(overlap.noteId), int(kQuietVelocity))

A028 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:89
Disposition: MATCHED
Mapping/reason: S104 executes `("A028", stem.session.selectedNoteOrder == [stemNotes[0].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId});

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId}))

A029 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:95
Disposition: NATIVE
Mapping/reason: Asserts that the native QQuickWindow acquired a mouse grab after a real QTest mouse press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();

    focusVelocityBand();
    mousePress(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kStemOnlyTick)));

Original expression:
  QTRY_VERIFY(m_quickWindow->mouseGrabberItem())

A030 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:96
Disposition: MATCHED
Mapping/reason: S105 executes `("A030", stem.page.observation.targetNoteID == stemNotes[0].id && stem.session.selectedNoteOrder == [stemNotes[0].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId}))

A031 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:99
Disposition: MATCHED
Mapping/reason: S106 executes `("A031", stem.document.revision == stemBefore.revision)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kQuietVelocity, kStemOnlyTick)));


Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A032 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:100
Disposition: MATCHED
Mapping/reason: S107 executes `("A032", stem.document.history.currentIdentity == stemBefore.identity)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A033 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:101
Disposition: MATCHED
Mapping/reason: S108 executes `("A033", stem.document.note(stemOverlap)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(overlap.noteId), int(kQuietVelocity))

A034 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:102
Disposition: MATCHED
Mapping/reason: S109 executes `("A034", stem.document.note(stemNotes[0].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A035 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:103
Disposition: MATCHED
Mapping/reason: S110 executes `("A035", stem.document.note(stemNotes[1].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(kQuietVelocity))

A036 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:104
Disposition: MATCHED
Mapping/reason: S111 executes `("A036", stem.document.note(stemNotes[2].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLoudVelocity))

A037 | VelocityEditingTest::selectedStemWinsAtStackedStem | src/checks/velocity/velocityhitpriority.cpp:105
Disposition: MATCHED
Mapping/reason: S112 executes `("A037", stem.session.selectedNoteOrder == [stemNotes[0].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId}))

A038 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:110
Disposition: MATCHED
Mapping/reason: S113 executes `("A038", moved.session.selectedNotes.isEmpty)]) _ = moved.page.pointerPress(x: laterHandle.x, y: laterHandle.y, surface: 1`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::movedNodeDoesNotClickThrough()
{

Original expression:
  QVERIFY(m_tab->view().selectionModel().noteSelection().empty())

A039 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:116
Disposition: NATIVE
Mapping/reason: Asserts that the native QQuickWindow acquired a mouse grab after a real QTest mouse press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();

    focusVelocityBand();
    mousePress(Qt::LeftButton, windowPoint(nodePoint(kLoudVelocity, 60)));

Original expression:
  QTRY_VERIFY(m_quickWindow->mouseGrabberItem())

A040 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:117
Disposition: MATCHED
Mapping/reason: S114 executes `("A040", moved.page.observation.targetNoteID == movedNotes[2].id && moved.session.selectedNoteOrder == [movedNotes[2].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_later.noteId}))

A041 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:119
Disposition: MATCHED
Mapping/reason: S115 executes `("A041", moved.page.frozenPreview[movedNotes[2].id] != nil)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseMove(windowPoint(nodePoint(kLoudVelocity, 12)));

Original expression:
  QTRY_VERIFY(m_tab->view().previewVelocity(m_later.noteId).has_value())

A042 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:121
Disposition: MATCHED
Mapping/reason: S116 executes `("A042", moved.page.frozenPreview[movedNotes[2].id] != nil)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    const std::optional<uint8_t> preview = m_tab->view().previewVelocity(m_later.noteId);

Original expression:
  QVERIFY(preview.has_value())

A043 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:122
Disposition: MATCHED
Mapping/reason: S117 executes `("A043", moved.page.frozenPreview[movedNotes[2].id] == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*preview), int(kLoudVelocity))

A044 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:125
Disposition: MATCHED
Mapping/reason: S118 executes `("A044", moved.page.frozenPreview[movedNotes[2].id] == nil)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseRelease(Qt::LeftButton, windowPoint(nodePoint(kLoudVelocity, 12)));


Original expression:
  QTRY_VERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A045 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:126
Disposition: MATCHED
Mapping/reason: S119 executes `("A045", moved.document.revision == movedBefore.revision)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A046 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:127
Disposition: MATCHED
Mapping/reason: S120 executes `("A046", moved.document.history.currentIdentity == movedBefore.identity)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A047 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:128
Disposition: MATCHED
Mapping/reason: S121 executes `("A047", moved.document.note(movedNotes[0].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A048 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:129
Disposition: MATCHED
Mapping/reason: S122 executes `("A048", moved.document.note(movedNotes[1].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A049 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:130
Disposition: MATCHED
Mapping/reason: S123 executes `("A049", moved.document.note(movedNotes[2].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLoudVelocity))

A050 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:131
Disposition: MATCHED
Mapping/reason: S124 executes `("A050", priorityTimelineVelocity(moved, movedNotes[0].id) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A051 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:132
Disposition: MATCHED
Mapping/reason: S125 executes `("A051", priorityTimelineVelocity(moved, movedNotes[1].id) == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A052 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:133
Disposition: MATCHED
Mapping/reason: S126 executes `("A052", priorityTimelineVelocity(moved, movedNotes[2].id) == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(kLoudVelocity))

A053 | VelocityEditingTest::movedNodeDoesNotClickThrough | src/checks/velocity/velocityhitpriority.cpp:134
Disposition: MATCHED
Mapping/reason: S127 executes `("A053", moved.session.selectedNoteOrder == [movedNotes[2].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_later.noteId}))

A054 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:140
Disposition: MATCHED
Mapping/reason: S128 executes `("A054", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::rightPressPreservesSelectedGroup()
{
    m_tab->view().selectionModel().setNoteSelection({m_quietStacked.noteId, m_later.noteId});

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A055 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:146
Disposition: NATIVE
Mapping/reason: Asserts that the native QQuickWindow acquired a mouse grab after a real QTest right-button press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();

    focusVelocityBand();
    mousePress(Qt::RightButton, windowPoint(nodePoint(kQuietVelocity, 12)));

Original expression:
  QTRY_VERIFY(m_quickWindow->mouseGrabberItem())

A056 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:147
Disposition: MATCHED
Mapping/reason: S129 executes `("A056", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A057 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:150
Disposition: MATCHED
Mapping/reason: S130 executes `("A057", right.document.revision == rightBefore.revision)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseRelease(Qt::RightButton, windowPoint(nodePoint(kQuietVelocity, 12)));


Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore)

A058 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:151
Disposition: MATCHED
Mapping/reason: S131 executes `("A058", right.document.history.currentIdentity == rightBefore.identity)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore)

A059 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:152
Disposition: MATCHED
Mapping/reason: S132 executes `("A059", right.document.note(rightNotes[0].id)?.velocity == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A060 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:153
Disposition: MATCHED
Mapping/reason: S133 executes `("A060", right.document.note(rightNotes[1].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A061 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:154
Disposition: MATCHED
Mapping/reason: S134 executes `("A061", right.document.note(rightNotes[2].id)?.velocity == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(kLoudVelocity))

A062 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:155
Disposition: MATCHED
Mapping/reason: S135 executes `("A062", priorityTimelineVelocity(right, rightNotes[0].id) == 20)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(kQuietVelocity))

A063 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:156
Disposition: MATCHED
Mapping/reason: S136 executes `("A063", priorityTimelineVelocity(right, rightNotes[1].id) == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(kLoudVelocity))

A064 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:157
Disposition: MATCHED
Mapping/reason: S137 executes `("A064", priorityTimelineVelocity(right, rightNotes[2].id) == 70)`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(kLoudVelocity))

A065 | VelocityEditingTest::rightPressPreservesSelectedGroup | src/checks/velocity/velocityhitpriority.cpp:158
Disposition: MATCHED
Mapping/reason: S138 executes `("A065", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id])`; it drives the production VelocityPage seam with the original preconditions and event sequence, then asserts the same observable state. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

Remaining original source (closing sequence and helper bodies):
;
}

Swift executable assertion index
The S entries are exact predicates already called by the suite. Their presence alone
does not close any A entry. Preserve their surrounding setup and order in the paired source.

S001 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:14
  report.fail(drawerVelocityTransactionID, "the synthetic fixture published fewer than three notes")

S002 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:26
  report.expectEqual(baseline.revision + 1, committed.revision, cppID: drawerVelocityTransactionID,
                     what: "one released drag advances the document revision once")

S003 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:28
  report.expect(committed.identity != baseline.identity, cppID: drawerVelocityTransactionID,
                message: "one released drag makes exactly one history entry")

S004 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:30
  report.expect(committed.canUndo && !baseline.canUndo, cppID: drawerVelocityTransactionID,
                message: "the drag's entry becomes undoable")

S005 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:32
  report.expect(document.note(notes[0].id)?.velocity != notes[0].velocity, cppID: drawerVelocityTransactionID,
                message: "the drag's commit reached the document")

S006 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:34
  report.expect(document.note(notes[1].id)?.velocity != notes[1].velocity, cppID: drawerVelocityTransactionID,
                message: "every selected target moved with the gesture")

S007 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:36
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityTransactionID,
                message: "a released gesture clears its preview")

S008 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:38
  report.expect(fixture.handle(notes[0])?.y != before, cppID: drawerVelocityProjectionID,
                message: "the published handle follows the committed value (before \(before) "
                    + "after \(fixture.handle(notes[0])?.y ?? -1) velocity "
                    + "\(document.note(notes[0].id)?.velocity ?? 0) level "
                    + "\(fixture.handle(notes[0])?.level ?? -99)")

S009 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:45
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores the captured velocity")

S010 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:47
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores every target of the transaction")

S011 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:50
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityHistoryID,
                     what: "Undo rebuilds the page without losing its handles")

S012 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:53
  report.expectEqual(committed.identity, drawerVelocityDocumentSnapshot(document).identity, cppID: drawerVelocityHistoryID,
                     what: "Redo restores the committed history identity")

S013 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:61
  report.expectEqual(pointerBaseline.revision, drawerVelocityDocumentSnapshot(document).revision,
                     cppID: drawerVelocityTransactionID, what: "a stationary click records no history")

S014 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:63
  report.expect(fixture.session.selectedNotes == [notes[0].id], cppID: drawerVelocityTransactionID,
                message: "a stationary click selects only its own note")

S015 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:74
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "a live drag previews before release")

S016 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:76
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                    message: "a live drag reports an active interaction")

S017 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:78
  report.expect(page.handleEscape(), cppID: drawerVelocityCancellationID,
                    message: "Escape is claimed while a gesture is live")

S018 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:81
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityCancellationID,
                message: "Escape clears the frozen preview")

S019 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:83
  report.expect(drawerVelocityDocumentSnapshot(document) == cancelBaseline, cppID: drawerVelocityCancellationID,
                message: "Escape writes nothing at all")

S020 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:85
  report.expect(fixture.session.selectedNoteOrder == [notes[2].id, notes[0].id], cppID: drawerVelocityCancellationID,
                message: "Escape restores selection membership and insertion order")

S021 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:96
  report.expectEqual(foreignRevision + 1, document.revision, cppID: drawerVelocityCancellationID,
                         what: "a release after a foreign change writes nothing")

S022 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:98
  report.expect(!page.hasGesture && page.frozenPreview.isEmpty, cppID: drawerVelocityCancellationID,
                    message: "a stale release ends the gesture")

S023 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:109
  report.expect(page.pointerPress(x: 10, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "the ruler consumes its own press")

S024 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:112
  report.expectEqual(rulerBaseline.revision + 1, document.revision, cppID: drawerVelocityTransactionID,
                     what: "one ruler click makes one revision")

S025 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:114
  report.expect(document.history.currentIdentity != rulerBaseline.identity, cppID: drawerVelocityTransactionID,
                message: "one ruler click makes one history entry")

S026 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:116
  report.expect(!page.pointerPress(x: 200, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "a press inside the plot is not the ruler's")

S027 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:130
  report.expect(fixture.session.camera.snapshot.scrollX > panScroll, cppID: drawerVelocityTransactionID,
                message: "a middle drag pans the shared camera")

S028 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:132
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                message: "a live pan suspends follow through the page's own interaction")

S029 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:135
  report.expect(!page.hasGesture, cppID: drawerVelocityTransactionID, message: "the pan ends on release")

S030 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:136
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "panning writes no document state")

S031 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:143
  report.expect(page.detentsAvailable && page.detentsEnabled, cppID: drawerVelocityTransactionID,
                message: "a PSG context offers the detent control, enabled by default")

S032 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:145
  report.expectEqual(VelocityAxisModel.Mode.intrinsic.rawValue, page.axisMode,
                     cppID: drawerVelocityTransactionID,
                     what: "the PSG context presents the intrinsic ruler")

S033 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:148
  report.expect(page.axisGraduationsVisible, cppID: drawerVelocityTransactionID,
                message: "an enabled detent set draws the level graduations")

S034 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:153
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "the detent case starts from a live preview")

S035 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:156
  report.expect(!page.detentsEnabled && page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "toggling detents cancels the live interaction")

S036 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:159
  report.expect(page.axisGraduationsVisible == false, cppID: drawerVelocityTransactionID,
                message: "disabled detents put the ruler on the continuous rendering")

S037 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:161
  report.expect(page.axisModel.mode == .intrinsic, cppID: drawerVelocityTransactionID,
                message: "the voice's own map stays intrinsic under the ruler switch")

S038 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:165
  report.expect(abs(handle.y - exactY) < 0.001, cppID: drawerVelocityTransactionID,
                    message: "a disabled detent set places nodes at their exact velocity")

S039 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:168
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "the detent toggle writes no document state")

S040 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:181
  report.expectEqual(bandBaseline.revision, document.revision, cppID: drawerVelocityTransactionID,
                     what: "a band selection writes nothing")

S041 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:183
  report.expectEqual(3, fixture.session.selectedNotes.count, cppID: drawerVelocityTransactionID,
                     what: "the band selected every note it covered")

S042 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:14
  report.fail(drawerVelocityGestureID, "the synthetic fixture published fewer than three notes")

S043 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:18
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityProjectionID,
                     what: "every note of the primary track publishes one handle")

S044 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:23
  report.fail(drawerVelocityProjectionID, "the fixture's notes have no published handles")

S045 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:27
  report.expect(!firstHandle.selected, cppID: drawerVelocityProjectionID,
                message: "a fresh selection publishes no selected handle")

S046 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:29
  report.expectEqual(openingMap.level(of: Int(notes[0].velocity)) ?? -1, firstHandle.level,
                     cppID: drawerVelocityProjectionID,
                     what: "an intrinsic handle publishes the level of its displayed value")

S047 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:32
  report.expect(abs(firstHandle.x - page.axisModel.geometry.labelWidth - 56) < 1.0
                    || firstHandle.x <= 400, cppID: drawerVelocityProjectionID,
                message: "the handle's x is a plot-local camera projection")

S048 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:35
  report.expect(fixture.handle(notes[0])!.y < firstHandle.hitRadius * 2
                    || firstHandle.y > 0, cppID: drawerVelocityProjectionID,
                message: "the handle publishes a drawn y inside the body")

S049 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:52
  report.expect(!gesture.relativeActivated && gesture.preview.isEmpty, cppID: drawerVelocityGestureID,
                message: "a drag inside the activation distance previews nothing")

S050 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:55
  report.expect(gesture.relativeActivated, cppID: drawerVelocityGestureID,
                message: "leaving the activation distance arms the relative drag")

S051 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:57
  report.expectEqual(30, Int(gesture.preview[NoteID(1)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the low note takes the whole delta")

S052 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:59
  report.expectEqual(127, Int(gesture.preview[NoteID(2)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the high note clamps to the maximum instead of wrapping")

S053 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:61
  report.expectEqual(2, VelocityGesturePolicy.updates(gesture).count, cppID: drawerVelocityGestureID,
                     what: "the commit payload carries one update per frozen note")

S054 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:78
  report.expectEqual(Int(psgMap.representative(9)), Int(psgGesture.preview[NoteID(3)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the first level follows the level delta")

S055 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:80
  report.expectEqual(Int(psgMap.representative(11)), Int(psgGesture.preview[NoteID(4)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the second level keeps its own offset")

S056 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:82
  report.expectEqual(127,
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: true, y: 0)),
                     cppID: drawerVelocityGestureID,
                     what: "the detent unlock takes exact MIDI velocity")

S057 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:87
  report.expectEqual(Int(psgMap.canonicalize(76)),
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: false,
                                                                y: psgAxis.levelToY(9))),
                     cppID: drawerVelocityGestureID,
                     what: "a locked intrinsic drag canonicalizes onto the note's own map")

S058 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:104
  report.expectEqual(2, painted.count, cppID: drawerVelocityGestureID,
                     what: "the sweep covers every node the column reaches")

S059 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:106
  report.expect(painted[0].velocity < painted[1].velocity, cppID: drawerVelocityGestureID,
                message: "the painted ramp follows the swept line's own direction")

S060 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:111
  report.expectEqual(1, partial.count, cppID: drawerVelocityGestureID,
                     what: "a sweep that stops short leaves the far node alone")

S061 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:116
  report.expectEqual(1, singleColumn.count, cppID: drawerVelocityGestureID,
                     what: "a stationary paint only takes the nodes in its own column")

S062 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:128
  report.expectEqual(100, Int(ramp.preview[NoteID(5)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the note at the press position keeps the press velocity")

S063 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:130
  report.expectEqual(75, Int(ramp.preview[NoteID(7)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note inside the ramp span takes the swept line's value")

S064 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:132
  report.expectEqual(40, Int(ramp.preview[NoteID(6)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note outside the ramp span keeps its captured velocity")

S065 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:142
  report.expect(VelocityGesturePolicy.updates(flatState).isEmpty, cppID: drawerVelocityGestureID,
                message: "a preview that matches the captured values commits nothing")

S066 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:148
  report.expect(moved.count == 1 && moved[0].noteID == movedNote.noteID
                && moved[0].velocity == Int(movedNote.velocity) + 7,
                cppID: drawerVelocityGestureID,
                message: "only the values that differ from the captured ones are committed")

S067 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:152
  report.expect(abs(velocityRampValue(at: 20, x0: 0, y0: 0, x1: 40, y1: 40) - 20) < 1e-9,
                cppID: drawerVelocityGestureID, message: "the ramp interpolates linearly")

S068 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:154
  report.expect(abs(velocityRampValue(at: 5, x0: 10, y0: 3, x1: 10, y1: 9) - 9) < 1e-9,
                cppID: drawerVelocityGestureID, message: "a vertical span resolves to the near endpoint")

S069 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:156
  report.expect(thirdHandle.hitRadius > 0, cppID: drawerVelocityGestureID,
                message: "the published hit radius is font-relative and positive")

S070 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:195
  report.expect(!grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity is unavailable with no selection")

S071 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:209
  report.expect(grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity becomes available with a selection")

S072 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:213
  report.expectEqual(1, requested, cppID: drawerVelocityCommandID,
                     what: "the existing command row asks its owner for the prompt")

S073 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:215
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "the command commits no value before prompt acceptance")

S074 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:220
  report.expectEqual(2, requested, cppID: drawerVelocityCommandID,
                     what: "the command routes every execution through the same owner")

S075 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:222
  report.expect(fixture.page.promptOpen, cppID: drawerVelocityCommandID,
                message: "the routed command opened the page's captured prompt")

S076 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:225
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "cancelling the routed prompt still commits nothing")

S077 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:229
  report.expectEqual(EditNotesOperation.setVelocity.rawValue,
                     row?.notesOperation.rawValue ?? -1, cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity on the notes arm")

S078 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:232
  report.expectEqual(EditKeyRoute.alwaysConsume.rawValue, row?.keyRoute.rawValue ?? -1,
                     cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity's consume route")

S079 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:51
Swift expression:
  ("A001", selected.document.note(selectedOverlap) != nil),

S080 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:52
Swift expression:
  ("A002", priorityTimelineVelocity(selected, selectedOverlap) == 20),

S081 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:53
Swift expression:
  ("A003", selected.session.selectedNoteOrder == [selectedOverlap]),

S082 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:58
Swift expression:
  ("A005", selected.page.observation.targetNoteID == selectedOverlap
  && selected.session.selectedNoteOrder == [selectedOverlap]),

S083 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:63
Swift expression:
  ("A006", selected.document.revision == selectedBefore.revision),

S084 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:64
Swift expression:
  ("A007", selected.document.history.currentIdentity == selectedBefore.identity),

S085 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:65
Swift expression:
  ("A008", selected.document.note(selectedOverlap)?.velocity == 20),

S086 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:66
Swift expression:
  ("A009", selected.document.note(selectedNotes[0].id)?.velocity == 20),

S087 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:67
Swift expression:
  ("A010", selected.document.note(selectedNotes[1].id)?.velocity == 70),

S088 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:68
Swift expression:
  ("A011", selected.document.note(selectedNotes[2].id)?.velocity == 70),

S089 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:69
Swift expression:
  ("A012", selected.session.selectedNoteOrder == [selectedOverlap]),

S090 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:87
Swift expression:
  ("A013", unselected.document.note(unselectedOverlap) != nil),

S091 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:88
Swift expression:
  ("A014", priorityTimelineVelocity(unselected, unselectedOverlap) == 20),

S092 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:89
Swift expression:
  ("A015", unselected.session.selectedNoteOrder == [unselectedNotes[0].id]),

S093 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:94
Swift expression:
  ("A017", unselected.page.observation.targetNoteID == unselectedOverlap
  && unselected.session.selectedNoteOrder == [unselectedOverlap]),

S094 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:99
Swift expression:
  ("A018", unselected.document.revision == unselectedBefore.revision),

S095 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:100
Swift expression:
  ("A019", unselected.document.history.currentIdentity == unselectedBefore.identity),

S096 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:101
Swift expression:
  ("A020", unselected.document.note(unselectedOverlap)?.velocity == 20),

S097 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:102
Swift expression:
  ("A021", unselected.document.note(unselectedNotes[0].id)?.velocity == 20),

S098 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:103
Swift expression:
  ("A022", unselected.document.note(unselectedNotes[1].id)?.velocity == 70),

S099 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:104
Swift expression:
  ("A023", unselected.document.note(unselectedNotes[2].id)?.velocity == 70),

S100 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:105
Swift expression:
  ("A024", unselected.session.selectedNoteOrder == [unselectedOverlap]),

S101 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:119
Swift expression:
  ("A025", stem.document.note(stemOverlap) != nil),

S102 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:120
Swift expression:
  ("A026", priorityTimelineVelocity(stem, stemNotes[1].id) == 20),

S103 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:121
Swift expression:
  ("A027", priorityTimelineVelocity(stem, stemOverlap) == 20),

S104 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:122
Swift expression:
  ("A028", stem.session.selectedNoteOrder == [stemNotes[0].id]),

S105 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:126
Swift expression:
  ("A030", stem.page.observation.targetNoteID == stemNotes[0].id
  && stem.session.selectedNoteOrder == [stemNotes[0].id]),

S106 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:131
Swift expression:
  ("A031", stem.document.revision == stemBefore.revision),

S107 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:132
Swift expression:
  ("A032", stem.document.history.currentIdentity == stemBefore.identity),

S108 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:133
Swift expression:
  ("A033", stem.document.note(stemOverlap)?.velocity == 20),

S109 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:134
Swift expression:
  ("A034", stem.document.note(stemNotes[0].id)?.velocity == 20),

S110 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:135
Swift expression:
  ("A035", stem.document.note(stemNotes[1].id)?.velocity == 20),

S111 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:136
Swift expression:
  ("A036", stem.document.note(stemNotes[2].id)?.velocity == 70),

S112 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:137
Swift expression:
  ("A037", stem.session.selectedNoteOrder == [stemNotes[0].id]),

S113 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:149
Swift expression:
  ("A038", moved.session.selectedNotes.isEmpty)])
  _ = moved.page.pointerPress(x: laterHandle.x, y: laterHandle.y, surface: 1,

S114 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:153
Swift expression:
  ("A040", moved.page.observation.targetNoteID == movedNotes[2].id
  && moved.session.selectedNoteOrder == [movedNotes[2].id]),

S115 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:158
Swift expression:
  ("A041", moved.page.frozenPreview[movedNotes[2].id] != nil),

S116 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:159
Swift expression:
  ("A042", moved.page.frozenPreview[movedNotes[2].id] != nil),

S117 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:160
Swift expression:
  ("A043", moved.page.frozenPreview[movedNotes[2].id] == 70),

S118 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:164
Swift expression:
  ("A044", moved.page.frozenPreview[movedNotes[2].id] == nil),

S119 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:165
Swift expression:
  ("A045", moved.document.revision == movedBefore.revision),

S120 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:166
Swift expression:
  ("A046", moved.document.history.currentIdentity == movedBefore.identity),

S121 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:167
Swift expression:
  ("A047", moved.document.note(movedNotes[0].id)?.velocity == 20),

S122 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:168
Swift expression:
  ("A048", moved.document.note(movedNotes[1].id)?.velocity == 70),

S123 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:169
Swift expression:
  ("A049", moved.document.note(movedNotes[2].id)?.velocity == 70),

S124 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:170
Swift expression:
  ("A050", priorityTimelineVelocity(moved, movedNotes[0].id) == 20),

S125 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:171
Swift expression:
  ("A051", priorityTimelineVelocity(moved, movedNotes[1].id) == 70),

S126 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:172
Swift expression:
  ("A052", priorityTimelineVelocity(moved, movedNotes[2].id) == 70),

S127 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:173
Swift expression:
  ("A053", moved.session.selectedNoteOrder == [movedNotes[2].id]),

S128 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:186
Swift expression:
  ("A054", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id]),

S129 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:191
Swift expression:
  ("A056", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id]),

S130 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:195
Swift expression:
  ("A057", right.document.revision == rightBefore.revision),

S131 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:196
Swift expression:
  ("A058", right.document.history.currentIdentity == rightBefore.identity),

S132 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:197
Swift expression:
  ("A059", right.document.note(rightNotes[0].id)?.velocity == 20),

S133 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:198
Swift expression:
  ("A060", right.document.note(rightNotes[1].id)?.velocity == 70),

S134 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:199
Swift expression:
  ("A061", right.document.note(rightNotes[2].id)?.velocity == 70),

S135 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:200
Swift expression:
  ("A062", priorityTimelineVelocity(right, rightNotes[0].id) == 20),

S136 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:201
Swift expression:
  ("A063", priorityTimelineVelocity(right, rightNotes[1].id) == 70),

S137 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:202
Swift expression:
  ("A064", priorityTimelineVelocity(right, rightNotes[2].id) == 70),

S138 | drawerVelocityHitPriorityGapPredicates | src/checks/velocity/velocitypriority_gap.swift:203
Swift expression:
  ("A065", right.session.selectedNoteOrder == [rightNotes[0].id, rightNotes[2].id]),
