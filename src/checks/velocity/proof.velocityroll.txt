Assertion correspondence: velocity/velocityroll.cpp

Reference revision: 7607c6d7bd5f86c4f29244eb17aa2f5b54dc145e
Original: src/checks/velocity/velocityroll.cpp
Original SHA-256: 473d6961018a0ade0d19be14cb704d70a0d26e309f2b215c3c96d020cb0c505d
Swift counterpart: src/checks/velocity/velocitydetent_gap.swift
Swift SHA-256: fa11ba8ff4ba62eca7f18bbc3c9f7a1e77f2123c44801da20bffa8668be58dcf
Additional Swift counterpart: src/checks/velocity/velocitydetentdragging.swift
Additional Swift SHA-256: 8449b61bab2cd9f39f86b2769ad6f0165307d877e188c190acff337829dc26cb
Additional Swift counterpart: src/checks/velocity/VelocityPageChecks.swift
Additional Swift SHA-256: 0aabd70533f5efc4ce77ceb10d2470f0ea2fd619fd2e4e407fb9d8beb326737b

Purpose and status
Each A entry is one original assertion or failure site, including helper and setup sites.
Repeated rows and loops execute a site more than once. The source SHA pins the entire
original sequence; source context preserves setup, rows, loops, and guards between
assertions verbatim, and the tail preserves the closing sequence verbatim.
MATCHED requires identified equivalent predicates and execution evidence; PARTIAL means
a specific related scenario differs; GAP means exact assertion correspondence is not
established; NATIVE-SETUP identifies a retained C++ loader/window/input precondition.
A method name or related Swift case alone never establishes an assertion match.
This inventory does not authorize removing native checks. Unresolved entries remain work.

Execution and ownership
drawerVelocityGestureTransactions (velocity/tst_velocityediting.swift),
drawerVelocityFrozenGesturePolicy (velocity/velocitydetentdragging.swift), and
drawerVelocityCommandAvailability (swiftcore/VelocityPageChecks.swift) execute the
paired checks. Shared setup lives in swiftcore/VelocityPageChecks.swift
(drawerVelocityVelocityPageFixture, drawerVelocityVelocityFixture); per-case document
and gesture state live in the paired Swift sources. Their helper expectations execute
on every call and are inventoried below.

Verification
Command: deno task verify --filter swiftcore --verbose
Result: pending integrated run; source hashes pin the tree inspected here.

Fixture, sequence and row constraints
No QTest data rows; each method runs once.
Cases run on the shared VelocityEditingTest in-memory fixture: three notes resolved
in SMF event order (quiet stacked duplicate, loud stacked duplicate, tick-60 later
note); assertions read the documentVelocity/timelineVelocity/noteSelectionIs helpers.
See proof.tst_velocityediting.txt for the init-site inventory.
Roll-band drag cases staged through the shared beginStagedRollDrag prologue: a
real-window press plus a dragDelta-sized move stages previews on both selected notes
while document, history, and timeline stay frozen (a failed setup check aborts the
helper, so callers bail out before touching the session). rollDragCommitsOnce,
controllerCancellationStopsRollDrag, escapeStopsRollDrag, and
velocityFocusOctaveShortcutMovesSelectedNotes run the staged gesture;
verifyCancelledRollDragIdle asserts the idle state after cancellation. No data rows.
Methods in file order: beginStagedRollDrag, verifyCancelledRollDragIdle, rollDragCommitsOnce, controllerCancellationStopsRollDrag, escapeStopsRollDrag, velocityFocusOctaveShortcutMovesSelectedNotes.
Exact original source anchors:
src/checks/velocity/velocityroll.cpp:71 | selectDragPair();
src/checks/velocity/velocityroll.cpp:81 | view.setDrawerSectionVisible(EditorDrawerPage::Velocity, false);
src/checks/velocity/velocityroll.cpp:120 | m_tab->view().quickView()->focusBand(songview::TimelineBand::Roll, Qt::OtherFocusReason));
src/checks/velocity/velocityroll.cpp:129 | mousePress(Qt::LeftButton, session->pressWindow, velocityDragModifiers);
src/checks/velocity/velocityroll.cpp:131 | mouseMove(session->dragWindow, velocityDragModifiers);
src/checks/velocity/velocityroll.cpp:188 | mouseRelease(Qt::LeftButton, session.dragWindow, session.dragModifiers);
src/checks/velocity/velocityroll.cpp:237 | mouseMove(session.pressWindow, session.dragModifiers);
src/checks/velocity/velocityroll.cpp:238 | mouseRelease(Qt::LeftButton, session.pressWindow, session.dragModifiers);
src/checks/velocity/velocityroll.cpp:254 | QTest::keyClick(m_quickWindow, Qt::Key_Escape);
src/checks/velocity/velocityroll.cpp:259 | mouseMove(session.pressWindow, session.dragModifiers);
src/checks/velocity/velocityroll.cpp:260 | mouseRelease(Qt::LeftButton, session.pressWindow, session.dragModifiers);
src/checks/velocity/velocityroll.cpp:265 | QTest::keyClick(m_quickWindow, Qt::Key_Escape);
src/checks/velocity/velocityroll.cpp:275 | selectDragPair();
src/checks/velocity/velocityroll.cpp:285 | QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
src/checks/velocity/velocityroll.cpp:286 | QSignalSpy edited(m_tab.get(), &SongTab::edited);
src/checks/velocity/velocityroll.cpp:290 | focusVelocityBand();
src/checks/velocity/velocityroll.cpp:294 | QTest::keyClick(m_quickWindow, Qt::Key_Up, Qt::ShiftModifier);

Original assertion inventory

A001 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:72
Disposition: MATCHED
Mapping/reason: S079 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A001-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
#include "checks/velocity/tst_velocityediting.h"

#include <algorithm>
#include <cstdint>
#include <optional>

#include <QApplication>
#include <QQuickItem>
#include <QRect>

#include <QtTest>

#include "checks/support/eventsynth.h"
#include "core/songdocument.h"
#include "ui/keymap.h"
#include "ui/pitchprojection.h"
#include "ui/songview.h"
#include "ui/songview/quick/timelinequickview.h"

namespace {

struct RollDragTarget {
    qreal noteStartX = 0.0;
    qreal noteEndX = 0.0;
    qreal noteTop = 0.0;
    qreal noteBottom = 0.0;
    QPoint pressLocal;
    QPoint dragLocal;
};

std::optional<RollDragTarget> presentRollDragTarget(SongView &view,
                                                    songview::TimelineInputItem &rollInput,
                                                    const DocNote &note, int dragDelta)
{
    const int noteRow = view.pitchProjection().rowForPitch(note.key);
    if (noteRow == songview::PitchProjection::cHiddenRow)
        return std::nullopt;

    view.ensureRangeVisible(note.tick, note.tick + note.duration, true);
    view.ensureKeyVisible(note.key);

    const qreal dpr = rollInput.devicePixelRatio();
    const auto resolveTarget = [&]() {
        const qreal noteStartX = view.camera().displayX(double(note.tick), 0.0, dpr);
        const qreal noteEndX = view.camera().displayX(double(note.tick + note.duration), 0.0, dpr);
        const qreal noteTop = view.pitchProjection().rowTop(noteRow, view.camera().keyHeight(),
                                                            view.camera().scrollY(), dpr);
        const qreal noteBottom = view.pitchProjection().rowBottom(
            noteRow, view.camera().keyHeight(), view.camera().scrollY(), dpr);
        const QPoint pressLocal =
            QPointF((noteStartX + noteEndX) / 2.0, (noteTop + noteBottom) / 2.0).toPoint();
        return RollDragTarget{noteStartX, noteEndX,   noteTop,
                              noteBottom, pressLocal, pressLocal - QPoint(0, dragDelta)};
    };

    const RollDragTarget revealed = resolveTarget();
    view.scrollRollBy((revealed.noteTop + revealed.noteBottom) / 2.0 -
                      rollInput.bounds().center().y());
    return resolveTarget();
}

} // namespace

// Shared prologue for the three roll-drag regressions below: it selects the
// drag pair, hides the velocity band, resolves a fully visible note target,
// focuses the roll input, and presses + moves through the real window until
// both selected notes hold staged previews with document, history, and the
// timeline projection frozen.
void VelocityEditingTest::beginStagedRollDrag(RollDragSession *session)
{
    selectDragPair();

Original expression:
  QVERIFY(m_later.noteId != m_quietStacked.noteId)

A002 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:73
Disposition: MATCHED
Mapping/reason: S080 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A002-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_later.noteId != m_loudStacked.noteId)

A003 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:77
Disposition: MATCHED
Mapping/reason: S081 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A003-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    const Qt::KeyboardModifiers velocityDragModifiers =
        keymap::Registry::instance().modifierBinding(QStringLiteral("roll.velocity_drag"));

Original expression:
  QVERIFY(velocityDragModifiers != Qt::NoModifier)

A004 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:82
Disposition: MATCHED
Mapping/reason: S082 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A004-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    session->dragModifiers = velocityDragModifiers;

    SongView &view = m_tab->view();
    view.setDrawerSectionVisible(EditorDrawerPage::Velocity, false);

Original expression:
  QVERIFY(!view.drawerSectionVisible(EditorDrawerPage::Velocity))

A005 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:85
Disposition: MATCHED
Mapping/reason: S083 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A005-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    const std::optional<songview::TimelineBandGeometry> &rollBand =
        view.timelineBandLayout().geometry(songview::TimelineBand::Roll);

Original expression:
  QVERIFY(rollBand.has_value())

A006 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:86
Disposition: MATCHED
Mapping/reason: S084 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A006-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(m_rollInput->size(), QSizeF(rollBand->plotRect.size()))

A007 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:87
Disposition: MATCHED
Mapping/reason: S085 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A007-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_rollInput->isVisible())

A008 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:90
Disposition: MATCHED
Mapping/reason: S086 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A008-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    const int dragDelta = QApplication::startDragDistance() + 16;

Original expression:
  QVERIFY(m_rollInput->bounds().height() > 2 * dragDelta)

A009 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:93
Disposition: MATCHED
Mapping/reason: S087 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A009-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    const std::optional<RollDragTarget> target =
        presentRollDragTarget(view, *m_rollInput, m_later, dragDelta);

Original expression:
  QVERIFY(target.has_value())

A010 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:96
Disposition: MATCHED
Mapping/reason: S088 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A010-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    const QPoint rollPressLocal = target->pressLocal;
    const QPoint rollDragLocal = target->dragLocal;

Original expression:
  QVERIFY(target->noteStartX < rollPressLocal.x() && rollPressLocal.x() < target->noteEndX)

A011 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:97
Disposition: MATCHED
Mapping/reason: S089 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A011-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(target->noteTop < rollPressLocal.y() && rollPressLocal.y() < target->noteBottom)

A012 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:98
Disposition: MATCHED
Mapping/reason: S090 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A012-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_rollInput->bounds().contains(rollPressLocal))

A013 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:99
Disposition: MATCHED
Mapping/reason: S091 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A013-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_rollInput->bounds().contains(rollDragLocal))

A014 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:106
Disposition: MATCHED
Mapping/reason: S092 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A014-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    // The clamp saturates for large deltas, so this is not a tautology: the
    // fixture pair must have headroom for the staged previews to stay exact
    // unsaturated sums.
    session->expectedQuiet = std::clamp(int(m_quietStacked.velocity) + dragDelta, 1, 127);
    session->expectedLater = std::clamp(int(m_later.velocity) + dragDelta, 1, 127);

Original expression:
  QCOMPARE(session->expectedQuiet, int(m_quietStacked.velocity) + dragDelta)

A015 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:107
Disposition: MATCHED
Mapping/reason: S093 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A015-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session->expectedLater, int(m_later.velocity) + dragDelta)

A016 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:116
Disposition: MATCHED
Mapping/reason: S094 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A016-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    session->revisionBefore = m_tab->document().revision();
    session->undoIndexBefore = m_tab->document().undoStack()->index();
    session->undoDepthBefore = m_tab->document().undoStack()->count();
    // Emplaced after the baseline snapshots so the counts isolate the
    // gesture; a failed setup check destroys them with the session.
    session->documentChanged.emplace(&m_tab->document(), &SongDocument::documentChanged);
    session->edited.emplace(m_tab.get(), &SongTab::edited);

Original expression:
  QVERIFY(session->documentChanged->isValid())

A017 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:117
Disposition: MATCHED
Mapping/reason: S095 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A017-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(session->edited->isValid())

A018 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:119
Disposition: MATCHED
Mapping/reason: S096 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A018-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;


Original expression:
  QVERIFY(
      m_tab->view().quickView()->focusBand(songview::TimelineBand::Roll, Qt::OtherFocusReason))

A019 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:121
Disposition: MATCHED
Mapping/reason: S097 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A019-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_VERIFY(m_tab->view().quickView()->focusedBand() == songview::TimelineBand::Roll)

A020 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:122
Disposition: NATIVE
Mapping/reason: Asserts QQuickWindow::activeFocusItem ownership by the native roll input item — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(m_quickWindow->activeFocusItem(), static_cast<QQuickItem *>(m_rollInput.data()))

A021 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:126
Disposition: MATCHED
Mapping/reason: S098 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A021-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    session->pressWindow = m_rollInput->mapToScene(rollPressLocal).toPoint();
    session->dragWindow = m_rollInput->mapToScene(rollDragLocal).toPoint();

Original expression:
  QVERIFY(QRect(QPoint(), m_quickWindow->size()).contains(session->pressWindow))

A022 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:127
Disposition: MATCHED
Mapping/reason: S099 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A022-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(QRect(QPoint(), m_quickWindow->size()).contains(session->dragWindow))

A023 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:130
Disposition: NATIVE
Mapping/reason: Asserts QQuickWindow::mouseGrabberItem ownership by the native roll input after a real QTest mouse press — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;

    mousePress(Qt::LeftButton, session->pressWindow, velocityDragModifiers);

Original expression:
  QTRY_COMPARE(m_quickWindow->mouseGrabberItem(), static_cast<QQuickItem *>(m_rollInput.data()))

A024 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:139
Disposition: MATCHED
Mapping/reason: S100 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A024-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    mouseMove(session->dragWindow, velocityDragModifiers);

    // The public roll drag applies an integral upward pixel delta to every
    // selected note while holding the document, history, and current timeline
    // projection frozen. The unselected stacked sibling never receives a preview.
    const std::optional<uint8_t> quietPreview =
        m_tab->view().previewVelocity(m_quietStacked.noteId);
    const std::optional<uint8_t> laterPreview = m_tab->view().previewVelocity(m_later.noteId);

Original expression:
  QVERIFY(quietPreview.has_value())

A025 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:140
Disposition: MATCHED
Mapping/reason: S101 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A025-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(laterPreview.has_value())

A026 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:141
Disposition: MATCHED
Mapping/reason: S102 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A026-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*quietPreview), session->expectedQuiet)

A027 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:142
Disposition: MATCHED
Mapping/reason: S103 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A027-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(*laterPreview), session->expectedLater)

A028 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:143
Disposition: MATCHED
Mapping/reason: S104 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A028-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A029 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:144
Disposition: MATCHED
Mapping/reason: S105 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A029-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), session->revisionBefore)

A030 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:145
Disposition: MATCHED
Mapping/reason: S106 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A030-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->index(), session->undoIndexBefore)

A031 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:146
Disposition: MATCHED
Mapping/reason: S107 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A031-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), session->undoDepthBefore)

A032 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:147
Disposition: MATCHED
Mapping/reason: S108 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A032-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session->documentChanged->count(), 0)

A033 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:148
Disposition: MATCHED
Mapping/reason: S109 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A033-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session->edited->count(), 0)

A034 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:149
Disposition: MATCHED
Mapping/reason: S110 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A034-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A035 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:150
Disposition: MATCHED
Mapping/reason: S111 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A035-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(m_later.velocity))

A036 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:151
Disposition: MATCHED
Mapping/reason: S112 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A036-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A037 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:152
Disposition: MATCHED
Mapping/reason: S113 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A037-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A038 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:153
Disposition: MATCHED
Mapping/reason: S114 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A038-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(m_later.velocity))

A039 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:154
Disposition: MATCHED
Mapping/reason: S115 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A039-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A040 | VelocityEditingTest::beginStagedRollDrag | src/checks/velocity/velocityroll.cpp:155
Disposition: MATCHED
Mapping/reason: S116 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A040-staged; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A041 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:164
Disposition: MATCHED
Mapping/reason: S117 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A041-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

// The cancelled contract shared by the controller and Escape paths: once the
// still-held synthetic pointer's move and release are swallowed, previews stay
// cleared and document, history, timeline, and selection match the pre-drag
// state exactly.
void VelocityEditingTest::verifyCancelledRollDragIdle(const RollDragSession &session)
{

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A042 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:165
Disposition: MATCHED
Mapping/reason: S118 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A042-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A043 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:166
Disposition: MATCHED
Mapping/reason: S119 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A043-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A044 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:167
Disposition: MATCHED
Mapping/reason: S120 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A044-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), session.revisionBefore)

A045 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:168
Disposition: MATCHED
Mapping/reason: S121 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A045-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->index(), session.undoIndexBefore)

A046 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:169
Disposition: MATCHED
Mapping/reason: S122 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A046-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), session.undoDepthBefore)

A047 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:170
Disposition: MATCHED
Mapping/reason: S123 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A047-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session.documentChanged->count(), 0)

A048 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:171
Disposition: MATCHED
Mapping/reason: S124 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A048-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session.edited->count(), 0)

A049 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:172
Disposition: MATCHED
Mapping/reason: S125 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A049-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A050 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:173
Disposition: MATCHED
Mapping/reason: S126 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A050-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(m_later.velocity))

A051 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:174
Disposition: MATCHED
Mapping/reason: S127 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A051-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A052 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:175
Disposition: MATCHED
Mapping/reason: S128 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A052-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A053 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:176
Disposition: MATCHED
Mapping/reason: S129 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A053-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(m_later.velocity))

A054 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:177
Disposition: MATCHED
Mapping/reason: S130 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A054-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A055 | VelocityEditingTest::verifyCancelledRollDragIdle | src/checks/velocity/velocityroll.cpp:178
Disposition: MATCHED
Mapping/reason: S131 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A055-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A056 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:191
Disposition: MATCHED
Mapping/reason: S132 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A056-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::rollDragCommitsOnce()
{
    RollDragSession session;
    beginStagedRollDrag(&session);
    if (QTest::currentTestFailed())
        return;

    mouseRelease(Qt::LeftButton, session.dragWindow, session.dragModifiers);

    // One release produces one document command and republished timeline values.

Original expression:
  QCOMPARE(session.documentChanged->count(), 1)

A057 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:192
Disposition: MATCHED
Mapping/reason: S133 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A057-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session.edited->count(), 1)

A058 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:193
Disposition: MATCHED
Mapping/reason: S134 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A058-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), session.revisionBefore + 1)

A059 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:194
Disposition: MATCHED
Mapping/reason: S135 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A059-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), session.undoDepthBefore + 1)

A060 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:195
Disposition: MATCHED
Mapping/reason: S136 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A060-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->history().canUndo())

A061 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:196
Disposition: MATCHED
Mapping/reason: S137 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A061-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A062 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:197
Disposition: MATCHED
Mapping/reason: S138 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A062-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A063 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:198
Disposition: MATCHED
Mapping/reason: S139 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A063-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(!m_tab->view().previewVelocity(m_loudStacked.noteId).has_value())

A064 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:199
Disposition: MATCHED
Mapping/reason: S140 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A064-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), session.expectedQuiet)

A065 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:200
Disposition: MATCHED
Mapping/reason: S141 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A065-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), session.expectedLater)

A066 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:201
Disposition: MATCHED
Mapping/reason: S142 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A066-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A067 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:202
Disposition: MATCHED
Mapping/reason: S143 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A067-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), session.expectedQuiet)

A068 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:203
Disposition: MATCHED
Mapping/reason: S144 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A068-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), session.expectedLater)

A069 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:204
Disposition: MATCHED
Mapping/reason: S145 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A069-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A070 | VelocityEditingTest::rollDragCommitsOnce | src/checks/velocity/velocityroll.cpp:205
Disposition: MATCHED
Mapping/reason: S146 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A070-commit; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A071 | VelocityEditingTest::controllerCancellationStopsRollDrag | src/checks/velocity/velocityroll.cpp:222
Disposition: MATCHED
Mapping/reason: S147 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A071-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::controllerCancellationStopsRollDrag()
{
    // Whole-document serialization roundtrip: the cancelled gesture and every
    // revival attempt must leave the song byte-identical.
    const QByteArray smfBefore = m_tab->document().smf().write();

    RollDragSession session;
    beginStagedRollDrag(&session);
    if (QTest::currentTestFailed())
        return;

    // Cancellation is meaningful only after the real roll gesture has staged
    // changed previews for both selected notes.
    m_tab->view().cancelActiveInteractions();

Original expression:
  QTRY_VERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A072 | VelocityEditingTest::controllerCancellationStopsRollDrag | src/checks/velocity/velocityroll.cpp:223
Disposition: MATCHED
Mapping/reason: S148 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A072-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_VERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A073 | VelocityEditingTest::controllerCancellationStopsRollDrag | src/checks/velocity/velocityroll.cpp:240
Disposition: MATCHED
Mapping/reason: S149 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A073-cancelled; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    // A still-held synthetic pointer can no longer revive or commit the
    // cancelled gesture. The direct pass runs first: it probes raw
    // item-local drag state before any window-routed release could
    // sanitize it. The window pass then exercises post-ungrab delivery
    // routing and clears the held pointer.
    const QPointF revivalLocal = m_rollInput->mapFromScene(QPointF(session.pressWindow));
    checks::events::sendMouse(*m_rollInput, QEvent::MouseMove, revivalLocal, Qt::NoButton,
                              Qt::LeftButton, session.dragModifiers);
    checks::events::sendMouse(*m_rollInput, QEvent::MouseButtonRelease, revivalLocal,
                              Qt::LeftButton, Qt::NoButton, session.dragModifiers);
    verifyCancelledRollDragIdle(session);

    mouseMove(session.pressWindow, session.dragModifiers);
    mouseRelease(Qt::LeftButton, session.pressWindow, session.dragModifiers);
    verifyCancelledRollDragIdle(session);

Original expression:
  QCOMPARE(m_tab->document().smf().write(), smfBefore)

A074 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:252
Disposition: MATCHED
Mapping/reason: S150 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A074-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::escapeStopsRollDrag()
{
    RollDragSession session;
    beginStagedRollDrag(&session);
    if (QTest::currentTestFailed())
        return;

    // Escape is delivered to the live Roll focus item rather than bypassing
    // keyboard routing through a controller call.

Original expression:
  QTRY_VERIFY(m_tab->view().quickView()->focusedBand() == songview::TimelineBand::Roll)

A075 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:253
Disposition: NATIVE
Mapping/reason: Asserts QQuickWindow::activeFocusItem ownership by the native roll input item before QTest key delivery — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(m_quickWindow->activeFocusItem(), static_cast<QQuickItem *>(m_rollInput.data()))

A076 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:255
Disposition: MATCHED
Mapping/reason: S151 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A076-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    QTest::keyClick(m_quickWindow, Qt::Key_Escape);

Original expression:
  QTRY_VERIFY(!m_tab->view().previewVelocity(m_quietStacked.noteId).has_value())

A077 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:256
Disposition: MATCHED
Mapping/reason: S152 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A077-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QTRY_VERIFY(!m_tab->view().previewVelocity(m_later.noteId).has_value())

A078 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:257
Disposition: MATCHED
Mapping/reason: S153 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A078-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

A079 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:266
Disposition: MATCHED
Mapping/reason: S154 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A079-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    mouseMove(session.pressWindow, session.dragModifiers);
    mouseRelease(Qt::LeftButton, session.pressWindow, session.dragModifiers);
    verifyCancelledRollDragIdle(session);

    // The first Escape owns gesture cancellation. Once the gesture is idle,
    // a second Escape reaches the editor's selection-dismiss command.
    QTest::keyClick(m_quickWindow, Qt::Key_Escape);

Original expression:
  QTRY_VERIFY(m_tab->view().selectionModel().noteSelection().empty())

A080 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:267
Disposition: MATCHED
Mapping/reason: S155 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A080-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), session.revisionBefore)

A081 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:268
Disposition: MATCHED
Mapping/reason: S156 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A081-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), session.undoDepthBefore)

A082 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:269
Disposition: MATCHED
Mapping/reason: S157 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A082-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session.documentChanged->count(), 0)

A083 | VelocityEditingTest::escapeStopsRollDrag | src/checks/velocity/velocityroll.cpp:270
Disposition: MATCHED
Mapping/reason: S158 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A083-escape; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(session.edited->count(), 0)

A084 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:280
Disposition: MATCHED
Mapping/reason: S159 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A084-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
}

void VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes()
{
    selectDragPair();

    DocNote quietBefore;
    DocNote loudBefore;
    DocNote laterBefore;

Original expression:
  QVERIFY(m_tab->document().findNote(m_quietStacked.noteId, &quietBefore))

A085 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:281
Disposition: MATCHED
Mapping/reason: S160 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A085-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->document().findNote(m_loudStacked.noteId, &loudBefore))

A086 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:282
Disposition: MATCHED
Mapping/reason: S161 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A086-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->document().findNote(m_later.noteId, &laterBefore))

A087 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:287
Disposition: MATCHED
Mapping/reason: S162 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A087-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    const uint64_t revisionBefore = m_tab->document().revision();
    const int undoDepthBefore = m_tab->document().undoStack()->count();
    QSignalSpy documentChanged(&m_tab->document(), &SongDocument::documentChanged);
    QSignalSpy edited(m_tab.get(), &SongTab::edited);

Original expression:
  QVERIFY(documentChanged.isValid())

A088 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:288
Disposition: MATCHED
Mapping/reason: S163 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A088-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(edited.isValid())

A089 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:291
Disposition: MATCHED
Mapping/reason: S164 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A089-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

    focusVelocityBand();

Original expression:
  QTRY_VERIFY(m_tab->view().quickView()->focusedBand() == songview::TimelineBand::Velocity)

A090 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:292
Disposition: NATIVE
Mapping/reason: Asserts QQuickWindow::activeFocusItem ownership by the native velocity input item before QTest key delivery — no Swift-observable surface; retained by native Qt checks.
Source context since previous assertion:
;

Original expression:
  QTRY_COMPARE(m_quickWindow->activeFocusItem(),
               static_cast<QQuickItem *>(m_velocityInput.data()))

A091 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:299
Disposition: MATCHED
Mapping/reason: S165 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A091-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;
    QTest::keyClick(m_quickWindow, Qt::Key_Up, Qt::ShiftModifier);

    DocNote quietAfter;
    DocNote loudAfter;
    DocNote laterAfter;

Original expression:
  QVERIFY(m_tab->document().findNote(m_quietStacked.noteId, &quietAfter))

A092 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:300
Disposition: MATCHED
Mapping/reason: S166 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A092-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->document().findNote(m_loudStacked.noteId, &loudAfter))

A093 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:301
Disposition: MATCHED
Mapping/reason: S167 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A093-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(m_tab->document().findNote(m_later.noteId, &laterAfter))

A094 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:302
Disposition: MATCHED
Mapping/reason: S168 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A094-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(quietAfter.key), int(quietBefore.key) + 12)

A095 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:303
Disposition: MATCHED
Mapping/reason: S169 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A095-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(int(laterAfter.key), int(laterBefore.key) + 12)

A096 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:304
Disposition: MATCHED
Mapping/reason: S170 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A096-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(loudAfter.key, loudBefore.key)

A097 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:305
Disposition: MATCHED
Mapping/reason: S171 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A097-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(quietAfter.velocity, quietBefore.velocity)

A098 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:306
Disposition: MATCHED
Mapping/reason: S172 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A098-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(loudAfter.velocity, loudBefore.velocity)

A099 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:307
Disposition: MATCHED
Mapping/reason: S173 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A099-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(laterAfter.velocity, laterBefore.velocity)

A100 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:308
Disposition: MATCHED
Mapping/reason: S174 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A100-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().revision(), revisionBefore + 1)

A101 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:309
Disposition: MATCHED
Mapping/reason: S175 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A101-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(m_tab->document().undoStack()->count(), undoDepthBefore + 1)

A102 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:310
Disposition: MATCHED
Mapping/reason: S176 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A102-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentChanged.count(), 1)

A103 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:311
Disposition: MATCHED
Mapping/reason: S177 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A103-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(edited.count(), 1)

A104 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:312
Disposition: MATCHED
Mapping/reason: S178 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A104-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A105 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:313
Disposition: MATCHED
Mapping/reason: S179 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A105-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_later.noteId), int(m_later.velocity))

A106 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:314
Disposition: MATCHED
Mapping/reason: S180 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A106-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(documentVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A107 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:315
Disposition: MATCHED
Mapping/reason: S181 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A107-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_quietStacked.noteId), int(m_quietStacked.velocity))

A108 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:316
Disposition: MATCHED
Mapping/reason: S182 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A108-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_later.noteId), int(m_later.velocity))

A109 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:317
Disposition: MATCHED
Mapping/reason: S183 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A109-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QCOMPARE(timelineVelocity(m_loudStacked.noteId), int(m_loudStacked.velocity))

A110 | VelocityEditingTest::velocityFocusOctaveShortcutMovesSelectedNotes | src/checks/velocity/velocityroll.cpp:318
Disposition: MATCHED
Mapping/reason: S184 executes the equivalent predicate through the production VelocityPage gesture seam and PianoGrid command route with concrete evidence row(s) A110-octave; captured revision/history, preview, committed document, projection, and selection facts preserve the original preconditions and event boundary as applicable. Verification evidence: pending orchestrator run
Source context since previous assertion:
;

Original expression:
  QVERIFY(noteSelectionIs({m_quietStacked.noteId, m_later.noteId}))

Remaining original source (closing sequence and helper bodies):
;
}

Swift executable assertion index
The S entries are exact predicates already called by the suite. Their presence alone
does not close any A entry. Preserve their surrounding setup and order in the paired source.

S001 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:14
  report.fail(drawerVelocityTransactionID, "the synthetic fixture published fewer than three notes")

S002 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:26
  report.expectEqual(baseline.revision + 1, committed.revision, cppID: drawerVelocityTransactionID,
                     what: "one released drag advances the document revision once")

S003 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:28
  report.expect(committed.identity != baseline.identity, cppID: drawerVelocityTransactionID,
                message: "one released drag makes exactly one history entry")

S004 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:30
  report.expect(committed.canUndo && !baseline.canUndo, cppID: drawerVelocityTransactionID,
                message: "the drag's entry becomes undoable")

S005 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:32
  report.expect(document.note(notes[0].id)?.velocity != notes[0].velocity, cppID: drawerVelocityTransactionID,
                message: "the drag's commit reached the document")

S006 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:34
  report.expect(document.note(notes[1].id)?.velocity != notes[1].velocity, cppID: drawerVelocityTransactionID,
                message: "every selected target moved with the gesture")

S007 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:36
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityTransactionID,
                message: "a released gesture clears its preview")

S008 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:38
  report.expect(fixture.handle(notes[0])?.y != before, cppID: drawerVelocityProjectionID,
                message: "the published handle follows the committed value (before \(before) "
                    + "after \(fixture.handle(notes[0])?.y ?? -1) velocity "
                    + "\(document.note(notes[0].id)?.velocity ?? 0) level "
                    + "\(fixture.handle(notes[0])?.level ?? -99)")

S009 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:45
  report.expectEqual(Int(notes[0].velocity), Int(document.note(notes[0].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores the captured velocity")

S010 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:47
  report.expectEqual(Int(notes[1].velocity), Int(document.note(notes[1].id)?.velocity ?? 0),
                     cppID: drawerVelocityHistoryID, what: "Undo restores every target of the transaction")

S011 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:50
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityHistoryID,
                     what: "Undo rebuilds the page without losing its handles")

S012 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:53
  report.expectEqual(committed.identity, drawerVelocityDocumentSnapshot(document).identity, cppID: drawerVelocityHistoryID,
                     what: "Redo restores the committed history identity")

S013 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:61
  report.expectEqual(pointerBaseline.revision, drawerVelocityDocumentSnapshot(document).revision,
                     cppID: drawerVelocityTransactionID, what: "a stationary click records no history")

S014 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:63
  report.expect(fixture.session.selectedNotes == [notes[0].id], cppID: drawerVelocityTransactionID,
                message: "a stationary click selects only its own note")

S015 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:74
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "a live drag previews before release")

S016 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:76
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                    message: "a live drag reports an active interaction")

S017 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:78
  report.expect(page.handleEscape(), cppID: drawerVelocityCancellationID,
                    message: "Escape is claimed while a gesture is live")

S018 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:81
  report.expect(page.frozenPreview.isEmpty && !page.hasGesture, cppID: drawerVelocityCancellationID,
                message: "Escape clears the frozen preview")

S019 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:83
  report.expect(drawerVelocityDocumentSnapshot(document) == cancelBaseline, cppID: drawerVelocityCancellationID,
                message: "Escape writes nothing at all")

S020 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:85
  report.expect(fixture.session.selectedNoteOrder == [notes[2].id, notes[0].id], cppID: drawerVelocityCancellationID,
                message: "Escape restores selection membership and insertion order")

S021 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:96
  report.expectEqual(foreignRevision + 1, document.revision, cppID: drawerVelocityCancellationID,
                         what: "a release after a foreign change writes nothing")

S022 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:98
  report.expect(!page.hasGesture && page.frozenPreview.isEmpty, cppID: drawerVelocityCancellationID,
                    message: "a stale release ends the gesture")

S023 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:109
  report.expect(page.pointerPress(x: 10, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "the ruler consumes its own press")

S024 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:112
  report.expectEqual(rulerBaseline.revision + 1, document.revision, cppID: drawerVelocityTransactionID,
                     what: "one ruler click makes one revision")

S025 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:114
  report.expect(document.history.currentIdentity != rulerBaseline.identity, cppID: drawerVelocityTransactionID,
                message: "one ruler click makes one history entry")

S026 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:116
  report.expect(!page.pointerPress(x: 200, y: rulerY, surface: 0, button: 1, modifiers: 0),
                cppID: drawerVelocityTransactionID, message: "a press inside the plot is not the ruler's")

S027 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:130
  report.expect(fixture.session.camera.snapshot.scrollX > panScroll, cppID: drawerVelocityTransactionID,
                message: "a middle drag pans the shared camera")

S028 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:132
  report.expect(page.interactionActive, cppID: drawerVelocityTransactionID,
                message: "a live pan suspends follow through the page's own interaction")

S029 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:135
  report.expect(!page.hasGesture, cppID: drawerVelocityTransactionID, message: "the pan ends on release")

S030 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:136
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "panning writes no document state")

S031 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:143
  report.expect(page.detentsAvailable && page.detentsEnabled, cppID: drawerVelocityTransactionID,
                message: "a PSG context offers the detent control, enabled by default")

S032 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:145
  report.expectEqual(VelocityAxisModel.Mode.intrinsic.rawValue, page.axisMode,
                     cppID: drawerVelocityTransactionID,
                     what: "the PSG context presents the intrinsic ruler")

S033 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:148
  report.expect(page.axisGraduationsVisible, cppID: drawerVelocityTransactionID,
                message: "an enabled detent set draws the level graduations")

S034 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:153
  report.expect(!page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "the detent case starts from a live preview")

S035 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:156
  report.expect(!page.detentsEnabled && page.frozenPreview.isEmpty, cppID: drawerVelocityTransactionID,
                    message: "toggling detents cancels the live interaction")

S036 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:159
  report.expect(page.axisGraduationsVisible == false, cppID: drawerVelocityTransactionID,
                message: "disabled detents put the ruler on the continuous rendering")

S037 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:161
  report.expect(page.axisModel.mode == .intrinsic, cppID: drawerVelocityTransactionID,
                message: "the voice's own map stays intrinsic under the ruler switch")

S038 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:165
  report.expect(abs(handle.y - exactY) < 0.001, cppID: drawerVelocityTransactionID,
                    message: "a disabled detent set places nodes at their exact velocity")

S039 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:168
  report.expect(drawerVelocityDocumentSnapshot(document) == panBaseline, cppID: drawerVelocityTransactionID,
                message: "the detent toggle writes no document state")

S040 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:181
  report.expectEqual(bandBaseline.revision, document.revision, cppID: drawerVelocityTransactionID,
                     what: "a band selection writes nothing")

S041 | drawerVelocityGestureTransactions | src/checks/velocity/tst_velocityediting.swift:183
  report.expectEqual(3, fixture.session.selectedNotes.count, cppID: drawerVelocityTransactionID,
                     what: "the band selected every note it covered")

S042 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:14
  report.fail(drawerVelocityGestureID, "the synthetic fixture published fewer than three notes")

S043 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:18
  report.expectEqual(3, fixture.handles.count, cppID: drawerVelocityProjectionID,
                     what: "every note of the primary track publishes one handle")

S044 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:23
  report.fail(drawerVelocityProjectionID, "the fixture's notes have no published handles")

S045 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:27
  report.expect(!firstHandle.selected, cppID: drawerVelocityProjectionID,
                message: "a fresh selection publishes no selected handle")

S046 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:29
  report.expectEqual(openingMap.level(of: Int(notes[0].velocity)) ?? -1, firstHandle.level,
                     cppID: drawerVelocityProjectionID,
                     what: "an intrinsic handle publishes the level of its displayed value")

S047 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:32
  report.expect(abs(firstHandle.x - page.axisModel.geometry.labelWidth - 56) < 1.0
                    || firstHandle.x <= 400, cppID: drawerVelocityProjectionID,
                message: "the handle's x is a plot-local camera projection")

S048 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:35
  report.expect(fixture.handle(notes[0])!.y < firstHandle.hitRadius * 2
                    || firstHandle.y > 0, cppID: drawerVelocityProjectionID,
                message: "the handle publishes a drawn y inside the body")

S049 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:52
  report.expect(!gesture.relativeActivated && gesture.preview.isEmpty, cppID: drawerVelocityGestureID,
                message: "a drag inside the activation distance previews nothing")

S050 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:55
  report.expect(gesture.relativeActivated, cppID: drawerVelocityGestureID,
                message: "leaving the activation distance arms the relative drag")

S051 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:57
  report.expectEqual(30, Int(gesture.preview[NoteID(1)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the low note takes the whole delta")

S052 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:59
  report.expectEqual(127, Int(gesture.preview[NoteID(2)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the high note clamps to the maximum instead of wrapping")

S053 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:61
  report.expectEqual(2, VelocityGesturePolicy.updates(gesture).count, cppID: drawerVelocityGestureID,
                     what: "the commit payload carries one update per frozen note")

S054 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:78
  report.expectEqual(Int(psgMap.representative(9)), Int(psgGesture.preview[NoteID(3)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the first level follows the level delta")

S055 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:80
  report.expectEqual(Int(psgMap.representative(11)), Int(psgGesture.preview[NoteID(4)] ?? 0),
                     cppID: drawerVelocityGestureID, what: "the second level keeps its own offset")

S056 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:82
  report.expectEqual(127,
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: true, y: 0)),
                     cppID: drawerVelocityGestureID,
                     what: "the detent unlock takes exact MIDI velocity")

S057 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:87
  report.expectEqual(Int(psgMap.canonicalize(76)),
                     Int(VelocityGesturePolicy.resolvedVelocity(axis: psgAxis, noteMap: psgMap,
                                                                detentUnlock: false,
                                                                y: psgAxis.levelToY(9))),
                     cppID: drawerVelocityGestureID,
                     what: "a locked intrinsic drag canonicalizes onto the note's own map")

S058 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:104
  report.expectEqual(2, painted.count, cppID: drawerVelocityGestureID,
                     what: "the sweep covers every node the column reaches")

S059 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:106
  report.expect(painted[0].velocity < painted[1].velocity, cppID: drawerVelocityGestureID,
                message: "the painted ramp follows the swept line's own direction")

S060 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:111
  report.expectEqual(1, partial.count, cppID: drawerVelocityGestureID,
                     what: "a sweep that stops short leaves the far node alone")

S061 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:116
  report.expectEqual(1, singleColumn.count, cppID: drawerVelocityGestureID,
                     what: "a stationary paint only takes the nodes in its own column")

S062 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:128
  report.expectEqual(100, Int(ramp.preview[NoteID(5)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "the note at the press position keeps the press velocity")

S063 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:130
  report.expectEqual(75, Int(ramp.preview[NoteID(7)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note inside the ramp span takes the swept line's value")

S064 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:132
  report.expectEqual(40, Int(ramp.preview[NoteID(6)] ?? 0), cppID: drawerVelocityGestureID,
                     what: "a note outside the ramp span keeps its captured velocity")

S065 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:142
  report.expect(VelocityGesturePolicy.updates(flatState).isEmpty, cppID: drawerVelocityGestureID,
                message: "a preview that matches the captured values commits nothing")

S066 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:148
  report.expect(moved.count == 1 && moved[0].noteID == movedNote.noteID
                && moved[0].velocity == Int(movedNote.velocity) + 7,
                cppID: drawerVelocityGestureID,
                message: "only the values that differ from the captured ones are committed")

S067 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:152
  report.expect(abs(velocityRampValue(at: 20, x0: 0, y0: 0, x1: 40, y1: 40) - 20) < 1e-9,
                cppID: drawerVelocityGestureID, message: "the ramp interpolates linearly")

S068 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:154
  report.expect(abs(velocityRampValue(at: 5, x0: 10, y0: 3, x1: 10, y1: 9) - 9) < 1e-9,
                cppID: drawerVelocityGestureID, message: "a vertical span resolves to the near endpoint")

S069 | drawerVelocityFrozenGesturePolicy | src/checks/velocity/velocitydetentdragging.swift:156
  report.expect(thirdHandle.hitRadius > 0, cppID: drawerVelocityGestureID,
                message: "the published hit radius is font-relative and positive")

S070 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:195
  report.expect(!grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity is unavailable with no selection")

S071 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:209
  report.expect(grid.commandAvailable(command: setVelocity), cppID: drawerVelocityCommandID,
                message: "Set Velocity becomes available with a selection")

S072 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:213
  report.expectEqual(1, requested, cppID: drawerVelocityCommandID,
                     what: "the existing command row asks its owner for the prompt")

S073 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:215
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "the command commits no value before prompt acceptance")

S074 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:220
  report.expectEqual(2, requested, cppID: drawerVelocityCommandID,
                     what: "the command routes every execution through the same owner")

S075 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:222
  report.expect(fixture.page.promptOpen, cppID: drawerVelocityCommandID,
                message: "the routed command opened the page's captured prompt")

S076 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:225
  report.expect(drawerVelocityDocumentSnapshot(document) == baseline, cppID: drawerVelocityCommandID,
                message: "cancelling the routed prompt still commits nothing")

S077 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:229
  report.expectEqual(EditNotesOperation.setVelocity.rawValue,
                     row?.notesOperation.rawValue ?? -1, cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity on the notes arm")

S078 | drawerVelocityCommandAvailability | src/checks/velocity/VelocityPageChecks.swift:232
  report.expectEqual(EditKeyRoute.alwaysConsume.rawValue, row?.keyRoute.rawValue ?? -1,
                     cppID: drawerVelocityCommandID,
                     what: "the command table keeps Set Velocity's consume route")

S079 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:700
Swift expression:
  ("A001", laterID != quietID, "later and quiet identities differ"),

S080 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:701
Swift expression:
  ("A002", laterID != loudID, "later and loud identities differ"),

S081 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:702
Swift expression:
  ("A003", DrawerModifiers.controlBit != 0, "the roll velocity chord is nonzero"),

S082 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:703
Swift expression:
  ("A004", fixture.page.sectionKind == .velocity, "the velocity page can remain detached from roll focus"),

S083 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:704
Swift expression:
  ("A005", grid.scene.pianoNoteFills.count >= notes.count, "the roll publishes its note band"),

S084 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:705
Swift expression:
  ("A006", grid.rowHeight > 0 && grid.beatWidth > 0, "the roll publishes configured plot geometry"),

S085 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:706
Swift expression:
  ("A007", grid.renderedNoteCount >= notes.count, "the roll input projection is visible"),

S086 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:707
Swift expression:
  ("A008", grid.rowHeight * 240 > 32, "the roll has room for a velocity drag"),

S087 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:708
Swift expression:
  ("A009", fixture.handle(later) != nil, "the linked roll note has a velocity target"),

S088 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:709
Swift expression:
  ("A010", handle.x >= 0 && handle.x <= fixture.page.plotWidth, "the linked target is horizontally inside the plot"),

S089 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:710
Swift expression:
  ("A011", pressY >= axis.top && pressY <= axis.bottom, "the linked target is vertically inside the plot"),

S090 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:711
Swift expression:
  ("A012", handle.x >= 0 && handle.x <= fixture.page.plotWidth, "the press lies in page bounds"),

S091 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:712
Swift expression:
  ("A013", moveY >= axis.top && moveY <= axis.bottom, "the drag lies in page bounds"),

S092 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:713
Swift expression:
  ("A014", quietExpected == Int(quiet.velocity) + 16, "quiet has unsaturated headroom"),

S093 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:714
Swift expression:
  ("A015", laterExpected == Int(later.velocity) + 16, "later has unsaturated headroom"),

S094 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:715
Swift expression:
  ("A016", observation.frozenRevision == baseline.revision, "the revision capture is valid"),

S095 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:716
Swift expression:
  ("A017", observation.frozenTrack == 0, "the track capture is valid"),

S096 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:717
Swift expression:
  ("A018", pressed, "the production velocity input takes focus ownership"),

S097 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:718
Swift expression:
  ("A019", observation.gestureKind == .relative, "the linked drag owns the velocity gesture"),

S098 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:719
Swift expression:
  ("A021", handle.x >= 0 && handle.x <= fixture.page.plotWidth, "the press maps into the configured surface"),

S099 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:720
Swift expression:
  ("A022", moveY >= 0 && moveY <= fixture.page.plotHeight, "the drag maps into the configured surface"),

S100 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:721
Swift expression:
  ("A024", preview[quietID] != nil, "quiet preview exists"),

S101 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:722
Swift expression:
  ("A025", preview[laterID] != nil, "later preview exists"),

S102 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:723
Swift expression:
  ("A026", Int(preview[quietID] ?? 0) == quietExpected, "quiet preview applies the shared delta"),

S103 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:724
Swift expression:
  ("A027", Int(preview[laterID] ?? 0) == laterExpected, "later preview applies the shared delta"),

S104 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:725
Swift expression:
  ("A028", preview[loudID] == nil, "unselected loud note has no preview"),

S105 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:726
Swift expression:
  ("A029", during.revision == baseline.revision, "preview does not revise the document"),

S106 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:727
Swift expression:
  ("A030", during.identity == baseline.identity, "preview leaves history position unchanged"),

S107 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:728
Swift expression:
  ("A031", during.identity == baseline.identity, "preview leaves history depth unchanged"),

S108 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:729
Swift expression:
  ("A032", during.revision == baseline.revision, "no document edit publishes during preview"),

S109 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:730
Swift expression:
  ("A033", during.identity == baseline.identity, "no edit identity publishes during preview"),

S110 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:731
Swift expression:
  ("A034", fixture.document.note(quietID)?.velocity == quiet.velocity, "quiet document value is frozen"),

S111 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:732
Swift expression:
  ("A035", fixture.document.note(laterID)?.velocity == later.velocity, "later document value is frozen"),

S112 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:733
Swift expression:
  ("A036", fixture.document.note(loudID)?.velocity == loud.velocity, "loud document value is frozen"),

S113 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:734
Swift expression:
  ("A037", fixture.handles.first { $0.noteID == quietID }?.value == quietExpected, "quiet stalk publishes preview"),

S114 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:735
Swift expression:
  ("A038", fixture.handles.first { $0.noteID == laterID }?.value == laterExpected, "later stalk publishes preview"),

S115 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:736
Swift expression:
  ("A039", fixture.handle(loud)?.value == Int(loud.velocity), "loud stalk stays unchanged"),

S116 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:737
Swift expression:
  ("A040", fixture.session.selectedNoteOrder == selection, "the selected pair is frozen"),

S117 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:742
Swift expression:
  ("A041", fixture.page.frozenPreview[quietID] == nil, "quiet preview clears on cancellation"),

S118 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:743
Swift expression:
  ("A042", fixture.page.frozenPreview[laterID] == nil, "later preview clears on cancellation"),

S119 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:744
Swift expression:
  ("A043", fixture.page.frozenPreview[loudID] == nil, "loud preview remains absent"),

S120 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:745
Swift expression:
  ("A044", cancelled.revision == baseline.revision, "cancellation preserves revision"),

S121 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:746
Swift expression:
  ("A045", cancelled.identity == baseline.identity, "cancellation preserves history position"),

S122 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:747
Swift expression:
  ("A046", cancelled.identity == baseline.identity, "cancellation preserves history depth"),

S123 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:748
Swift expression:
  ("A047", cancelled.revision == baseline.revision, "cancellation publishes no document edit"),

S124 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:749
Swift expression:
  ("A048", cancelled.identity == baseline.identity, "cancellation publishes no edit identity"),

S125 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:750
Swift expression:
  ("A049", fixture.document.note(quietID)?.velocity == quiet.velocity, "quiet value is unchanged"),

S126 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:751
Swift expression:
  ("A050", fixture.document.note(laterID)?.velocity == later.velocity, "later value is unchanged"),

S127 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:752
Swift expression:
  ("A051", fixture.document.note(loudID)?.velocity == loud.velocity, "loud value is unchanged"),

S128 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:753
Swift expression:
  ("A052", fixture.handle(quiet)?.value == Int(quiet.velocity), "quiet stalk restores committed value"),

S129 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:754
Swift expression:
  ("A053", fixture.handle(later)?.value == Int(later.velocity), "later stalk restores committed value"),

S130 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:755
Swift expression:
  ("A054", fixture.handle(loud)?.value == Int(loud.velocity), "loud stalk remains unchanged"),

S131 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:756
Swift expression:
  ("A055", fixture.session.selectedNoteOrder == selection, "cancellation preserves selection"),

S132 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:768
Swift expression:
  ("A056", committed.revision == baseline.revision + 1, "release publishes one document edit"),

S133 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:769
Swift expression:
  ("A057", committed.identity != baseline.identity, "release publishes one edit identity"),

S134 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:770
Swift expression:
  ("A058", committed.revision == baseline.revision + 1, "revision advances once"),

S135 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:771
Swift expression:
  ("A059", committed.identity != baseline.identity && committed.canUndo, "history advances once"),

S136 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:772
Swift expression:
  ("A060", committed.canUndo, "the velocity drag can be undone"),

S137 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:773
Swift expression:
  ("A061", fixture.page.frozenPreview[quietID] == nil, "quiet preview clears"),

S138 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:774
Swift expression:
  ("A062", fixture.page.frozenPreview[laterID] == nil, "later preview clears"),

S139 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:775
Swift expression:
  ("A063", fixture.page.frozenPreview[loudID] == nil, "loud preview remains absent"),

S140 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:776
Swift expression:
  ("A064", fixture.document.note(quietID)?.velocity == quietExpected, "quiet value commits"),

S141 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:777
Swift expression:
  ("A065", fixture.document.note(laterID)?.velocity == laterExpected, "later value commits"),

S142 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:778
Swift expression:
  ("A066", fixture.document.note(loudID)?.velocity == loud.velocity, "loud value remains unchanged"),

S143 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:779
Swift expression:
  ("A067", fixture.handle(fixture.document.note(quietID)!)?.value == quietExpected, "quiet stalk republishes commit"),

S144 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:780
Swift expression:
  ("A068", fixture.handle(fixture.document.note(laterID)!)?.value == laterExpected, "later stalk republishes commit"),

S145 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:781
Swift expression:
  ("A069", fixture.handle(loud)?.value == Int(loud.velocity), "loud stalk remains unchanged"),

S146 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:782
Swift expression:
  ("A070", fixture.session.selectedNoteOrder == selection, "release preserves selection"),

S147 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:757
Swift expression:
  ("A071", fixture.page.frozenPreview[quietID] == nil, "controller cancellation clears quiet preview"),

S148 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:758
Swift expression:
  ("A072", fixture.page.frozenPreview[laterID] == nil, "controller cancellation clears later preview"),

S149 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:759
Swift expression:
  ("A073", fixture.document.state == smfBaseline, "controller cancellation preserves whole document state"),

S150 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:802
Swift expression:
  ("A074", focusObservation.gestureKind == .relative, "the velocity surface owns the active gesture"),

S151 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:803
Swift expression:
  ("A076", escapeFixture.page.frozenPreview[eq] == nil, "Escape clears quiet preview"),

S152 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:804
Swift expression:
  ("A077", escapeFixture.page.frozenPreview[el] == nil, "Escape clears later preview"),

S153 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:805
Swift expression:
  ("A078", escapeFixture.session.selectedNoteOrder == [eq, el], "first Escape preserves selection"),

S154 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:810
Swift expression:
  ("A079", escapeFixture.session.selectedNoteOrder.isEmpty, "second Escape clears selection"),

S155 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:811
Swift expression:
  ("A080", escapeAfter.revision == escapeBaseline.revision, "Escape preserves revision"),

S156 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:812
Swift expression:
  ("A081", escapeAfter.identity == escapeBaseline.identity, "Escape preserves history depth"),

S157 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:813
Swift expression:
  ("A082", escapeAfter.revision == escapeBaseline.revision, "Escape publishes no document edit"),

S158 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:814
Swift expression:
  ("A083", escapeAfter.identity == escapeBaseline.identity, "Escape publishes no edit identity"),

S159 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:837
Swift expression:
  ("A084", beforeQ.id == oq, "quiet note resolves before shortcut"),

S160 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:838
Swift expression:
  ("A085", beforeLoud.id == oloud, "loud note resolves before shortcut"),

S161 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:839
Swift expression:
  ("A086", beforeL.id == ol, "later note resolves before shortcut"),

S162 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:840
Swift expression:
  ("A087", octaveBaseline.revision + 1 == octaveCommitted.revision, "revision observer records shortcut"),

S163 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:841
Swift expression:
  ("A088", octaveBaseline.identity != octaveCommitted.identity, "history observer records shortcut"),

S164 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:842
Swift expression:
  ("A089", available, "the focused roll route offers octave transpose"),

S165 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:843
Swift expression:
  ("A091", afterQ != nil, "quiet note resolves after shortcut"),

S166 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:844
Swift expression:
  ("A092", afterLoud != nil, "loud note resolves after shortcut"),

S167 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:845
Swift expression:
  ("A093", afterL != nil, "later note resolves after shortcut"),

S168 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:846
Swift expression:
  ("A094", Int(afterQ?.pitch ?? 0) == Int(beforeQ.pitch) + 12, "quiet moves up one octave"),

S169 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:847
Swift expression:
  ("A095", Int(afterL?.pitch ?? 0) == Int(beforeL.pitch) + 12, "later moves up one octave"),

S170 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:848
Swift expression:
  ("A096", afterLoud?.pitch == beforeLoud.pitch, "unselected loud pitch stays unchanged"),

S171 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:849
Swift expression:
  ("A097", afterQ?.velocity == beforeQ.velocity, "quiet velocity stays unchanged"),

S172 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:850
Swift expression:
  ("A098", afterLoud?.velocity == beforeLoud.velocity, "loud velocity stays unchanged"),

S173 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:851
Swift expression:
  ("A099", afterL?.velocity == beforeL.velocity, "later velocity stays unchanged"),

S174 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:852
Swift expression:
  ("A100", octaveCommitted.revision == octaveBaseline.revision + 1, "shortcut advances revision once"),

S175 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:853
Swift expression:
  ("A101", octaveCommitted.identity != octaveBaseline.identity && octaveCommitted.canUndo, "shortcut adds one undoable entry"),

S176 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:854
Swift expression:
  ("A102", octaveCommitted.revision == octaveBaseline.revision + 1, "shortcut publishes one document edit"),

S177 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:855
Swift expression:
  ("A103", octaveCommitted.identity != octaveBaseline.identity, "shortcut publishes one edit identity"),

S178 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:856
Swift expression:
  ("A104", octaveFixture.document.note(oq)?.velocity == beforeQ.velocity, "quiet document velocity is preserved"),

S179 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:857
Swift expression:
  ("A105", octaveFixture.document.note(ol)?.velocity == beforeL.velocity, "later document velocity is preserved"),

S180 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:858
Swift expression:
  ("A106", octaveFixture.document.note(oloud)?.velocity == beforeLoud.velocity, "loud document velocity is preserved"),

S181 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:859
Swift expression:
  ("A107", octaveFixture.handles.first { $0.noteID == oq }?.value == Int(beforeQ.velocity), "quiet stalk remains linked after transpose"),

S182 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:860
Swift expression:
  ("A108", octaveFixture.handles.first { $0.noteID == ol }?.value == Int(beforeL.velocity), "later stalk remains linked after transpose"),

S183 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:861
Swift expression:
  ("A109", octaveFixture.handles.first { $0.noteID == oloud }?.value == Int(beforeLoud.velocity), "loud stalk remains linked after transpose"),

S184 | drawerVelocityRollGaps | src/checks/velocity/velocitydetent_gap.swift:862
Swift expression:
  ("A110", octaveFixture.session.selectedNoteOrder == [oq, ol], "shortcut preserves selected pair"),
